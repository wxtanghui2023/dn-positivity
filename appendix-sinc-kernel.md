# 附录：sinc 核数据（早期探索记录——不适用于文档）

> ⚠️ **重要声明**：本附录记录的是早期探索中**自创的 sinc 核** `g_n(t) = sin(t/n)/t` 的全部数值结果。
> **这不是项目文档的真实定义**（文档定义为 θ=π−2arctan(2t)，g_n=[t sin(nθ)+0.5cos(nθ)]/(1/4+t²)）。
> 这些数据**不适用于文档**，仅保留作为探索过程记录与 bug 教训参考。
> 文档真实定义的分析见 `analysis.md`。

---

## 0. 为什么这个附录存在

早期代码片段未提供 g_n 定义，我基于注释假设了 g_n = sin(nθ)/t，后来为找 O(1/n) 衰减自创 sinc 核 g_n(t) = sin(t/n)/t，并沿此做了十几轮实验。**用户澄清文档定义后确认 sinc 核不适用**。以下数据全部基于 sinc 核 + RS theta，与文档无关。

---

## 1. sinc 核：D_n = c/n + O(1/n²)（早期"发现"，不适用于文档）

`sinc 核 g_n(t) = sin(t/n)/t`，100k 零点，γ_max ≈ 74920.8：

| n | Σg_n(γ) | −(1/π)∫θ'g_n | D_n | n·D_n |
|---|---|---|---|---|
| 10 | +0.0596 | +0.0404 | +0.100047 | 1.0005 |
| 20 | +0.1888 | −0.1388 | +0.050013 | 1.0003 |
| 50 | +0.3927 | −0.3727 | +0.020001 | 1.0001 |
| 100 | +0.5562 | −0.5461 | +0.010008 | 1.0008 |
| 200 | +0.7281 | −0.7231 | +0.004995 | 0.9989 |
| 500 | +0.9459 | −0.9439 | +0.001994 | 0.9968 |
| 1000 | +1.1064 | −1.1054 | +0.000996 | 0.9963 |
| 2000 | +1.2584 | −1.2579 | +0.000498 | 0.9962 |
| 3000 | +1.3395 | −1.3392 | +0.000332 | 0.9962 |

## 2. A+B+C 分解（sinc 核专用，不适用于文档）

n·D_n = A(n) + B(n) + C(n)，γ₁ ≈ 14.1347 处拆分：

| 项 | 定义 | 极限 |
|---|---|---|
| A | −(1/π)∫₀^{γ₁}θ'(t)sinc(t/n)dt | → −θ(γ₁)/π ≈ +0.5503 |
| B | −sinc(γ₁/n) | → −1 |
| C | ∫_{γ₁}^∞ sinc(t/n)dS(t) | → ≈ +1.4453 |

和 = 0.5503 − 1 + 1.4453 ≈ 0.9956。A、B 误差 O(1/n²) 解析可证；C 项收敛快（n=100 达 ±0.001）。

## 3. C 项收敛（sinc 核）

C(20)=1.3396, C(50)=1.4318, C(100)=1.4462, C(500)=1.4457, C(1000)=1.4454, C(2000)=1.4453, C(3000)=1.4452。

## 4. 尾部抵消证据（sinc 核）

求和尾部 Σ_{γ>n} sinc(γ/n) 与积分尾部 (1/π)∫_n^∞θ'sinc 各自增长到 ~1700（n=3000），差值恒 O(1)（±0.5）——显式公式 N=θ/π+1+S 的精确抵消。

## 5. M(T) = ∫₀^T S(u)du（独立于核选择，修正版）

| T | M(T)（Gauss-16 修正）| max\|M\| |
|---|---|---|
| 236 | −0.84 | ~1.22 |
| 1419 | −1.22 | 1.22 |
| 9878 | −0.87 | 1.22 |
| 40434 | −0.87 | 1.22 |
| 74921 | −0.46 | ~1.22 |

⚠️ **修正记录**：早期 Simpson /12 版在首区间 [0, γ₁] 的 ∫θ/π 低估 0.2351（θ 曲率大，5 点 Simpson 不足），导致全部 M(T) 值系统性偏差 +0.235。已用 Gauss-16/mpmath 参考修正。**结论不变：M(T) = O(1) 有界（max|M| ≈ 1.2）**。

（另：曾误报 ~T²，系 Simpson 系数 /24 写错；修正后实测有界）

## 6. mean(S(γ_k)) = 0.5000（独立于核选择，仍有价值）

| 统计量 | 值 |
|---|---|
| mean(S(γ_k)) k≤100000 | +0.5000 |
| median | +0.5001 |
| std | 0.31 |
| S>0 比例 | 94.45% |

机制：θ(γ_k)/π ≈ k − 3/2（零点偏 Gram 区间前部），S(γ_k) = k − θ(γ_k)/π − 1 ≈ 1/2。这是统计结构（依赖零点分布），非纯解析恒等式。

## 7. 截断修正（sinc 核）

n ≥ 15000 时 n·D_n 偏离 1（0.86→1.54）是截断假象（γ_max/n < 5）。尾部修正 ≈ −sinc(γ_max/n)·S(γ_max) 后 n·D_n ≈ 1.0±0.05 对 n ∈ [100, 60000]。

## 8. 早期失败分析（其他核）

### 8.1 文档 θ + 原核 g_n = sin(nθ)/t
- 积分部分 = 2(−1)ⁿ（非 0——早期用错误 g_n 形式的结果，文档真实 g_n 下 ≡ 0）
- D_n ≈ −2 + (1/2π)log n + 振荡 → n·D_n 线性发散 ❌

### 8.2 RS θ + 原核 g_n = sin(nθ)/t
- 积分部分 ≈ −1.3430 常数 = θ'(0)/2（t=0 端点 Dirichlet 奇点）
- Σg_n(γ) 在 [−0.38, +0.13] 伪随机振荡
- D_n ≈ −1.34 + 振荡 = O(1) ❌

### 8.3 小 n 的 "O(1/n) 幻觉"
n ≤ 20 时 D_n 恰好落在 ~C/n 曲线上 → 误判 O(1/n)，扩展零点后打破。

---

## 9. 相关脚本（sinc 核专用，仅供追溯）

dn_full.py（鲁棒傅里叶积分）、dn_optc.py（sinc vs sinc²）、dn_100k.py（100k 验证）、dn_decomp.py（A+B+C）、dn_mverify.py（M(T)）、dn_sawfix.py（8点Gauss）、dn_tailcancel.py（尾部抵消）、dn_cdecay2.py（C 衰减）、dn_mfix2.py（M(T) 修正）、dn_envelope.py（包络）、dn_check10.py（n≥10 判据）、dn_split.py、dn_tail2.py、dn_identity.py、dn_poskernel.py、dn_vdc.py、dn_bignd.py、dn_tailfix.py（截断修正）
