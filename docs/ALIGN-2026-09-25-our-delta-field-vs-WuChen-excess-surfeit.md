已查地图：已跑 scripts/prework_map_check.sh K(10,1) excess surfeit Wu-Chen 同余 ⟹ 执行自 EXCESS-2026-09-25 与 PLAN-2026-09-25 两档；本档为**符号对齐**（唐先生 23:27 指定今晚最高优先项）；不开新研究方向。
D0: 本档对象 = 既有 δ-场 与文献 excess/surfeit 的**符号对应**（非新对象）
D1: 0（无新独立自由度；产出为对应表、同一性判定、缺失约束类定位）

> ⚠️ **本档 §2 为审计修正版**（2026-09-25 23:33）：原"surfeit 切掉均匀解"表述**有定义混淆** ✗，已按唐先生 23:29 质疑更正为**三泛函 A/B/C 区分**（−1024 属无切割力的 A；B 在均匀点 = −930.9 仅为**候选**割，有效性待证 ✗）✓

# ALIGN-2026-09-25 · 我方 δ-场 ↔ Wu–Chen 的 excess / surfeit

## §1 逐项对应（全部经 120-码数值验证 ✓）

| 我方 | Wu–Chen | 关系 | 验证 |
|---|---|---|---|
| `m(x)=\|C∩B₁(x)\|` | `\|N[x]\cap D\|` | **同** | ✓ |
| `δ(x)=m(x)−1` | 局部 excess `\|N[v]\cap D\|−1` | **同** | ✓ |
| `δ_i(x)=Σ_{y∈L_i(x)}δ(y)` | `δ_S(D)` 取 `S=L_i(x)` | **同**（层集） | ✓ |
| `Σ_x δ(x)=11\|C\|−1024` | `δ_{V(Q_n)}(D)=(n+1)\|D\|−2^n` | **同一恒等式** | `296=296` ✓ |
| `Σ_i δ_i(x)=δ_V(D)` ∀x | （其框架蕴含） | **层重分配** | ✓ 全 1024 个 x |
| `δ_{N[v]}=δ_0(v)+δ_1(v)` | ball-sum | 同（`N[v]=L_0∪L_1`） | ✓ |
| `Σ_v(δ_0+δ_1−1)=11E−1024` | — | 无限制泛函 ⟹ **无新息** ✗ | `2232` ✓ |
| **`ζ=Σ_{v∉C}(ball(v)−1)`** | **surfeit `ζ_S(D)=Σ_{v∈S∖D}(δ_{N[v]}(D)−1)`** | **同**（取 `S=V`） | `1556` ✓ |

**⟹ 结论 1（修正）**：我此前称作"局部守恒律/新发现"的 `Σ_iδ_i(x)=E`，**并非独立不变量** ✗ ——
它就是 Wu–Chen 的基本 excess 恒等式的**层重分配** ✓（同一全局量，换 index 集切分 ✓）。措辞已改 ✓

## §2 ⚠️ **审计修正**：三个泛函必须分开（原表述有误，已更正）

> **唐先生 23:29 质疑**："surfeit 切掉均匀解(ζ=−1024<0)"一句**暂停** ⟹ 经审计，**原表述确有定义混淆** ✗ ⟹ 本节为更正版 ✓

**三个不同泛函（本档实测）**

| 泛函 | 定义 | 120-码(整数) | 均匀分数点 `z_c=1/11` | 性质 |
|---|---|---:|---:|---|
| **A** 无限制 | `Σ_v(ball(v)−1)` | **2232** `=11E−1024` ✓ | **−1024** | **线性** ⟹ 被恒等式吸收 ⟹ **无切割力** ✗ |
| **B** 受限·两步（= Wu–Chen surfeit 结构） | `Σ_{v∉C}(ball(v)−1)` | **1556** | `−(10/11)·1024 =` **−930.9091** < 0 | **双线性** ⟹ **候选割** ✓（⚠️ 有效性未证 ✗） |
| **C** 受限·一步 | `Σ_{v∉C}(m(v)−1)` | **196** | **0** | 不切 ✗ |

```
其中 ball(v) := Σ_{u∈N[v]} δ(u)  （两步）；m(v) := |N[v]∩C| （一步）
```

**⚠️ 关键反例检验：`B ≥ 0` 并非普遍真命题** ✗

```
完美码（δ≡0 ⟹ ball≡0）⟹ B = −|V∖C| < 0；例 n=7 Hamming(16 码字): B = −(128−16) = −112 < 0
⟹ **不得**把 B ≥ 0 当作有效割平面 ✗ ⟹ 其切割力必须以 **条件形式或同余形式** 出现
⟹ 这正是 Wu–Chen 走 congruence 而非 plain inequality 的原因 ✓
```

**⟹ 结论 2（更正版，可锁死）**

```
① 可锁死: surfeit **含二阶信息**，普通线性 excess 无法表达 ✓
② 可锁死: 它给出进入 **pair-level relaxation** 的明确入口 ✓
③ **不可**锁死: "surfeit 已切掉 z=1/11" ✗ —— 须先确定 −1024/−930.9 各属哪个泛函
   （本档已定: −1024 属 A〔无切割力〕；B 在均匀点确为负 ⟹ 仅**候选**，有效性待证或待引原文 ✗）
④ 因此路线写为: Haas 线性层系统 → excess → 受限 excess/surfeit → **pair-level 约束** ✓
   而**不**写成 "surfeit already cuts 1/11" ✗
```

## §3 代价：surfeit 对 `z` 是**双线性** ⟹ 天然属 **pair-level**

```
ζ = Σ_v (1−z_v)·(ball(v)−1),  ball(v)−1 = Σ_{u∈N[v]}(Σ_{c∈C}[u∈B₁(c)]·z_c − 1)
⟹ 含 (1−z_v)·z_c 乘积 ⟹ **二阶（配对）约束** ⟹ 与 Gijswijt–Polak 2025 的 LP→pair/triple→SDP 阶梯同层 ✓
两条实现路径:  (i) ILP 内线性化（配对变量） / (ii) 同余路线（Habsieger 型 mod p 处理二次型）✓
⚠️ n=10 落在 Wu–Chen(6|n) 与 Haas(n≡−1 mod 3) 两个"好同余情形"之外 ⟹ 同余须**自行推导或新构造** ✗
```

## §4 明日四刀（按唐先生 23:27 排序）

```
① 符号对齐 —— **本档已完成** ✓
② LP 求 OPT(L_Haas) —— 已知 1024/11（解析）；改为求 **LP + surfeit 受限泛函** 的新 OPT ✓
③ 若不足 ⟹ 加 congruence / surfeit cuts（**按结构**加，不随机加）✓
④ 才重启 119 ILP：`z_0=1`（平移破缺，严格 WLOG）＋ S₁₀ 对称破缺；记录 root bound / incumbent / gap ✓
```

## §5 边界（诚实标注）

- 对应关系与数值均在本项目内**独立验证** ✓；Wu–Chen 的 `surfeit` 与同余陈述**引自摘要/条目级**（未逐字精读原文 ✗）
- **不**主张我方层式族是首创；**不**主张 surfeit 能为 n=10 产生新界（仅定位约束类）✓
- `ζ=1556` 为 120-码上的单一数据点 ⟹ **不能**据此断言同余 ✓（只能作为候选）

## 【技术词回查】（定稿前逐字输出）

```
技术词 surfeit          命中文件数=0    :: 
技术词 层重分配     命中文件数=0    :: 
技术词 均匀分数解  命中文件数=2    :: ./X1-K10-5-global-cp-sat-run1-result.md ./PLAN-2026-09-25-K10-1-next-direction-and-difficulty-breakdown.md 
技术词 割平面        命中文件数=1    :: ./C3860-B1alpha3-first-cut-void-no-feasible-initialisation.md 
```

- **本档新增**（回查命中数=0，即**全档未出现过**）：`surfeit`、`层重分配`
- **档案已有（引用，不列为提出）**：`均匀分数解`（早档 `X1-K10-5-global-cp-sat-run1-result.md` ✓）、`割平面`（早档 `C3860-B1alpha3-first-cut-void-no-feasible-initialisation.md` ✓）
- **通用词（不计）**：—
- （说明：`均匀分数解`/`割平面` 虽在本档使用，但**非本档新增** ⟹ 已从"新增"栏移出 ✓）
