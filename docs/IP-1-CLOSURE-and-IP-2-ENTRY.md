已查地图：命中既有条目（`IP-1-G0`／`IP-1-G0b`／`IP-1-C1` 系本线自档）⟹ **引用，不开新案** ✓

# **`IP-1` 收口（三资产 ＋ 失败模板）＋ `IP-2` 入口（二分结构能否产生可证结果）**

**唐先生令（2026-09-23 10:10）**：① ✅ 正式判定 $$\boxed{IP\text{-}1C1 = \mathrm{CLOSED}}$$ 且**是一次有价值的 CLOSED** —— 未发现"第四种独立量"，但把 `extra_zeros` **为什么逃出 rank flag 的机制精确剥离** ✓✓；
　② ✅ 差异源**已完全参数化**：`p_a \propto (x-a)(x+1)(2ax-a-x+1)` ⟹ `r(a)=\frac{a-1}{2a-1}` ⟹ 即
　$$\boxed{\text{同一个 rank flag} + \text{节点参数 } a \longrightarrow \text{第三根的位置}}$$（`r(r(a))=a`、`r'(a)=\frac{1}{(2a-1)^2}>0` ⟹ **刚性无实不动点 Möbius 对合** ✓）；
　③ ✅ 区间律已显式：$$ec=1 \iff a\in[-1,0]\cup[\tfrac23,1]$$（比"实验发现 `ec` 不由 rank 决定"高一层：**有了可证明的根位置判据** ✓）；
　④ ⛔ **但未过"去名字硬门"**：**没有证据超出经典 T-system／Chebyshev alternation 的零点几何**；⛔ **最该避免的误判**：『发现 rank 之外控制 `ec` 的量』$$\not\Longrightarrow$$『发现新数学不变量』✓；
　⑤ ⛔ **不要开 `C2`（再找第四个量）**（极易掉回旧循环）✓；⛔ **不要继续扩大 Chebyshev 实验**（`r=5,6,\dots`）✓；
　⑥ ⭐ **下一刀 `IP-2`**：$$\boxed{\text{寻找一个独立问题，其中"强制消失阶／剩余根位置"二分结构本身产生可证明结果}}$$ **三出口**：**（1）新定理**（非经典结构律）｜**（2）其他猜想资产**（哪怕与 RH 无关，但推进一个具体猜想）｜**（3）立即 CLOSED**（证明只是已有 T-system／alternation／interlacing 的重述）✓；
　⑦ ⛔ ⭐ **硬门**：**不能因为它最终"可能碰到 RH"就算成功** ✓✓

D0: 本档对象 = **`IP-1` 收口 ＋ `IP-2` 入口普查**（引本线自档；**未开 RH 案** ✓）
D1: 0
FREEZE-ACK: D1=0（本档不主张新自由度）；§8.1 冻结债务见 `IP-1-G0` 档 §7 ✓

---

## §1 **`IP-1` 三资产 ＋ 失败模板**（承令 ①②③ ✓✓）

| 资产 | 状态 | 含义 |
|:--|:--|:--|
| **`d_V`** | ✅ **B 强确认**（8208/8208） | **完全由 rank flag 决定**（`d_V=q+\min\{j:\operatorname{rank}(\mathrm{prefix}_j)<j+1\}`）✓ |
| **`extra_zeros`** | ✅ **C 已解释** | **不由 rank 决定**，但**由根位置决定** ✓ |
| **`r(a)`** | ✅ **显式结构资产** | **Möbius 对合给出剩余根的位置** ✓ |

```
【⭐ 失败模板（照录 ✓）】 $$\boxed{\text{rank flag} \longrightarrow \text{forced zeros} \quad+\quad \text{geometric residual roots}}$$ ✓
【⭐ 它的价值（照录）】 这个"失败"**不是"算不出来"**，而是**算出来以后发现其机制属于经典零点几何** ✓✓
【精确陈述（不得越界 ✓）】 只能说 $$\boxed{\text{rank 不决定零点位置；节点几何决定零点位置}}$$ —— **是数学事实，但不是新机制** ✓
```

## §2 **`IP-2` 入口：宿主问题普查（第一轮，三个出口 ✓）**

| 宿主候选 | 二分结构在其中的角色 | 出口判定 |
|:--|:--|:--|
| **`H1`** T-system 零点几何／alternation／interlacing | forced zeros ＝ alternation 集；residual roots ＝ 交替余项 | ⛔ **exit 3**（**重述**）✓ |
| **`H2`** 正交多项式零点的 forced／residual 分解 | 与 `H1` 同源（interlacing 已含） | ⛔ **exit 3** ✓ |
| **`H3`** Chebyshev 求积／插值**误差函数的额外零点** | 残根位置 ＝ 误差函数附加零点 | ⛔ **exit 3** ✓ |
| **`H4`** Möbius／模群（`r` 的系数矩阵 `\begin{pmatrix}1&-1\\2&-1\end{pmatrix}`，`\det=1` ⟹ `r\in\mathrm{SL}_2(\mathbb Z)` 投影）算术动力学 | 残根位置由模群元给出 | ⛔ **exit 3**（**Möbius 映射算术已成熟**：共轭分类）✓ |
| **`H5`** 逆问题：**处方可达性**（给定目标残根 `ρ`，哪些可实现？） | ⚠️ 本族 `r` 为**满射**（Möbius）⟹ **平凡**；一般 `(q,r)` ⟹ 代数多值 ⟹ 归入**可实现性／逆 Galois 型** | ⚠️ **不可开**（属已知难题区域，且**扩样禁令**下不可验证）⟹ 记 **HOLD** ✓ |
| **`H6`** ⭐ **一般 `(q,r)` 二分猜想**（是否由**广义交比／Möbius 型映射**统辖） | — | ⛔ **exit 3 的可能极高**：见 §3 **决定性理由** ✓ |

## §3 ⭐ **决定性理由：二分结构本身近乎"定义式"**（本档核心判断 ✓✓）

```
【判断】 $$\boxed{\text{强制消失阶 ＝ 代数存在性（rank／flag）}；\qquad \text{剩余根位置 ＝ 环境几何}}$$ ⟹
　两者的分离**是该框架固有的"代数—几何"分离**，而**不是**本族新出现的机制 ✓
【两条支撑（本仓已有 ✓）】
　① **零点计数是拓扑不变量的**：本仓 `M0` 档已证 **T-system 性质在递增双射拉回下保持**（偶族即用之）⟹ 故"零点**数目**"由组合／代数决定 ✓
　　而"零点**位置**"必然只能落在**环境几何**里 ⟹ **二分的一半是定义式的** ✓✓
　② **`r(a)` 是射影对象**：交比／Möbius 结构 ＝ `\mathbb P^1` 的**标准射影几何** ⟹ 其"穿越区间边界"判据亦为标准（区间像的位置）✓
【⟹ 结论】 `IP-2` 以"该二分结构"为入口者，**极可能落在 exit 3**（重述）—— 因为**二分本身不是可搬运的新机制**，而**代数—几何分离是任何 T-system 框架自带的** ✓✓
【⛔ 依令】 故**不制造"新定理"**、**不登记新猜想**（一般形式很可能**本就经典**，登记为"猜想"反而误导）✓
```

## §4 **判定**

```
【`IP-2` 第一轮】 ⛔ **主结果 ＝ exit 3**（**立即 CLOSED**）：二分结构属**已有 T-system／alternation／interlacing ＋ 标准射影几何**的重述 ✓✓
【⛔ 无双出口达成】 **无新定理**（exit 1 未达）｜**无新猜想资产**（exit 2 未达，理由见 §3：登记为猜想会误导）✓
【⚠️ 诚实边界】 ① 本判定**限于"以该二分结构为入口"**的一类问题；**不声称**"T-system 理论已被穷尽" ✓；
　② `H5`（逆问题／可达性）**保留为 HOLD**：它**未被本轮排除**，但（i）本族平凡、（ii）一般情形需扩样而**扩样被令禁止** ⟹ **不可开** ✓；
　③ ⛔ **不得**因"可能碰 RH"记为成功（依令 ⑦ ✓）
【⭐ 线状态】 `IP-1` ＝ **收口**（三资产 ＋ 失败模板 ＋ 一条显式初等定律）｜`IP-2` 第一轮 ＝ **exit 3**（CLOSED）｜**数学新量 ＝ 仍未成立** ✓
```

## §5 归档与边界

```
【本档不做】 ⛔ 不扩样｜⛔ 不开 `C2`｜⛔ 不造新定理／新猜想｜⛔ 不以 RH 相关性计成功 ✓
【脚本与数据（已入仓 ✓）】 `scripts/ip1_g0{,_v2,_v3}*.py`／`ip1_c1*.py`；`out/ip1_g0/*.tsv`；`out/out_ip1_*.txt` ✓
【R7】 结论数字驱动（8208／7-7／区间律）✓
✗ 未证 RH／未接 ζ／未开 RH 案 ✓
```
