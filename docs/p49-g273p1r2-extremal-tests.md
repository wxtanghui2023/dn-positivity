# P49-G2.7.3-P1-R2：Odd-Sector Limiting Extremal——三测试结果

> 2026-09-02 14:28 · q₀ 确认 −2.1407 · g₋ 骤降信号 · 过拟合机制支持

## q₀ 精确值确认
- **直接矩阵 M3[0,0] = −2.1407（V₀ 真实 Q_prime——权威）**
- ⚠️ 公式 −2ΣΛ(n)n^{−1/2}(L−logn)/L = −2.449 错误（与直接矩阵不符——公式权重有误——弃用）
- 之前 V₀ RQ = −2.141 正确

## Test A（N 扫描——用 q₀ = −2.1407）
| N | m₋ | g₋ = m₋−q₀ | peak k |
|---|---|---|---|
| 20 | −1.799 | 0.342 | o16,o12,o19 |
| 25 | −1.806 | 0.335 | o16,o12,o14 |
| 30 | −1.826 | 0.315 | o16,o12,o14 |
| 40 | **−2.023** | **0.118** | **o38,o32,o34** |

- **N=20-30 平台（g₋ ~ 0.32-0.34——peak o16 稳定）——但 N=40 骤降（g₋ = 0.12——peak 跳 o38）**
- ⚠️ **骤降 + peak 跳变 = 过拟合信号**（唐先生假设支持——更大 N 提供高频模式——更好匹配 {logn} 采样）

## Test B（K_λ(ω) 极值——λ=3）
- K_λ(ω) = Σa_n·cos(ω·logn)——a_n = Λ(n)n^{−1/2}——K(0) = 4.74（sup）
- [0,200] inf = −3.17（ω=1.73）——高频区 sup 仍 3.69（大正——K 振荡）
- q₀ 对应 ∫K·(V₀ 谱)——V₀ 谱 = sinc² 主瓣（非 δ₀——唐先生修正确认）

## Test C（单频符号匹配）
- 最优单频 K = −3.17（ω=1.735）——**远未达理论最优 −4.74（全 −1 相位）**
- 相位 [0.36, −0.33, −0.74, −0.94, −0.97, −0.89, −0.78]——**n=2 (log2) 相位 +0.36 破坏全负**——单频不足
- ⚠️ 但——多频混合/高 N 过拟合可能补足（N=40 骤降支持）

## ⚠️ 判断（P1 生死）
- **数据趋势支持 Case B/C 方向**（g₋ → 0——odd 逼近 q₀——P1 uniform gap 失败/prime 机制不足）
- **N=40 骤降（0.12）+ peak 跳变 = 过拟合机制的第一个明确信号**——更大 N 提供更多自由度拟合有限 {logn} 采样
- ⚠️ 需确认：N=40 单点骤降是真实趋势还是数值伪影（矩阵 81×81——条件数？）——需 N=50,60 + 高精度
- **若 g₋ → 0 确认——P1 = Case B（m₋^∞ = q₀——No-Go——simple-even 不能由 prime-sector uniform dominance 证明）——CCM 的 even ground 需要 W_{0,2} 或其他 sector 补偿（Case C）或 perturbation 结构**

## 判定
- q₀ 确认（−2.1407）——g₋ 骤降信号（过拟合）——**P1 uniform gap 危险——倾向 Case B/C**
- ⚠️ 未定论（N=40 单点——需 N=50,60 确认趋势 + 高精度）

## 下一步候选
- (a) N=50,60 + 高精度确认（g₋ 是否继续趋零——决定性）
- (b) 接受 Case B/C 倾向（P1 uniform gap 失败候选——标注需确认——转向 W_{0,2}/perturbation 结构）
- (c) 唐先生指示
