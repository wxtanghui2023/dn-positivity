# P49-G2.7.3-R2.2b：High-kk/Near-kernel audit——‖M‖↑ 真实 + 近核低 k

> 2026-09-02 15:09 · 逐项精确 · MP 高频尖峰 · λ_min 符号 float64 极限

## ① ‖M_N‖↑ = 真实高频谱效应（非 M_R 污染）
- **逐项 R(e_k) = R_A+R_R+R_P 差 0.0e+00**（三 sector 逐项和与 Mtot 对角精确一致）
- 高 k 对角（N=6）：V₅: M_kk = 2.249 = MR 0.816 + MP 1.439（**MP 的 V₅ 对角 +1.439 大正——prime 高频共振**）——V₇ (N=8): 1.529 = MR 1.152 + MP 0.380
- max|M_ij| 在 (−5,−5)——V_{−5} 对角 2.249——**真实高频结构**
- **机制**：MP（prime）的 V_k 对角在特定 k（k=5——频率 2π×5/L ≈ 14.3）与 {logn} 采样共振——大正对角

## ② 近核向量（低 k——非高频迁移）
- λ_min(M₊) ≈ −1.5e-12——λ_min(M₋) ≈ +5e-11 (N=4) 到 −4e-13 (N=6)——**都在 ±1e-11 内**
- **even ground 系数集中低 k**（e₀+e₁ 主导）：N=4: [0.44, 0.48]——N=8: [0.57, 0.40]
- **odd ground 也低 k**（o₁ 主导 0.48-0.72）
- **推翻"near-kernel migration to high frequency"假设**（N ≤ 8——近核在低 k）

## ③ ⚠️ λ_min 符号在 float64 极限
- even λ_min = −1.5e-12（负）——odd = +5e-11 到 −4e-13——**符号在 1e-12 级——float64 极限**
- **Δ_N 符号完全不可判定**（±1e-12——机器精度边缘）
- Mtot 有"近核"（λ_min ~1e-12——矩阵奇异到机器极限——非组装 bug——真实近核）
- ⚠️ even 负 1.5e-12 若高精度确认——Mtot 非半正定（Weil 正性疑问）——若 0+——半正定——**需 R2.2c mpmath 高精度回答问题 I（正性）**

## 判定
- **R2.2b PASS**：‖M_N‖↑ 真实（逐项精确——MP 高频共振）——近核低 k——**但 λ_min 符号（问题 I）需高精度**
- 双近核（even/odd 各一近零）——Mtot 核空间 ≥ 2 维（或近核）

## 下一步候选
- (a) **R2.2c 高精度**（mpmath 50-100 digits——N=2,4,6——λ₊/λ₋/Δ_N——sign 是否稳定——回答问题 I 正性 + II ordering）
- (b) 唐先生指示
