已查地图：命中（`ZF-CONT-1/2/3`（含 `D8` 对数型排除）／`ZF-G5G6-PL-gate`／`ZF-EDR-1/2/3`／`ZF-RPC-1`／`T7`（显式公式路线已封）／`V192`（β-盲）／`W8`（信息上界） 本线自档与既有封存）⟹ **引用，不开新案** ✓
D0: 本档对象 = `ZAI-1` 三项检验（对显式公式/素数计数误差候选）＋ "逐零点可分解性"新判据 ＋ 对 `D8` 适用范围的界定
D1: 0 （`[REVIEW]` 轮次：检验与判据推导，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`ZF-ZAI-1`：三项检验 ＋「逐零点可分解性」判据**（本档全为自行推导 ✓✓）

**【任务（照录您的指令 ✓）】** 资源从右侧（`event\Rightarrow`finite capacity，已 PASS）**全部移到左侧**：攻击 $$\boxed{\text{off-axis }\beta\ \longrightarrow\ \text{arithmetic quantity changes}}$$；三检验 **A** β-native｜**B** arithmetic landing（离散）｜**C** finite-event consequence；⛔ 不碰 RH 主命题、不找新 collision ✓

---

## §1 **最自然的候选（落到显式公式）**

```
【候选】 素数计数误差（Chebyshev 误差）$$A_X(\rho):=\psi(x)-x\ \text{的零点分解中的单项}\quad \frac{x^{\rho}}{\rho}$$ ✓
【为何自然】 显式公式 `\psi(x)-x=-\sum_\rho x^\rho/\rho+\text{(经典项)}` ⟹ **`\psi(x)` 是算术量（`\Lambda` 求和）** ✓；`x^{\rho}` 的模 `=x^{\beta}` ⟹ **对 `\beta` 极敏感** ✓✓
```

## §2 ⭐ **Test A（β-native）：PASS —— 且**一阶（奇）**响应（本档关键界定）**

```
【计算】 `|x^{\rho}|=x^{\beta}=x^{1/2+\delta}` ⟹ 与在线（`\delta=0`）相比，差异含**一阶项** $$\delta\cdot\log x\cdot x^{1/2}+\cdots\ \ne0$$ ⟹ $$\boxed{\text{一阶（奇）横向响应}}$$ ✓✓
【⭐ 界定 `D8` 的适用范围（本档更正性说明）】 `D8` 判死的是**对数/势型**载体（`\log\xi`、Jensen、Green 势、`\xi'/\xi` 求和），其横向响应**偶/二阶**；而此处 `x^{\rho}/\rho` 的响应**一阶/奇** ⟹ **不属于 `D8` 的排除域** ✓✓ —— 即 **`D8` 不可外推到"一切含零点的载体"**，只能外推到"对数/势型" ✓
【⟹ Test A】 **PASS** ✓（β-native，且非偶盲）
```

## §3 **Test B（arithmetic landing）：PASS（但仅**聚合级**）**

```
【左端】 `\psi(x)=\sum_{n\le x}\Lambda(n)` ⟹ **天然算术、且逐项为算术** ✓；`\psi(x)-x` 为**算术误差** ✓
【右端】 单项 `x^{\rho}/\rho` 本身是**复数解析量**（非算术）✗ ⟹ "arithmetic landing" 只在**求和后**成立 ⟹
【⟹ Test B】 **PASS（聚合级）**，但 ⚠️ **逐项不落算术** ✓（此为下一检验的伏笔）
```

## §4 ⭐⭐ **Test C（finite-event consequence）：FAIL —— 两条独立死因**

```
【死因 (i)：循环（RH-等价）】 控制 `\psi(x)-x` 的**一致界**即 RH 级陈述（`T7` 已封）⟹ 用它产生"有限事件"必然回到 RH-等价 ⟹ ✗
【死因 (ii)：**逐零点不可分解**（本档核心）】 显式公式给出的是**求和** `\sum_\rho x^\rho/\rho` ⟹ 无法从**有限算术数据**中**单独**抽出单项 `x^{\rho_0}/\rho_0` ⟹
　$$\boxed{\text{单项不可分辨}\ \Longrightarrow\ \text{违反 }PL\ (\text{逐零点可求值})}$$ ✗✓✓
　⟹ 故 Test C **FAIL**：既不能产生**新**事件类型（只能产生 RH-等价型振荡），也不满足 `PL` ✓
```

## §5 ⭐⭐⭐ **由三检验得到的新判据（本档核心产出）**

```
【三项必需（合并您的 A/B/C 与既有 `PL`）】 所需对象须同时：
　**(A)** β-native（一阶/奇响应，非 `D8` 型偶盲）✓；
　**(B)** 落**离散算术**对象（valuation／order／有限群／递推轨道）；**(B′)** ⭐ **落到**逐项**级（而非仅求和级）**；
　**(C)** 产生**新**事件类型且**非 RH-等价**；**(C′)** ⭐ 满足 `PL`（逐零点可求值、不聚合）✓
【⟹ 单一缺口的新表述】 $$\boxed{\text{须存在一个 β-native、离散算术取值、\textbf{且逐零点可分解}的函数}}$$ ✓✓✓
【⭐ 与既有墙的关系（统一）】 「逐零点可分解」正是本仓 `W8`（信息上界）与 β-盲所指向的**分辨力**缺失；本档把它**精确化为一个可陈述的判据**：不是"看不见 β"，而是"**看见 β 的算术量无法逐零点分辨**" ✓✓
【⟹ 对 ZAI-1 自然候选的判定】 显式公式/`\psi(x)-x` ＝ **A✓ B✓(聚合) C✗** ⟹ $$\boxed{CLOSED\ (\text{循环}＋\text{不可分解})}$$，⛔ **不接 EDR/RPC** ✓（遵您 §Test C 的"若不能即不要往 EDR/RPC 接" ✓）
```

## §6 **账本更新（照您表格）**

| 层 | 状态 |
|:--|:--|
| `EDR` finite-capacity | **PASS / 保留** |
| `RPC-1` | **PASS / 保留** |
| RPC 自然族 A/B/C | **CLOSED** |
| 继续寻找 `S_{\rm exc}` | **停止** |
| 离轴 → 稀有算术集 | **CLOSED 型思路，不再包装** |
| 离轴 → 新算术量发生变化 | **OPEN，本档首攻** |
| β-native quantity | **本档：显式公式候选 A✓B✓C✗ ⟹ CLOSED**；**新判据 `(A)(B′)(C′)` 已立** |
| 新算术量 → finite event | **OPEN（须满足 `(B′)(C′)`）** |
| finite event → finite zeros | **已有预算工具可接** |

```
【边界】 ⚠️ §1 显式公式为经典（档级）；⛔ 未制造候选／未启动搜索／未改状态；⭐ §2 的 `D8` 范围界定、§4 的不可分解性诊断、§5 的新判据均为**本档自行推导** ✓
```
