# P49-G2.7.3-R2.2c-Step2：近核 tail projection——部分执行（计算瓶颈）

> 2026-09-02 15:20 · 分段积分框架建立 · λ_min 扫描/近核投影超时

## 状态
- 分段积分（[0,40],[40,80],[80,120],[120,200]）完成——[200,400] 未完成（超时）
- **λ_min(T) 扫描和近核 tail projection r_T(c_min) 未执行**（分段积分本身耗尽了时间预算——dps=50 的 mp.quad 振荡被积——15 上三角元 × 2 侧 × 每段——太慢）

## 技术瓶颈
- mpmath 高精度逐元积分（25 元 × 5 段 × 2 侧 = 250 次 mp.quad——dps=50）——单次 exec 超时
- **需唐先生的 BB* 共享节点方案**（一次 quadrature 节点算全矩阵 outer product——而非逐元）——或——后台分批跑

## 已确认（前轮）
- float64 1e-12 负值 = REVOKED artifact——dps=50 T=80 λ_min = 3.76e-9 > 0（初步——C3 未完成）
- 近核向量低 k——Mtot 近半正定方向

## 判定
- R2.2c 进行中——**C3 tail 判定（情形 A/B）未完成——λ_min 正性的 T-稳定性未确认**
- **计算瓶颈 = mpmath 逐元积分效率**——需优化（共享节点/后台）

## 下一步候选
- (a) 后台长任务完成 T-scan + 近核 projection（exec background——分批——或——减少 dps 到 40 + 减段）
- (b) BB* 共享节点积分实现（唐先生方案——效率关键——一次节点全矩阵）
- (c) 唐先生指示（继续投入 vs 阶段性收束——考虑已极多轮）
