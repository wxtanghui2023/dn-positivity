# 数域素数几何（Norm-Frobenius 耦合）：统一语言成立——RH 机制判死（2026-09-03）

## 状态
唐先生最终判定：数域素数几何（素理想位置-方向二元性——）作为统一语言成立（PNT_K/Chebotarev/Goldbach_K/Twin_K/GRH_K 统一表述——）——作为 RH 新机制判死。

## 我的修正（接受）
"有限 G 特征标陷阱"错误：χ(gh) ≠ χ(g)χ(h)（只对一维表示——）——高维不可约表示给矩阵元耦合：
1_C(g_𝔭 g_𝔮) = (|C|/|G|) Σ_χ χ(c⁻¹) Σ_{i,j} ρ_χ(g_𝔭)_ij ρ_χ(g_𝔮)_ji
有限 G ⟹ 有限表示论矩阵系综——**非** Artin L 函数组合（我的逻辑链断在 χ(gh)=χ(g)χ(h)——）

## 精确死因（唐先生——比我更准）
- Frobenius 增加的是**有限状态自由度**（G 的离散坐标——）
- 范数加法增加的是**相关问题**（Σ_{N𝔭+N𝔮=X}——加法尺度 X——）
- 二者耦合**没有产生新的临界尺度不变量**
- **变量层缺失**：RH 零点项在 log 尺度振荡（x^ρ = e^{ρ log x}——γ ↔ log p 对偶——）——几何的天然变量是加法尺度 X = N𝔭+N𝔮——**X → log X 的尺度变换缺失**（该变换是 Archimedean 的——已封——）
- 表示论展开：A_C(X) = (|C|/|G|) Σ_ρ d_ρ-weighted matrix correlations——新部分是"表示值素序列的加法相关"（a_{ρ,ij}(n) = Σ_{N𝔭=n} ρ(F_𝔭)_ij——的加法卷积——）——非新 L 函数——也非新临界尺度

## 层级表（修正版）
| 层次 | 结果 |
|---|---|
| 有限 G + 单素理想统计 | Artin/Hecke/Chebotarev |
| 有限 G + 两素理想 Frobenius 耦合 | 表示值序列的加法相关 |
| K=ℚ | Goldbach/twin/prime tuples |
| 无限 Galois 群 | 更丰富但需重定义可控 L 函数/自守结构 |
| RH/GRH | 仍缺"加法几何 → 临界线"的必然机制 |

## 关键结论
- "非交换 G"不是突破口（S₃ 数值实验无需做——符号层已判——）
- "有限 G 自动退化为 Artin L"也不是正确死因
- 数域素数几何 = Chebotarev 状态空间 + 范数加法几何——统一表述成立——统一证明机制缺失
- 死因：Frobenius 有限自由度 + 范数加法相关问题——耦合无新临界尺度不变量——log 尺度变换缺失（Archimedean——已封——）

## 文件
docs/number-field-prime-geometry-death.md
