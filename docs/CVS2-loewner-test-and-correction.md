# **Loewner/谱作用矩阵的 TP 抽查**：⚠️ 我设定错了对角元 + ⭐ **发现"符号正则"现象**

**依据**：唐先生 2026-09-11 20:44（"继续"）｜**脚本**：`scripts/CVS2_loewner_tp.py` ✓
**标注**：【ERR】｜【核验】｜【推导】

---

## §0 ⚠️ **ERR：我把对角元设成了 f″(λ_i)，而 C–vS 的对角元是【自由的】**
```
C–vS (13)：**q_ij = (b_i − b_j)/(λ_i − λ_j)（i≠j）；【q_ii = a_i】**（a_i 为**自由参数** ✓）
   —— 即 **对角元 a_i 不是由 f 决定**，而是**待定的** ✓✓
   证据：其 §5（有限维偶情形）给出**三条显式"正性条件"**（含 a₁,a₂,a₃,β,y 的三角表达式 ✓）
   ⟹ **正性 = 对 a_i 的条件族**，不是"取 a_i = f″"就自动成立 ✗
我设 a_i = f″(λ_i)（自然后果）✗ ⟹ **矩阵非正定是【预期】的** ✓，**不构成对其理论的否证** ✓
【自查】按此设定，φ=e^x 的 2×2 子式 det[[1, e−1],[e−1, e]] = e − (e−1)² = −0.234 < 0 ✗
   ⟹ 说明"Loewner 矩阵（对角取 φ′）必 PSD ⟺ φ 凸"这一**我记忆中的陈述不成立** ✗（须以原文为准 ✓）
```

## §1 **实测结果**（脚本 ✓）
```
                          case |    min eig |     k=2     |     k=3     |    k=4   |    k=5
 a) f′=e^x        (abs.monotone) |  -8.45e+01 | NEG:-8.4e+03| NEG:-1.5e+04|  OK      |  OK
 b) f′=x²         (convex only)  |  -3.97e+01 | NEG:-1.2e+02| NEG:-2.6e+03| NEG      | NEG
 c) f′=logistic   (abs.monotone) |  -4.97e-01 | NEG:-2.5e-02| NEG:-6.2e-04|  OK      |  OK
 d) f′=log(1+e^x) (convex only)  |  -1.32e+00 | NEG:-4.2e-01| NEG:-4.2e-02|  OK      | NEG:-2.7e-12
```
## §2 ⭐ **但出现一个真实结构现象：符号正则（sign-regularity）**
```
【观察】(a)(c)（**绝对单调**的 f′）在 **k ≥ 4 时子式全部为正** ✓，而 **k = 2,3 为负** ✗；
   (b)（仅凸）则**各阶皆负** ✗⟹ 两类**可区分** ✓✓
⟹ 读法：**(a)(c) 的子式呈"低阶负、高阶正"的【交替符号】模式** ⟹ 这正是经典
   **符号正则矩阵（sign-regular / TP up to signs，即"广义全正"）** 的特征 ✓✓
   —— 经典上，**Loewner 型与 Fourier 型矩阵**正是这种"带符号模式"的对象 ✓
⟹ 而 (b)(d)（仅凸）**不呈**该模式 ⟹ **该模式可能正是"绝对单调 vs 仅凸"的区别标识** ⚠️（待验证 ✓）
【注】因 §0 的对角元错误，以上**不能作为定论** ✗，只能作为**待检现象** ✓（且 k=4 的"全正"可能受阶数-点数限制 ✓）
```

## §3 **结论与修正后的下一步**
```
① CVS1c 的核验（**正 Toeplitz + 秩亏 1 ⟹ 核多项式根全在单位圆** ✓ 6/6）**不受影响** ✓✓
② Loewner 部分须按 **C–vS §5 的自由对角元 + 三条正性条件**重做 ✗⟹ **下一读目标 = C–vS §5** ✓✓
③ 【副产品】找到本项目 P50/P51 旧脚本仍在盘上 ✓：
   `escape_range_scan.py`｜`p51_t3_coeff_test.py`／`_multi_test.py`／`_representation_test.py`｜`pillar3_escape_test.py`
   ⟹ **对它们做"有限素数 ⟹ 秩亏"检查在技术上可行** ✓（下一步 ✓）
```

## §4 边界
```
【ERR】§0 的对角元设定错误（并撤回对 Loewner 判据的记忆性陈述 ✗，以原文为准 ✓）
【核验】§1 的实测表 ✓（脚本可复现 ✓）
【推导】§2 的"符号正则"读法（**待验证** ⚠️）；§3 的结论
【未做】未读 C–vS §5 ✗；未跑 P50/P51 秩亏检查 ✗；未输入 1/2；未构造模型；未改 L2；未声称任何证明
```
## §5 提交链
```
CVS1（27a2fa2 机制 6/6 验证）→ 本篇（Loewner 抽查 + ERR + 符号正则现象）
```
