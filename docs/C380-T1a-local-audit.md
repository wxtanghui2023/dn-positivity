已查地图：命中 **2** 处（`T1a`／`低次消失元` 皆为本仓既有条目 ⟹ **引用，不开新案** ✓）
　档级引用：`C380-MASTER-LEDGER-FREEZE-2026-09-22.md`（L21 `T1a` 定义；L119/137/155/188「`T1a only`」）✓｜`C3889`（L41/L68 `T1a` 定位）✓｜`C3890`（`§0⑤ T1a-β`；`§0④` 事后认证）✓

# **`C-380 / T1a` 局部审计（第一刀）**：唯一活支 ＋ 五出口 ＋ 反循环窄检查

**唐先生令（2026-09-23 08:57）**：
① 选 **(i) 回归 `C-380` 活支 `T1a`** ✓；理由（照录）：**`E-10` 已完成其作为候选发生器的使命**，继续造候选的边际收益低于回到**已有、明确 OPEN** 的数学支线 ✓
② ⭐ **关键纪律**：$$\boxed{\text{T1a 是 OPEN 支} \neq \text{重新打开整个 }C\text{-}380}$$ —— **只围绕 T1a 当前剩余缺口做局部审计**，
　⛔ **不重新触碰已 CLOSED 的 `T1b`／`T2`／其它支** ✓✓
③ **并行采用五出口**：**A RH interface｜B 是否可能提高 `2/3`｜C 能否排除/控制有限离轴零点｜D 其它素数问题｜E 其它猜想**；
　⭐ **B/E 不得因 T1a 原属 RH 项目而自动置空** ✓✓；若 T1a 最终不能过 RH bridge 但能得独立非平凡定理 ⟹ **应作独立资产留下，不简单记 DEAD** ✓
④ ⭐ **新增窄启动检查（防循环）**：$$\boxed{\text{T1a 的下一推导是否真正改变了 quantity？}}$$ —— ⛔ **不接受**换坐标／重新命名／已有不等式改包装／倒灌 RH 的 `1/2`／只改 notation；
　**必须有新的 quantity／law／parameter／obstruction** ✓✓
⑤ **路线图（照录）**：$$\boxed{E\text{-}10\ \mathrm{FROZEN} \to C\text{-}380/T1a\ \mathrm{OPEN} \to \text{局部审计} \to \text{五出口评价}}$$；⛔ **不再开 `C-B`/`C-C`** ✓
⑥ **工作区纪律**：与 KH-5 无关的杂项**不因切换到 T1a 而混入提交；各归各的主线** ✓

D0: 本档对象 = **`T1a` 活支的局部审计（定义/缺口/五出口/反循环）**（引既有 `C380` 档；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算／零数值／未写程序**／未做数值实验／未算零点／未接 ζ／未找 `1/2`／未证 RH ✓

---

## §1 **`T1a` 精确定义与当前缺口**（逐字引用 ✓✓）

```
【定义（`C380-MASTER-LEDGER-FREEZE-2026-09-22.md` L21 逐字）】
　`T1a` ＝ **`T1` 唯一活线**；性质 ＝ **Chebyshev 根计数／minimizing polynomial 的"结构性工具"**（**非** support-compression 工具）✓
【定位（`C3889` 逐字）】 「**`T1a`（活）✓：Chebyshev 根计数／最小化多项式 ＋ Carathéodory 数**」✓✓
【⭐ 具体活探针（`C3890` `§0⑤` 逐字）】
　$$\textbf{T1a-}\beta\ \text{（值得小探针）}：\boxed{\text{约束张量中是否存在}\ \textbf{低次}\ \text{消失元（零点}\supseteq\text{支撑、且}\ \textbf{零点数}\le2）}$$ ✓✓
【已 CLOSED 的两条（**本轮不碰** ✓）】 `T1b` ＝ CLOSED（矩空间边界／principal 表示：支撑界 `\lceil(k+1)/2\rceil=13 \gg 2`；重开判据未满足）✓；
　`T1c` ＝ CLOSED（de la Garza／T-system 支撑下降：六 Gate 中 `Gate 3/4/5` 全 FAIL；"机制存在、对 M1 无效"）✓
```

## §2 ⭐ **反循环窄检查**（用户新检查的首次执行 ✓）

```
【问题】 T1a 的下一推导（＝推进 `T1a-β`）**是否真正改变 quantity？**
【⚠️ 关键事实（`C3890` `§0⑥` 逐字）】 「数值核验（本档）✓✓：`s = m^2 + a^2` ✓；`p` 展开 ＝ `y^2 + y/4 - 13/32` ✓；**两零点** `\in[-1,1]` ✓✓」
　⟹ **`T1a-β` 的"存在面"已有（档级）数值见证**：一个**二次**多项式、零点数 **2**、落在 `[-1,1]` ✓✓
【⚠️ 同时（`C3890` `§0④` 逐字）】 「⟹ **回溯测试只能事后认证、不能前置推导** ⟹ **信号混合**」✓
【⭐ 本档反循环判定】
　· **若**下一步只是"再找一个数值见证" ⟹ ⛔ **不通过**（事后认证；quantity 未变）✓
　· **若**下一步是"给出**低次（`\le 2`）消失元的结构性前置推导**（为何必须存在、且次数必然 ≤ 2）" ⟹ ✅ **通过**：
　　其 quantity 改变方向明确 ＝ **经典次数界 `36` ⟶ 目标 `2`（量级改善）** ✓✓（`C3890` 逐字已记「次数界 `36 \gg 2`」✓）
　⟹ **判定：通过（条件式）**，条件 ＝ 必须是**前置推导**而非事后认证 ✓✓
```

## §3 **五出口评价**（⭐ B/E 不置空 ✓）

| 出口 | T1a 当前状态 | 依据／说明 |
|:--|:--|:--|
| **A RH interface** | ⛔ **空／至多间接** | `C-380-135` 已裁定：**不得表述为 RH 进展**；`C-380` 求解的是**有限支撑结构问题** ⟹ A 出口对本支**为空** ✓ |
| **B 提高 `2/3`** | ⛔ **无路径** | T1a 对象（Chebyshev 根计数／最小化多项式／Carathéodory）**不接触 `N_0/N` 计数** ⟹ 无自然接入 ✓ |
| **C 有限离轴零点控制** | ⛔ **无** | 同上，无零点侧接口 ✓ |
| **D 其它素数问题** | ⚠️ **弱** | 对象为**有限 Chebyshev／矩结构**；无直接素数出口（若后续出现因子/素性结构，方可重评）✓ |
| **E 其它猜想** | ⚠️ **可能有（须审）** | 「低次消失元＋零计数 `\le2`」属 **Chebyshev 系统／矩问题／稀疏表示** 的独立问题面 ⟹ **须审其是否只是经典界的特例**（若只是"更小次数界"则非独立；若给出**新的存在性判据/机制**则可作独立资产 ✓✓）|

## §4 判定与下一刀

```
【判定】 $$\boxed{T1a = \mathrm{OPEN}\ (\text{唯一活支，局部审计第一刀完成})}$$ ✓
　**已确立**：精确定义 ✓｜具体活探针 `T1a-β` ✓｜**存在面已有数值见证** ⚠️｜**缺口 ＝ 结构性前置推导** ✓✓
【⭐ 下一刀（建议，最窄）】 **`T1a-β` 的"前置推导 vs 事后认证"判定**：
　$$\boxed{\text{能否给出"约束张量必有低次（}\le2\text{）消失元"的}\ \textbf{结构性理由}\text{（而非依赖已展示的数值见证）？}}$$ ✓✓
　**过 ⟹** quantity 真变（`36 \to 2`）⟹ 再评 E 出口是否独立；**不过 ⟹** 精确记 `T1a-β` ＝ 事后认证（非推导）✓
【⛔ 边界】 不碰 `T1b`／`T1c`／`T2`／其它 CLOSED 支 ✓；不开 `C-B`/`C-C` ✓；不混工作区杂项 ✓
```

## §5 边界与回查（✗✓）

```
✗ 零计算／零数值／未写程序／未做数值实验／未算零点／未接 ζ／未找 `1/2`／未证 RH／**未开案** ✓
✗ ⛔ 未把 `T1a` 当"重开整个 `C-380`" ✓；⛔ 未碰 `T1b`/`T1c`/`T2` ✓；⛔ 未因"原属 RH 项目"而把 B/E 置空 ✓
⚠️ 数值见证（`p = y^2 + y/4 - 13/32`，两零点 `\in[-1,1]`）为 **`C3890` 档级引用**（本档未复算 ✓）
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 反循环窄检查 命中文件数=0 ⟹ **本档新增** ✓
　技术词 低次消失元 命中文件数=1 ⟹ **档案已有（`C3890`，本档引用）⟹ 不主张** ✓
　技术词 前置推导 命中文件数=4 ⟹ **档案已有（`C3891`／`C3899e`／`C3890`）⟹ 引用** ✓
　技术词 事后认证 命中文件数=4 ⟹ **档案已有（`C3891`／`C380-LAYER5-GATE-PAPER0`／`C3899e`）⟹ 引用** ✓
　（⚠️ **自纠**：本档首版**预填**了回查数字，实测不符 ⟹ 已按实测改正 ✓✓）
【档级引用】 `C380-MASTER-LEDGER-FREEZE-2026-09-22.md`（`T1a` 定义／`T1a only`）✓｜`C3889`（`T1a` 定位）✓｜`C3890`（`T1a-β` 探针；事后认证；数值核验）✓｜`C380-135`／`C-380-FINAL-STAGE-REPORT`（不得表述为 RH 进展）✓
