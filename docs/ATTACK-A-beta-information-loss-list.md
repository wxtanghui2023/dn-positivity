已查地图：命中（`ASTRA-TRANSFER-three-attacks`／`META-OBSTRUCTION`／`ZF-ZAI-2`／`ZF-LOCAL-POWER-1`／`V186`）⟹ 引用，不开新案
D0: 本档对象 = 攻击 A：现有量"β 信息丢弃清单"（逐量：保留什么／丢弃哪一类 β 信息）
D1: 0 （[REVIEW] 轮次：审计，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **攻击 A：β 信息丢弃清单**

## §1 逐量审计

| 量 | 保留的 β 信息 | **丢弃的 β 信息类型** |
|:--|:--|:--|
| `N(T)`／`N(\sigma,T)`／密度 | β 的**阈值化计数** | **位置**（β 连续值被压成 `\beta\ge\sigma`）；β–高度**耦合**（只留边缘） |
| 显式公式固定核 `\sum_\rho \hat h(\rho)` | β 的**二阶幅度** | **一阶／符号**（σ-配对一阶抵消 ⟹ 余 `O(\delta^2)`） |
| `HS` 范数／二阶矩 | β 的**全局规模** | **位置**（"global norm forgets where the mass lies" 的同构：只记幅度不记在哪） |
| `\operatorname{rank}`／惯性 `n_-` | 离轴**配对计数** | **数值**（β 大小被压成布尔：`\beta\ne\frac12` 与否） |
| 配对相关（γ 差分布） | 高度差结构 | **β 全体**（β-blind：平面上只看 γ 差） |
| 局部系数 `\operatorname{Re}C(\rho)` | 在线/离线的**单向**判据 | **方向／符号**（单向 ⟹ 不作 criterion） |
| 盒覆盖证书（`k=4`） | 有限域内**排除** | β 的**连续坐标**（仅作被排除参数，非载体） |
| 幂和 `H_m`／局部谱几何 | **最近邻距离** `d(\rho)` | **方向**（4-元组中镜像方向信号指数小 ⟹ 丢失） |

## §2 横向结论（本档核心）

```
【共同丢弃项】 我们的量**几乎全都丢弃同一类东西**：$$\boxed{\text{把 }\beta\text{ 从\textbf{坐标}降级为\textbf{标签/布尔/阈值}\text{，并把\textbf{位置}换成\textbf{规模}}}$$ ✓
【四类具体丢弃】**(i) 位置**（全局范数、计数）｜**(ii) 一阶/符号**（σ-对称抵消）｜**(iii) 方向**（`H_m`）｜**(iv) 数值大小**（惯性只留布尔）✓
【⟹ 新载体应具备的性质（对 B 的唯一硬要求）】 量的**值本身携带 β 作为坐标**（非标签），且**逐零点局部**（可算），同时**保留位置** ⟹ 即：不要"更大的矩阵"，要"随 `\rho` 移动的载体" ✓✓
【与球堆积同构的失败】 "global norm sees mass but forgets location" ⟷ 我们 "statistic sees zeros but forgets `\beta`" ⟹ **同一失败类型** ✓
【边界】 各量的性质取自本线既有档（档级，未逐字复核）；§2 结论为本档自行推导；未制造候选／未启动搜索／未碰 RH 总攻。
