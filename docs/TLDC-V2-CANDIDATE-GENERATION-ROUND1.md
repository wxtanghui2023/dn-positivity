已查地图：命中 1 处 —— 先逐条判 已DEAD/已封/已登记；命中即引既有条目，不得开新案
　判定：唯一命中 = `MASTER-STATUS-AND-CLOSURES` L452（`V238` 行）中的「**再入**」一词，
　指「再入 Deninger／𝔽₁ 旧环路」＝**重新进入某旧路线**，与本档「**reentry 槽**」**同形异义、毫无关联** ✗✓
　其余 5 关键词（尺寸性失败／不变量性失败／第三实例／可迁移性判据／reentry）在五份地图档中 **0 命中** ✓
　⟹ 本档**非新案**：V-2 候选生成（已在 `TLDC-V2-OPENING-AND-GATES.md` 登记）✓

# TLDC / **Phase V-2 第一轮候选生成**（3 候选；逐个 killer board；**零计算**）

D0: 本档对象 = **TLDC/Phase V-2 第一轮候选评价**（档案已有对象 `TLDC C–R–D–K 机制` 的**适用性评价关系**；非重命名、非新对象）
D1: 0
FREEZE-ACK: 本档为**纸面 killer board**（零计算、零 Lean、零 RH 映射 ✓）

---

## §0 令登记（照录唐先生 2026-09-22 20:26 ✓✓）

```
【状态锁】 TLDC-Core=FROZEN/REUSABLE｜乙-3 结构形式化=CLOSED｜A2=FROZEN/CONTENT DEFERRED｜B5=FAIL/ARCHIVED
          Mathlib=NO｜C-380=HOLD｜旧桥=CLOSED｜RH interface=NOT STARTED｜V-2 候选数=0/3｜计算=禁止 ✓
【V-2 真问题】 从「TLDC 能不能抽象？」转为
   「**TLDC 的这套 C–R–D–K 结构，除了重新包装已知定理之外，能不能产生一个新的数学结论？**」✓✓
   目标形态 = 独立问题 ＋ TLDC compression ＋ strict descent ＋ reentry ⟹ **原文/已知理论没有的强化结论** ✓
   最关键 = **D-1 ＋ 最大失败点**：compression 须**比目标严格弱**；descent **不得偷用目标**；
   **最终 reentry 后结论必须确实比输入定理更强，或解决一个独立新命题** ✓✓
【第一轮纪律】 候选 **≤3**；每个只做 **paper-level killer board**：
   C1 独立压缩 → C2 失败保持 → C3 严格下降 → C4 reentry → C5 well-foundedness → **新结果检查** ✓
   ⛔ 任一候选在 **D-1／C3／reentry** 上发现**循环** ⟹ **立即 ARCHIVE**，
   **不做计算、不做 Lean、不做 RH 映射** ✓✓
【命名纪律】 引用须区分 **`TLDC-1 乙-3`** 与 **`V278 乙-3`**，避免审计假交叉命中 ✓
```

## §1 三候选（**3/3 已走完板** ✓）

### V2-C1 「任意判别式二元二次型的素数表示（含 class number > 1）」

| 板 | 内容 | 判定 |
|---|---|---|
| ① 独立问题 | 正定本原二元二次型 f（判别式 D）**本原表示哪些素数 p**？已知其**硬内容**在 genus／类域（**类群元素**）✓ 非 Liouville／Fermat 派生 ✓ | ✓ 独立 |
| C1 独立压缩 | **有**：Minkowski／约化 ⟹ 短向量（范数 ≤ 常数·\|D\|^{1/4}）⟹ 缺陷乘子 **m ≤ B(D)**，**与目标无关** ✓ | ✓ 存在 |
| C2 失败保持 | **有**：缺陷的**类**在约化下不变 ⟹ 失败保持 ✓ | ✓ 存在 |
| C3 严格下降 | **有**：m → m′（≤ m/2 型）✓ | ✓ 存在 |
| C4 **reentry** | ⚠️ **K 可行却无用**：再入回到**同一个类**，而失败谓词是**类不变量** ⟹ 循环走到**最小尺度处仍失败** ⟹ **无矛盾** ✗✗ | ⛔ **DEAD** |
| C5 良基 | ✓（m 严格下降）但**不导向结论** | — |
| 新结果检查 | ✗ 得不到新结论：硬内容＝**不变量**（须另行计算类群），**非尺寸可消** | ✗ |
| **判定** | **DEAD at C4**（reentry 不能消去**不变量性失败**）✓ | ⛔ |

⭐ **本候选的结构性产出**（见 §2）：暴露 **R 的双重作用** —— R（失败保持）既是循环的**燃料**，
又正是**不变量性失败永不被消去**的原因 ✓✓**

### V2-C2 「Lagrange 四平方 ＋ 分量上界强化」

| 板 | 内容 | 判定 |
|---|---|---|
| ① 独立问题 | n 表示为四平方，且**每分量 ≤ f(n)**（**优化型**强化） | ✓ 独立 |
| C1 独立压缩 | **有**（鸽笼，同 A2：m ≤ 2）✓ | ✓ |
| C2 失败保持 | **有** ✓｜C3 严格下降 **有**（Euler 四平方恒等式）✓｜C4 reentry **有**（Euler 恒等式闭环）✓ | ✓✗ |
| C5 良基 | ✓ | — |
| **新结果检查** | ⛔ **不在射程**：schema 产出「**不存在失败对象**」，而「分量上界」是**优化／量化**型陈述，
　**不是失败谓词的空性** ✗；且其新颖性在**零计算＋零文献核验**下**不可判** ✗ ⟹ **不得声称新** ✓✗ | ⛔ |
| **判定** | **DEAD**（① 不在 schema 射程 ② 新颖性不可判）✓ | ⛔ |

### V2-C3 「把判据本身作为独立数学命题（元判据）」

| 板 | 内容 | 判定 |
|---|---|---|
| ① 独立问题 | 证「TLDC 适用 ⟺ 存在失败阈值 B」 | ⚠️ |
| 杀点 | ⛔ **元判据递归**：以 schema 攻自身判据 ⟹ **无独立对象**；且**前档已有禁令** |
| 既有条目（**引用** ✓） | `C380-LAYER5-GATE-FREEZE` §2 meta-criterion-recursion 注（回查：`元判据递归` 命中 **6 档**）✓ | ⛔ |
| **判定** | **DEAD**（元判据递归；引前档禁令，**不开新案**）✓ | ⛔ |

## §2 ⭐ **结构性产出**（**暂定经验判据，非定理** ✓✗）

```
【判据（暂定经验）】 TLDC 可迁移的**必要形态** = 失败谓词具**失败阈值性**：
   ∃ B（可**先于结论**独立给出）：**尺度 ≤ B ⟹ 失败不可能** ✓✓
   压缩界 B 必须**落在失败阈值之下** —— 这才是"矛盾"的真正来源 ✓
【三实例回检（一致性 ✓✓）】
   · A2：B = 2，尺度 ≤ 2 ⟹ m = 1 ⟹ **达标**（失败不可能）✓
   · Liouville：B = p/2，q ≤ p/2 ⟹ q ∈ H（**外层归纳**给）⟹ 失败不可能 ✓
   · B5（FAIL）：**无独立压缩** ⟹ **无 B** ⟹ 伪 TLDC ✓（与既有 `FAIL` 记录一致 ✓）
【⭐ 新解释：D-1 门为何必要】 D-1（压缩须**独立于结论**）的**理由**由此得到：
   若压缩偷用结论，则**无法确定 B 落在阈值之下** ⟹ 阈值论证失效 ✓✓
   ⟹ D-1 与阈值性 = **同一件事的两面** ✓
【边界】 本判据为**由三实例归纳的暂定经验判据**，**未证**（证它＝元判据命题，见 V2-C3 已 DEAD ✗）✓
```

## §3 V-2 状态与判定

```
【走板结果】 候选 **3/3** 全部走完 killer board ⟹ **无幸存者** ✓
【依开局档 §4】 ⟹ **EXIT-B（DEGRADE）** 建议：**TLDC 定格为 proof-engineering schema**
   （**保留** `TLDC-Core` 资产 ✓；`TLDC-Core` 仍 FROZEN/REUSABLE ✓）⟹ **待唐先生裁定** ✓
【本结论的**准确**含义（防误读 ✗）】
   ✗ 不是「TLDC 无用」—— 它是**已验证可迁移的证明骨架**（乙-3 G1–G5 ✓✓）
   ✗ 不是**战绩**（只是把**适用边界**画清）
   ✓ 是：**第一轮候选池中没有任何候选能产出一个新数学结论** ✓
【为何这不是"执行不力"】 三候选分别死在**不同**关口（C4 不变量性／射程外优化型／元判据递归）⟹
   缺口**不在执行**，而在「**新结果**」这一目标本身对 schema 类方法的天然限制 ✓✓
```

## §4 若开第二轮：**新门建议**（待裁 ✓）

```
【PROMOTE 的必要条件（建议）】 候选必须**自带可核验的新颖性路径**：
   (a) **文献核验**（授权查权威来源，判定"已知/未知"）或
   (b) **计算授权**（开 Scale Gate，做**可判定的**新命题）
⟹ 本阶段（**零计算 ＋ 无文献核验**）**不可能**产生 PROMOTE ✓✓（阶段天花板已明 ✓）
⟹ 若唐先生希望续跑 V-2，须**先放宽一条**（授权文献核验 或 授权计算）—— **否则第二轮必重复同一天花板** ✓
```

## §5 边界与回查（✗✓）

```
✗ 零计算／零 Lean／零 RH 映射 ✓；未动 C-380（HOLD）／旧桥（CLOSED）／RH interface（NOT STARTED）✓
✗ 不主张 §2 判据为**定理**（标暂定经验 ✓）；不因"关掉三候选"而宣称成果 ✓
✗ 命名纪律：本档 `TLDC-1 …` 与 `V278 乙-3` 严格区分 ✓
【回查（**先跑后写** ✓）】
　prework_map_check：命中 1（`V238` 的「再入」＝同形异义）⟹ 非新案 ✓
　tech_word_check：尺寸性失败／不变量性失败／K-闭合／可迁移性判据／新颖性核验／短表示界／尺度阈值／B-阈值／
　　　　　　　　　阈值性／失败阈值／尺寸性 ＝ **0 ⟹ 本档新增** ✓
　　　　　　　　　killer(1，本轨开局档)／判据工具箱(7)／元判据递归(6) ⟹ **档案已有 ⟹ 引用** ✓
```
