已查地图：命中（`E-32`／`AMEND-6/7/8` 本线自档）⟹ **引用，不开新案** ✓

# **卡 4 `S4-PRE-EXACT` 修补 A ＋ B**（两处结构缺陷；**零计算** ✓；`RUN` **不授权** ✓）

**唐先生令（2026-09-23 14:09 ✓✓）**：**暂不核可 `RUN`**；E-32 形式层钉得很紧，但出现**比措辞更基础的结构性问题** ✓；
　**准确状态**：$$\boxed{\text{Card 4}=\text{S4-PRE-EXACT}，发现两个待修逻辑点；RUN 不授权}$$ ✓；**不是退回卡 4，也不是关闭卡 4** ✓
　⚠️ **且第一个问题极须重视**：若不修，卡 4 所谓"跨形式 × 双尺度"检验**实际退化**为一个**纯跨形式共同递推检验** —— 那样它**仍可能是有价值的 SOURCE**，但**已不是 E-32 当前声称的那个 `S4`** ✓✓

D0: 本档对象 = **卡 4 的 `S4-PRE-EXACT` 修补 A（第二尺度）＋ B（`\mathcal R_{\rm old}` 解释充分性）**（引本线自档；**未开 bridge 案** ✓）
D1: 0 （`[REVIEW]` 轮次：预注册修补，不主张新自由度 ✓）
FREEZE-ACK: D1=0 ✓
[REVIEW]

---

## §1 ⚠️ **缺陷 1（致命）：`X_1,X_2` 在当前 `p_h` 坐标下是同一前缀 ⟹ "四路同系数"退化**

```
【原因（照录 ✓✓）】 `x_h=a_{p_h}(f)`，两尺度方程范围**都是** `h=0,\dots,1220` ⟹ 对每个 `0\le h\le1220` 有**同一个 `p_h`** ⟹
　$$M^{(f,X_1)}=M^{(f,X_2)},\qquad M^{(g,X_1)}=M^{(g,X_2)}$$ ⟹ 实际堆叠 $$M=\begin{pmatrix}M_f\\M_f\\M_g\\M_g\end{pmatrix},\qquad \ker M=\ker\begin{pmatrix}M_f\\M_g\end{pmatrix}$$ ✓
　⟹ 所谓"四路同系数"**实际只剩**：$$\boxed{\text{两种形式 }f,g\text{ 的同一条素数系数递推}}$$，**而不是** `两形式\times两尺度` 的**四重独立约束** ✗
【⛔ 撤回（照录 ✓✓）】 `\S4` 中"第二尺度同系数（**机械满足**）"这句**必须撤回** ✗ —— 原因正是 `X_1,X_2` 进入矩阵的坐标区间**产生完全相同的数据** ✓
【与卡 1 的本质差别（照录 ✓）】 卡 1 的 `X=10^6,10^7` 改变的是 $$X\mapsto\sum_{n\le X}d(n)d(n+h)$$ ⟹ 两尺度产出**不同数值对象** ✓；
　卡 4 当前却是 `(a_{p_0},\dots,a_{p_{1220}})` 在两个 `X` 下**完全相同** ✗ ⟹ `AMEND-7` 的"第二层/第二尺度**独立复现**同一结构"**未被满足** ✓✓
```

## §2 ✅ **修补 A：真正定义第二尺度（**重新预注册**，非尾巴跑 ✓）**

```
【⚠️ 明确（照录 ✓）】 **不能**简单把 `h\le2261` 改掉就算修复（那会改变原两尺度判定规则 ⟹ 须**重新预注册**）✓
【本档采用：两个**互不相交、事前固定**的素数窗口 ✓✓】
　$$\text{窗 }W:=\text{素数 }p\le10^4\ (\pi=1229);\qquad \text{窗 }W':=\text{素数 }10^4<p\le2\times10^4\ (\pi=2262-1229=1033)$$ ✓
　两者**互不相交** ✓；**事前固定** `H_{\rm row}=600` ⟹ 每窗需 `H_{\rm row}+J+1\le605` 个素数 ⟹ 取各窗**前 605 个素数**（窗 `W'` 有 1033 个 ⟹ 充足 ✓）✓
　四块改为：$$M=\begin{pmatrix}M^{(f,W)}\\M^{(f,W')}\\M^{(g,W)}\\M^{(g,W')}\end{pmatrix}\qquad (\text{每块 }601\times(J+1))$$ ✓
【判定（不变 ✓）】 $$\boxed{\exists\,c\in\mathbb Z^{J+1}:\ Mc=0,\quad c_J=1}$$，`J\le4` ✓
【⭐ 反事后条件 (2) 须改写（照录精神 ✓✓）】 由"第二尺度"改为：$$\boxed{\text{两个\textbf{互不相交}的素数窗口（事前固定的两个区间）复现同一结构}}$$ ✓
【诚实标注】 这是**语义变更**（从"尺度前缀"变为"两个独立窗口"）⟹ 属**重新预注册**，须经您核可 ✓
```

## §3 ⚠️ **缺陷 2：`\bigcup_x\ker H_x` 不等价于"被旧机制吸收"**

```
【逻辑缺口（照录 ✓✓）】 现定义 $$c\ \text{被吸收}\iff c\in\bigcup_{x\in\mathcal L_{\rm old}}\ker_{\mathbb Q}(H_x)$$；
　但 `c\in\ker H_x` **只证明**"同一 `c` 也湮灭了序列 `x`"，**不能自动证明** `f` 的关系**由该机制产生** ✓
　$$\boxed{\text{OLD-ANNIHILATOR}\ \neq\ \text{OLD-MECHANISM-EXPLANATION}}$$ ✓✓
【假阳性（照录）】 存在 $$\boxed{\text{新对象 }c\ \text{偶然属于某个旧序列的核}}\Longrightarrow\text{被归入 OLD 而非 FORCED}$$ ✗
【✅ 修补 B（照录 ✓✓）】 先定义**由预注册旧机制生成的关系空间** $$\mathcal A_{\rm old}:=\{\text{由预注册旧机制生成的关系空间}\}$$，
　判定改为 $$\boxed{c\in\mathcal A_{\rm old}\ ?}$$**而不是** `c\in\bigcup_x\ker H_x` ✓；
　⭐ 若最终证明两者在当前**有限实验域恰好等价**，可再写成核判定 —— ⛔ **不得预先假定等价** ✓✓
【本档给出的 `\mathcal A_{\rm old}` 生成规则（事前写死 ✓；⛔ 不事后增补 ✓）】
　**① `\mathcal A_{\rm Hecke}`**：由 Hecke 递推/乘性生成的**线性**关系。⚠️ Hecke 关系是**乘法性**（`a_{mn}=a_ma_n`、`a_{p^{k+1}}=a_pa_{p^k}-p^{k-1}a_{p^{k-1}}`）⟹ **不生成**不同素数系数间的**线性**关系 ⟹ **claimed: `\mathcal A_{\rm Hecke}=\{0\}`**（**纸面主张，待核** ⚠️）
　**② `\mathcal A_{\rm sym}`**：水平 1 新形式**自对偶**（Fricke 平凡）⟹ **claimed: `\mathcal A_{\rm sym}=\{0\}`**（同待核 ⚠️）
　**③ `\mathcal A_{\rm dens}`／`\mathcal A_{\rm log}`／`\mathcal A_{\rm sqrt}`**：素数分布/对数/平方根结构 ⟹ 皆为**渐近型**，**不产生精确线性关系** ⟹ **claimed: `=\{0\}`**（同待核 ⚠️）
【⭐ 由此得到的诚实结论（本档新增，须您裁定 ✓）】 若 ①②③ 的 claimed 结论经核可，则 $$\mathcal A_{\rm old}=\{0\}\Longrightarrow\text{任一命中 }c\text{ 自动"非旧机制解释"}$$ ⟹
　**吸收判别式变为空判据** ⟹ 此时反事后保护**不再来自"吸收"**，而来自：(i) **四块同系数的高超定精确判定**；(ii) **两个互不相交窗口**（修补 A）；(iii) **全部事前固定**（形式/对/`J`/`h` 范围/窗口）✓✓
　⟹ **核相交判别（`\bigcup_x\ker H_x`）降级为"诊断性报告"**（发现命中恰落在某旧序列核内时**如实报告**），⛔ **不再用作 OLD/FORCED 的分类依据** ✓✓
```

## §4 **状态（照录唐先生表格 ✓✓）**

| 项目 | 状态 |
|:--|:--|
| `SOURCE qualification` | ✓ |
| `S4-format` | ✓ |
| `(f,g)` 预固定 | ✓ |
| `J\le4` | ✓ |
| `10` 对全部检验 | ✓ |
| exact integer/rational criterion | ✓ |
| **真正独立的第二尺度** | **FAIL/PENDING**（本档修补 A ⟹ **待核**） |
| **`\mathcal R_{\rm old}` 解释充分性** | **PENDING**（本档修补 B ⟹ **待核**） |
| **`S4-EXACT`** | **NOT YET** |
| **`COMPUTATION`** | **LOCKED** |

```
【边界】 ✗ 未计算／⛔ 未挑形式／⛔ 未改 `S4` 判定标准／⛔ 未事后增补 `\mathcal L_{\rm old}`／⛔ 未把核相交当分类依据／⛔ 未假定 `\mathcal A_{\rm old}=\bigcup\ker` 等价 ✓
【诚实标注】 ⚠️ `\mathcal A_{\rm old}=\{0\}` 为**纸面主张**（三条 claimed，**待核** ⚠️），本档**未**据其下任何结论 ✓
```
