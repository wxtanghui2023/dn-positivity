已查地图：命中（`ZF-LOCAL-ANGLE-Hm-verdict`／`ZF-GAUSS-LOC-2`／`ZF-G5G6-PL-gate`／`ZF-1`／`T7` 本线自档与既有封存）⟹ **引用，不开新案** ✓
D0: 本档对象 = 「相对间距条件」的平凡性纠正 ＋ 真正非平凡形式（`2\delta`-球内三零点）＋ 平均统计不足性判定
D1: 0 （`[REVIEW]` 轮次：判定与纠正，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`ZF-LOCAL-POWER-1`：相对间距条件的判定**（本档全为自行推导 ✓✓）

## §1 ⚠️ **先纠正：您提的检查条件在字面上是平凡成立的**

```
【您的条件】 是否存在零点对满足 $$|\rho-\rho'|\le 2|\beta-\tfrac12|$$ ✓
【字面答案】**恒真** —— 镜像对 `\{\rho,\ 1-\bar\rho\}` 的距离**恰等于** `2\delta=2|\beta-\tfrac12|`（等式成立）⟹ 对**每个**离轴零点自动满足 ✓✓
【⟹ 结论】 该条件**不能作为判别量**（它不区分任何东西）✗✓✓ —— 必须改成非平凡形式 ↓
```

## §2 ⭐ **非平凡形式（本档）**

```
【真正的条件】 存在**第三个**零点 `\rho_1\notin\{\rho,1-\bar\rho\}` 且 $$|\rho-\rho_1|\le2\delta$$ ⟹ 即 **`2\delta`-球内出现三零点（含镜像）** ✓
【等价读法（本档；与 `D9-1` 会合）】 镜像对的**高度差为 `0`** ⟹ 该结构 ＝ **「高度 `\gamma` 处纵坐标重数 `\ge2`」** ✓✓ —— 与前述 `k(\gamma)\ge2` 重述**同一件事** ✓
【几何约束（您的 §8 ✓）】 `\rho_1` 须落于尺寸 `O(\delta)` 的圆盘 ⟹ `|\gamma_1-\gamma|\le2\delta` ⟹ `\delta\ll1` 时**逼出极小高度间距 cluster** ✓
```

## §3 ⭐⭐ **为何该输入不可从显式公式获得（本档核心）**

```
**(a) 极值型 vs 固定核型** $$\text{“存在一个更近的零点”}＝\textbf{极值型泛函}\ (\min \text{型})$$ 而显式公式的一切输出都是**固定核**的单点/对求和 $$\sum_\rho\hat h(\rho),\quad \sum_{\rho,\rho'}\hat h(\rho,\rho')$$ ⟹ 固定核和只能给**平均（统计）**信息，**不可能**约束**单个局部 cluster** ✗✓✓
**(b) 相对（尺度无关）核须知道 `\beta`** 若要核只对**相对间距** `|\rho-\rho'|/(2\delta)` 敏感 ⟹ 核须依赖 `\beta` ⟹ **循环** ✗✓
**(c) 聚合性（承 `PL` 门）** 上述对求和**依赖其它零点** ⟹ **违反 `PL`** ⟹ 不能成为独立算术约束 ✗✓（与 `H_m` 同族 ✓）
**(d) 唯一"可无条件计算"的相关量** 纵坐标重数可在原则上由**Turing 法**无条件算 `N(T)`（平滑项＋临界线辐角 `S(T)`）而得 ✓ —— **但**只能**有限个高度**上检验；**对所有 `T`** 的重数/跳跃定理仍是 `S(T)` 的全域控制 ⟹ **RH 邻域强度** ✗✓
```

## §4 ⭐⭐ **判定（照您的判准）**

```
【您的判准】 若算术侧只能给**平均 spacing／GUE／zero density** ⟹ **不够**；必须出现对**单个局部 cluster** 有硬约束的**新量** ⟹ 否则落入旧统计墙 ⟹ 干净 CLOSED ✓
【本档判定】 由 §3(a)(b)(c)：固定核和**原理上**不能约束单个局部 cluster；相对核须循环；对求和违反 `PL` ⟹ $$\boxed{LOCAL-POWER-1:\ CLOSED\ \text{（落入 zero-statistics／correlation 墙）}}$$ ✓✓
【与既有档的关系】 非新墙：与 `ZF-1`（近 `1/2` 无一致幂次节省）、`T7`（显式公式）**同族**；且本档补上**原理性理由**（极值型 vs 固定核型）✓✓
```

## §5 **保留（工具级）＋ 账本 ＋ 边界**

```
【保留 ✓】 (a) `H_m\leftrightarrow1/d(\rho)` 幂和字典（`\limsup|H_m|^{1/m}=1/d`）；(b) **纵坐标重数 ↔ `N(T)` 跳跃**的可计算性说明（Turing 法，有限高度可无条件检验）—— 作为**局部几何/计算工具**保留 ✓
【⛔ 不做】 不再算更多 `H_m`、不再做 `H_{4k}` 组合、不再打 cluster 不等式（§3 已给原理性理由）✓

| 层 | 状态 |
|:--|:--|
| 字面相对间距条件 | **平凡恒真 ⟹ 非判别量** |
| 非平凡形式（三零点 `2\delta`-球） | ＝ 纵坐标重数 `\ge2`（与 `D9-1` 会合）|
| 极值型泛函 | 不可能由固定核显式公式给出 ✗ |
| 平均统计（spacing/GUE/density） | **不足**（您判准）✗ |
| 结论 | **CLOSED（统计墙）** |

【边界】 ⚠️ §3(d) 的 Turing 法可计算性按**档级**引述；⛔ 未制造候选／未启动搜索／未改状态；⭐ §1 平凡性纠正、§2 非平凡化、§3(a) 极值/固定核区分、§4 判定均为**本档自行推导** ✓
```
