# P28-E：Projection-Overlap Asymptotics——inter-block coherence 机制确认 + 实现审计

> 2026-09-01 · 唐先生 P28-E 指令 · W_M(N)=Tr(P_M P_N⁻) · operator bound 目标

## ⚠️ 实现审计（关键——必须先记录）
**决定性验证（scipy 广义特征值——GCG x = λGx——复 Hermitian 对）**：
- **n₋ = N（N=1:1——N=2:2——N=3:3——N=4:4——N=5:5）——G8.2′ 成立 ✓**
- **实部近似（cholesky G.real——之前所有实现）与广义一致（n₋=N）——之前所有 n₋=N/W_M 结果有效 ✓**
- **mpmath 复版（eighe——G^{1/2}CG^{1/2}）也一致（n₋=N）✓**
- **⚠️ numpy 复版（手写 G^{1/2} = U√ΛU*——eigh）有 bug（n₋=1,2,2,2,3——错误）——是手写实现的 bug（不影响结论——但——需注意）**

## ⭐ P28-E 结构分析（修正版——广义特征向量）
**N=8——M=1（低块 6 维）——每负方向的二次型分解**：
- **q_M ≥ 0（min 217.5——低块自身无负性——P_M K_N P_M ≥ 0 确认）**
- **q_H ≥ 0（min 217.5——高块自身也正！——每个固定块都正）**
- **q_cross < 0（−1.9e9 到 −1.4e6——跨块相干——唯一负贡献）**
- **q_tot = q_M + q_cross + q_H ≈ λ < 0（负方向——完全来自跨块耦合）**

## ⭐⭐ 核心机制确认——"negative inertia is generated by inter-block coherence"
- **每个固定块（低块和高块）自身二次型都 ≥ 0——负性纯跨块（q_cross < 0）**
- **"negative inertia generated by inter-block coherence, not by any fixed finite block"——严格确认**
- **这解释 n₋(P_M K_N P_M) = 0（任何固定块无负型）+ n₋(K_N) = N（跨块联合方向）**

## ⚠️ 诚实边界
- 归一化尺度异常（scipy seigh 的 X——mass_M ~ 3.4e7——巨大——x*Gx ≠ 1）——**但——定性结构（块自身正——交叉负）独立于归一化——鲁棒**
- 归一化 bug 待查（seigh 的归一化约定）——不影响定性结论
- N=8——更大 N 确认（W_M 的 operator bound——≤ C_M？）

## 下一步
- (a) operator bound 推导（W_M(N) ≤ C_M——从跨块结构——q_cross 的上界）
- (b) 归一化 bug 修复（seigh 约定）——定量 mass 分布
- (c) 唐先生指示
