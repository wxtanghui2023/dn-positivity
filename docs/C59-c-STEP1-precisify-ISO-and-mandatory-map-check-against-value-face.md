已查地图：**部分命中** —— `显式公式` 439 档／`值面` 48 档／`单个离轴` 4 档（`HOW-OTHERS-DID-IT:98`、`DOOR5e:64`、`dh-comparison:36`、`lemma3-hostile-audit:42`）＝**已覆盖** ✗；`振荡激发`／`smooth absorption`／`显式公式激发`／`单个离轴刚性`／`零点激发引理` ＝ **0–1 档**＝未覆盖 ✓。**结论：本档只做 (c) 的第一步（精确化＋必做的对审清单），不启动推导** ✓

# (c) 第一步：把 (ISO) 写成精确形式 ＋ **启动前必做的"值面墙"对审**（强制）

> **时间**：2026-09-17 21:19 唐先生「继续」⟹ 执行 (c)（显式公式"振荡激发"）。
> **本档**：① (ISO) 的精确形式；② ⚠️ **启动前必做**的对审清单（对 `值面` 48 档 ／ `V258`／`POS1`／`POS2`）✗；③ 诚实：本档**不启动推导**（见 §4）✗

---

## §1 (ISO) 的精确形式（本档提出）`[新]`

$$\textbf{(ISO)}\qquad \Big\{\rho:\ \Re\rho\ne\tfrac12\Big\}\ \ne\ \varnothing\quad\Longrightarrow\quad \Big|\Big\{\rho:\ \Re\rho\ne\tfrac12\Big\}\Big|\ =\ \infty$$

**等价改写（便于攻）**：不存在"**孤立的离轴零点族**"—— 即若离轴零点非空，则其计数函数
$$\mathcal O(T)\ :=\ \#\Big\{\rho:\ \Re\rho\ne\tfrac12,\ |\Im\rho|\le T\Big\}\qquad\text{满足}\qquad \limsup_{T\to\infty}\mathcal O(T)=+\infty$$
**严格只用于**（与 8/23 合成）：$\mathcal O\not\equiv0\Rightarrow\mathcal O(T)\to\infty$ ✓ ＋ 8/23（无穷多 ⟹ $S_2$ 指数发散 ⟹ 矛盾）⟹ **$\mathcal O\equiv0$ ⟹ RH** ✓✓

**推论（(ISO) 的弱形式，若可证则仍够用）**：$\mathcal O(T)\ge1\ \Rightarrow\ \exists\,T'\le C(\gamma_0)$ 使 $\mathcal O(T')\ge2$ ✓（即"一个 ⟹ 至少两个" ✓ —— 迭代即得无穷多 ✓ **更易证伪、更易攻** ✓✓）

## §2 ⚠️ 启动前**强制**对审清单（对 `值面` 墙）

| # | 对审对象 | 必答问题 |
|:--|:--|:--|
| 1 | `V258` §7（值面墙）| (ISO) 的论证是否**必须**经过零点计数／值面？若是 ⟹ 落 E2 排除类 ✗ |
| 2 | `POS1`／`POS2`（正性循环）| (ISO) 是否需要正性输入？若是 ⟹ 循环 ✗ |
| 3 | `V211 §5`（八类未实例化的 Φ）| (ISO) 的机制属哪一类？是否已被 `V211` 覆盖 ✗ |
| 4 | `E18-NOGO-ALIGNMENT-2:84` | "No fixed finite-order Li/Maślanka observation can uniformly exclude arbitrarily high off-line zeros" —— (ISO) 是否等价于该 NO-GO 的变体 ✗ |
| 5 | `HOW-OTHERS-DID-IT:98`／`DOOR5e:64` | 已有的"单个离轴"分析（$e^{n\delta/\gamma^2}$ 指数增长）是否**已经蕴含** (ISO) 的某种形式？若有 ⟹ 只需**重述**而非新建 ✓ |

## §3 技术词回查输出（`scripts/tech_word_check.sh`）`[纪律]`

```
技术词 显式公式激发   命中文件数=0  ::
技术词 单个离轴刚性   命中文件数=0  ::
技术词 零点激发引理   命中文件数=0  ::
技术词 一个离轴推无穷 命中文件数=0  ::
```
⟹ §1 的定形**未覆盖** ✓；但 §2 的**对审未做** ⟹ **不得**据此宣称 (c) 为新方向 ✗

## §4 诚实说明（为何本档不启动推导）

1. **上下文预算耗尽**：本会话已极长，继续开展 (ISO) 的实质推导会**质量下降**（本项目已有多次"异常先疑己"的教训 ✓）✗ —— 建议**新会话**开 (c) ✓
2. **纪律要求**：§2 的五项对审是**启动前提**（R-1＋值面墙教训 ✓）—— 未过审即推导＝重复风险极高 ✗
3. **本档交付**：精确目标 ＋ 对审清单 ＋ 判据，**这已是 (c) 的正规第一步** ✓

## §5 边界

- `[新]` §1 的 (ISO) 及其弱形式为本档提出（回查 0 档 ✓）。
- `[已有]` §2 的五项对审对象均为档案既有（逐字引用 ✓）。
- **不声称**：(ISO) 可证 ✗；(c) 为新方向（待 §2 过审）✗；不证 RH ✓；不修改任何原档 ✓。
