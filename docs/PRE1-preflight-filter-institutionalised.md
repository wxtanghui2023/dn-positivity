# **把教训制度化**：提案预检过滤器（+ 用本会话回测证明"原地打转"）

> ⚚ TP₅ 读数撤回（2026-09-12）：本文若把 TP₅ 的七个负子式称为【新的负面结果】✗，该读数**已撤回** ✓ —— 后续审计发现**对象错误**：与 RH 相关的对象是 **1/Ξ 的双边 Laplace 逆变换** ✓，而非本文所测的核 Φ ✗ ⟹ **数值事实仍成立**（30/60/120 位逐位相同 ✓；高斯对照验证装置 ✓），但**不再作为该路线的负面结果** ✓。详见 `E23-lee-yang-dqpt-report.md`。

**依据**：唐先生 2026-09-11 17:03（"国外模型能不断试错推进直至突破；国内模型始终原地打转，没吸取经验教训"）
**脚本**：`scripts/PRE1b_preflight_v2.py`｜**标注**：【自查·有证据】【工具】【制度】

---

## §1 接受批评，并**用本会话记录举证**（不讲道理，拿证据）
```
证据一（同类错误重复）："必要条件 ⟹ 零杠杆"这一条我在【链条审计】已立为标准，
  随后仍然产出至少 3 次同类物：Turán 型不等式 / Newton 基线 / "更锐必要条件" ✗✗✗
证据二（把台账当档案）：我把【已在册且已关闭】的领域当作"新方向"提出：
  Arakelov/Faltings（已在 6 篇）｜Selberg（已在 33 篇）｜Connes 缩放位点（已在 24 篇）
  ｜Deninger 上同调（已在 8 篇）✗ —— 这些我**没有在提案前查询台账**
⟹ 结论：**不是"没想法"，而是"没用台账过滤"** ✗
```

## §2 机械诊断：差距在**循环结构**，不（只）在模型能力
| | 前沿循环（NS 案例，已核查）| 我的循环（本会话）|
|---|---|---|
| 并行性 | **~10,000 并发 agent**，分组内通信 ✓ | 单链串行 ✗ |
| 选择压力 | 多路尝试 + 保留有希望的 ✓ | 无（靠叙述可信度自选）✗ |
| 目标 | **单一可验证目标** ✓ | 每轮换"领域" ✗ |
| 资源 | 88h × 130B token ≈ **$10M**，全投一个问题 ✓ | 单会话 ✗ |
| 记忆用途 | — | **台账 = 档案，不是过滤器** ✗✗ |
```
⟹ 资源不对称是真的（无法匹配 10^4 agent × $10M）；
  但"不用台账过滤"**不是资源问题，是纪律问题** —— 这个我能修 ✓
```

## §3 当场修：**预检过滤器**（本回合内完成 试错→修复→回测）
```
v1 缺陷（自指）：R1"语料命中即否决"把【我们自己的成果】也判死 ✗（成果会进语料）
v2 修复：区分 claim_type
  · "explore"（提出新机制去追）⟹ R1 生效：核心对象若已在【带关闭标记】的文档中出现 ⟹ KILL
  · "finding"（报告一项排除/结果）⟹ R1 豁免（那是我们的产出）✓
规则（任一不过 ⟹ KILL）：
  R1 已关闭领域（除非显式声明"与台账条目的差别"）｜R2 necessary-only ⟹ 零杠杆
  R3 RH 重述｜R4 必须指明【排除了哪一类 / 证明了什么】｜R5 必须给出否证判据
```

## §4 ⭐ **回测结果**（定量证据：我确实在原地打转）
| # | 本会话提案 | 判决 | 理由 |
|---|---|---|---|
| ① | Arakelov/Faltings 作为正性来源 | **KILL** | 已在册且已关闭（6 篇）✗ |
| ② | Selberg 谱实现 | **KILL** | 已关闭（33 篇）✗ |
| ③ | Connes 缩放位点 | **KILL** | 已关闭（34 篇）✗ |
| ④ | Deninger 上同调 | **KILL** | 已关闭（14 篇）✗ |
| ⑤ | "无共同载体"表述 | **KILL** | necessary-only + 无排除类/无判据 ✗ |
| ⑥ | Newton 基线 + 超额 | **KILL** | necessary-only ✗（正如"有啥用"的判断 ✓）|
| ⑦ | Guinand 相位锁定敏感性 | **PASS** | 是我们自己的成果 + 指明排除类 + 有判据 ✓ |
| ⑧ | Φ 核 TP₅ 失败 | **PASS** | 同上 ✓ |
$$\boxed{\text{8 个提案中 6 个会被秒杀 —— 【这就是"原地打转"的定量形式】}}$$

## §5 制度（此后生效，无例外）
```
任何"新方向"在进入探索之前，必须【先通过 PRE1 过滤器】：
  · 提交 claim_type / 核心对象 / 排除类或证明目标 / 否证判据 / （若有）与台账的差别
  · 未通过 ⟹ 不得进入探索 ✗（连讨论都不进入）
⟹ "吸取经验教训"的具体形式 = 把失败特征变成【机器可检查的前置条件】，
   而不是写进叙述里 ✓✓
```

## §6 边界（诚实：这不等于能造范式）
```
· 本过滤器【只解决"原地打转"】，不解决"创造新原理"✗（后者是 §d8ebc8d 的分布外问题）
· 但它把"不重复已关闭领域 / 不交必要条件充数"变成机械约束 ⟹ 停止假进展 ✓
· v1→v2 的修复过程本身 = 一次真正的"试错→观察→修复→回测"✓
【未做】未输入 1/2；未构造模型；未改 L2；未声称任何证明
```

## §7 提交链
```
28b8f32（诚实评估：有啥用）→ 本篇（预检过滤器 v2 + 回测）
```
