已查地图：命中（`E-25`–`E-30`／`AMEND-6/7/8` 本线自档）⟹ **引用，不开新案** ✓

# **`Card 2` vs. `Card 4`：SOURCE 级横向比较**（三问逐项证据；⛔ 不做"谁更好"排名；**零计算** ✓）

**唐先生令（2026-09-23 14:06 ✓✓）**：卡 1 **不再补审**（闭环已完成）✓；下一阶段唯一问题＝$$\boxed{\text{下一个 SOURCE 是什么？}}$$；
　⛔ **不立即选卡 2 或卡 4 去跑**；先做**三问逐项比较**：**(1)** 谁的 `S4` 对象空间**更容易被计算真正强迫**（而非事后解释）｜**(2)** 对象一旦出现，**是否具有脱离实验的定义域**｜**(3)** 对象出现后，**是否存在比卡 1 更实质的既有 RH 接口** ✓；
　⚠️ **并须在计算前机械化**：卡 2 的"两个素数子族**同时精确消失**"、卡 4 的"**跨形式同一系数结构／不能由 Hecke 解释**" ⟹ 否则极易重新产生 post-hoc 自由度 ✓✓
　**经验资产（卡 1 已排除的误区）**：$$\boxed{\text{"存在天然 }\zeta/L\text{ 接口"}\not\Rightarrow\text{"存在可计算的新对象"}}$$ ✓

D0: 本档对象 = **卡 2／卡 4 的 SOURCE 级三问比较 ＋ `S4` 判据机械化**（引本线自档；**未开 bridge 案** ✓）
D1: 0 （`[REVIEW]` 轮次：比较与机械化，不主张新自由度 ✓）
FREEZE-ACK: D1=0 ✓
[REVIEW]

---

## §1 三问逐项证据（逐条，不做总排名 ✓）

```
【问 1：谁的 `S4` 对象空间更容易被计算**真正强迫**？】
　卡 2（最小二次非剩余 `n(p)`）：⚠️ **弱** —— `n(p)` 取值**普遍很小**（`n(p)=2` 当且仅当 `2` 是非剩余 ⟹ **正密度**；Chebotarev 给出各小值 `n(p)=j` 的密度）⟹
　　计数向量**高度集中** ⟹ "精确关系"很可能只是**局部密度事实**的产物 ⟹ 该判据**易被密度账本事后吸收** ⚠️ ⟹ **须把"密度蕴含的关系空间"事前算出并排除**（见 §2 ①）✓
　卡 4（模形式系数 `\{a_p\}`）：✅ **强** —— `a_p` 有 Ramanujan 界 `|a_p|\le2\sqrt p` 与 **单形式内**的 Hecke 递推；而**跨形式、同系数、跨尺度**的精确线性关系**既不被 Hecke 蕴含、也不被密度账本蕴含** ⟹
　　**低先验、判定性强**（命中则强、未命中则决定性负）⟹ 与卡 1 同型的"干净筛选" ✓✓
【问 2：对象出现后是否具有**脱离实验的定义域**？】
　卡 2：⚠️ 潜在产物是"**关于计数向量的关系**"，其**天然定义域不明显**（是素数集上的统计关系，而非一个既有对象空间）⟹ 弱 ✓
　卡 4：✅ 潜在产物是"**跨新形式的不变量**"，其定义域＝**新形式空间**（**既有对象空间** ✓）⟹ 强 ✓
【问 3：对象出现后，是否存在比卡 1 **更实质的既有 RH 接口**？】
　卡 1 的接口＝$$\sum d(n)n^{-s}=\zeta(s)^2$$（**直连 `\zeta`** ✓）
　卡 2 的接口＝`n(p)\leftrightarrow L(s,\chi_p)`，经类数 `h(-p)` 与 `L(1,\chi_p)` 触及 **Siegel 零/例外零** 现象 ⟹ **深但条件性/邻接** ⚠️
　卡 4 的接口＝`\{a_p\}\leftrightarrow L(s,f)` ⟹ 属 **GRH 族**（**广义** RH），对 **RH 本身**的接口**更间接** ⚠️
　$$\boxed{\text{诚实结论：}Q3\ \textbf{两者均未超过卡 1}}$$（卡 1 的 `\zeta^2` 是最直接的接口 ✓）
　⟹ 故**继续推进的价值只能来自 `Q1`／`Q2`，而非 `Q3`** ✓✓
```

## §2 ⭐ **判据的事前机械化（`AMEND-7/8` 的机械形式；⛔ 计算前写死 ✓✓）**

```
【① 卡 2："两个素数子族同时精确消失"的机械化】
　**预固定**：子族 `P_\pm=\{p\equiv\pm1\ (4)\}`；分箱数 `K`；两尺度 `P_1<P_2`（如 `10^6`／`10^7`）✓
　**判据**：存在**同一组**整系数 `c\neq0` 使计数向量 `v^{(P_\pm)}(P_i)` 同时满足 `c\cdot v=0`（**4 路同系数**：2 子族 × 2 尺度）✓
　**事前排除空间（关键 ✓✓）**：先由 **Chebotarev** 写出各 `\{p:n(p)=j\}` 的**密度恒等式** ⟹ 生成"**密度蕴含的关系子空间**" `\mathcal R_{\rm dens}` ⟹ **要求命中关系 ∉ `\mathcal R_{\rm dens}`**（此步**必须在计算前完成**）✓
【② 卡 4："同系数跨形式结构／不能由 Hecke 解释"的机械化】
　**预固定**：两个具体新形式 `f,g`（权/水平事前写死）；两个素数上界 `P_1<P_2` ✓
　**判据**：存在**同一组**整系数 `c\neq0` 使 `\sum_j c_j a_{p_j}(f)=0` 且 `\sum_j c_j a_{p_j}(g)=0`，**且在 `P_1`／`P_2` 同系数成立**（4 路同系数）✓
　**事前排除空间**：`\mathcal R_{\rm Hecke}` ＝ **单形式内**由 Hecke 递推/乘性生成的关系；`\mathcal R_{\rm sym}` ＝ 由对偶/对称性生成的关系 ⟹ 要求命中 ∉ `\mathcal R_{\rm Hecke}\cup\mathcal R_{\rm sym}` ✓
　（说明：**跨形式**关系**天然不被** `\mathcal R_{\rm Hecke}` 蕴含 ⟹ 该排除项在此卡上**几乎自动满足**，但仍须**显式写出**以避免事后争议 ✓）
【③ 两卡共用**先决缺位检查**】 判据机械化完成后，须由**第三方（唐先生）核可**方可进入 `S4-PRE-EXACT` 审核；⛔ 不得先跑后补 ✓
```

## §3 判定与建议（供裁，⛔ 我不代跑 ✓）

```
【逐项小结】 `Q1`：卡 4 **强于** 卡 2（卡 2 判据易被密度账本吸收）｜`Q2`：卡 4 **强于** 卡 2（新形式空间是既有对象空间）｜`Q3`：**两者均未超过卡 1**（`\zeta^2` 最直接）✓
【建议】 让**卡 4** 进入下一次 `S4-PRE-EXACT` 审核（**不进入计算**）✓；卡 2 **保留**但须先解决"密度蕴含空间"排除（`\S2①`）问题 ✓
【⚠️ 诚实标注】 因 `Q3` 均未超过卡 1，**本轮推进的理由是 `Q1`/`Q2` 的"可强迫性"与"对象域独立性"，而不是 RH 接口的改进** ✓
```

## §4 状态 ＋ 边界

```
$$\boxed{\text{Card 1}=\text{CLOSED};\ \text{Card 2,4}=\text{SOURCE candidates};\ \text{Card 3}=\text{CLOSED};\ D_{\rm new}=0;\ \text{COMPUTATION}=\text{LOCKED until next card passes exact }S4;\ RH\ \text{target}=\text{OPEN}}$$ ✓
【边界】 ✗ 未计算／⛔ 未排名／⛔ 未改 `S4`／⛔ 未把 `Q3` 的不足隐去／⛔ 未开新案 ✓
```
