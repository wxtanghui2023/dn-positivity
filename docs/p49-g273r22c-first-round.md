# P49-G2.7.3-R2.2c：High-Precision PSD——第一轮（dps=50, N=2）

> 2026-09-02 15:15 · λ_min = 3.76e-9 正 · float64 不可信 · 尾部未量化

## 结果（dps=50, N=2, λ=3）
- **λ_min(M) = 3.76e-9（正！）**——λ_min(M₊) = 3.76e-9——λ_min(M₋) = 4.95e-7——**Δ = +4.9e-7 > 0（even ground 弱）**
- ⚠️ **float64 给 ~1e-12——mpmath 50-digit 给 3.76e-9——差 3 个量级——float64 近零完全不可信**（近零被数值噪声淹没——float64 的 −1.5e-12"负值"是伪影——真实为正）
- **finite positivity PASS 候选**（λ_min = 3.76e-9 > 0——但需确认）

## ⚠️ 未完成项
1. **C3 尾部未量化**：T=80 截断——尾部 ∫_{80}^∞ 未报告——粗估 ~0.01/元但特征值 ~1e-9 说明实际尾部小（振荡抵消？）——**需严格量化 ‖R_T‖**（若尾部 > λ_min 尺度——3.76e-9 不可信）
2. **dps=80 超时**（mp.quad 每元 3 段——25 元——50 digits 已 ~2 分钟——80 digits 更慢）——**需矩阵共享节点优化**（唐先生的 BB* outer product 方案）
3. dps=120 未跑——sign 稳定性未确认（50/80/120 三档）

## 初步信号（需确认）
- λ_min > 0（finite positivity PASS 方向——与 Weil 正性一致但只限 tested params）
- Δ > 0（even ground——但 4.9e-7 弱）
- **但——C3 尾部不量化前——3.76e-9 的"正"不能作为证书**

## 判定
- R2.2c 第一轮初步：λ_min 正（float64 负值是伪影——真实正）
- **需 C3 尾部 + dps=80/120 确认——矩阵积分优化（共享节点）是效率关键**

## 下一步候选
- (a) C3 尾部量化（T=100/200 扫描——‖R_T‖——确认 < 1e-12）+ 矩阵共享节点积分（BB* outer——一次全矩阵）+ dps=80/120 三档 sign 稳定
- (b) 接受初步（λ_min 正——但非证书——需 C3）
- (c) 唐先生指示
