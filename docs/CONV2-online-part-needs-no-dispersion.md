# ⚠️ **更正 + 突破点的真正形态**：在线部分**自动非负** ⟹ 缺的是**零密度定理（无条件）**

**依据**：唐先生 2026-09-11 20:26（"继续"）｜**标注**：【推导】【撤回】【⭐突破点】

---

## §0 ⚠️ **撤回**：我说"缺在线相位分散界"是**错的**
```
我此前反复称：Li 转换缺"在线零点相位分散/一致界"（Σ_k cos(nθ_k)）✗ —— **错了** ✗
重新推导（用本项目已登记的 θ = π − 2·arctan(2t) ✓）：
   在线零点 ρ = 1/2 + iγ ⟹ 1 − 1/ρ = e^{iθ(γ)}，**|1−1/ρ| = 1** ✓
   ⟹ 其贡献 = 1 − e^{inθ(γ)}，而 **Re[1 − e^{inθ}] = 1 − cos(nθ) ≥ 0** ✓✓
   ⟹ 且由 ±γ 配对，虚部 sin(nθ(γ)) + sin(nθ(−γ)) = sin(nθ) − sin(nθ) = 0 ✓（λ_n 实 ✓）
```
$$\boxed{\lambda_n^{\text{在线}} = \sum_{\gamma}\bigl[1-\cos(n\theta(\gamma))\bigr]\ \ge\ 0\quad\text{（逐项非负，无需任何分布界）}}$$
```
⟹ **在线部分不需要任何"分散界"** ✓✓ —— 那一整条"缺均匀性"的说法在【这一处】是错的 ✗
```

## §1 ⭐ **真正的结构**
```
只有 **β < 1/2** 的零点才会造成负贡献（因 |1−1/ρ| > 1 ⟺ β < 1/2 ✓）✓
   其对数模：log|1−1/ρ| ≈ |β−1/2| / |ρ|²  ⟹ **|w|^n ≈ e^{n|δ|/γ²}** ✓（危险项）
   其配对伙伴（1−β > 1/2）贡献 ≥ 0 ✓（无害 ✓）
   ⟹ 一对的贡献 ≈ **− e^{n|δ|/γ²}**（危险项主导 ✓）
```
$$\boxed{\lambda_n\ \ge\ \underbrace{\sum_{\gamma\le T}\bigl[1-\cos(n\theta_\gamma)\bigr]}_{\text{已验证零点的贡献：可算、}\ge0}\ -\ \underbrace{\sum_{\text{离轴},\,\gamma>T} e^{\,n|\delta_\rho|/\gamma_\rho^{2}}}_{\text{需要【控制个数】}}}$$
## §2 ⭐⭐⭐ **突破点的真正形态：需要的输入是【零密度定理】——而它是【无条件】的** ✓✓
```
被减项（离轴零点之和）需要：对 γ > T 的离轴零点计数/求和的控制 ⟹ **正是 N(σ,T) 型零密度定理** ✓✓
   —— 经典零密度定理（Ingham 等）**无条件成立** ✓✓ —— **这正是本项目此前没有用过的工具** ✓✓
⟹ **转换方案（可陈述）**：
   ① 由 RH 已验证到 T ⟹ 一切危险零点满足 γ > T ✓
   ② 用无条件的零密度界控制 γ > T 处离轴零点的**计数** ✓
   ③ 与第①项（由已验证零点**直接计算**的在线和 ✓）比较 ⟹ 得 **λ_n ≥ 0 对 n ≤ N(T)** ✓✓
```
## §3 **为什么这可能真的走通**（量级直观）
```
第①项量级：每零点平均贡献 1（cos 的长期均值 0 ✓）⟹ **≈ N(T) ≈ (T/2π)log T** ✓✓（极大 ✓）
第②项量级：离轴零点数目由零密度界控制（稀疏 ✓），每项 ~ e^{n|δ|/γ²}
⟹ 只要 n 不超过 ~ (γ²/|δ|)·log(γ²/|δ|)-量级，第②项远小于第①项 ✓✓
⟹ **平衡点即 N(T)**（本项目先前的 n* ≈ 6.6γ²/|δ| 阈值在此重新出现 ✓✓）
```

## §4 与前几轮结论的关系（诚实）
```
✓ 保留：**阈值律 n* ≈ 6.6γ²/|δ|**（本项目的核验 ✓）；**Voros 的无条件趋势**（math/0506326 ✓）
✗ 撤回：**"缺在线相位分散界"**（§0 ✓）—— 在线部分自动非负 ✓
⬆️ 新增：**真正缺的输入 = 零密度定理（无条件 ✓）** ⟹ 比"均匀性"更**可获得** ✓✓
⚠️ 需核：① 第①项的**下界**（振荡！需用长期均值或 Voros 趋势 ✓）
   ② 第②项能否被密度界定住**加权和**（不只计数 ✓）——**这是关键待检步骤** ✗
```

## §5 下一步（具体）
```
① 写清平衡式：以显式零密度界（Ingham 型 N(σ,T) ≪ T^{3(1−σ)/(2−σ)+ε} ✓）代入，
   求 N(T) 的显式表达式 ⟹ 得一条**可陈述的转换定理**（若平衡成立 ✓）
② 检查第②项的加权和（指数权 × 稀疏性）是否收敛/有界 ⟹ **这是能否成立的关键** ✗
```

## §6 边界
```
【推导】§0 的逐项非负与配对消虚部；§1 的结构；§2–§3 的方案与量级
【撤回】§0 对"缺相位分散界"的撤回 ✓
【外部·引用】零密度定理（Ingham 型）为经典引用 ⚠️（**未取原文** ✗，按 P9 标注）
【未做】§5 的平衡式**未算** ✗；未输入 1/2；未构造模型；未改 L2；未声称任何证明
```
## §7 提交链
```
P9c（cdeb657 Farmer + 突破点）→ 本篇（撤回 + 真正突破点：零密度输入）
```
