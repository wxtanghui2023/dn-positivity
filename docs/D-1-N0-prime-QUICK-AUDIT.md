已查地图：未覆盖（关键词: 代价模型|加法链|整数复杂度）—— 可开档，首行须照抄本行
　（⚠️ `参数化审计` 3 档＝档案已有（`V221-pointwise-escape-parametrization-audit` 等）**异义** ⟹ 不主张 ✓）

# **`D-1 / N0′` 快审**：整数复杂度的"代价模型"是否已被成熟理论同类参数化？

**唐先生令（2026-09-23 08:53）**：
① **(a)**，但**严格限定为一次低成本 `N0′` 快审**，**不是重新启动 `E-10`** ✓
② **只答一问**：$$\boxed{\theta=(a,b)\text{ 的"代价模型"是否已经被成熟理论作为同类参数系统使用？}}$$ ✓
③ **重点查五项**：① Selfridge 缺陷猜想**自身采用的复杂度/代价定义**｜② 是否已有**参数化的整数复杂度族**｜
　③ `(a,b)` 是否只是已有**加权复杂度／生成元成本／加法长度**的换记｜④ `\|mn\| \le \|m\|+\|n\|` 是否已属成熟 **subadditive complexity framework**｜
　⑤ ⭐ **最关键**：**改变 `(a,b)` 是否真的改变研究对象的数学内容，还是只改变度量／权重／坐标** ✓✓
④ **判定规则**：`N0′` FAIL ⟹ `D-1 = GAP/WATCHLIST`，**直接停，不做 `N1–N7`**；PASS ⟹ **也不立即 Promote**，只进 `N1→N5` ✓
⑤ ⚠️ **本门标准应比 `C-A` 更严**：⛔ **不能因为文献没有恰好写成 `(a,b)` 就算 PASS**；须做**同构/等价参数化审计** ✓✓
⑥ 主线状态：$$\boxed{E\text{-}10 = \mathrm{PAUSED}}$$；失败 ⟹ **正式冻结 `E-10`，转其他主线** ✓

D0: 本档对象 = **`D-1`（整数复杂度＋代价模型）的 `N0′` 快审**（档案 0 覆盖 ⟹ 新档；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算／零数值／未写程序**／未做数值实验／未算零点／未接 ζ／未找 `1/2`／未证 RH ✓

---

## §1 文献矩阵（快审核心数据 ✓✓）

| 项 | 发现 | 判定 |
|:--|:--|:--:|
| **① Selfridge 缺陷猜想的代价定义** | 用**标准 `\|n\|`**（原子 `1`，运算 `+`、`×`，**单位代价**）；**缺陷 `\delta(n)=\|n\|-3\log_3 n` 已是命名对象**（Altman：`Integer complexity: the integer defect`，`arXiv:1804.07446`；**缺陷集良序、序型 `\omega^\omega`** ✓） | ⚠️ **命名对象** |
| **② 参数化整数复杂度族** | ⛔ **已有**：「**On a generalization of addition chains: Addition–multiplication chains**」（*Discrete Math.* **308(4)**, 2008, 611–616）✓✓ —— **正是"加法链 ↔ 加法-乘法链"之间的插值族** ✓ | ⛔ **同构命中** |
| **③ 已有加权／成本／加法长度** | **加法链**（`l(n)`）＝**独立成熟方向**（Knuth TAOCP §4.6.3；**有专门文献目录**：addition chain bibliography ✓）；**加法-减法链**、**星链/窗法/Brauer 链**等**命名变体**齐全 ✓✓ | ⛔ **命名对象** |
| **④ subadditive framework** | `\|mn\| \le \|m\|+\|n\|` **由定义直接得出**（把 `m`、`n` 的表示**拼接**即得 `mn` 的表示）⟹ ⚠️ **不是非平凡可证结构，而是定义级的平凡次加性** ✓（**自纠：`E-10-ROUND2.5` 把它列为 `N5` 结构属偏弱** ✓） | ⚠️ **定义级（非 N5 结构）** |
| **⑤ ⭐ 改变 `(a,b)` 是否改数学内容** | ⛔ **改变 `(a,b)` 正是改变"哪些运算被允许及其权重"** —— 文献中这**就是**"链类"参数化（仅加法／加法+乘法／加减法／窗法）⟹ **只改变度量/权重/坐标** ✓✓ | ⛔ **命中禁止项** |

## §2 等价/同构审计（**比 `C-A` 更严的标准** ✓✓）

```
【问题】 `(a,b)` 是否只是已有参数系统的换记？
【审计】 文献的参数系统 ＝ **"允许哪些运算、各计多少代价"** ⟹ 其**参数空间**正是：
　{仅加法（加法链 `l(n)`）} ⊂ {加法+乘法（addition–multiplication chains）} ⊂ {加/减/乘} ⊂ {窗法/星链/…}
　并与 **`k` 阶（迭代）**、**原子集合**、**权重** 等合流 ✓
　⟹ `(a,b)` 只是**该参数空间的一个坐标化写法** ⟹ **同构成立** ⟹ **非新参数化对象** ✓✓
【⛔ 结论（依令 ⑤）】 **不能**因文献未恰好写成 `(a,b)` 而给 PASS ⟹ $$\boxed{N0'\ \mathrm{FAIL}}$$ ✓✓
```

## §3 判定

```
【`N0′` 判定】 ⛔ **FAIL**（②③⑤ 三项皆是硬命中；⑤ 尤其直接命中"只改变度量/权重"的禁止项 ✓✓）
【依令处置】 $$\boxed{D\text{-}1 = \mathrm{GAP}/\mathrm{WATCHLIST}}$$ —— **直接停止，不做 `N1–N7`** ✓✓
【附加诚实发现】 即便 `N0′` 侥幸通过，其 `N5` 也**偏弱**：`\|mn\| \le \|m\|+\|n\|` 是**定义级平凡次加性**（拼接表示），**非非平凡结构** ⚠️
　（此项为本档对 `E-10-ROUND2.5` §2 的**自纠** ✓✓）
【校准价值】 这是 **`N0′` 门第 2 次命中**（第 1 次＝`C-A` 的 `D_S`）⟹ $$\boxed{\text{「自然参数＋独立问题」型候选的两个样本，全部倒在「参数已有命名对象」上}}$$ ✓✓
【复活条件】 若后续发现**加法链／加法-乘法链文献之外**的参数化（例如与**代价无关**的结构性参数），可复活 ✓
```

## §4 **`E-10` 正式冻结**（依令：失败 ⟹ 正式冻结并转其他主线 ✓✓）

```
【状态】 $$\boxed{E\text{-}10 = \mathrm{FROZEN}\ (\mathrm{PAUSED})}$$ ✓✓
【冻结理由（两条，皆为验证过的筛选器输出）】
　① `C-A`（算术导数动力学）：`N0′` FAIL —— `\theta` ＝ 已有命名对象 `D_S` ✓
　② `D-1`（整数复杂度＋代价模型）：`N0′` FAIL —— `\theta` ＝ 已有链类参数化 ✓
　⟹ 候选池在 `N0′` 门下**耗尽**；**宁可暂停，不硬推**（依 `E-14` 战略纪律 ✓）
【⭐ 冻结 ≠ 关闭】 筛选器（`N0′`＋`N1–N7`＋五出口＋去 RH 化测试）**已成型并两度验证** ⟹ 留作**长期可复用准入标准** ✓✓
　一旦出现**参数非已有命名对象**的候选，可**立即重启**（无需重建标准）✓
【下一主线指针（不擅自动手）】 待唐先生定；可选：**(i)** 回归 `C-380` 活支 `T1a`（`C380-FINAL-STAGE-REPORT` §：`T1 = T1a only`；矩/T-system 路线 CLOSED-NEGATIVE ✓）｜
　**(ii)** 归档侧其他未结线｜**(iii)** 暂停研究线、转入工程/角色任务 ✓
```

## §5 边界与回查（✗✓）

```
✗ 零计算／零数值／未写程序／未做数值实验／未算零点／未接 ζ／未找 `1/2`／未证 RH／**未开案** ✓
✗ ⛔ 未因"文献未写成 `(a,b)`"给 PASS（依令 ⑤ ✓）；⛔ 未 Promote `D-1` ✓
⚠️ 文献为 **Web Search 提取级（untrusted，未逐字核）**：Altman `arXiv:1804.07446`／Altman 博论（Michigan 2014）／
　`Discrete Math. 308(4) 611–616 (2008)`／addition chain bibliography（Bielefeld）✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 加法链 命中文件数=0 ⟹ **本档新增** ✓
　技术词 代价模型 命中文件数=1 ⟹ **档案已有（本仓自击：`E-10-ROUND2.5`）⟹ 实质新** ✓
　技术词 整数复杂度 命中文件数=1 ⟹ **同上，实质新** ✓
　技术词 参数化审计 命中文件数=3 ⟹ **档案已有（异义）⟹ 不主张** ✓
【地图回查】 关键词 3 项 = **未覆盖** ⟹ 可开档 ✓
【档级引用】 `E-14`（`N0′` 门）✓｜`E-10-ROUND2.5-candidate-regeneration.md`（`D-1` 提案 ＋ 本档 §3 自纠）✓｜`C380-FINAL-STAGE-REPORT.md`（`T1a` 活支指针）✓
