# E45 — 自建"近-CUE 法"：把不可复核的输入变成**我们自己的**（2026-09-13，阶段报告）

**任务来源** ✓：唐先生 2026-09-13 09:53 指令"先做 E45"。**目的** ✓：E44 发现 0.682 天花板的唯一数值输入
（`EnclOK`）依赖**未公开的法数据文件** ✗ ⟹ 本项尝试**自建法**，让这一步不再依赖作者的材料 ✓。

**本阶段结论（一句话）** ✓：**四件确定的东西** ✓（原④的未决问题**已在 §9 解决 ✓**）——
① 前沿定理**对 N 参数化** ✓（我方实例**原理上可行** ✓）；
② 法设计问题 = **权重线性可行性（LP）** ✓，代数权重**能数值达到斜坡** ✓；
③ **有理权重不可能** ✓（§3 的"分裂判据"，在三族上穷尽）；
④ ✅ **已解（§9 ✓）：代数权重能【精确】达到斜坡 ⟹ τ = 0 可达** ✓✓
   （N = 4 与 N = 6 已构造并**从零精确复核** ✓；且 S(j) = j/N 对 j = 1..N 全成立 ✓，
    ⟹ 定理输入 (τ, d₁) = **(0, 1/(2N))** **全有理** ✓ —— E45 的目标达成 ✓）

**标签** ✓：核验 = 本项算得 ✓｜引用 = 读到的源码/论文 ✓｜推导 = 本项推理 ✓｜负面 = 判否 ✓｜未决 = 明确标出 ✓。
**脚本** ✓：`scripts/E45_nearcue_law_probe.py` ✓｜`E45_nearcue_rational_law.py` ✓｜`E45_nearcue_exact_law.py` ✓
｜`E45_nearcue_consistency_check.py` ✓（各含 `.txt` ✓）

---

## 0. ⭐ 结构发现（引用 ✓ —— 本阶段最重要的正面收获）

```
[引用] 仓库 `Zeta23/PairCeiling/NearCUE.lean` ✓：
   theorem ceiling_nearCUE (hN : 0 < N) (S : ℕ → ℝ) {τ d₁ : ℝ} (hτ : 0 ≤ τ) (hS : NearCUE S N τ)
     (hD1 : |Dfun (massOf S N) N 1| ≤ d₁) … :
     c₀ + ∫₀¹ r·x ≤ p + d₁|r(1)| + (1/(6N²) + τ/(2N))(|r′(1)| + ∫|r″|)
   def NearCUE S N τ := ∀ j, 1 ≤ j → j < N → |N·S(j) − j| ≤ τ
【涵义 ✓】① 定理**对 N 参数化** ⟹ 0.682 那一版只是 **N = 256 的一个实例** ✓
   ② `NearCUE` 恰好就是**斜坡条件** ✓ ⟹ 只要我方给出满足它的 S，定理即可**直接套用** ✓
   ③ ⟹ **"唯一数值输入"不是定理的内在属性** ✓，而是**那一个实例**的属性 ✓ —— 这正是 E45 的立足点 ✓
⚠️ 此前我方档案（含 E8/HOT-STATE）**均未记录**"定理参数化"这一点 ✗（E8 因 §7.2 未读而止步）⟹ 本项补上 ✓
```

## 1. 法设计问题（推导 ✓ —— 把问题变成有限 LP）

```
法 ✓ = 有限混合：配置 c（位置 x ∈ [0,N) 有理、标 m ∈ {1,2}、每构型 Σm = N），权重 w_c ≥ 0、Σ w_c = 1 ✓
网格形状因子 ✓：S(j) = (1/N)·E|F(j)|²，F_c(j) = Σ_k m_k exp(2πi j x_k/N)
要求 ✓（= NearCUE 取 τ = 0）：  Σ_c w_c |F_c(j)|² = j，  j = 1..N−1
⟹ ⭐ **这是关于权重 w 的线性可行性问题** ✓（|F_c(j)|² 是已知系数）⟹ **可用 LP 判定/求解** ✓
⟹ 若得 **τ = 0**，则定理的常数变成 1/(6N²)（无 τ 项），且**数值输入退化为纯代数恒等式** ✓✓
```

## 2. 实（代数）权重：能数值达到 ✓（核验）

```
脚本 `E45_nearcue_law_probe.py` ✓（N = 4,5,6,8,12,16；位置在网格 m/(2N)；单位标 + 标2 两族）
【结果 ✓】全部 N：**最大残差 = 0.000e+00**（机器精度）✓，且**支持很小** ✓：
   N = 4：**2 个配置**｜N = 5：5 个｜N = 6：6 个｜N = 8：7 个｜N = 12：12 个｜N = 16：16 个
【涵义 ✓】斜坡在**凸包**内（非仅线性张成）⟹ 法**确实存在**（数值意义）✓，构造**不神秘** ✓
⚠️ 但见 §3、§4：这个"0"是**浮点意义**的 ✓ —— 它**不能**推出 τ = 0 ✓
```

## 3. 有理权重：**不可能** ✓（核验/负面 —— 三族穷尽）

```
【判据（推导 ✓）】位置在 Q = 2N 网格上 ⟹ 每个 |F_c(j)|² 属于二次域 ✓：
   N=4: Q(√2)｜N=6: Q(√3)｜N=5: Q(√5) ⟹ 写 |F_c(j)|² = a_cj + b_cj√D，a,b **有理** ✓
   **有理权重**下"值 = j" ⟺ **两个有理等式同时成立** ✓：Σ w_c a_cj = j **且** Σ w_c b_cj = 0
【结果 ✓】`E45_nearcue_rational_law.py`（**全族穷尽** ✓）：N = 4（98 构型）✗｜N = 5（252）✗｜N = 6（1144）✗
   ⟹ **三族一致 INFEASIBLE** ✗ ⟹ 这些网格上**不存在有理权重的精确斜坡法** ✓
【定位 ✓】`E45_nearcue_consistency_check.py`（N = 4，70 构型全族）：
    E1 仅"有理部分"匹配：**INFEASIBLE** ✗｜E2 仅"根号部分"归零：**feasible** ✓
    单行 j = 1 / 2 / 3 **各自都可行** ✓ —— 失败在**联合系统** ✓（一个权重向量要同时满足所有 j）
    有理部分的取值集 = **{0, 2, 4, 8}**（全偶）✓（⚠️ 但"偶"**不是**障碍：偶数的凸组合可以是奇数 ✓）
```

## 4. ⭕→✅ **未决 → 已解**（§9 ✓）

```
> ✅ **已解（§9 ✓）：成立** —— 代数权重**能**精确达到斜坡 ⟹ **τ = 0 可达** ✓（N = 4、N = 6 已构造并精确复核 ✓）。
> 以下保留当时的推理**原样** ✓（含一处已撤回的错误断言 ✓），以便留痕 ✓。

❓ **代数权重能否使 Σ_c w_c|F_c(j)|² = j 精确成立？**
   关键逻辑（本项自查修正的一处**先前错误** ✗）：对**实权重**，"分裂判据"**不适用** ✓ ——
   因为 (Σ w a − j) + (Σ w b)√D = 0 是**一个实方程**，**两个括号可以都不为零**（例如 b 部分 = 1、
   a 部分 = −√2）⟹ §3 的 INFEASIBLE **只**排除**有理权重** ✗，**不**排除实/代数权重 ✓
   ⟹ 因此**不能**由 §3 推出"τ = 0 不可能" ✗（我在本阶段中途曾如此断言，**已撤回** ✓，见 §6-③）
【性质 ✓】这是**代数问题**（不是 LP 问题）⟹ 需在数域中求解/判定 ✓
【若成立 ⟹ 收获 ✓】τ = 0 ⟹ 定理常数为 1/(6N²) ⟹ **数值输入 = 数域中的精确恒等式** ✓
   （代价：证书是**代数**恒等式而非有理算术 ⟹ 属"较弱但可用"的"无区间算术"形式 ✓）
【若不成立 ⟹ 收获 ✓】则任何构造都必须接受 τ > 0 ⟹ 与前沿处境**一致** ✓，
   此时 E45 的可达成版本 = **自包含复制**（自建法 + 自有严格包络）✓
```

## 5. 与 E44 的关系（推导 ✓）

```
✓ E44 的贡献不受影响 ✓：唯一数值输入 = EnclOK，且**不可**由公开材料复核 ✓（法数据未发布 ✗）
✓ E45 本阶段**没有**消除那个输入 ✓，但**改变了它的性质** ✓：
   原来："作者的 40 位区间算术，我方无法重跑" ✗
   现在："**我方可以自建法**（LP ✓）并在**自有数据**上做**自有认证** ✓" —— 无论 §4 的答案如何 ✓
⚠️ 仍未做 ✗（下一阶段）：① 判定 §4；② 若需 τ > 0，写**自有严格包络**（有理区间算术，或数域精确算术）
   ③ 把 S 代入 `ceiling_nearCUE` 做 **Lean 实例**（编译环境已验证可用 ✓，3–4 秒/文件 ✓）
   ④ 缩放到大 N（我方 LP 在 N = 256 的规模）⚠️（前沿是精确有理 LP + 证书文件 ✓）
```

## 6. 自查留痕（**本阶段 6 处** ✓ —— 全是我自己的问题，全在落盘前被抓出 ✓）

```
① 步 1 的"**精确**"是**浮点假象** ✗ —— 8000 未知数 vs 少量等式 ⟹ 欠定 ⟹ 残差自然为 0 ✓
   （我最初读作"存在精确代数解"，错 ✗；已降调为"凸包可行（数值）" ✓）
② 步 3 的"支撑上无精确解"一度看似与步 1 矛盾 ⟹ 写**对账脚本** ✓（[C]：两实现一致到 **5.77e-15** ✓
   ⟹ 差异是**数学**的，不是 bug ✓）⭐ 习惯再次生效：**结果"太好"时先怀疑实现** ✓
③ ⭐ **分裂判据的适用范围搞错** ✗ —— 我一度认为它判定"任何实权重"的精确性并据此断言"τ = 0 不可达" ✗
   正确 ✓：它**只**判定**有理权重**（两括号须各自有理 ⟹ 需要 w 有理 ✓）
   ⟹ 已**撤回**该断言并在两个脚本内写明更正 ✓（§4）
④ [D] 首版把"绝对值 ≤ δ"错写成"绝对值 **=** δ" ✗（对约束方向理解错）⟹ 已改写为正确的 min-max LP ✓
⑤ [G] 首版用"**偶数凸组合必为偶数**"解释 floor = 1 ✗ —— 该命题**假** ✓（½·0 + ½·2 = 1 ✓）
   ⟹ 已撤回该解释 ✓，改为报告**实测事实**（取值集 {0,2,4,8}，floor = 1.0）+ 标注"原因未定" ✓
⑥ [F] 首版把"有理部分 floor = 1"当作 **τ 的下界** ✗ ⟹ 已加 CAUTION ✓（实权重下它**不是** τ 的下界 ✓）
```

## 7. 本文件**不**主张什么（边界 ✓）

```
✗ 不主张 E45 已完成 ✗（§4 未决；§5 的 ②③④ 未做 ✗）
✗ 不主张"τ = 0 不可能" ✗（该断言已撤回 ✓，见 §6-③）
✗ 不主张"τ = 0 可能" ✗（未证 ✓）
✗ 不主张我方已能复现前沿的 N = 256 构造 ✗（我方 LP 只做到小 N ✓）
✗ 不涉及 RH ✓；不使用任何 RH 假设 ✓
```

## 8. 复现命令（✓）

```
python3 scripts/E45_nearcue_law_probe.py          # 步 1：凸包可行性（小 N）
python3 scripts/E45_nearcue_rational_law.py       # 步 2：有理权重判据（三族穷尽）
python3 scripts/E45_nearcue_exact_law.py          # 步 3：按步 1 的支持做精确求解（结果为否定）
python3 scripts/E45_nearcue_consistency_check.py  # 步 4：对账 + 定量 floor + 定位障碍
python3 scripts/E45_nearcue_exact_attainment.py   # 步 5：**未决问题判定**（精确可达性）
python3 scripts/E45_nearcue_instance_data.py      # 步 6：定理输入的精确数值（τ、S(N)、d₁）
python3 scripts/check_archive.py                  # R5 ✓
```

---

## 9. ⭐ **未决问题已解决：τ = 0 可达**（2026-09-13 续，步 5–6 ✓）

```
【结论 ✓】§4 的未决问题答案是**正面**的 ✓：在网格 Q = 2N 上**存在**代数权重的法，使斜坡
   Σ_c w_c |F_c(j)|² = j **精确成立** ✓ —— 已在 **N = 4 与 N = 6** 找到并**从零精确复核** ✓✓

【判据（推导 ✓）】令 w_c = u_c + t_c√D（u,t ∈ Q；D = 2,3,5）✓：
   w_c |F_c(j)|² = (u_c a_cj + D t_c b_cj) + (u_c b_cj + t_c a_cj)√D ✓
   ⟹ "Σ w_c|F_c(j)|² = j"（有理右端 ✓）**等价于两个有理方程** ✓：
      Σ_c (u a + D t b) = j   与   Σ_c (u b + t a) = 0
   加上 Σ u = 1、Σ t = 0 ⟹ **关于 (u_c,t_c) 的有理线性方程组** ⟹ **可精确判定** ✓
   非负性 w_c = u + t√D ≥ 0 事后**精确**检验 ✓：
      t = 0 ⟹ u ≥ 0｜t > 0 且 u ≥ 0 ⟹ ✓｜t > 0 且 u < 0 ⟹ D t² ≥ u²｜t < 0 ⟹ u ≥ 0 且 u² ≥ D t²
   ⚠️ 注意 w_c = 0 ⟺ u_c = t_c = 0（u,t 有理 ✓）⟹ 零权重恰是零对 ✓

【两个精确法 ✓（`E45_nearcue_instance_data.py` 从零复核 ✓）】
   N = 4（Q(√2)，**2 个构型**）：{0,2,6,7} 与 {0,3,5,7}（单位标），权重 **(1/2 ∓ √2/4)** ✓
   N = 6（Q(√3)，6 个构型）：−11/30+(4/15)√3｜28/15−(14/15)√3｜−1/3+(1/3)√3｜
      8/15−(4/15)√3｜−19/30+(2/5)√3｜−1/15+(1/5)√3（**全非负** ✓，和 = 1 ✓）
   N = 5（Q(√5)）：支持 5..9、每档 4000 个随机支持**未命中** ⚠️ ⟹ 该网格存疑（**证据** ≠ 证明 ✓）

【⭐ 意外收获：结构比预期干净 ✓】两例都有 **E|F(N)|² = N 恰好** ✓ ⟹ **S(N) = 1** ✓ ⟹
   **S(j) = j/N 对 j = 1..N 全部成立** ✓（**边界行也在斜坡上** ⟹ 边界行不再是自由参数 ✓）
   ⟹ C(1) = (N+1)/(2N) ✓、**D(1) = 1/(2N) 精确有理** ✓ ⟹ d₁ = **1/8**（N=4）/ **1/12**（N=6）✓

【⟹ E45 目标达成 ✓】定理输入 = (τ, d₁) = **(0, 1/(2N))** —— **全为有理数** ✓✓
   数值输入从"前沿的 40 位区间算术（**不可复核** ✗）"变为"**精确恒等式 + 有理界**" ✓
   定理结论常数：**1/(6N²) + τ/(2N) → 1/(6N²)** ✓（τ 项**消失** ✓）

【与 E44 的最终关系 ✓】E44 的缺口**在我方实例上已消除** ✓；对**前沿那一个实例**仍不可复核 ✗
   （其法数据未公开 ✓）—— 但"**天花板机制**能否独立建立"这一问题**已回答：能** ✓
```

## 10. 仍未做 / 下一步（✗ 诚实列清）

```
① ⭐ **Lean 实例化**（`ceiling_nearCUE` 代入 τ = 0、d₁ = 1/(2N) ✓；环境已验证可用 ✓ 3–4 秒/文件 ✓）
   需注意 ✓：该定理还有 **hvalid**（证书对法逐构型有效）与 **p**（法的简单点比例）两个假设 ✓
   ⟹ 完整"实例"还需构造一个**在该法上有效**的证书 (c₀, r) ⚠️（尚未做 ✗）
② 大 N 推广（N = 8 …；与 N = 256 的差距 ⚠️：前沿是精确有理 LP + 证书文件 ✓）
③ N = 5 之谜（Q(√5) 为何未命中：偶 N 有标 2 族、奇 N 没有 ⟹ 候选原因 ⚠️ 未验证 ✗）
④ 边界行"**恰好**落在斜坡上"（E|F(N)|² = N）的原因 ⚠️ —— 两例均出现 ⟹ 疑为结构必然 ✓ 未证 ✗
```

## 11. 本轮新增自查 **2 处** ✓（均为我自己的错，均已在落盘前修正 ✓）

```
⑦ 非负判定**过保守** ✗ —— 步 5 首版要求 u ≥ 0 时必 u² ≥ D t²（对 t > 0 的情形过强 ✓）
   ⟹ 会**误拒合法权重**（漏解）⚠️；已修为完整分情形 ✓，并**重跑**验证 ✓
   ⭐ 关键 ✓：**N = 5 的否定结论在修正前后一致** ✓ ⟹ 不是该 bug 造成的假象 ✓
⑧ 同名判定**另一版漏一种情形** ✗ —— 步 6 首版只列三种情形，把合法的 **1/2+(1/4)√2** 报成负 ✗
   ⟹ 已修为单一函数、四处情形齐全 ✓
⟹ 教训 ✓：**同一小判定写两遍就会出现两套 bug** ⚠️ ⟹ 应当**单一实现 + 单一调用** ✓
```
