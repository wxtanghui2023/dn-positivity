# E45 — 自建"近-CUE 法"：把不可复核的输入变成**我们自己的**（2026-09-13，阶段报告）

**任务来源** ✓：唐先生 2026-09-13 09:53 指令"先做 E45"。**目的** ✓：E44 发现 0.682 天花板的唯一数值输入
（`EnclOK`）依赖**未公开的法数据文件** ✗ ⟹ 本项尝试**自建法**，让这一步不再依赖作者的材料 ✓。

**本阶段结论（一句话）** ✓：**四件确定的东西** ✓（原④的未决问题**已在 §9 解决 ✓**）——
① 前沿定理**对 N 参数化** ✓（我方实例**原理上可行** ✓）；
② 法设计问题 = **权重线性可行性（LP）** ✓，代数权重**能数值达到斜坡** ✓；
③ **有理权重不可能** ✓（§3 的"分裂判据"，在三族上穷尽）；
④ ✅ **已解（§9 ✓）：代数权重能【精确】达到斜坡 ⟹ τ = 0 可达** ✓✓
   （N = 4 与 N = 6 已构造并**从零精确复核** ✓；且 S(j) = j/N 对 j = 1..N 全成立 ✓，
    ⟹ 定理输入 (τ, d₁) = **(0, 1/(2N))** **全有理** ✓ —— E45 的目标达成 ✓）

**标签** ✓：核验 = 本项算得 ✓｜引用 = 读到的源码/论文 ✓｜推导 = 本项推理 ✓｜负面 = 判否 ✓｜未决 = 明确标出 ✓。
**脚本** ✓：`scripts/E45_nearcue_law_probe.py` ✓｜`E45_nearcue_rational_law.py` ✓｜`E45_nearcue_exact_law.py` ✓
｜`E45_nearcue_consistency_check.py` ✓（各含 `.txt` ✓）

---

## 0. ⭐ 结构发现（引用 ✓ —— 本阶段最重要的正面收获）

```
[引用] 仓库 `Zeta23/PairCeiling/NearCUE.lean` ✓：
   theorem ceiling_nearCUE (hN : 0 < N) (S : ℕ → ℝ) {τ d₁ : ℝ} (hτ : 0 ≤ τ) (hS : NearCUE S N τ)
     (hD1 : |Dfun (massOf S N) N 1| ≤ d₁) … :
     c₀ + ∫₀¹ r·x ≤ p + d₁|r(1)| + (1/(6N²) + τ/(2N))(|r′(1)| + ∫|r″|)
   def NearCUE S N τ := ∀ j, 1 ≤ j → j < N → |N·S(j) − j| ≤ τ
【涵义 ✓】① 定理**对 N 参数化** ⟹ 0.682 那一版只是 **N = 256 的一个实例** ✓
   ② `NearCUE` 恰好就是**斜坡条件** ✓ ⟹ 只要我方给出满足它的 S，定理即可**直接套用** ✓
   ③ ⟹ **"唯一数值输入"不是定理的内在属性** ✓，而是**那一个实例**的属性 ✓ —— 这正是 E45 的立足点 ✓
⚠️ 此前我方档案（含 E8/HOT-STATE）**均未记录**"定理参数化"这一点 ✗（E8 因 §7.2 未读而止步）⟹ 本项补上 ✓
```

## 1. 法设计问题（推导 ✓ —— 把问题变成有限 LP）

```
法 ✓ = 有限混合：配置 c（位置 x ∈ [0,N) 有理、标 m ∈ {1,2}、每构型 Σm = N），权重 w_c ≥ 0、Σ w_c = 1 ✓
网格形状因子 ✓：S(j) = (1/N)·E|F(j)|²，F_c(j) = Σ_k m_k exp(2πi j x_k/N)
要求 ✓（= NearCUE 取 τ = 0）：  Σ_c w_c |F_c(j)|² = j，  j = 1..N−1
⟹ ⭐ **这是关于权重 w 的线性可行性问题** ✓（|F_c(j)|² 是已知系数）⟹ **可用 LP 判定/求解** ✓
⟹ 若得 **τ = 0**，则定理的常数变成 1/(6N²)（无 τ 项），且**数值输入退化为纯代数恒等式** ✓✓
```

## 2. 实（代数）权重：能数值达到 ✓（核验）

```
脚本 `E45_nearcue_law_probe.py` ✓（N = 4,5,6,8,12,16；位置在网格 m/(2N)；单位标 + 标2 两族）
【结果 ✓】全部 N：**最大残差 = 0.000e+00**（机器精度）✓，且**支持很小** ✓：
   N = 4：**2 个配置**｜N = 5：5 个｜N = 6：6 个｜N = 8：7 个｜N = 12：12 个｜N = 16：16 个
【涵义 ✓】斜坡在**凸包**内（非仅线性张成）⟹ 法**确实存在**（数值意义）✓，构造**不神秘** ✓
⚠️ 但见 §3、§4：这个"0"是**浮点意义**的 ✓ —— 它**不能**推出 τ = 0 ✓
```

## 3. 有理权重：**不可能** ✓（核验/负面 —— 三族穷尽）

```
【判据（推导 ✓）】位置在 Q = 2N 网格上 ⟹ 每个 |F_c(j)|² 属于二次域 ✓：
   N=4: Q(√2)｜N=6: Q(√3)｜N=5: Q(√5) ⟹ 写 |F_c(j)|² = a_cj + b_cj√D，a,b **有理** ✓
   **有理权重**下"值 = j" ⟺ **两个有理等式同时成立** ✓：Σ w_c a_cj = j **且** Σ w_c b_cj = 0
【结果 ✓】`E45_nearcue_rational_law.py`（**全族穷尽** ✓）：N = 4（98 构型）✗｜N = 5（252）✗｜N = 6（1144）✗
   ⟹ **三族一致 INFEASIBLE** ✗ ⟹ 这些网格上**不存在有理权重的精确斜坡法** ✓
【定位 ✓】`E45_nearcue_consistency_check.py`（N = 4，70 构型全族）：
    E1 仅"有理部分"匹配：**INFEASIBLE** ✗｜E2 仅"根号部分"归零：**feasible** ✓
    单行 j = 1 / 2 / 3 **各自都可行** ✓ —— 失败在**联合系统** ✓（一个权重向量要同时满足所有 j）
    有理部分的取值集 = **{0, 2, 4, 8}**（全偶）✓（⚠️ 但"偶"**不是**障碍：偶数的凸组合可以是奇数 ✓）
```

## 4. ⭕→✅ **未决 → 已解**（§9 ✓）

```
> ✅ **已解（§9 ✓）：成立** —— 代数权重**能**精确达到斜坡 ⟹ **τ = 0 可达** ✓（N = 4、N = 6 已构造并精确复核 ✓）。
> 以下保留当时的推理**原样** ✓（含一处已撤回的错误断言 ✓），以便留痕 ✓。

❓ **代数权重能否使 Σ_c w_c|F_c(j)|² = j 精确成立？**
   关键逻辑（本项自查修正的一处**先前错误** ✗）：对**实权重**，"分裂判据"**不适用** ✓ ——
   因为 (Σ w a − j) + (Σ w b)√D = 0 是**一个实方程**，**两个括号可以都不为零**（例如 b 部分 = 1、
   a 部分 = −√2）⟹ §3 的 INFEASIBLE **只**排除**有理权重** ✗，**不**排除实/代数权重 ✓
   ⟹ 因此**不能**由 §3 推出"τ = 0 不可能" ✗（我在本阶段中途曾如此断言，**已撤回** ✓，见 §6-③）
【性质 ✓】这是**代数问题**（不是 LP 问题）⟹ 需在数域中求解/判定 ✓
【若成立 ⟹ 收获 ✓】τ = 0 ⟹ 定理常数为 1/(6N²) ⟹ **数值输入 = 数域中的精确恒等式** ✓
   （代价：证书是**代数**恒等式而非有理算术 ⟹ 属"较弱但可用"的"无区间算术"形式 ✓）
【若不成立 ⟹ 收获 ✓】则任何构造都必须接受 τ > 0 ⟹ 与前沿处境**一致** ✓，
   此时 E45 的可达成版本 = **自包含复制**（自建法 + 自有严格包络）✓
```

## 5. 与 E44 的关系（推导 ✓）

```
✓ E44 的贡献不受影响 ✓：唯一数值输入 = EnclOK，且**不可**由公开材料复核 ✓（法数据未发布 ✗）
✓ E45 本阶段**没有**消除那个输入 ✓，但**改变了它的性质** ✓：
   原来："作者的 40 位区间算术，我方无法重跑" ✗
   现在："**我方可以自建法**（LP ✓）并在**自有数据**上做**自有认证** ✓" —— 无论 §4 的答案如何 ✓
⚠️ 仍未做 ✗（下一阶段）：① 判定 §4；② 若需 τ > 0，写**自有严格包络**（有理区间算术，或数域精确算术）
   ③ 把 S 代入 `ceiling_nearCUE` 做 **Lean 实例**（编译环境已验证可用 ✓，3–4 秒/文件 ✓）
   ④ 缩放到大 N（我方 LP 在 N = 256 的规模）⚠️（前沿是精确有理 LP + 证书文件 ✓）
```

## 6. 自查留痕（**本阶段 6 处** ✓ —— 全是我自己的问题，全在落盘前被抓出 ✓）

```
① 步 1 的"**精确**"是**浮点假象** ✗ —— 8000 未知数 vs 少量等式 ⟹ 欠定 ⟹ 残差自然为 0 ✓
   （我最初读作"存在精确代数解"，错 ✗；已降调为"凸包可行（数值）" ✓）
② 步 3 的"支撑上无精确解"一度看似与步 1 矛盾 ⟹ 写**对账脚本** ✓（[C]：两实现一致到 **5.77e-15** ✓
   ⟹ 差异是**数学**的，不是 bug ✓）⭐ 习惯再次生效：**结果"太好"时先怀疑实现** ✓
③ ⭐ **分裂判据的适用范围搞错** ✗ —— 我一度认为它判定"任何实权重"的精确性并据此断言"τ = 0 不可达" ✗
   正确 ✓：它**只**判定**有理权重**（两括号须各自有理 ⟹ 需要 w 有理 ✓）
   ⟹ 已**撤回**该断言并在两个脚本内写明更正 ✓（§4）
④ [D] 首版把"绝对值 ≤ δ"错写成"绝对值 **=** δ" ✗（对约束方向理解错）⟹ 已改写为正确的 min-max LP ✓
⑤ [G] 首版用"**偶数凸组合必为偶数**"解释 floor = 1 ✗ —— 该命题**假** ✓（½·0 + ½·2 = 1 ✓）
   ⟹ 已撤回该解释 ✓，改为报告**实测事实**（取值集 {0,2,4,8}，floor = 1.0）+ 标注"原因未定" ✓
⑥ [F] 首版把"有理部分 floor = 1"当作 **τ 的下界** ✗ ⟹ 已加 CAUTION ✓（实权重下它**不是** τ 的下界 ✓）
```

## 7. 本文件**不**主张什么（边界 ✓）

```
✗ 不主张 E45 已完成 ✗（§4 未决；§5 的 ②③④ 未做 ✗）
✗ 不主张"τ = 0 不可能" ✗（该断言已撤回 ✓，见 §6-③）
✗ 不主张"τ = 0 可能" ✗（未证 ✓）
✗ 不主张我方已能复现前沿的 N = 256 构造 ✗（我方 LP 只做到小 N ✓）
✗ 不涉及 RH ✓；不使用任何 RH 假设 ✓
```

## 8. 复现命令（✓）

```
python3 scripts/E45_nearcue_law_probe.py          # 步 1：凸包可行性（小 N）
python3 scripts/E45_nearcue_rational_law.py       # 步 2：有理权重判据（三族穷尽）
python3 scripts/E45_nearcue_exact_law.py          # 步 3：按步 1 的支持做精确求解（结果为否定）
python3 scripts/E45_nearcue_consistency_check.py  # 步 4：对账 + 定量 floor + 定位障碍
python3 scripts/E45_nearcue_exact_attainment.py   # 步 5：**未决问题判定**（精确可达性）
python3 scripts/E45_nearcue_instance_data.py      # 步 6：定理输入的精确数值（τ、S(N)、d₁）
python3 scripts/check_archive.py                  # R5 ✓
```

---

## 9. ⭐ **未决问题已解决：τ = 0 可达**（2026-09-13 续，步 5–6 ✓）

```
【结论 ✓】§4 的未决问题答案是**正面**的 ✓：在网格 Q = 2N 上**存在**代数权重的法，使斜坡
   Σ_c w_c |F_c(j)|² = j **精确成立** ✓ —— 已在 **N = 4 与 N = 6** 找到并**从零精确复核** ✓✓

【判据（推导 ✓）】令 w_c = u_c + t_c√D（u,t ∈ Q；D = 2,3,5）✓：
   w_c |F_c(j)|² = (u_c a_cj + D t_c b_cj) + (u_c b_cj + t_c a_cj)√D ✓
   ⟹ "Σ w_c|F_c(j)|² = j"（有理右端 ✓）**等价于两个有理方程** ✓：
      Σ_c (u a + D t b) = j   与   Σ_c (u b + t a) = 0
   加上 Σ u = 1、Σ t = 0 ⟹ **关于 (u_c,t_c) 的有理线性方程组** ⟹ **可精确判定** ✓
   非负性 w_c = u + t√D ≥ 0 事后**精确**检验 ✓：
      t = 0 ⟹ u ≥ 0｜t > 0 且 u ≥ 0 ⟹ ✓｜t > 0 且 u < 0 ⟹ D t² ≥ u²｜t < 0 ⟹ u ≥ 0 且 u² ≥ D t²
   ⚠️ 注意 w_c = 0 ⟺ u_c = t_c = 0（u,t 有理 ✓）⟹ 零权重恰是零对 ✓

【两个精确法 ✓（`E45_nearcue_instance_data.py` 从零复核 ✓）】
   N = 4（Q(√2)，**2 个构型**）：{0,2,6,7} 与 {0,3,5,7}（单位标），权重 **(1/2 ∓ √2/4)** ✓
   N = 6（Q(√3)，6 个构型）：−11/30+(4/15)√3｜28/15−(14/15)√3｜−1/3+(1/3)√3｜
      8/15−(4/15)√3｜−19/30+(2/5)√3｜−1/15+(1/5)√3（**全非负** ✓，和 = 1 ✓）
   N = 5（Q(√5)）：支持 5..9、每档 4000 个随机支持**未命中** ⚠️ ⟹ 该网格存疑（**证据** ≠ 证明 ✓）

【⭐ 意外收获：结构比预期干净 ✓】两例都有 **E|F(N)|² = N 恰好** ✓ ⟹ **S(N) = 1** ✓ ⟹
   **S(j) = j/N 对 j = 1..N 全部成立** ✓（**边界行也在斜坡上** ⟹ 边界行不再是自由参数 ✓）
   ⟹ C(1) = (N+1)/(2N) ✓、**D(1) = 1/(2N) 精确有理** ✓ ⟹ d₁ = **1/8**（N=4）/ **1/12**（N=6）✓

【⟹ E45 目标达成 ✓】定理输入 = (τ, d₁) = **(0, 1/(2N))** —— **全为有理数** ✓✓
   数值输入从"前沿的 40 位区间算术（**不可复核** ✗）"变为"**精确恒等式 + 有理界**" ✓
   定理结论常数：**1/(6N²) + τ/(2N) → 1/(6N²)** ✓（τ 项**消失** ✓）

【与 E44 的最终关系 ✓】E44 的缺口**在我方实例上已消除** ✓；对**前沿那一个实例**仍不可复核 ✗
   （其法数据未公开 ✓）—— 但"**天花板机制**能否独立建立"这一问题**已回答：能** ✓
```

## 10. 仍未做 / 下一步（✗ 诚实列清）

```
① ⭐ **Lean 实例化**（`ceiling_nearCUE` 代入 τ = 0、d₁ = 1/(2N) ✓；环境已验证可用 ✓ 3–4 秒/文件 ✓）
   需注意 ✓：该定理还有 **hvalid**（证书对法逐构型有效）与 **p**（法的简单点比例）两个假设 ✓
   ⟹ 完整"实例"还需构造一个**在该法上有效**的证书 (c₀, r) ⚠️（尚未做 ✗）
② 大 N 推广（N = 8 …；与 N = 256 的差距 ⚠️：前沿是精确有理 LP + 证书文件 ✓）
③ N = 5 之谜（Q(√5) 为何未命中：偶 N 有标 2 族、奇 N 没有 ⟹ 候选原因 ⚠️ 未验证 ✗）
④ 边界行"**恰好**落在斜坡上"（E|F(N)|² = N）的原因 ⚠️ —— 两例均出现 ⟹ 疑为结构必然 ✓ 未证 ✗
```

## 11. 本轮新增自查 **2 处** ✓（均为我自己的错，均已在落盘前修正 ✓）

```
⑦ 非负判定**过保守** ✗ —— 步 5 首版要求 u ≥ 0 时必 u² ≥ D t²（对 t > 0 的情形过强 ✓）
   ⟹ 会**误拒合法权重**（漏解）⚠️；已修为完整分情形 ✓，并**重跑**验证 ✓
   ⭐ 关键 ✓：**N = 5 的否定结论在修正前后一致** ✓ ⟹ 不是该 bug 造成的假象 ✓
⑧ 同名判定**另一版漏一种情形** ✗ —— 步 6 首版只列三种情形，把合法的 **1/2+(1/4)√2** 报成负 ✗
   ⟹ 已修为单一函数、四处情形齐全 ✓
⟹ 教训 ✓：**同一小判定写两遍就会出现两套 bug** ⚠️ ⟹ 应当**单一实现 + 单一调用** ✓
```

---

## 12. Lean **状态**（2026-09-13 续 ✓）

```
【可做且已做 ✓】`lean/E45-NearCUE-Instance.lean` ✓（**exit 0 / 零 error / 零 sorry / 4 秒** ✓）
   用**仓库自己的定义**（`Csum`、`massOf`、`Dfun(1) = C(1) − 1/2` ✓ 逐字重述 ✓）形式化：
   · `nearCUE_zero` ✓：**斜坡满足 τ = 0 的 NearCUE**（定理的数值假设之一 ✓）
   · `Csum_ramp_partial` ✓：Σ_{j≤m} (j/N)/N = m(m+1)/(2N²)（归纳法 ✓）
   · `Csum_ramp` ✓：C(1) = (N+1)/(2N)
   · `D1_ramp` / `abs_D1_ramp_le` ✓：**D(1) = 1/(2N) 精确** ⟹ |D(1)| ≤ 1/(2N) = d₁ ✓
   · `instance_N4` / `instance_N6` ✓：**τ = 0、d₁ = 1/8 或 1/12、常数 1/96 或 1/216** ✓
⟹ **定理消耗的两条数值假设已在核内验证** ✓（这正是我们能机器验证的那一半 ✓）
⟹ 全目录巡检 ✓：**16 个 .lean 文件全部 exit 0 / 零 error** ✓（新文件在内 ✓）

【⚠️ 新发现：**第二处可复现性缺口** ✗】仓库 pin **toolchain `leanprover/lean4:v4.33.0-rc2`** ✓ +
   特定 **Mathlib rev `51e6992…`** ✓；而本机 `~/mathlib4` 是 **v4.33.0 正式版** ✗
   ⟹ **无法导入** `Zeta23.PairCeiling.ceiling_nearCUE` ✗（需重建那套 pinned Mathlib，数 GB ✗）
   ⟹ 与 E44 的"法数据未公开"并列 ✓：这是该仓库**第二处**"内核检验"主张的复现障碍 ✗
   ⟹ 严格说 ✓：论文的 "kernel-checked" 指**其 pin 定工具链下**的检验 ✓；第三方复核需先精确重建该环境 ✓

【未做 ✗】① 完整实例化（需 hvalid 证书 + 重建 pinned 环境 ✗）② 大 N（N = 8 …）③ N = 5 之谜 ④ 边界行之因
```

## 13. Lean 侧新增自查 **2 处** ✓（均为我的错，且已在落盘前修好 ✓）

```
⑨ `def` 在 ℝ 上用除法需 `noncomputable` ✗（**定理**内不需要 ✓，**定义**需要 ✓）⟹ 已加 `noncomputable section` ✓
⑩ 归纳步里**先 `rw` 求和引理**，而目标里求和仍被 `Csum` 包着 ⟹ `rw` 找不到模式 ✗
   ⟹ 应先 `simp only [Csum]` 展开再 `rw` ✓（同 E44 的 ② 类错误：**重写前先看目标形状** ✓）
⚙️ 环境记 ✓：`lake` 不在默认 PATH —— 需 `export PATH="$HOME/.elan/bin:$PATH"` ✓（已记入 `lean/README.md` ✓）
```

---

## 16. ⭐⭐ **再深化：边界值被【支持】锁定 ⟹ 可达集离散**（步 8–9 ✓，并**收窄 §14 的说法** ✗）

```
【问题 ✓】§14 证明边界行**不受斜坡约束**（多种值可达），但**可达集是区间还是离散集**？
   我当时的 §14 推论写的是"前沿的大边界形状**可以精确匹配**" ⚠️ —— **说强了** ✗，本例修正 ✓

【步 8（采样 ✓）】`E45_nearcue_edge_set_structure.py` ✓
   支持 5..7（N=4）与 7..9（N=6），共收集到 **16** 与 **50** 个不同边界值 ✓
   中位间隙 0.146（N=4）/ 0.0200（N=6）⟹ **看似离散** ⚠️
   ⚠️ **但步 8 本身不足以定论** ✗：我的精确解算器**总把自由变量取 0** ⟹ 每个支持只得到一个点 ✓，
      **根本探不到解族内部** ✗ ⟹ "离散"可能是方法偏差 ⚠️（此为方法学自查 ✓）

【步 9（直接优化 ✓ 定论）】`E45_nearcue_edge_interval.py` ✓ —— 固定支持上对 S(N) 做**极大/极小 LP**：
   N = 4：m = 5 ⟹ S(N) = [**1.000000000, 1.000000000**]（长度 0 ✓）｜m = 6 ⟹ [**1.292893219, 1.292893219**] ✓
   N = 6：m = 7 ⟹ [**1.666666667, 1.666666667**]（= 5/3 ✓）｜m = 8 ⟹ [**1.000000000, 1.000000000**] ✓
   ⟹ ⭐ **在每一个测试支持上，边界泛函在【整个可行族】上为常数** ✓✓（极大小 = 极小）
   ⟹ 即：**S(N) 由【支持】决定，与权重无关** ✓✓ —— 这**同时解释了步 8 的离散外观** ✓
   ⟹ 且与步 7 吻合 ✓：m=5 锁 1、m=6 锁 2−√2/2（正是步 7 那 3 个值中的两个 ✓✓）
   ⚠️ 且解释了步 7 的"S(N) = 1 最常见" ✓（出现该值的支持最多 ✓）

【⟹ 修正后的结论（取代 §14 的过强说法 ✓）】
   ① τ = 0 **可达** ✓（步 5–6 ✓）—— 不变 ✓
   ② 边界行**不被斜坡钉在 N** ✓，但被**支持锁定** ✓ ⟹ **可达集是离散集** ✗（≠ 区间）
   ③ ⟹ **不能**保证"任意给定值（如前沿的 211.43）可精确达到" ✗ —— 需找到能锁定该值的支持 ⚠️（未做）
      故正确表述 ✓："**多种**边界值可精确达到（N=4 已见 3 种、N=6 已见 28+ 种 ✓），
      但是**逐支持锁定**的离散集，而非可连续调节的区间" ✓
   ④ 前沿的 τ > 0 仍然**不是**斜坡内在要求的 ✓（精确斜坡确实存在且边界值多样 ✓），
      但其**具体那个实例**的数值仍不可复核 ✗（数据未公开 ✓）—— 与 §14 不矛盾 ✓

【步 9 的另一处我自己的错 ✓】目标里**漏了 /N** ✗ ⟹ 首版把 E|F(N)|² 当成 S(N) 报出（差因子 N ✓）
   ⟹ 已修并重跑 ✓（修正后数值与步 7 完全一致 ✓✓，这本身就是交叉验证 ✓）
   ⚠️ 另：对退化 LP，我的"把近零坐标设零再精确求解"启发式**不总是可靠** ✗
      （m=6 一处报 'negative weight' ⚠️）⟹ 已如实记入输出 ✓
```

## 17. 仍待做（✗ 再次更新）

```
① 完整实例化（解析半边：构造在该法上有效的证书 (c₀, r)）⚠️ —— 仍是最大的缺口 ✓
② 大 N（N = 8 …）：需 4 次域实现（本项两分体系只在 Q ∈ {8,10,12} 可用 ✓）
③ N = 5 之谜（Q(√5) 未命中 ⚠️ 未证）
④ **可达集是否稠密** ✓：支持越大是否值越密？（决定"能否逼近任意给定边界值" ✓）—— 未做 ✗
⑤ ⭐ **"边界值被支持锁定"能否升为定理** ✓？（现为 4 个支持上的 LP 事实 ✓）—— 未证 ✗
```

---

## 14. ⭐ **深化：边界行是否被斜坡钉死？—— 否**（步 7 ✓，唐先生 11:13 指令 ✓）

```
【为何这是尖问题 ✓】定理只约束 1 ≤ j < N（`NearCUE` ✓），而原子质量用到 j = 1..N ⟹
   **边界行 S(N) 恰好是剩下的唯一自由度** ✓；且在 τ = 0 下，定理唯一的其他输入就是
      **d₁ = |D(1)| = |S(N)/N − 1/(2N)|** ✓（由前 N−1 行的斜坡给出 C(1) = (N−1)/(2N) + S(N)/N ✓）
   前沿的法：S(256) = **211.432009…** ⟹ d₁ = **0.82395317**（恰好等于记录的边界界 ✓✓）
   ⟹ ⭐ **整个实例完全由这一个数决定** ✓

【结果 ✓（`E45_nearcue_edge_control.py` ✓，共 40 例精确法）】
   N = 4：**3 个不同**边界值 ✓
      S(4) = 1（36 次）⟹ d₁ = 0.125 ✓
      S(4) = 2 − √2/2 = 1.292893219（3 次）⟹ d₁ = 0.198223305 ✓
      S(4) = 2 + √2/2 = 2.707106781（1 次）⟹ d₁ = 0.551776695 ✓
   N = 6：**28 个不同**边界值 ✓（1.0 → 79/33 = 2.393939394），d₁ 1/12 → 0.315656566 ✓
   ⚠️ 两例的理论上限：(Σm)²/N = 16/4 = **4**（N=4）与 36/6 = **6**（N=6）⟹ **还有空间** ✓

【⟹ 结论 ✓】**边界行不受斜坡约束** ✓ ⟹ **前沿的"大边界"形状与 τ = 0 相容** ✓✓
   ⚠️ **但 §16 收窄了这一点** ✗：边界值虽多样，却**逐支持被锁定** ✓（离散集，非区间 ✗）
      ⟹ 正确读法 ✓："**原则相容**（精确斜坡 + 多种边界值确实共存 ✓），
         但具体某一个值（如 211.43）需找到能锁定它的**支持** ✓，不保证可达" ✓
   即：**他们的 τ = 3e-40 不是斜坡要求的** ✗，而是**他们自己选择的有理数据所要求的** ✓：
   步 2 已证 ✓：三族内**有理权重不可能**精确达到斜坡 ✓（分裂判据 ⟹ 两个有理系统同时成立 ✗）
   而我们用**代数（二次域）权重**就能达到 τ = 0 ✓✓

【⟹ 对 E44 的最终升级 ✓】E44 的"唯一数值输入不可复核"✗ 在原理上**可以被精确恒等式取代** ✓：
   · τ = 0 可达 ✓（步 5–6 构造 ✓）
   · 边界行可自由控制 ✓（步 7 ✓）⟹ 故**前沿那个形状（含大边界）也能做到** ✓
   · 代价 ✓：权重落在二次域（非有理）；且**解析半边（hvalid / p）仍需另做** ⚠️
   ⟹ 精确表述 ✓："该天花板的**数值部分**可以由精确代数恒等式提供（已构造两例 ✓）；
      前沿保留 τ = 3e-40 是其**有理数据 + 区间算术**路线下的结果，而非问题的内在要求" ✓
   ⚠️ 不主张 ✓：不主张前沿有错 ✗；也不主张我们已能复现 N = 256 的实例 ✗（规模与解析半边未做 ✗）
```

## 15. 仍待做（✗ 更新）

```
① 完整实例化：**构造一个在该法上有效的证书 (c₀, r)**（解析半边 ✓ 尚未做 ✗）+ 重建 pinned 环境 ✗
② 大 N（N = 8 …）：需 4 次域实现 ⚠️（步 5 的两分体系只在 φ(Q)/2 = 2 时可用 ✓，即 Q ∈ {8,10,12} ✓）
③ N = 5 之谜（Q(√5)：偶 N 有标 2 族、奇 N 无 ⟹ 候选原因 ⚠️ 未证 ✗）
④ 边界行可达集的结构 ✓：N=4 恰为 {1, 2 ∓ √2/2}（**共轭对** ✓）；N=6 已见 28 个值 ⟹
   可连续还是离散？⚠️（未定 ✗）；以及 **S(N) = N（即 1）为何最常见** ✓（36/40、13/40 ✓ 弱结构迹象 ⚠️）
```

---

## 18. ④ 可达集是否稠密 —— **不稠密** ✗（步 10 ✓，`E45_nearcue_density.py` ✓）

```
【方法 ✓】固定 N = 4；采样支持 m = 6, 8, 10, 12（各 4000 次）；记录每个可行支持的**被迫**边界值 S(4) ✓
【结果 ✓（Te先生可见数字 ✓）】
   m = 6  ：可行 1353/4000；**不同值 15 个**；范围 [1.000000, 3.000000]；最小间隙 0.023689 ✓
   m = 8  ：可行 1416/4000；**不同值 14 个**；范围 [1.0, 3.0]；最小间隙 0.023689 ✓
   m = 10 ：可行 1441/4000；**不同值 11 个**；范围 [1.0, 3.0]；最小间隙 0.025126 ✓
   m = 12 ：可行 1452/4000；**不同值 16 个**；范围 [1.0, 3.292893]；最小间隙 0.023689 ✓
   四档的**中位间隙全为 0.146447**（= (1 − √2/2)/2 ✓）⟹ 值呈 √2 型格状分布 ✓
【⟹ 结论 ✓】**不同值数目不随 m 增长、最小间隙不缩小** ⟹ **无稠密化** ✗
   即：可达边界集在这些规模上是一个**固定的稀疏离散集** ✓（非区间、也非稠密集 ✓）
【⟹ 实务含义 ✓】"匹配任意给定边界值（如前沿 N=256 的 211.43）**不保证可行**" ✗ —— 属搜索问题 ✓
   （本项**未**主张大规模也如此 ✗；但已给出**反向证据** ✓）
```

## 19. ① 完整实例化 —— **机制齐备 ✓，且与"小 p"不冲突** ✓（步 11 ✓）

```
【(A) 完整实例的算术 ✓ 已可做 ✓】`E45_nearcue_instance_completeness.py` ✓
   masses s_j = j/N² ✓；C(1) = (N+1)/(2N) = 5/8（N=4）/ 7/12（N=6）✓；τ = 0 ✓；d₁ = 1/(2N) = 1/8 / 1/12 ✓
   证书 ✓：r(x) = x（⟹ g ≡ 1、h ≡ 0、T = ∅ ✓ 可微性条件满足 ✓），c₀ = 0，
       p := Σ_j s_j r(j/N) = **15/32**（N=4）/ **91/216**（N=6）⟹ **hvalid 取等号成立** ✓
   结论 ✓：1/3 ≤ p + d₁ + 1/(6N²) ⟹ N=4：**1/3 ≤ 29/48** ✓；N=6：**1/3 ≤ 55/108** ✓ **均为真** ✓
   ⟹ **定理的全部假设都能被我们精确供给** ✓（数值部分 ✓ + 一个有效证书 ✓）
   ⚠️ 但该实例**弱** ✓：p 由我们选定，而非"法的简单点比例" ✓（见 (B) ✓）

【(B) ⚠️ 我们的法是"全单位标" ⟹ p = 1 ✗（对天花板无用）】
   天花板的界**取决于法的简单点比例 p**（p 越小界越强 ✓）；我们的记录法每构型皆单位标 ⟹ **p = 1 最大宽松** ✗
   ⟹ 用它们**无法界定证书类** ✗ —— 这是 ① 的核心发现 ✓（此前未意识到 ✓）

【(C) ⭐ 关键问题：精确斜坡是否强制 p = 1？—— **否** ✓✓（LP ✓）】
   N = 4：mark-2 构型最大可行权重 = **0.25** ⟹ 存在精确斜坡法使 **p = 0.75** ✓
   N = 6：**0.111111** ⟹ **p = 0.888889** ✓
   ⟹ **"数值半边"与"小 p"在原理上相容** ✓✓（不是二选一 ✓）
【仍开放 ✗】① p 能否压到 ≈ 0.68（前沿值）？需更大支持 ⚠️（LP 只扫到 m = N+2 ✓）
   ② ⭐ **解析半边**——构造一个值**接近 p** 的有效证书——**仍未做** ✗（这是与前沿同质实例的真正缺口 ✓）
```

---

## 20. ⭐⭐ ①-② 精确斜坡对简单点比例 p 的**下界** —— 结构性障碍（步 12 ✓）

```
【问法 ✓】把"mark-2 构型的权重"在**全族**上最大化（不采样 ✓）⟹ 直接得 p 的全局下界 ✓
   等价地：min p = 1 − max(Σ_{mark-2} w_c) ✓（因 p = Σ_{unit} w_c ✓：单位标构型贡献 1、mark-2 贡献 0 ✓）
【结果 ✓】
   N = 4（全族 98 构型：70 单位 + 28 mark-2）：max mark-2 权重 = **0.286611652** ⟹ **p_min = 0.713388348** ✓
      （最优法用 **7 个构型**，其中 **4 个是 mark-2** ✓）
   N = 6（全族 1144 构型：924 + 220）：max = **0.292663844** ⟹ **p_min = 0.707336156** ✓
      （用 **11 个构型**，含 **9 个 mark-2** ✓）
【⭐ 与前沿比较 ✓】前沿记录常数 **0.6818287** ⟹ **两个 p_min 都在它【之上】** ✗
   LP 直接判定 ✓："是否存在 p ≤ 0.6818277 的精确斜坡法？" ⟹ **NO**（N = 4 与 N = 6 **皆否** ✓）
【⟹ 结论 ✓★】**在这两个网格上，"精确斜坡（τ = 0）"与"见证天花板（需 p ≲ 0.682）"【不相容】** ✗
   ⟹ 这是一个**干净的结构性障碍** ✓（与项目的"墙"同类：不是技巧不足，而是**两目标互斥** ✓）
   ⟹ 具体含义 ✓：我们的粗网格精确法**不能**充当天花板的见证法 ✗ —— 尽管它们把数值输入做到了精确 ✓
【⚠️ 边界（诚实 ✓）】① 这是 **Q = 2N 粗网格**上的结论 ✓；**不**排除更细网格（位置分母更大）上
   p_min 降到 0.682 以下 ⚠️ —— **那正是决定性的下一个实验** ✓（需 4 次域实现 ⚠️）
   ② 前沿的法（p = 0.6818 < 我们的下界 ✓）必然**不在**这个粗网格类里 ✓（其位置分母更大 ✓）
   ⟹ 故两者不矛盾 ✓：只说明"**我们的**构造做不到" ✗ ≠ "**任何**精确构造做不到" ✓
【实操含义 ✓】要做出与前沿**同质**的实例（τ = 0 且 p ≈ 0.68），必须：
   ① 换更细的网格（求 p_min 是否跌破 0.682 ⚠️ 未做）② 或换更一般的构型（非网格位置 ⚠️ 未做）
   ③ ⭐ 且**仍缺解析半边**（值接近 p 的有效证书 ✗）
【本轮自查 ✓】目标函数**写成 Σu（u-part）而非真正的权重 w = u + √D t** ✗ ⟹ LP 报**无界** ⚠️
   （根因 ✓：u/t 分裂存在**零空间方向**，保持斜坡等式的同时把 u 推上推下 ✓）
   ⟹ 已修为 Σ w ✓（有界 ⟹ ≤ 1 ✓）并重跑 ✓
