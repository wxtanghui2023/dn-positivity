# ARI 全家族归档——Arithmetic Renormalization Invariant（正式封存——）

> 2026-09-07 22:00 · 唐先生终审——结构性瓶颈识别——非"候选不够"

## 归档判定
```
ARI-A：连续尺度缩放 —— DEAD（G(aρ)=G(ρ) 所有 a ⟹ G 常值或守恒不成立——）
ARI-B：零模态匹配 —— CIRCULAR（需知道 λ——）
ARI-C：有限尺度差分/cocycle —— DEAD（D_{a,b} = (R_a−I)(R_b−I)——尺度半群代数——）
ARI-D：离散素因子块重整化 —— COLLAPSED
```

## ARI-D 的 collapse mechanism（精确——）
```
exact block ⟹ relabeling（只是重编码——无新信息——）
coarse block ⟹ statistical projection（丢失信息——I(n₁)=I(n₂)——）
可逆块：无新信息——不可逆块：只见 quotient 算术
自然块全落两端：log-block → Mellin/Euler——residue-block → character/L(s,χ)
```

## 结构张力（比"无 block spin"更准——）
```
唯一分解允许大量统计粗粒化——但无发现：
  既保持可验证精确组合律 + 产生新自由度 + 不退化为 Euler/Mellin/character/显式公式
的块操作
```

## 今天弧线闭合（——）
```
Connes → Weil/谱表示 → P51-T5 → 放大-分辨二难 → ARI → 离散重整化
四种尝试全遇同一结构：
① 直接观察 β → 需谱分辨
② 放大观测 → 重读零点
③ 纯算术对象 → 无独立 β-敏感 rigidity
④ 尺度流 → 重进 Mellin/Euler/显式公式
```

## 母问题澄清（最重要产出——）
**RH 的剩余障碍不是"如何观察零点"——而是"如何获得独立权重"**（W_independent——）
- 现有数学连接素数算术与零点位置的通道——都把权重信息经解析/谱变换传递
- 无独立权重定理

## 函数域真模板（——）
不是"有 Frobenius"——是**存在独立几何权重定理**（|α| = q^{w/2}——几何固定 w——）
缺 = 特征零的独立 purity/weight 原理

## 下一轮（唐先生定——）
**搜索跃迁到"特征零的独立 purity/weight 机制本身"**
- 不从 RG/尺度流/粗粒化/谱流/显式公式变形进入（已排除——）
- 问：什么数学定理可以独立产生 weight/purity？（非"什么对象编码 β"——）

## 文件
- docs/ari-direction.md + ari-round1.md + ari-survival-window.md
