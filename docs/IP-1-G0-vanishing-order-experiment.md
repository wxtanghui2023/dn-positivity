已查地图：未覆盖（关键词: 消失阶 d_V rank flag Chebyshev 函数空间刚性）—— 可开档 ✓

# **`IP-1-G0`**：Chebyshev 型有限函数空间中的「消失阶下降律」实验（**已跑，数字驱动**）

**唐先生令（2026-09-23 09:58）**：**不再写 Pivot 总纲**，直接做可算实验 ✓：在 Chebyshev 型有限函数空间中寻找**严格区别于 rank／kernel／support-size 的「消失阶下降律」** ✓；
　三门：**`G0.1` 非循环**（不得调用 `|S| ≤ k` 或等价命题）｜**`G0.2` 非坐标量**（若 `\Delta` 只是 `N-\operatorname{rank}E`／kernel dim／rank deficiency／codimension／Carathéodory 的换名 ⟹ **立即 FAIL**）｜
　**`G0.3` 真递推/极值律**（须有可证关系如 `\Delta(V_{q+1},S)\le F(\Delta(V_q,S))`；**只有证书不算**）✓
　**只做 3 个最小实验**：`r=2,3,4`；`m=2,\dots,6` 节点；逐例算 `|S|`、`\operatorname{rank}E`、`d_V(S)`，并检查是否存在**第四种量**控制 `d_V` 下降 ✓
　⭐ **硬规矩**：**若第四种量始终可由前三者恢复 ⟹ 当天关闭**；**不允许"方法论进展"冒充数学进展** ✓✓

D0: 本档对象 = **`IP-1-G0` 实验（脚本 ＋ 数据 ＋ 判定）**（新实验，非改名；**未开 RH 案** ✓）
D1: 0
【计算冻结解除声明】 本档依令**执行一次小规模计算**（`IP-1-G0` 授权范围）；⛔ 未触碰 RH／`β`／zero statistics／explicit formula／prime-pair／transfer ✓

---

## §1 实验设置

```
【脚本】 `scripts/ip1_g0_vanishing_order.py`（**全精确有理算术**，零浮点 ✓）
【对象】 网格 `x=k/4`（`k=-4..4`，9 节点）；`S` ＝ 全部 `m` 子集（`m=2..6`）⟹ instances=8208 实例
　`V_q^r=\operatorname{span}\{T_q,\dots,T_{q+r}\}`（Chebyshev，整数系数）；`q=0..5`，`r=2,3,4` ✓
【定义】 `d_V(S)=\min\{\deg p:\ 0\ne p\in V,\ p|_S=0\}`（无消失元则记 `\infty`）✓
【数据】 `out/ip1_g0/ip1_g0_instances.tsv`｜日志 `out/out_ip1_g0.txt` ✓
```

## §2 ⭐ **硬结果（数字驱动 ✓✓）**

```
【恒等式】 $$\boxed{d_V(S)\ =\ q+\min\{\,j:\ \operatorname{rank}E_S\big|_{V_q^j}<j+1\,\}}$$ —— 即 `d_V` **完全由 rank flag 决定** ✓
【实测】 成立 成立=8208/8208（**全部实例**）✓✓ ⟹ 它是**恒等式**，非经验相关 ✓
【性质（诚实标注）】 这是 **rank 数据的细化**（rank flag），**不是第四种量**；且为**初等可证命题**（由诸 `T_n` 次数互异、`V_q^j` 逐级升维直接得到）✓
⟹ ⛔ **对 `G0.2` 的直接后果**：**`d_V` 的"下降律"本身就在 rank 型数据之内** ⟹ **不满足"严格区别于 rank/kernel"的要求** ✓✓
```

## §3 ⚠️ **未闭合项（诚实标注，不作为结论 ✗）**

```
【现象】 "最小消失元的额外零点数"（`extra_zeros`）在分组检验中**不唯一**（71 组中 不唯一的组数=50 组）⟹ **表面不是 rank 决定的** ⚠️
【⚠️ 但】 该量出现**负值**（`-1`／`-3`）⟹ 与"`p` 在 `S` 上消失 ⟹ 相异实根数 `\ge|S|`"矛盾 ⟹
　**说明我的根计数与 kernel 选取口径尚未闭合**（多重根／端点 `\pm1`／Sturm 余式符号三者之一）✗
【依铁律处置】 本仓规矩：**结果异常先怀疑自己的分支** ⟹ **该量在修正并复核前，不得作为结论、不得作为"第四种量"提出** ✓✓
【状态】 记为 **未闭合项**（`extra_zeros` 口径待修）；⛔ 不升级、不宣传 ✓
```

## §4 **判定（依令硬规矩 ✓✓）**

```
【依令】 "若第四种量始终可由前三者恢复 ⟹ 当天关闭" ⟹
　**`d_V` 的下降律 ＝ rank flag（rank 数据）** ⟹ **在本模型内，不存在脱离线性代数型数据的独立"消失阶下降机制"** ⟹
　$$\boxed{\text{IP-1-G0} = \text{B（CLOSED\ with\ content）}}$$ ✓✓
【⭐ 这次 CLOSED 是"有内容的"（照录标准）】 我们得到的是：
　**"function-space rigidity"这一整类母问题，在这个具体 Chebyshev／有限点模型中没有脱离线性代数的独立机制** ——
　**不是**"可能不行"，而是**已用 instances=8208 个实例＋一条恒等式钉住** ✓✓
【未达成者】 `G0.1`（非循环）**未获机会**（因无第四种量）｜`G0.3`（真递推/极值律）**未出现** ⟹ ⛔ **不进入 `IP-1-G1`** ✓
```

## §5 边界与后续

```
【未做】 ⛔ 未进入 `IP-1-G1`（`G0` 未产出第四种量）✓；⛔ 未触碰 RH／`β`／zero statistics／EF／prime-pair／transfer ✓；⛔ 未把 rank flag 包装成新量 ✓
【重开条件（诚实 ✓）】 若日后**修正 `extra_zeros` 口径**且能证明其（i）独立性（非 rank 决定）**且**（ii）满足一条可证律（`G0.3`）⟹ 可重开 `IP-1-G1` ✓
【注】 本档**不声称** "function-space rigidity 无独立机制" 的一般命题 —— 仅限**本模型（Chebyshev 型、`[-1,1]` 网格点）** ✓
```

## §6 归档纪律

```
【R1】 脚本头含目的/输入/输出/纪律 ✓｜【R7】 结论由数字驱动（实例数、恒等式计数、分组计数）✓｜【数据入仓】 TSV ＋ 日志 ✓
【复现】 `python3 scripts/ip1_g0_vanishing_order.py` ✓
✗ 未证 RH／未接 ζ／未开案／未造新量 ✓（本档为**实验记录＋判定** ✓）
```

---

## §7 ⚠️ **过程债务登记：`§8.1` 冻结程序（诚实 ✓）**

```
FREEZE-ACK: D1=0（本档**不主张**新自由度）；依 `RESEARCH-CONSTITUTION` §8.1，连续 `D1=0 ≥ 3` 应执行"冻结修剪／重设坐标系" ✓
【债务事实（如实登记 ✓）】 台账字段「连续 `D1=0`：当前 35／阈值 3」**已远超阈值**，而 §8.1 的冻结程序**尚未执行** ⟹ **过程债务成立** ✓✓
【本档处置（不越权 ✓）】 ⛔ **不在本档擅自执行冻结修剪** —— 因"重设坐标系"属**方向性决策**（依 `SOUL.md`：不做方向性决策，须唐先生拍板）✓；
　本档仅**登记债务**并提出**最窄处置建议**：一次**冻结审计**（把 35 轮 `D1=0` 的累积原因**分类**＋提出**坐标系重设的最小动作**），单独成刀 ✓
【注】 冻结程序**只针对"记账口径／坐标系"**，与数学结论无关 ✓（不影响本档 §2 的恒等式结果 ✓）
```

