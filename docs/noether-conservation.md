# 想法 1 深挖：对偶守恒的 Noether 版本——含 β 的守恒量不存在

> 2026-09-01 · 唐先生想法 1（动力学——运动量）的深挖

## 对偶守恒的精确结构（数值确认）
- 素数速度：v(x) = 1/log x（素数定理）
- 零点密度：dN/dt = (1/2π)log(t/2πe)
- **v·dN ≈ 1/2π（x≈t 时——x=1e3: 0.094——1e7: 0.131——趋向 1/2π=0.159）**
- **"对偶守恒"**——来自"倒数关系"（1/log × log）——log 尺度下的对偶密度

## Noether 分析（对称性 → 守恒量）
| 对称性 | 守恒量 | 含 β？ |
|--------|--------|:--:|
| 尺度不变性（x→λx） | 对偶密度积（v·dN≈1/2π） | ❌（计数） |
| 函数方程（s↔1−s） | 轨道中心（Re=½） | ⚠️ 但不约束偏离 |
| 共轭（s↔s̄） | 零点成共轭对 | ❌（不约束实部） |

## 关键：含 β 的守恒量不存在
**需要"含 β 的对称性"**（变换改变 β 但保持某个量）：
- σ 平移（s→s+ε）：ζ 改变——不是对称性
- 零点实部独立偏移：配置改变——不是对称性
- **"含 β 的对称性"不存在——所以——"含 β 的守恒量"（Noether）不存在**

## "ψ 的动量"（β 体积——想法 2 的）
- P_ψ = ∫(ψ−x)²x^{−3}dx——含 β（振幅）——从素数数据可算（数值）
- **但——判别基准（在线期望）需零点——循环**

## 结论
**想法 1（Noether）不提供 RH 约束**：
- ✓ 对偶守恒（v·dN≈1/2π）——真实（尺度不变性——Noether）——**新结构认识**（素数与零点的"密度对偶"——log 尺度的倒数关系）
- ✗ 所有守恒量不含 β 或不约束 β
- ✗ "含 β 的守恒量"需要"含 β 的对称性"——不存在
- **与所有方向一致：β 需要独立约束——未找到**

## 两个新想法的最终评估
- **想法 1（动力学——Noether）**：提供"密度对偶"新认识（v·dN≈1/2π——计数守恒）——不含 β——不约束
- **想法 2（托里拆利——β 体积）**：提供"β 体积"新探针（尾部指数——数值支持 β≈½）——检测非约束
- **两者都回到"检测非约束"模式**——与完整图景一致

## 战略状态
- C1–D7 + P1–P4 + K1–K2 + THH + 搭接 + 变分 + 能量 + 动力学 + 两个新想法——**全部收敛**
- 完整结构性结论已存档（final-structural-conclusion.md——9b63b45）
- **β 需要"独立约束来源"（重数强制/Fr 类）——Spec Z 无——这是统一的结构解释**
