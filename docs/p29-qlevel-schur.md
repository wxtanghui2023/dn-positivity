# P29：q-Level Schur Balance——决定性确认 Γ=2（mpmath 复广义 50 位——精确结构恒等）

> 2026-09-01 · 唐先生 P29 指令 · Δ_q / R_Schur / Γ_N · 三 invariant

## ⚠️ 实现审计（先记录）
- **scipy 广义（复——GCG x = λGx）**：Γ=2.0000——q_M=q_H——λ_crit=−2.3e-6
- **mpmath 实部近似（Greal——cholesky）**：Γ=−0.156——q_M≠q_H——λ_crit=−4.7e-2（**错误实现——实部近似——错误临界方向**）
- **✅ mpmath 复广义（eighe(G)——G^{1/2}CG^{1/2}——50 位）**：**Γ=2.0000000000（Γ−2=9.0e-13）——q_M=q_H（差 1.0e-6）——q_cross=−2.248697e6=−2q_M（精确）——λ_crit=−1.01e-6**

## ⭐⭐ 决定性结论——Γ=2 是真实结构恒等
- **Γ = −q_cross/√(q_M q_H) = 2（mpmath 50 位——精确——不是数值巧合）**
- **q_M = q_H（精确）——q_cross = −2q_M（精确）——"临界方向：对角精确等于跨块——q = q_M+q_H+q_cross ≈ 0（positivity wall 上的精确抵消）"——结构定理候选确认**
- **scipy 广义与 mpmath 复广义（正确实现）一致（Γ=2）——之前的 mpmath 实部近似（−0.156）是错误实现**

## Δ_q / R_Schur（双精度——N 扫描）
- **Δ_q → 0（q_H = q_BCB——1e-5 到 1e-10——q-level balance 真实）**
- **R_Schur → 0（q 级——r_Schur/q_H 小——趋近 Schur minimizing——但——欧氏向量残差 0.94——"C-范数对齐非欧氏对齐"）**
- **Γ_N = 2.0000（所有 N——所有 M——双精度 + mpmath 确认）**

## ⭐ P29 判定
- **Δ_q → 0（q-level balance 真实——q_H = q_BCB）**
- **R_Schur → 0（q 级 C-范数——趋近 Schur minimizing——但——欧氏向量不对齐（0.94——表示问题））**
- **Γ_N = 2（精确——临界方向的"最大相干"——q_cross = −2√(q_M q_H)——positivity wall 精确抵消）**
- **"临界模式：genuine quadratic-form Schur balance（q_M=q_H=q_BCB——Γ=2——q≈0）——但——向量级不局域化（0.94）"——唐先生的理想结果部分确认（Δ_q→0——但——Γ=2 不是 ↓1——是精确 2）**

## ⚠️ 诚实边界
- Γ=2 是"临界方向"（最接近零的负）的性质——**非所有负方向**（需检查其他负方向的 Γ）
- q_M=q_H 的精确机制（η=1 平衡？——结构对称？）——需更深分析
- λ_crit 的 O(1e-6)——Γ 精确 2 但 λ 非零——q ≈ λ（O(1e-6)——相对 q_M（1e6）——1e-12 修正——不可见）
- 双精度 N 扫描的 Δ_q/R_Schur 波动（N=5 异常 1.8e-3——数值）

## 下一步
- (a) 所有负方向的 Γ（非仅临界——Γ=2 是普遍的还是临界特有）
- (b) q_M=q_H 的精确机制（η=1 平衡的结构来源）
- (c) 唐先生指示
