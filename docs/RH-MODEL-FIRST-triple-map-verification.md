已查地图：命中 70（①组）／27（②组）／105（③组）／20（有限压缩|惯性）—— 先逐条判 已DEAD/已封/已登记；命中即引既有条目，不得开新案
　判定：三组命中**并非散点**，而是**索引级大区**（`INDEX-BY-DIRECTION`）：
　　**A. Li 系数／有限高度转换（74 份）**｜**C. Weil 正性／有限压缩／惯性（P27–P33）（73 份）**｜**D. Nyman–Beurling／显式公式／Guinand（11 份）** ✓✓
　⟹ 唐先生 ①②③ **三方向均为既有大区** ⟹ 依纪律**只引既有条目、不新开案** ✓
　⚠️ 旁注：术语「短支持」在档案 **0 命中**（同类内容落在 §C「**有限压缩**」，20 命中 ✓）

# RH-MODEL-FIRST · **三候选四元组的地图核验**（①Weil／②Nyman–Beurling／③Li）

**唐先生令（2026-09-22 20:43）**：① 三候选**不是推荐证明 RH**，而是"具体到足以过十三门槛初筛"的候选 ✓；
② **下一刀不是写程序**，而是对 ①②③ 做**地图核验**：「**只查是否已被 `MASTER-NOGO-AND-LIVE-PATHS`／`C309T`／`V195–V198`／`V238` 等档案覆盖；不通过地图就不进入十三门槛**」✓✓；
③ 特别盯住 **① Weil「负性见证是否可压缩」**：「它与已封掉的**整体 Weil 正性**并不完全是同一个问题，但**很容易重新掉回原墙**，所以必须**先审计，而不能凭名字判新**」✓✓

D0: 本档对象 = **三候选四元组的地图覆盖判定**（档案已有对象 `§A Li／§C Weil 正性·有限压缩／§D Nyman–Beurling` 的**归属关系**；非重命名、非新对象）
D1: 0
FREEZE-ACK: 零计算／零 Lean／**未写程序** ✓（仅地图核验＋引既有裁决 ✓）

---

## §1 ① **Weil-局部正性缺陷四元组**

```
【候选（照录）】 𝒳 = 检验函数空间（Weil 二次泛函的作用域，**非零点集**）｜ℒ = 受限支持/局部测试函数类上的
　Weil 正性｜P = ∃f：W(f*f̃) < 0｜D = 支持／膨胀**压缩**（若存在负方向，构造**支持尺度严格更小**且仍负的 f′）✓
【唐先生自设 killer】 ① 能否证「**负性缺陷保持 ＋ 支持严格下降**」？② 若下降只是**缩放**、而泛函同步缩放
　（信息不变）⟹ 即 **L3／等价性搬运** ⟹ **立即 DEAD** ✗✓
```
```
【地图核验（命中与裁决）】
　命中：`INDEX §C「Weil 正性／有限压缩／惯性（P27–P33）」73 份` ✓｜`有限压缩|惯性` 20 命中 ✓｜`P26 系列（A3–A17）` ✓
　⛔ **既有裁决（逐字，`p26a17-meta-conclusion`）**：
　　 「组合没有新的面——'点'（反射刚性／β 不变／变形-零点耦合／β 动力学／物理模板）是**同一堵墙**（β 墙）的不同面」✓
　　 「**'β 墙'的最深层形态**：**算术结构（Euler 积——乘性——局部）不含 β 的约束/恢复力**——
　　　 β 的约束/恢复力需要**非局部/热/变分**——**非算术**——**循环**」✓✓✓
　⛔ 其他既有闸门：`T7`「正性**是否依赖零点位置／显式公式／Weil 型？若是 ⟹ 循环**」✓｜门槛 `P-SqrtPos`「**正性须独立于零点位置**」✓
　⛔ `L3` 现状（逐字）：「char 0 无条件 √-正性**皆来自有限性**；**全局类比全部循环**」✓
```
```
【⭐ 结构判定（我的分析，非档案结论 ✗——须标）】
　唐先生的 killer 问题「**负性见证能否被算术压缩**」在档案中**已被回答为否**，且**理由正是**：
　**压缩所需的"约束/恢复力"不在算术侧**（乘性/局部结构不含 β 信息）⟹ 负性见证**没有算术下降** ✓✓
　唐先生候选的**唯一新意在**"局部/短支持 ℒ"：⚠️ 但（i）短支持下的**正性成立**是**经典事实**（唐先生引文 ✓），
　　（ii）该事实**不**提供"负向**压缩**"机制 ⟹ 仍须把负性见证**压到更小支持而保持负** ⟹ 正是 §C「**有限压缩**」题目 ✓
　　（iii）而 §C 已 73 份 ＋ `P26-A17` 判「非算术 ⟹ 循环」⟹ **回墙** ✓
【判定】 **DEAD（已被覆盖 ⟹ 不进十三门槛）** ✓✗
　唯一可辩驳处：若唐先生能给出**非缩放型**、且**不依赖非局部/热/变分**的压缩机制 ⟹ 才算新（须另报 ✓）
```

## §2 ② **Nyman–Beurling「非逼近见证」四元组**

```
【候选（照录）】 𝒳 = L²(0,∞)｜ℒ = Möbius／膨胀局部关系｜P = 距离缺陷 δ_N > ε｜D = **尺度压缩**
　（若某尺度 N 存在不可消除的逼近缺陷，能否由 Möbius／膨胀结构压到更小尺度 N′<N 且保留定量缺陷）✓
【唐先生自设 killer】 若 compression 只是**已知 Nyman–Beurling 闭包等价性的另一种表述** ⟹ **DEAD** ✗
```
```
【地图核验（命中与裁决）】 `INDEX §D（11 份）`＋`DOOR5` 系列 ＋ `NBTHRESH-measured` ＋ `guinand-theorem-framework` ✓
　⭐ 既有裁决（`DOOR5` §2，逐字要点）：判据 `RH ⟺ d_N → 0`（H²-距离）✓｜`Báez-Duarte 2003`：膨胀可限制为**整数 a>1** ✓
　｜⭐ **文献明言「d_N < ε 给出 ζ 的无零点区域」⟹ 转换存在** ✓✓｜定量现状：**Burnol 已证一侧**（常数即猜想值），
　　**开放部分＝匹配的另一侧** ⟹ 逐字「又是'**单侧已证、另一侧是边际**'的**同一模式**」✓✓
　另：`DOOR5b–5f`（Burnol 原文／uniformity／precise disc）已把该线**定量读尽** ✓
```
```
【⭐ 结构判定（我的分析 ✗——须标；规范化以原文为准 ⚠️）】
　按档案规范，`d_N` 是 **N 递增族上的下确界** ⟹ **对 N 单调不增** ✓
　⟹ 「失败压缩到**更小** N′」被**单调性平凡化**（若 `d_N > ε` 则 `d_{N′} ≥ d_N > ε` 对一切 `N′<N`）✓
　⟹ 但**平凡压缩不产生矛盾**：不存在可穿越的 **failure-free 区域**（小 N 恰是 `d` 更大的方向）✗
　⟹ 若要压缩，方向须**向上**（大 N）；而大 N 正是 `d_N` **变小**的方向 ⟹ **与目标同向 ⟹ 无判别力** ✗✓
【判定】 **DEAD（结构上无判别力；且定量侧已被 `DOOR5` 读尽）** ✓✗
　（若唐先生能给出**非单调**的压缩量或其他有判别力的度量 ⟹ 须另报 ✓）
```

## §3 ③ **Li／Weil 系数「异常增长见证」四元组**

```
【候选（照录）】 𝒳 = {λ_n}｜ℒ = 显式公式递推｜P = "前缀 λ_1..λ_n 无法满足某**独立的**递推／凸性／正性约束"｜
　D = **索引下降**（P(n) ⟹ P(m)，m<n）✓
【唐先生自设 killer】 若 P 最终等价于「存在 λ_n<0」、或 D 直接调用 RH／Li criterion ⟹ **立即 DEAD** ✗
```
```
【地图核验（命中与裁决）】 `INDEX §A（74 份）`＋`DOOR1-li-modulus-door-located`＋`CONV1–CONV3`（转换可证）＋
　`CVS1–CVS4`（**Loewner 检验**／CVS5 findings ＋ **NB threshold**）＋`CVS2-loewner-test-and-correction` ✓
　⭐ 既有裁决（`DOOR1` §1，逐字）：门的**精确定位** ＋ **交叉律常数**：
　　`n_first_neg ≈ 3.2–3.5 × 2γ²/|2β−1| ≈ 6.6γ²/|2β−1|` ✓✓
　＝「**失败指标 n 由离轴数据 (β,γ) 自下界锁定**」✓
　另：`CVS1–CVS3` 已把「**前缀约束**」型检验（Loewner／Toeplitz-PSD／Hurwitz 路线）做过 ✓
```
```
【⭐ 结构判定（我的分析 ✗——须标）】
　由 `DOOR1` 的定量律：若某 n 处失败，则**绑定**该失败的那对 (β,γ) 满足 `6.6γ²/|2β−1| ≲ n` ✓
　⟹ 下降 `m<n` 时，若要 `P(m)` 成立，需让**同一** (β,γ) 也满足 `… ≲ m < n` ⟹ **不可能** ✓
　⟹ 除非允许换成**另一**离轴数据 ⟹ 但那样**失败对象未被保持**（换了对象）⟹ `D` **不产生矛盾** ✗✓
【判定】 **DEAD（`D` 与 `DOOR1` 的定量律冲突）** ✓✗
　（注：唐先生已自设"若 D 调用 RH／Li criterion 即 DEAD"⟹ 本候选在**不调用**的前提下，被 `DOOR1` 常数律排除 ✓）
```

## §4 ⭐ 三者共同结论（**一致性** ✓）

```
【结论】 唐先生**自设的三个 killer 问题**，在档案中**都已有否定性答案**，且**理由统一**：
　①「负性见证能否被算术压缩」⟹ **否**：`P26-A17`「**算术结构不含 β 的约束/恢复力**（需非局部/热/变分 ⟹ 非算术 ⟹ 循环）」✓
　②「逼近缺陷能否压缩到更小尺度」⟹ **否**（无判别力）：`d_N` 对 N **单调不增** ⟹ 压缩平凡、无 failure-free 区可穿越 ✓
　③「Li 前缀异常能否索引下降」⟹ **否**：`DOOR1` 常数律 `n_first_neg ≈ 6.6γ²/|2β−1|` ⟹ 失败指标**自下界锁定** ⟹ 下降不保持同一失败 ✓
【统一回墙】 三者皆回落到既有墙：**β 墙（算术侧无约束/恢复力）**／**第一断裂（有限处 α_p ≡ 1）**／
　**定义式局部律（§C309T §6：RH 侧缺 D3/D4）**／`T1 局部⇏全局`／`C0 紧性` ✓✓
【判定（照录纪律）】 **①②③ 均未通过地图 ⟹ 不进入十三门槛** ✓；**未新开案** ✓
【与唐先生自我预期一致】 唐先生已预料「① 很容易重新掉回原墙」⟹ 核验结果**确认**该预料（且给出**为何**回墙的机制）✓✓
```

## §5 边界与回查（✗✓）

```
✗ 零计算／零 Lean／**未写程序** ✓（严格落实"下一刀不是写程序" ✓）
✗ 未新开案；未把"三候选皆 DEAD"记为战绩 ✓（依既有纪律）
⚠️ §1／§2／§3 中标注"我的分析"处＝**本档新增推理**，**非**档案既有结论；规范（`d_N` 幂次方向等）**以原文为准** ✓
【回查（先跑后写 ✓）】
　prework_map_check：①组 70／②组 27／③组 105／`有限压缩|惯性` 20 —— 命中项**全部**为索引级既有大区（§A/§C/§D）✓
　术语「短支持」= 档案 **0 命中**（同类内容在 §C「有限压缩」）✓
【引用的既有条目】 `p26a17-meta-conclusion`（β 墙最深层）｜`INDEX §A/§C/§D`｜`DOOR5`＋`DOOR5b–5f`＋`NBTHRESH`｜
　`DOOR1`（门的定位＋常数）｜`CONV1–3`｜`CVS1–CVS4`｜`T7`｜`P-SqrtPos`｜`L3`｜`C309T §6` ✓
