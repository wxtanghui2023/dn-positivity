# V115 · ⭐⭐ **方向 (ii)「表征收缩」首轮审计（R1／R2／R3）** ✓ —— 𝒞 精确定义 ＋ KILL 顺序判定

> **首轮纪律（唐先生 2026-09-14 12:04 逐字 ✓）**：**第一轮不提新机制** ✗；**不得先构造 R** ✗；
> 只做：① 先把覆盖对象 𝒞 **精确定义**出来 → ② 按 **R1 → R2 → R3** 的 KILL 顺序审计 ✓。
> **本质**：本档是**判定档**，不是构造档 —— 目标 ＝ 判 R1/R2/R3 三个 KILL 门是否通过 ✓；**通过 ⟹ LIVE，任一失败 ⟹ CLOSED／NO-GAIN** ✓。
> **写作纪律**：先骨架 ＋ §①立即落盘 ✓，每节完成即追加 ✓。

---

## ① 编号（领用脚本输出）与查重

```
【编号 ✓】`bash scripts/id_claim.sh audit representation-contraction-r1r2r3` ⟹ 输出 **`V115`** ✓（V 号段＝审计线 ✓；**非 E 号段** ✓）。
   · 占用判定 ✓：`docs/V115-*` 不存在 ✓；`docs/.idclaims/V115.lock` 由脚本 mkdir 原子占位 ✓；
     台账 `docs/ID-CLAIMS.tsv` 已追加本行 ✓（V115 audit representation-contraction-r1r2r3 ✓）。
   · ⚠️ **注意名近项** ✓：`docs/E115-bridge-is-one-log.md`（**E**115，主线；与本档 **V**115 前缀零重叠 ✓，非冲突 ✓）。
【查重（显式路径 ＋ docs 内定向 grep ✓）】
   · `grep -ril "表征收缩" docs/*.md` ⟹ 命中 **2 处**：`V109-coverage-argument-charter.md`（立项档 ✓，§②-2 中档 M／§⑤-2 方向 (ii) ✓）
     ＋ `EXPLORATION-POINTS-REGISTER.md`（登记册 ✓）。
   · `grep -rl "representation-contraction" docs/*.md` ⟹ **0**（无同名专档 ✓）。
   · ⟹ **结论 ✓：无专档 ⟹ 可做** ✓；但**同对象在 `V109` 已登记为"方向 (ii)"**（**审计线尚未独立成档** ✓）—— 本档为**首个 (ii) 的独立审计档** ✓。
【对象逐字源 ✓】`V109` §②-2（中档 M ＝ 表征收缩 ✓）／§⑤-2（方向 (ii) ✓）／§③（空间 𝒞 ✓）／§⑥-5（KILL ✓）；
   `E102` §8 靶1（类表完整性 ✓）；`E105` §5（五类 ✓）／§7（第六种 ✓）。
【HARD NO-GO 预检 ✓】未触碰 `fn_backup/fupeng/github/quant-system/.git` ✓；未全盘 find/grep ✓；未用 RH ✓；不改既有文件 ✓（仅新建本档 ✓）。
```

---

## ② 覆盖空间 $\mathcal C$ 的精确定义（**本步成败关键 ✓** —— 逐字出处 ＋ admissible 判据 ＋ 待澄清项）

> **纪律 ✓**：本轮**不发明**术语 ✗；命名沿用 `V109` §③-2 的**既有拟定义 C** ✓；凡档案**未给定义**者**明标【本方拟定义】／〔本方推论〕** ✓。
> **一句话 ✓**：$\mathcal C$ ＝ **识别方案** $I=(D,M,E)$ 的类（数据侧 ／ 机制型 ／ 有效性 三坐标 ✓），admissible 判据 ＝ `E102` **C1–C4 ＋ 内在性 C★** ✓。

### ②-1 载体 ／ 机制 ／ canonical 的既有逐字义（先固定，不发明 ✓）

```
【载体 carrier ✓】`E106` §4 逐字：“每个‘谱＝零点’的载体 (𝒜,𝒯) 自动给出一个 RH 等价判据”✓；
   **对偶同一 ✓**：`E106` §4 逐字“**两个空间【对偶同一】**”（载体空间 ≡ 判据空间 ✓；`V109` §③-1 引 ✓）。
【单点隔离机制 ✓】`E105` §5 逐字：“**‘canonically 单点隔离’的已知机制 —— 五类**”✓（＝ 𝔐₅）：
   ① 方程（零点集 $F=0$）⟹ 类 IV／T1 ✗｜② 不等式／极值（$\arg\max$）⟹ 箱 5 ✗｜③ 对称／不动点（$g\!\cdot\!x=x$）⟹ 仍是方程 ✗；
   ④ 可定义性（“最小满足 P 的点”）⟹ 类 VI ✗（Presburger ⟹ 最终周期 ⟹ 回 congruence）｜⑤ 动力／发生（迭代极限）⟹ 箱 5 ✗。
   ⚠️ 逐字（`E105` §5）：“**五类皆已排除或归类 ✓ —— 但‘清单穷举性’未证** ✗”（＝ 地图 §E.4 OPEN ✓）。
【第六种（唯一残余 ✓）】`E105` §7 方框逐字：“是否存在【第六种】canonically 单点隔离机制，不属于‘方程／极值／对称／可定义性／动力’？”✓
```

### ②-2 ⭐【引自 `V109` §③-2 的拟定义 C】覆盖空间 $\mathcal C$

> **注 ✓**：以下方框为 `V109` §③-2 的**本方拟定义 C 逐字照录**（`V109` §③-2 ✓）—— 本档**不改写**其定义 ✗，仅**承接**并用于 R1/R2/R3 ✓。

$$\boxed{\mathcal C\ :=\ \{\,I=(D,M,E)\ :\ D\ \text{过}\ \texttt{E102}\ \text{C1–C4},\ M\in\mathfrak M,\ E\ \text{可认证}\,\}\ ✓}$$

```
【D｜数据侧 ✓】〔逐字锚 `E102` §C1〕：“𝒜 ≠ 𝒜({ρ}) —— 只能由整数／素数／理想／Galois／adelic／global 构造”✓（⟹ 排**循环** ✗）。
【M｜机制型 ✓】M ＝ 从 D 到“**单点标签** $L(\rho)$”的 canonical 过程 ✓；其机制型 $M(I)\in\mathfrak M$，
   𝔐 ＝ {方程, 极值/不等式, 对称/不动点, 可定义性, 动力/发生}（`E105` §5 逐字五类 ✓）⊕ **可能第六类** ✓（`E105` §7 ✓）。
【E｜有效性 ✓】〔逐字锚 `E105` §3(b)〕：“任何【证书】都是**有限数据**”✓；`E106` §2 normal form：“有限算术数据 → **有效 Cauchy name** → ρ”✓。
【canonical ✓】〔逐字锚 `E102` §C1〕：否式 $\mathcal A\neq\mathcal A(\{\rho\})$ ✓（**只排除“人为”**，非正定义 ⚠️，见 §②-4-C ✓）。
【单点隔离的精确义 ✓】〔逐字锚 `E105` §5〕：“canonically 单点隔离”✓ —— 即每个零点 ρ 可由 $L(\rho)$ 复原（`E106` §2 等价形：收缩块 $D_1\supseteq D_2\supseteq\cdots$ 且 $\bigcap_N D_N=\{\rho\}$ ✓）。
```

### ②-3 ⭐ **admissible（“允许”）的判据 ＝ `E102` C1–C4 ＋ 内在性 C★**（逐字 ✓）

> `E102` §1 表**逐字**给出“允许”的四条必要判据 ＋ 一条内在性约束（本档照录 ✓）：

```
| C1 canonicality ✓ | 𝒜 ≠ 𝒜({ρ}) —— 只能由整数／素数／理想／Galois／adelic／global 构造 ✓ | 排【循环】✗        （`E102` §1 逐字）
| C2 global visibility ✓ | 存在可控操作使 ρ↦α_ρ 真正入谱 ✓ —— 非仅经 tr ρ(Frob_p) 的局部 character 数据 ✓ | 排【类 I】✗   （`E102` §1 逐字）
| C3 P-scale avoidance ✓ | 归一化 |α_{1/2+iγ}|=1 ✓ 且 ∂_σ log|α_ρ| ≍ 1 ✓（非 O(γ⁻²)✗） | 排【类 IV】✗            （`E102` §1 逐字）
| C4 非 HP 化 ✓ | 不得经正定 Q 直接得 T†QT=T 或 T†QT=qT ✓ | 排【类 II/III】✗                              （`E102` §1 逐字）
| ★ 内在性 ✓ | α_ρ 是载体的【内在谱坐标】✓，不是 α_ρ=Ψ(ξ,ξ',…) 的函数重参数化 ✗ | 排【伪类 V】✗                    （`E102` §1 逐字）
```

⟹ **admissible 判据（〔本方拟定义〕合取式 ✓）**：$I\in\mathcal C$ ⟺ **C1 ∧ C2 ∧ C3 ∧ C4 ∧ C★ ∧ E-可认证** ✓。

$$
\boxed{\text{Cover}(I)\ :=\ \big[\,M(I)\in\mathfrak M_5\,\big]\quad\text{（〔本方记号〕；逐字锚 }V109\ \text{§③-2 方框：覆盖论证＝}\forall I\in\mathcal C,\ M(I)\in\mathfrak M_5\ \checkmark\text{）}}
$$

### ②-4 ⚠️ **待澄清项**（**未澄清 ⟹ $\mathcal C$ 当前非唯一确定 ⟹ 覆盖论证非可判定** ✓）

```
【A–I（`V109` §③-4 逐字汇总 ✓）】
  A “识别” ＝ 命名／判定／单点隔离？         —— 无档案定义 ✗（`V108` §②-3；本档取**单点隔离** ✓〔本方推论〕）  ⟹ 决定对象
  B “不经解析延拓” ＝ 强读／弱读？           —— 无档案定义 ✗（`V108` §②-4；本档取**弱读** ✓〔本方推论〕）      ⟹ **决定死活** ⚠️
  C “canonical”的【正判据】？                —— `E102` §C1 只给**否式** ✓                                        ⟹ 决定 Q2 可判性
  D 识别须**逐零点一致**（uniform）／可单点？ —— 未定 ✗                                                        ⟹ 决定强度
  E 是否要求“**可用**”？                     —— `E105` §3 已证“可用 ⟹ 类 IV” ✓（**防火墙** ✓）              ⟹ 决定 E 坐标
  F 𝔐 是否允许**机制复合**（composite）？     —— **`V109` 新提** ⚠️ 未定 ✗                                          ⟹ 决定中档是否需“复合封闭性”引理
  G $\mathcal C$ 定义在**载体空间**／**判据空间**？ —— `E106` §4 逐字“两空间**对偶同一**”✓；§6 逐字“**后者更可攻**”✓ ⟹ 建议取判据空间 ✓
  H “有限性”是否作**滤子**（D1：无条件 ∧ β-盲 ∧ X对角 ✓）？ —— `E131`(=`V107`) §②-1 逐字 D1 ✓                    ⟹ **决定中档是否成“有限族必属”型** ⚠️
  I 五类的“排除／归类”＝**归零**／**归箱**？ —— `E105` §5 逐字“皆已排除或归类 ✓”（两读法强度不同 ⚠️）        ⟹ 决定中档前置
【J（**本档新增待澄清 ⚠️，〔本方拟定义〕—— 与 R1 直接相关** ✓）】
  J **𝔐 的外延：是“𝔐₅ ⊕ 未知第六类”（开放集）还是“字面 𝔐₅ ∪ {“可能第六类”}”（封闭枚举）？**
     · 若 𝔐 ＝ **开放集**（“一切拟单点隔离机制”）⟹ $M(I)$ 对一般 $I$ **不可判定** ⟹ R1 的“$R(I)$ 有定义”成问题（见 §③ ✓）；
     · 若 𝔐 ＝ **封闭枚举**（字面 𝔐₅∪6th）⟹ $\mathcal C$ **把“结论文”写进了定义** ⟹ 覆盖论证**同义反复** ✗（＝ §⑥“包装”结论的**定义级来源** ✓）。
     ⟹ ⚠️ **J 未裁定 ⟹ $\mathcal C$ 的外延两读皆不可用**（一读 R1 不成立，一读循环 ✓ —— 这正是 §③ 的判定内核 ✓）。
```

$$\boxed{\text{未澄清 A–I ＋ J}\Longrightarrow\ \mathcal C\ \text{当前非唯一确定}\ \Longrightarrow\ \text{表征收缩【当前】非可判定}\ ✗\quad(\text{与 }V109\ \text{§③-4、}V108\ \text{§④-2(c) 一致}\ ✓)}$$

---

## ③ **R1 完备性判定**（KILL 第一门 ✓）—— **判＝不成立 ⟹ CLOSED**

> **R1（逐字 ✓）**：$\forall C\in\mathcal C,\ R(C)$ **有定义**；不成立 ⟹ **立即 CLOSED** ✓。
> **关键检查（逐字 ✓）**：定义 $R$ 时是否**偷用 RH／解析延拓／零点位置** ✗ —— 若需要 ⟹ R1 不成立 ⟹ **CLOSED**，并写明“**表征收缩 ≡ 覆盖论证包装**” ✓。

### ③-1 候选 $R$ 的枚举（**唯一自然候选 ＝ 机制型投影** ✓）

```
【候选 R₁（机制型投影 ✓，本档主候选）】$R(I):=M(I)$ ✓（即 §②-2 的 M 坐标）。
   理据 ✓：`V109` §③-2 方框把覆盖判定**直接定义为** $M(I)\in\mathfrak M_5$ ⟹ 若 M 合法可得，则 $R_1$ 自动满足 R2 ✓。
【候选 R₂（判据空间型 ✓）】$R(I):=$ I 所诱导 RH 等价判据的**形状类** ✓（`V109` §⑤-2 RS-1 ✓；`E106` §4/§5/§6 ✓）。
【候选 R₃（数据侧型 ✓）】$R(I):=$ 一个仅由 $D$ 的**组合/算术**结构算出的不变量 ✓。
【候选 R₄（平凡型 ⚠️）】$R(I):=\operatorname{Cover}(I)\in\{0,1\}$ ✗（＝“把答案当表征”，见 §③-4 反事实 ✓）。
```

### ③-2 ⭐ **判定：R1 不成立**（逐条依据 ✓）

```
【(i) 候选 R₁ 无定义性 ✗（循环／开放两难 ✓）】$R_1(I)=M(I)$ 要求**给每个 $I$ 指派机制型** ✓，而指派机制型**＝ 𝔐₅ 的穷举性判定本身** ✗：
    `E105` §5 逐字：“五类皆已排除或归类 ✓ —— **但‘清单穷举性’未证** ✗”✓；
    · 取 §②-4-J **开放读**：对一般 $I$，$M(I)$ **不可判定** ⟹ $R_1$ **非全定义** ⟹ **R1 不成立** ✓；
    · 取 §②-4-J **封闭读**：$\mathcal C$ 把“$M(I)\in\mathfrak M_5\cup\{6th\}$”写进定义 ⟹ $R_1$ 全定义但**同义反复** ✗（定义已含结论 ⟹ 不是定理）✓。
【(ii) 候选 R₂（判据空间型）**命中硬 NO-GO** ✗】`V109` §⑤-2 把 R₂ 的“抓手”定为判据空间完备性 ✓（来源 `E106` §5 逐字：RH 等价判据 ＝【正性型】∪【求和-公式型】✓）：
    · 判据“**正性型**” ⟹ 逐字命中宪法 §1／§2.1 **N2**：“Weil positivity 及其一切变体（Gram／kernel／trace／能量／test function 重选）”⟹“**判据，非独立机制**”✗（`RESEARCH-CONSTITUTION` §2.1 逐字 ✓）；
    · 判据“**测试函数正性**” ⟹ 逐字命中 **N29**：“测试函数变量的正性【位置盲】（谱侧正性 ＝ Parseval／L² 范数 ⟹ **不含 (Re ρ−1/2)²**）”✗（§2.6 逐字 ✓）；
    · 且 `E105` §3 逐字：“**可用（有效＋可认证）载体 ⟹ 类 IV** ✗ ⟹ 可用载体空间【形式封闭】”✓ ⟹ R₂ 对**可用** $I$ **坍缩入类 IV**，**不产生独立有限表征** ✗。
    ⟹ R₂ **既命中 N2／N29，又坍缩入类 IV** ⟹ **不构成合法 $R$** ✗（**这一步直接反驳 `V109` §⑤-2 “抓手最实”的乐观** ⚠️）。
【(iii) 候选 R₃（数据侧型）**忠实性上死** ✗】`E105` §6 **Lemma E 逐字**：“零点是【不可】由素数【集合】的组合信息给出的 ✗”“**任何能定位零点的载体都必须使用【解析结构】** ✓”✓
    ⟹ **纯 $D$-组合量不含零点信息** ⟹ $R_3$ **不可能是覆盖判定的完备不变量** ⟹ R2 必失败 ⟹ 亦**不能**作 R1 的合法候选 ✓
    （更精确 ✓：若强行让 $R_3$ “读到”零点 ⟹ `E105` §3(a) 逐字“零点从算术数据出发【可有效计算】… 任何 ρ 的可计算函数都是【重参数化】⟹ **违反【内在性】C★ ⟹ 归 类 IV**”✓ ⟹ R₃ 要么不忠实、要么退化为类 IV ✗）。
【(iv) 候选 R₄（平凡型）＝**把答案当表征** ✗】$R_4=\operatorname{Cover}$ 形式上满足 R2（按定义 ✓）且全定义 ✓，但**计算 $R_4(I)$ 需要先解出 $\operatorname{Cover}(I)$** ⟹ 正是**待证命题本身** ⟹ **循环／空转** ✗（不构成“验证前的收缩”✓；＝ §④ 所称**空收缩** ✓）。
```

### ③-3 ⭐ **R1 的“偷用 RH”检查（逐字锚 ＋ 二分 ✓）**

$$
\boxed{\text{忠实 }R\ \Rightarrow\ R\ \text{须编码“零点定位信息”}\ \overset{\texttt{E105}\ \text{§6 Lemma E}}{\Longrightarrow}\ R\ \text{必用【解析结构}}\ \overset{\texttt{E105}\ \text{§3(a)}}{\Longrightarrow}\ R\ \text{是算术数据的可计算函数（重参数化）}\ \Longrightarrow\ \text{违 }C★\ \text{／归类 IV}\ ✗}
$$

```
【逐字链 ✓】忠实（R2）要求 $R$ 区分“看见零点／看不见”⟹ $R$ 必含零点信息（`E102` §8 逐字：“零点**只**通过 ζ 的**解析延拓**对素数数据可见”✓）；
   而 $\mathcal C$ 的 D 坐标**禁止**以 {ρ}／L-值为输入（`E102` §C1 逐字 ✓）⟹ 从 D **生成**零点信息**只能经解析延拓** ⟹ **偷用 RH／解析延拓** ✗ ⟹ **R1 的关键检查【命中】** ✓。
【结论 ✓】$R$ **无法**在**不偷用 RH／解析延拓／零点位置**的前提下**同时** 全定义（R1 ✓）＋ 忠实（R2 见 §④ ✓）＋ 非平凡（≠ $\operatorname{Cover}$ ✗）——
   ⟹ **候选 R 全部出局** ⟹ **R1 不成立** ✓。
```

### ③-4 ⭐⭐ **R1 判决（KILL 第一门触发 ✓）**

$$\boxed{\textbf{R1 ＝ 不成立（FAIL）}\ \Longrightarrow\ \textbf{立即 CLOSED}\ ✗\quad\text{且}\quad \boxed{\textbf{“表征收缩”≡ 覆盖论证（中档 M）的另一种包装，不是新入口}\ ✓}}$$

```
【一句话依据 ✓】覆盖判定 $\operatorname{Cover}(I)=[M(I)\in\mathfrak M_5]$（`V109` §③-2 逐字 ✓）**其完备表征 $R$ 就是 $M$ 本身** ——
   而“$M$ 对全 $\mathcal C$ 有定义且合法（不偷用 RH）”**恰好等价于**“$\mathfrak M_5$ 穷举 $\mathcal C$”＝ **覆盖论证本体** ✗（`E105` §5 清单穷举性未证 ✓）。
   ⟹ **同一个待证命题**：$R$ 的存在性／完备性 ≡ u1（覆盖论证）✗ ⟹ **不能把自己当工具** ✓（同 `V108` §⑤-3 逐字逻辑：“不能把自己当工具”✓）。
【与“预期快结论”的对照 ✓（唐先生任务书逐字 ✓）】：“若连 $\mathcal C$ 都无法在不偷用 RH 的前提下给出完备表示 ⟹ 结论 ＝ ‘表征收缩’本身就是覆盖论证的另一种包装，而不是新入口”✓
   —— **本轮判定与此预言【一致】 ✓**（如实写出，不强求 LIVO ✓）。
【纪律 ✓】R1 触发 ⟹ **按 KILL 顺序【立即 CLOSED】** ✓ —— §④/§⑤ 的 R2/R3 **在 R1 失败后为【不适用／moot】**，但为**交付完整**，下列两节给出**条件性（反事实）复核**并**显式标注其为 moot** ✗（**不以其支撑判决** ✓，判决**仅**由 R1 得出 ✓）。
```

## ④ **R2 保真性判定**（**条件性 ｜ R1 已失败 ⟹ 本节为 moot（不适用）✗；仅为交付完整而作反事实复核；判决不依赖本节** ✓）

> **R2（逐字 ✓）**：$R(C_1)=R(C_2)\Rightarrow$ 两者**具有完全相同的覆盖判定**；不成立 ⟹ **立即 CLOSED** ✓。

```
【反事实前提 ⚠️】假设 R1 已被“强制通过”（即已得一个全定义的 $R$）。仍不符：
【(1) 忠实性二分（〔本方推论〕依据 `E105` §6 Lemma E ＋ §3(a) ✓）】
    · 若 $R$ 仅由 $D$ 的**组合/算术数据**定义 ⟹ 由 Lemma E 逐字“零点是【不可】由素数【集合】的组合信息给出的 ✗” ⟹ $R$ **无法区分**“看见零点”与“看不见” ⟹ **失败** ✗；
    · 若 $R$ 忠实（能分覆盖两值）⟹ 由 §③-3 逐字链 ⟹ 必含零点／解析延拓信息 ⟹ **违 C★ ／归类 IV** ⟹ 已非合法非平凡表征 ✗。
【(2) 两值退】若取 $R=\operatorname{Cover}$ ⟹ $R_2$ 按定义成立 ✓，但 **$R$ 不可在“不解覆盖”的前提下算出** ⟹ **空收缩** ✗（同 §③-2(iv)）。
【(3) 同一机制的反例］】`E105` §5 逐字：五类“皆已排除或**归类**”✗（“归零”与“归箱”两读强度不同 ⚠️，`V109` §③-4-I ✓）—— 若两读不同 ⟹ 两个“同 $R$ 值”对象可有**不同**覆盖判定（一个归零死、一个归箱活）⟹ **$R_2$ 失败** ✗ 〔本方推论〕。
　【⟹】本项（3）**正好是 R2 的独立反例**：即使 R1 强过，R2 也可因“归零／归箱”混同而失败 ⟹ **双 CLOSED** ✓。
```

$$\boxed{\textbf{R2（条件性）＝ 亦不成立（FAIL）}\ ✗\quad\text{（即：即使宽过 R1，保真性也无可证依据 ✓）}}$$

---

## ⑤ **R3 真压缩判定**（**条件性 ｜ R1/R2 已失败 ⟹ 本节 moot ✗，仅为交付完整；判决不依赖本节** ✓）

> **R3（逐字 ✓）**：$|\mathcal R|\ll|\mathcal C|$，或存在**明确的复杂度下降**（如 $\dim\mathcal R<\dim\mathcal C$），且该下降**不是把原对象换一种坐标写法**；只有 R1+R2 而无 R3 ⟹ **重编码 ⟹ 立即 NO-GAIN** ✓。

```
【反事实：满足 R1+R2 的 R 只有两种 ✓】
    · $R=\operatorname{Cover}$（|𝒥|＝2 ⚠️）：看似“大压缩”✓，但 **非“真”压缩** ✗ —— 算 $R$ **＝解问题本身** ⟹ **验证负担未降** ⟹ 命中 R3 逐字“**把原对象换一种坐标写法**”✗ ⟹ **NO-GAIN（重编码）** ✓；
    · $R=M$（机制型）：$|𝒥|=5$（或 6 ⚠️）看似有限 ✓，但 **需先证 𝔐₅ 穷举**（＝覆盖论证本体 ✗）⟹ **前提即待证命题** ⟹ 不得计为 R3 的真压缩 ✗。
【反面教训对号 ✓（与本题同型 ✓）】
    · `V114` §⑥ **K2-E″ 逐字**：“B(n) 的新输入若最终只提供**绝对大小界／可求和性／截断界**，而**不改变覆盖秩**，则**立即 NO-GAIN**”✓ —— 对号：$R=\operatorname{Cover}$ **不改变覆盖秩**（仍两值 ✓）⟹ **命中 K2-E″** ✓；
    · `V113` §⑤-3 **K3-E 逐字**：“若…判定为‘**无增益**’… ⟹ 出‘…不承担…’ ⟹ **如实归档，不强行推进** ✗”✓ —— 对号：本档 **同型**（R3 无增益 ⟹ 如实归档，不硬判 LIVE ✓）；
    · `V113` §⑦ **P-Type 逐字**：“点式⇏聚合…”✓ —— 对号：逐对象可指的 $R$ **不自动聚合**为全局忠实收缩（点式→全局，同 P-Type 型障碍 ✓）。
```

$$\boxed{\textbf{R3（条件性）＝ 真压缩不成立} \Longrightarrow \textbf{NO-GAIN（重编码／空收缩）}\ ✗}$$

---

## ⑥ **总判定** ＋ 逐条依据

$$\boxed{\ \textbf{总判定 ＝ CLOSED}\ \text{（R1 第一门即失败）}\ \;＋\;\ \textbf{副判 ＝ NO-GAIN（空收缩）}\ \;＋\;\ \textbf{结构结论 ＝ “表征收缩 ≡ 覆盖论证包装”}\ }$$

```
【逐条依据 ✓】
  · 【R1】FAIL ✗ —— $\forall$ 候选 $R$ 皆出局（§③-2）：“$R_1=M$”循环／非全定义；$R_2$（判据空间型）命中 N2／N29 ＋ 坍缩入类 IV；$R_3$（数据侧型）反 Lemma E；$R_4=\operatorname{Cover}$ 空转（§③-2/§③-3 ✓）。⟹ **立即 CLOSED** ✓。
  · 【R2】（条件性）FAIL ✗ —— 忠实性二分 ＋ 归零／归箱反例（§④ ✓）。
  · 【R3】（条件性）NO-GAIN ✗ —— 真压缩不成立；$R=\operatorname{Cover}$ 不改变覆盖秩 ⟹ 命中 K2-E″（§⑤ ✓）。
  · 【结构结论 ✓】R 的完备存在性 **≡** u1（覆盖论证）✗（§③-4 ✓）⟹ **“表征收缩”≡ 覆盖论证（中档 M）的另一种包装，不是新入口** ✓（**与任务书“预期快结论”逐字一致 ✓**）。
【口径标注 ✓（L 编号，强制标口径 ✓）】
  · 本档引 **L1–L4** 时口径＝**宪法 §4.2**（四种合法结构形态 ✓）；引 **L1／L2／L3 三条活路**时口径＝**【总册】第四部分**（`MASTER-…` ✓）—— 二者互换已由 `V108` §⑨-u8 登记 ⚠️，本档**照标、未消解** ✗。
【硬 NO-GO 自检 ✓】本档**不构造对象**（只判 R 的存在性 ✓）⟹ 不预先构造 ⟹ **不直接命中宪法 §1 硬表** ✓；
  且**未触发** `V109` §⑥-5 K1-A/B/C/D（未改称 T5／未再枚举候选／未以 {ρ} 为输入／未强推进 ✓）。
```

---

## ⑦ **对 `V109` 三方向排序的影响**

```
【原排序（`V109` §⑤-4 逐字表 ✓）】(i) 归纳式分类 ⚠️｜(ii) 表征收缩 ⚠️（“**抓手最实**”）｜(iii) 反例搜索 ✅
【本轮影响 ✓】
  · (ii) 表征收缩 —— **下调** ⬇️：本轮证其**无独立入口**（R 的存在性≡u1 ✗；“抓手最实”的判据空间路径命中 N2／N29 ＋ 坍缩入类 IV ✗）⟹ (ii) **并入 (i)**（同一对象 ✓），**不再单列为并列方向** ✗。
  · (i) 归纳式分类 —— **不变（但地位上升为唯一 “收缩底座”）** ⚠️：因 (ii) ≡ (i) 的**全局版**，而 (i) 的**逐类／局部版**仍可做（`V109` §⑤-1 逐字“局部可做”✓）⟹ **收缩问题归约为“逐类 normal form”** ✓。
  · (iii) 反例搜索 —— **不变为最高优先级 ✅**：因 (i) 全局受阻（𝔐₅ 穷举未证 ✗）、(ii) 判为包装 ✗ ⟹ 唯一**现成可判定判据**仍在 (iii)（`E101` §8.4 CS-1 二择一 ✓）。
【新排序 ✓】$$\textbf{(iii) 反例搜索}\ \succ\ \textbf{(i) 归纳式分类︖局部 normal form}\ \succ\ \textbf{(ii) 表征收缩（降为包装）}$$
【与另两轮审计的一致性 ✓】与 `V113`（F1 因“不消费目标输入”降为 LIVE\* ✓）、`V114`（bl1 因“不改覆盖秩”判 CLOSED ✓）**同向** —— 三者共同表明：**“不改覆盖秩／不消费目标输入”的动作一律 NO-GAIN** ✓（＝ 本档 §⑤ 的同一模式 ✓）。
```

---

## ⑧ 证据等级 ＋ 未证清单（诚实边界 ✓）

```
【证据等级 ＝ **结构性／档案级**（KNOWN-candidate level） ✓】
   · §② 的 𝒞 定义、C1–C4＋C★ 判据、E105 五类／第六种、三处硬 NO-GO（N2／N29／N31）—— **均有逐字出处** ✓；
   · ⚠️ **〔本方拟定义〕／〔本方推论〕部分（已逐处标注 ✓）**：§②-3 合取式 admissible、§②-4-J、§③-2 候选枚举与二分、§④ 归零／归箱反例、§⑤ 真压缩判定、§⑦ 新排序 —— **皆为**本方判断**，非档案定理** ✗。
   · ⚠️ **不得写成**：“不存在表征收缩” ✗／“覆盖论证已证” ✗／“T5 已死” ✗（宪法 §0“未找到 ≠ 不存在”✓；`E105` §7 逐字 ✓）。
【未证清单 ✓】
  u1 𝔐₅ 的**穷举性未证** ✗ —— 即覆盖论证本体（`E105` §5／`CLOSED-ROUTES-MAP` §E.4／`E102` §7 ✓；`V109` §⑨-u1 同 ✓）。
  u2 §③-4 **A–I ＋ J** 未由唐先生裁定 ⚠️ —— 本档只**指出** J（𝔐 外延两读）决定 R1 死活，**未裁定** ✗（需唐先生 ✓）。
  u3 “靶1 ≡ u1 ≡ 覆盖论证”为**本方判断** ✓（五处逐字互指支撑 ✓；`V109` §⑨-u3／`V108` §⑨-u3 同 ✓）。
  u4 **`E106` §2 normal form 仅 partial** ⚠️（‘B 分支（非有效）未被分类”✗，`E106` §6 逐字 ✓）—— 本档未推进 ✗。
  u5 **D1／C1–C4 能否“切到有限”未证** ✗（＝ `V109` §④-2-H ✓）—— 本档未推进 ✗。
  u6 **未逐字读** `E102` 全文（只取 §1 表／§8／§6 三处 ✓）与 `E101` §8.4（经 `V109`／`V108` 转引 ✓）—— 记录为**间接引用**缺口 ⚠️。
  u7 **口径张力**：引 L 编号必须标口径 ✓（宪法 §4.2 vs 【总册】第四部分；已互换 ⚠️，未消解 ✗，同 `V108` §⑨-u8 ✓）。
【纪律自陈 ✓】零代码 ✓｜**仅新建本档 1 份** ✓｜未改任何既有文件 ✗✓｜只用显式路径 ＋ `docs/` 内**定向** `grep -n`／`sed` ✓（未全盘 find／grep ✗）｜未触碰 `fn_backup／fupeng／github／quant-system／.git` ✓｜未用 RH ✗｜未预设结论 ✓｜命中 R1 KILL ⟹ **立即 CLOSED 并如实归档** ✓。
```

---

## 附：提交链与指针

```
【本档】`docs/V115-representation-contraction-r1r2r3.md`（**新建 1 份** ✓；未改任何既有文件 ✗；编号 `V115` ✓）
【对象逐字源 ✓】`V109` §②（三档）／§③-2（拟定义 C ✓）／§③-3（防火墙）／§③-4（A–I）／§⑤（三方向）／§⑥-5（KILL ✓）
【admissible 判据 ✓】`E102` §1（C1–C4 ＋ 内在性 C★ ✓）／§8（靶1 ✓）｜`E105` §5（𝔐₅ ✓）／§7（第六种 ✓）／§6（Lemma E ✓）／§3（二分 ✓）
【反面教训 ✓】`V114` §⑥（K2-E″ ✓）／§⑦（总判定 ✓）｜`V113` §⑤-3（K3-E／K3-A–D ✓）／§⑦（P-Type ✓）
【宪法 ✓】`RESEARCH-CONSTITUTION` §1（硬 NO-GO ✓）／§2.1（N2 ✓）／§2.4（N31 ✓）／§2.6（N29 ✓）／§4.1（A–H）／§4.2（L1–L4 ✓）
【编制同源 ✓】`V108`（靶1 精确化 ✓：§②／§④(c)／§⑤-4 “应改写为覆盖论证”✓）
```

```
【一句话 ✓】方向 (ii)“表征收缩”**首轮 KILL 审计**：𝒞 ＝ **识别方案 I=(D,M,E)**（D 过 `E102` C1–C4，M∈𝔐₅⊕6th，E 可认证）；
   所有自然候选 R 均在 **R1** 出局（循环／非全定义／命中 N2，N29／反 Lemma E）⟹ **R1＝不成立 ⟹ CLOSED**，且 **“表征收缩 ≡ 覆盖论证的另一种包装”（非新入口）**；
   三方向新排序 **(iii) ≻ (i) ≻ (ii)**（(ii) 降为包装）✓。
```
