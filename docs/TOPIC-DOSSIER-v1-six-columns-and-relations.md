已查地图：命中（`CONCRETE-TOPIC-LIST-r2`／`TOPIC-INVENTORY-MASTER-v1`）⟹ 扩充其逐条为六栏档案，不开新案
D0: 本档对象 = **逐课题六栏档案**（领域／内容与要求／前沿／难点／攻击点／关联）＋ **全课题逻辑关联**（方法族／竞争分层／依赖复用／共同瓶颈）
D1: 0 （[REVIEW] 轮次：档案与关联，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **课题档案 v1（六栏 ＋ 逻辑关联）**

> 验证等级：`逐字` = 检索片段逐字；`档级` = 领域知识，**未核**。

## **T-1 `z_L(5,5)` 精确值** ⭐（首攻候选）

```
**【领域】** 极值组合（Zarankiewicz 变体：limited augmented `C_4`-free 二分图）；与编码理论邻接 ✓
**【内容与要求】** `P`：确定 `z_L(5,5)`。`X`：`5\times5` 二元矩阵（无全 1 的 `2\times2`）＋"limited augmented"附加约束（**须逐字核**）✓
　**要求成果形态**：精确整数；或**完备的不可达证书**（排除上界 `16`）✓
**【前沿】** `z_L` 在 `6\times4`、`5\times3`、`5\times4` 有精确值；`5\times5` **下界 `15`**；作者逐字称确定精确值需"exhaustive search over all possible triples and quadruples of 2-edges for all extremal `C_4`-free graphs on `5\times5`"，且"beyond the scope of this paper" ✓（`逐字`）
**【难点】** **(a)** `z_L` 定义须先锁定；**(b)** "extremal `C_4`-free `5\times5` 图" **是否已完备枚举** —— 若无，**它是前置子任务**；**(c)** 穷举后仍需**同构去重 ＋ 双侧证书** ✓
**【我们的攻击点】** `D✓✓`（exact 枚举 ＋ 双证书：存在侧给矩阵；不存在侧给**穷尽性论证/覆盖证书**）＋ `A`（`S_5\times S_5` 群作用降代表元）＋ `G`（最小反例）✓✓
**【关联】** 与 `T-6/T-7` 同属**方法族 M1（表填空＋双证书）**；与 `T-9` 同源问题族但 `T-9` **已被收割** ⟹ **`T-1` 的差异化 = 作者留下的、规模明确受限的缺口** ✓✓
```

## **T-2 `AG(7,3)` 最大 cap**（候选，规模待估）

```
**【领域】** 有限几何 / cap set ✓
**【内容与要求】** `P`：`c(7)`（`AG(7,3)` 最大 cap 大小）。要求：**精确值**或**严格改进的界** ✓
**【前沿】** `AG(6,3)` 最大 cap `=112` 且**仿射等价唯一**（Potechin 2008）✓；上界约 `2.756^n`（Ellenberg–Gijswijt）✓；逐字："It is still not known how large maximal caps are in dimensions larger than 6" ✓（`逐字`）
**【难点】** `3^7=2187` 点，规模**天文**；**缺少压缩机制**（除非走多项式方法/对称）⟹ `G2` 天然适配度**低** ⚠️
**【我们的攻击点】** `A`（cap = 无三元共线；可用仿射群与多项式方法）——**但 `D` 仅在极小规模可用** ⟹ **攻击点弱** ⚠️
**【关联】** 与 `T-3`（complete cap）同族；与 `T-7`（arc/saturating）同族；**三者共享"有限几何结构"但我们的工具最不占优** ✓
```

## **T-3 `t(n)`：`AG(n,3)` 最小 complete cap**（暂：疑已解）

```
**【领域】** 有限几何 ✓
**【内容与要求】** `P`：`t(n)` 的上界改进（已知 `\sqrt2\,3^{n/2}\le t(n)\le 2^n`）✓
**【前沿】** Csajbók–Nagy 2025 `Problem 5.1` **明确列为 open**；**但** Grace–Voloch 已给 `O(3^{n/2})` 代数构造，博客逐字"thus **solving the problem**" ✓（`逐字`）
**【难点】** **疑已闭合** ⟹ 须先核；若仅余常数精细 ⟹ 属"改进型"，价值低 ⚠️
**【我们的攻击点】** **弱**（代数构造机制已占）✓
**【关联】** 与 `T-2` 同族 ⟹ **`T-2` 的"未被覆盖"结论须连带重核** ⚠️
```

## **T-4 SNIEP `n=5`（对称非负逆特征值）**（候选，须离散化）

```
**【领域】** 矩阵分析 / 逆特征值问题（含图版本 IEP-G）✓
**【内容与要求】** `P`：刻画 `n=5` 的全部可实现实谱。要求：**充要条件**或**有限列表上的判定 ＋ 证书** ✓
**【前沿】** `n\le4` 完全解决；逐字："remains unsolved for `n\ge5`" ✓；`5\times5` 有部分充分条件（Loewy–McDonald、Soto 等）✓（`逐字`＋`档级`）
**【难点】** 谱空间**连续**；现有结果**碎片化**（充分/必要条件各半）；**缺统一判据** ⚠️
**【我们的攻击点】** `E✓✓`（**trace／rank／inertia／Perron 是必要条件的天然来源**）＋ `D`（对**有限整数谱**逐个精确判定并出证书）✓；**第一步**：取一批"未判定"小整数谱，用必要条件先筛，再尝试构造实现矩阵 ✓
**【关联】** 与 `T-5` **强关联**（图 IEP 的 inertia set 是**同一工具箱**）；`T-5` 的完备表可作为 `T-4` 的**工具资源** ✓✓
```

## **T-5 图的 minimum rank / inertia set：小阶完备表** ⭐（规模最友好）

```
**【领域】** 图谱论 / 矩阵分析（min rank、inverse inertia、零强迫数）✓
**【内容与要求】** `P`：确定 `n` 阶**所有图**的 min rank（或 inertia set）。要求：**完备表 ＋ 每图双证书** ✓
**【前沿】** inverse inertia 对"阶数不超过某值"的图**已解决**（**阈值须核**）✓；图族结果较多；表随 `n` 增长极快 ✓（`逐字（截断）`＋`档级`）
**【难点】** 每图需 **(i)** 矩阵证书（低秩实现）＋ **(ii)** **秩下界证明**（零强迫集/覆盖论证）⟹ **下界侧是主要难点**；另需**同构去重** ⚠️
**【我们的攻击点】** `E✓✓ D✓✓`（rank／inertia／零强迫；**与我们的秩–迹–惯性工具直接对口**）✓；**第一步**：核阈值 ⟹ 若 `n=8` 未完，做 `n=8` 表（约 `1.2\times10^4` 图，去重后略少）✓✓
**【关联】** 与 `T-4/T-8` 同属**方法族 M2（秩/惯性）**；**为 `T-4` 提供结构素材** ✓✓
```

## **T-6 covering code `K_q(n,R)` 具体格子**（候选，须逐格筛）

```
**【领域】** 编码理论 / 覆盖半径 ✓
**【内容与要求】** `P`：具体格 `(q,n,R)` 的 `K_q(n,R)` 精确值。要求：精确值或**改进一方界 ＋ 机器可核证书** ✓
**【前沿】** 2026-08 `arXiv:2608.19872` 改进 **83 个界**（`q\ge5` 自 2011 以来**首次上界改进**）；`K_6(8,4)\le166`（原 `216`）；`441\le K_6(10,4)\le2751`；**全部码与证书机器可读 ＋ 独立验证器** ✓（`逐字`）
**【难点】** 该跑道**已自带证书链**（我们的传统优势被削弱）；大面积已被推进 ⟹ 须**逐格筛"仍开且差距小"** ⚠️
**【我们的攻击点】** `A D`（有限域线性码；覆盖条件 = **syndrome 覆盖**；可用线性代数 ＋ 对称压缩）；**差异化 = 上界方向的穷尽性证书** ✓
**【关联】** 与 `T-1/T-7` 同族（表填空）；与 `T-9` **竞争结构类似** ⟹ **必须先做"未收割"核验** ⚠️
```

## **T-7 `PG(2,q)` 的 minimal `1`-saturating sets**（候选）

```
**【领域】** 有限几何 / 射影平面 ✓
**【内容与要求】** `P`：具体 `q` 的最小 1-saturating set 尺寸。要求：精确值或改进界 ✓
**【前沿】** `q\le16` 有表（Pambianco 等）；大 `q` 仅渐近界（Nagy：`|S|\le(1+o(1))\sqrt{(n+1)q^{n-1}\ln q}`）✓（`档级`）
**【难点】** `q` 增大时 `q^2+q+1` 点**穷举不可行**；须**结构构造 ＋ 上界技巧** ⚠️
**【我们的攻击点】** `A`（`PG(2,q)` 结构与对撞）＋ `D`（小 `q`）；**差异化有限** ⚠️
**【关联】** 与 `T-2/T-3` 同族；与 `T-6` **同机制**（syndrome 覆盖 ↔ saturating）✓✓
```

## **T-8 `\pm`-rank of `(0,\pm1)`-matrices**（暂缓）

```
**【领域】** 矩阵组合 / 符号模式 ✓
**【内容与要求】** `P`：`\pm`-rank 的界与分类（具体类：ASM、二分图关联矩阵）。要求：精确值/界/分类 ✓
**【前沿】** `ILAS 2026` 报告：新秩概念，建立 `\pm`-rank 与 binary/term/实秩的不等式 ✓（`档级`）
**【难点】** **缺口未锁定** ⟹ 须先扫文献找**具体开放格** ⚠️
**【我们的攻击点】** `E D` ✓
**【关联】** 与 `T-5/T-4` 同族（秩）✓
```

## **T-9 / T-10（REJECT，附死因）**

```
**【T-9】** `z(m,n;2,2)` frontier：**2026 `arXiv:2605.01120`（LLM 进化搜索）已系统收割"差一条边"格** ⟹ **他方机制更强** ✓（`逐字`）
**【T-10】** `R(5,5)`（`43\le R\le46`）／`ES(7)`（`33\le ES(7)\le81`）／Hadamard `668`：**计算密集 AI 队已收割** ✓
```

# **全课题逻辑关联**

## §A 方法族（骨架）

```
$$\textbf{M1 表填空＋双证书}:\ T\text{-}1,\ T\text{-}6,\ T\text{-}7\ (\text{同族已死}:T\text{-}9)$$ ✓
$$\textbf{M2 秩/惯性/符号}:\ T\text{-}4,\ T\text{-}5,\ T\text{-}8$$ ✓
$$\textbf{M3 有限几何结构/构造}:\ T\text{-}2,\ T\text{-}3$$ ✓
【判读】**我们最强 = `M1` 的"双证书"与 `M2` 的"秩下界"；`M3` 最弱** ✓✓
```

## §B 竞争分层（决定"能不能碰"）

```
$$\text{被收割层}:\ T\text{-}9,\ T\text{-}10\ (\text{差一条边型}／\text{著名数值型})\ \Longrightarrow\ \textbf{禁止入场}$$ ✓
$$\text{作者留缺口层}:\ T\text{-}1\ (\text{原文自述"beyond the scope"})\ \Longrightarrow\ \textbf{首选}$$ ✓✓
$$\text{规模可控层}:\ T\text{-}5\ (n=8,\ \sim10^4\ \text{图})\ \Longrightarrow\ \textbf{首选}$$ ✓✓
$$\text{自备证书层}:\ T\text{-}6\ (\text{已有机器可读证书链})\ \Longrightarrow\ \textbf{优势被削弱}$$ ⚠️
$$\text{机制已占层}:\ T\text{-}3\ (\text{代数构造已完成})\ \Longrightarrow\ \textbf{暂/退}$$ ⚠️
```

## §C 依赖与工具复用（谁喂谁）

```
$$T\text{-}5\ \text{的完备表}\ \longrightarrow\ T\text{-}4\ \text{的判定素材（inertia/rank 约束）}$$ ✓✓
$$T\text{-}1\ \text{的前置子任务（extremal }C_4\text{-free }5\times5\ \text{图枚举）}\ \longrightarrow\ \text{可复用于其它 Zarankiewicz 变体与 }T\text{-}6/T\text{-}7\ \text{型"表填空"}$$ ✓
$$T\text{-}6\ \text{与 }T\text{-}7\ \text{同机制（syndrome 覆盖）}\ \Longrightarrow\ \text{一方的上界技巧可移植}$$ ✓
```

## §D 共同瓶颈与共同风险

```
**【共同瓶颈 = 下界侧的"穷尽性证明"】** `T-1/T-6/T-7` 的难点**全在**"证明没有更好构造"⟹ **这正是我们 `D`（完备性证书）的差异化所在** ✓✓
**【共同风险 = 他方机制】** LLM 进化搜索（`T-9`）、SAT/nauty 队、编码理论证书链（`T-6`）⟹ **每个候选入场前必做"未收割核验"** ✓✓
【共同结构】 `M1` 全部可写成"有限对象 + 存在侧证书 + **不存在侧完备证书**"；`M2` 全部可写成"矩阵族 + **秩/惯性下界**" ✓
```

## §E 一句话逻辑链

```
$$\boxed{\text{资产}(D/E)\to\text{方法族}(M1/M2)\to\text{具体课题}(T\text{-}1/T\text{-}5)\to\underbrace{\text{穷尽性/秩下界}}_{\text{难点＝差异化}}\to\text{可登记成果}}$$ ✓✓
$$\text{反向（禁止）}:\ \text{手头推导}\to\text{顺手继续算}\ \Longrightarrow\ \text{即 }U_{2,3}\ \text{的教训}$$ ✗
```

## §F 下一步（单点，二选一）

```
**(1)** 核 `T-5`：inverse inertia **已解决的阶数阈值** ＋ `n` 阶图数 ⟹ 若 `n=8` 未完且 `\sim10^4` 图 ⟹ **升首攻** ✓
**(2)** 核 `T-1`：是否仍 open ＋ `z_L` 定义逐字 ＋ 极端 `C_4`-free `5\times5` 图是否**已完备枚举** ✓
【⛔ 纪律】 本轮**未计算、未实现**；`U_{2,3}` 暂停 ✓
【边界】 `T-2/T-4/T-5/T-6/T-7/T-8` 的"前沿"栏含**档级**内容（未逐字核）；`T-1/T-3/T-6/T-9` 含**逐字**片段 ✓

## §附 【技术词回查】（补录）
```
技术词 zero forcing     命中文件数=0    :: 
技术词 syndrome covering 命中文件数=0    :: 
```
