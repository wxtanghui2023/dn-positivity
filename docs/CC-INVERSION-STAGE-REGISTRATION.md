已查地图：命中 1 处 —— 先逐条判 已DEAD/已封/已登记；命中即引既有条目，不得开新案
　判定：唯一命中＝**本日自建 `E-9`**（`ASSETS-REGISTRY` L139，自命中）⟹ 非新案 ✓
　（`反例约束反演`／`KERNEL-HUNT` 五档检索 = **0** ⟹ 本档新增；`缺失坐标` 命中＝自命中；`核方向`(2)／`硬判据`(7) ⟹ **异义，引用不主张** ✓）

# **反例约束反演（Counterexample-Constrained Inversion）· 阶段登记**

**唐先生令（2026-09-22 21:26）**：
① ✅ **D-13 应正式封死** ✓（已落 `ASSETS-REGISTRY` **D-13**，状态＝**已封闭** ✓）；
② ⭐ **三层连续验证（照录）**：
　$$\boxed{\text{换对象}\;\to\;\text{换交互}\;\to\;\text{换表示}}\quad\textbf{都会被成熟数学机器吸收}$$ ⟹ 继续沿此元层面搜索**收益已很低** ✓✓；
③ ⭐ **暂停所有新 generator 的文献地图搜索** ✓；
④ **下一阶段只做一件事**：`KERNEL-HUNT` ＝ **反例约束反演**——不是找新理论，而是把已有 NO-GO 反例反过来做**核分析** ✓✓；
⑤ **流程只有四步**：`反例族 → 旧约束映射 Φ → ker Φ → 缺失坐标` ✓；
⑥ ⭐ **四条"没有"（照录）**：**没有 13 门。没有 generator 搜索。没有文献先验。没有 RH 接口要求。** ✓✓
　（⚠️ 保留的只有**仓内**地图纪律：`prework_map_check` 是**仓库自检**、非文献检索 ✓）
⑦ ⭐⭐ **唯一硬判据（照录）**：
　$$\boxed{\text{如果我们自己不能从已有反例证明某个新自由度确实存在，就不准宣布"新方向"}}$$ ✓✓

D0: 本档对象 = **反例约束反演阶段的登记与三实验场映射**（档案已有对象 `E-9 反向生成器`＋`REVERSE-0 扫描` 的**阶段化**；非重命名、非新对象）
D1: 0
FREEZE-ACK: 零计算／零 Lean／未写程序／未调用 C-380 代码／**未开案** ✓

---

## §1 流程与定义（照录 ＋ 形式化 ✓）

```
【Φ】 ＝ **所有已经通过审计的旧约束**（逐条可查档；`Φ: X → ℝ^m`）✓
【反例族】 `X_t`（连续或离散参数）满足 φ恒等而 Q 变化：
　$$\Phi(X_t)=\Phi(X_0)\qquad\text{而}\qquad Q(X_t)\neq Q(X_0)$$ ✓
　⟹ 此时 `X_t` **不是"又一个反例"**，而是**旧约束的不可见方向** ✓✓
【四步】 反例族 → 旧约束映射 Φ → `ker Φ` → **缺失坐标** ✓
【⭐ 与之前阶段的分界】 本阶段**不设定**"机制应长什么样"、**不查文献先验**、**不适用十三门**；
　**唯一准入** ＝ **能从已有反例证明该自由度的存在** ✓✓
```

## §2 三个天然实验场 × 硬判据执行（**逐字引用档案** ✓）

### ① β-blindness —— ⭐ **硬判据：已满足（定理级）** ✓✓
```
Φ ＝ **γ 侧聚合量**：既证 **`序数测度 μ_γ` 的 Hankel 矩 ＝ β-盲（门①②）**（`MASTER-STATUS-AND-CLOSURES` L773）✓
族 ＝ **离轴对 (β,γ)** 连续参数 ＋ 在线零点集（`V192`：`Ξ` 非实零点 ⟺ `ξ` 离轴零点）✓
见证 ＝ **同一 γ 侧数据而 β 不同**（Hankel 矩不变 ⟹ `Φ` 相同）✓｜Q ＝ **RH**（是否全在线）⟹ **变化** ✓
【硬判据】 ✅ **新自由度存在已被档案证明**（**β-盲定理**本身即"存在 β 方向落于 `ker Φ`"的证明）✓✓
【缺失坐标候选】 **重数／退化** —— `V192` 逐字：「离轴对 ＝ **二重（退化）点**，在线零点 ＝ 单点」✓
　（当日硬问题原话亦同向：「能否构造**同时依赖 (β,γ)** 的对象」✓）
【本阶段新问题】 ⭐ **最小什么扰动能让 `Φ` 开始感知 β？**（＝求最小复杂度 `Ψ`，使 `Φ+Ψ` 对 β 敏感）✓
```
### ② C-380 —— ⭐ **硬判据：已满足（反例级）** ✓✓
```
Φ ＝ **even 约束**（`F_{2r} ≤ 1/2`；`k≥4`／`k_min=5` 等**已通过审计的偶侧约束**）✓
族 ＝ **共享同一批 active even constraints 的五点构型** ✓
见证 ＝ **S1-A 近碰撞**：`ASSETS-REGISTRY` 逐字「则存在 `|δ| ≲ 8.8×10^-20` 的近碰撞」＋
　「→ near-collision → R／Hankel → 【**pairwise-cancellation wall**】」✓✓
Q ＝ **odd 侧行为**（奇层非退化／谱分离）⟹ **变化** ✓
【硬判据】 ✅ **已由反例证明**：`ker(even) ∩ odd-sensitive ≠ ∅` ✓✓（该近碰撞**就是**存在性见证 ✓）
【缺失坐标候选】 **符号／相位（σ／φ）的细结构**（超越 `|u_j|` 的**支撑分离**）✓
【本阶段新问题】 ⭐ **求对近碰撞稳健的相位敏感 functional**（须能区分 `|δ| ≲ 8.8e-20` 的成对消去见证）✓
```
### ③ Li／算术下降（prefix side）—— ⚠️ **硬判据：部分满足（经验级，待升）**
```
Φ ＝ **prefix／Toeplitz／Hurwitz 不变量**（已通过审计者）✓
见证（经验级 ⚠️）＝ `DOOR1` **交叉律**：首个负指标 `n_first ≈ 3.2–3.5 × 2γ²/|2β−1| ≈ 6.6γ²/|2β−1|` ✓
　⟹ **核方向候选** ＝ 保持 `γ²/|2β−1|` 不变、而改变 `(β,γ)` 的方向 ✓
Q ＝ **零点位置／RH** ⟹ 沿该方向**变化** ✓
【硬判据】 ⚠️ **尚未达"证明"**：交叉律为**经验常数律**（`DOOR1` 数值观测 ⚠️）⟹ 须先**升为定理或给出反例级见证**
　⟹ ⛔ **依硬判据：本场暂**不准宣布新方向** ✓✓（这正是判据的应用示例 ✓）
【本阶段新问题】 ⭐ **哪些零点敏感方向被现有 prefix invariant 完全杀掉？**（须先取得**可证或反例级**的见证）✓
```

## §3 硬判据执行表（**本阶段核心纪律** ✓✓）

| 实验场 | `Φ` 来源 | 新自由度存在性 | 是否可宣布方向 |
|:--|:--|:--|:--|
| ① β-blindness | β-盲定理（档级） | ✅ **定理级** | **可** ✓ |
| ② C-380 | S1-A 近碰撞（反例级） | ✅ **反例级** | **可** ✓ |
| ③ Li／desc. | `DOOR1` 交叉律（**经验级** ⚠️） | ⚠️ **未达证明** | ⛔ **暂不准** ✓ |

```
【判据应用（示范）】 ③ 恰说明该硬判据**有牙齿**：同一族在三层文字搜索中都"看起来像新方向"，
　但**只要拿不出自有证明或反例级见证** ⟹ **不准宣布** ✓✓
```

## §4 边界与回查（✗✓）

```
✗ 零计算／未写程序／未调用 C-380 代码／**未适用十三门（依令）**／**未做 generator 文献搜索（依令）**／**未开案** ✓
✗ 不把三实验场记为战绩；① ② 的"可宣布"仅指**自由度存在**已被证，**不**暗示新定理已得 ✓
⚠️ ③ 明标**经验级**并**主动禁其宣布**（判据执行 ✓）；`核方向`／`硬判据` 检索命中为**异义**，引用不主张 ✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 反例约束反演 命中文件数=0 ⟹ **本档新增** ✓
　技术词 KERNEL-HUNT   命中文件数=0 ⟹ **本档新增** ✓
　技术词 缺失坐标     命中文件数=2 ⟹ **自命中**（REVERSE-0／E-9）⟹ 引用 ✓
　技术词 核方向       命中文件数=2 ⟹ **档案已有（异义）⟹ 不主张** ✓
　技术词 硬判据       命中文件数=7 ⟹ **档案已有（异义）⟹ 不主张** ✓
【档级引用】 `MASTER-STATUS-AND-CLOSURES` L773（Hankel 矩 β-盲）／L360（`V192` 退化点＋当日硬问题）｜
　`ASSETS-REGISTRY` L537（近碰撞 `8.8e-20`）／L565（pairwise-cancellation wall）／**D-13**（已封闭）／**E-9**（反向生成器）｜
　`DOOR1`（交叉律，**经验级** ⚠️）✓
```
