已查地图：命中 5 处 —— 先逐条判 已DEAD/已封/已登记；命中即引既有条目，不得开新案
　判定（逐条）：**`V290` 命中为实质性** ⭐ —— 档案已有 **`O1-1` ＝〔合法类内 off-line 锚点〕＋〔**有限纤维分离**〕**，
　且已**结构性封口**（两墙分别 ↔ **GRH 反例问题**／**on-line 伪装排除问题**），并写明**禁语纪律**：
　**允许**「现有两类锚定路线均已被精确封锁」／**禁止**「`O1-1` 不存在」「已全局数学封口」✓✓
　其余命中（`V256`／`E18-NOGO-ALIGNMENT` 等）为用词异义 ⟹ 不计 ✓
　⟹ **"有限纤维"语言在本仓已存在** ⟹ 本档**引用**，**不**列为本档提出 ✓

# **KERNEL-HUNT-1**：两个最小化问题（`Ψ_β^min` ／ `Ψ_C380^min`）

**唐先生令（2026-09-22 21:29）**：
① ✅ **D-13：CLOSED** ✓；② **换对象 → 换交互 → 换表示：全部暂停**（不再用文献地图寻找 generator）✓；
③ **KERNEL-HUNT 固定为**：$$\boxed{\text{反例族}\ \to\ \Phi\ \to\ \ker\Phi\ \to\ \Psi_{\rm missing}}$$ ✓；
④ **四条纪律（照录）**：**不先命名新理论**｜**不先找文献背书**｜**不把已有结论换符号**｜**不把"存在核"直接当成"存在新机制"** ✓✓；
⑤ **`β`-blindness：定理级** ✓（β 方向已确认落在现有观测的 kernel）⟹ 下一问**不是**"β 是否可见"，而是找**最小 `Ψ`** 使 β 从 kernel 中出来 ✓；
⑥ **C-380：反例级** ✓ ⟹ 下一问＝构造**对近碰撞仍稳健的相位/符号敏感 functional** ✓；
⑦ **Li／算术下降：HOLD** ⚠️（只有经验迹象，未达"可证或反例级见证"门槛）⟹ **不宣布 kernel** ✓；
⑧ ⭐⭐ **关键区分（照录）**：$$\ker D\Phi\quad\text{vs.}\quad\Phi^{-1}(\Phi(X_0))$$ ——
　「**一阶 kernel 不够**。如果二阶约束立即把扰动压死，就只是**切空间假象**；
　真正值得升级的见证必须尽可能给出**有限纤维中的两个点 `X≠Y`**，满足**旧观测完全相同而目标量不同**」✓✓；
⑨ ⭐ **核心原则（照录）**：「**先证明旧坐标系确实漏掉了一个可观测自由度，再让那个自由度自己告诉我们新量应该长什么样**」✓✓

D0: 本档对象 = **两个最小化问题（`Ψ_β^min`／`Ψ_C380^min`）的执行与观测量阶梯判定**（引用既有 `O1-1`／`V192`；**未开案** ✓）
D1: 0
FREEZE-ACK: 零计算／零 Lean／未写程序／未调用 C-380 代码／**未做 generator 文献搜索** ✓

---

## §1 witness 阶梯（**本档核心纪律** ✓✓）

```
【L0 切空间级】 只有 `ker DΦ ≠ 0`（一阶）⟹ **可能是切空间假象** ⛔（若二阶约束立刻压死则无效）✓
【L1 近似纤维级】 两个具体点 `X≠Y` 使旧观测**近似**相同（差 `δ`）、目标量不同 ⟹ **次优**（须报 `δ`）⚠️
【L2 精确纤维级】 `X≠Y` 使旧观测**完全相同**、目标量不同 ⟹ ⭐ **本阶段要求的目标形态** ✓✓
【L3 二阶稳健精确纤维】 L2 ＋ **对二阶/高阶约束稳健**（扰动不被高阶项杀死）⟹ 最强 ✓✓
【判定规则】 只有 **L2／L3** 才允许升级；**L0 一律不准**；**L1 须报 `δ` 并给出升 L2 的路径** ✓
```

## §2 问题 A：`Ψ_β^min`（β-blindness 场）

```
【已有（档级 ✓）】 `Φ_old` ＝ γ 侧聚合量；**β-盲定理**（Hankel 矩 β-盲）＝ L0 级证据（β 方向在 `ker` 内）✓
　另：`V192` 逐字「**RH ⟺ 纵坐标谱无非本质退化**」＋「离轴对＝**二重（退化）点**，在线零点＝单点」✓
【⭐ 档案对"最小 Ψ"的既有答案】 `V192` §② 逐字：「硬问题的答案 ＝ **是的，只能看到 γ，原因是结构性的**」＋
　「**谱实现族 β-内容 ＝ 退化计数 ＝ `N_0^s／N_d` 问题**」＋「**谱实现族不是第四类，是同一堵墙的 A 侧**」✓✓
　⟹ **候选 `Ψ_β^min` ≅ 退化计数（重数型 observable）** ✓（＝重数/多重点计数，而非 γ 位置本身）
【L2 检验（本档执行）】 构造精确纤维对：`X` ＝ 含一个离轴对（贡献 γ 处**二重**点）｜`Y` ＝ 同一 γ **集合**而该点**单重**（全在线）
　⟹ 旧观测（γ **集合**）相同 ✓ 而目标量（RH）不同 ✓ ⟹ **集合 vs 多重集** 层面成 L2 ✓
　⛔ 但⚠️ **该 L2 对落入 `O1-1` 的两墙**（`V290` 逐字）：〔合法类内 off-line 锚点〕↔ **GRH 反例问题**；〔**有限纤维分离**〕↔ **on-line 伪装排除问题** ✓✓
　⟹ 依 `O1-1` 禁语纪律：**允许**"该路线已被精确封锁"；**禁止**"不存在" ✓
【⭐ 本档判定（硬判据应用）】 **坐标存在 ✓（定理级）**，但 **`Ψ_β^min` 的"新性"被否** ✗ ——
　因其**已等于既有墙的 A 侧**（`N_0^s／N_d`，与 `Alpöge–Furman` 带宽问题同墙，档级 ✓）⟹ **不宣布新方向** ✓✓
```

## §3 问题 B：`Ψ_C380^min`（C-380 场）

```
【已有（档级 ✓）】 `Φ_old` ＝ even 约束；见证＝**S1-A 近碰撞**：`|δ| ≲ 8.8×10^-20` ＋
　「near-collision → R／Hankel → 【pairwise-cancellation wall】」✓✓
【⭐ 本档的阶梯判定】 该见证＝**L1 近似纤维级** ⚠️（`δ ≈ 8.8e-20 ≠ 0`：even 约束**近似**相同而非完全相同）
　⟹ 依 §1：**L1 不得直接升级**，但**须给出升 L2 的路径** ✓
【⭐ 本阶段最锐利的可执行问题（无需文献、无需程序）】
　$$\boxed{\text{能否把 C-380 近碰撞升级为**精确纤维见证**：even 约束**逐条精确相等**而 odd 行为不同？}}$$ ✓✓
　（若可 ⟹ **L2 达成**，则该自由度**已由自有反例证明存在** ⟹ 依硬判据**可宣布** ✓；
　　若证不可 ⟹ 得到一个**结构性否定**（切空间假象型结论），同样是有价值的判据输出 ✓）
【候选 `Ψ_C380^min` 形态】 **对 `|u_j|` 近碰撞稳健的符号/相位敏感 functional**（＝在近碰撞极限下仍非零的 σ／φ 型量）✓
　要求：**二阶稳健**（不被更高阶偶约束压死）⟹ 目标形态 **L3** ✓✓
```

## §4 本档净产出（**不命名新理论、不引文献背书** ✓）

```
① **阶梯纪律落地**：L0（切空间）／L1（近似纤维，须报 δ）／L2（精确纤维）／L3（二阶稳健）✓
② **β 场**：候选 `Ψ_β^min` ＝ **退化计数**；**坐标存在（定理级 ✓）** 但**新性被否**（＝既有墙 A 侧／`O1-1` 两墙）⟹ **不宣布** ✓
③ **C-380 场**：现有见证＝**L1**；本阶段唯一可执行动作 ＝ **升 L2（精确纤维）或证不可** ⟹ **二选一，皆可交付** ✓✓
④ **Li／算术下降**：维持 **HOLD**（依令）✓
【下一步（二选一，均不需新文献／不需程序）】
　(a) **C-380 升 L2**：把近碰撞升级为精确纤维见证（或证不可）✓
　(b) **β 场换问法**：把"最小 Ψ 使 β 可见"改为"**在 `O1-1` 两墙内的合法类里，退化计数是否可观测**" ✓
```

## §5 边界与回查（✗✓）

```
✗ 零计算／未写程序／未调用 C-380 代码／**未做 generator 文献搜索（依令）**／**未适用十三门（依令）**／**未开案** ✓
✗ 守 `O1-1` 禁语纪律：**允许**"该路线已被精确封锁"；**禁止**"`O1-1` 不存在"／"全局数学封口" ✓
✗ 不给新理论命名；不用文献背书；不把既有结论换符号；**不**把"存在核"当成"存在新机制" ✓✓
⚠️ §2 的"候选 `Ψ_β^min` ＝ 退化计数"系**引用档案既有结论**（`V192` §②），非本档新提出 ✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 有限纤维见证     命中文件数=0 ⟹ 本档新增（⚠️ 但"**有限纤维分离**"档案已有＝`O1-1`，**引用** ✓）
　技术词 切空间假象       命中文件数=0 ⟹ **本档新增** ✓
　技术词 二阶稳健         命中文件数=0 ⟹ **本档新增** ✓
　技术词 最小新增观测量   命中文件数=0 ⟹ **本档新增** ✓
　技术词 Ψ_β              命中文件数=0 ⟹ **本档新增** ✓
【地图回查】 命中 5 ⟹ **`V290`（`O1-1`／有限纤维分离）为实质命中 ⟹ 引用** ✓
```
