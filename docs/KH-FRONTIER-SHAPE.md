已查地图：未覆盖（关键词: 前沿形状|准入筛|正空间|闭口入口）—— 可开档，首行须照抄本行
　⚠️ **概念级已有先例（必须引用，本档与之并存互补，不替代 ✓）**：
　· **`NOGO-registry-and-screens.md`**：**§一 新候选准入判据**（唐先生 2026-09-10 定）＋ **§二 两道反模式筛**（**线性性筛**／**形变-插值筛**）＋ §四 已封存 NO-GO 登记 ✓
　· **`HOT-STATE.md`**（项目状态交接，新会话开场读；**新路径准入**指向 `MASTER-STATUS-AND-CLOSURES.md`）✓
　· **`EXPLORATION-POINTS-REGISTER.md`**（探索点登记；**准入 8 步**）✓

# **`KH-FRONTIER-SHAPE`**：KH 系列**当前前沿形状**（短而硬 · 候选准入筛）

**唐先生令（2026-09-22 23:05）**：
① 选 **(b) 先做"当前前沿形状"资产**，**然后再进 (a)** ✓；理由（照录）：**`KH-2→KH-4` 已形成一条非常清晰的"禁止重复搜索边界"**；
　**现在直接择候选，最容易又掉回"看起来有参数、实际只是已有对象重编码"的坑** ✓✓
② **定位（照录）**：**不是普通总结**，而是一份**短而硬的资产**；**核心不是流水账，而是把已证搜索空间压成一个"候选 admission filter"** ✓✓
③ ⭐ **本资产最重要的一句话（照录，须作 Rule T ＋ SURVIVOR-5 联合入口）**：
　$$\boxed{\text{当前缺口已不再是"从哪里得到 }1/2\text{"；而是"找到一个独立的、参数真正改变其数学内容的算术对象，并让 RH 成为它的后置性质"}}$$ ✓✓
④ **落档后下一刀（(a)）的约束（照录）**：**第一轮候选完全不以"寻找 `1/2`"为筛选目标**；**RH relevance 先只记 `RH-interface: UNKNOWN`**；
　只有候选通过 **N1–N7** 后，才进入第二阶段问 `\theta \stackrel{?}{\to} \beta\text{-sensitive observable} \stackrel{?}{\to} RH` ✓✓
⑤ ⛔ **不要立刻再开 `KH-5`**：**先落本档** ✓

D0: 本档对象 = **KH 系列前沿形状与候选准入筛（`KH-FRONTIER-SHAPE`）**（档案**零覆盖** ⟹ 新档；⚠️ **概念级已有三档先例 ⟹ 引用并存** ✓；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算／零数值／未写程序**／未做数值搜索／未证 RH／未接 β／未接 ζ ✓

---

## §1 **已封闭入口**（本系列新增部分；附「再开条件」✓）

| 入口 | 当前结论 | **再开条件** |
|:--|:--|:--|
| Euler characteristic／`\det`／height／entropy／mixed discriminant／Dirichlet convolution | **KH-2 DEAD** ✓ | 必须出现**未被现有机制吸收**的新算术量 ✓ |
| `\Omega,\omega` | **DEAD** ✓ | 必须出现 **β-sensitive 新接口** ✓ |
| Selberg invariants／conductor | **DEAD** ✓ | 同上 ✓ |
| Mertens／Chebyshev defects | **DEAD** ✓ | **非 RH-equivalent** 新量 ✓ |
| Hecke relation defects | **DEAD** ✓ | 必须存在**真正的 defect 参数** ✓ |
| second-order local data | **DEAD** ✓ | 必须突破 **`\alpha_p \equiv 1`** ✓ |
| explicit-formula probes | **DEAD** ✓ | 必须摆脱 **explicit-formula 主接口** ✓ |
| **prime race** | **已有资产**（`P-3`／`P3-B`／`P3-B2`／`F-2`）✓ | **只能作后置接口，不算新桥** ✓ |
| **arithmetic-flow `\to 1/2`** | **KH-3 `P2-COUPLED-DEAD`** ✓ | **不再从 flow 谱中寻找 `1/2`** ✓ |
| **divisor symmetry `\to x^{1/2}`** | **KH-4 GAP**（**窄口径** ✓） | **不再寻找 position→eigenvalue 的第四种编码** ✓ |
| **`r f(x/t)` 型加权谱** | **artificial encoding DEAD** ✓ | **永不作为 survivor** ✓✓ |

（既有更早的 NO-GO 总表见 `NOGO-registry-and-screens.md` §四 与 `MASTER-STATUS-AND-CLOSURES.md`；本表**只列本系列新增** ✓）

## §2 当前真正的"**正空间**"（照录 ✓）

```
【不是"什么都没找到"】 搜索空间已被压缩成：
　$$\boxed{\text{prime-native arithmetic object} + \text{independent mathematical problem} + \text{genuine variable parameter}}$$ ✓
【然后才问】 $$\boxed{\text{该对象} \to \text{新的 arithmetic observable/interface} \to \text{RH relevance}}$$ ✓
【⛔ 禁止反向（KH-4 之后最重要的战略变化 ✓）】
　$$\text{RH 的 }1/2 \ \longrightarrow\ \text{寻找一个能产生 }1/2\ \text{的算子}$$ ✗✗
```

## §3 **下一候选必须同时满足的硬门**（N1–N7 ✓✓）

| 门 | 要求 |
|:--|:--|
| **N1** | **对象先于 RH 定义** ✓ |
| **N2** | **存在独立的数学问题／未知量** ✓ |
| **N3** | 存在**真正可变的 `\theta`**，且**不是人为塞入** ✓ |
| **N4** | ⭐ **`\theta` 改变对象本身，而非仅改变坐标／归一化** ✓✓ |
| **N5** | 至少一种**可证明**的组合律、递推律或极值律 ✓ |
| **N6** | **不能由现有 explicit-formula／zero-statistics／spectral encoding 吸收** ✓ |
| **N7** | **RH 只作后置接口，不参与对象定义** ✓ |

```
【⭐ N4 的地位（照录）】 「**参数变化必须改变 quantity，而不仅仅改变 representation**」
　—— **这正是 `KH-4` 最后暴露出来的核心漏洞** ✓✓
【与既有序目关系（引用 ✓）】 本节与 `NOGO-registry-and-screens.md` **§一 准入判据** ＋ **§二 两道反模式筛（线性性筛／形变-插值筛）** **并存**：
　既有两筛管**反模式**，本表 N1–N7 管**正空间准入**；**二者须同时通过** ✓✓
```

## §4 下一刀形态（(a) 的边界条件 ✓）

```
【第一轮筛选目标（照录）】 **寻找本身有独立问题、参数 `\theta` 真正改变算术对象的 prime-native quantity** ✓
　⛔ **不以"寻找 `1/2`"为筛选目标** ✓✓
【RH 相关性的登记方式】 第一轮**只记**：$$\text{RH-interface: UNKNOWN}$$ ✓
【两阶段】 阶段一：`N1–N7` 准入 ⟹ 阶段二（**仅通过者**）：`\theta \stackrel{?}{\to} \beta\text{-sensitive observable} \stackrel{?}{\to} RH` ✓✓
【目的】 **彻底避免 `KH-3/P2` 与 `KH-4` 的旧循环** ✓
```

## §5 ⭐ 本资产的核心（boxed ✓）

```
$$\boxed{\text{当前缺口已经不再是"从哪里得到 }1/2\text{"；而是"找到一个独立的、参数真正改变其数学内容的算术对象，并让 RH 成为它的后置性质"}}$$
【用途（照录）】 此句此后作为 **Rule T ＋ SURVIVOR-5 的联合入口** ✓✓
【本档定位】 **KH 系列专属更新**：与 `NOGO-registry-and-screens`（准入判据＋两筛）／`HOT-STATE`（交接）／`EXPLORATION-POINTS-REGISTER`（探索点）**并存互补，不替代** ✓
```

## §6 边界与回查（✗✓）

```
✗ 零计算／零数值／未写程序／未做数值搜索／未证 RH／未接 β／未接 ζ／**未开 `KH-5`** ✓
✗ ⛔ 不把"11 入口封闭"记为战绩 ✓；⛔ 不宣布"不存在新入口"（本表＝**准入筛**，非存在性否定 ✓）
⚠️ 本档**不扩写新数学**（依令"短而硬" ✓）；既有更早 NO-GO 以 `NOGO-registry-and-screens` §四 为准 ✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 闭口入口 命中文件数=0 ⟹ **本档新增** ✓｜正空间=0 ⟹ **新增** ✓｜N4门=0 ⟹ **新增** ✓
　技术词 前沿形状 命中文件数=2 ⟹ **档案已有（`EXPLORATION-POINTS-REGISTER`／`HOT-STATE`）⟹ 引用** ✓
　技术词 准入筛 命中文件数=2 ⟹ **档案已有（`NOGO-registry-and-screens`／`PHASE2-ENDPOINT`）⟹ 引用** ✓✓
【地图回查】 关键词 4 项 = **未覆盖** ⟹ 可开档 ✓
【档级引用】 `KH-2`（六候选 DEAD）｜`KH-3`（漏斗／`P2-FEASIBILITY`／`P2-COUPLED-FLOW`）｜`KH-4`（`EXPONENT-TO-SPECTRUM`／`POSITION-TO-EIGENVALUE`）｜
　`ASSETS-REGISTRY` `D-11`–`D-14`／`F-2`／`P-3`｜`NOGO-registry-and-screens`／`HOT-STATE`／`EXPLORATION-POINTS-REGISTER` ✓

---

## §7 **`KH-5` 线封口 ＋ 封口链冻结 ＋ 工作区纪律**（2026-09-23 08:37 定 ✓✓）

```
【已封闭入口（本表新增行 ✓）】
　| 入口 | 当前结论 | 再开条件 |
　| **`KH-5` 整线**（`S2` → 非局部三类 `A/B/C`） | **`CLOSED`**（分层：`C = CLOSED`；`NONLOCAL = CLOSED under current three-class map`；
　　⛔ **不写"非局部方法不存在"**）✓ | **须出现完全不同的 global arithmetic object**（届时可调用 **`E-11`／`E-12`** 后置接口）✓ |
【链条（存档指针）】 `R8 → S2 缺主项 → 有限阶 G(K) ≤ 0 → 非局部三类 → A 已封／B 饱和／C 部分计数 → 2/3 → 1/3 = R−1 → support>1 → W6 原子墙` ✓
　（`2/3 → 1` 的终点 ＝ `n₋(Q_T)=0` ⟺ Weil 正性 ⟺ RH）✓
【封口链冻结 ✓】 `KH-5` 四提交（`07f9580`／`dfb525f`／`7f723ad`／`2e6ba2c`）＝ 本线**封口链**，**保持原样** ✓
【工作区纪律（照录）】 `scripts/dB_krawczyk_11.py`（修改）／`scripts/out_c380_81_aset.txt`（修改）／未跟踪 `C298`／`C305`／`PDF`
　⟹ **保留现状，待所属主线判定**；⛔ **不与 `KH-5` 混 commit**（避免错误并入旧档、丢失 provenance、制造无数学意义的混合提交）✓✓
```

## §8 **`E-10` 第二轮（依令启动 ✓）**

```
【严格三要素（照录）】 $$\boxed{\text{prime-native object} + \text{independent problem} + \text{genuine parameter}}$$
　然后才问 $$\boxed{\text{new arithmetic observable/interface} \to \text{RH relevance}}$$ ✓
【⛔ 禁反向】 **不再从 `1/2`／Weil／惯性／`R8`／`S2`／`W6` 反向寻找对象** ✓✓
【目标形态】 找一个 **"即使完全不谈 RH，也值得研究"** 的问题；**RH 只作第二阶段的一个特殊结构/性质** ✓
【`E-11`／`E-12` 定位】 **工具箱**（以后遇到完全不同 global arithmetic object 时的后置接口），**不是下一候选本身** ✓
【第二轮产出】 见 `docs/E-10-ROUND2-candidate-generation.md` ✓
```
