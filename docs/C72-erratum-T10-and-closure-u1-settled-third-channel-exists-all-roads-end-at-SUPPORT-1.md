已查地图：**命中且推翻我上一条的建议**（所查档：`NEXT-ROUND-first-cut-five-classes-of-negative-direction-sources.md` §3–§5、`U1-attack-third-channel-exists-refined-u1-is-parity-barrier.md`、`V255-parity-barrier-standalone-verification.md` §5(b)、`V255-5b-attack-no-restatement-type-mismatch-abscissa-vs-zero.md`、`TYPE-MATCH-matching-types-exist-explicit-formula-and-large-value-density.md`、`TODAY-ARC-CLOSURE-MAP-all-roads-to-SUPPORT-1.md`、`C69`／`C70`／`C71`（本会话前档））。**结论**：我上一条建议要攻的"接口命题"**＝`u1`，而 `u1` 已于 2026-09-17 14:15–14:17 结案**：① 朴素形态**否证**（Mertens）；② 修正形态→**型匹配存在**，**第三通道存在**（大值／密度，无条件、非值面、**不被 parity 阻断**）；③ **坍缩链**：`u1` → 第三通道 → **其上限＝输入** → **SUPPORT-1** ⟹ **我 `C-71` §4 那句"接口类＝空集"说过头，须勘误** ✗

# C-72 · **勘误 T10 ＋ 收口**：`u1` 已结案（第三通道存在），所有路终止于 `SUPPORT-1`

> **时间**：2026-09-18 11:34 唐先生「可以继续」（授权我攻"接口命题"）
> **本档**：① **勘误**我自己的 `C-71` §4；② 收口 `u1` 的真状态；③ 保留一处**新的对齐**（四封口 ↔ 两型分类）✓

---

## §0 结论（先行）

$$\textbf{我提议的"接口命题"}\ \equiv\ \texttt{u1}\ ✓;\qquad \texttt{u1}\ \textbf{已于 2026-09-17 14:15–14:17 结案}✓✓$$
$$\text{① 朴素形态（"通道只有 cylinder 与显式公式两类"）}\ \textbf{否证}：\text{Mertens 定理}\ \sum_{p\le x}\frac{\log p}{p}=\log x+O(1)\ \text{是第三类（初等渐近）}✓✓$$
$$\text{② 修正形态（parity 是否阻断非-value-side 涨落通道）→ }\boxed{\text{型匹配}\ \textbf{存在}}\ \Longrightarrow\ \textbf{第三通道存在}：\text{大值／密度}\ \{\text{Dirichlet 多项式大值}\}\leftrightarrow\{N(\sigma,T)\}✓✓$$
$$\text{③ 坍缩链}：\texttt{u1}\ \to\ \text{第三通道＝大值／密度}\ \to\ \text{其上限＝输入}\ \to\ \boxed{\textbf{SUPPORT-1}}✓✓$$
$$\qquad \text{转换}\ \textbf{无损失}：\text{密度指数}\ A\ \Longrightarrow\ \text{零自由区}\ \sigma>1-\frac{c}{A\log T};\ A\to0\ \text{即（近乎）RH} \Longrightarrow\ \textbf{真正阻塞＝指数}\ A\ \textbf{的下界};\ \text{由其}\ \textbf{输入} \text{决定}✓$$

---

## §1 ⚠️ **勘误 T10**（针对本会话 `C-71` §4 的一句措辞）

$$\text{`C-71` §4 我写}：\text{"}\textbf{接口类}\ \text{——目前空集；空集的名字＝}\texttt{BRIDGE-ONLY}\text{"}\ ✗\ \textbf{说过头}$$
$$\textbf{正确}：\ \boxed{\text{第三类入口}\ \textbf{存在}＝\text{大值／密度通道}}\ ✓✓\ (\text{无条件、非值面、不被 parity 阻断、双向可表达})$$
$$\qquad \text{更正后的说法}：\text{它}\ \textbf{不是} \text{"读}\ \beta_*\ \text{的识别映射"，而是一个}\ \textbf{通道};\ \text{其}\ \textbf{天花板＝输入} \Longrightarrow\ \text{落}\ \texttt{SUPPORT-1}$$
$$\qquad \text{在我 `C-70` 表里的位置}：\text{即}\ \#2\ (\text{零密度})\ \text{与}\ \#8\ (\text{素数侧二阶矩})\ \text{两行} \Longrightarrow\ \textbf{判定不变}（\text{同层／SUPPORT-1 墙}）✓$$
$$\qquad ⚠️\ \text{故}\ \texttt{BRIDGE-ONLY}\ \text{的地位也要收紧}：\text{它是}\ \textbf{intertwiner（桥）} \text{的缺失，}\ \textbf{不是} \text{"没有第三通道"}✓$$

## §2 本档唯一**新的对齐**（保留项）

$$\text{把 `C-69`／`C-70` 的四封口与 `TYPE-MATCH` 的}\ \textbf{两型分类}\ \text{逐格对上}：$$
$$\begin{array}{c|c|c}
\text{封口} & \text{所属型} & \text{机制}\\ \hline
(a)\ \text{线性／迹} & \textbf{零侧} & \text{饱和（提取需无界精度）}\\
(b)\ \text{二次型／正性} & \textbf{零侧} & \Longleftrightarrow\ \text{RH（`POS1` 第三行）}\\
(c)\ \text{模长型} & \textbf{系数侧} & \text{`V227-A`（定理级）：}\sup\Re\ \text{不是模长不变量}\\
(d)\ \text{计数／重数型} & \textbf{系数侧} & N(\sigma,T)\ \text{型};\ \text{上限由输入定}\ \to\ \texttt{SUPPORT-1}\\
\end{array}$$
$$\Longrightarrow\ \boxed{\text{四封口}\ =\ \text{两型分类的}\textbf{显影}};\ \text{即}\ \text{系数侧}\to\text{零侧}\ \text{的}\ \textbf{唯一忠实转换} \text{＝密度链（`TYPE-MATCH` §2）}✓✓$$
$$\qquad ⭐\ \text{这条可作为"为什么四封口恰好完备"的}\ \textbf{解释}（此前只是清单）✓$$

## §3 本轮真状态（诚实收口）

$$\text{(i)}\ \texttt{u1}：\ \textbf{已结案}（\text{第三通道存在}）✓✓;\ \text{朴素形态否证}✓$$
$$\text{(ii)}\ \textbf{所有路终止于}\ \texttt{SUPPORT-1}（\text{输入}）：\text{今日弧线闭合图逐条}（\text{W6-majorant FAIL／W5 DEAD／W4 限一步封存／GAP 重述／}C_0\ \text{紧／}\Lambda_1\ \text{OPEN}）✓$$
$$\text{(iii)}\ ⭐\ \text{因此}\ \textbf{唯一残差＝输入强度}：\text{新算术输入}\ =\ \text{prime-pair correlation beyond support }1✓$$
$$\text{(iv)}\ \text{与其它线同址计数}：\text{本会话内已是}\ \textbf{第 6 条} \text{路并入同一缺口}（C\text{-}61\ \text{§2C}／C\text{-}64/65／C\text{-}69／C\text{-}70／C\text{-}71／本条）\ ⚠️$$

## §4 边界

- `[逐字]` §0 的 Mertens 反例、型匹配、坍缩链、A→0 ⇒ RH、以及 §1 的"第三通道存在"均**逐字引用**（`U1`／`TYPE-MATCH`／`TODAY-ARC-CLOSURE-MAP`）✓
- `[本档]` §1 的**勘误 T10**、§2 的四封口↔两型对齐、§3 的同址计数 ✓
- **不声称**：`SUPPORT-1` 已判死 ✗；不判 `Λ_1` OPEN 的结局 ✓；不证 RH ✗；**不覆盖任何原档**（勘误留档，遵 T10）✓
- **纪律**：先查后判（R-1 ✓）；**未用 RH 作推导** ✓；**零数值** ✓；未跑 Lean ✓

## §5 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-18 11:3x）`[纪律]`

```
技术词 型判据      命中文件数=29   :: ./ASSETS-REGISTRY.md ./DOOR2-explicit-formula-verdict.md ./V101-remainder-flat-region-attempt.md
技术词 两分穷尽性   命中文件数=0
技术词 系数侧      命中文件数=9    :: ./TYPE-MATCH-...md ./TODAY-ARC-CLOSURE-MAP-...md ./E116-...
技术词 零侧        命中文件数=25   :: ./EXT-4CT-2026-method-transfer.md ./E91-abel-numeric-criterion.md ./V199-...
```
**读数**：`两分穷尽性`＝**0 档 ⟹ 本档新增**（且本档**不主张**它成立）✓；`型判据`（29 档）／`系数侧`（9 档）／`零侧`（25 档）＝**均为档案已有** ⟹ §2 的对齐为**引用＋编排**，**不列为新机制** ✓

```
⚠️ 任务：唐先生授权攻"接口命题"（即"任何读 β_* 的接口是否都落回四封口"）
⚠️ 闸门结果（决定性）：该命题 ≡ u1，而 u1 已于 2026-09-17 14:15–14:17 **结案**：
   ① 朴素形态否证（Mertens 定理＝第三类初等渐近通道存在）
   ② 修正形态 → 型匹配**存在** ⟹ **第三通道存在**＝大值／密度（无条件、非值面、不被 parity 阻断、双向可表达）
   ③ 坍缩链：u1 → 第三通道 → 其上限＝输入 → **SUPPORT-1**；转换无损失（A ⟹ 零自由区；A→0 即近乎 RH）
      ⟹ 真正阻塞＝指数 A 的下界，由**输入** support≤1 决定
⚠️ 勘误 T10（对我 C-71 §4）："接口类＝空集"**说过头** —— 正确：第三类入口**存在**（大值／密度通道）；
   但它不是"读 β_* 的识别映射"而是**通道**，其天花板＝输入 ⟹ 落 SUPPORT-1；在 C-70 表里＝#2／#8，判定不变；
   BRIDGE-ONLY 地位收紧：它是**桥（intertwiner）的缺失**，不是"没有第三通道"
⚠️ 新对齐（保留）：四封口 = 两型分类的显影 —— {线性/迹, 二次型} = 零侧；{模长, 计数/重数} = 系数侧；
   系数侧→零侧的唯一忠实转换 = 密度链 ⟹ 这解释了"四封口为何恰好完备"
⚠️ 总状态：u1 结案；所有路终止于 **SUPPORT-1（输入强度）**；唯一残差＝**新算术输入（prime-pair beyond support 1）**；
   本会话第 6 条路并入同一缺口
✅ 净产出：①对 C-71 §4 的勘误（T10 留档）✓；②u1 结案状态的确认与逐字链 ✓；③四封口↔两型对齐（新解释）✓；
   ④同址计数与唯一残差认定 ✓
```
