已查地图：命中（`ENGINE-X-PROBLEM-emptiness-pilot`（v1 不合格）／`CROSS-DOMAIN-MAPPING`／`CORRECTION-idea-first-not-capacity`）⟹ **引用，不开新案** ✓
D0: 本档对象 = 空白格试点 **v2**（加硬规则：引擎与问题**均不得有 RH/数论血统**）＋ 重建候选格
D1: 0 （`[REVIEW]` 轮次：重建与提名，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **空白格试点 v2：排除 RH/数论血统（含对 v1 的逐格自认）**

## §0 ⚠️ **v1 不合格，逐格自认（唐先生矫正："你这个还是围绕RH"）**

```
**`B`**：引擎＝**de Bruijn–Newman 单调流** ⟹ **本身即 RH 概念** ✗
**`C`**：问题＝**乘性函数/Chowla 家族** ⟹ **数论/RH 邻居** ✗
**`E`**：引擎＝**我们的 RH 派生资产**（转移原理/惯性）穿"引擎"外衣 ⟹ **仍是 asset-driven** ✗
**`A`/`D`/`F`**：问题侧非 RH ✓，但 **`A`/`F` 的引擎仍是我们项目里练出来的**（区间覆盖／证书形状）⚠️
【根因】 我把引擎池**从"我们已有的东西"里取**，而那批东西**全部出生于 RH** ⟹ 必然带阴影 ✓✓
```

## §1 **v2 硬规则（本档新增）**

```
**规则 `R1`**：引擎与问题**都不得有 RH/数论血统** ✓
**规则 `R2`（可操作检验）**：$$\boxed{\text{能否在\textbf{完全不提本项目}的前提下陈述该引擎？}}$$ 能 ✓ / 不能 ✗（`E` 即不能 ⟹ 出局）✓
**规则 `R3`**：问题须出现在**非数论文献族**中（编码论／优化／数值分析／组合几何／CS）✓
```

## §2 **重建的引擎池（全部非 RH/非数论出身；只列出身领域）**

```
**`X1`** 区间 Newton／Krawczyk 型**根与不动点严格认证**（出身：**数值分析**）✓
**`X2`** **SAT 证明日志**（`DRAT`/`LRAT`，机器可检 UNSAT 证书）（出身：**CS/形式方法**）✓
**`X3`** **SOS／Lasserre 层次严格对偶证书**（出身：**优化/代数**）✓
**`X4`** **证书形状先行**：把 `\forall`-型判定重述为有限覆盖（**抽象方法**，可无项目背景陈述）✓
```

## §3 **v2 候选格（四格，五列）**

| # | 引擎 | 空白格候选（**非数论**问题） | 为何可能未用过（假设，待查） | 第一步可检验推论 | 判死条件 |
|:--|:--|:--|:--|:--|:--|
| **`G1`** | `X1` 区间 Newton/Krawczyk | **统计/学习模型中被"数值断言"的唯一性**（某具体模型的 MLE 唯一性／不动点唯一性只有迭代证据） | 该领域惯例接受数值收敛；证书被视为不必要 | 挑**一个**已发表模型，判定其唯一性结论**是否只有数值证据** | 该模型已有严格认证 ⟹ ✗ |
| **`G2`** | `X2` SAT 证明日志 | **较早发表的"小规模穷尽分类／不存在性"声明**（组合设计、覆盖码）**缺随发布证书** | 年代较早、证书未随发布；复现成本高 | 挑**一条** 1990–2010 年代此类声明并试补机器可检证书 | 已有证书 ⟹ ✗ |
| **`G3`** | `X3` SOS/Lasserre | **编码理论／组合优化中某些小参数的精确值**（最小距离、最大码字数）**严格性等级不明** | 表值多由启发式/半严格方法给出 | 挑**一个**表项，判定其严格界是否已有 | 已有严格界 ⟹ ✗ |
| **`G4`** | `X4` 证书形状先行 | **非数论的组合几何**：小 `n` 最优覆盖/装填构型的**极值性** | 主流从**搜索算法**入手，从**证书形状**入手少见 | 写"若最优性成立，一页证书长什么样"；写不出＝`NO-IDEA` | 写不出 ⟹ 换格 ✓ |
【⛔ 已删除】 `B`（DH/DBM＝RH）、`C`（乘性函数＝数论）、`E`（引擎＝本项目派生）✓
```

## §4 **试点判据（承 v1，不变）＋ 边界**

```
【成功】 至少 **1 格**：**30 分钟核查确认空白 ∧ 第一步具体、小、当天可做** ✓
【失败】 四格**全部**被占据 ⟹ 结论＝**该矩阵亦无空白格**（＝创造力上限）✓
【排序】 `G2` → `G1` → `G3` → `G4`（前三格只依赖"证书缺失"这一**可查事实**，不需新想法）✓
【边界】 ⚠️ 四格"为何未用过"**是假设**（未逐字核文献）⟹ 第一动作＝30 分钟核查；§1 规则、§3 重建为**本档自行推导** ✓；⛔ 未制造候选／未启动搜索／未改状态／未碰 RH／未碰数论 ✓
```

## §5 【技术词回查】（逐字粘贴 ✓）

```
技术词 Krawczyk         命中文件数=18   :: ./C222-B-interval-newton-KKT-strict-box-X0-existence-and-uniqueness.md ./C356-execution-stratification-registration-not-a-new-framework.md ./C354-branch-completeness-as-core-audit-quantity-and-discovery-certificate-coverage-separation.md 
技术词 DRAT             命中文件数=2    :: ./CROSS-DOMAIN-MAPPING-of-our-capabilities.md ./ENGINE-X-PROBLEM-pilot-v2-no-RH-ancestry.md 
技术词 Lasserre         命中文件数=1    :: ./ENGINE-X-PROBLEM-pilot-v2-no-RH-ancestry.md 
```
【三分类】 **本档新增**：`R1`–`R3` 硬规则、`X1`–`X4` 引擎池、`G1`–`G4` 候选格 ✓；**档案已有（引用）**：见上逐字；**通用词（不计）**：`Krawczyk`／`DRAT`／`Lasserre`（专名）✓
