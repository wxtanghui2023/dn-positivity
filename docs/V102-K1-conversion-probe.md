# E124 · K1 —— **转换探针 $h(n)$：是否存在严格优于 $\sqrt n$ 者？**

---

# 勘误：迁号（编号领用制 ✓ 2026-09-14）

```
【⚠️ 迁号 ✓】本档原编号 **E124** ⟹ 新编号 **V102** ✓（`docs/ID-ALLOCATION-PROTOCOL.md` ✓）
   原因 ✓：**跨会话编号撞车**（唐先生 2026-09-14："编号撞车问题必须解决，不然会覆盖不该覆盖的文本和内容"）；
   本会话（审计线）**整体迁出 E 号段** ⟹ 改用 **V 系列**（`V*` 与现有任何编号前缀**零重叠** ⟹ 通配不会误命中 ✓）
   按 **T10**：原文保留 ✓、以勘误留档 ✓、**不静默改写** ✗；本档**内容/数字/证据等级不变** ✓
   历史别名 ✓：`E124` ⟹ `V102`（引用旧号须转写 ✓）
```


> **任务**：唐先生委托（子代理执行 ✓）｜**类型**：纸面/检索探索（**未写代码 ✓**；**未改任何既有文件 ✓**；仅新建本档 ✓）
> **对象**：E97 登记的突破候选 **K1**｜**判据**：成功 = 给出 $h(n)$ 优于 $\sqrt n$ 的探针（含常数与适用范围）；穷尽已知族且皆 $\asymp T^2$ ⟹ **律 I 的精确边界**（亦为结构性产出）
> **纪律**：只走显式路径 ＋ `docs/` 目录内定向 grep（未做全盘 find/grep，未触及 `fn_backup`/`fupeng`/`github`/`quant-system`/`.git` ✓）；未预设 RH ✓；未循环 ✓；未把结论当前提 ✓；边推边落盘 ✓
> **证据等级标签**：`[档案]` 已有档案逐字/级联 ｜ `[文献]` 外部文献（含二手）｜ `[推导]` 本档推导 ｜ `[机制]` 结构性论证（启发式）

---

## ① 查重结果（硬性第一步 ✓）

**检索范围**：`EXPLORATION-POINTS-REGISTER.md`（grep `K1`）｜`MASTER-NOGO-AND-LIVE-PATHS.md`（grep `律 I`/`律 II`/`勘误`）｜`HOT-STATE.md`（§三/§四）｜`CROSS-DIRECTION-ANALYSIS-2026-09-13.md`（§4 K1 条目）｜`docs/` 内 `E30*`/`E47*`/`E99`/`E100`/`DOOR5*`/`CONV1-3`/`BL1`/`A1-*`

**结论：同一 ID 档案不存在；同一对象已【部分覆盖】，但"逐族枚举 $h(n)$"这一合并审计未做过 ⟹ 非完全重复，本档完成之。**

```
【①-1 无同 ID 档案】`docs/` 内**无 E124**；`ls docs/ | grep -i K1` 命中三份，但**均为无关对象** ✗：
    · `euclid-trace-k1.md`（Euclidean cocycle Tr(P_s^k) 的 k=1，2026-09-03）
    · `euclid-k2-death.md`（同族 k=2）
    · `k1-k2-positivedefinite-kernel.md`（2026-09-01 正定核审计 K1/K2 候选）
   ⟹ **三者与本任务的 K1（突破候选）对象不同** ✓（不得当作重复 ✓）

【①-2 同对象已覆盖【部分】，逐条引其结论】
  · **E97（2026-09-13，登记）** ✓：K1 首次登记为突破候选 ★。逐字：「**K1 找 $h(n)$ 优于 $\sqrt n$ 的转换定理** …… 成败都有产出：成功＝突破；穷尽皆 $T^2$ ⟹ **律 I 精确边界**」；
    并明确边界：「**K1 未穷尽检索 ⟹ 记开放**」✓。⟹ **K1 = 已登记、开放、未判死** ✓
  · **E30（统一转换律）§3** ✓：给出可操作判据：$h(n)\sim\sqrt n \Rightarrow$ 可达 $T^2$；$h(n)\sim n \Rightarrow$ 可达 $T^1$；
    「**突破点 = 找到一个"索引→高度"优于 $\sqrt n$ 的探针**」✓（K1 的原始定义出处 ✓）
  · **DOOR5 / 5b / 5c / 5d** ✓：转换形状**三分法**（Li/Jensen 增长型 ⟹ $T^2$；NB 衰减型 ⟹ 对数型；真墙类 ⟹ 无转换）。
    **已撤回**"三条皆 $T^2$ 的普遍性主张"（DOOR5c，NB 被排除 ✗）⟹ **修正为"两条已核 + 一条已排除"** ✓
  · **E30-2 / E30-2b(E47)** ✓：**机制**已解 —— 平方来自共享恒等式里的 $\gamma^2$：$|1-1/\rho|^2=1+\frac{1-2\beta}{\beta^2+\gamma^2}$ ⟹ $h(n)\approx\sqrt{n\delta/(2\log n)}$，$\alpha=\tfrac12$ ✓；
    E47 给出**判据级分类表**（A1 窗口 $\alpha=1$／A1 尾 $\alpha=\frac12$／A2 Jensen $\alpha=\frac12$／A3–A7 不适用），并**更正 E30 的歧义表述**：「**突破 = 把 $\alpha$ 压到 $\tfrac12$ 以下**（**不是**让 $h$ 增长更快）」✓
  · **E99** ✓：给出 K1 的**尖锐形式**与**盲区**；**未宣布 K1 判死**（只判"仅消费验证高度"类被迫 $T^2$）✓
  · **E100** ✓：K1 归结为 **L2（P-Scale）**；char-0 Frobenius/purity 载体**已在总册判死**（第 6 箱）✓
  · **CONV1 / CONV3 / BL1** ✓：**$T^2$ 的机制级来源已定位**：$n\lesssim N(T)/B_T$，$B_T=\tfrac12\sum_{\gamma>T}\gamma^{-2}\asymp\frac{\log T}{T}$ ⟹ $T^2$「**从'逐项非负 + $\Sigma1/\gamma^2$ 收敛'里长出来**」✓

【①-3 缺口（本档要做的那一件）】
   E47 覆盖的是 **A1–A7 判据级**；DOOR5d 覆盖的是**形状三分法**；**均未**对 K1 点名的**具体族**——
   **B–L 分解／Palojärvi–Coffey／Lagarias 分解／Droll／Brown**——做**逐族 $h(n)$ 尺度**的合并枚举与判定 ✗
   ⟹ 本档 = 该合并枚举 ＋ 三选一结论 ✓（**停止条件不触发**：K1 既未"已覆盖"，亦未"已判死" ✓）

---

## ② 分类（命中 §1 禁止表？是否即已登记的 NO-GO？）

```
【②-1 §1 硬 NO-GO 表（N0/N10/N36/N26/N27/N45/N42/N20-A）】K1 是**分类/枚举型任务**，**不构造任何对象** ⟹ **未直接命中** ✓
【②-2 §2 结构性关闭 —— ⚠️ 有两处【条件性命中】，必须在"换探针"支上守门】
    · **N2（Weil positivity 及其一切变体）** ⚠️：Lagarias 2007 逐字「each positivity condition λ_n ⩾ 0 **encodes 'Weil positivity'** for g_n」
      ⟹ **Li 族内的任何"新探针"若只是变体的重选，即命中 N2** ✗（[档案] `E5-lagarias-gn-explicit.md` §1 ／ `ALIGN-A1-li-coefficients.md` §1）
    · **N30（尺度族错配；"升级指数"是错误抽象）** ⚠️：来源 **Exponent-Bridge**，已**终止性关闭**：
      `exponent-bridge-final-audit.md` 裁决「**不存在"从 1/4 族提升"的机制路线 …… 整个"约束/延拓"范式【死】**」，
      自杀测试分类：**A 同族／B 人为归一化死／C $\alpha=\frac12$ 且来自【新的泛函方程型自对偶】（唯一候选区）／D 无标度律不许继续** ✓
      ⟹ **K1 的措辞若被读成"把指数升级"，即命中 N30** ✗；**但 K1 的确切问法在同一对象（$\zeta$）内比较各族转换定理的标度**，
         与 N30 所禁的**跨对象指数搬运**（1/4 族 vs 1/2 族）**不同** ✓ ⟹ **本档按"同对象内标度枚举"受理，并在 §⑤ 写死守门判据** ✓
【②-3 是否即已登记的 NO-GO？】**否** ✗：E97 明列 K1 为**突破候选（开放）**；E99/E100 **均未判死**；总册第四部分活路为 L2/L3，
       K1 不在 NO-GO 册内 ⟹ **允许探索** ✓（但按 §0 协议需过 §③ 门槛与 §④ 形态 —— 见下，二者对"分类型任务"是**部分不适用**的 ✓）
【②-4 输入类判定（本档核心分类动作）[档案/推导]】
       把"消费**已验证零点高度 T**"作为输入的转换定理记为 **$\mathcal C$-类**；K1 问题**只在 $\mathcal C$-类内有意义** ✓
       ⟹ **Nyman–Beurling / Báez-Duarte / DFMR 被【排除出 $\mathcal C$-类】** ✗：DOOR5c 逐字「DFMR 型：**解析输入（距离界）⟹ 几何输出** …… **不是数值放大器**」✓
       ⟹ 排除后，$\mathcal C$-类 = **系数型（Li/Jensen 及其分解与 $\tau$-变体）** ⟹ 枚举面收敛（§⑥）✓
```

---

## ③ 18 门槛逐条（对"分类型任务"逐条裁读 ✓）

> 说明：18 门槛是为**新对象/新判据**设的过筛器；K1 **本身不提出对象**，故多数为 **N/A**。
> 但 **K1 若"成功"，其产物（$\alpha<\tfrac12$ 的探针）必须补过全部门槛** —— 故逐条列出**对该产物的约束** ✓

| 门槛 | 对 K1 及其产物的裁读 | 状态 |
|---|---|---|
| P1″ 具体位置约束 | K1 产物须给出**位置**约束（非仅符号范围） | **N/A**（K1 是标度问题）／产物须补 ✓ |
| P0′ 不读未来 prime label | 未涉及 | N/A ✓ |
| P-Info 三量分离 | 须申报信息预算；K1 产物不得以"输出熵大"论证 | N/A（K1 无数值输出）✓ |
| P-Type 点式⇏聚合 | ⚠️ **相关**：CONV3 §4① 的"平均 vs 每个 $n$"**正是此门槛** | **K1 的 $\alpha=\frac12$ 现仅【平均/密度一】意义** ⟹ 留待 §⑨ ✓ |
| P-Phase 须携带相位 | ⚠️ **相关**：窗口路线（$\alpha=1$）**正是相位型**；尾路线（$\alpha=\frac12$）**是模长型** | **已识别** ✓（E30-2 §三） |
| P-Basis 申报基底 + 误差家族 | K1 产物须申报（同余型淘汰） | 产物须补 ✓ |
| **P-Exponent A/B/C/D（仅 C 可继续）** | ⭐ **直接命中**：$\alpha<1/2$ 若来自**新平方根全局定理** ⟹ **C 类**（唯一合法）；否则 D 类（无标度律）**不许继续** | **本档按 C 类标准执行** ✓ |
| P-Dual 申报自对偶类别（(b)(c) 淘汰） | K1 产物须申报 | 产物须补 ✓ |
| **P-Scale 申报阳性尺度类型（height/log 淘汰）** | ⭐ **直接命中**：**height 型与 log 型正性尺度【被淘汰】** ⟹ 若某"更优 $h(n)$"探针的**正性尺度是 height/log**，**即淘汰** | **守门项** ✓（见 §⑤ K1-B） |
| P-Diag 对角尺度（须 X 级） | 未涉及 | N/A ✓ |
| **P-SqrtPos 正性须独立于零点** | ⭐ **直接命中**：若候选探针的正性**依赖已知零点**（如"用 $\gamma\le T$ 的在线项"构造）⟹ **未过** | **守门项** ✓（见 §⑤ K1-B） |
| P-Lock* 申报 $\Phi(C,M,W)$ | 未涉及 | N/A ✓ |
| P-Basis-2 误差族（1/4 or 1/2） | 未涉及 | N/A ✓ |
| A_min 四要素齐备 | K1 产物须齐备（整性+对偶+独立阳性+共轭同步）**= quartet** | 产物须补 ✓（= L1 的全部难点 ✓） |
| **非循环性** | ⭐ K1 产物不得以 RH 为前提 | 守门 ✓ |
| 非 zero-encoding | ⭐ K1 产物不得为"零点位置编码" | 守门 ✓ |
| **非重包装（不得 ≡ 显式公式/迹公式/已知判据）** | ⭐⭐ **最关键**：$\mathcal C$-类的族**本已全是已知判据**（Li/Jensen/Brown/Droll/Palojärvi） ⟹ **任何"新探针"若仍落在这些族内，即重包装** | **守门项** ✓（见 §⑤ K1-B） |

**③ 小结**：K1 作为分类任务**未过筛受阻**（多数 N/A）；但其**"成功产物"必须同时守住 P-Scale / P-SqrtPos / 非重包装 / N2 四道** ⟹ 这把 K1 的"存在"支**逼进一个极窄窗口**（§⑤/§⑥）✓

---

## ④ 形态 L1–L4

```
【④-1】K1 **不是** 一个载体提案 ⟹ **不属于 L1–L4 任一** ✓（L1 三重锁载体／L2 非自伴谱刚性／L3 边界—内部锁定／L4 非标准几何载体 —— 皆非"标度枚举"）
【④-2】按 §0 协议 ③「形式：是否属于 §4 的四种合法形态之一？不属于 → 停」——**若把 K1 当"新想法"读，则被 ③ 拦截** ✗
        但 **K1 是 E97 登记的【突破点候选 K1–K7】**，与"活路"（L1–L4）**不同类**：候选需先**自行判定**，判定通过才升格为活路 ✓
【④-3】⭐ **K1 成功后的归属（关键交叉，[档案] E100）**：即使找到 $\alpha<\frac12$ 的探针，其 **char-0 实现**仍须过 L1–L4；
        E100 已定：**K1 归结为 L2（P-Scale）** ⟹ **K1 不产生【新形态】，其成功只【灌入 L2】** ✓
        ⟹ 本档据此：**K1 的形态层不新增活路，价值在①判定 ②律 I 边界 两端** ✓
```

---

## ⑤ 纸面否决判据（**先写死**）＋ 一轮预算

**【否决判据——写于探索之前，触发即关闭】**
```
K1-A（穷尽型否决）**若**逐族枚举后，每一【$\mathcal C$-类】族的 $h(n)$ 皆满足
       h(n) ≳ c·n^{1/2}（即 n_0 ≍ γ^{p}，p ≤ 2，$\alpha\ge\frac12$）⟹
       **判定"已知族内不存在优于 $\sqrt n$ 的探针"** ⟹ **K1 的"存在"支关闭**，转为"律 I 精确边界"支（结构性产出 ✓）。
K1-B（守门型否决）若某候选族给出 $\alpha<\frac12$，但**任一条**成立 ⟹ **判死**，不计入成功：
   (i) 其输入**不是"验证高度 T"**（如 NB 的解析距离界）⟹ **类别错误** ✗（§②-4）
   (ii) 命中 **P-Scale（height/log 淘汰）** 或 **P-SqrtPos（正性依赖零点）** ✗
   (iii) 命中 **N2（Weil 正性变体）** 或 **非重包装（≡ 已知判据/显式公式）** ✗
   (iv) 命中 **N30/Exponent-Bridge C 类之外**（无新泛函方程型自对偶，或人为归一化）✗
K1-C（量级型否决）若"改进"仅来自**同族内常数因子**（如 BL1 §4A 高度参数 $\alpha$、§4B 预算常数，**增益为常数因子、非数量级**）⟹ **不算突破** ✗
```
**【一轮预算】** 本轮 = **纯纸面/档案检索**：不写代码 ✗｜不改既有文件 ✗｜不下载新文献 ✗｜不启动计算 ✗｜只交付本档 1 份 ✓｜**触发 K1-A/B/C 即停，不合并不变通** ✓

**预算执行结果**：本轮**完成**枚举（§⑥），**触发 K1-A**（全族 $p\le2$） ⟹ 按判据将"存在"支关闭、转出"律 I 精确边界"支 ✓（**K1-B 未触发**：无族给出 $\alpha<\frac12$ 需守门；**K1-C 命中**：BL1 §4A/§4B 的额增益确为常数因子 ✓）

---

## ⑥ 探索主体：**逐族枚举**（$\mathcal C$-类：输入 = "已验证高度 $T$"）

> 约定 **$\alpha$ / $p$ 的方向**（依 E47 §一 的**精确表述**，并更正 E30 §3 的歧义）：$h(n)\asymp n^{\alpha}$，$n_0\asymp\gamma^{p}$ ⟹ **$\alpha=1/p$**；
> 给定验证高度 $T_0$ ⟹ **可达索引 $n\lesssim T_0^{1/\alpha}=T_0^{p}$**。**突破 $=\alpha<\frac12$（等价 $p>2$）⟹ 可达 $>T_0^2$** ✓
> ⚠️ **注意**：K1 措辞为"$h(n)$ 严格**优于** $\sqrt n$"= $h$ 增长**更慢** ⟹ $\alpha<\frac12$ ⟹ 可达**更大**（**不是** E30 §3 字面"$h\sim n$ 更强"的读法，该读法已被 E47 §一 判为歧义并更正 ✓）

### ⑥.1 枚举表

| # | 族（转换定理） | 机制 | $h(n)$ | $\alpha$ | $p$ | 可达（$T_0\!\approx\!3\!\times\!10^{12}$） | 无条件? | 出处 |
|:-:|---|---|---|:-:|:-:|---|:-:|---|
| 1 | **Li·模长/尾**（我方组装） | 离轴项 $\|1{-}1/\rho\|^n\!\approx\!e^{n(2\beta-1)/(2\gamma^2)}$ 转负阈值 $n^*\!\approx\!2\gamma^2/\lvert2\beta{-}1\rvert$ | $h\approx\sqrt{n\delta/2}$ | **½** | 2 | $\sim T_0^2\approx9\!\times\!10^{24}$ | ✗(平均意义✓) | `CONV1`§3／`CONV3`§3／`E30-2` |
| 2 | **Brown 2005 Thm 2**（= Droll Conj 1.7.10） | 零自由 $\Rightarrow$ Li 非负 | $k\le 2T^2\log T$ ⟹ $h\!\approx\!\sqrt{k/(2\log k)}$ | **½**(带 $\log$) | 2 | $\sim T_0^2/\log$ | ✗（**Thm 2 未证**，Lemma 5 含洞）✗ | `A1-BROWN-ORIGINAL`§2／`ENGINE-FOUND-brown-thm2`§1 |
| 3 | **Droll 2012 Conj 3.2.7**（修复路线） | 不展开、AM-GM + 单调($x{>}1$) + 尾部积分 | $T=\sqrt n+1$ ⟹ $h\approx\sqrt n$ | **½** | 2 | $\sim T_0^2$ | ✗（路线识别；证明未写） | `A1-ROUTE-conjecture-3.2.7`§1–2 |
| 4 | **D-037 显式预算** | 完整 $B(n)$（$T=\sqrt n+1$，五处更正 F-A…F-E） | $h\approx\sqrt n$ | **½** | 2 | $(T_0{-}1)^2\approx9.0\!\times\!10^{24}$ | ✗（链条源头 = 有洞的 Brown Thm 2） | `A1-ROUTE`§5／`BL1`§1–2 |
| 5 | **Bombieri–Lagarias 1999 分解**（"B–L 分解"） | $\lambda_n=S_\infty(n)-S_f(n)+\delta$（阿基米德/有限位） | **引擎，不独立给 $h(n)$**；承载者取 $T=\sqrt n$（α=½）或 $T(n)=2n$（α=1） | **½ 或 1** | 2 或 1 | 随承载者 | 视承载者 | `E5`§3(Rmk)／`BL1`§1／`ALIGN-A1-li-coefficients`§1 |
| 6 | **Lagarias 2007 框架**（$g_n$ 显式） | $\lambda_n\ge0\iff$ Weil 正性（$g_n=L^1_{n-1}(-\log x)$ 截断于 $(0,1]$） | **框架，无独立 $(n,T)$ 转换**；且**落在 N2** ⚠️ | — | — | — | — | `E5-lagarias-gn-explicit`§2–3 |
| 7 | **Jensen–Pólya 双曲性**（GORZ 1910.01227） | Hermite 极限/大 $d$ 由吸引子控制 | $d\le\lfloor T\rfloor^2$ ⟹ $h=\sqrt d$ | **½** | 2 | $d\lesssim9\!\times\!10^{20}$ | ⚠️**名义✓**（E47 §五① 标"无条件性**待核**"） | `ALIGN-A2-jensen-polya`／`E47`§二 |
| 8 | **Palojärvi 1807.01506 主定理** | 窗口内 $\Re\lambda_F(n,\tau)\ge0\Rightarrow$ 零自由 | 窗口上端 $5N^2(A_F\log N+M_F)$ ⟹ 二次 | **½** | 2 | $\sim T_0^2$ 形状 | ✗（需显式不等式；$\tau\!\to\!1$ 未核） | `A1-COMPLETE-proof-and-constants`§4 |
| 9 | **Palojärvi Thm 2.1（$\tau$-Li）** | $n\ge\max\{e,\,T_0/(e\tau)\}$，$T(n):=n\,e\,\tau$ | $h\approx n\,e\,\tau$ | **1** | 1 | $\sim T_0/(e\tau)$ | 条件 | `ALIGN-A1-li-coefficients`§2／`ALIGN-A1-COMPLETE`§2 |
| 10 | **我方窗口路线**（相位窗口） | $\theta(\gamma)\!=\!2\arctan(1/2\gamma)$，相位 $\approx2\pi$ 时 $n\!\approx\!2\pi\gamma$ | $h\approx 2\pi n$ | **1** | 1 | $n\le 2T_0\approx6\!\times\!10^{12}$ | **✓** | `FRONTIER-VS-OURS`§A1／`PAPERA-v2-structure` |
| 11 | **Coffey 2004/2005** | $\eta$ 结构 + 数值 | **无转换定理**（递推支持 $n\le10^5$ 数值） | — | — | — | — | `FRONTIER-MAP-v2`／`LATEST1-combination-report` |
| 12 | **Freitas**（Li 型判据 ↔ 零自由半平面） | 半平面判据（**输入非"高度"**） | — （非 $\mathcal C$-类） ✗ | — | — | — | — | `ENGINE-FOUND-brown-thm2`§2 |
| 13 | **Nyman–Beurling / Báez-Duarte / DFMR** | 距离泛函 $d_r^2$；margin $\sim d^2\asymp C/\log N$ | $h(N)\!\approx\!N^{1/\delta}$（$\delta\!\in\![0.083,0.5]$ 实测） | **非幂律**（log—线性配对） | — | $N\!=\!10^{12}$ 时 $\gamma_0\lesssim10\!\sim\!10^6$ | — | `DOOR5/5b/5c/5d`／`HOT-STATE`§五 A4 ⟹ **排除出 $\mathcal C$-类** ✗ |
| 14 | **矩/阶/相位型**（A3 Weil 压缩／A5 显式公式／A7 Connes） | 只用 1–2 阶矩／共振于 $x=\log p$／5 素数 | **无幂律**（log 预算） | 不适用 ✗ | — | $\ll T_0^2$（log 级） | ✓ | `E47`§二／`E30`§1律 II |
| 15 | **函数域 purity/Frobenius**（对照） | $\lambda_K(n)=-\sum_j\alpha_j^n$，**$\lvert\alpha_j\rvert=q^{1/2}$ 精确**（Hodge–Riemann 极化上同调） | $n_0\approx\log(2g)/(\varepsilon\log q)$，**与"高度"无关** | **0** ⭐ | **0** | 与 $T$ 无关（最优） | ✓（函数域） | `E100`§1–2／`A14` |

### ⑥.2 枚举读出的三条结构事实

```
【⑥-2a】**所有 char-0 的 $\mathcal C$-类族的 $p\le2$（$\alpha\ge\frac12$）** ✓ —— 各族 $\alpha$ 取 **{0*, ½, 1}**，**无 $\alpha\in(0,\frac12)$ 者** ✗
   （*$\alpha=0$ 仅函数域 purity 有 ✓；char-0 无对应物 ⟹ 见 ⑥-2c）
   机制 [档案 CONV3 §3 / E30-2 §二]：$T^2$ **不是被输入的**，而是从 $n\lesssim N(T)/B_T$，$B_T=\frac12\sum_{\gamma>T}\gamma^{-2}\asymp\frac{\log T}{T}$ 长出 ✓
   —— 而 $\gamma^{-2}$ 的**来源是共享恒等式** $\lvert1-1/\rho\rvert^2=1+\frac{1-2\beta}{\beta^2+\gamma^2}$ 中 $\gamma$ 的**平方** ✓
   ⟹ **凡"消费验证高度 + 经模长/相位感知零点"的探针，指数被同一恒等式锁在 $p\le2$** ✓[机制]
【⑥-2b】**唯一 $\alpha<\frac12$（$p<2$）的已知机制 = 函数域 purity，$p=0$** ⭐ —— 它**根本不消费"高度"**（$n_0$ 与高度无关）✓
   ⟹ **它不落在 $\mathcal C$-类内** ⟹ **不构成对 K1 的正面回答** ✗（K1 问的是"有限高度 $\Rightarrow$ 有限 $n$"的族）
   ⟹ 且其 **char-0 载体已判死** ✗✗：`E100`§3 交叉核对 `CLOSED-ROUTES-MAP` 第 6 箱「Deninger/Connes（char 0 **缺正性**）」＋
      「**purity 载体（Frobenius/HP/Weil —— 全封 —— ）死**」＋「char 0 **无 canonical similitude**」；
      N19（p-adic/Frobenius lift ✗、Arakelov height ✗）／G13（char 0 无条件 $\sqrt{}$-正性皆来自**有限性**）✓
【⑥-2c】各族 $\alpha$ 的**梯级**（对"可达范围"）：$\alpha=\frac12$（$T_0^2$）**>** $\alpha=1$（$T_0$）**>** log 型（$\ll T_0^2$）
   ⟹ 已知族中 **$\alpha=\frac12$ 即最优** ‡；$\alpha=1$ 反而**更弱**（我方窗口路线 $2T_0$ 比 $T^2$ 弱 $T_0$ 倍 ✓）
   ‡ **无条件**最优 $=\alpha=\frac12$ 由 **A2-Jensen**（§⑦ 诚实边界见 §⑨-3 待核）达到 ⟹ **$T^2$ 可达本身已不稀奇**（E47 §三 逐字）✓
```

### ⑥.3 对 K1 的直接判定

```
【K1 的存在支】需 $\alpha<\frac12$ 且**落在 $\mathcal C$-类**且**过 §③ 守门（P-Scale/P-SqrtPos/非重包装/N2）** —
   **枚举 15 族 ⟹ 无一命中** ✗；唯一 $\alpha<\frac12$ 机制（purity）**不在 $\mathcal C$-类**且 **char-0 载体已判死** ✗
【K1 的穷尽支】**已知族皆 $p\le2$（$\alpha\ge\frac12$），$\alpha=\frac12$ 可达并已被达到** ⟹ **律 I 在已知族与"仅消费验证高度"输入类内是【精确（sharp）边界】** ✓✓
【交叉印证】`exponent-bridge-final-audit`§6 的自杀测试 **C 类**（"$\alpha=1/2$ 且来自**新的泛函方程型自对偶**"）与 K1 的 $\alpha<\frac12$ 需求**同构** ⟹ 同一道墙 ✓
```

---

## ⑦ 结论（三选一 ⟹ **穷尽皆 $T^2$**）

$$\boxed{\text{K1 判定} = \textbf{穷尽（在已知族 ＋ "仅消费验证高度"输入类内）},\quad \text{皆 } \alpha\ge\tfrac12 \ (\Leftrightarrow p\le2),\quad \text{可达}\le T_0^2 \ \Rightarrow\ \textbf{律 I 是精确边界}}$$

```
【⑦-1 明示证据等级】
   · **档案级穷尽** ✓：15 族逐族取档（§⑥.1 全表附出处），无族给出 $\alpha<\frac12$；
   · **机制级论证** ✓[机制]：CONV3 $n\lesssim N(T)/B_T$ ＋ E30-2 共享恒等式 ⟹ $T^2$ 是**结构性长出来的**，非输入；
   · **⚠️ 非定理级** ✗：本结论**不是**"不存在 $\alpha<\frac12$ 探针"的证明；与 E47 §六逐字一致 ——「**未找到 ≠ 不存在**」✓
【⑦-2 与已有结论的一致性核对】
   · 与 **E97**（K1 开放、成败都有产出）✓一致（本档走出"穷尽"支，产出 = 律 I 边界 ✓）
   · 与 **E99**（只判"仅消费验证高度"类被迫 $T^2$；**未宣布 K1 判死**）✓一致
   · 与 **E100**（K1 归结为 L2）✓一致：$\alpha<\frac12$ 需**换输入**或**换载体** ⟹ 正是 L2 的 territory ✓
   · 与 **DOOR5d 三分法**（增长型 $T^2$／衰减型对数／无单调无转换）✓一致，且本档**把"增长型"细化到 15 族**
【⑦-3 结构性产出的确切陈述】[推导]
   「在**所有已知**的"有限高度 $\Rightarrow$ 有限 $n$"转换定理中，可达索引 $\le$（已验证高度）$^2$；
     且该上界**被达到**（Li-模长平均意义 ✓／Jensen 名义无条件 ✓／Palojärvi 主定理 $\sim$ 同形 ✓）⟹ **律 I 精确（sharp）**」
   **适用范围**：char-0、$\zeta$（及 Selberg 类，因 Brown/Droll 对 Selberg 类叙述 ✓）；**不含**函数域（那里 $\alpha=0$ ✓）
【⑦-4 未穷尽者（须显式列出，不得掩盖）】⟹ 见 §⑨-1
```

---

## ⑧【技术手段】＋【对我们的启发/推进】

**【技术手段】（本档实际用到的，便于复用）**
```
T1 **显式路径定向 grep**：`grep -n "<ID>" <显式档案>`＋`sed -n 'a,bp'` 取片段；目录内 `grep -rl` 限定 `docs/`，
   **未做全盘 find/grep** ✓（合纪律 ✓）
T2 **$\alpha/p$ 方向归一**：先把 $h(n)\asymp n^\alpha$、$n_0\asymp\gamma^p$、可达 $=T_0^{1/\alpha}=T_0^{p}$ **写死**，
   再读各族 —— 此步**直接防住** E30 §3 的歧义读法（否则会把 $\alpha=1$ 误当"更强"）✓
T3 **输入类筛选（$\mathcal C$-类）**：先按"输入是否是验证高度"筛掉 NB 等 ✗，**再**枚举 —— 避免把"不同输入型"混入同一标度表 ✓
T4 **机制反查**：把 $T^2$ 拆到 $N(T)/B_T$ 与共享恒等式 $\lvert1-1/\rho\rvert^2$ 两层，**任何族都回到同一恒等式** ⟹ 穷尽性有机制支撑 ✓
T5 **先行否决判据 + 一轮预算 + 触发即停**（§⑤）✓
```

**【对我们的启发 / 推进】**
```
① ⭐ **K1 的"换探针"支已穷尽 ⟹ 唯一活支是"换输入"** —— 与 `BL1`§4 选项 **C（弱化输入）**、`E100` 的 **L2** 三方**收敛** ✓✓
   具体：BL1 逐字「若能用**【零密度型】信息替代** [零自由信息]，则可能突破 $n\le(T_0-1)^2$」＋
        「φ 相位框架下"一个离轴零点毁掉正性"**不适用于算术框架**，因**预算 $B(n)$ 已把可能的离轴零点计入** ⟹ 算术框架**可容忍离轴零点**」✓✓
   ⟹ **"密度型输入能否替代零自由输入"是良定义、未被占据的问题**（BL1 原话 ✓）—— **这是 K1 穷尽后的合法后继** ✓
② ⭐ **把"$\alpha$ 的梯级"当工具箱**：遇到新判据，先量 $p$ 再谈价值；$p\le2$ 者**不必再投入**（律 I 已封顶）✓
③ ⭐ **$T^2$ 的来源不是 $\lambda_n$ 的量级而是恒等式里的 $\gamma^2$** ⟹ 想破 $\frac12$ 必须**换掉模长感知**（换输入或换载体），
   **不能**靠改窗口/改预算（BL1 §4A/§4B 已证只给常数因子 = K1-C 命中 ✓）
④ ⭐ **A2-Jensen 已无条件达到 $\alpha=\frac12$**（E47 §三）⟹ **"达到 $T^2$"不再是有价值目标**；价值全在 $\alpha<\frac12$ 或 $\alpha<\frac12$ 之外
⑤ ⚠️ **函数域对照的制度性提醒**：函数域 $\alpha=0$ 的引擎是 **purity（Hodge–Riemann 正定）**，
   而 char-0 **缺正性**（E100/AOB3-4：元素化失败 + 极化缺失，两个**独立**原因）⟹ **"为什么 purity 不能迁移"是可再攻的机理问题**（低频、不设路 ✓）
```

---

## ⑨ 未证清单（诚实边界）

```
【⑨-1 未穷尽者（本档未能定标度、不得计为"已枚举"）】
   u1 **高阶 Li 变体** $\lambda_n^{(m)}$（E99 §4 列为候选）—— 档案内**无其 $(n,T)$ 标度** ✗
   u2 **衍生态 $\xi^{(k)}$**（E99 §4，"高度"定义不同 ⟹ 标度可能不同）—— **未定** ✗
   u3 **Palojärvi Thm 2.1 的完整条件 (c)**（显式不等式）**未读** ⟹ 族 #9 的 $\alpha=1$ 仅**摘录级** ⚠️
   u4 **Brown 2005 原文**仍未取得（付费墙；仅有 Droll/Palojärvi 转述）⟹ 族 #2/#4 的陈述为**二手级** ⚠️
   u5 **零密度型输入族**（BL1 §4C）—— **属"换输入"支，非 K1 的"换探针"支**，本档不展开 ✓
【⑨-2 门槛/形态遗留】
   l1 **P-Type（点式⇏聚合）**：$\alpha=\frac12$ 现仅【平均/密度一】意义（CONV3 §4① 自陈局限）⟹ "对**每个** $n$"未达成 ✗
   l2 **K1 产物尚未成形** ⟹ §③ 的 P-Info/P-Basis/P-Dual/A_min 等"产物须补"项**全部悬空** ✓
【⑨-3 ⚠️ 本档发现的两处**内部张力**（如实归档，不掩盖）】
   t1 **E47 §五① 待核**：A2-Jensen 的"**无条件** $\alpha=\frac12$"仅**名义 ✓**；GORZ Thm 1.1 的 $N(d)\le c\,e^{d/2}$（**指数于 $d$**）
      与 $d\le\lfloor T\rfloor^2$（**多项式于 $T$**）是否相容 **未核** ⟹ 若不相容，则 **$T^2$ 的"无条件"仅在存在性层面** ⚠️
      ⟹ **不影响 ⑦ 的"穷尽"结论**（上界仍成立 ✓），但**影响"$T^2$ 已被无条件达到"这句** ⚠️
   t2 **E99 §4 的 $p<2$ 方向存疑** ⚠️：E99 记「找 $n_0(\gamma,\varepsilon)$ 按 $\gamma^p/\varepsilon^q$、**$p<2$** ⟹ **对一切 $\varepsilon$ 优于 $T^2$**」；
      按本档归一（§⑥ 约定，$\alpha=1/p$）$\;p<2$ 给出 $\alpha>\frac12$ ⟹ 可达 $<T_0^2$，**与"优于 $T^2$"字面相反** ✗。
      可能解释：E99 的"优于"指**敏感度**（对近线更灵敏）而非**可达范围** ⚠️。**本档不擅自改 E99**（T10：不静默改写 ✓），
      **只标记为需复核项** ⟹ **建议**：复核 E99 §4 并以 $p>2$ 表述"可达 $>T^2$" ✓
【⑨-4 本档未做（明确）】未写代码 ✗｜未改任何既有文件 ✗｜未下载文献 ✗｜未输入 $1/2$ ✗｜未构造模型 ✗｜未声称任何证明 ✗｜未宣布 K1 判死 ✗
```

---

## 提交链与定位

```
E97（K1 登记 ✓ 开放）→ E30 §3（判据 ✓）→ DOOR5/5b/5c/5d（形状三分法 ✓）
  → E30-2 / E47（机制 + 判据级分类表 ✓）→ E99（尖锐形式 + 盲区）→ E100（K1 ⟺ L2）
  → CONV1/CONV3（$T^2$ 机制：$N(T)/B_T$）→ BL1（换输入支 B/C）
  → **本篇 E124（K1 逐族 $h(n)$ 合并枚举 ⟹ 穷尽皆 $\ge T^2$ ⟹ 律 I 精确边界）** ✓
【一句话】K1 的"换探针"支在本轮**在已知族与"仅消费验证高度"输入类内被判穷尽**（15 族，皆 $\alpha\ge\frac12$；
唯一 $\alpha<\frac12$ 机制 = 函数域 purity，且其 char-0 载体已判死）⟹ **律 I 是精确边界**（档案级穷尽 + 机制级论证，非定理）⟹ **唯一活的后继 = "换输入"**（= BL1 §4C = L2）✓
```
