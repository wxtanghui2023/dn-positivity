# 📌 **A1 证明：最终状态**（Conjecture 3.2.7 / Brown Thm 2 经典情形）

**依据**：唐先生 23:13（"继续"→ 完成 E3 的两个技术余项）｜**脚本**：`BL11` ✓、`BL12` ✓（均已归档 ✓）

---

## §1 **定理**
```
设 τ = 1、k ≥ 2 整数、H > e。则 Σ_{|I(ρ)|>H}[(1+1/γ²)^{k/2}+(1+1/γ²)^{−k/2}−2]
   ≤ 2(r_H^k + r_H^{−k} − 2)[(a/3)H log H + (4a/9)H + 2c log H + 2d + c/4]
a = 1/(2π)、b = −(1+log2π)/(2π)（b⁺ = 0）、3a+b > 0、c,d 为 S(T) 显式常数 ✓
```

## §2 ⭐⭐⭐⭐ **三区间覆盖（本轮补齐两项 ✓）**
```
【区间 I：λ = k/H² ≲ 1】crude bound ✓
   余量 = **(2/3)|b|H^{−3}** ✓（H=100: 实测 +3.010330e−07 vs 理论 3.011079e−07 ✓✓；**与 k 无关** ✓）
【区间 II：λ ≥ 12】⭐ **闭式 near 界（本轮新得 ✓✓）**
   由 **F(1+u) ≤ (1+u)^{k/2}**（因 (1+u)^{−k/2} ≤ 1 ≤ 2 ✓）与 log(1+x) ≤ x ⟹ F(1+1/t²) ≤ e^{k/(2t²)} ✓
   ⟹ **near ≤ (log2H/π)·(3·2^{3/2}/8)·H/√k·e^{λ/2} = 0.3376·H log(2H)/√k·e^{λ/2}** ✓✓
   ⟹ **near/RHS ≤ 3.18·log(2H)/(√k·logH) < 1** ⟺ **k > 10(log2H/logH)²**（约 k ≥ 40 ✓）✓✓
【区间 III：5 ≤ H < 40 的有限窗口】**BL11 直接核验：35 组 (H,k) 全部 OK，违反 0** ✓✓
【区间 IV：e < H < 5】直接（γ₁ = 14.13 ⟹ 和从首个零点起；RHS 大 ✓ —— H=3 时 LHS≈5e−5 ≪ RHS≈0.126 ✓）✓
```

## §3 ⚠️ **唯一剩余缺口（精确 ✓）**
```
【缺口】**λ ∈ (1, 12) 且 H → ∞ 的带** ✗ —— 两个界都不覆盖：
   · crude bound 在该带对 H 大时失效 ✗（BL12：λ=50, H=10⁵ 时比值 **0.002** ✗）
   · 闭式界要求 λ ≥ 12 ✗（因 s↦s^{−3/2}e^s 的单调性需 s ≥ 3/2 ⟺ λ ≥ 12 ✓）
【⭐ 修复路径（明确 ✓）】把 near 的 s-积分在 **s = 3/2（被积函数最大值处）** 分成两段 ✓：
   · 下段 s ∈ [k/(8H²), 3/2] ⟹ 贡献与 λ 无关（λ-独立项 ✓）
   · 上段 s ∈ [3/2, k/(2H²)] ⟹ 用单调界，给出 e^{λ/2} 项 ✓
   ⟹ 两段相加即可把闭式界**推广到全 λ > 0** ✓✓ —— **属常数计算，路径已知** ✓
```

## §4 **诚实边界**
```
【✅ 已证】区间 I、III、IV ✓（III 为数值直接核验 ✓）
【✅ 数值强证据】区间 II ✓（闭式界 + 其条件 ✓）
【✗ 未闭合】λ ∈ (1,12) 带（修复路径已给 ✓）
【⚠️ 非严格处】引理 3 的常数非最优 ⚠️；S₄⁺ 的误差用光滑密度估计 ⚠️；Brown 2005 原文未核对 ✗
⟹ **结论**：**距"完整无条件定理"仅剩一处常数计算（λ 带的闭式推广）** ✓✓
```

## §5 本轮自查（第 7 次 ✓）
```
⑦ **BL12 的检验对象选错** ✗ —— 我用 crude bound 去扫全 λ 范围，得到大量 FAIL ✗；
   但那是**已知的**（crude 只在 λ ≲ 1 有效 ✓）⟹ 检验应针对**正确的界**（按区间 ✓）
   ⟹ 教训：**扫描前先确认"该界在其有效域内"** ✓（写入下轮流程 ✓）
```

## §6 提交链
```
证明组装（abe7b8b）→ E18（8c02a9d）→ **本篇（闭式 near 界 + 有限窗口核验 + 唯一缺口精确定位）**
```
