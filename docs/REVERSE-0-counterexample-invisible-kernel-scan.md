已查地图：命中 3 处 —— 先逐条判 已DEAD/已封/已登记；命中即引既有条目，不得开新案
　判定：3 命中＝`V248`（锥分离／**"要求单边 β-敏感性"**——**相关异义**：指判别锥的单边性，非本档"β-敏感缺失坐标" ✗✓）＋
　`V256`／`E18-NOGO-ALIGNMENT`（反例族用词）⟹ 皆**非本机制** ⟹ **"反例→不可见核"生成器在本仓零覆盖（MAP-NEW）** ✓

# **REVERSE-0**：反例 → **不可见核** → **最小缺失信息**（统一扫描：三既有反例族）

**唐先生令（2026-09-22 21:25）**：
① ⚠️ **诊断**：真正的问题已不是"过滤器够不够严"，而是**过滤器开始反过来定义搜索空间** ⟹
　「这样迟早会得到一个漂亮的 **NO-GO 博物馆**，却不会产生新数学」✓✓；
② **换向**：不问"这个反例能否被排除"，改问「**为什么所有现有机制都会制造这种反例？反例共同保留了什么量？**」✓；
③ 三条新路：**(A) 反例的共同核**（求 `I(X_i)` 恒定，且**现有约束看不见 I、目标性质看得见 I**）｜
　**(B) 现有约束的核**（把已证约束写成 `Φ: X→ℝ^m`；找 `ker Φ`，再找 `Ψ ≢ f∘Φ`）｜
　**(C) 最小不可见扰动** `δ(X) = min{改变目标性质但保持全部旧约束不变的扰动}` ⟹ **information-theoretic witness** ✓✓；
④ **新生成器（核心原则，照录）**：
　$$\boxed{\text{我们不再要求"文献里已经存在一个成熟机制"才允许继续；我们要求的是"反例迫使一个尚未被现有约束记录的量出现"}}$$ ✓✓
⑤ **只保留 3 个硬问题**：**G1** 反例是否成**族**（有参数）？｜**G2** 现有约束对该族是否存在**不可见方向**（`ker Φ`）？｜
　**G3** 目标性质是否**沿该不可见方向变化**？（若 `Φ(X_t)` 恒定而 `P(X_t)` 变 ⟹ **自动获得 OPEN 攻击点**）✓✓
⑥ **本轮砍掉一半过滤器**：新第一阶段**只保留 G1–G3**（**不适用**十三门）✓；⑦ **下一刀＝对三既有反例族统一扫描** ✓

D0: 本档对象 = **三既有反例族的"不可见核／最小缺失信息"统一扫描**（档案**零覆盖** ⟹ 新档；**未开案** ✓）
D1: 0
FREEZE-ACK: 零计算／零 Lean／未写程序／**未调用 C-380 代码**（仅读档）／未放宽门槛 ✓

---

## §1 protocol（先写死 ✓）

```
【定义】 `Φ` ＝ 已被证明的约束映射（聚集为一向量）；`ker Φ` ＝ "`Φ(X)=Φ(Y)` 但 `X≠Y`" 的方向；
　`δ(X)` ＝ 保持 `Φ` 不变而翻转 `P` 的**最小扰动**；**最小缺失坐标** ＝ 使 `Φ+Ψ` 对 `P` 敏感的**最小复杂度 Ψ** ✓
【G1】 家族性（有参数）｜【G2】 `ker Φ` 是否有**显式见证**｜【G3】 `P` 是否沿核**变化** ✓
【阶梯】 G1∧G2∧G3 ⟹ **OPEN 攻击点**（**不**在本轮谈证明，亦**不**适用十三门——依令 ✓）
【证据等级】 **档级**（本仓档案逐字引用 ＋ 标准事实凭记忆 ⚠️）【预算】 一轮；**不开案** ✓
```

## §2 三族扫描（**档级**，逐字引用 ✓）

### F1 · **β-blind 反例族**（谱/矩侧）
```
Φ ＝ 只记录 **γ 侧**的聚合量（例：**序数测度 `μ_γ` 的 Hankel 矩 ＝ β-盲**〔`MASTER-STATUS-AND-CLOSURES` L773（门①②）〕✓）
家族 ＝ **离轴对 (β,γ)** 连续参数 ＋ 在线零点集（`V192` 关键翻译：`Ξ` 非实零点 ⟺ `ξ` 离轴零点，
　　　且一次离轴对给出**共享同一 γ** 的两个零点）✓
带内见证 ＝ 同一 `γ` 侧数据而 `β` 不同（`Hankel` 矩**不变**——档案已证 **β-盲** ✓）
P ＝ **RH**（零点是否全在线上）✓ → **变化** ✓
G1 ✓（(β,γ) 连续家族）｜G2 ✓（**档案自证 β-盲**）｜G3 ✓ ⟹ **OPEN** ✓✓
⭐ 最小缺失坐标（**档案已自行指出**）：**重数／退化** ——
　`V192` 逐字：纵坐标多重集里「**离轴对 ＝ 二重（退化）点，在线零点 ＝ 单点**」✓✓；
　且当日**硬问题**正是：「Hedenmalm 能否在不假设 RH 下构造**同时依赖 (β,γ)** 的算术二元对象？若仍只能看到 γ 则干净封死」✓✓
　⟹ 档案以"**谱实现族一次性封**"结案 ⟹ 本生成器下**应升级为测量**：**最小 Ψ ＝ 使 Φ 对 β 敏感的重数型 observable** ✓
```
### F2 · **C-380 极值族**（支撑/相位侧）
```
Φ ＝ **支撑结构 ＋ 约束值**（`F_{2r} = Σ_j T_{2r}(u_j) ≤ 1/2`；`k` ＝ 不同 `|u_j|` 数；`k≥4`／`k_min=5` 证书）✓
家族 ＝ 五点极值构型（含近碰撞参数）✓
⭐ 显式见证 ＝ **S1-A 近碰撞**：`ASSETS-REGISTRY` 逐字「则存在 `|δ| ≲ 8.8×10^-20` 的近碰撞」
　＋「→ near-collision → R／Hankel → 【**pairwise-cancellation wall**】」✓✓
　（＝**同支撑、不同符号/相位细数据**：偶层可行而奇层可被**成对消去** ⟹ 旧约束看不见 ✗）
P ＝ **奇层非退化／谱分离** ✓ → **变化** ✓
G1 ✓｜G2 ✓（近碰撞见证）｜G3 ✓ ⟹ **OPEN** ✓✓
最小缺失坐标 ＝ **符号/相位（σ／φ）的细结构**（超越 `|u_j|` 的"支撑分离"）✓
　（与既有方法过滤一致：档案已登记「**支撑分离 ≠ 谱分离**」✓）
```
### F3 · **算术局部数据族**（有限处局部数据恒等）
```
Φ ＝ **有限处局部数据**（`α_p ≡ 1`；局部因子／Hecke 关系**按定义**成立）✓
家族 ＝ 局部数据相同的算术对象族 ✓
见证 ＝ 局部数据**完全相同**而**全局零点行为不同**（⟹ `ker Φ` **巨大**：Φ 在有限处近乎恒等 ✓）
P ＝ **RH 型全局性质** ✓ → **变化** ✓
G1 ✓｜G2 ✓｜G3 ✓ ⟹ **OPEN** ✓✓
⭐ 最小缺失坐标（**档案已点名**）：**Archimedean 层** —— `ASSETS-REGISTRY` **D-3** 逐字：
　「**ζ 零点不在 motive 层，在 Archimedean 层** ⟹ 所有 Frobenius／几何类比失败的根因 ＝ **层错了**」✓✓
```

## §3 ⭐ 统一读数（**读法，非定理** ⚠️）

```
【三族的"最小缺失坐标"】 F1 ＝ **重数/退化** ｜ F2 ＝ **符号/相位** ｜ F3 ＝ **Archimedean 层** ✓
【共同形态（暂定读法）】 旧约束**只记录一阶/聚合量**（γ 侧矩、支撑计数、有限处局部数据），
　而 `P` 由**离对角／二次／层间**数据决定 ⟹ 三处缺口同型：
　$$\boxed{\text{聚集可见} \ne \text{离对角可见}}$$ ⚠️（**读法**，未证；与既有"支撑分离 ≠ 谱分离"、"β 墙"一致 ✓）
【⭐ 与传统过滤器的差别】 本档**不**以"文献已有成熟机制"为门槛；三处 OPEN **由反例本身强制**（G2 有档级见证 ✓✓）
```

## §4 下一步（三族各自的**最小 Ψ 问题**，可执行陈述 ✓）

```
【F1】 求**最小复杂度** observable `Ψ`，使 `Φ(Hankel 型 γ 侧矩) + Ψ` 对 `β` 敏感；已知候选＝**重数/退化** ✓
【F2】 求对**近碰撞稳健**的相位敏感 functional（须能区分 `|δ| ≲ 8.8e-20` 的成对消去见证）✓
【F3】 求**最小 Archimedean 型** observable，使有限处局部数据族对全局性质敏感 ✓
（三者在**开口阶段均不要求**已知证明路径；求出 `Ψ` 后再谈结构／证明 ✓）
```

## §5 边界与回查（✗✓）

```
✗ 零计算／未写程序／未调用 C-380 代码（仅读档）／不适用十三门（**依令**）／**未开案** ✓
✗ 不把三处 OPEN 记为战绩；**未**声称三族"必有新结果"（`Ψ` 存在性＝**待答问题** ✓）
⚠️ §3 明标**读法非定理**；标准事实凭记忆处已标 ⚠️；`V248` 命中为**相关异义**（单边 β-敏感性）✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 不可见核      命中文件数=0 ⟹ **本档新增** ✓
　技术词 最小缺失信息  命中文件数=0 ⟹ **本档新增** ✓
　技术词 反例不变量    命中文件数=0 ⟹ **本档新增** ✓
　技术词 反向生成器    命中文件数=0 ⟹ **本档新增** ✓
　技术词 反例族        命中文件数=15 ⟹ **档案已有 ⟹ 引用**（不作本档提出）✓
【档级引用】 `MASTER-STATUS-AND-CLOSURES` L773（Hankel 矩 β-盲）／L360（`V192` 纵坐标退化封印＋当日硬问题）｜
　`ASSETS-REGISTRY` D-3（Archimedean 层）／L537（近碰撞 `8.8e-20`）／L565（pairwise-cancellation wall）｜
　`V248`（单边 β-敏感性，相关异义）✓
