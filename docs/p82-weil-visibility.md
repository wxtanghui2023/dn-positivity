# P8.2：Weil Visibility Gap——单离轴轨道解析模型——A/B/C

> 2026-09-01 · 唐先生 P8.2 指令 · 先做严格单离轴解析（不跑大规模数值）

## 核心定义
- W_Λ(f) = Σ_ρ f̂((ρ−½)/i)·f̄̂((1−ρ−½)/i)——Weil 二次型
- 离轴轨道（½±δ+iγ₀）：W 项 = 2Re[f̂(γ₀−iδ)·conj(f̂(γ₀+iδ))]
- V(Λ) = −inf_{||f||=1} W_Λ(f)——Visibility Gap——攻击 V(Λ) ≥ Φ(d(Λ))

## A. 单离轴轨道（解析式——完成）
**W_orb(δ) = 2Re[f̂(γ₀−iδ)·conj(f̂(γ₀+iδ))]**
- 实偶 f̂（高斯）：W_orb 随 δ 减小（δ=0.1：−7.3e-19）——负可见性存在
- 奇 f̂（Hermite1）：负可见性更大（δ=0.1：−1.6e-16）
- **解析确认：离轴轨道的 W 项随 δ 单调减小（负可见性）**

## B. 局部化 f̂（完成）
| σ | 负差/||f||² | 背景/||f||² |
|---|---|---|
| 0.15 | −1.5e-5（Hermite1） | 3.42 |
| 0.30 | −3.7e-17 | 1.18 |
| 0.60 | −1.5e-68 | 0.49 |
- **负差极小——背景（在线零点——正）远大于负差**

## C. 补偿（真正的新问题——完成——墙）
**背景吃掉负质量**：
- 在线零点（正——0.4-3.4）≫ 离轴负差（−1e-5 到 −1e-68）——W_total 仍 > 0
- **且——素数侧自适应**（P6.2——Weil 恒等式：W_zeros(f) = W_primes(f)——任何配置——实际离轴（ζ）匹配——循环）

## ⭐ P8.2 判定（初步）
**Visibility Gap 的量化（V(Λ) ≥ c·Φ(d)）——撞两堵墙**：
1. **背景补偿**：在线零点（正）吃掉离轴负差（数值——C）
2. **素数侧自适应**：W_zeros 的负——素数侧吸收（恒等式——P6.2 循环）

**"V > 0 ⟹ 离轴"是 Weil criterion（已知——重发现风险）**——量化版本（V ≥ c·Φ——新）——但——素数侧自适应——墙

## 与文献（arXiv 2608.24827——有限窗口退化）的关系
- 确认"有限窗口 W 可接近退化"（σ 大——背景小——负差）——与文献一致
- **但——"退化"（λ_min → 0）≠ "负可见性"（V ≥ c·Φ）**——量化是新的——但——素数侧自适应——墙

## 有价值的新对象（记录——即使不提供排除）
**W_orb(δ) = 2Re[f̂(γ₀−iδ)·conj(f̂(γ₀+iδ))]**——离轴轨道 W 项的解析式——负可见性的精确形态（随 δ 单调减小——依赖 f̂ 的相位结构）——这是 P8.2 的实质产物（虽然是 Weil 已知机制的解析细化）

## 下一步（P8 系列）
- (a) ③ global budget（负贡献必须有界正项承担——全局预算）
- (b) ④ spectral flow（离轴化必然造成谱漂移）
- (c) 接受 P8.2 初步收口（背景补偿 + 素数侧自适应——与 P5-P7 相同的墙）
- (d) 唐先生指示
