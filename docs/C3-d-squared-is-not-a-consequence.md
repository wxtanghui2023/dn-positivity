已查地图：命中（`C3-RETRACTION-c2-does-not-centralize-b`／`C3-bridge-C1-verified-and-abi`）⟹ **本档冻结其依赖项**，不开新案
D0: 本档对象 = **`(A)(B)(C)` 逐字** ＋ **`d^2` 单独核验**：⭐**`d^2=1` 不是三条 Coxeter 关系的推论（`S_3` 商模型反证）** ＋ 冻结清单
D1: 0 （[REVIEW] 轮次：核验与冻结，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`d^2` 核验：不是 Coxeter 层推论**

## §1 关系集逐字（`(A)(B)(C)` 的完整定义）

```
$$\Gamma:\quad \rho_0^2=\rho_1^2=\rho_2^2=\rho_3^2=1,\qquad (\rho_0\rho_1)^3=(\rho_1\rho_2)^6=(\rho_2\rho_3)^3=1,\qquad (\rho_0\rho_2)^2=(\rho_0\rho_3)^2=(\rho_1\rho_3)^2=1$$ ✓
$$a:=\rho_0\rho_1,\quad b:=\rho_1\rho_2,\quad c:=\rho_2\rho_3\ \Longrightarrow\ a^3=b^6=c^3=1$$ ✓
$$\textbf{(A)}\ (ab)^2=1\qquad\textbf{(B)}\ (bc)^2=1\qquad\textbf{(C)}\ (abc)^2=1$$ ✓✓（`(A)\iff(\rho_0\rho_2)^2=1`；`(B)\iff(\rho_1\rho_3)^2=1`；`(C)\iff(\rho_0\rho_3)^2=1`）✓
【额外（本线目标关系的两条）】 $$T_L=b^2a^{-1},\ T_L^2=1;\qquad T_R=b^2c^{-1},\ T_R^3=1$$ ✓
```

## §2 `d^2` 的计算入口（照录）

```
$$d:=b^2c\ \Longrightarrow\ \boxed{d^2=b^2c\,b^2c}$$ ✓ **不得把 `c` 与 `b^2` 交换** ✓✓
```

## §3 ⭐⭐ 核验结论：`d^2=1` **不是** `(A)(B)(C)`+阶关系的推论

```
**【方法】** 造一个**满足全部六条 Coxeter 关系**的小商，在其中算 `d^2`；若 `\neq1`，则 `d^2=1` **不可由关系推出** ✓✓
**【反证模型（`S_3`）】** $$a=(123),\qquad b=(12),\qquad c=(123)$$ ✓
　逐条验：`a^3=1` ✓；`b^6=1` ✓；`c^3=1` ✓；$$(ab)^2=\big((123)(12)\big)^2=(13)^2=1$$ ✓；$$(bc)^2=\big((12)(123)\big)^2=(23)^2=1$$ ✓；$$(abc)^2=\big((123)(12)(123)\big)^2=(12)^2=1$$ ✓✓ **六条全满足** ✓
**【在该模型中算 `d^2`】** $$d=b^2c=(12)^2(123)=c\ \Longrightarrow\ d^2=c^2\neq1$$ ✓✓（`c` 阶为 `3`）✓
$$\boxed{\textbf{故 }d^2=1\ \textbf{不是 }(A)(B)(C)\ \textbf{的推论}}$$ ✓✓✓
```

## §4 ⚠️ 结论的**范围**（严格界定，不越界）

```
**【只否定了什么】** `d^2=1` **不是**"三条 Coxeter 关系 ＋ 阶关系"的**推论** ✓✓
**【未否定的】** `d^2=1` 在**真正目标群 `\Gamma_{2,3}`**（**再加 `T_L^2=1`、`T_R^3=1`**）中**是否成立 —— \textbf{仍 OPEN}** ✓
　（反证模型 `S_3` **不满足**额外关系：`T_L=a^{-1}=c^2`，`T_L^2=c\neq1` ✗ ⟹ 该模型**不是** `\Gamma_{2,3}` 的商 ✓）
【⟹ 精确判词】**在 `\Gamma_{2,3}` 内判 `d^2` 需要额外关系下的结构**，**不能**用 Coxeter 层推理代替 ✓✓
```

## §5 冻结清单（按您的规则立即执行）

```
$$\boxed{\textbf{全部冻结待复核}:\quad d^2=1\ \text{及其一切依赖分支};\quad Bridge-C\text{-}1;\quad (5);\quad (TR3');\quad \text{旧 }X\ \text{化简}}$$ ✓✓
$$\boxed{\textbf{无争议保留}:\quad T_L=b^2a^{-1},\ T_R=b^2c^{-1}\ (\text{角抵消判据});\quad T_L^2=1,\ T_R^3=1;\quad \text{非平凡性 }(24/54,\ \text{dart 计数});\quad bab=a^{-1};\quad bcb=c^{-1};\quad cbc=b^{-1};\quad ab=cabc;\quad a\,b^2c=ca}$$ ✓✓
【⟹ 行动】**不进入 `B1/B3`** ✓（依赖面已冻结）✓
```

## §6 下一步（两条，供裁）

```
**【路 1】** 判 `d^2` 在 `\Gamma_{2,3}` 内的真值：**必须在含 `T_L^2=1`、`T_R^3=1` 的关系闭包内做** ⟹ **手工推理代价高**；**最省力者 = 允一次性"最小解禁"**：在**任一显式小商**（如 `[3,6,3]` 的某个低阶模商或 `\{3,6\}_{(2,0)}`／`\{6,3\}_{(3,0)}` 的已知群）**只算一个元素 `d^2`** ✓⚠️（**需您明示**）
**【路 2】** **不判 `d^2`**，把 `d^2` 从资产清单**永久移除**，`B` 改在新关系集（`a b^2c=ca` 等）上重设第一刀 ✓
【⛔ 纪律】 本档**未计算、未实现**（`S_3` 反证为**手验级**）；**未制造候选／未启动搜索／未碰 RH** ✓
【边界】 §1、§2 为**逐字关系与入口**；§3 为**手验反证**；§4 为**范围界定**；§5 为**冻结执行**；均**可手核**。

## §7 【技术词回查】（补录）
```
技术词 quotient         命中文件数=58   :: ./TARGET-L9-q11-dir-probe.md ./p59s-fixed-direction.md ./p131-range-defect.md 
技术词 freeze           命中文件数=9    :: ./p38-g18-analytic-rigidity.md ./C357-execution-round1-singular-stratum-hit-degenerate-solutions-fall-back-to-2plus2.md ./C380-S1-FREEZE.md 
```
