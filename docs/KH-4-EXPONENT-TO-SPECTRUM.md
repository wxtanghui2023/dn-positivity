已查地图：命中 43 处 —— 先逐条判 已DEAD/已封/已登记；命中即引既有条目，不得开新案
　判定（**两处实质命中，全部引用** ✓）：⭐ **`C90-DEC-1`**（**桥接审计先例**：六门 ＝ A 对象转换／B 曲率来源／C `n`-保持／D 一致性／E RH-free／F 强度，
　　**判定 `(III)`：根本没有可用对象转换 ⟹ 四分支全关** ✓✓）｜⭐ **`ASSETS-REGISTRY` `F-2`**（**Mellin 算子 β-提取**：由**素数单独构造**，
　　读出前 ~500 零点 `β≈1/2`（最大偏差 <0.1）｜**已核验 ✓**）✓
　⚠️ 覆盖度高（`Mellin` **181**／`Perron` 26／`dilation` 31／`尺度指数` 7）⟹ **Mellin 类方案本仓已重覆盖 ⟹ 本档只作"审计对象"不作采用** ✓✓

# **KH-4 / EXPONENT-TO-SPECTRUM**：`1/2` 的**身份转换**是否存在非循环机制？

**唐先生令（2026-09-22 22:49）**：
① 选 **(a)**，但**目标再收紧**：**不是直接"发明一个桥"**，而是先**审计"组合最优指数 `\to` 谱量"是否存在非循环的中间机制** ✓；
② ⭐ **缺口已钉住（照录）**：
　$$\boxed{\underbrace{\sqrt x = x^{1/2}}_{\text{算术/组合原生}}\ \ \not\Rightarrow\ \ \underbrace{\tfrac12 \in \operatorname{Spec}(J)}_{\text{谱型临界指数}}}$$
　⟹ ⭐「**`1/2` 已经找到了；缺的是"指数的身份转换"**」✓✓
③ **四门（照录）**：
　**G1 桥的输入必须真的改变**：要求存在算术对象 `Q_x`，其自然尺度来自 `d \mid n`、`d \leftrightarrow n/d`，且 `x^{1/2}` 由该对称性**优化产生**；
　　⛔ 若构造算子时直接写 `D_{1/2}f(x) = f(x^{1/2})` 或任何等价"把 `1/2` 塞进 dilation" ⟹ **DEAD — artificial normalization** ✓；
　**G2 必须存在 canonical operator**：不是任意线性算子，而是由 divisor decomposition **唯一／自然诱导**者（如 `TQ_x = Q_{\Phi(x)}` 或尺度递推 `Q_x = \mathcal A_x(Q_{\sqrt x},Q_{\sqrt x})`）；
　　⭐ 关键审计问题：**不指定 `1/2`，算子本身是否仍然唯一地产生**？若有无穷多 operator 都能把 `x^{1/2}` 做成 eigenvalue ⟹ **不算桥** ✓✓；
　**G3 谱量必须是"指数"，而不是人为取 log**：允许 `Tv = \lambda v` 再由自然尺度关系推出 `\lambda = \beta` 或临界增长指数；
　　⛔ **杀掉**「`\|T^n v\| \asymp x^{1/2}` ⟹ 所以谱指数是 `1/2`」；若只是把已有增长率通过 `\alpha = \log\lambda/\log x` 重命名成"谱指数" ⟹ **SURVIVOR-5 FAIL** ✓✓；
　**G4 ⭐ 最重要：必须有反向信息增益**：`组合 1/2 → 谱 1/2 → 谱量还能区分新的算术信息`；
　　若谱化后只得到恒等的 `\lambda = 1/2` 而无任何 β-sensitive 新增 ⟹ 只是**漂亮的谱化包装** ⟹ 最终问：
　　$$\boxed{\text{谱化后的对象是否比原来的 }\sqrt x\ \text{多携带一个可变化参数？}}$$ ✓✓（**SURVIVOR-5 硬门** ✓）
④ ⭐ **必须提前排除的"熟悉桥"（照录）**：**主动把 Mellin／Perron／transfer-operator／dilation-operator 类方案放进审计，而不是直接采用**——
　因它们极易制造 `x^\alpha \leftrightarrow` Mellin 指数 `\alpha \leftrightarrow` 算子本征值 的**形式桥**；
　若 `\alpha = 1/2` 是输入端的 divisor split，则所得"谱 `1/2`"很可能只是**尺度指数的谱表示** ⟹ 即使正确也应判 **P4/P5 insufficient，而非 survivor** ✓✓
⑤ **本档唯一目标（照录）**：**不证明 RH；不接 β；甚至暂时不接 ζ**；只回答：
　$$\boxed{\text{能否从除数对称强制产生一个 canonical operator，使 }1/2\text{ 成为其真正的谱量，而非重命名？}}$$ ✓✓

D0: 本档对象 = **"组合最优指数 → 谱量"之身份转换审计（`KH-4/EXPONENT-TO-SPECTRUM`）**（档案**零覆盖**机制 ⟹ 新档；⚠️ **但桥接审计格式与 Mellin 方案均已有档案先例 ⟹ 引用** ✓；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算（解析）／零数值／未写程序**／未做数值搜索／未证 RH／未接 β／未接 ζ ✓

---

## §1 **G1 ✅**：桥的输入是真的（且未塞 `1/2`）

```
【算术对象】 `Q_x := \#\{(a,b) \in \mathbb N^2 : ab \le x\}`（**除数和函数的超双曲线计数** ✓）✓
【自然尺度来源】 `d \mid n` 与 **divisor 对称** `d \leftrightarrow n/d`（在超双曲线中是 `(a,b) \leftrightarrow (b,a)` ✓）✓
【`1/2` 的产生方式】 由**对称性优化**产生：切分代价 `F(t) = t + x/t` 的最小点 `t = \sqrt x` ✓✓
　（等价：不动点集 `\{(a,a)\}` ⟹ `a = \sqrt x` ⟹ **`1/2` 是"对称不动点的指数"** ✓）✓✓
【⛔ 未塞 `1/2`】 构造算子时**不**写 `D_{1/2}` ✓；⛔ **G1 禁止项已守** ✓
【⚠️ 本档主动标注的陷阱】 常见"对称化"写法 `P = (\mathrm{id} + \sigma)/2` 中的那个 **`1/2` 是归一化常数，不是导出的几何量** ⟹
　按 G1 的精神，此 `1/2` **不得**被当作"由对称性产生" ✓✓（**与令 ③ 的 artificial normalization 同类** ✓）
```

## §2 **G2 ✅**：canonical operator 确实**唯一自然诱导**（且**不需**先指定 `1/2` ✓）

| canonical operator（由 divisor 分解诱导） | 唯一性 | 备注 |
|:--|:--:|:--|
| **对合** `\sigma_x: f(t) \mapsto f(x/t)`（divisor involution） | ✅ **唯一**（由 `d \leftrightarrow n/d` 唯一诱导）✓ | 固定点 `t = \sqrt x` ✓ |
| **超双曲线分解算子**（`\Sigma_{n \le \sqrt x}` 投影） | ✅ 唯一（切割点由对称强制）✓ | `\mathrm{Spec} \subseteq \{0,1\}` |
| **尺度递推** `Q_x = \mathcal A_x(Q_{\sqrt x}, Q_{\sqrt x})`（hyperbola 递推 ＋ 对角修正） | ✅ 唯一 ✓ | 步长 `\sqrt x` 由对称强制 ✓ |
| **对数尺度重整化算子**（递推的迭代） | ✅ 唯一 ✓ | 在 `u = \log x` 上即**乘 `1/2`** ✓ |

```
⟹ **G2 ✅ 通过**：**不提及 `1/2` 也能唯一产生这些算子** ✓✓（**这一门是本次唯一"过"的环节**）
　⟹ 桥的失败**不在"算子不存在"**，而在**谱**（见 §3 ✓）
```

## §3 ❌ **G3**：谱量**不是**导出的"指数"（两个方向皆失败 ✓✓）

```
【(i) 对合 `\sigma_x`】 `\sigma_x^2 = \mathrm{id}` ⟹ **`\mathrm{Spec}(\sigma_x) = \{+1,-1\}`** ✗✗
　⭐ **关键读数**：**`1/2` 是"不动点的指数（位置）"，而 `\pm 1` 才是"本征值"** ⟹
　　$$\boxed{\text{位置} \neq \text{本征值}}$$ —— **这正是令 ② 所说"身份转换"的精确形态** ✓✓✓
【(ii) 分解投影】 `\mathrm{Spec} \subseteq \{0,1\}` ✗（无 `1/2` ✓）
【(iii) 对数尺度重整化】 在 `u = \log x` 上**就是乘 `1/2`** ⟹ 其"谱 `\{1/2\}`"**字面上就是递推自身的步长** ⟹
　由 `\alpha = \log\lambda/\log x` 得到的"谱指数 `1/2`" ＝ **输入重命名** ⟹ ⛔ **SURVIVOR-5 FAIL** ✓✓✓
　（⭐ 这是**最锐的伪桥实例**，而且它是**canonical 的** —— 说明"canonical"**不**自动等于"非重命名" ✓）
【(iv) Mellin／Perron（依令 ④ 主动审计）】 `x^\alpha \leftrightarrow` Mellin 指数 `\leftrightarrow` 算子本征值 ⟹
　⛔ 依令 ④：若 `\alpha = 1/2` 来自输入端 divisor split ⟹ 所得谱量**只是尺度指数的谱表示** ⟹ 判 **P4/P5 insufficient** ✓✓
　⚠️ 且 Mellin 路径得到"临界轴 `1/2`"须经**函数方程**（**档案已封来源** ⟹ 引 `P-3`：对称＝已封闭 ✓）
⟹ **G3 ❌ FAIL**（两方向：对合给 `\pm1` 而非 `1/2`；重整化给 `1/2` 但**纯属重命名** ✓✓）
```

## §4 ❌ **G4**：无反向信息增益（＋ 与档案既有资产对照 ✓✓）

```
【增益检验】 `\sqrt x` **无可变参数** ⟹ 其任何谱化（对合／投影／重整化／Mellin）**亦不携带新参数** ⟹
　$$\boxed{\text{谱化后不比 }\sqrt x\ \text{多携带任何可变化参数}}$$ ⟹ **G4 ❌ FAIL** ✓✓
【⭐ 与档案 `F-2` 的关键区分（诚实标注 ✓）】
　· `F-2`（**已核验 ✓**）：**Mellin 算子 β-提取**——**由素数单独构造**，读出前 ~500 零点 `β \approx 1/2`（偏差 <0.1）✓
　· ⟹ 档案**已有一个"素数侧构造的算子能读出 β"的数值资产** ✓✓ —— 即"**读取 β**"这一侧**已经存在** ✓
　· ⚠️ 但它是 **β-提取（extraction）**，**不是 `1/2` 的导出（derivation）**：`β` 是**输出读数**，而非由算子谱**强制出的值** ✓✓
　⟹ 本档的结论与之**相容且互补**：**"读 β"已成（数值）**；**"导出 `1/2`"仍缺** ✓✓
【桥接审计先例（引用 ✓）】 `C90-DEC-1`（六门 A–F）判定 **`(III)`：根本没有可用对象转换** ⟹
　本档结构与之一致（本档四门中 **G1／G2 过**、**G3／G4 不过**），且**卡点更窄**（＝**"位置→本征值"的身份转换**）✓✓
```

## §5 ⭐ 出口（依令：**精确收口**，不泛泛说"没找到" ✓✓）

```
【出口】 ⛔ **G3／G4 失败** ⟹ 缺口**精确收为**
　$$\boxed{\text{组合最优指数}\ \not\leadsto\ \text{非人为 canonical spectrum}}$$ ✓✓
【精确形态（本档新刻画）】 缺的**不是算子**（G2 ✅ 已唯一产生），而是
　$$\boxed{\text{"不动点位置（指数 }1/2\text{）"}\ \not\leadsto\ \text{"本征值（谱 }1/2\text{）"}\ \text{的转换机制}}$$ ✓✓✓
　（任何已知转换手段——对合／投影／log 重整化／Mellin——**要么给别的本征值，要么就是重命名** ✓）
【⛔ 禁项已守】 未证 RH／未接 β／未接 ζ／未采用 Mellin 类方案（仅审计）✓；⛔ 守 `V290` 禁语 ✓
【⛔ 不宣布"不存在此类桥"】 依"**未找到 ≠ 不存在**"：本档**只审了由 divisor 分解自然诱导的 canonical 算子族** ⟹ 保持 `GAP` ✓✓
【状态建议】 `KH-4` = **GAP（窄口径）**；卡点＝**"位置 → 本征值"的身份转换** ✓
```

## §6 边界与回查（✗✓）

```
✗ 零计算（解析）／零数值／未写程序／未做数值搜索／未开案／未证 RH／未接 β／未接 ζ ✓
✗ ⛔ 不把"G3/G4 不过"记为战绩；⛔ 不宣布"不存在"；⛔ 不把 Mellin 方案当采用 ✓
⚠️ 对合谱 `\{\pm1\}`／投影谱 `\subseteq\{0,1\}` 为**标准事实**（凭记忆 ⚠️，未逐字核档）；`F-2` 与 `C90-DEC-1` 为**档级引用** ✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 指数转谱 命中文件数=0 ⟹ **本档新增** ✓｜身份转换=0 ⟹ **新增** ✓｜canonical算子=0 ⟹ **新增** ✓｜反向信息增益=0 ⟹ **新增** ✓
　技术词 伪桥 命中文件数=1 ⟹ **档案已有（`C90-DEC-1`）⟹ 引用** ✓
【地图回查】 命中 43；**实质命中＝`C90-DEC-1`（桥接审计六门／判定 `(III)`）＋ `F-2`（素数侧 Mellin β-提取，已核验）** ⟹ **引用** ✓
　覆盖度：`Mellin` 181／`Perron` 26／`dilation` 31／`尺度指数` 7／`双曲法` 0 ✓
【档级引用】 `ASSETS-REGISTRY` **`F-2`** ✓｜**`C90-DEC-1`**（六门桥接审计先例；判定 `(III)`）✓｜`P-3`（对称＝已封闭来源）✓｜`P3-B2`（指数来源单点卡点）✓
