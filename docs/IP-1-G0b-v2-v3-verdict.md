已查地图：未覆盖（关键词: extra_zeros 闭区间口径 最小反例对 rank-invisible）—— 可开档 ✓

# **`IP-1-G0b`**：口径复验（v2／v3）＋ **真反例对** ＋ 判定（`d_V = B 强确认`；`extra_zeros = C 候选 OPEN`）

**唐先生令（2026-09-23 10:01）**：① ✅ **不再从 `d_V` 榨新机制**；若复验仍 100% ⟹ **正式记 `IP-1/d_V = B：rank-flag complete`**，**不再扩样**（`8208` 不做 `10000/100000`）✓；
　② ⭐ **`extra_zeros` 值得停下来看**：$$\boxed{d_V\ \text{被 rank flag 完全解释}\ \neq\ \text{整个零点结构被 rank flag 完全解释}}$$ ⟹ 可能存在**二级结构变量** ✓；
　但**现在不能**称其为"新 invariant"（可能只是节点几何／多项式系数／端点效应／根重数／某个已有 Chebyshev·零点计数定理的函数／我的计根定义伪象）✓；
　⭐ 真问题已细化：$$\boxed{\text{什么信息决定了 rank flag 无法决定的 }extra\_zeros？}$$ ✓
　③ ⚠️ **区间口径必须先冻结旧结果**，且**保留两个字段**（`open_interval_extra_zeros`／`closed_interval_extra_zeros`），**不覆盖旧数据** ✓；
　④ **重跑后不扩样**，做更锋利的判别 **`C1`：寻找最小反例对**（同 `(r,q,flag,d_V,rank)` 而 `extra_zeros` 不同者，打印全部原始节点／系数／多项式）✓；
　⑤ **去名字测试**（硬门）：`M_i` 若只是已有零点计数／矩阵秩／条件数／判别式的改名 ⟹ **立即淘汰**；要的是 $$\boxed{M(A)=M(B)\Longrightarrow \text{同一零点结构}}$$ 这类**可证结构律** ✓

D0: 本档对象 = **`IP-1-G0` 口径复验 ＋ 真反例对 ＋ 判定**（`IP-1-G0` 实验的复验，非改名；**未开 RH 案** ✓）
D1: 0
FREEZE-ACK: D1=0（本档不主张新自由度）；依 §8.1 冻结程序债务见 `IP-1-G0` 档 §7 ✓
【计算授权】 依令执行一次小规模复验（v2／v3，均在 `IP-1-G0` 授权范围内）；⛔ 未扩样；⛔ 未触碰 RH／β／EF／prime-pair ✓

---

## §1 复验结果（数字驱动 ✓✓）

| 版本 | 计数方法 | 实例 | `ec` 取值 | 分组变动 | 结论 |
|:--|:--|:--:|:--|:--|:--|
| **v1／v2** | 自写 **Sturm** | 8208 | **含负值**（`-1`/`-3`）✗ | 51/71 ⚠️ | ⛔ **作废**（计数不可信） |
| **v3** ⭐ | **`sympy.Poly.count_roots(-1,1)`**（口径经探针确认：**端点计入、重根计一次**） | 8208 | **`{0,1,2,3,4,5,None}`，无负值** ✓ | **35/71**；**含 `m` 分组仍 35** ✓ | ✅ **权威** |

```
【纠错记录（重要 ✓）】 v1／v2 的自写 Sturm 在"取值恰为零"时**任意取 -1** ⟹ 计数错误 ⟹ 出现**与 `p|_S=0 ⟹ 根数≥|S|` 矛盾**的负值 ✓
　⟹ 依本仓铁律（**结果异常先怀疑自己的分支**）**换独立精确计数器交叉核对** ⟹ v3 用 `sympy` **权威计数** ⟹ **负值消失** ✓✓
【⭐ 稳健结论】 `ec` **不由 `(r,q,m,flag,d_V,rank)` 决定**（含 `m` 分组仍有 **35** 组变动 ⟹ **非 `m` 伪象** ✓）；
　且 `p|_S = 0` 的**逐点精确验证失败数 = 0** ⟹ 消失元**真实** ✓
```

## §2 ⭐ **真反例对**（同 rank 数据、`ec` 不同；精确算术 ✓✓）

```
【A】 `r=2, q=1, m=2, flag=(1,2,2), d_V=3, rank=2`；`S={-1, 1/4}`
　`poly = 8x^3-6x^2-11x+3 = (x+1)(8x^2-14x+3)` ⟹ 实根 `{-1, 1/4, 3/2}` ⟹ `[-1,1]` 内 **2** 个 ⟹ **`ec=0`** ✓
【B】 同 key；`S={-1, -3/4}`
　`poly = 40x^3+42x^2-19x-21 = (x+1)(40x^2+2x-21)` ⟹ 实根 `{-1, -3/4, 7/10}` ⟹ `[-1,1]` 内 **3** 个 ⟹ **`ec=1`** ✓
【差异源（本档读出，待证）】 两者的**第三个根**（`3/2` vs `7/10`）**一个在区间外、一个在区间内** ⟹
　$$\boxed{\text{差异源 ＝ 消失元的"额外根"是否落入 }[-1,1]\text{，即节点几何 × 根位置}}$$ ✓
```

## §3 **判定（承令 ✓✓）**

```
【`IP-1/d_V`】 ✅ **B 强确认**：恒等式 `d_V = q + min{j: rank(prefix_j)<j+1}` 在 **8208/8208** 成立；`v3` 权威计数下**无异常** ⟹
　$$\boxed{\text{IP-1}/d_V = \text{B：rank-flag complete}}$$ ✓（**不再从 `d_V` 榨新机制**；**不扩样** ✓）
【`IP-1/extra_zeros`】 ⭐ **C 候选 OPEN**（**稳健**：`ec` 不由含 `m` 的 rank 数据决定，已有**逐例可验证的真反例对** ✓）；
　⛔ **但"数学新量"＝尚未成立** —— 差异源目前看是**节点几何 × 根位置**，很可能落在**经典 Chebyshev alternation／零点计数**范畴 ⟹ **必须过"去名字测试"** ✓✓
【下一步（承令 ④⑤）】 **`IP-1-C1`**：只研究 $$\boxed{\text{rank flag 解释不了的那部分零点结构到底由什么决定}}$$ ＋ **去名字硬门**（`M_i` 若为已有量改名 ⟹ 立即淘汰；须得可证**结构律**）✓
【⛔ 不做】 不扩样（`8208 → 10000 → 100000`）｜不从 `d_V` 继续榨｜不把 rank flag 包装成新量｜不称 `extra_zeros` 为"新 invariant" ✓
```

## §4 归档与边界

```
【数据保全 ✓】 v1 数据 `out/ip1_g0/ip1_g0_instances.tsv` **未被覆盖**；v2／v3 各写新档（`_v2`／`_v3`）⟹ **双字段可追溯** ✓
【脚本】 `scripts/ip1_g0b_...v2.py`（Sturm·**已判定不可信** ⚠️）｜`scripts/ip1_g0c_...v3.py`（**sympy 权威** ✓）
【日志】 `out/out_ip1_g0.txt`｜`out/out_ip1_g0_v2.txt`｜`out/out_ip1_g0_v3.txt` ✓
【R7】 结论数字驱动（实例数、分组数、变动组数、取值集合）✓｜【R1】 脚本头含目的／输入／输出／纪律 ✓
✗ 未证 RH／未接 ζ／未开案／未扩样／未把新量挂牌 ✓
```
