# ⭐ **P2 的实质进展**：比较归结为【**归一化剖面**】的单个显式不等式 ✓✓

**依据**：唐先生 2026-09-12 09:09（"1,2,3,4都要做"）｜前三轮（BL13/BL14/BL16）之后的**结构性简化** ✓

---

## §1 ⭐⭐⭐ **关键恒等式（本轮发现）**：RHS 的因子**就是 2× 在 t=H 处的被加项** ✓✓
```
【论文 B 的 RHS 因子】2(r_H^k + r_H^{−k} − 2) ✓，其中 r_H = (1+1/H²)^{1/2} ✓
   ⟹ **r_H^k = (1+1/H²)^{k/2} = e^{v_H}** ✓ ⟹ **r_H^k + r_H^{−k} − 2 = e^{v_H} − 2 + e^{−v_H} = F(1+1/H²)** ✓✓
⟹ **RHS = 2·F(1+1/H²)·T(H)** ✓✓ —— 即 **RHS 恰好是"被加项在 t=H 处的取值"的 2T 倍** ✓✓
【⟹ 比较的真形】near = Σ_{H<|γ|≤2H}F(1+1/γ²) **≤** 2·F(1+1/H²)·T(H) ✓
   ⟹ **LHS 与其在端点的取值之比** ≤ 2T ✓ ⟹ 比较 = **"剖面的积分" vs "端点值×bracket"** ✓✓
```

## §2 ⭐ **归结（清晰的单一不等式）** ✓
```
【记 δ = t/H − 1 ∈ [0,1] ✓，则】
   near ≈ F(1+1/H²)·H·w(λ)·(1/π)·log(2H) ✓
   其中 **w(λ) := ∫_0^1 F(1+1/(H²(1+δ)²))/F(1+1/H²) dδ** ✓✓（**归一化剖面** ✓，与 H 无关 ✓）
⟹ 比较 **⟺ w(λ) ≤ logH/(3·log2H)** ✓✓（因 (1/π)/(2a/3) = 3 ✓）
【阈值】H=10³: **0.303** ✓｜H=10⁶: 0.317 ✓｜H=10²: 0.285 ✓ ⟹ **阈值稳定 ~0.30** ✓
【精确 w 的数值】λ=3: **w ≈ 0.278** ✓（< 0.303 ✓）；λ=6: w ≈ 0.277 ✓
   ⟹ ⭐ **真实剖面【满足】该不等式** ✓✓（余量 ~10% ✓）—— **故比较为真，只差显式估计** ✓
```

## §3 **显式界的两条路线与各自短板** ✓
```
【路线 A（v²e^v）】w ≤ 7λ²/32 ✓（由 4sinh²(v/2) ≤ v²e^v ✓）⟹ **有效 λ < 2.04** ✓
【路线 B（浓度剖面）】w ≤ 8/(3λ) ✓（由 φ(δ) ≥ 3δ/4 ✓，其中 φ = 1−(1+δ)^{−2} ✓）⟹ **有效 λ > 8.8** ✓
   改进：由 ψ(u) = (λ/2)u² − 2log u 的凸性（ψ′(1) = λ−2 ✓）得 **w ≤ 1/(λ−2)** ✓ ⟹ **有效 λ > 5.3** ✓
【⚠️ 间隙】**λ ∈ (2.04, 5.3)** ✗ —— 两路线皆不覆盖 ✓
【根因（第三次确认）】**F 在 v ≲ 1 时 ≈ v²** ✓，故需 **min(1, v²)** 型逐项界 ✓
   且 **v ≥ 1 区的 (1−e^{−v})² 因子**（∈ [0.4, 1] ✓）也需保留 ✓ —— 两处合计约 1.8× 松弛 ✗
【⟹ 剩余任务】一条 **Laplace 型显式估计**（含配平常数 ✓）—— **属技术性但非原则性** ✓
```

## §4 **状态与论文 B**
```
【✅ 已得】① RHS = 2F(H)·T 的结构性恒等式 ✓✓（**最有用** ✓，使比较"归一化" ✓）
   ② 归约为 w(λ) ≤ ~0.30 的单一不等式 ✓✓ ③ 两条显式路线 + 有效域 ✓ ④ 真实 w 已数值证实 < 阈值 ✓
【✗ 未得】λ ∈ (2.04, 5.3) 的显式估计 ✗
【⭐ 论文 B】**保持可投** ✓（数值验证 + 局限 (a) ✓）；**新增**：§1 的恒等式可写入论文（**使表述更清晰** ✓✓）
【第 10 次自查 ✓】本轮发现：我用"profile×F(H)"口径一度与本项目已算的比值差 3.5× ✗
   ⟹ 原因是**混用了"密度加权"与"计数加权"平均** ✓ —— 归一化后自洽 ✓✓
```
