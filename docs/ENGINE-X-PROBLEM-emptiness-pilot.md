已查地图：命中（`CORRECTION-idea-first-not-capacity`／`CROSS-DOMAIN-MAPPING`／`EXTERNAL-DEMAND-SCAN`／`S13-EXECUTION-TEST-FAIL`）⟹ **引用，不开新案** ✓
D0: 本档对象 = `(已知引擎 × 尚未用过它的具体问题)` 空白格试点（纸面，6 候选格；每格五列）＋ 试点自身成功判据
D1: 0 （`[REVIEW]` 轮次：想法生成试点，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`(引擎 × 问题)` 空白格试点（纸面）**

## §0 **新矩阵（照录您的裁示）**

```
错矩阵（我们一直在搜）：$$\text{我们的资产}\times\text{任何问题}\ \Rightarrow\ \text{三次重发现}$$ ✗
对矩阵：$$\boxed{\text{已知引擎}\times\text{尚未用过它的具体问题}\ }=\ \text{找空白格}$$ ✓✓
【为什么可搜】 数学创造最常见的形式＝**移植**：`\Lambda\ge0`＝热流＋单调性移植入 ζ；熵递减＝信息论/动力系统移植入乘性函数；我们的 `k=4`＝区间盒覆盖移植入存在性判定 ✓
```

## §1 **六候选格（逐格五列）**

| # | 引擎 | 空白格候选（具体问题） | 为何可能未用过（**假设，待查**） | 第一步可检验推论 | 判死条件 |
|:--|:--|:--|:--|:--|:--|
| **`A`** | **区间盒覆盖证书**（`k=4` 那套） | **"数值上已知、但只有数值证据"的小型极值常数**（某有限维极值问题的最优值／最优构型） | 该常数多由**数值优化**给出，作者未做**严格覆盖**；领域内"可接受数值"惯例 | 锁定**一个**公布常数，判定"其极值性证明是否仅为数值" ⟹ 若是，覆盖证书可做 | 若该常数已被区间法严格覆盖 ⟹ ✗ |
| **`B`** | **单调流（CNV 型：全实零点随时间单调）** | **其他具 Riemann 型 FE 的对象**（含已知离轴零点者，如 `DH`）的**临界时间**（`\Lambda` 的类比量） | 该引擎长期**只在 ζ 上用**；别的 FE 对象少见 | 写出该对象的 `H_t`、检验单调性定理是否可用；**数值定位**"最后一个实零点消失"的时间 | 若文献已有该对象的 `\Lambda` 研究 ⟹ ✗（⚠️ 我们已知 **`DH` negative-control 方向有 prior art**） |
| **`C`** | **熵递减（entropy decrement）** | **乘性函数相关和的"非标准自变量"版本**：沿**多项式自变量／稀疏集／带数字限制（自动序列）**的 `\lambda(n)\lambda(n+h)` 型相关 | 该引擎成熟于**平移（`n+h`）与对数平均**；`f(n)` 加权或稀疏自变量下的关键输入（短区间抵消）**未必现成** | 写出一条**带权的精确陈述**（`\sum_{n\le x}\lambda(n)\lambda(n+h)f(n)=o(...)`），再判定"引擎的短区间输入对 `f` 是否可得" | 若短区间抵消对 `f` 完全不可得 ⟹ 引擎不可移植 ✗ |
| **`D`** | **盒覆盖＋穷举完整性审计** | **已发表数据库的"完整性声明"缺独立证书**（小规模组合设计／小阶 Hadamard／小参数最优编码等） | 这类声明多为**计算性**且**年代较早**、证书未随发布 | 取**一个**数据库，判定其"已穷尽到 `N`"是否**只有程序输出**、无独立复核 | 若已有独立复核 ⟹ ✗ |
| **`E`** | **转移原理（一阶＋二阶矩＋正惯性上界 ⟹ 秩/计数下界）** | **"由矩数据确定隐成分个数"**（矩方法可辨识性）／**signed graph 的负惯性↔失配计数** | 该原理在**线性代数/矩阵分析**里部分属**民间定理**；在具体计数问题上的用法**未见系统化** | 写出**精确定理形状**（输入：迹、`HS` 范数、正惯性上界；输出：秩/计数下界），再核文献 | 若已有同型定理 ⟹ ✗（中风险） |
| **`F`** | **"证书形状先行"重述**（我们的 `k=4` 方法论本身） | 某个**已知难但小**的判定问题，其难点**恰在"如何把 `\forall` 变成有限覆盖"** | 多数人从**搜索算法**入手，而从**证书形状**入手少见 | 对**一个**具体问题，写出"若它成立，一页证书长什么样"；写不出即 `NO-IDEA` | 写不出 ⟹ 换格 ✓ |

## §2 ⭐ **试点自身的成功判据（本档核心，避免又变成"漂亮资产"）**

```
【成功 ＝】 至少 **1 个格**满足：$$\text{30 分钟文献核查确认该格确为空白}\quad\land\quad\text{第一步是一个\textbf{具体、小、当天可做}的动作}$$ ✓✓
【失败 ＝】 六格**全部**在 30 分钟核查内被占据（已有文献）⟹ 那么结论就是**该矩阵亦无空白格**（创造力上限，非流程问题）✓
【⛔ 不允许的事】 不把"表格做得漂亮"当产出；不写长篇方法论；不为候选格造工具；不碰 RH／零点／`\beta` ✓
【排序（优先核查）】 **`D` → `A` → `F`**（三者**不依赖新数学想法**，只依赖"证书缺失"这一可查事实）✓；`C`（需写新陈述）次之；`B`（prior-art 风险高）、`E`（中风险）最后 ✓
【边界】 ⚠️ 六格的"为何未用过"**全部是假设**（未逐字核文献）⟹ 每格的第一动作就是**30 分钟核查**；§2 判据为**本档自行推导** ✓；⛔ 未制造候选／未启动搜索／未改状态／未碰 RH ✓
```

## §3 【技术词回查】（逐字粘贴 ✓）

```
技术词 空白格        命中文件数=2    :: ./final-milestone-primitive-gap-2026-09-09.md ./ENGINE-X-PROBLEM-emptiness-pilot.md 
技术词 移植           命中文件数=45   :: ./ALIGN-variable-object-phase-three-level-audit.md ./C3899h-T1-closure-and-gamma13-M1-seven-wall-screening.md ./V184-external-input-triage-inertia-paircorrelation-and-positivity-machines.md 
技术词 entropy decrement 命中文件数=2    :: ./WHY-GRH-REMOVAL-WORKED-AND-OUR-WALLS.md ./ENGINE-X-PROBLEM-emptiness-pilot.md 
```
【三分类】 **本档新增**：`(引擎×问题)` 矩阵、六候选格、试点成功判据 ✓；**档案已有（引用）**：见上逐字；**通用词（不计）**：`空白格`／`移植` ✓
