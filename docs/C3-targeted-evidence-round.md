已查地图：命中（`C3-parameter-table-spec-and-status`／`C3-s_t-status-framework-and-hard-fork`）⟹ 引用，不开新案
D0: 本档对象 = `C3` 定点取证：**正则性条件逐字获得**（参数空间结构性钉死）＋ `§11E` 正文仍未取到的诚实标注
D1: 0 （[REVIEW] 轮次：取证，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`C3` 定点取证（本轮）**

## §1 ⭐⭐ 结构性收获（`Cambridge` 样章 PDF 逐字）

```
**"The vertices of `\{3,6\}` … a non-zero integer vector `(s,t)` generates a translation subgroup `\Lambda_{(s,t)}` … Thus, we may suppose that `(s,t)` is a non-negative vector. … we have maps `\{3,6\}_s=\{3,6\}_{(s,t)}` and `\{6,3\}_s=\{6,3\}_{(s,t)}` on the torus. They will generally be chiral: \textbf{the condition for regularity is again `st(s-t)=0`}. Observe that, in this case, only the 'small' vector `(s,t)=(1,0)` must be excluded."** ✓✓✓
【⟹ 参数空间被**结构性**钉死（非约定）】 正则性要求：$$st(s-t)=0\ \Longrightarrow\ s=0\ \text{或}\ t=0\ \text{或}\ s=t$$ ✓✓ —— **正是 `(a,0)` 与 `(a,a)` 两种形态**（与您先前的表述一致，且此处为**逐字**）✓
【⟹ 且 `(1,0)` 被排除】 故最小合法参数 `a\ge2`（`(a,0)`）或 `a\ge1`（`(a,a)`）✓
```

## §2 目录级确认（逐字）

```
$$11D:\ pp.410\text{–}416\ (\{\{6,3\}_{(s,0)},\{3,6\}_{(t,0)}\})\quad 11E:\ pp.417\text{–}422\ (\text{The Type }\{3,6,3\})\quad 11F:\ 423\text{–}430\ (cuts)\quad 11H:\ 437\text{–}444\ (relationships)$$ ✓✓
【⟹ 定点取证方案成立】 `11E` 仅 **`6` 页**，**无需抓整章** ✓
【附带】 `AMS Trans. 348 (1996)` "**Twisted Groups and Locally Toroidal Regular Polytopes**"（`McMullen–Schulte`）**侧重 rank `5,6`** ⟹ rank 4 的 `\{3,6,3\}` 主体仍在书中 ✓
```

## §3 ⚠️ 诚实状态（本轮**未完成**）

```
【未取到】 **`Table 11E1` 与 `§11E` 的全部参数结论** —— `Chapter 11` 为 `pp.387`–`444` 的**整章 PDF**，`§11E` 位于第 `30` 页之后 ⟹ **单次抓取代价超出当前预算** ✓
【⟹ `D` 集与 `(s_*,t_*)` 仍 `OPEN`】 承前档四项 `OPEN` 不变：完整 `(s,t)` 表／最小未决／显式 `H(s,t)`／`N_{\rm eff}` ✓
【本轮净推进】 **(i)** 参数空间**由逐字正则性条件钉死**（`st(s-t)=0`，`(1,0)` 排除）⟹ **候选集从"约定形态"升为"定理级形态"** ✓✓；**(ii)** `11E` 篇幅与页界确认，定点取证可执行 ✓
```

## §4 下一步（不计算）

```
【唯一动作】 取 **`§11E`（`pp.417`–`422`）正文** ⟹ 提取 **`Table 11E1`** 与全部参数结论 ⟹ 严格分离 **"known to be finite only when …"** 与 **"all other cases unknown"** ✓
【再】 取 **`§11H`** 仅提取 `(s,t)\sim(s',t')` 与 duality／cuts 等价 ✓
【再】**与 `Monson–Schulte` modular 构造做差集**（注意：$$d=4\ \text{得}\ \langle\{3,6\}_{(4,0)},\{6,3\}_{(4,0)}\rangle\ \text{有限 locally toroidal 4-polytope，群阶 }7680$$ ⟹ **"参数出现过"≠"universal 判定已解决"** ✓✓）
【⟹ 才得】 $$D=\{(s,t):\ 11E\ \text{未决}\ \land\ 11H\ \text{不可归约}\ \land\ \text{modular}\ne\text{universal 判定}\ \land\ \text{finite/infinite 真未知}\}$$；若 `D=\varnothing` ⟹ **`C3` 立即退出** ✓
【⛔ 纪律】 **`D` 集与显式 `H` 落地前绝不进入计算**；`C2` 暂停 ✓
【边界】 §1–§2 为**逐字 snippet**（`Cambridge` 样章、目录）；**`§11E` 正文未读**；§3–§4 为**本档状态与规格**；未制造候选／未启动搜索／未碰 RH。
