已查地图（所查：`FREEZE-AUDIT-FZ1-FZ4.md`（**`FZ-2` 原语覆盖表 ＋ `FZ-3` Canonical–Information Dichotomy ＋ 六筛子含 `null separation` N46**）、`CLOSED-ROUTES-MAP.md`（`:121` irreversible compatibility、`:1925` F1/缩放 site tropicalization、`:505/507` archimedean 赋值／A-leak）、`V250`（Connes–Consani F1 审计）、`V326-E3C`（自然非交换方块）、`V145`（Deninger 缺 canonical polarization）、`V237-D`（算术动力学＋内禀配对＋缺陷）、`E4-3`（`(B1)` 独立构造）、`final-milestone-primitive-gap-2026-09-09.md`（**组合独立性（matroid/CSP/amalgamation）＝死**））。**结论**：对三项外部突破提取的**五个 primitive**（`K1` 赋值提升／`K2` 行列式编码／`M1` hereditary small-ball／`M2` 插值／`R1` 可逆参考）作**机制级审计** ⟹ 关键词 `hereditary`／`small-ball`／`min-plus`／`reference process`／`Matrix Spencer`／`k-server` **全部 0 命中**（**形式上新**）✓✓；但按**档案既有原语表**判别：`K1`（valuation＝`M4` ✓ 已覆盖）、`K2`（行列式／上同调 ✓，Deninger 已封）、`R1`（**`M6`："运动来自我们的坐标选择"** ⟹ 运动族已判）**均落已覆盖**；**唯 `M1` 的形式（"对所有 restriction 一致"的 quantifier）未在表中**，`M2` 则**正落在档案唯一未覆盖原语（free probability／非交换熵 ✗）的邻域** ⟹ **值得开案的只有 `M1`（形式新）与 `M2`（邻域新）；但两者仍须过 `FZ-3` 二分与六筛子** ✓✓

# C-104 · **机制提取审计：三项外部突破的五个 primitive × RH 可实现性**

> **时间**：2026-09-18 15:41 唐先生：**不做"读全文"，做"机制提取审计"**——提取五个 primitive（`K1`／`K2`／`M1`／`M2`／`R1`），逐个问"**是否存在 RH 的 arithmetic realization**"（而非"能不能应用到 RH"），并先过本项目**硬过滤器** ✓
> **事实纠正（采纳唐先生）**：三项**并非都是"AI 从零独立发现"**——`k-server`（作者先做 `k=3` 势函数，后经对话重构）、`Matrix Spencer`（作者自列 **AI-assisted manuscript**）、`Matroid Secretary`（**claimed resolution**，2026-09-13 预印本，按 claimed 审计）✓

---

## §0 结论（先行）

$$\textbf{(1)}\ \text{五个 primitive 的}\ \textbf{关键词命中＝0}（\text{hereditary／small-ball／min-plus／reference process／Matrix Spencer／k-server}） \Longrightarrow \textbf{形式上未登记}✓✓$$
$$\qquad ⚠️\ \text{但}\ \text{发现档案已有}\ \textbf{专门的同类审计}：\text{`FZ-2` 原语覆盖表}＋\text{`FZ-3` 二分}＋\text{六筛子}✓✓$$
$$\textbf{(2)}\ \text{按}\ \text{`FZ-2` 原语表逐项落地}：\text{`K1` ⟹ valuation＝}\textbf{已覆盖}(M4);\ \text{`K2` ⟹ 上同调／行列式}\ \textbf{已覆盖};\ \text{`R1` ⟹ }\textbf{运动族}(M6)\ \textbf{已判}✓$$
$$\textbf{(3)}\ ⭐\ \textbf{唯二值得开案的}：\text{`M1`}（\text{hereditary 的}\ \textbf{quantifier 形式}\ \text{未在表中}）;\ \text{`M2`}（\text{落在档案}\ \textbf{唯一未覆盖原语} \text{free probability 的邻域}）✓✓$$
$$\textbf{(4)}\ ⚠️\ \text{但两者仍须过}\ \text{`FZ-3`}\ \textbf{Canonical--Information 二分}：\text{canonical} \Longrightarrow \beta\ \text{盲};\ \text{带零点位置} \Longrightarrow \text{循环}✓✓$$
$$\Longrightarrow\ \textbf{判词}：\text{外部启发}\ \textbf{形式上确有新形状}，\ \textbf{但无一项自动逃出二分};\ \text{当前 arithmetic realization}\ \textbf{候选数}=0✓$$

---

## §1 查地图（关键词）

| 关键词 | 命中 | 结论 |
|:--|--:|:--|
| `hereditary`／`heredit` | **0** | **未登记** |
| `small-ball`／`谱体` | **0**／**0** | 未登记 |
| `min-plus`／`min,+` | **0** | 未登记 |
| `reference process` | **0** | 未登记 |
| `Matrix Spencer`／`k-server` | **0** | 未登记 |
| `motion` | **0** | 未登记 |
| `null separation` | **1** | ⚠️ **已登记为六筛子之一（`N46`）** |
| `valuation` | 37 | ⚠️ **假阳性**（子串 `evaluation`）|
| `赋值` | 19 | ⚠️ 多为 **archimedean 赋值**／Gauss 赋值（`A-leak`、F1 缩放 site）|
| `determinant` | 33 | 已有（Deninger／det-正则化）|
| `matroid` | 1 | ⚠️ **是"组合独立性（matroid/CSP/amalgamation）＝死"** |
| `非交换` | 75 | 已有（`V326-E3C` 等）|
| `尺度流` | 9 | 已有 |
| `tropical` | 2 | 已有（`V250`）|
| `irreversible` | 11 | 已有（`CLOSED-ROUTES-MAP:121`、`E4-2`）|

## §2 逐 primitive 判定表

| 原语 | 档案覆盖 | 落在哪一 horn（`FZ-3`）| 是否有 RH candidate | 判定 |
|:--|:--|:--|:--|:--|
| **`K1`** 赋值提升（`min,+ → +,×`）| ✓ `order/valuation`＝`M4`；⚠️ `transfer` 未覆盖但"无算术内容入口" | canonical | 无 | **已覆盖（组合两半皆落旧类）** |
| **`K2`** 行列式编码 | ✓（上同调 ✓；Deninger `V145` 缺 polariz. 已封）| canonical | 无 | **已覆盖／重编码风险** |
| **`M1`** hereditary small-ball | **✗ 未覆盖**（表中无"restriction 一致"原语）| ? | **无** | ⭐ **形式新，唯二可开** |
| **`M2`** 插值（对角→非交换）| △ **邻档案唯一未覆盖原语**（free probability ✗）| ? | 无 | ⭐ **邻域新，预判"被同一吸收机制吞掉"** |
| **`R1`** 可逆参考 | ✓ `renormalization`／`filtration` ＋ **`M6`："运动来自我们的坐标选择"** | canonical | 无 | **已判（motion＝坐标人为）** |

$$\Longrightarrow\ \text{五项中}\ \textbf{三项落已覆盖}，\ \textbf{两项形式／邻域新}✓$$

## §3 `FZ-3` 二分的应用（决定性）

$$\boxed{\textbf{Canonical--Information Dichotomy}}\ \text{（档案原文，`FZ-3`）}：$$
$$\qquad \text{① 凡}\ \textbf{可从算术 canonical 定义} \text{的结构} \Longrightarrow \text{算术信息量有上界} \Longrightarrow \text{结构化到"聚合／比例／能量"层} \Longrightarrow \textbf{给不出零点位置选择}✓$$
$$\qquad \text{② 凡}\ \textbf{携带零点位置信息} \text{的结构} \Longrightarrow \textbf{立即循环}✓$$
$$\qquad \Longrightarrow\ \text{每一候选必落两端之一：}\boxed{\text{canonical}\Rightarrow\beta\text{ 盲};\quad \text{信息充分}\Rightarrow\text{循环}}✓✓$$
$$\text{应用到本轮}：\text{`K1`／`K2`／`R1`}\ \text{直接落①；}\ \text{`M1`／`M2`}\ \textbf{形式未落}，\ \text{但只要其内容仍是 canonical 可定义的} \Longrightarrow \textbf{仍落①}✓$$
$$\qquad ⚠️\ \text{故关键问题被精确化}：\textbf{hereditary 一致性／非交换插值 本身能否}\ \textbf{生成} \text{"位置选择"}，\ \text{而非仅做}\ \textbf{聚合}✓✓$$

## §4 六筛子（含 `null separation` `N46`）对本轮的作用

$$\text{六筛子}：\text{信息／压缩};\ \text{统计};\ \text{数值};\ \text{恒真／形式};\ \text{pincer};\ \boxed{\textbf{null separation}（N46）}✓$$
$$\qquad \text{唐先生提议的目标}\ \textbf{"arithmetic null separation"}\ \text{与}\ \text{`N46`}\ \textbf{同名} \Longrightarrow \text{该概念}\ \textbf{已在档}，\ \text{须先与其对齐（避免重命名旧筛子为新机制）}✓✓$$
$$\qquad ⚠️\ \text{同理}：\text{`R1`（可逆参考）}\ \textbf{不自动} \text{逃出}\ \text{`M6`}\ \text{的判词（运动＝坐标人为）};\ \text{须证明其运动是}\ \textbf{内禀} \text{的}✓$$
$$\qquad \text{而"内禀流"}\ \text{已在}\ L2\ \text{被否}：\text{承载物皆"几何／群对象＋内禀流"},\ \text{Spec}\ \mathbb Z\ \textbf{无内禀流}✓✓$$

## §5 预注册（若开 `M1`／`M2`，须先交付什么）

$$\textbf{开案门槛（本档预注册，任一缺失不得开）}：$$
$$\qquad \text{(a)}\ \text{给出}\ \textbf{具体 arithmetic hereditary structure} \text{实例（"对每个合法 restriction 一致"要能写出 }S\text{ 的显式族）}✓$$
$$\qquad \text{(b)}\ \text{过三闸}：\text{算术可实现}／\ \textbf{对}\ \beta\ \text{有可检验响应}／\ \text{对 restriction 一致（不随 }T,S\text{ 恶化）}✓$$
$$\qquad \text{(c)}\ \text{过六筛子（尤其}\ \text{`N46` null separation}、\ \text{`N9/N12/N27` 信息压缩}）✓$$
$$\qquad \text{(d)}\ \text{不落}\ \text{`FZ-3`}\ \text{两端（canonical ⟹ β 盲；信息充分 ⟹ 循环）}✓$$
$$\qquad \text{(e)}\ \text{不与}\ \text{`V259`}\ \text{判死判据冲突}：\textbf{此为关键}——\text{"对所有 restriction 一致"}\ \textbf{形式上} \text{不是}\ \lim F_n\ \text{型有限局部聚合}✓✓$$
$$\Longrightarrow\ \text{即}\ \text{`M1`}\ \text{的}\ \textbf{唯一真正新之处＝quantifier 结构};\ \text{而非其内容}✓$$

## §6 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-18 15:4x）`[纪律]`（先跑后写）

```
技术词 机制提取审计   命中文件数=1  :: ./C104-mechanism-extraction-audit-five-primitives-vs-archive-FZ2-FZ3.md
技术词 hereditary   命中文件数=1  :: ./C104-mechanism-extraction-audit-five-primitives-vs-archive-FZ2-FZ3.md
技术词 原语覆盖表    命中文件数=1  :: ./C104-mechanism-extraction-audit-five-primitives-vs-archive-FZ2-FZ3.md
```
**读数（按实测）**：三项均＝**1 档（仅本档）⟹ 本档新增措辞** ✓

## §7 边界

- `[查证]` §1 关键词命中**本档实跑** ✓；⚠️ `valuation` 37 命中**为假阳性**（子串 `evaluation`）已标 ✓
- `[档案]` §2–§5 的 `FZ-2`／`FZ-3`／六筛子／`M6`／`L2`／`V259` 均为**档内引用** ✓
- **不声称**：`M1`／`M2` 能解决或不能解决问题 ✗（**仅判"形式新"＋"须过闸"**）；`support>1` 相关结论不受影响 ✓
- **纪律**：先查后判（R-1 ✓，**先跑后写** ✓）；**未用 RH 作推导** ✓

```
⚠️ 唐先生 15:41：对三项外部突破（k-server / Matrix Spencer / Matroid Secretary）做**机制提取审计**：
   提取五 primitive（K1 赋值提升 / K2 行列式编码 / M1 hereditary small-ball / M2 插值 / R1 可逆参考），
   逐个问"是否存在 RH arithmetic realization"；先过本项目硬过滤器；并给出事实纠正（AI 参与度）
✅ 查地图（关键词，本档实跑）: hereditary=0, heredit=0, small-ball=0, 谱体=0, min-plus=0, min,+=0,
   reference process=0, Matrix Spencer=0, k-server=0, motion=0 ⟹ **形式上新** ✓✓
   ⚠️ 已登记项: null separation=1(**六筛子 N46 之一**)；valuation=37（**假阳性**，子串 evaluation）；
   赋值=19（archimedean/Gauss 赋值）；determinant=33；matroid=1（**"组合独立性(matroid/CSP/amalgamation)＝死"**）；
   非交换=75；尺度流=9；tropical=2；irreversible=11
⭐⭐ 关键发现: 档案已有同类审计 —— **FREEZE-AUDIT-FZ1-FZ4.md**: FZ-2 **原语覆盖表** + FZ-3 **Canonical–Information Dichotomy**
   + 六筛子（信息/压缩、统计、数值、恒真/形式、pincer、**null separation N46**）
✅ 逐 primitive 判定:
   K1 赋值提升 ⟹ order/valuation 已覆盖(M4)；transfer 半"无算术内容入口" ⟹ 落已覆盖
   K2 行列式编码 ⟹ 上同调已覆盖；Deninger V145 缺 canonical polarization 已封 ⟹ 已覆盖/重编码风险
   M1 hereditary small-ball ⟹ **表中未覆盖**（唯一形式新）⟹ 唯一真正新之处 = **quantifier 结构**（"对所有 restriction 一致"）
   M2 插值(对角→非交换) ⟹ **邻档案唯一未覆盖原语**（free probability/非交换熵 ✗），FZ-2 预判"被同一吸收机制吞掉"
   R1 可逆参考 ⟹ renormalization/filtration 已覆盖 + **M6："运动来自我们的坐标选择"** ⟹ 已判；且 L2 否决内禀流
⭐ FZ-3 二分应用: canonical ⟹ β 盲；信息充分 ⟹ 循环 ⟹ 五项**无一项自动逃出**；当前 candidate 数 = 0
   ⚠️ 唐先生目标"arithmetic null separation"与 **N46** 同名 ⟹ 须先对齐，避免重命名旧筛子为新机制
✅ 预注册开案门槛: (a) 具体 hereditary 实例 (b) 三闸（算术可实现/对 β 可检验响应/restriction 一致）(c) 六筛子
   (d) 不落 FZ-3 两端 (e) 与 V259 不冲突 —— 关键: "对所有 restriction 一致"形式上**不是** lim F_n 型有限局部聚合
✅ 净产出: ①五 primitive 覆盖审计表 ✓ ②发现并对齐既有 FZ-2/FZ-3/六筛子 ✓ ③唯一形式新 = M1 quantifier 结构 ✓
   ④M2 邻唯一未覆盖原语（需实测才可判死）✓ ⑤开案门槛预注册 ✓
```
