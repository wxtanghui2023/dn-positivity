# **TP/PF 路线的判定**：以零点实例化后，条件**自动坍缩**（+ 一次 ERR）

**依据**：唐先生 2026-09-11 20:04（"先继续"）｜**脚本**：TPX2 ✓、TPX3b ⚠️（**ERR**）
**标注**：【ERR】【推导】【核验】

---

## §0 ⚠️ **ERR 记录**（数值部分作废）
```
TPX3b 想算 Λ(0) = −Σ_k 1/Ξ′(γ_k)：得到 **|和| 膨胀到 1e+19** ✗ 且 Ξ′ 的符号交替在第 11–15 处**断裂** ✗
⟹ **Ξ′(γ_k) 的数值不可信** ✗ ⟹ 该"决定性检查"**作废** ✗
【病因】**只校准了 Ξ（零点全对 ✓ 14.1347251/21.0220396/25.0108576/30.4248761/32.9350616/37.5861782 ✓），
   却【从未校准 Ξ′】** ✗ —— 正是本项目登记过的"跳过校准"失误 ✓（同一族）
【教训】**派生物（导数/留数）必须单独校准**，不能因原函数校准通过就沿用 ✓
```

## §1 ⭐⭐ **解析部分的结论（不依赖上面的数值，成立）**
```
正确的单边对象：Λ(x) = Σ_j c_j e^{−γ_j² x} (x ≥ 0)，其 Laplace 变换 = Ξ(0)/Ξ(i√s) = ∏_j(1+s/γ_j²)^{−1} ✓
  （因为 Ξ(t) = Ξ(0)∏(1−t²/γ_j²) ⟹ Ξ(i√s) = Ξ(0)∏(1+s/γ_j²) ✓ 精确 ✓）

【(a) 核的 Toeplitz 检验 → 平凡 ✗】
   单边支集 ⟹ 矩阵 (Λ(x_j − y_k)) 为**三角阵** ⟹ det = ∏Λ(0) ✓ 恒 ≥ 0 ⟹ **不含信息** ✗

【(b) 系数序列条件 → 自动（Newton）⟹ 空洞 ✗】（**已数值核实** ✓）
   L(s)=∏(1+s/γ_j²)^{−1} = Σ a_n s^n，其中 **a_n = (−1)^n e_n(b)，b_j = 1/γ_j² > 0** ✓（初等对称函数 ✓）
   TPX2 实测（569 个零点，46 个系数）：a_n = 1, −2.2047e−2, +2.6161e−4, −2.2436e−6, …（**交替** ✓）
   · 正性 = e_n(b) ≥ 0 ✓ —— **对任意正数 b_j 自动成立** ✓（无信息 ✗）
   · 对数凹 r_n = a_n²/(a_{n−1}a_{n+1}) ≥ 1 ✓ —— 实测 **45 项 0 违反** ✓✓
     ⭐ 而这**正是 Newton 不等式**（正实数的初等对称函数必对数凹 ✓）⟹ **自动成立** ✗
⟹ **两条 PF-序列条件都是"对任何正 γ_j 都成立"的** ⟹ **无法区分 RH 与 RH 的失效** ✗✓
```
$$\boxed{\text{以零点实例化} \Longrightarrow \text{TP/PF 条件自动成立} \Longrightarrow \text{空洞（无鉴别力）}}$$

## §2 ⭐⭐⭐ **由此解释了文献的空白**（2020 原文："该判定尚未在 zeta 上检验"）
```
若用【零点本身】实例化 ⟹ 条件自动（§1）⟹ 空洞 ✗
若不用零点 ⟹ 就要判定 Ξ(i√s) 自身的 PF 结构 ⟹ 那**就是** RH ⟹ 循环 ✗
⟹ **"未检验"不是疏忽，而是结构性事实** ✓✓（= 本项目反复遇到的
   "RH 的正性重述在实例化时消解" 的又一例 ✓）
```

## §3 **唯一仍然可检验的形态**（下一步的真实候选）
```
【变差递减（Theorem 6）】Λ 为 PF ⟺ **卷积算子 T_Λ 不增变差**：v(T_Λ f) ≤ v(f) ✓✓
   —— 这是**泛函**条件（不是 Toeplitz 子式 ✗）⟹ 对**具体测试函数 f** 给出**具体变差数** ✓
   ⟹ **可数值检验** ✓（选 f 为有限支集阶梯/振荡函数，数卷积后的符号变化 ✓）
⚠️ 但它检验的是"Λ 的变差不增" ⟹ 与 PF 等价 ⟹ 仍与 RH 等价 ✓（等价式不可免 ✓）
   ⟹ 价值在于：**它是可算的等价陈述**（不同于空洞的 Toeplitz/系数条件 ✓）✓
```

## §4 边界
```
【ERR】§0 的 Ξ′ 数值（作废 ✗）；【核验】§1(b) 的 45 项对数凹（0 违反 ✓，与 Newton 预测一致 ✓）
   §1(a)(b) 的代数推导（a_n = (−1)^n e_n(b) ✓ 精确式 ✓）
【外部】Schoenberg/Gröchenig 定义与定理（arXiv:2007.12889 原文 ✓）
【未做】§3 的变差递减检验未跑 ✗；未输入 1/2；未构造模型；未改 L2；未声称任何证明
```
## §5 提交链
```
R6（14f1587 撤回 + 正确对象）→ 本篇（实例化坍缩 + ERR）
```
