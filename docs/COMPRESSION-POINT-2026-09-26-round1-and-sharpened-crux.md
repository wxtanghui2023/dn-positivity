已查地图：已跑 scripts/prework_map_check.sh 119 整数接口 压缩 二阶矩 平展 ⟹ 执行自 SZ-FILTER-2026-09-26 与 NEXT-2026-09-26 两档；本档为**今日主线的第 1–2 轮代数实测＋crux 等价改写**（唐先生 2026-09-26 09:30 指令）；不开旁支 ✓。
D0: 本档对象 = 既有 δ-体系的整数接口搜寻，及 `A₁+A₂≤143` 的等价形式（非新对象）
D1: 0（无新独立自由度；产出为一处消去恒等式、一次失败记录、crux 等价链）

# COMPRESSION-2026-09-26 · D-路线第 1–2 轮 ＋ crux 等价改写

## §1 第一轮：现有 δ-系统的整数接口搜寻（结果：**一处真消去，但无严格界** ✗）

### 发现 ①（真、新）：**交替层预算恒等消去** ✓

```
层预算: Σ_x δ_i(x) = C(10,i)·E ✓（我前一档已证 ✓）
$$i=0..10\text{ 交替求和}:\quad \sum_{i=0}^{10}(-1)^i\binom{10}{i}=0\ \Longrightarrow\ \boxed{\sum_{i=0}^{10}(-1)^i\sum_x\delta_i(x)\ \equiv\ 0}$$
⟹ **全局交替层和恒等为零** ✓（这就是你说的"设计一个会发生消去的对象"在现有骨架里的实现 ✓）
⚠️ 但它给出的是**恒等式**，不是**严格界** ✗：逐点 `s(x):=Σ_i(-1)^iδ_i(x)` 无固定值 ✓，
   且 `Σ_x s(x)=0` 只说明正负部平衡 ✗ ⟹ **不产生 (C) 型 `0≤D<1`** ✗
```

### 发现 ②（失败记录）：其余候选全部落空 ✗

```
· 线性重包装（Σδ, Σδ², P, T 的组合）⟹ 被筛器第 2 条排除 ✗
· `G_{ij}` 子式（整数 ✓、`≥0` ✓）⟹ 要 `<1` 须秩上界，而秩上界来自结构非容量 ✗（已证仅 `rank(G)≥2`）
· 同余＋界型 ⟹ **= van Wee/Haas/Habsieger 已有机制** ✗
⟹ **结论：现有恒等式体系里不存在 (C) 型严格界** ✗ —— 缺口仍是 (B) **独立压缩** ✓
```

## §2 ⭐ 第二轮：把 crux 改写成**可攻击形式**（本档主产）

```
从已验证链: `Σδ² + Σδ = 4(A₁+A₂)`、`Σδ = 285`、`2(A₁+A₂)=285+Q`、`Q=Σ_xC(δ(x),2)`
$$\boxed{A_1+A_2\le143\iff Q\le1\iff \sum_x b(x)^2\le 1881\iff \textbf{覆盖重数二阶矩取到整数性允许的最小值}}$$
（因 `Σ_x b² = 11M + 4(A₁+A₂) = 1309 + 4(A₁+A₂)` ✓ 精确 ✓）
```

**"最小平展 profile"（唯一）**：

```
Σb=1309、b≥1、1024 点、Σb² 最小: 285 点 b=2 + 739 点 b=1 ⟹ Σb²=**1879**（=1309+4·142.5 ✗ 非整数 ⟹ 不允许 ✓）
整数性强制（Σδ²≡3 mod 4 ✓）⟹ 真正最小 = **1881** = 283×{b=2} + 1×{b=3} + 740×{b=1} ✓
⟹ crux 的等价说法: **119-码的覆盖重数 profile 必须恰好落在"最平"的那个格点上**（= Q=1 的 profile ✓）
⟹ 通俗版: **任何 119-码的闭球里，最多只能有一个中心含 3 个码字** ✓（`b(x)≥3` 的中心至多一个 ✓）
```

## §3 位置判定：它**正落在已知墙的地址上** ✓（诚实）

```
`Σ_x b(x)²` 的上界 = **覆盖密度/二阶矩型**陈述 ⟹ 这正是 van Wee / Haas 的 excess-density 机制所在 ✓
已知强度: `94 < 103(van Wee: 2^n/n) < 107(混合码) ≪ 120` ✗
⟹ 要得 120，需要**严格强于 van Wee 的 n=10 二阶矩界** ✓（这就是墙 ✓）
⟹ ⚠️ 但 n=10 是**固定有限实例** ⟹ 可有"渐近界之上的额外整数性/同余结构" ✓（107>103 已证明该现象存在 ✓）
   ⟹ 具体问题（today 的可攻击形态）: **n=10 特有的整数性/奇偶/同余结构，能否把 `Σb²` 压到 ≤1881？** ✓
```

## §4 下一刀的具体形态（按你第五刀：转入 `A₁+A₂≤143`）

```
攻击"覆盖集中度"（= 两个 triple-center 的互斥性）:
  设 y₁≠y₂ 均满足 `b(y)≥3`。则各含一个共覆盖三元组 ⟹ `T≥2` ✓
  但 `T = Q + Σ_xC(δ(x),3)` ✓ ⟹ `T≥2` ⟺ `Q≥2` 或某点 `δ≥3` ✓ ⟹ **与 `Q≤1` 同题** ✓（自洽 ✓）
  ⟹ 需**局部＋全局**混合论证：两个 triple-center 的球结构相互"浪费"的覆盖槽 ≤ 可用余量（285 ✓）
  可执行第一步: 枚举 `d(y₁,y₂)∈{2,3,4,...}` 时两球并/交的槽位账本，看是否强制超出余量 ✓
```

## §5 边界（诚实标注）

- §1 发现①（交替层和恒等为零）为**我方推导** ✓（`Σ(-1)^iC(10,i)=0` 标准事实 ✓）；发现②为**失败记录** ✓
- §2 等价链为**精确代数** ✓（已在前档数值上自检一致 ✓：`Σb²=1881 ⟺ Q=1 ⟺ A₁+A₂=143` ✓）
- §3 的"落在已知墙地址"为**我方判断** ✓，非文献结论 ✓
- **未**排除 Q=1 ✗、**未**排除 119 ✗；**未跑程序** ✓
- 按唐先生指令：**不再横向铺新恒等式/新同余/新 census** ✓

## 【技术词回查】（定稿前逐字输出）

```
技术词 压缩点        命中文件数=0    :: 
技术词 平展度        命中文件数=0    :: 
技术词 二阶矩界     命中文件数=9    :: ./E116-bazzanella-localL2-and-crossline.md ./E51-candidates-A-B-feasibility.md ./EXPLORATION-POINTS-REGISTER.md 
技术词 交替层预算  命中文件数=0    ::
```

- **本档新增**（命中数=0）：压缩点、平展度、交替层预算
- **档案已有（引用，不列为提出）**：二阶矩界
- **通用词（不计）**：—
