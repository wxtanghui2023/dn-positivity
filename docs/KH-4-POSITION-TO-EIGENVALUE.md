已查地图：未覆盖（关键词: 位置到本征值|去参数测试|人工谱编码|自洽参数型）—— 可开档，首行须照抄本行
　（⚠️ `谱编码` 3 档＝异义 ⟹ 不主张；`d_k` **15** 档／`除数和` 6 档 ⟹ **`k` 重除数和数已有部分覆盖 ⟹ 本档仅作旁证并标注** ✓）

# **KH-4 / POSITION-TO-EIGENVALUE**：`1/2` 的"位置 → 本征值"转换（**最后一次窄口审计**）

**唐先生令（2026-09-22 22:58）**：
① 选 **(a)**，但**这是最后一次窄口审计**：**只攻** $$\boxed{\text{固定点位置 }1/2\ \longrightarrow\ \text{真正本征值 }1/2}$$ ✓
　**不再搜索新的 `1/2`，也不再扩大状态空间** ✓；理由（照录）：**KH-4 已证 `G2` 不是问题（canonical operator 已存在）**，真正缺口是**身份转换** ⟹
　**值得做一次"桥本身"的审计，但必须设很硬的止损条件**，否则极易重新制造**重命名** ✓✓
② **G1**：位置参数必须在**算子定义前**自然出现（`\rho_* = 1/2` 须由此前的**算术优化/对称性确定**，非人为参数输入）—— **KH-4 已基本通过** ✓；
③ **G2（只许测三种结构）**：**① 边界条件型** `Tv = \lambda v,\ B_{\rho_*}v = 0`（查 `\rho_*` 是否被边界条件**迫使成为 `\lambda`**）｜
　**② 尺度共轭型** `D_{\rho_*}^{-1}TD_{\rho_*}`（查 `\rho_*` 是否**改变谱**，而非只改坐标）｜
　**③ ⭐ 自洽参数型** `\lambda = \Phi(\rho_*),\ \rho_* = \Psi(\lambda)` ⟹ 闭合方程 `\lambda = \Phi(\Psi(\lambda))`
　（⭐ 照录：「**第三类尤其值得审，因为它目前唯一可能真正实现"位置变成本征值"，而不是简单读取尺度**」）✓✓；
④ **G3 ⭐ 去参数测试（本档最重要的杀手）**：把 `\rho_* = 1/2` 暂换为任意 `r`，问 `\mathrm{Spec}(T_r)` 是否真随 `r` 改变；
　若结果始终只是 `\{+1,-1\}`、或只是 `\lambda = r` 这类**显式编码** ⟹ $$\boxed{\text{SURVIVOR-5 FAIL}}$$（只是把**位置标签写到谱上** ✓）；
　真正需要：`r 改变 ⟹ T_r 的动力学改变 ⟹ \lambda(r) 非平凡改变`，且**由算术结构强制** ✓✓；
⑤ **G4 信息增益**：即使 `\lambda(1/2) = 1/2` 也**还不够**；须存在**第二个可变算术参数** `\lambda = \Lambda(\rho,\theta)`
　（`\theta` ＝ **此前不存在于 `\sqrt x` 计数中的真正算术自由度**）；否则仍只是"给同一常数换语言" ⟹
　$$\boxed{\text{位置}\to\text{本征值}\ +\ \text{新可变参数}}\ \text{才算真正过桥}$$ ✓✓
⑥ ⭐ **特别审计（令明文）**：**不要把 `\sigma_x^2 = 1` 的对合直接改造成带参数的加权对合** —— 如 `(T_rf)(t) = r f(x/t)`
　**虽立即产生 `\pm r` 的谱，但这是最典型的"人工谱编码"（`r` 被直接乘进算子）⟹ 应直接判死，不值得展开** ✓✓
　真正有价值的是：$$\boxed{\text{参数只进入几何/算术约束} \Longrightarrow \text{本征值由解方程自行产生}}$$ ✓✓
⑦ **出口严格限定（照录）**：`G2–G4 全过 ⟹ POSITION→EIGENVALUE SURVIVOR`（才继续研究能否与 β 接口）｜
　`G2 失败 ⟹ 没有找到自然转换机制，但不宣称不存在`｜**`G3/G4 失败 ⟹ 把 KH-4 精确升级为**：
　$$\boxed{\text{canonical operator 存在}\ +\ \text{组合指数存在}\ +\ \text{但位置}\to\text{谱的非重命名转换失败}}$$（**比现在的 GAP 更窄、更干净** ✓✓）

D0: 本档对象 = **"位置 `\to` 本征值"转换的窄口审计（`KH-4/POSITION-TO-EIGENVALUE`）**（档案**零覆盖** ⟹ 新档；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算（解析）／零数值／未写程序**／未做数值搜索／未证 RH／未接 β／未接 ζ ✓

---

## §1 **G1 ✅**：位置参数**在算子定义前自然出现** ✓

```
【位置来源（承前档 ✓）】 `\rho_* = 1/2` ＝ **divisor 对称不动点的指数**（`t = \sqrt x = x^{1/2}` ✓），
　由**对称优化的最小点**强制（`F(t) = t + x/t`）✓、**ζ-free** ✓✓ ⟹ **非人为输入** ✓
【旁证（⚠️ 仅作旁证，`d_k` 档案已有 15 档覆盖 ✓）】 对 `k` 重除数和数，**自然切割指数 ＝ `1/k`**（`k` 重对称的不动点）⟹
　**`1/2` 恰是 `k = 2` 的特例** ⟹ 说明位置参数**由算术结构强制**且**成族** ✓✓（但注意：这是**位置族**，非**谱族** —— 见 §4 ✓）
```

## §2 **G2**：三种"参数 → 谱"结构（**逐条判定** ✓）

| 结构 | 机制 | 判定 | 理由 |
|:--|:--|:--:|:--|
| **① 边界条件型** `Tv=\lambda v,\ B_{\rho_*}v=0` | 令 `B_{\rho_*}` 施加在**对称不动点**上（如 `v(x^{1/2}) = 0`） | ⛔ **FAIL** | **边界条件只筛选特征向量，不改变本征值** ✓；若让边界**进入算子定义**（resolvent／Green 型），本征值依赖形如 `\propto 1/L^{k}` ⟹ **显式标度** ⟹ 非导出 ✓✗ |
| **② 尺度共轭型** `D_{\rho_*}^{-1}T D_{\rho_*}` | 用 dilation 做相似变换 | ⛔ **FAIL（定义级）** | **可逆共轭 ＝ 相似变换 ⟹ 谱严格不变**（基本事实 ✓✓）；若 `D` 无界/部分（非可逆），谱只被**整体标度** ⟹ 仍是**编码** ✗ |
| **③ ⭐ 自洽参数型** `\lambda=\Phi(\rho),\ \rho=\Psi(\lambda)` | 闭合方程 `\lambda = \Phi(\Psi(\lambda))` | ⚠️ **未通过（未能构造）** | 此为**唯一可能真正实现**者 ✓；但**未能给出具体 `\Phi,\Psi`**：任何已知的"切割指数 `\leftrightarrow` 误差增长"关系均经 **`\zeta^2` 的零点** ⟹ **循环** ✗；⛔ 依令 **不宣称不存在** ✓ |

```
【G2 小结】 ①② **结构性 FAIL**（各有一句话的硬理由 ✓✓）；③ **未通过（构造缺失）** ⟹ 依令：**没有找到自然转换机制，但不宣称不存在** ✓
```

## §3 **G3 ⭐ 去参数测试**（本档最重要杀手 ✓✓）

```
【做法】 `\rho_* = 1/2 \rightsquigarrow r`，检查 `\mathrm{Spec}(T_r)` 是否**非平凡**随 `r` 变 ✓
```

| 结构 | `\mathrm{Spec}(T_r)` 随 `r` 的行为 | 判定 |
|:--|:--|:--|
| **① 边界条件型** | **与 `r` 无关**（`\{\pm1\}`）✗；若边界进入定义则 `\propto x^{-kr}` ⟹ **显式编码** ✗ | ⛔ **FAIL** |
| **② 尺度共轭型** | **谱不变**（可逆）✗；或**整体标度** ✗ | ⛔ **FAIL** |
| **③ 自洽参数型** | **`T_r` 未构造 ⟹ 测试空转** ⚠️（谈不上通过 ✓） | ⚠️ **未通过** |

```
【⭐ 人工谱编码杀手（令明文：立即判死，不值得展开 ✓✓）】
　$$(T_rf)(t) = r\,f(x/t)\ \Longrightarrow\ \mathrm{Spec}(T_r) = \{\pm r\}$$
　⟹ `r` **被直接乘进算子** ⟹ 只是把**位置标签写到谱上** ⟹ $$\boxed{\text{SURVIVOR-5 FAIL}}$$ ✓✓
【G3 小结】 无一条结构满足 `r 改变 ⟹ T_r 动力学改变 ⟹ \lambda(r) 非平凡改变（由算术强制）` ⟹ **G3 ❌ FAIL** ✓
```

## §4 **G4 ❌**：无信息增益

```
【增益检验】 需 `\lambda = \Lambda(\rho,\theta)`，`\theta` ＝ **`\sqrt x` 计数中不存在的真算术自由度** ✓
【本档结果】 自然可得的新参数只有 **`k`（重数）** ⟹ 它给的是**位置族 `\{1/k\}`**（`k = 2` ⟹ `1/2` ✓），
　**不是谱族** ⟹ 谱化对象**不比位置多携带任何信息** ⟹
　$$\boxed{\text{谱化后不比 }\sqrt x\ \text{多携带一个可变化参数}}$$ ⟹ **G4 ❌ FAIL** ✓✓
【注】 这同时说明：`1/2 = 1/k|_{k=2}` 是**位置层的族结构**，**不是**"位置 → 谱"的转换 ✓
```

## §5 ⭐ 出口（依令 ⑦：**精确升级** ✓✓）

```
【出口】 **G3／G4 失败** ＋ **G2 未通过** ⟹ 依令把 KH-4 **精确升级为**：
　$$\boxed{\text{canonical operator 存在}\ +\ \text{组合指数存在}\ +\ \text{但位置}\to\text{谱的非重命名转换失败}}$$ ✓✓
　（**比原 `GAP` 更窄、更干净** ✓✓）
【精确形态（承前档 ＋ 本档加固）】
　· **不缺算子**（前档 G2 ✅：对合／分解／递推／对数重整化皆**唯一诱导** ✓）
　· **不缺位置参数**（本档 G1 ✅：`1/2` 由除数对称强制，且成**位置族 `1/k`** ✓）
　· **缺的是**：把位置**转为本征值**的任何**非重命名**机制 ——
　　已知三类载体**逐条失败**：边界条件**不改变本征值**；共轭**不变谱**；自洽型**无法脱离 `\zeta` 构造** ✓✓✓
【⛔ 止损已守（依令 ✓）】 未搜新 `1/2`；未扩大状态空间；未采用 Mellin；未证 RH；未接 β／ζ ✓；**不宣称不存在** ✓
【状态】 `KH-4/POSITION-TO-EIGENVALUE` = **窄口 FAIL（升级版收口）** ✓；卡点＝**"位置 → 本征值"无非重命名机制** ✓
```

## §6 边界与回查（✗✓）

```
✗ 零计算（解析）／零数值／未写程序／未做数值搜索／未开案／未证 RH／未接 β／未接 ζ ✓
✗ ⛔ 不把"三结构皆失败"记为战绩；⛔ 不宣称"不存在"（G2 未通过 ⟹ 依令只述"未找到" ✓）
⚠️ "可逆共轭谱不变"为**基本事实** ✓；`k` 重除数和数**档案已有 15 档 ⟹ 旁证并标注** ✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 位置到本征值 命中文件数=0 ⟹ **本档新增** ✓｜去参数测试=0 ⟹ **新增** ✓｜人工谱编码=0 ⟹ **新增** ✓｜自洽参数型=0 ⟹ **新增** ✓
　技术词 谱编码 命中文件数=3 ⟹ **档案已有（异义）⟹ 不主张** ✓
【地图回查】 关键词 4 项 = **未覆盖** ⟹ 可开档 ✓；档案覆盖：`除数和` 6／`d_k` 15／`超双曲线` 1 ✓
【档级引用】 `KH-4/EXPONENT-TO-SPECTRUM`（G2 ✅ canonical 算子存在；`1/2` ＝不动点指数）✓｜`C90-DEC-1`（桥接审计先例，判定 `(III)`）✓｜
　`ASSETS-REGISTRY` `F-2`（素数侧 Mellin β-提取：**读 β** 已成，**导 `1/2`** 未成）✓｜`P-3`／`P3-B2`（指数来源单点）✓

---

## §7 **终局归档**（唐先生 2026-09-22 23:01 确认 ✓✓）

```
【状态（照录）】
　$$\boxed{\texttt{KH-4} = \textbf{GAP} \ \to\ \textbf{精确结构性失败（窄口径）}}$$ ✓✓
　⛔ **并明确**：这是「**窄口径 GAP／当前机制未找到**」归档，**不是数学不存在性结论** ✓✓
【缺口终局形式（照录框图）】
　$$\boxed{
　\begin{array}{c}
　\text{divisor symmetry}\\
　\Downarrow\\
　\text{forced optimal position }x^{1/2}\\
　\Downarrow\quad\text{\bf GAP}\\
　\text{canonical non-relabeling eigenvalue }1/2
　\end{array}}$$ ✓✓
【⭐ 为何这比"没有 `1/2` 来源"准确得多（照录四条）】 ✓✓
　① **`1/2` 有来源** ✓；② **canonical operator 有** ✓；③ 缺的是二者之间的**非重命名谱转换** ✓；
　④ 已测试的**三种自然转换机制均未提供**该转换 ⟹ **不能把组合切割指数升级为谱临界指数** ✓
【三条转换机制的审计含义（照录表）】
　| 机制 | 结果 | **真正死因** |
　|:--|:--:|:--|
　| ① 边界条件型 | FAIL | 边界**可以编码位置参数**，但**未形成"位置参数被迫成为谱值"的独立机制** ✓ |
　| ② 尺度共轭型 | FAIL | **共轭保持谱不变**，至多改变**坐标表示** ✓ |
　| ③ 自洽参数型 | FAIL | **未得到独立的非平凡闭合方程**，把 `\rho_* = 1/2` 强制成新的本征值 ✓ |
　（另：`(T_rf)(t) = r f(x/t)` ⟹ `\mathrm{Spec}(T_r) = \{\pm r\}` ＝ **把答案乘进算子** ⟹ **人工谱编码，不计入 survivor** ✓✓）
【`d_k` 旁证保持其层级（照录）】 它说明 `1/2` 属于**更广泛的自然切割指数族**，但**没有解决 position→eigenvalue** ⟹
　⛔ **不能把它升级为新桥** ✓✓
【⛔ 止损（依令）】 **不再攻第 4／第 5 种 position→eigenvalue**（极易重回**为目标谱值设计算子**的循环 ✓✓）
```

## §8 **策略回归**（依令 ✓✓）

```
【大策略（照录）】 $$\boxed{\text{independent problem} > \text{adjacent asset} > \text{RH relevance}}$$ ✓✓
【下一主线约束（照录）】 **不再围绕 `1/2` 本身搜索**；若继续 KH 系列，下一候选应寻找
　**本身有独立数学问题、有自己的非平凡可变参数**的算术对象；**RH 只作后置接口**，**不再从 `1/2` 倒推机制** ✓✓
【本档状态】 `KH-4/POSITION-TO-EIGENVALUE` ＝ **窄口径 GAP（已归档）** ✓；`KH-4` 整线至此收口 ✓
【登记】 `ASSETS-REGISTRY` **D-14**（三载体已封 ＋ 总体窄口径 GAP；**明标非存在性 ✗**）✓
```
