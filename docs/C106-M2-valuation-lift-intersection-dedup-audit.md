已查地图（所查：`CLOSED-ROUTES-MAP.md:1089`（**半环／运算结构三最小模型实算**）、`V250`（Connes–Consani F1／**tropical·Gauss 赋值**）、`FREEZE-AUDIT-FZ1-FZ4.md`（**`FZ-2` 原语表：free probability／microstates／非交换熵＝✗ 唯一实质未覆盖**）、`V326-E3C-natural-noncommuting-square-audit.md`（**自然非交换方块八族 ＋ `C1`／`C2`／`C3` 判死树**）、`V208`／`V211`（非交换极限类）、`C-104`／`C-105`）。**结论**：对 **`M2`（对角→非交换插值）＋ `K1`（`valuation-lift`）交叉点**作去重审计 ⟹ **`K1` 的内容（改变运算结构）已被 `CLOSED-ROUTES-MAP:1089` 的"半环三最小模型"实测判死**（`KILL-1` 有限 ＋ **`KILL-2` 素数直积** ＋ 设计 3 表示刚性）✓✓；**`M2` 的"非交换"落 `V326-E3C` 的 `C1`（有限局部）／`C2`（传播序）两个 DEAD 类，`C3`（全局层不交换）八族无一实例** ✓✓；⟹ **交叉点不构成新开口**；**唯一档案自陈的未测项＝`FZ-2` 的 free probability／非交换熵 原语**（原文自述"**须实测才可判死**"）⟹ 本档给出其**靶向测试预注册** ✓✓

# C-106 · **`M2` ＋ `valuation-lift` 交叉点去重审计**

> **时间**：2026-09-18 15:48 唐先生：`M1` 已被实际试刀，**不能再作为当前主突破线**；**不继续扩张 `M1` 六族**；下一刀＝对 **`M2`（diagonal → noncommutative interpolation）＋ `valuation-lift`** 的**交叉点做去重审计** ✓

---

## §0 结论（先行）

$$\textbf{(1)}\ \text{`K1`（valuation lift）}：\text{其内容＝}\textbf{改变运算结构} \Longrightarrow \textbf{已被实测判死}✓✓$$
$$\qquad \text{依据}：\text{`CLOSED-ROUTES-MAP:1089`}\ \text{三最小模型}：\text{设计 2（加法＋乘法精确规则）}\Longrightarrow \Sigma\ \text{得}\ \textbf{交换半环商} \Longrightarrow \text{有限商分类} \Longrightarrow \mathbb Z/M\cong\prod_p\mathbb Z/p^{a_p}✓$$
$$\qquad \qquad \Longrightarrow \text{KILL-1（有限）}＋\text{KILL-2（素数直积）}\ \text{双杀};\quad \text{设计 3（无限字母表＋精确局部规则）}\Longrightarrow \text{表示刚性（仅}\ c(n)=\lambda n\text{）}\Longrightarrow \mathrm{DEAD}$$
$$\qquad \text{且}\ \text{tropical／Gauss 赋值}\ \text{＝}\text{`V250`}\ \text{F1 缩放 site（已登记）}✓$$
$$\textbf{(2)}\ \text{`M2`（非交换）}：\text{落}\ \text{`V326-E3C`}\ \text{判死树}：\text{`C1`（有限局部不交换）}\mathrm{DEAD};\ \text{`C2`（传播序／path dependence，}\|\Delta_T\|\le C\cdot\text{bandwidth}(T)\text{）}\mathrm{DEAD}✓✓$$
$$\qquad \text{`C3`（}\textbf{全局对象层不交换}）＝\text{唯一值得留}，\ \text{而}\ \textbf{八族候选无一}\ \text{`C3`}\ \text{实例}✓✓$$
$$\textbf{(3)}\ ⭐\ \textbf{交叉点}\ \text{`M2`}\cap\text{`K1`}：\textbf{未作为整体登记}，\ \text{但两半}\ \textbf{各有落点} \Longrightarrow \textbf{不构成新开口}✓$$
$$\textbf{(4)}\ ⭐\ \textbf{唯一真开放项}＝\text{`FZ-2`}\ \text{自陈未测项}：\text{free probability／microstates／}\textbf{非交换熵}✓✓$$
$$\qquad \text{原文逐字}：\text{`| free probability / microstates / 非交换熵 | ✗（唯一实质未覆盖）| 提供"非交换变量上的正性 + 变分优化"——组合形态与 M3 不同 |`}✓$$
$$\qquad \qquad \text{评估逐字}：\text{"与已审的 Connes 吸收谱 ＋ 随机矩阵统计相邻} \Longrightarrow \text{预判被同一吸收机制吞掉}（\boxed{\text{须实测才可判死}}\text{，但不足以直接开放 Discovery）"}✓✓$$

---

## §1 `K1`（`valuation-lift`）的覆盖证据

$$\text{唐先生的严格问法}：\text{是否存在一种}\ \textbf{arithmetic valuation}，\ \text{能}\ \textbf{改变运算结构} \text{而}\ \textbf{不只是重新编码} \text{已有对象？}✓$$
$$\text{档案答复（逐字，`CLOSED-ROUTES-MAP:1089`）}：$$
$$\qquad \text{**(2.1) 设计 1**（可除边}\ c(kp)=F_p(c(k))\text{）}：\text{一致性} \Longrightarrow F_pF_q=F_qF_p \Longrightarrow c(n)\ \text{由}\ c(1)\ \text{唯一决定} \Longrightarrow \mathcal X_N\cong\Sigma\（N\ \text{无关}）✓$$
$$\qquad \qquad \Longrightarrow \text{规则逐个素数作用＝}\textbf{Euler 局部} \Longrightarrow \text{`KILL-2`};\ \text{且}\ \text{整个传播系统退化为单个标签空间，除}\ c(1)\ \text{外无算术内容}✓$$
$$\qquad \text{**(2.2) 设计 2**（加法＋乘法精确规则）}：\Sigma\ \text{得}\ \textbf{交换半环商} \Longrightarrow \text{有限商分类}\ \Longrightarrow \mathbb Z/M\cong\prod_p\mathbb Z/p^{a_p}\（\text{CRT}） \Longrightarrow \boxed{\text{`KILL-1`＋`KILL-2` 双杀}}✓✓$$
$$\qquad \text{**(2.3) 设计 3**（无限字母表＋精确算术局部规则）}：\text{经典刚性} \Longrightarrow \text{解只有}\ c(n)=\lambda n \Longrightarrow \textbf{表示刚性} \Longrightarrow \mathrm{DEAD}✓✓$$
$$\Longrightarrow\ \textbf{判定}：\text{`K1` 的}\ \textbf{"改变运算结构"}\ \text{这一具体形式}\ \textbf{已被实测};\ \text{其}\ \textbf{杀手正是}\ \text{`KILL-2`（素数直积）} \Longrightarrow \text{运算结构}\ \textbf{在素数上因子化}，\ \text{故}\ \textbf{造不出全局刚性}✓✓$$
$$\qquad ⚠️\ \text{且}\ \text{tropical／Gauss 赋值}\ \text{已在}\ \text{`V250`}\ \text{（F1 缩放 site：}\tau(f)=-v_\bullet(f)\text{）登记} \Longrightarrow \text{无新增入口}✓$$
$$\qquad \Longrightarrow\ \text{`K1`}\ \textbf{维持"已覆盖"}（\text{与}\ \text{`C-104`}\ \text{一致}）✓$$

## §2 `M2`（对角→非交换插值）的覆盖状态

$$\text{`V326-E3C` 纪律（逐字）}：\text{先建自然方块的完备候选族} \to \text{逐个归约};\ \text{不先找"最复杂的不交换"};\ \textbf{不预判 DEAD}✓$$
$$\qquad \text{`C1`}\ \textbf{有限局部不交换}\（\Delta_T=\Delta_{p,q}\ \text{只依赖有限阶局部数据}）\Longrightarrow \text{`V294-A`}\ \mathrm{DEAD}✓$$
$$\qquad \text{`C2`}\ \textbf{传播顺序型}\（\text{仅因信息路径不同} \Longrightarrow \text{path dependence};\ \|\Delta_T\|\le C\cdot\text{bandwidth}(T)\text{）}\Longrightarrow \text{`V162`／`V295`}\ \mathrm{DEAD}✓$$
$$\qquad \text{`C3`}\ \textbf{唯一值得留}：\text{两条自然路径各自合法}，\ \text{方块在}\ \textbf{全局对象层面} \text{不可交换}✓$$
$$\qquad \qquad \text{但}：\text{第一轮八族候选（含 `SQ4` 迹式方块＝Selberg／Guinand／Deninger 算术 site）}\ \textbf{全数归约}，\ \textbf{`C3` 无实例}✓✓$$
$$\Longrightarrow\ \text{`M2` 的"非交换"若只是}\ \textbf{技术产生的模型} \Longrightarrow \text{其方块必落}\ \text{`C1`／`C2`}\（\text{皆 DEAD}）;\ \text{唯若落在}\ \text{`C3`} \text{（全局层）才有意义}，\ \text{而}\ \text{`C3`}\ \textbf{无实例}✓✓$$
$$\qquad ⚠️\ \text{故}\ \text{`M2`}\ \textbf{不是独立新机制}：\text{它是一条}\ \textbf{生产技术}（\text{对角模型} \to \text{非交换模型，经 Poincaré 不等式}），\ \text{其}\ \textbf{落点由}\ \text{`V326-E3C`}\ \text{的三分类决定}✓$$

## §3 交叉点判定（`M2` ∩ `K1`）

| 半 | 落点 | 状态 |
|:--|:--|:--|
| `K1` 改变运算结构 | `CLOSED-ROUTES-MAP:1089` 半环三最小模型（`KILL-1`＋`KILL-2`＋表示刚性）| **已判死** |
| `M2` 非交换模型 | `V326-E3C`：`C1`／`C2` DEAD；`C3` 无实例 | **已归约** |
| 交集（非交换的运算结构提升）| **未整体登记**，但两半各有杀点 | **不构成新开口** |

$$\Longrightarrow\ \text{交集若要活，必须}\ \textbf{同时} \text{避开}：\text{(i) 素数直积因子化};\ \text{(ii) 有限局部}\ \text{`C1`};\ \text{(iii) 传播序}\ \text{`C2`} \Longrightarrow \text{即须落}\ \text{`C3`}\（\textbf{全局层不交换且无路径依赖}）✓✓$$
$$\qquad ⚠️\ \text{而}\ \text{`C3`}\ \text{在八族中}\ \textbf{无一实例} \Longrightarrow \text{交集目前}\ \textbf{空}✓$$

## §4 ⭐ 唯一真开放项与靶向测试预注册

$$\text{`FZ-2` 自陈}：\text{free probability／microstates／非交换熵}＝\textbf{✗ 唯一实质未覆盖};\ \text{且}\ \textbf{"须实测才可判死"}✓✓$$
$$\qquad \text{即}：\text{档案}\ \textbf{自己留了一个未做的测试} \Longrightarrow \text{这是}\ \text{`M2`}\ \text{邻域内}\ \textbf{唯一被授权的开放动作}✓$$
$$\textbf{靶向测试（本档预注册，三条判据）}：$$
$$\qquad \text{(T1)}\ \textbf{`β` 响应}：\text{该非交换构造能否给出}\ \beta\ne\tfrac12\Longrightarrow\text{可计算 defect}？\（\text{不能} \Longrightarrow \text{落}\ \text{`FZ-3`}\ \text{①}）✓$$
$$\qquad \text{(T2)}\ \textbf{非编码性}：\text{该构造是否}\ \textbf{独立于}\ \zeta/L/\Lambda/\mu\ \text{的截断与变换}？\（\text{依赖} \Longrightarrow \text{污染测试失败}）✓$$
$$\qquad \text{(T3)}\ \textbf{非吸收性}：\text{是否真的}\ \textbf{不被} \text{"Connes 吸收谱 ＋ 随机矩阵统计"}\ \text{吞掉}？\（\text{`FZ-2` 的预判点}）✓$$
$$\qquad \Longrightarrow\ \text{三条全过} \Longrightarrow \text{才可进入}\ \text{`FZ-3`}\ \text{深审};\ \text{任一失败} \Longrightarrow \text{与}\ \text{`FZ-2`}\ \text{预判一致，落旧类}✓✓$$
$$\qquad ⚠️\ \text{且仍须过}\ \text{`N46`}（\text{null 可复现} \Longrightarrow \text{无信号资格}）\ \text{与六筛子}✓$$

## §5 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-18 15:5x）`[纪律]`（先跑后写）

```
技术词 M2-K1 交叉点   命中文件数=1  :: ./C106-M2-valuation-lift-intersection-dedup-audit.md
技术词 靶向测试      命中文件数=1  :: ./C106-M2-valuation-lift-intersection-dedup-audit.md
技术词 非吸收性      命中文件数=1  :: ./C106-M2-valuation-lift-intersection-dedup-audit.md
```
**读数（按实测）**：三项均＝**1 档（仅本档）⟹ 本档新增措辞** ✓

## §6 边界

- `[档案]` §1 的 `:1089` 三最小模型、§2 的 `V326-E3C` `C1`／`C2`／`C3` 与八族、§4 的 `FZ-2` 两处**均逐字引用** ✓
- `[本档]` §3 的交叉点表、§4 的 `T1–T3` 靶向测试**为本档预注册** ✓
- **不声称**：`M2` 或 free probability 路线原则上不可能 ✗；**不声称** `C3` 不存在 ✗（仅"八族无实例"）；不证 RH ✗
- **纪律**：先查后判（R-1 ✓，**先跑后写** ✓）；**未用 RH 作推导** ✓；**未引入 Gaussian／随机矩阵／Gibbs／determinant 作为候选** ✓

```
⚠️ 唐先生 15:48：M1 已被实际试刀 ⟹ 不能作为当前主突破线; 不继续扩张 M1 六族; 下一刀＝M2 + valuation-lift 交叉点去重审计
✅ K1（valuation lift）: 其内容"改变运算结构"**已被实测判死** —— CLOSED-ROUTES-MAP:1089 半环三最小模型:
   设计 1（可除边）⟹ F_pF_q=F_qF_p ⟹ 规则逐个素数作用=Euler 局部 ⟹ KILL-2;
   设计 2（加法+乘法精确规则）⟹ Σ 得交换半环商 ⟹ 有限商 ⟹ Z/M ≅ ∏_p Z/p^{a_p}(CRT) ⟹ **KILL-1 + KILL-2 双杀**;
   设计 3（无限字母表+精确局部规则）⟹ 经典刚性 ⟹ 只有 c(n)=λn ⟹ **表示刚性 ⟹ DEAD**
   ⟹ 杀手正是 KILL-2（**素数直积**）: 运算结构在素数上因子化 ⟹ 造不出全局刚性; 且 tropical/Gauss 赋值已在 V250 登记 ⟹ 无新入口
✅ M2（非交换）: 落 V326-E3C 判死树 —— C1 有限局部不交换 ⟹ V294-A DEAD; C2 传播序(path dependence, ‖Δ_T‖≤C·bandwidth) ⟹ V162/V295 DEAD;
   C3(全局对象层不交换)=唯一值得留, 但**八族候选无一 C3 实例**(含 SQ4 迹式方块=Selberg/Guinand/Deninger 算术 site ⟹ 旧类)
   ⟹ M2 是"生产技术"(对角模型→非交换模型, 经 Poincaré 不等式), 其落点由 V326-E3C 三分类决定
✅ 交叉点 M2 ∩ K1: 未作为整体登记, 但两半各有落点 ⟹ **不构成新开口**; 交集若要活须同时避开 KILL-2/素数直积、C1 有限局部、C2 传播序 ⟹ 即须落 C3, 而 C3 八族无实例 ⟹ 交集目前**空**
⭐ 唯一真开放项: FZ-2 自陈未测项 —— 「free probability/microstates/非交换熵 | ✗（唯一实质未覆盖）」+ 「与已审的 Connes 吸收谱+随机矩阵统计相邻 ⟹ 预判被同一吸收机制吞掉（**须实测才可判死**）」
   ⟹ 这是 M2 邻域内唯一被授权的开放动作
✅ 靶向测试预注册(三条判据): T1 β 响应(能否给可计算 defect); T2 非编码性(是否独立于 ζ/L/Λ/μ 的截断与变换);
   T3 非吸收性(是否真不被"Connes 吸收谱+随机矩阵统计"吞掉); 三条全过才进 FZ-3 深审; 且仍须过 N46 与六筛子
✅ C-105 §8 已补: A6 作为独立资产保存 + 三个保留判词(N46/M1 去重; Gate E 真绿灯; M1 DEAD≠不可能) + 六条重开门槛(缺第一项不再开工)
✅ 净产出: ①K1 覆盖证据（半环三最小模型 + KILL-2 素数直积）✓ ②M2 归约证据（C1/C2 DEAD, C3 无实例）✓ ③交叉点判空 ✓ ④FZ-2 未测项定位 + T1–T3 靶向测试预注册 ✓
```
