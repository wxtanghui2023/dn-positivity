已查地图：本档属**独立问题线**（`IP-5`／Card 3 `RUN`），不涉 RH/ZF 地图条目 ⟹ 按唐先生 2026-09-23 15:12 的 `GO` 执行 ✓
D0: 本档对象 = `D(-3)`-四元组有界精确搜索的 `RUN` 结果（预注册域 `d\le200`）
D1: 0 （`[RESEARCH]` 轮次；结论为**预注册域内有限负结果**，不主张新自由度）
FREEZE-ACK: D1=0
[RESEARCH]

# **`IP-5`：`RUN` 结果 —— `D(-3)`-四元组有界精确搜索（域 `1\le a<b<c<d\le200`）**

**唐先生授权（15:12 ✓✓）**：$$\boxed{A=200\ \text{采纳为\textbf{预注册固定实验截断}（非文献界）};\qquad GO:\ \text{授权 }RUN}$$ ✓；
　⭐ **措辞修正（已落实）**：`A=200` **不是**"当前文献明确允许我们处理的固定有限界"，而是**预注册的、结果无关的固定实验截断**；其选择仅作本轮独立问题的**有限证书搜索域**，**不赋予数学上的普适意义** ✓✓
　⭐ **仅两个出口**：**`EXIT-POSITIVE`**（给出证书）｜**`EXIT-NEGATIVE-FINITE`**（域内穷尽：`NOT FOUND for d\le200`，**不得升级、不得扩到 201/1000/10^6、不得包装成不存在性定理**）✓✓

---

## §1 **原始输出（逐字 ✓✓）**

```
=== RUN: D(-3)-quadruple 有界精确搜索 (1 <= a < b < c < d <= 200) ===
[1] D(-3)-pairs (edges) within domain: 148
[2] D(-3)-triples within domain: 76
    first triples: [(1,3,4), (1,3,28), (1,4,7), (1,7,12), (1,7,52), (1,12,19), (1,19,28), (1,28,39), (1,39,52), (1,52,67), (1,67,84), (1,84,103)]
[3] D(-3)-quadruples within domain: 0
[4] verified quadruples (all six squares exact): 0
RESULT: EXIT-NEGATIVE-FINITE
```

## §2 **完整性与精确性说明（可审计 ✓✓）**

```
【枚举完整（无抽样）】 先枚举**全部** `D(-3)`-对（`148` 条边）；再枚举**全部**三元组（`76` 个，＝三角形）；再对每个三元组取**公共邻域**中 `d>c` 者 ⟹ **穷尽全部四团** ⟹ 结论**非抽样** ✓✓
【精确性】 全部判定用 `\lfloor\sqrt{\cdot}\rfloor^2` **整数比较**（⛔ 无浮点）✓；命中者亦要求**六个平方式逐项整数验证** ✓
【域内结构健康】 域内确有 `148` 对、`76` 个三元组 ⟹ 域**非退化**（不是"无结构"造成的空结果）⟹ 负结果**有信息量** ✓✓
```

## §3 **结论（严格按预注册措辞 ✓✓）**

```
【判定】 $$\boxed{EXIT-NEGATIVE-FINITE}$$ ✓
【允许的陈述（唯一）】 $$\boxed{\text{在 }1\le a<b<c<d\le200\ \text{域内不存在 }D(-3)\text{-四元组（严格有限域负结果）}}$$ ✓✓
【⛔ 禁止的陈述】 $$\nexists\ D(-3)\text{-quadruple}$$（**全局不存在性**）✗｜⛔ 扩域到 `201`/`1000`/`10^6` ✗｜⛔ 把有限负结果包装成不存在性定理 ✗ ✓✓
【与世界知识的一致性（⚠️ 档级）】 与"整数版 `D(-3)` 四元组**尚无人知道**"一致（`Filipin–Jurasić` 2022 标题即以**存在性**为问题；`Dujella` 记「no Diophantine quadruple with property `D(-3)` is known」）⟹ 本 `RUN` **未与文献冲突** ✓；
　⚠️ 另注：文献中确有 `D(-3)`-四元组出现在**其它环**（如 `\mathbb Z[\sqrt{-2}]`），⛔ **不可**据此倒推整数版尺度 ✓
```

## §4 **结果的价值与不价值（诚实标注 ✓）**

```
【有】 ① 一条**严格、可机检**的有限域负记录：$$\text{no }D(-3)\text{-quadruple with }d\le200$$ ✓；② 域内 `148` 对 / `76` 三元组的**完整结构数据**（可作为后续 `C3-A/B` 的素材）✓
【不有】 ⛔ 未产生**新定理**（正方向未命中 ⟹ 无证书 ⟹ 无存在性结果）✗；⛔ 未推进 `Problem 5.1` 的解答 ✗（诚实）✓
【⛔ 未做】 未升级为不存在性／未扩域／未改预注册／未碰 Card 2/4／未连 ZF/RH ✓
```


---

## §5 ⭐⭐ **收口（唐先生 2026-09-23 15:14 裁定 ✓✓：裁（丙）`HOLD`）**

```
【裁定（照录 ✓✓）】 **裁（丙）`HOLD`** ✓；理由**不是"本轮没找到"就保守**，而是 **`E-47` 的独立问题纪律在这一轮已得到一次完整验证**：
　$$\text{独立母题}\to\text{预注册}\to\text{可审计计算}\to\text{严格有限域结论}\to\text{不因负结果扩域}$$ ✓✓
　⟹ 继续在 Card 3 内部优化，**很容易重新滑回"由缺口制造下一刀"** ✓
【正式状态表（照录 ✓✓）】 $$R1\ \text{LOCKED};\ n_0=-3\ \text{LOCKED};\ A=200\ \text{LOCKED（实验截断）};\ D(-3),d\le200\ \textbf{严格排除};\ 148\ \text{对（完整枚举）};\ 76\ \text{三元组（完整枚举）};\ \text{四元组}=0;$$
　$$\text{全局 }D(-3)\ \text{不存在}\Rightarrow\textbf{不得声称};\ C3\text{-}A\ \text{负方向}\Rightarrow\textbf{不启动};\ \text{再扩大 }A\Rightarrow\textbf{禁止};\ \text{换 }n_0\ \text{重跑}\Rightarrow\textbf{暂不启动};\ Card\ 3\Rightarrow\textbf{HOLD};\ \text{独立母题搜索}\Rightarrow\textbf{重新开放}$$ ✓
【⛔ 不选（甲）（照录）】 换 `n_0` 或扩大 `A` 本质仍是 $$\text{同一 SOURCE}\to\text{同一 OBJECT：}D(n)\text{-quadruple}\to\text{同一有限证书搜索}$$ ⟹ **未改变 discovery channel、未产生新数学对象** ⟹ `A:200\to1000` 或 `n:-3\to5` **只能说明同一实验族的另一参数点，不能升级成 `D_{\rm new}`** ✓✓
【⛔ 不选（乙）（照录）】 `C3-A` **更危险**：已知 `d\le200` 内无四元组后立即设计"为什么没有"的机制 ⟹ 正是 $$\boxed{\text{有限负结果}\to\text{寻找解释}}$$（`E-47` 要避免的**逆向流程**）✗；且负方向与 `n\equiv2\pmod4`/`\bmod4,8` 已有局部障碍**碰撞风险明显** ⟹ 极易"**先看到结果，再选择能解释结果的机制**" ✓✓
【⭐ 本轮真正值得保留的是数据资产（照录 ✓✓）】 148 边、76 三元组**不是无价值的 `NOT FOUND`**：它们构成**完整有限图** $$G_{200}=(V,E),\ V=\{1,\dots,200\},\ (a,b)\in E\iff ab-3\in\mathbb Z_{\ge0}^2$$；
　**四元组恰好对应 `G_{200}` 中的 `K_4`** ⟹ $$\boxed{|E|=148,\qquad \#K_3=76,\qquad \#K_4=0}$$ ✓
【⛔ 处理纪律（照录 ✓✓）】 **暂时不要从这个图反向发明图论问题**（否则又变成"**已有数据 → 人造新母题**"）✗；
　✅ 正确处理：**完整归档，等待外部独立问题出现**；若日后**某个独立理论自然要求**研究此类 `D(-3)` 图结构，**再回来取用** ✓✓
【⭐ 正式落档句（照录 ✓✓）】 $$\boxed{\text{Card 3 HOLD；本轮数据归档；不重跑、不扩域、不设计 }C3\text{-}A\text{；回到 }E\text{-}47\text{ 等待独立母题}}$$ ✓✓
【⛔ 下一刀来源（照录 ✓✓）】 **不建议再列一批"候选独立问题"**；**下一刀应来自一个真正独立的数学来源，而不是我们为了继续计算而主动制造** ✓✓
【归档】 完整图数据：`out/ip5_dminus3_graph_data.txt`（`|E|=148` 全表 ＋ `76` 三元组全表 ＋ `#K_4=0`）✓
```
