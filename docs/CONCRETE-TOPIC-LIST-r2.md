已查地图：命中（`TOPIC-INVENTORY-MASTER-v1`／`TOPIC-SEARCH-R1-candidates-seven-field`）⟹ 引用，不开新案
D0: 本档对象 = **具体课题清单（带参数/缺口/资产/规模）** —— 取代先前"规划型"清单；每项标**验证等级**
D1: 0 （[REVIEW] 轮次：清单，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **具体课题清单 R2**

> **每项给出**：`编号｜具体问题（含参数）｜已知｜缺口｜资产｜规模｜验证等级`。**验证等级**：`逐字`（检索片段逐字）／`档级`（我的领域知识，**未核**）。

## §1 ⭐ 具体条目

```
**【T-1】`z_L(5,5)` 精确值**（limited augmented Zarankiewicz，`5\times5`）
　已知：`z_L` 在 `6\times4`、`5\times3`、`5\times4` 有精确值；`5\times5` **下界 `15`** ｜ 缺口：**精确值**（`15`？或 `\ge16`）｜ 资产：`D✓✓ A G`
　规模：**有限**（作者自述需对 extremal `C_4`-free `5\times5` 图的 2-edges 三/四元组穷举）｜ 等级：**逐字**（`MDPI Symmetry 18(7) 1076`）
　**状态：活跃（首攻）** ✓✓

**【T-2】`AG(7,3)` 最大 cap 的大小**（`n\ge7`）
　已知：`AG(6,3)` 最大 cap `=112`，且**仿射等价唯一**（Potechin 2008）；`AG(n,3)` 上限约 `2.756^n`（Ellenberg–Gijswijt）｜ 缺口：**`n\ge7` 的最大 cap 大小未知**
　资产：`A D`｜ 规模：`AG(7,3)` 有 `3^7=2187` 点，**极大**（须先评估可压缩性）｜ 等级：**逐字**（"It is still not known how large maximal caps are in dimensions larger than 6"）
　**状态：候选（规模待估）** ⚠️

**【T-3】`t(n)` = `AG(n,3)` 中最小 **complete cap** 的大小**
　已知：`\sqrt2\,3^{n/2}\le t(n)\le 2^n`；Csajbók–Nagy 2025 `Problem 5.1` 明确列为 open ｜ **但**：**Grace–Voloch** 已给出 `O(3^{n/2})` 构造，"**solving the problem**"｜
　**状态：疑似已被解决 ⟹ 暂**（须核是否完全闭合）⚠️

**【T-4】SNIEP（对称非负逆特征值问题）`n=5` 的可实现谱刻画**
　已知：`n\le4` 完全解决；`n\ge5` **未解决**；`5\times5` 有部分结果（Loewy–McDonald 等给出新可实现列表）｜ 缺口：**`n=5` 的完整刻画**
　资产：`E✓✓ D`（trace／rank／inertia／符号推导）｜ 规模：谱参数空间**连续**⟹ 须先切成**有限具体列表**（如小整数谱）才可认证 ｜ 等级：**逐字**（"remains unsolved for `n\ge5`"）
　**状态：候选（须先离散化）** ⚠️

**【T-5】图的 minimum rank / **inertia set**：小阶完备表**
　已知：min-rank 问题对多类图族有结果；**inverse inertia 问题对"阶数不超过某值"的图已解决**（检索片段在此处截断，**具体阈值须核**）｜ 缺口：**下一阶数的完备表**（如 `n=8`）
　资产：`E✓✓ D✓✓`（rank + inertia + 精确证书；**每个图需"矩阵证书 + 秩下界证明"双证**）｜ 规模：`n=8` 图数约 `1.2\times10^4` ⟹ **可能可行** ✓✓ ｜ 等级：**逐字（截断）＋档级**
　**状态：候选（规模最友好）** ✓⚠️

**【T-6】covering code `K_q(n,R)` 的具体格子**
　已知：2026-08 `arXiv:2608.19872` 改进 83 个界（含 `q\ge5` 自 2011 以来首次上界改进）；`K_6(8,4)\le166`（原 `216`）；`441\le K_6(10,4)\le2751`｜ 缺口：**逐格**须核（旧表缝隙多已被填）｜ 资产：`A D` ｜ 等级：**逐字**
　**状态：候选（须逐格筛）** ⚠️

**【T-7】`PG(2,q)` 的 minimal `1`-saturating sets（具体 `q`）**
　已知：`q\le16` 有表；大 `q` 只有界｜ 缺口：**具体 `q` 的精确最小尺寸**｜ 资产：`A✓ D✓`（有限几何 + 精确枚举/证书）｜ 规模：`PG(2,q)` 点数 `q^2+q+1`，小 `q` 可行 ✓ ｜ 等级：**档级**（表存在性须核）
　**状态：候选** ⚠️

**【T-8】`\pm`-rank of `(0,\pm1)`-matrices（小阶/特定类）**
　已知：`ILAS 2026` 有报告建立 `\pm`-rank 与 binary/term/实秩的不等式｜ 缺口：**具体阶数或类（如 ASM）上的精确值/界** —— **未锁定** ⚠️ ｜ 资产：`E D`
　**状态：暂缓（缺口未锁定）** ⚠️

**【T-9】Zarankiewicz `z(m,n;2,2)` frontier**（`m,n\ge13`）
　已知：**2026 `arXiv:2605.01120`（LLM 进化搜索）已系统收割"差一条边"格** ｜ **状态：REJECT（他方机制更强）** ✓

**【T-10】`R(5,5)`（`43\le R\le46`）／`ES(7)`（`33\le ES(7)\le81`）／Hadamard `668`**
　**状态：REJECT（计算密集 AI 队已收割）** ✓
```

## §2 优先级（按"资产对口 × 规模可控"）

```
$$\boxed{T\text{-}5\ }(\text{图 rank/inertia 小阶完备表};\ \text{规模最友好},\ E+D\ \text{双对口})\ >\ \boxed{T\text{-}1\ }(z_L(5,5);\ D\ \text{对口},\ \text{有限})\ >\ T\text{-}7\ >\ T\text{-}6\ >\ T\text{-}4\ >\ T\text{-}2\ >\ T\text{-}8$$ ✓
$$\textbf{暂/REJECT}:\ T\text{-}3\ (\text{疑已解}),\ T\text{-}9,\ T\text{-}10\ (\text{已收割})$$ ✓
```

## §3 下一动作（单点）

```
**(1)** 核 `T-5`：inverse inertia 问题**具体已解决的阶数阈值** ＋ `n` 阶图数规模 ⟹ 若 `n=8` 未解决且图数 `\sim1.2\times10^4`，则 **`T-5` 升为首攻** ✓✓
**(2)** 核 `T-1`：是否仍 open ＋ `z_L` 定义逐字 ＋ 极端 `C_4`-free `5\times5` 图是否已完备枚举 ✓
【⛔ 纪律】 本轮**未计算、未实现**；`U_{2,3}` 暂停 ✓
【边界】 `T-2/T-5/T-7` 的"已知"栏为**检索片段/档级**，**未逐字核**；`T-1` 为**逐字** ✓

## §附 【技术词回查】（补录）
```
技术词 inertia set      命中文件数=1    :: ./CONCRETE-TOPIC-LIST-r2.md 
技术词 minimum rank     命中文件数=1    :: ./CONCRETE-TOPIC-LIST-r2.md 
```
