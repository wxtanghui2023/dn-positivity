# Citrin 对数链深入：共振刚性 + 标度不变性（2026-08-23 22:10）

> 唐先生：继续深入 Citrin 模型
> 结果：✅ 复现准透明态=零点 + 共振刚性（微扰稳定）——标度不变性机制

## 1. Citrin 模型复现（准透明态 = 零点 ✓）

```
1D 对数链：z_j = d·ln(j+1)——形状因子 (−1)^{j+1}e^{−αz_j}——αd = ½
传输矩阵（δ-势散射）——传输系数 t(E)
零点处：t(2γ_n/d) = 0.9997-0.999998（全部 ≈ 1——准透明 ✓）
→ Citrin 的核心声明复现：准透明态 = 零点
```

## 2. ⭐ 共振刚性（物理版本的零点刚性）

```
微扰 α（0.500→0.510——2%）：透明点移动 = 0.0000（完全锁定！）
微扰 d（1.00→1.02——2%）：透明点移动 = 0.0008-0.0014（0.006%——几乎不动）
→ 零点（传输共振）对链参数极度稳定——共振刚性
```

## 3. ⭐ 机制：对数链的标度不变性

```
z_j = d·ln(j+1)——d 是标度因子
准透明条件：y = kd/2（y = 零点虚部——k = 波矢）
→ d 的微扰：y = (2y/d)·d/2 = y（标度抵消！）——y 不变
→ 对数链自相似（ln 位置——标度不变）⟹ 共振稳定 ⟹ 零点刚性
```

**物理-数学连接**：
- 对数链（标度不变）→ 共振稳定（零点刚性）
- 数学：N₀(t) ~ t log t（对数标度——Riemann-von Mangoldt）——零点的"对数结构"
- S(t) 的刚性（Σδ=O(1)）来自对数标度（标度不变）

## 4. 意义

1. **Citrin 模型提供零点的物理实现**（1D 对数链——真实凝聚态系统）
2. **共振刚性 = 零点刚性的物理版本**（微扰稳定）
3. **标度不变性机制**：对数链自相似 ⟹ 零点锁定——物理解释 Σδ=O(1)
4. **新的物理工具**：共振理论（Fano/散射相位——共振位置的一阶不敏感性）——可能推导零点刚性

## 5. 待探索

- 共振刚性能否严格化（共振位置的一阶不敏感性——散射理论）
- 标度不变性与 N₀(t) 的关系（数学严格化）
- 与 BKT/超 GUE 的统一（标度不变 = 临界 = 自相似）

## 6. 文件

- scripts/citrin_chain.py（复现——准透明 = 零点）
- scripts/citrin_rigidity.py（共振刚性——微扰稳定）
