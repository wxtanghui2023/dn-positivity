已查地图：未覆盖（关键词: 算术导数|进制数字|数字和|Collatz|覆盖系统）—— 可开档，首行须照抄本行
　（⚠️ `覆盖系统` 1 档＝`MATH-STATEMENTS-all-22-items-rigorous.md`**异义** ⟹ 不主张；`算术导数` 1 档＝**本仓自击**（`KERNEL-HUNT-3-prime-native-interface-search.md`）⟹ 实质新 ✓）

# **`E-10` 第二轮：候选生成（依新纪律，`RH-interface: UNKNOWN`）**

**唐先生令（2026-09-23 08:37）**：
① **回到 `E-10`**，严格执行三要素筛：$$\boxed{\text{prime-native object} + \text{independent problem} + \text{genuine parameter}}$$ 然后才问
　`new arithmetic observable/interface → RH relevance` ✓
② ⛔ **禁反向**：**不再从 `1/2`／Weil／惯性／`R8`／`S2`／`W6` 反向寻找对象** ✓✓
③ **目标形态（照录）**：找一个 **"即使完全不谈 RH，也值得研究"** 的问题；**RH 只能在第二阶段作为它的一个特殊结构/性质出现** ✓
④ `E-11`／`E-12` ＝ **工具箱**（遇**完全不同的 global arithmetic object** 时的后置接口），**不是下一候选本身** ✓
⑤ 本轮**只做候选生成 ＋ N1–N5 初筛**；`N6`／`N7` 延后；**`RH-interface = UNKNOWN`**（⛔ 不得因"看起来和 RH 有关"提前给 relevance）✓

D0: 本档对象 = **E-10 第二轮候选生成与 N1–N5 初筛**（档案**零覆盖** ⟹ 新档；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算／零数值／未写程序**／未做数值实验／未算零点／未接 ζ／未找 `1/2`／未证 RH ✓

---

## §1 三个候选（**皆为 prime-native ＋ 独立问题 ＋ 真参数**）

| 编号 | 候选对象 | `\theta`（真参数） | 独立问题（**不谈 RH 也值得研究**） | 档案覆盖 |
|:--|:--|:--|:--|:--:|
| **C-A** ⭐ | **算术导数动力学** `D(n)`（`D(p)=1`，Leibniz 律） | **阶 `k` / 进制或 arity 形变** | **`D` 的迭代下降/终止猜想**、`D(n)=1`／`D(n)=n` 的解、**Giuga 型**判据 ✓✓ | **0** ✓✓ |
| **C-B** | **素数在给定进制下的数字结构** | **基 `b` / 数字集** | **受限数字素数**（Maynard 线）、**数字和/正规性**（开 ✓） | **0** ✓✓ |
| **C-C** | **广义 Collatz 族** `n ↦ (a n + b)/2^{v_2}` | **`(a,b)`** | **周期存在性**（Steiner／Simons–de Weger 线）、密度结果 | **0** ✓ |

## §2 **N5**：各候选的**可证**非平凡结构（硬门 ✓✓）

```
【C-A ⭐（最强）】 三条**精确可证**结构：
　(i) **Leibniz 律** `(ab)' = a'b + ab'` ✓；幂律 `(a^k)' = k a^{k-1} a'` ✓✓
　(ii) ⭐ **精确对数导数公式**：$$D(n) = n\sum_{p^a \| n}\frac{a}{p}$$ —— **恒等式，可证、可算** ✓✓✓
　(iii) 由 (ii) 直接得到**极值/单调结构**：`D(n)/n = \sum a_p/p` ⟹ 可比较 `D(n)` 与 `n`（`D(n) < n \iff \sum a_p/p < 1`）✓✓
　⟹ **N5 ✅**（且非"随参数变化"这类弱理由 ✓）
【C-B】 一条**精确可证**结构：**进位列恒等式**
　$$s_b(m+n) = s_b(m) + s_b(n) - (b-1)\,c(m,n)$$（`c` ＝ 进位数）✓✓ —— 精确、可证、且**真正是组合律** ✓
【C-C】 可证结构：**周期定理**（`Steiner`／`Simons–de Weger`：`m`-环不存在于低阶/特定类 ✓）＋ **Terras 型密度定理**（几乎处处有限步落入有界区 ✓）
```

## §3 **N1–N4 初筛**（✓）

```
【N1 对象先于 RH 定义】 三者皆**纯整数/素数定义**，**与零点、RH 无任何定义层接触** ✓✓
【N2 独立问题】 三者皆有**成熟且与 RH 无关**的独立问题（见 §1 表）✓✓
【N3 真参数（非归一化）】 C-A：阶/进制；C-B：基/数字集；C-C：`(a,b)` —— 皆**形式化地改变对象本身** ✓
【N4 参数改变 quantity 而非仅坐标】 ⭐ 逐项确认：
　C-A：`D` 的**阶/进制形变**改变 **Leibniz 律的系数结构**（非坐标缩放）✓；
　C-B：换基 `b` 改变**进位结构**（`s_b` 的定义随 `b` 变，非缩放）✓；
　C-C：换 `(a,b)` 改变**映射的动力学**（周期/密度结构变）✓✓
```

## §4 **风险标注（诚实 ✓）**

```
【C-A】 ⚠️ 无（档案 0 覆盖；且不与 `1/2`/Weil/惯性/`R8`/`S2`/`W6` 同源 ✓）；唯需注意"迭代下降猜想"属**开放猜想** ⟹ 只作独立问题，不作前提 ✓
【C-B】 ⚠️ 文献活跃（Maynard 已解决"无数字 0"型；Mauduit–Rivat 型结果为既有机器）⟹ **`N6` 风险中等** ⚠️
【C-C】 ⚠️ 文献成熟（周期/密度线已有大量结果）⟹ **`N6` 风险较高** ⚠️
【`RH-interface`】 **三者皆登记 `UNKNOWN`** ✓（⛔ 未因"素数=RH 邻域"提前给 relevance ✓）
【⛔ 禁反向检查】 三者**均非**由 `1/2`／Weil／惯性／`R8`／`S2`／`W6` 反推而来 ✓✓
```

## §5 判定与下一刀

```
【初筛结果】 ⭐ **C-A（算术导数动力学）＝ 本档首选候选**（`N1–N5` 全过；档案 0 覆盖；结构为**精确恒等式**级 ✓✓）
　C-B／C-C ＝ **备选**（`N1–N5` 亦过，但 `N6` 风险分别中/高 ⚠️）
【状态】 `E-10` 第二轮**第一轮初筛完成**；**未开案** ✓；`N6`／`N7` 延后 ✓；`RH-interface: UNKNOWN` ✓
【下一刀建议】 对 **C-A** 做 **`N6`／`N7` 审计**（是否被既有机器吸收：算术导数是否已有成熟"机器"？其独立问题是否已饱和？）
　—— 若 `N6` 过，则进入**该对象自身**的"独立问题 ＋ 可证结构"展开（**仍不谈 RH**）✓
```

## §6 边界与回查（✗✓）

```
✗ 零计算／零数值／未写程序／未做数值实验／未算零点／未接 ζ／未找 `1/2`／未证 RH／**未开案** ✓
✗ ⛔ 未从 `1/2`／Weil／惯性／`R8`／`S2`／`W6` 反向找对象 ✓；⛔ 未因"与 RH 邻接"提前给 relevance ✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 进制数字 命中文件数=0 ⟹ **本档新增** ✓｜数字和=0 ⟹ **新增** ✓｜广义Collatz=0 ⟹ **新增** ✓
　技术词 算术导数 命中文件数=1 ⟹ **本仓自击（`KERNEL-HUNT-3`）⟹ 实质新** ✓
　技术词 覆盖系统 命中文件数=1 ⟹ **档案已有（异义）⟹ 不主张** ✓
【地图回查】 关键词 5 项 = **未覆盖** ⟹ 可开档 ✓；档案覆盖：`数字和` 0／`Collatz` 0／`Giuga` 0／`覆盖系统` 1／`迭代` 67（通用词）✓
【档级引用】 `E-10`／`KH-FRONTIER-SHAPE`（准入筛）✓｜`KERNEL-HUNT-3`（`A1` 算术导数曾列 GAP；**新纪律下 `RH-interface` 后置 ⟹ 该 GAP 不阻碍其作独立候选** ✓）✓
```

---

## §7 **五出口打分（首用新标准 v；`E-13`）**

```
【依据】 `KH-FRONTIER-SHAPE` §9（五出口审计）＋ §9.5（**去 RH 化测试**）✓
【⭐ `C-A`（算术导数动力学）逐出口】
　A RH：**无接口**（`RH-interface: UNKNOWN`，且**不得**因"素数邻接"提前给 relevance）⟹ **A 出口：空** ⚠️
　B 零点比例：**无接触**（`D(n)` 不涉及 `N_0/N`）⟹ **B：无** ⚠️
　C 离轴零点：**无接触** ⟹ **C：无** ⚠️
　D 其它素数问题：**弱**（可接 prime gaps／`\Omega` 型结构，但需先证新量）⚠️
　E ⭐ **其它猜想：强** —— **迭代下降/终止猜想**（本身即开放）＋ **`D(n)=1`／`D(n)=n` 的解**（Diophantine）＋ **Giuga 型判据** ✓✓
　⟹ **去 RH 化测试：通过**（拿掉 RH，仍有**独立、有参数、有非平凡结论**的问题 ✓✓）
　⟹ ⭐ **判定：`C-A` 依 E 出口立住**（**A 出口空 ≠ DEAD** ✓✓）
【`C-B`／`C-C` 简述】
　`C-B`（进制数字）：D 出口**中**（受限数字素数／数字和型问题）＋ E 出口**中**；⚠️ `N6` 吸收风险中 ✓
　`C-C`（广义 Collatz）：E 出口**强**（周期/密度线）＋ D 出口**弱**；⚠️ `N6` 吸收风险高 ✓
【本轮结论】 三候选**皆通过"去 RH 化测试"** ⟹ 依新标准**均不作"因不能 RH 而 DEAD"** ✓✓
【下一刀建议（不变）】 对 **`C-A`** 做 **`N6`／`N7` 审计**；并按 **E 出口**先展开其**自身**的独立问题（**仍不谈 RH**）✓
```
