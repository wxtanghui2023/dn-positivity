# X_Q 公理分层（2026-09-09 16:34 唐先生修正——）

## 分层（避免把 RH 结论写进公理——）
I 几何入口（真正需创造——）：G1 算术空间（闭点↔素理想）/G2 内禀时间 φ_t/Θ/G3 Frobenius-like 局部作用（F_p ≠ id——）
II 长度公理（最强过滤器——）：L1 闭轨 γ_p/L2 T_p = log p/L3 h_top = 1
III 自动结果（非公理——）：C1（Z = ζ 自动——）C2（迹公式 = 结构要求——）
IV RH 层（目标非入口——）：C3 含 β = ½（= RH——目标——）

## 最小未知公理（只有三个——）
(1) X_Q 存在
(2) p → γ_p——T_p = log p
(3) 函数域式 Frobenius 权重机制（来自内部——非定义——）

## 最值得攻击的位置（D1+F2 的张力——）
如何让 Arakelov 的无穷位（∞——）携带 Frobenius 时间？
——F₁ 失败点：有绝对 Frobenius——但无可用上同调 + 时间流——
——Arakelov 紧化已存在（静态——）——缺时间——

## 最终最小研究对象
(X_Q, φ_t, F_p)——Per(p) = log p——h = 1
——Frobenius-like 来自内部（非定义进去——）——
——今天（9/9）所有路线压缩后的唯一未封存入口——
