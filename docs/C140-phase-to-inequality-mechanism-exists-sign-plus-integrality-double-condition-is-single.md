已查地图（**先查后写**）：`C-127`（**逐字**：`log-free 排除`**存在**，但**只在有限范围**：验证法（`N(T)` 为**整数** ＋ 定位**符号变化**）；`(3)` **破法已找到，其极限障碍已定位**：机制＝**计数＋整数性**⟹**排除**；**渐近时** `N(σ,T)→∞` ⟹ **整数性失效** ⟹ 必须付精度尺度；**唯一破法**＝在极限中存活的**一致整数性／有限性界**；反例：`②` 由 `Z(t)` 的**符号变化**定位 `N(T)` 个零点（严格区间算术））、`C-126`（SUSPENDED／唯一破口）、`C-128`／`C-129`（一致有限性界；判为**路线循环**）、`C-137`（缺口＝"相位→不等式"）、`C-138`／`C-139`（完备窗口／共轭饱和；ζ 侧通 `W6`）、`W6`。关键词回查：`符号整数性机制`=0、`双条件归一`=0、`渐近推广`=0（**均本档新增**）。
**本档任务（唐先生 2026-09-19 11:55「甲5」）**：**攻双条件第二条**：相位（或其替代品）。
**结论（先行）**：$$\textbf{(一)}\ ⭐\ \text{"相位}\to\text{不等式"机制}\ \textbf{存在，且档案已登记}：\text{即}\ \text{`C-127`}\ \text{的}\ \textbf{计数＋整数性}\Longrightarrow\textbf{排除}，\ \text{实现}＝\textbf{验证法}（N(T)\ \text{整数} ＋ Z(t)\ \textbf{符号变化}）✓✓$$
$$\textbf{(二)}\ ⭐\ \text{关键}：Z(t)\ \text{的}\ \textbf{实值性}＝\text{结构性资产}（\text{把"相位"换成"}\textbf{实函数的符号}"），\ \text{而符号＋整数性机制}\ \textbf{不付精度尺度}✓✓$$
$$\textbf{(三)}\ \text{极限障碍（逐字）}：\textbf{渐近时}\ N(\sigma,T)\to\infty \Longrightarrow \textbf{整数性失效} \Longrightarrow \text{必须付精度尺度};\ \textbf{唯一破法}＝\text{一致有限性界}✓✓$$
$$\textbf{(四)}\ ⟹\ ⭐⭐\ \textbf{双条件实为单条件}：\text{两轴（完备窗口／相位→不等式）}\ \textbf{都归结到同一破口}＝\text{一致有限性界／support}>1 ✓✓$$
$$\textbf{(五)}\ ⭐\ \text{目标重写（比 RH 更具体）}：\ \boxed{\text{能否把"符号＋整数性"机制从}\ \textbf{有限范围} \text{推广到}\ \textbf{渐近}？}✓✓$$

FREEZE-ACK: 本档即冻结期内的机制定位与双条件归一（依 `§8.1`；不产候选结论）

D0: 本档对象 = **"相位→不等式"机制的定位（＝`C-127` 符号＋整数性通道）＋ 双条件归一（两轴→同一破口）＋ 目标重写** —— 关系 = 定位与归一，非新机制
D1: 0

# C-140 · **甲5：攻"相位 → 不等式" —— 机制已存在且已登记；双条件实为单条件**

> **唐先生 2026-09-19 11:55**：**「甲5」**（攻双条件第二条：相位／替代品）✓

---

## §1 机制定位：**它就是 `C-127` 的"计数＋整数性"通道**（逐字）

$$\text{实现}＝\textbf{验证法}：$$
$$\qquad ①\ N(T)\ \text{为}\ \textbf{整数}（\text{RvM／辐角原理}）✓$$
$$\qquad ②\ \text{由}\ Z(t)\ \text{的}\ \textbf{符号变化} \text{定位}\ N(T)\ \text{个零点（严格区间算术）}✓✓$$
$$\qquad ③\ \text{若符号变化数}＝N(T) \Longrightarrow \text{范围内零点}\ \textbf{全在线上} \Longrightarrow \textbf{排除}✓✓$$
$$\text{（实例：Platt–Trudgian 无离线零}\ |\gamma|\le3\times10^{12}）✓$$
$$\textbf{这正是"相位}\to\text{不等式"的机制}：Z(t)\ \text{的}\ \textbf{实值性} \text{把"相位"换成"}\textbf{实函数的符号}✓✓$$
$$\qquad ⭐\ \text{而符号＋整数性机制}\ \textbf{不付精度尺度}（\text{`C-127`}\ \text{逐字}）✓✓$$

## §2 为什么只在**有限**范围（逐字）

$$\textbf{"渐近时}\ N(\sigma,T)\to\infty \Longrightarrow \textbf{整数性失效} \Longrightarrow \textbf{必须付精度尺度}"✓✓$$
$$\textbf{"唯一破法}＝\text{在极限中存活的}\ \textbf{一致整数性／有限性界}"✓✓$$
$$\qquad ⚠️\ \text{而该破法已在}\ \text{`C-128`／`C-129`}\ \text{审计：}\text{"离线零有限"} \Longrightarrow \text{需 ISO} \Longrightarrow \text{其两输入已判}\ \textbf{循环} \Longrightarrow \text{路线循环}✓✓$$

## §3 ⭐⭐ **双条件归一**（本档核心产出）

$$\text{轴一（完备窗口）}：\text{共轭饱和／support}>1 \ \Longrightarrow \text{`W6`}✓$$
$$\text{轴二（相位→不等式）}：\text{符号＋整数性通道的渐近推广} \Longrightarrow \textbf{一致有限性界}✓$$
$$\boxed{\text{两轴}\ \textbf{都归结到同一破口}：\ \text{一致有限性界}\ \equiv\ \text{support}>1\ \equiv\ \text{`W6`}}✓✓$$
$$\qquad ⟹\ \text{`C-138` 的"双条件"}\ \textbf{实为单条件};\ \text{与}\ \text{`C-126`}\ \text{的 SUSPENDED}\ \textbf{同址}✓✓$$

## §4 目标重写

$$\boxed{\text{新目标}：\text{能否把"}\textbf{符号＋整数性}\text{"机制从}\ \textbf{有限范围} \text{推广到}\ \textbf{渐近}？}✓✓$$
$$\qquad \text{优点}：\text{比 RH}\ \textbf{更具体};\ \text{失败模式}\ \textbf{已精确定位}（N(\sigma,T)\to\infty）✓✓$$
$$\qquad \text{实现条件（等价形式）}：\text{离线零计数}\ n_{\text{off}}(T)\ \text{有}\ \textbf{与}\ T\ \text{无关的一致界}（\text{`C-128`}）✓$$
$$\qquad ⚠️\ \text{而该形式已判}\ \textbf{路线循环}（\text{`C-129`}） \Longrightarrow \text{目标与已审对象}\ \textbf{重合}✓✓$$

## §5 边界与回查

- ⚠️ §1／§2 全为 `C-127` **逐字**（含"不付精度尺度""一致有限性界"）✓
- ⚠️ §3 的"两轴归一"为**本档结构判断**（非定理）✓
- ⚠️ **不声称** 该目标不可解；只判：**与已审对象重合**（`C-128`／`C-129`）✓
- ⚠️ §1 中 Z(t) 实值性为**经典事实**（未另核原文）✓
- **未用** RH；**未改**任何原档 ✓
- **纪律**：先查后判（R-1 ✓，**先跑后写** ✓）

## §6 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-19 12:0x）`[纪律]`（先跑后写）

```
技术词 符号整数性机制  命中文件数=0 ::  ⟹ 本档新增
技术词 双条件归一     命中文件数=0 ::  ⟹ 本档新增
技术词 渐近推广      命中文件数=0 ::  ⟹ 本档新增
```
**读数（按实测）**：三项**全 0 档 ⟹ 均本档新增** ✓
