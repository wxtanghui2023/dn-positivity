已查地图：命中（本线自档 `WS-J1-first-sitting-...`／`KH-4-*`）⟹ **引用，不开新案** ✓

# **`WS-J1b` 不带预设版**：加权尺度反演的对称性要求什么？（**结论：没有自由 `r`**）

**唐先生方法论纠正（2026-09-23 12:41，照录 ✓✓）**：我此前那条判据（"是否存在不含 `1/2` 的条件把 `r` 逼到 `1/2`"）**本身预设了答案的形状** ✗ —— 那是**目标导向搜索**（带已知答案反推条件），与 `J-4` 同型 ✗；
　正确做法：**完全不去想 RH 或 `1/2`**，单纯问「**这个加权算子共轭，在什么 `r` 下会是自伴的／幺正的／满足某种自然对称性**」，**再看**自然确定的 `r` **是否碰巧**等于 `1/2` ✓✓

D0: 本档对象 = **`WS-J1b` 不带预设的对称性计算**（引本线自档；**未开 bridge 案** ✓）
D1: 0 （`[RESEARCH]` 轮次；结论为**结构定位**：自由参数消失于"哪种算子应保距"之选择 ✓）
FREEZE-ACK: D1=0 ✓
[RESEARCH]

---

## §1 **计算（`scripts/ws_j1b_presupposition_free.py`；符号精确 ✓）**

```
【符号约定】 `\sigma_x f(t)=f(x/t)`（尺度反演）｜`J f(t)=\frac1t f(1/t)`（实现 Mellin 反射 `s\mapsto1-s`）｜`S_x:=D_{1/x}\circ J`，即 $$\boxed{S_x f(t)=\frac xt\,f\!\left(\frac xt\right)}$$ ✓
【结果 1（✓）】 $$\boxed{S_x^2=x\cdot\mathrm{Id}}$$（符号核验：`S(S(f))/f=x` ✓）
【结果 2（✓✓ 显式谱结构）】 在基 `\{1,\ t^{-1}\}` 上：`S_x(1)=x/t`、`S_x(t^{-1})=1` ⟹ 矩阵 $$\begin{pmatrix}0&x\\1&0\end{pmatrix}$$ ⟹ **特征值 `\{\pm\sqrt x\}`** ✓✓
　$$\boxed{\text{位置以\textbf{本征值}出现：}\ \lambda=\pm x^{1/2}}$$ —— 且 `x^{1/2}` 是 `S_x^2=x\cdot\mathrm{Id}` **逼出的平方根**，**非手工插入** ✓✓
【结果 3（✓）】 `J` 在 `L^2(dt)` 上**保距**（重量 `t^{-1}` 与 `dt` 相消 ✓）；在 `L^2(dt/t)` 上**不保距**（残留 `t^{-1}` ✗）
【结果 4（✓）】 `\sigma_x` 在 **Haar `dt/t`** 上**自伴且酉**（配对经 `u=x/t` 保持 ✓）⟹ 谱 `\{+1,-1\}` ⟹ **位置不可见**（纯相位）✓
【结果 5（✓★ 关键）】 **没有自由 `r`**：`r` 由"**哪一种算子应当保距**"唯一决定 ——
　要求 `\sigma_x` 保距 ⟹ **Haar（重量 `t^0`）** ⟹ 谱 `\{\pm1\}`｜要求 `J` 保距 ⟹ **Lebesgue（重量 `t^{-1}`）** ⟹ 谱 `\{\pm x^{1/2}\}` ✓✓
```

## §2 ⭐ **判读（不带预设地看结果 ✓✓）**

```
【发现】 `1/2` **确实出现了**，但出现方式**不是**"某个被选定的参数"，而是 `S_x^2=x\cdot\mathrm{Id}` 逼出的**平方根／几何平均**：
　$$\lambda=\pm\sqrt x=\pm\sqrt{1\cdot x}$$（两个权重 `1` 与 `x` 的**几何平均** ✓✓）
【与 §J-1 自纠的关系（一致 ✓）】 两个 `1/2` **角色不同**：`\sqrt x` 里的 `1/2` 是**平方根指数**；临界线的 `1/2` 是**反射 `s\mapsto1-s` 的轴** ✓
　⟹ 它们在 **Lebesgue 归一化**下**对齐**（反射轴 `\mathrm{Re}\,s=1/2` 与谱值 `\pm x^{1/2}` 同现 ✓），但**不是同一个 `1/2`** ✓✓
【残留的真参数（本轮唯一剩下的 ✓）】 **测度／配对的选择**（Haar vs Lebesgue）——
　⚠️ 且这里有**算术侧输入**：**函数方程反射 `s\mapsto1-s` 恰由 `J` 实现，而 `J` 保距的配对正是 `dt`** ⟹ **算术结构指向 Lebesgue 归一化** ✓
　（**标注为观察**，非定理：该"指向"是在**权重因子层面**算出的 ✓）
【⛔ 未做的事】 未预设 `1/2`；未设计"产出 `1/2`"的判据；未宣称已导出临界线；未接 RH ✓
```

## §3 **回答唐先生的问题（"不带预设地做，看看 `r` 是不是 `1/2`" ✓）**

```
【答】 **做完了，而且更好**：自由参数 `r` **根本不存在**（§1 结果 5）；
　真正出现的是**两种自然归一化的二择**，其中**要求"算术型反射 `J` 保距"的那一支**，**自动**给出 $$\boxed{\text{位置成为谱值：}\lambda=\pm x^{1/2}}$$ ✓✓
【诚实定位】 这是一个**干净的正面结构事实**（位置→谱值有规范实现，且 `1/2` 由平方根逼出）；
　但**它尚未与 `\beta` 或零点分布发生任何联系** ⟹ **下一步的合法问题**（仍不带预设）：
　$$\boxed{\text{该 `\pm x^{1/2}` 谱结构对\textbf{离轴结构}（}\beta\ne\tfrac12\text{）是否敏感？还是仅由尺度结构决定（}\gamma\text{ 型）？}}$$ ✓
```

## §4 边界

```
✗ 未证 RH／未接 ζ｜⛔ 未把 localization·transport·realization 当候选机制 ✓
【证据等级】 **档级＋符号核验**（`S^2=x\cdot\mathrm{Id}` 与 `2\times2` 谱已逐项核验 ✓）
【无新性主张】 未宣称"新" ⟹ 无需【技术词回查】✓
```
