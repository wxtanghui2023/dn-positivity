# S̄ 平均 Euler 展开审计（2026-09-09 10:20——）

## 状态标记（唐先生——）
```
M(T) 路线：
之前：零点几何候选 → 死
现在：prime-frequency averaged dynamics
状态：★★★★★ 机制解释
      ★★★ 证明入口
      ★ RH/LH 通道（尚未）
——独立算术振荡稳定性问题（未必触及 RH——但可能独立成结果——）
```

## 目标（重定义——）
不证明 M(T) = O(1)——先证明一个【正确的 S_avg 展开命题】：

命题（候选）：存在平均核 w_H，使
  S̄_H(t) = −(1/π)Σ_p [ŵ_H(log p)/√p]·sin(t log p) + E_H(t)
且 ∫₀^T E_H(t)dt = O(1)

——只 k=1（k≥2 不是瓶颈——k=1 危险——）
——【死亡测试】误差 E_H 必须来自平均滤波——不是 ζ'/ζ 的零点项——
  （若 E_H 控制依赖 Σx^ρ/ρ——退化到显式公式→零点→旧墙——）

## 关键新变量：平均尺度 H
S̄_H(t) = (1/H)∫_{t−H/2}^{t+H/2} S(u)du——固定 H（参数化——）
低通滤波：ω = log p——ωH ≫ 1 被压——ωH ≪ 1 保留——
  ——小素数（log p < 1/H）保留——大素数衰减——
  ——NS frequency cascade 类比——

## 三个审计测试
Test 1: 拟合系数 a_p√p ≈ ŵ_H(log p)？（滤波形状——）
Test 2: 改变 H——DC(H) 是否 = Σ ŵ_H(log p)/(√p log²p)？
Test 3: 残差积分 R_M(T) = ∫(S̄ − S_model)dt——O(1)/log/随机游走？

——完成后才决定是否进入解析证明——

## 测试结果（11:00——）
### Test 1 ✅（决定性——）: a_p·√p vs ŵ_H(log p)
固定 H 窗口平均——a_p·√p 随 H 衰减【精确跟随 sinc(H·log p/2)】
——包括符号翻转（H=4: p=5,7,11 的 ŵ 变负——a_p 变负——完美——）
——矩形窗 Fourier 确认——a_p(H) ≈ a_p(0)·ŵ_H(log p)——
H 无关部分：a_p√p ~ −0.155（p=2-13 恒定——）→ a_p ~ −0.155/√p
⚠️ 无 1/log p 因子（与 Titchmarsh 理论不同——待查——）

### Test 2 🟡（定性——）: DC_model(H) = Σa_p(H)/log p
DC_model 随 H 增而减小（−0.48→−0.13——H=0.5→4.0——）——滤波压高频——
⚠️ 采样积分的漂移问题（M_H max 11.6 假象——∫S̄_H = ∫S 理论 H 无关——）
⚠️ M 的 DC 符号（零点公式 −0.69 vs 区间平均 +0.6——定义差——待澄清——）

### Test 3 🟡（疑问——）: 残差积分
固定 H 采样：残差 std 占比 72-77%——∫R 增长到 74（>随机游走基线——）
⚠️ 但采样积分有漂移问题（Test 2 发现的——）——零点区间平均版
   ∫R max 0.98（O(1)——）——矛盾——需用精确方法复核——

## Test 5 结果（11:05——）部分通过 + 大 p 系统偏离
Q_p(H1,H2) = a_p(H1)/a_p(H2)：
- ✅ 小 p（2,3——）匹配 sinc 比（偏差 <0.5%——）
- ✅ 偏差非噪声（分段数据完全一致——3.693/3.689/3.696——）
- ❌ 大 p 系统偏离（p=11: Q=3.70 vs sinc 3.33——p=19: 48.7 vs 13.7——）
  ——a_p(H) ≠ a_p(0)·sinc 精确——大 p 有系统修正——
  来源候选：(a) 离散窗 Fourier（Dirichlet 核）vs 连续 sinc
         (b) S 的阶梯（零点跳）残余（高频——）
         (c) 真实修正（H 依赖的额外因子——）

## Test 4 结果（11:10——）测度差异确认（唐先生 dS 洞察支持——）
同一个模型（Bohr k=1——）三种测度：
- 测度1（零点区间平均——）∫R max 0.90——O(1)
- 测度2（固定窗口勒贝格——）∫R max 24——≈随机游走（√N·σ·ds~25——）
- 测度3（零点求和 ΣR(γ)——）~−0.1——O(1)
⟹ 差异来自测度——零点测度（dN = N₀'dt + dS——）含 dS 补偿——
  勒贝格无补偿——残差积分随机游走——
⚠️ 数学上 ∫S̄_H dt = ∫S dt（H 无关——）——但数值（采样——）不同——
   ——勒贝格侧需要更完备模型（k≥2——）或 M 的 O(1) 依赖零点测度——

## 最终判定（11:15——）路线封存
验证 24：T=2万→24——4万→40——8万→74（~√T 增长——真发散——）
（T>8万 爆炸 = 数据范围 bug——t 超 γ_max——排除——）
⟹ 固定窗口（勒贝格）残差积分真随机游走——
⟹ "素数滤波+积分平滑⟹M(T)=O(1)" 不成立
⟹ M(T) 路线【正式封存】（不作为 RH 独立突破路线——）
保留：
- S_H 平均算子的 Fourier multiplier 结构（Test 1/5 小 p——✅ 有价值——）
- M=O(1) 依赖零点测度 dS 补偿（= 显式公式结构——非独立机制——）

## Weighted δ-sum 测试（11:40——唐先生死亡测试——）
第1步: A(N)=Σδ = O(1)（徘徊−3.7）——B(X)=∫A dx 线性发散（−2.3N）
第2步: P_N 与所有 δ 加权表达【无关】（相关 ~0.03——）
  ——"P_N = δ 二次加权"假设【否定】——P_N 不是 δ（零点偏移）的——
第4步（对照 Selberg）: ⭐ 方差压缩发现——
  S(γ_k) 零点采样: mean 0.5000——std 0.3231（常数——）
  Selberg 随机时间: std √(½log log T) = 1.127（增长——）
  ——零点采样的 std 是随机的 0.29 倍（特殊位置——）
  P_N/√N·std 基线 = 0.0066（压缩 150 倍——）
  【1+2Σρ(dS_k) = −0.018 ≈ 0】（lag<200——精确压缩——）
  ——P_N = O(1) 的机制 = dS 相关结构的方差压缩——
  ——= 8/22-23"方差压缩视角"的再现（Σδ 定理的解析基础同款——）
  dS_k 自相关: ρ(1)=+0.21, ρ(3)=−0.13, ρ(5)=−0.20（短正中负——GUE 特征?）
候选新引理: Var(P_N) = O(1)（方差压缩定理——）
  ⟺ ΣΣ Cov(S(γ_j), S(γ_k)) = O(1)（对关联压缩——）
  ——来源候选: 零点间距相关（GUE 对关联——条件）或显式公式（无条件部分）
