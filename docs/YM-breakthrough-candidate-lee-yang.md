# **突破口候选**：算术反射配对 + Lee–Yang 型正性（由 YM 工具箱推出）

**依据**：唐先生 2026-09-11 14:43（"我需要你从 YM 的研究分析中找到 RH 突破口"）｜**约束**：无 $1/2$ 输入；L2 未动
**脚本**：`scripts/YM1_reflection_balance.py`（先校准）｜**标注**：【核验】【引用·经典】【推导】【新区域·本项目】

---

## §1 【核验】算术侧**已有**"反射配对 + 权重平衡"
$$\zeta(s)=\chi(s)\zeta(1-s),\qquad \chi(s)=\pi^{s-1/2}\frac{\Gamma((1-s)/2)}{\Gamma(s/2)},\qquad \chi(s)\chi(1-s)=1$$
```
数值核验（log-gamma，避免下溢）：
  |chi(sigma+it)| / t^{1/2-sigma}：【对 t 恒定】0.39894(σ=0)｜0.63162(σ=0.25)｜
                                    **1.00000(σ=1/2)**｜1.58323(σ=0.75)｜2.50663(σ=1)
  ⟹ 近似函数方程的两项 zeta(s) ≈ Σ_{n≤x} n^{-s} + chi(s) Σ_{n≤y} n^{s-1}（xy = t/2π）
     带权重 1 与 |chi|，二者【相等】⟺ **σ = 1/2** ✓✓✓
```
$$\boxed{\text{算术侧存在一个【反射配对 }n\leftrightarrow t/n\text{】+【权重平衡】，其平衡轨迹【恰为临界线】}}$$
$$\boxed{\Longrightarrow\ \frac12 \text{ 由"配对 + 平衡"【推出】，而非输入}}$$

## §2 YM 工具箱 ↔ 算术侧：**逐项字典**
| YM 部件 | 算术对应 | 状态 |
|---|---|---|
| 格点反射 $\theta$ | 配对 $n\leftrightarrow t/n$（近似函数方程 ✓） | **存在** ✓ |
| 权重 $e^{-S}\ge0$（实、正） | 系数模 $n^{-\sigma}\ge0$，**但带相位 $e^{-it\log n}$** | **部分失败**：权重是复的 ✗ |
| 实性（$Z$ 实） | $\Xi(1/2+it)$ **实** ✓（自动） | **存在** ✓ |
| **反射正性** | **???**（= 缺失的正性） | **✗ 缺** |
| 平衡轨迹 / 临界曲线 | $\sigma=1/2$（已核验 ✓） | **存在** ✓ |
| 转移矩阵 / Lee–Yang 零点 | ζ 作为"气体"在**复温度**下的配分函数；零点落在临界曲线上 | 框架已知（Riemann gas / Lee–Yang）⚠️ 但标准定理不适用（长程 + 复权重）✗ |
| 团簇展开（强耦合） | Euler 积（$\sigma>1$ ✓） | 存在但**只在带外** ✗ |
$$\boxed{\text{三个部件中：配对 ✓、实性 ✓、平衡轨迹 ✓；【缺的正是"正性"】✗}}$$

## §3 ⭐⭐ 突破口：**Lee–Yang 型正性判据**（这正是 YM 工具箱的核心）
```
Lee–Yang 圆定理（1952，YM/统计力学工具箱的核心）：
  铁磁 + 有限程 ⟹ 配分函数在复逸度下的零点【全落在单位圆上】✓
Fisher（复温度）零点：同理给出"临界曲线"
```
$$\boxed{\text{"实性 + 正性 ⟹ 零点落在一条曲线上" —— 这正是 RH 的形状}}$$
```
RH 的对应陈述：Xi(1/2+it) 为【实】✓（自动，来自函数方程）+ 某个【正性】⟹ 零点全实
⟹ 故突破口 = 找出【算术的 Lee–Yang 条件】（铁磁 + 有限程 的算术对应物）
```

## §4 为什么这是**真突破口**（而非又一次重述）
```
① 它给出 1/2 【免费】：平衡轨迹（已核验）就是临界线，无需插入 ✓✓
② 它把【整套 YM 技术】变成可直接使用：反射正性判据、团簇展开、Lee–Yang、OS 重构、
   转移矩阵 —— 全部是 YM 侧已成熟、且【专门处理"实性+正性⟹零点在一条曲线上"】的工具 ✓✓
③ 它不在本项目已封闭的箱子里：它不是"形式正性"（§POS2/3 六类），
   而是【一个关于权重的结构性条件】—— 与"找更强的正性形式"不同 ✓
④ 障碍被精确定位：算术权重【非铁磁、非有限程】（长程、含 Möbius 型符号）✗
   ⟹ 具体工作项 = 刻画出【哪一类算术正性足以替代铁磁性】，并检验是否存在 ✓✓
```

## §5 具体可攻的问题（下一步）
```
Q1：Xi(1/2+it) 的"实性"可否被写成一个【正性求和】？（即：是否存在显式正的表示？）
Q2：若不，是否存在【部分正性】（前 N 项正、误差可控）—— 并由此得到新的零点密度结果？
Q3：算术侧的"有限程"对应物是什么？（如：截断到 n≤√t 的两项结构 ✓ 已是有限程！）
    ⟹ 关键观察：近似函数方程【本身就是有限程的】（两项、界限 t/2π）✓✓
       缺的是【铁磁性】——即系数的符号结构 ✗
```
$$\boxed{\text{Q3 最有希望：算术侧"有限程"已具备（两项结构）；缺口【收窄到"铁磁性"一项】}}$$

## §6 诚实边界
```
· §1 为【核验】（|chi| 的平衡点恰在 σ=1/2；chi(s)chi(1-s)=1）
· Lee–Yang 圆定理、Fisher 零点、OS 重构、团簇展开为【引用·经典】
· §3/§4 的"突破口"定位、§5 的 Q1–Q3 为【推导】
· 【新区域·本项目】：Lee–Yang/复温度框架此前【未】进入本项目死路地图（Riemann gas 与
  Bost–Connes 相邻，后者已因 KMS/实温度而关闭）；但【未验证】该框架能走通 ✗
· 【未做】未输入 1/2；未构造模型；未改 L2；未声称解决任何问题
```

## §7 提交链
```
5e1c157（正性机制穷举）→ 本篇（YM 突破口候选：反射配对 + Lee–Yang 正性）
```
