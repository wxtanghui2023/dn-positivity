已查地图（**先查后写**）：`C-177`（M=3 区间算术证书，目标 1/2）、`C-176`（m_M 上界证书）、`RPM-LEMMA-CONJECTURE-LEDGER`（C2 精确值／D1c）。关键词回查：`下界收窄`、`两侧夹逼`——本档新增用法。
**本档任务（唐先生 2026-09-19 20:0x，承接「开始下一条」）**：把 `M=3` 的下界从 `1/2` 收窄（即台账 `3A`／`C2`）。
**结论（先行）**：$$\textbf{(一)}\ ⭐⭐\ \textbf{成功}：\text{区间算术证书（目标}\ 0.75\text{）}\ \textbf{全部认证}（\mathbf{54{,}045}\ \text{箱}，\textbf{0 未决}，\text{最大深度}\ 27，\text{余量}\ 9.889\times10^{-5}，392.5\ \text{秒}）✓✓$$
$$\qquad \Longrightarrow \boxed{m_3\ \ge\ 0.75\ \textbf{严格}（\text{区间算术；无 SLACK、无浮点误差假设}）}✓✓$$
$$\textbf{(二)}\ \textbf{两侧夹逼几乎合拢}：m_3\ \in\ [\,0.75,\ 0.777171\,]\qquad（\text{宽度}\ 0.027）✓✓$$
$$\qquad \text{上界}\ U_3=0.777171\（\text{`C-176`}\ \text{的区间算术证书}）；\ \text{下界}\ 0.75\（\text{本档}）✓$$
$$\textbf{(三)}\ \text{对照}：\text{目标}\ 1/2\ \text{时}\ 13{,}455\ \text{箱／81.8 s；目标}\ 0.75\ \text{时}\ 54{,}045\ \text{箱／392.5 s}✓$$
$$\qquad \text{箱数}\ \times4.0、\text{时间}\ \times4.8（\text{符合"目标越接近真值、剖分越细"的预期}）✓$$

FREEZE-ACK: 本档即冻结期内的证书与登记（依 `§8.1`；不产候选结论）

D0: 本档对象 = **`m_3` 下界收窄到 0.75（区间算术证书）＋ 两侧夹逼状态** —— 关系 = 严格化与登记，非新机制
D1: 0

---

## §1 结果

$$\textbf{定理（本档）}：m_3=\min_{\varphi\in[0,\pi]^3}\max_{1\le k\le15}\sum_{j=1}^3\cos(k\varphi_j)\ \ge\ \mathbf{0.75}\ \textbf{严格}✓✓$$
$$\begin{array}{c|r}
\text{评估箱数} & 54{,}045\\
\text{未决箱数} & \mathbf 0\\
\text{最大深度} & 27\\
\text{认证最小余量} & 9.889\times10^{-5}\\
\text{耗时} & 392.5\ \text{秒}\\
\text{全部认证} & \textbf{True}\\
\end{array}✓$$

## §2 现状：`m_3` 的两侧夹逼

$$0.75\ \le\ m_3\ \le\ 0.777171\qquad(\text{宽度}\ 0.027)✓✓$$
$$\qquad \text{下界来源}：\text{本档区间算术证书（目标}\ 0.75\text{）}✓$$
$$\qquad \text{上界来源}：\text{`C-176`}\ \text{显式配置}\ (113.334^\circ,76.418^\circ,20.216^\circ)\ \text{的区间算术认证}✓$$
$$\qquad \text{数值最优（启发式）}：\approx0.7641\text{–}0.7769\ \text{（多个局部极小），落在区间内 }✓$$

## §3 复现

```
脚本: scripts/m3_certificate_interval_arith.py   （已参数化: 预算 最大深度 M 目标阈值）
命令: python3 scripts/m3_certificate_interval_arith.py 3000000 60 3 0.75
输出: 54045 箱 | 未决 0 | 深度 27 | 余量 9.889e-5 | 全部认证 True
```

## §4 边界与回查

- ⚠️ 证书依赖 `mpmath.iv`（标准区间算术）的正确性；域取 `[0,P]^3`（`P`=π 的有理上界，故更强）✓
- ⚠️ 本档**只**收窄下界（`1/2 → 0.75`）；**未闭合**（仍差 0.027）✓
- ⚠️ **不声称** `m_3` 的精确值；**不声称** `(\text{RP}_M)` 一般成立 ✓
- **未用** RH；**未改**任何原档（台账为进度更新）✓
- **纪律**：先跑后写 ✓（脚本跑出 0 未决后才落档）

## §5 台账进度更新

- 第 1 圈 `C2`（`m_M` 精确值）：`m_3` 由 `[0.5, 0.777171]` 收窄到 `[0.75, 0.777171]` ✓
- 四层台账第 3 层 `3A`：由"未开工"→"**框架在 ＋ 已有实质推进**" ✓
