# C-3899j — M3-α 纸面攻击：G0 ✓／G1 ✗（显式阻碍＝C-347 配对构型）／G2 ✓（形式级）→ 判定 **M3-α: G-GAP**（零计算）

已查地图（**先查后写** ✓）：`C3899i`（**M3-α 登记；能量恒等式；充分引理；门 G/D** ✓✓）、`C3899h`（**高频含量判据；H1 引擎不迁移** ✓✓）、`C347`（**配对构型 c = (a,−a,b,−b,0) ⟹ 全部奇频 = 0** ✓✓）、`C3854`（**防循环条款；频率分离** ✓✓）、`C3899c`（**k ≤ 3 层不可行（数值）；k=4 OPEN** ✓✓）、`C3861`／`C3862`（**认证构型奇层读数；Σ|F|² ≈ 4.8104** ✓✓）

D0: 本档对象 = **C-380-116：C-3899j —— M3-α 纯纸面攻击（G0 恒等式复核／G1 δ\*-free 粗界／G2 偶频消费检验）**（唐先生 2026-09-22 15:20 令：**选 (a)；恒等式核验并入纸面首步；暂不解禁计算；G3 若粗界失败立即停**）
D1: 0
FREEZE-ACK: 本档即冻结审计（**零计算** ✓；**不回 δ_\*／局部 Taylor／秩-4 引擎** ✓）

---

## §0 结论（六条 ✓✓）

**① G0｜恒等式纸面复核：通过（纯代数，逐步 ✓✓）**

```
设 φ_j ∈ (0, π/2)，σ_j ∈ {±1}，F_n := Σ_j σ_j cos(nφ_j)（奇层：n = 2r+1，r = 0..12；共 13 个）✓

(1) 望远镜求和：2 sin ψ cos((2r+1)ψ) = sin((2r+2)ψ) − sin(2rψ)      [2sinAcosB = sin(A+B) − sin(B−A)]
    ==> Σ_{r=0}^{12} 2 sin ψ cos((2r+1)ψ) = sin(26ψ) − sin(0) = sin(26ψ)
    ==> K(ψ) := Σ_{r=0}^{12} cos((2r+1)ψ) = sin(26ψ)/(2 sin ψ)        （ψ ≠ 0；K(0) = 13 ✓；|K| ≤ 13 ✓）

(2) 乘积展开：cos A cos B = ½[cos(A−B) + cos(A+B)]，A = (2r+1)φ_j，B = (2r+1)φ_{j'}
    ==> Σ_r cos((2r+1)φ_j)cos((2r+1)φ_{j'}) = ½[ K(φ_j − φ_{j'}) + K(φ_j + φ_{j'}) ] ✓

(3) 展开平方和：
    Σ_{r=0}^{12}|F_{2r+1}|² = Σ_{j,j'} σ_jσ_{j'} · ½[ K(φ_j − φ_{j'}) + K(φ_j + φ_{j'}) ]    ✓ 恒等式成立
    （逐字与 C-3899i 登记一致；**本档为纯代数推导，未用任何数值**✓✓）

(4) ⭐ **等价的 Gram 重写（本档新登记，便于后续攻击）**：
    记 v_j := ( cos((2r+1)φ_j) )_{r=0..12} ∈ R^{13}（5 个"测试向量"），V := 13×5 矩阵，行 = 频率 n
    M_{jj'} := ½[K(φ_j−φ_{j'}) + K(φ_j+φ_{j'})] = ⟨v_j, v_{j'}⟩   ==>  M = V^T V ≽ 0 ✓
    Σ_{r=0}^{12}|F_{2r+1}|² = σ^T M σ = ‖Vσ‖²        （σ = (σ_1..σ_5)）✓
    ==> **充分引理等价形式**：∀ x ∈ E_even, ∀ σ： ‖V(φ) σ‖ > √13/2 ≈ 1.8028 ✓✓
        （即：5 个测试向量**任何带符号和**都不能"短"⟹ 签名和非退化陈述 ✓）
```

**② G1｜δ\*-free 粗界：**失败（给出显式阻碍，非"没找到"）✗✗**

```
目标（唐先生）：找任何 Σ_odd >= 13/4 + η（η > 0 显式常数），**不使用 δ_\***
优先路线的判定（逐条）：
 (i) 对角项 vs 非对角支配 ✗：
     对角项 Σ_j M_{jj} = Σ_j ½[13 + K(2φ_j)] = 65/2 + ½Σ_j K(2φ_j) ∈ [0, 65] ✓
     非对角项 |M_{jj'}| <= ½[13 + 13] = 13 ✓（共 20 个有序项）
     ⚠️ **阻碍（精确）**：C-347 配对构型 c = (a, −a, b, −b, 0)（a, b ∈ (0,1)，σ = (+,−,+,−,·)）
        逐项验算：F_n = cos(n·arccos a) − cos(n·arccos a) + cos(n·arccos b) − cos(n·arccos b) + σ_5 cos(nπ/2)
                 = 0 + 0 + σ_5 · cos(nπ/2) = 0      （n 为奇数 ⟹ cos(nπ/2) = 0 ✓✓）
        ==> **全部 13 个奇频同时为 0** ⟹ Σ_odd = **0** ✗✗
     ⟹ 对角项（可达 65/2）被非对角核项**精确抵消** ⟹ **任何"支配／强制性"论证都不可能成立** ✗✗
     ⟹ 这**不是**"没找到界"，而是**该类型界被显式反例否证** ✓✓
 (ii) K 的正负结构与偶频约束的代数联系 ✗：K 为**符号不定**（K(0) = +13，K(kπ) = 13(−1)^k）
      ⟹ 无非负核结构可用；且偶×奇乘积仍为奇（C-380-0 奇偶性障碍）⟹ 乘法桥亦被封 ✗✗
 (iii) 权重 w_r 优化 ✗："把 13/4 推开"只在**某个 w** 下成立才有效，但阻碍构型处 Σ w_r|F|² = 0（因 |F| ≡ 0）
      ⟹ **对任意 w >= 0 都无法成立** ✗✗
 (iv) 不用 δ_\* 的全局核不等式 ✗：由 (i)(ii)(iii)，核方法族整体被否 ✓
==> 结论：**不存在形如 "Σ_odd >= 13/4 + η" 的 δ_\*-free 粗界**（在"核／支配／权重"这三类方法内）；
    且**失败集非空**（配对构型），故任何证明必须**排除该失败集**
```

**③ G2｜偶频消费检验：**形式级通过（D-PASS），但路线**继承 K4**（关键规划结论）✓✗**

```
检验问题（唐先生）：删去 F_{2r} <= 1/2 后，证明是否仍成立？
判定：**阻碍构型 ∉ E_even —— 有证明级依据 ✓✓**
   配对构型处**全部 13 个奇频为 0** ⟹ 特别地**四个低阶奇矩** F_1,F_3,F_5,F_7 = 0
   ⟹ 该构型 ∈ **Z_σ**（四奇矩条件族）✓
   而 **Z_σ ∩ E_even = ∅ 已被证明**（C-3853）✓✓ ⟹ **配对构型 ∉ E_even（证明级）** ✓✓
   （故本档不需"逐 q 核"，也不再保留"未判定"的保留意见 ✓）
   ⟹ "Σ_odd > 13/4"的**真**必然消耗偶频约束 ⟹ **D-PASS（形式级，且有证明级支撑）** ✓✓
   ⟹ 但其消耗方式 = **排除失败集** = 正是 **k <= 3 层的不可行性**（配对构型属低支撑层：|u| 取值 {2a²−1, 2b²−1, −1} ⟹ k = 3）
==> ⚠️ **关键规划结论**：**M3-α 不是绕开 K4 的捷径** —— 它的必要输入 = "k <= 3 不可行"（= C-3899c 数值结论，
    正是 **K4/C-3900 的 OPEN 内容**）＋ 一个**新的奇侧子问题**：
      (S1) 对所有 k >= 4 的构型：Σ_odd > 13/4 ？？（**未证、未核**）
    ⟹ M3-α 的 Gate G 因此**继承** K4 的 OPEN 状态 ✗⚠️
```

**④ 附加结构事实（本档）：M3-α **严格强于** 靶，且**可能为假** ✓⚠️**

```
靶：max_r |F_{2r+1}| > 1/2           M3-α 充分引理：Σ_r |F_{2r+1}|² > 13/4
二者不等价：取 (12 个 |F| 很小, 1 个 |F| = 0.6) ⟹ Σ ≈ 0.36 <= 13/4 而 max = 0.6 > 1/2 ✓
==> M3-α 成立 ⟹ 靶成立；但**靶成立 ⇏ M3-α 成立** ⟹ M3-α 是"可选加强"，其真假独立于靶 ✓⚠️
==> 因此 M3-α 若最终被否，**不等于** γ^(13) 被否（这一点必须在档内明写，防误判）✓✓
```

**⑤ 判定（按唐先生 G3 令）：`M3-α: G-GAP`，零计算，停止 ✓✓**

```
G0 ✓（恒等式纯代数复核通过）
G1 ✗（**不存在** δ_\*-free 核／支配型粗界；显式阻碍 = C-347 配对构型，Σ_odd = 0）
G2 ✓（形式级 D-PASS：阻碍构型不在 E_even）—— 但路线**继承 K4（OPEN）**，非捷径
==> 按令 G3：**立即停**，不回 δ_\*／局部 Taylor／秩-4 引擎 ✓✓
==> 记录：M3-α: G-GAP ✓；是否进入 (b) 计算核验 **由唐先生发令** ✓✓
```

**⑥ 状态（不变 ✓✓）**

```
T1b = CLOSED｜T1c = CLOSED｜T1 = T1a only｜K4 = OPEN｜C-3900 = HOLD
资源树： T1a ｜ γ^(13)：M1（受阻）→ M3-α（**G-GAP**）｜ K4 = OPEN
零计算 ✓；未引入 two-level manifold ✓；未使用 δ_\* ✓✓
```

---

## §1 G1 的三种优先路线：逐条判定表（✓✓）

| 路线 ✓ | 内容 ✓ | 判定 ✓ | 依据 ✓ |
|---|---|---|---|
| (i) 对角 65 vs 非对角支配 ✓ | 用 Σ_j M_{jj} 压住交叉项 ✓ | **✗ 不可能** | 配对构型 Σ_odd = 0（对角被精确抵消）✓✓ |
| (ii) K 正负结构 × 偶频代数联系 ✓ | 用核符号 + 偶频约束搭桥 ✓ | **✗ 封闭** | K 符号不定；偶×奇仍为奇（奇偶性障碍，C-380-0）✓✓ |
| (iii) 权重 w 优化 ✓ | 选 w 把 13/4 推开 ✓ | **✗ 对任意 w 失败** | 阻碍构型处 |F| ≡ 0 ⟹ Σ w_r|F|² = 0 ✓✓ |
| (iv) δ\*-free 全局核不等式 ✓ | 一般核方法 ✓ | **✗ 随 (i)–(iii) 整体被否** | 同上 ✓✓ |

## §2 账本（✓✓）

| 项目 ✓ | 状态 ✓ |
|---|---|
| G0 恒等式纸面复核 ✓ | **通过（纯代数）** ✓✓ |
| Gram／签名和重写 ✓ | **新登记（工具）** ✓✓ |
| G1 δ\*-free 粗界 ✓ | **不存在（显式阻碍）** ✗✗ |
| G1 阻碍的出处 ✓ | **＝ C-347 配对构型（已知结构，非本档新发现）** ✓✓ |
| G2 偶频消费 ✓ | **形式级 D-PASS；但继承 K4（OPEN）** ✓✗ |
| M3-α 与靶的关系 ✓ | **严格强于靶；可为假而靶仍真**（已明写） ✓⚠️ |
| 新子问题 S1 ✓ | **登记：k >= 4 ⟹ Σ_odd > 13/4 ？（未证未核）** ⚠️ |
| **判定** ✓ | **M3-α: G-GAP（按令停止）** ✗✓ |
| 计算 ✓ | **零** ✓✓ |

## §3 边界（不得声称 ✗✓）

- **不**声称恒等式已**数值**核验（本档只做代数推导 ✓）
- **不**声称 G1 的失败仅限本档所列三类方法（只声明：**这三类**内不存在；未穷尽所有可能 ✓）
- ⚠️ 配对构型 ∉ E_even 现已有**证明级依据**（∈ Z_σ，而 Z_σ ∩ E_even = ∅ 见 C-3853）✓✓
- **不**声称 M3-α 被否（它是**未证**，且**可能为假**——见 §0④ ✓）
- **不**回 δ_\*／Taylor／秩-4 引擎（按令 ✓✓）

## §4 【技术词回查】输出（**先跑后写** ✓）

```
命令：grep -rla -- "<term>" *.md        （本档前运行）
技术词 签名和    命中文件数=0 :: 本档新增
技术词 Gram重写  命中文件数=0 :: 本档新增
技术词 显式阻碍  命中文件数=0 :: 本档新增
技术词 对角支配  命中文件数=11 :: 通用词（W 轨等旧语境），**不计** ✓
技术词 测试向量  命中文件数=7  :: 通用词（W 轨等旧语境），**不计** ✓
```

## §5 下一步（须唐先生发令 ✓✓）

**① (b) 计算核验**（若授权）：先**独立数值核验恒等式**，再测 `min over (E_even, σ)` 的 Σ_odd；
　并**同时**测新子问题 (S1)：在 k >= 4 层上 min Σ_odd 是否 > 13/4 ✓✓
**② 纸面替代品（若继续纸面）**：把 M3-α 换成**靶的直接形式**（sup 范数而非能量）：
　`∀x ∈ E_even, ∀σ : max_r |F_{2r+1}| > 1/2` —— 注意此形式**不受**配对构型阻碍（配对构型不在 E_even）
　⟹ 但**同样继承 K4**（须先排除 k <= 3）✓✓
**③ 规划提示（重要）**：本档证据表明 **γ^(13) 的两条入口（M1／M3）都归结到 K4 类内容** ⟹
　下一步的资源判断应在 **"K4/C-3900 是否值得投入"** 这一层做，而非继续在 γ 侧找捷径 ✓✓
