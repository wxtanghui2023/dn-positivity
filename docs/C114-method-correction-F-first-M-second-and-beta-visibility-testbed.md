已查地图（所查：`C68-SPEC-...four-deliverables-and-eight-filters.md` §3（**`F1`–`F8` 八过滤器全表 ＋ 四交付物 `D1`–`D4`**）、`:2357`（**已知 off-line 实例（Davenport–Heilbronn／Epstein／Beurling 型）全无 Euler 积 ⟹ 撞约束⑤ ⟹ 该角＝"Selberg 类成员有可证 off-line 零点"级问题**）、`E4-1-FE-twosidedness-S1-S2-S3-audit.md:44`（**存在有 FE 却带离线零点的对象：Davenport–Heilbronn 型**）、`V219`／`D1`（Epstein 反例）、`V227-A`、`V192` §①、`C-111`、`C-113`）。**结论**：**唐先生 16:37 的批评成立，本档接受并给出根因与修正** ⟹ 根因：**我一直把"档案分类"当主筛，而档案分类表就是 `β` 盲的分类表** ⟹ 放行的必然是 `β` 盲（九条同址是**方法保证**，不是运气）✓✓；而**`F1`–`F8` 八过滤器（含 `F7` Epstein 测试）档案已有**，我却把 `F` 用在"地图去重**之后**"＝等于没用 ⟹ **修正：`F`-先／`M`-后** ✓✓；**并且检验床逻辑立刻给出一个重要修正**：按"能否在已知 `β\ne\tfrac12` 对象上报火"分，九条**不是全盲**——**`DBN`／`Λ` 是形变参数，对离轴零点有响应 ⟹ `β`-可见**，**它的墙是 `C-111` 的端点退化，不是 `β` 盲** ✓✓ ⟹ 故正确分工是 **"八条 `β` 盲 ＋ 一条 `β` 可见但端点受阻"** ✓✓

# C-114 · **方法修正：`F`-先／`M`-后 ＋ `β`-可见性检验床**

> **时间**：2026-09-18 16:37 唐先生：**"所以我让你避开 `Beta` 盲，其实完全没有做到，当然会失败"** ✓
> **本档性质**：**方法错误诊断 ＋ 顺序修正 ＋ 新筛查协议**（非数学新结果）✓

---

## §0 结论（先行）

$$\textbf{(1)}\ \text{根因（本档接受批评）}：\text{我把}\ \textbf{档案分类} \text{当}\textbf{主筛}，\ \text{而档案分类表}\ \textbf{就是}\ \beta\ \text{盲的分类表}✓✓$$
$$\qquad \Longrightarrow\ \text{用}\ \beta\ \text{盲的分类表筛候选} \Longrightarrow \boxed{\text{放行的必然还是}\ \beta\ \text{盲}} \Longrightarrow \text{九条同址是}\ \textbf{方法保证}，\ \text{非运气}✓✓$$
$$\textbf{(2)}\ ⚠️\ \text{而}\ \text{`F1`–`F8`}\ \text{八过滤器}\ \textbf{档案已有}（含\ \text{`F7` Epstein 测试}），\ \text{我把它用在"地图去重}\ \textbf{之后} \text{"}\ ＝\ \textbf{等于没用}✓✓$$
$$\qquad \Longrightarrow\ \textbf{修正}：\boxed{\text{F-先 ／ M-后：先过 F1–F8，再查地图}}✓✓$$
$$\textbf{(3)}\ ⭐\ \textbf{检验床逻辑立刻给出的重要修正}：\text{按"能否在已知}\ \beta\ne\tfrac12\ \text{对象上}\ \textbf{报火} \text{"分}，\ \text{九条}\ \textbf{不是全盲}✓$$
$$\qquad \text{`DBN`／}\Lambda\ \text{是}\ \textbf{形变参数}（\text{对离轴零点}\ \textbf{有响应}） \Longrightarrow \boxed{\text{`S1`／`C6`}\ \textbf{`β`-可见}}✓✓$$
$$\qquad \qquad \text{它的墙是}\ \text{`C-111`}\ \text{的}\ \textbf{端点退化}（\text{`1/2` 阶分支}），\ \textbf{不是}\ \beta\ \text{盲}✓✓$$
$$\qquad \Longrightarrow\ \text{正确分工}：\boxed{\text{八条}\ \beta\ \text{盲}\ ＋\ \text{一条}\ \beta\ \text{可见但端点受阻}}✓✓$$

---

## §1 证据：`F1`–`F8` 档案已有（逐字，`C68-SPEC` §3）

$$\textbf{四交付物}：D1\ \text{内禀半自同构};\ D2\ \text{canonical 分级};\ D3\ ⭐\ \textbf{canonical 实部钉定机制}（\textbf{钉点}，\ \textbf{非模长型}，\ \textbf{非 FE}）;\ D4\ \text{双向识别}✓$$
| # | 过滤器 | 判据来源 |
|:--:|:--|:--|
| `F1` | **非正性型**（否则塌回角 I）| `V192`＋`POS1` 第三行 ⟺ RH |
| `F2` | **非坐标型／非绕数型**（否则塌回角 II）| `M4` line gap ⟹ Hermitianization |
| `F3` | **钉点不钉轴**（否则＝FE，只钉轴）| `CLOSED-ROUTES-MAP:1496` |
| `F4` | **非模长型** | ⭐ `V227-A`（定理级）：`sup Re` 不是模长不变量 |
| `F5` | **含独立算术输入**（不与 RH 等价）| 6 次同址收敛 |
| `F6` | **对算术见证盲** ⟹ 只能解析型 | `V150` W2 |
| **`F7`** | **过 Epstein 测试**（ζ 特有；FE＋Euler 积不充分）| `V219`／`D1` |
| `F8` | **过污染测试 ＋ 压力测试** | `V227` §4(5)(7) |

$$\Longrightarrow\ \text{八条}\ \textbf{本已齐备};\ \text{我的错误}\ \textbf{不在缺筛子}，\ \text{而在}\ \textbf{顺序}✓✓$$

## §2 ⭐ 检验床协议（新，且附档案已登记的**限制**）

$$\textbf{协议（提议）}：\text{任何候选须在}\ \textbf{已知}\ \beta\ne\tfrac12\ \text{的算术对象} \text{上、}\textbf{仅用素数侧数据} \text{就"报火"}✓$$
$$\qquad \text{检验床候选}：\textbf{Davenport--Heilbronn}（\text{有 FE、无 Euler 积、}\textbf{已证有离线零点}）、\textbf{Epstein }\zeta（\text{有 FE 却有离轴零点，Potter–Titchmarsh}）✓$$
$$\qquad \text{判死逻辑}：\text{若某候选在}\ \zeta\ \text{与检验床上}\ \textbf{输出同形} \Longrightarrow \text{它}\ \textbf{没能} \text{在已知 } \beta\ne\tfrac12\ \text{处报火} \Longrightarrow \textbf{不是检测器} \Longrightarrow \textbf{对 ζ 也不可能}✓✓$$
$$\qquad \qquad \Longrightarrow\ \text{可}\ \textbf{零成本} \text{在}\ \textbf{动手前} \text{杀死}\ \beta\ \text{盲候选}✓✓$$
$$\textbf{⚠️ 档案已登记的限制（`CLOSED-ROUTES-MAP:2357` 逐字要点）}：\text{已知 off-line 实例}（\text{Davenport--Heilbronn／Epstein／Beurling 型}）\ \textbf{全无 Euler 积}✓$$
$$\qquad \Longrightarrow\ \text{该角}\ ＝\ \boxed{\text{"Selberg 类成员有}\ \textbf{可证 off-line 零点}\text{"级问题}}✓✓$$
$$\qquad \Longrightarrow\ \textbf{两分筛查（本档）}：$$
$$\qquad \qquad \textbf{(a)}\ \text{不依赖 Euler 积的候选} \Longrightarrow \text{检验床}\ \textbf{可测}✓$$
$$\qquad \qquad \textbf{(b)}\ \text{依赖 Euler 积的候选} \Longrightarrow \text{检验床}\ \textbf{不可测}，\ \text{而它们}\ \textbf{正是 canonical 类} \Longrightarrow \beta\ \text{盲（}\text{`FZ-3` ①}）✓✓$$
$$\qquad \Longrightarrow\ \text{即：}\boxed{\text{筛查本身揭示分裂}：\text{可测的一侧}\ne\ \text{canonical 的一侧}}✓✓$$

## §3 ⭐ 用它重扫本轮九条（结果为**修正**，非"全盲"）

| 路线 | 输出类型 | 能在检验床报火？ | 判定 |
|:--|:--|:--:|:--|
| `K1` `valuation-lift` | 运算结构（半环商）| ✗（聚合／结构型）| **`β` 盲** |
| `K2` 行列式编码 | 编码 | ✗ | **`β` 盲** |
| `M1` hereditary | 表示集上的一致界 | ✗（`A6` 对 `β` 零响应）| **`β` 盲** |
| `M2` 非交换插值 | 生产模型 | ✗ | **`β` 盲** |
| `R1` 可逆参考 | 尺度迁移 | ✗ | **`β` 盲** |
| `W4-1d` 逐 `γ` 探针 | 频率读数 | ⚠️ 需零点 ⟹ `C2` FAIL | **`β` 盲（读出级）** |
| **`S1`／`C6`** `DBN`／`Λ` | ⭐ **形变参数** | ✅ **会对离轴零点有响应** | ⭐ **`β` 可见** |
| 经验线 `C-97`／`C-98` | 统计量 | ✗（`γ` 侧）| **`β` 盲** |
| `C-113` 的"模长通道" | （重述）| — | 与 `F4` 同址 |

$$\Longrightarrow\ \boxed{\text{八条}\ \beta\ \text{盲}\ ＋\ \textbf{一条}\ \beta\ \text{可见（`DBN`／}\Lambda\text{）}};\ \text{后者的墙＝}\textbf{端点退化}（\text{`C-111`}），\ \textbf{非}\ \beta\ \text{盲}✓✓$$

## §4 修正后的目标（**本档真正的产出**）

$$\text{目标由「找新对象」改为}：\boxed{\text{找一种输出类型，使其同时：(i) 在检验床上报火（β 可见，过 F1/F7）；(ii) 在 ζ 端点可证（非 C-111 型退化）}}✓✓$$
$$\qquad \text{已知唯一满足 (i) 的实例}＝\text{DBN／}\Lambda（\text{形变参数}）;\ \text{其 (ii) 失败} \Longrightarrow \text{目标＝}\boxed{\text{另一种}\ \textbf{`β` 可见输出类型}}（\text{非形变参数}）✓✓$$
$$\qquad ⚠️\ \text{且须过}\ \text{`F4`}（\text{非模长型}）、\text{`F3`}（\text{钉点不钉轴}）、\text{`F1`}（\text{非正性}）、\text{`F2`}（\text{非坐标型}）✓$$
$$\qquad \Longrightarrow\ \text{这比"再找一个对象"精确：}\textbf{要求的是一种输出类型}，\ \text{不是又一个构造}✓✓$$

## §5 边界

- `[档案]` §1 的 `F1`–`F8`、§2 的 `:2357`、`E4-1:44` 均**逐字引用** ✓
- `[本档]` §2 的检验床协议、§3 的重扫表、§4 的目标改写为**本档工作** ✓
- ⚠️ §3 中 `S1`／`C6`"会对离轴零点有响应"是**结构性判断**（形变参数定义在任意 `Ξ` 型函数上）；**未实测** ⟹ `[待核]`✓
- **不声称**：检验床协议完备 ✗（其限制已登记）；不证 RH ✗
- **纪律**：先查后判（R-1 ✓，**先跑后写** ✓）；**未用 RH 作推导** ✓

## §6 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-18 16:4x）`[纪律]`（先跑后写）

```
技术词 F-先/M-后        命中文件数=1  :: ./C114-method-correction-F-first-M-second-and-beta-visibility-testbed.md
技术词 检验床协议        命中文件数=1  :: ./C114-method-correction-F-first-M-second-and-beta-visibility-testbed.md
技术词 β 可见输出类型     命中文件数=1  :: ./C114-method-correction-F-first-M-second-and-beta-visibility-testbed.md
```
**读数（按实测）**：三项均＝**1 档（仅本档）⟹ 本档新增措辞** ✓
