# 编号领用制（ID ALLOCATION PROTOCOL）—— 防跨会话撞车

**建立** ✓ 2026-09-14（唐先生："编号撞车问题必须解决，不然会覆盖不该覆盖的文本和内容"）｜执行 ✓ 小灵
**性质** ✓ 工程纪律档（非数学结论）｜**依据** ✓ 2026-09-14 已发生 **5 次**撞车（E122／E127／E128／E129／E130）

---

## §0 风险（为什么必须机制化）

```
⚠️ 撞车的真实危害**不是**"名字难看"，而是**越界改写他人成果** ✓：
   · 一路以为 `docs/E130-*.md` 是自己的 ⟹ 追加载重【勘误】/ 重命名 / 批量 sed ⟹ **覆盖或污染他路内容** ✗
   · `git add` 用通配 ⟹ 把他路**在制品**（untracked）一起提交 / 或 `git mv` 误改他路文件 ✗
   · 交叉引用失效（他路文档指向同号但不同物的档）✗
⟹ 结论 ✓：**必须"先领号后建文件"**，且**任何批量操作前先跑检查器** ✓
```

## §1 三条硬规则

```
【规则 1 · 先领后建 ✓】新建任何 `E###-` 档/**`E###_` 脚本**前，先领号：
     $ scripts/id_claim.sh <stream> <slug>      # 输出一行 "E###"（原子占位 ✓）
   领到号**之后**才创建文件 ✓；**禁止**自行猜号/沿用旧号 ✗
【规则 2 · 分号段 ✓】每个工作流**只能在自己号段内领号**：
     · main  = **E###**（主线工作流：E100–E13x 零和/渐近律线等 ✓）
     · audit = **V###**（本会话：小灵 —— 审计/核验/裁决线 ✓，已用 V101–V107 ✓）
     · spare = **其它字母前缀**（如 `Z###`；需要时**先登记** ✓）
   ⚠️ **为何不用 E3xx**：项目已有 `E30/E32/E36/E39/E39b` ⟹ `docs/E30*` 通配会**误命中 E301+** ✗
      （＝"覆盖不该覆盖"的真实入口）⟹ 迁移改用 **V 系列**：`V*` 与现有 E/p/B/R/A/ALIGN 等**前缀零重叠** ✓
   非本体系命名（`gate<n>-`／`R-A1-`／`ALIGN-*`／`p<n>-`／`B<n>-`／`MASTER-*`／`HOT-STATE` 等）**不受本约** ✓
【规则 3 · 提交前自检 ✓】任何提交**前**跑：
     $ python3 scripts/check_ids.py     # 期望 exit 0（无同号多文件 ✓）
```

## §2 台账与原子性

```
· 台账 ✓ `docs/ID-CLAIMS.tsv`（列：ID｜stream｜slug｜时间 ✓）—— **唯一权威** ✓
· 原子占位 ✓ `docs/.idclaims/E###.lock/`（`mkdir` 原子 ⟹ 并发下不会双领 ✓）
· 占用判定 ✓ ① 锁存在 ② `docs/E###-*` 存在 ③ `scripts/E###_*` 或 `E###-*` 存在 ⟹ 任一即跳过 ✓
```

## §3 撞车响应（发现撞车时）

```
① **后到者改号** ✓（不改先到者 ✓）；② 改号**必须以【勘误】留档**（T10 ✓）并写映射 ✓；
③ **严禁**：对非本流文件追加载重/改名/注入内容 ✗；④ 若产权不明 ⟹ **停手并上报** ✓（不猜 ✓）；
⑤ 批量操作（`git add`／`git mv`／`sed -i`／重命名）**一律显式路径**，禁通配 ✗
```

## §4 历史撞车与迁号映射

| 撞车 | 号 | 本会话（audit）原用 | 他流（main） | 处置 |
|:--|:--|:--|:--|:--|
| 1 | E122 | —（仅登记行） | `scripts/E122_random_or_coherent.py` 等 | 登记行保留，标注来源 |
| 2 | E127 | `V105-L2-carrier-migration-survey.md` | `scripts/E127_multiscale.py` | 本会话迁号 → **E305** |
| 3 | E128 | `E128-L3-Q3-...`（已改 E130） | `docs/E128-bessel-form-and-multiscale.md` | 已改号（→E130→**E306**） |
| 4 | E129 | — | （他流占用） | 无需处置 |
| 5 | E130 | `E130-L3-Q3-...` | `docs/E133-identity-rigorous.md` | 本会话迁号 → **E306** |

**本会话（audit 流）迁号映射（2026-09-14 ✓）**

```
E123 → **V101**   E124 → **V102**   E125 → **V103**   E126 → **V104**
E127 → **V105**   E130 → **V106**   E131 → **V107**
（E132（TP₅/O₃ 测试）⟹ **已迁 V111** ✓（二次迁号，V110 先到者＝R1 档 ✓）；**V108 已改派** `E102 §8` 靶1 ✓；**V109 已改派** 覆盖论证章程 ✓ ⟹ 三者均以台账 `ID-CLAIMS.tsv` 为准 ✓）
（迁号后：本会话新档一律从 E3xx 领取 ✓；旧号**保留为历史别名**，引用旧号须转写 ✓）
```

## §5 给主线（main 流）的建议

```
1. 主线亦请使用 `scripts/id_claim.sh main <slug>` 领号 ✓（避免与自身历史号/agent 号冲突 ✓）
2. 主线在制品（untracked）请**尽早入库或标注 in-flight** ✓（本轮曾两次遇到他路在制品与被占号 ✓）
3. 若主线希望保留"从 E122 起连续编号"的习惯 ⟹ 本会话已全部让出 E1xx ✓，可连续使用 ✓
```


## §6 教训（2026-09-14 ✓ 实际发生）

```
⚠️ **手工迁号 ≠ 领号** ✗：本次 TP₅/O₃ 档**手工 `mv` 到 V110**（未领号），而 R1 扫描档**先经 `id_claim.sh` 占了 V110**
   ⟹ 按 §3① **后到者（我方手工档）改号 V110 ⟹ V111** ✓（**协议自我适用** ✓，不豁免 ✗）
⟹ **补强规则 ✓**：① **任何改号/新建，一律先经 `id_claim.sh`**（或至少先 `grep` 台账 + `ls` 目标号）✗禁手工 ✗；
   ② **并发窗口**（两路同时工作）下，**只有锁（`.idclaims/`）＋台账**能裁决先后 ✓ ⟹ 手工操作必须让位 ✓；
   ③ 撞车**登记在案**（台账 legacy-alias 行 + 本档勘误 ✓），便于事后追溯 ✓
```


## §7 跨线同题查重（2026-09-14 ✓ 实际发生三例）

```
⚠️ **教训 ✓**：本日**三例**同一问题被两条线同时开工 ✓（`V109`↔主线 RS 线（`E146`）｜`V115`↔`E146` 的方向 (ii)｜
   **`V118`↔主线 `E147`（第四种侦测方式）** ）⟹ 虽各自判为"补一步/口径不同"，但**存在重复劳动风险** ✗
⟹ **补强规则 ✓（任务书模板必含 ✓）**：任何新任务开工前，**先读 `docs/MASTER-STATUS-AND-CLOSURES.md`（路径关闭总表 ✓）**，除登记册/总册/宪法外，**必须**：
   ① 先看 `docs/` **近 1 小时内**（`find docs -maxdepth 1 -newermt '-60 minutes'` ✓ 或 `ls -t docs | head -20` ✓）主线新建档；
   ② 对**同对象**关键词 `grep -l`（不限本会话号段 ✓）；
   ③ 若命中同题 ⟹ 在交付物 §① 明确写出**既有档与净增量**；**净增量＝零 ⟹ 直接归档停止** ✗（不得"补一步"凑数 ✓）
【与 §3 撞车响应的区别 ✓】§3 处理**同号撞车**（编号层）；本节处理**同题撞车**（内容层）✓ —— 两者须分别执行 ✓
```

> **2026-09-16 治理行**：审计线编号带由 **199 槽（V101–V299）** 扩至 **299 槽（V101–V399）** —— 起因：V299 用满（`id_claim.sh` 返回 "no free id in band 100xx"）；由小灵执行、记入本档与 `scripts/id_claim.sh` 注释 ✓
