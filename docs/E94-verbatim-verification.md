# E94 · **三项逐字核验** ⚠️ —— ① 对象不匹配（真问题 ✗）｜② 截断无害（已钉死 ✓）｜③ 常数阈值更正 ✓

> 委托 ✓ 唐先生（20:52 指示：钉死两条硬条件 ＋ 上传 Karatsuba PDF 第 26–27 页 ✓）｜执行 ✓ 小灵
> 脚本 ✓ `scripts/E94_truncation_tail.py` 输出 `scripts/E94_truncation_tail.txt` ✓
> 纪律 ✓ 未用 RH ✓；未跑 Lean ✓

---

## 0. 三条结论（✓）

```
⛔ **① Theorem 4 的对象是 $S_1$，不是 $S$** ✗ —— **唐先生预判成立 ✓✓**：
   OCR 逐字被积函数 ✓：$\bigl[S_1(t)+\frac1\pi\int_0^\infty(\cdots)d\alpha-\frac1\pi\sum_{n\le x}\frac{\cos(t\log n)}{\sqrt n\log^2 n}\bigr]^{2k}$
   ⟹ **分母 $\log^2n$ ＝ $S_1$ 的签名** ✓（$S$ 级应为 $\log n$ ✓）
   ⟹ **E93 把 Theorem 4 用在 $S$ 的余项上 ＝ 对象不匹配** ⚠️ ⟹ **E93 降级为「强候选闭合」** ✓
✅ **② 截断 $x\le H$：【无害】（已钉死 ✓）**：
   (A)：$32.964\to\mathbf{29.527}$ ✓（尾段 $e^{11}<p\le H$ 仅 $15.26$ ✓，占 10.4% ✓）
   (B)：**$2.398236\times10^{4}$ 不变** ✓（全部驻相素数 $p\le e^{11}=59874<H=7.9103\times10^{5}$ ✓）
   素数侧合计：$2.401532\to2.401189\times10^{4}$ ✓（**差 0.014%** ✓）⟹ **总账与余量 4.1265× 不变** ✓✓
✅ **③ 常数阈值更正 ✓**：$\boxed{C_{\rm Th4}\le\mathbf{19.785}}$ ✓（我 E93 写的 4.45 ✗ **忘了常数在根号下** ✓）
```

## 1. ① Theorem 4 逐字（✓ OCR ＋ 数学结构双重确认 ✓）

```
原文（印刷页 927 ✓）：
"Theorem 4. Suppose that H = T^{27/82+a}, 0 < a < 0.001, T > T1(a) > 0, k is a natural number,
 T^{a/k} < x < H^{1/k}. Then  ∫_T^{T+H} [ S1(t) + (1/π)∫_0^∞ ... dα − (1/π)Σ_{n≤x} cos(t log n)/(√n log² n) ]^{2k} dt
 = O(H L^{-2k}),  where the constant in the symbol O depends only on a and k."
```

| 要素 | 内容 | 用途 |
|:--|:--|:--|
| **对象** | $S_1(t)+\frac1\pi\int_0^\infty(\cdots)d\alpha-\frac1\pi\sum_{n\le x}\frac{\cos(t\log n)}{\sqrt n\log^2 n}$ ✗ | **＝ $S_1$ 级，非 $S$ 级** ⚠️ |
| **范围** ✓ | $H=T^{27/82+a}$，$0<a<0.001$ ✓ | 我们的 $H\asymp T$ 在 $a\to1$ 端 ✓ |
| **截断** ✓ | $T^{a/k}<x<H^{1/k}$ ✓ | $k=1$：$x<H$ ✓ |
| **界** ✓ | $O(HL^{-2k})$ ✓ | $k=1$：$O(H/L^2)$ ✓ |
| **常数** ✗ | 「depends only on $a,k$」——**不显式** ✗ | 未知 ✓ |
| **归因** ✓ | *"Theorem 4 is the basis of the proof of Theorem C"* ✓ | **$S$ 级界须在 Theorem C 的证明里** ✓ |

```
⭐ **关键区分 ✓**：**Theorem C 的对象【是 $S$】** ✓（$\int_T^{T+H}|S|^{2k}dt=c_kH+O(H/\log T)$ ✓）
   ⟹ **E84b/E90 的接口本身不受影响** ✓（它用的是 Theorem C ✓）
   ⟹ 受影响的只是 **E93 的【新用】Theorem 4 去控制余项** ✗
⚠️ **故 E93 的 $2.3351\times10^5$ 目前【不能使用】** ✗ —— 除非在 Theorem C 的证明中找到 $S$ 级余项界 ✓
```

## 2. ② 截断重算（✓ 脚本产出 ✓）

| 量 | $x=T_0=1132490$ | $x=H=791031$ |
|:--|:--|:--|
| (A) 非驻相 $\sum2c_p/(|\phi'|_{\min}+\log p)$ | $3.296411\times10^{1}$ | $2.952720\times10^{1}$ |
| 尾段 $e^{11}<p\le H$ 的贡献 | — | $1.525737\times10^{1}$（占 (A) 10.43% ✓） |
| (B) 驻相（$p\le e^{11}$ ✓） | $2.398236\times10^{4}$（6047 个 ✓） | **同值 ✓** |
| 素数侧合计 | $2.401532\times10^{4}$ | $2.401189\times10^{4}$（差 0.0143% ✓） |
| 余项（Theorem 4 形状 ✓） | $2.335084\times10^{5}$ | 同 ✓ |
| **总账 ／ 允许 ／ 余量** | — | $2.575203\times10^{5}$ ／ $1.062658\times10^{6}$ ／ **4.1265×** ✓✓ |

## 3. ③ 常数阈值（✓ 更正 ✓）

$$\sqrt{C_{\rm Th4}}\cdot\underbrace{2.335084\times10^{5}}_{\text{余项单元}}+\underbrace{2.401189\times10^{4}}_{\text{素数侧}}\le1.062658\times10^{6}\ \Longrightarrow\ \boxed{C_{\rm Th4}\le\mathbf{19.785}}$$

```
⚠️ **更正 ✓**：E93 写的 $C\le4.45$ ✗ —— 那是 $(1.06266e6-2.4015e4)/2.3351e5$ 的一次幂 ✓
   **忘了常数在【根号下】** ✗ ⟹ 正确为**其平方** $=19.785$ ✓（**唐先生的 20.1 正确** ✓✓）
⟹ **阈值比 E93 判断的宽松 4.4 倍** ✓ —— **这是今日第三次对该类数值的口径更正** ✓
```

## 4. 当前准确状态（✓ 照唐先生表述 ✓）

$$\boxed{\text{E93 ＝ \textbf{强候选闭合}，尚差：① Theorem 4 对象的 $S$ 级对应 ✗｜② 常数 ✗（需 $C_{\rm Th4}\le19.8$ ✓）｜③ 截断 ✓（已钉死 ✓）}}$$

```
✗ **未解决**：$S$ 级余项界 —— 三个候选途径 ✓：
   (i) 在 **Theorem C 的证明**（页 927–931 ✓）中找出 $S$ 级余项界 ✓ —— **最可靠** ✓
   (ii) 直接从 $S$ 的显式公式结构重做非共振论证 ✓
   (iii) 用 Theorem C 自身的误差项 $O(H/\log T)$ ✓ —— 但那只给 $\|S\|_2$ ✓，不给 $\|R\|_2$ ✗
✅ **未受影响的** ✓：E84b/E90 的接口 ✓（用 Theorem C ✓，对象是 $S$ ✓）；E92 的素数侧非共振 ✓；截断结论 ✓
⚠️ **未宣布 T1 已证** ✗；**未宣布 E93 成立** ✗
```

## 5. 纪律（✓）

```
✓ **未用 RH** ✓；**未跑 Lean** ✓
✓ **自我更正 1 处** ✓（$C_{\rm Th4}$ 阈值：4.45 ✗ → **19.785** ✓）
✓ R1–R7 ✓：脚本入仓 ✓、输出入仓 ✓、docstring ✓、`check_archive.py` ✓
✓ **附：已渲染 Karatsuba 论文页 27／28／29（400 dpi）供唐先生逐字核对** ✓
```
