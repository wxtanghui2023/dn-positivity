已查地图：命中（`AUDIT-POST-MORTEM-were-astra-problems-considered`／`ASSET-PROBLEM-REVERSE-AUDIT-table`／`TLDC-1-PHASE-V1-FREEZE`／`C380-MASTER-LEDGER-FREEZE-2026-09-22`）⟹ 引用，不开新案
D0: 本档对象 = 登记新硬门 `P1`–`P6` ＋ 维度 `M`；**工具链再审计**（工具 → 已产出的独立对象 → 独立定理化程度 → 缺口）；`O-TILT` 按新标准再读
D1: 0 （[REVIEW] 轮次：登记与再审计，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **工具链再审计：哪些工具已具"独立定理"能力，却一直被当 RH 辅助用**

## §1 新硬门（照录）

```
【新维度 `M`】 $$\boxed{\text{独立数学产出能力：删除 RH 后，机制＋工具＋对象组合仍能否形成可陈述、可证明、可验证、可能新的数学命题}}$$ ✓
【`P1`–`P6`】**`P1`** 独立问题是否明确？｜**`P2`** 真正控制量是什么？｜**`P3`** 现有工具是否直接作用于该控制量？｜**`P4`** 是否存在有限压缩/可认证证书？｜**`P5`** 能否形成独立定理（而非 RH 重编码）？｜**`P6`** 即使 RH 永不解决，该结果是否仍成立且有价值？ ✓
【并用规则】 **先跑 `P1`–`P6`，最后才问 RH 相关性**（此前顺序**反了**）✓✓
【判词纪律】 "不能推进 RH" ⟹ 只推出"**不能作为当前 RH 主线**"，**不得**推出"该对象无独立价值" ✓✓
```

## §2 ⭐ **工具链再审计表（本档核心，实例全部取自本线既有档）**

| 工具 | 已产出的**独立**对象（本线实例） | 独立定理化程度 | 缺口 |
|:--|:--|:--|:--|
| 有限枚举＋精确整数判定 | **`(2,2,1)` 精确代数封口**；**`k=4` 证书（118,068 boxes，0 residual）** | 已是"机器可验证陈述" ✓ | 问题定义来自 RH 侧 ⟹ 需换载体量（`P1`） |
| **区间/盒覆盖认证方法** | 通用化：`\forall`-判定 → 有限覆盖＋区间界＋见证＋独立校验器 | **方法本身可独立成篇**（verified numerics）✓ | 缺一个**非 RH** 载体问题（`P1`/`P3`） |
| **EDS＋`r_q` 记账** | ⭐ **`EDR` 有限容量定理**：固定 `(E,P,S)`，`|Z_S(E,P)|\le N_0+|S|`（**纯独立**，无 RH 成分）✓✓ | **已具独立定理形态** ✓✓ | 显式 `N_0`／出版级细化（`P6` 已满足） |
| **rank–trace／惯性（Lemma R，已证紧）** | **独立线性代数定理** ✓ | 已具定理形态 | **等号分类未做**（`P1` 计划，长期未启动） |
| **Lean 形式化（已补 Mathlib 缺口）** | **Sylvester 惯性 ＋ von Neumann 迹不等式的机检化** ✓✓ | **可直接贡献上游** ✓✓ | 从未提交、未审计（`P3`/`P4` 满足） |
| **形式化证明模式（`TLDC` Core）** | **无公理、双实例共享同一 Core**（`A2`＋`Liouville`）✓✓ | **独立逻辑/CS 产物** ✓✓ | 未写成论文/未提交 |
| 符号消元＋精确结构公式 | 如 `g_w(10)=w\cos(2\pi/11)-\cos(\pi/11)` 型恒等式 ✓ | 独立恒等式 ✓ | 是否已知／是否值得（`P1`） |
| 精确穷尽＋反例搜索 | `D(-3)`：148 边／76 三角／`#K_4=0`（严格有限域负结论）✓ | 独立有限结论 ✓ | 全域问题仍开放（`P6` 已满足） |

## §3 ⭐⭐ **三个"已具独立形态、却从未按非 RH 标准评估"的候选**

```
**【候选 1｜`EDR` 有限容量定理】** `|Z_S(E,P)|\le N_0+|S|` —— 自足、可验证、非 RH；（`P1`）问题＝"EDS 在固定有限素数集下的有限容量" ✓
**【候选 2｜Lemma R ＋ 其 Lean 机检化】** —— 纯线性代数；（`P1`）问题＝**等号/锐性分类**（本线 `P1` 计划，从未启动）✓
**【候选 3｜`TLDC` 形式化证明模式】** —— 无公理双实例共享 Core；（`P1`）问题＝"可迁移下降证明模式的形式化" ✓
【共同点】 三者**都不需要 RH**，且**都已有实体产物**；此前**均未被按 `P1`–`P6` 评估过** ✓✓
```

## §4 **`O-TILT` 按新标准再读**

```
【新问】 "取向敏感局部计数"本身是否对应独立数学问题？ ⟹ 其一般数学对应物＝**带符号的局部计数/能量泛函**；本档**未见明确的独立待解问题** ⟹ 归档 ✓
【但归档理由必须写对】 ＝**"无独立问题（`P1` 未过）"**，**不是**"对 RH 无效" ✓✓（按 §1 判词纪律）
```

## §5 下一步

```
**从工具反推问题**：对 §3 三候选与 §2 的"认证方法"各问 `P1`–`P6`；**先做 `P1`**（是否存在明确独立问题）✓
【边界】 §1 为新门照录；§2 各行实例均取自本线既有档（可核）；§3/§4 判定为**本档自行推导**；未制造候选／未启动搜索／未碰 RH 总攻。
