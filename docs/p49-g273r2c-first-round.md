# P49-G2.7.3-R2c：真实 b_n 近似分析——第一轮（需 PDF 精读）

> 2026-09-02 14:06 · Weil 分布结构 · 近似模拟 · R2c 待定

## 已获取（论文 Section 3——Weil 显式公式）
- **Σ_ρ f̃(ρ) = ∫₀^∞ f + ∫₀^∞ f^♯ − Σ_v W_v(f)**（3.2）
- **W_p(f) = (log p)Σ_{m≥1}(f(p^m) + f^♯(p^m))**（3.3——素数项）
- **W_R(f) = (log 4π + γ)f(1) + ∫₁^∞ (f + f^♯ − 2f(1)/x)dx/(x−x⁻¹)**（3.4——archimedean）
- D = log*(Ψ^♯)——b_n = −(1/π)∫₀^L sin(2πny/L)D(y)dy——L = 2log λ
- 论文注明：显式公式对卷积 f**g（平方可积紧支撑）绝对收敛——无发散问题

## 近似模拟（素数项主导——λ=13, p≤169）
- b_n ≈ −(1/π)Σ_{p≤x}(log p)sin(2πn·log p/L)——δ 在 log p 的 Fourier
- b_1..5 = 24.2, 17.8, 12.5, 8.8, 7.9（正）
- **符号序列 n=1..20：++++++++++++++++----（前 16 正——后 4 负——非交替——准周期）**

## ⭐ 关键观察
- **真实 b_n（素数 δ 的 Fourier）非简单交替（alt 型）**——log p 不规则采样——准周期振荡
- **⚠️ 但——近似模拟不足判定**：完整 D 含 archimedean（W_R——平滑修正）+ 精确 QW 定义（3 节后半）——且 a_n = 2∫(1−y/L)cos D 完全未算
- **论文 10⁻⁵⁵ 精度暗示 b_n/a_n 有深层结构**（非准随机——尽管表面符号序列不规则）
- **R2c 分叉三情况——当前无法确定**（需完整系数）

## 判定
- **R2c 第一轮 = 近似结构观察**（b_n 非简单交替——准周期——但——完整判定需论文 Section 3-5 精读）
- 诚实：近似模拟（素数项主导 + 一维 δ）不足以判定 R2c 分叉——**需 PDF 本地精读**（HTML 750KB 截断——完整 QW/D 定义 + a_n/b_n 推导）

## 下一步候选
- (a) 下载 PDF 本地精读（Section 3-5——完整 QW/D——精确 a_n/b_n——Δ_N 计算——决定性）
- (b) 接受 R2c 待定（G2.7.3 依赖完整系数——近似不足以判分叉）
- (c) 唐先生指示
