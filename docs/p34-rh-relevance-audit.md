# P34 RH 相关性审计：K_X 是 σ=1 边界算子——与 RH 连接是"间接检测"

> 2026-09-02 08:40 · 唐先生"继续" · 优先级 (c)：元层面审计

## 核心结论
**P34 的素数侧算子 K_X（及其 renormalized 版本）是"σ=1 边界 Fourier 算子"——不是"RH 算子"。与 RH 的连接是"间接检测"（PNT 误差速率）——不是"直接排除"。**

## ① a_k(X) 的 σ 位置（部分求和分析）
```
Σ_{p≤X}p^{ik} = ∫_2^X t^{ik}dπ(t)——π(t)~t/log t（ζ 在 s=1 的极点结构）
→ 主项 X^{1+ik}/((1+ik)log X)——σ=1 边界主项——不是临界带
```
- **a_k 编码的是 ζ 在 s=1 的极点结构（PNT 主项）——不是 Re s=½ 零点**
- 端点主导（a_k ~ (cos(k log X)+k sin(k log X))/(1+k²)）正是这个 σ=1 主项的直接表现

## ② 权重 vs 数据源——w=p^(-1/2) 是"滤波器"不是"数据源"
- K_X 核 = w(p)w(q)Σa_k cos(kΔ)——**a_k（素数 Fourier 平均）永远是 σ=1 数据**
- w=p^(-1/2)（ε=0）把"特征"拉向 σ=½——但——**a_k 本身仍是 σ=1 的**
- 权重改变"滤波器"——不改变"数据源"——这解释了为什么权重修正实验（ε>0）不改变平台

## ③ 与零点的唯一连接——PNT 误差速率（检测层）
```
a_k(X) = 端点主项 + o(1)——o(1) 来自 PNT 误差 E(X) = π(X) − Li(X)
部分求和：a_k^ren(X) ~ X^(β_max−1) 型剩余（RH 下 X^(−1/2)——β_max>½ 时更慢）
```
- **连接 = "速率"（检测 β_max——β_max 敏感）——不是"排除"——检测≠排除**

## ④ 数值验证（X=10⁶）
| k | a_k^ren | X^(−1/2)（RH 尺度） | X^(−0.4)（β=0.6 尺度） |
|---|---|---|---|
| 1 | −0.0169 | 0.0010 | 0.0040 |
| 10 | −0.0013 | 0.0010 | 0.0040 |
- 剩余量级 ~X^(−0.4)-X^(−0.5)（β_max 敏感区）——"速率检测"指纹确认

## ⑤ 审计结论
1. **K_X 的数据（a_k）编码 ζ 在 s=1 的极点结构（σ=1 边界对象）——不是临界带零点**
2. **canonical limit 失败（平台）是 σ=1 现象（端点振荡 + PNT 结构）——与 RH 无直接连接**
3. **与 RH 的唯一连接：PNT 误差速率（a_k^ren ~ X^(β_max−1)——检测层——β_max 敏感）——是"检测"不是"排除"**
4. **P34 排除了"素数 Fourier 型二次型"作为 RH 工具——是"检测≠排除"元结论的又一实例**
5. **P34 不是 RH 的排除机制——是"σ=1 算子研究"——价值在排除（不是接近 RH）**

## ⚠️ 诚实边界
- σ 位置的"定位"是启发式（部分求和主项分析）——非严格定理
- "a_k^ren ~ X^(β_max−1)"是尺度估计（数值——非证明）
- 审计是"元层面"判断——不是数值实验的替代
- 但这回答了元问题：**P34 与 RH 的连接——间接（检测层）——直接连接从未建立**

## ⭐ 元意义
- P34 系列（P34-A 到 D3.1 + 权重修正）本质是"σ=1 边界算术算子的完整审计"
- 它的所有结论（平移不变正定/有限秩/canonical limit 失败/平台/权重无关）都是"σ=1 现象"
- **不应把 P34 的结论误读为"RH 相关"——它是"排除一个工具"（又一个"看起来像 RH 工具——实际是 σ=1 现象"）**
- 与 P5.8 一致：F_U 对 RH 无判别力——P34 是这个元结论的一个具体实例

## 下一步候选
- (a) P34 系列最终封档（含 RH 相关性审计——完整"排除"记录）
- (b) 转向其他方向（唐先生指示）
- (c) 其他
