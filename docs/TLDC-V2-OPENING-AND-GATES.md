已查地图：未覆盖（关键词: 第三实例|新数学结果|proof-engineering|数学方法）—— 可开档，首行须照抄本行
# TLDC / **Phase V-2 开局与门**（寻找**第三个独立问题**；本档**零计算**）

上位令：`TLDC-1-PHASE-V1-FREEZE.md` §0（唐先生 2026-09-22 20:21 ✓）
D0: 本档对象 = **TLDC/Phase V-2 阶段开局与准入门**（档案已有对象 `TLDC 两实例机制` 的**阶段推进关系**；非重命名、非新对象）
D1: 0
FREEZE-ACK: 本档为**门设计**（零计算、零候选执行 ✓）

---

## §0 阶段目标（V-1 与 V-2 的区别，逐字沿用令 ✓✓）

```
【V-1 目标（**已完成**）】 证明 **TLDC 不是 Liouville 特例** ✓
【V-2 目标（本阶段）】 寻找**第三个独立问题**，判据 =
   **TLDC 能否产生一个尚未被标准证明解决、或至少能导出一个新的可独立发表的数学命题？** ✓✓
【升级语义】
   若第三实例 = **已知定理 ⟹（TLDC）⟹ 同一个已知定理** ⟹ TLDC **仍是 proof-engineering schema** ✓
   若出现 **TLDC ⟹ 新的数学结果** ⟹ **那才是真正的升级点**（proof schema ⟹ **数学方法**）✓✓
【特别禁令（照录）】 ⛔ **不要因为有了 TLDC，就立即寻找 RH 接口** ✓
   （RH interface 保持 **NOT STARTED** ✓）
```

## §1 **决定性判据**（三级；只有 ③ 算升级 ✓✓）

| 级 | 形态 | 判定 |
|---|---|---|
| ① | 已知定理 ⟹（TLDC）⟹ 同一已知定理（**换证**） | **不升级**：TLDC 定格为 proof-engineering schema（**此为有价值的负结论** ✓，非失败 ✗） |
| ② | 已知定理 ⟹（TLDC）⟹ **更强／更一般的新陈述** | **弱升级（GAP）**：须核对该新陈述是否**真新**（查 NO-GO 地图 ＋ ID-CLAIMS 台账 ✓） |
| ③ | （TLDC）⟹ **新数学结果**（此前未解，或此前无独立证明） | ✅ **升级 = 数学方法** |

## §2 候选准入（每条候选须**填满 9 项**，缺一即 DEAD ✓）

```
① 问题定义（自足陈述，不依赖 TLDC 术语）
② 独立来源（**非** Liouville／Fermat 派生；**非**经典教材已完整解决者 ✓）
③ 非旧量重命名（对照 NO-GO 地图 CLOSED-ROUTES-MAP ＋ ID-CLAIMS.tsv ✓）
④ 四槽可实例化性：C（独立压缩＋**定量界 B**）／R（失败保持）／D（严格下降）／K（送回原类）✓
⑤ **D-1 门**：压缩步须有**独立于结论**的证明（B5 即反面教材：无独立压缩 ⟹ 伪 TLDC ✓）
⑥ **内生结构接口**：能否提出 TLDC 内部必然产生的新对象？（C-380 教训：**无内生接口 ⟹ 无桥**；**不得外贴** ✓）
⑦ 最大失败点（**一条**，写明；须是可判定的具体机制 ✗）
⑧ STOP 条件（何时停手并归类）
⑨ 计算需求（本阶段应为 **0**；若需计算 ⟹ **另开 Scale Gate**，不得默认开算 ✓）
```

## §3 纪律（硬 ✓✓）

```
① 候选 **≤ 3**；逐个走 **killer board**；只有**唯一幸存者**才可进 **Scale Gate** ✓
② 本阶段 **零计算**（仅纸面／设计）✓
③ ⛔ **不得**为凑第三实例把已知定理**包装**成"新结果"（自我升级幻觉；须写"已知还是新"的自证句 ✓）
④ ⛔ **不得**因 TLDC 存在而寻找 RH 接口 ✓（若研究者认为某候选必然通向 RH，须**显式**标注为 GAP 而非接口 ✗）
⑤ **不动**：C-380（**HOLD**）／旧桥（**CLOSED**）／乙-3 冻结结论（**FROZEN**）✓
⑥ 与 V-1 十候选表的关系：V-1 做过"机制存在性筛选"；V-2 **不得机械复用其名单**，
   须以**"是否产生新结果"**重判（前档作**引用** ✓）
⑦ 每条候选须写**独立压缩候选**与**最大失败点**各一行，便于快速杀 ✓
```

## §4 出口（三选一）

```
EXIT-A **PROMOTE**：某候选证实可产生新结果（判据 ③）⟹ 开 Scale Gate ✓
EXIT-B **DEGRADE**：三候选皆 DEAD ⟹ TLDC **定格为 proof-engineering schema**
   （**保留** `TLDC-Core` 资产 ✓；记为**有价值的负结论** ✓）
EXIT-C **GAP／HOLD**：信息不充分 ⟹ 标注"信息不充分"并停 ✓
```

## §5 边界与回查（✗✓）

```
✗ 本档零计算、零候选执行；不改任何既有状态（C-380／旧桥／RH interface／乙-3 冻结 ✓）
✗ 不声称 TLDC 已是"数学方法"（**升级与否由 V-2 判据决定** ✓）
【回查（`scripts/tech_word_check.sh`，**先跑后写** ✓）】
技术词 第三实例      命中=0 ⟹ **本档新增** ✓
技术词 数学方法      命中=0 ⟹ **本档新增** ✓
技术词 proof-engineering 命中=0 ⟹ **本档新增** ✓
技术词 自我升级      命中=0 ⟹ **本档新增** ✓
技术词 新数学结果    命中=1 ⟹ 本轨前档已有 ⟹ **引用** ✓
技术词 可迁移(21)／独立问题(20) ⟹ **档案已有** ⟹ **引用** ✓
技术词 冻结(521) ⟹ 通用词，不计 ✓
```
