# ∫SN'dt = O(1) 与 M(T) = O(1) 等价链完成态（2026-08-22 18:40）

## 一、终极等价链（全恒等式——数值全部验证）

```
M = O(1) ⟺ ∫SN'dt = O(1) ⟺ ΣS_k = K/2 + O(1) ⟺ Σθ(γ_k)/π = K²/2 − K + O(1)
✓ max1.31  ✓ max1.31    ✓ 偏离<0.7      ✓ 残差<1
```

## 二、关键严格结果

### 1. 锯齿中点恒等式
```
dM_k = S(γ_k + Δγ_k/2)·Δγ_k + O(Δγ_k³)
M(T) = Σ_k S(mid_k)·Δγ_k + O(1)
```
（max|err| = 0.11, Σerr = O(1)——2M 零点验证）

### 2. 分部积分恒等式（差 −1.3——精确）
```
Σθ(γ_k) = θ(T)²/2π + θ(T)·S(T) − π·∫SN'dt + O(1)
（端点约定：右极限 S(T) = S(γ_K)）
```

### 3. 关键代数（S_T 线性项抵消）
```
θ(T)²/2π + θ(T)S(T) = π(K−1)²/2 − πS_T²/2
→ ΣS_k = (K−1)/2 + S_T²/2 + ∫SN'dt + O(1)
→ ΣS_k = K/2 + O(1) ⟺ ∫SN'dt = O(1)
```

### 4. 加权 vs 等权（数值同阶）
- M_w = ∫SN'dt：max 1.31, ∫M_w²/T = 0.184（等权 0.41——同阶）
- M_w − M_eq：max 差 1.13（O(1)）

## 三、无条件定理（已证）

```
mean(S(γ_k)) = ½ + o(1)（Littlewood + 分部积分 + 恒等式）
⟺ mean(S(mid_k)) = 0
```

## 四、强数值结构（2M 零点）

### Σε_k = O(1)（相位均匀性）
```
ε_k = S(γ_k) − ½, max|Σε| = 1.67（不是 √K）
自相关：Σρ → −0.57（收敛）→ 1+2Σρ ≈ 0 → Var(Σε) = O(1)
```

### θ 离散和（残差 < 1）
```
Σθ(γ_k)/π = K²/2 − K + O(1)（K=2e6: 残差 +0.2）
```

## 五、框架定位（扎实基础——唐先生要求）

1. ✅ 严格恒等式（锯齿中点、分部积分、等价链）
2. ✅ 无条件定理（mean(S) = ½）
3. ✅ 数值确凿（2M 零点——多方法交叉验证）
4. ⚠️ 唯一鸿沟：∫SN'dt = O(1) 的严格证明——研究级（S 的加权振荡积分——RH 深度）

## 六、研究记录（教训）

1. **对象混淆**：Var[M(T)] vs ∫M²dt（时间平均）——不同量
2. **公式错误**：∫θ du 误为 dθ（差）——修正为 Taylor 积分
3. **素数协方差不解释 Var[M]**：权重 1/p 发散——高频积分 O(1)——真实机制在锯齿
4. **准周期是滤波层**：C(u) 是加权版本——非真实协方差
5. **框架必须扎实**：不能数字拟合——严格恒等式 + 无条件定理 + 明确研究级归结
6. **S_T 线性项抵消**（分部积分）——干净的结构——等价链无懈可击
