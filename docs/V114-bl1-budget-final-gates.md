# V114 · BL1 §4C（弱化输入／budget $B(n)$ 轴）三关终审

> 编号：**V114**（已在 `ID-CLAIMS.tsv` 领号，slug＝`bl1-budget-final-gates`）
> 日期：2026-09-14｜作者：subagent（唐先生指派）｜默认纸面审
> 状态：**完成 ✓**（纸面审；未跑脚本 ✗）｜总判定：**CLOSED**（作范围突破）＋ LIVE\*（预算轴残量）

## ① 编号与查重
```
【编号】V114 —— 已在 `dn-project/docs/ID-CLAIMS.tsv` 第 54 行在案 ✓
  逐字：`V114  audit  bl1-budget-final-gates  2026-09-14T11:51:18+08:00` ✓
  ⟹ 直接使用 V114，**不另领新号** ✓
【主档】docs/BL1-decomposition-change-done.md（全文 81 行，已整读 ✓）
【查重】关键词：BL1 §4C｜弱化输入｜budget B(n)｜零密度型｜输入上限 (T₀−1)²｜三关
  · V112（换输入立项）§③-F1 行 121／§④-1 行 141 ⟹ ≠ 本档（那是指标枚举；本档是**三关终审**）
  · V113（F1 悬点）§④／§⑤-3／§⑦ ⟹ ≠ 本档（那是**加权和界住性**；本档是**§4C 的对象与关**）
  · 本项 slug `bl1-budget-final-gates` 在台账**唯一** ✓，无同 slug 重复 ✓
【状态】全部 §①–§⑩ 已落盘 ✓；本节后无未完成节 ✓
```

## ② §4C 完整展开 + $B(n)$ 精确式子 + 对象识别定案

### ②-1 §4C 原文（逐字，BL1 行 61–66／75 ✓）
```
【§4 选项 C 逐字】“**弱化输入（最有价值但未探索）**：方法需要‘**高度 T = √n 附近的零自由信息**’ ✓
   —— **若能用【零密度型】信息替代**，则可能突破‘n ≤ (T₀−1)²’这一**输入上限**” ✓✓
【同段逐字】“⚠️ 注意：**φ 相位框架下‘一个离轴零点毁掉正性’**的论证**不适用于算术框架** ✓
   —— 因为**预算 B(n) 本身【已把可能的离轴零点计入】** ⟹ **算术框架可容忍离轴零点**” ✓
【行 75 逐字（下一步选项）】“A（α）⚠️ 常数因子｜B（预算常数）⚠️ 2 倍｜**C（弱化输入）⭐ 唯一可能突破上限**” ✓
```

### ②-2 $B(n)$ 的精确式子（两处逐字，互相印证 ✓）
```
【权威形式｜docs/A1-ROUTE-conjecture-3.2.7.md §5 行 56 逐字（D-037 的 Lean `Bn`）】
   B(n) = 4F(T) + 4T[(2/3)Z₂(T) + 10.5·log(T+2) + 48] + 40·1.3352
          + 2(e⁴+1)·7Ĥ(T) + 2·DogLegRight(n) + IIMaj(n) + 1，  T = √n + 1 ✓
   Ĥ(T) := 3C₀(T)² + A·log(T+2) + B₀ ✓；I_I ≤ 2√n(e^{√n/2}+1)·0.5699·2^{−(2√n−2)} < 10⁻³⁰ (n≥10³) ✓
【结构型｜docs/ALIGN-A1-FINAL-error2-and-code.md §3 行 44 逐字】
   B(n) = 2N(T) + 4T[(2/3)C₀(T) + A·log(T+2) + B₀] + （未知：40M₀ + 2(e⁴+1)[7H(T)+3.3] + I_I + 1）
   同段逐字：F(T)=(T/2π)log(T/2πe)+7/8；|S(T)| ≤ 0.137logT+0.443loglogT+1.588；
              C₀(t)=F(t+1)−F(t)+S_bnd(t+1)+S_bnd(t)；A=2.0, B₀=2.0 ✓
【形状 ✓】经 T=√n+1：F(T)~(T/2π)log T ⟹ **B(n) ≍ √n·log n**（与 D-037 所述“≈2.54√n log n”同量级 ✓,
   ALIGN §3 校准：n=10³ 比值 3.03 → 10¹² 比值 2.16 ⟹ “量级正确 ✓”）
```

### ②-3 【被替换的原输入】的精确定义（对照式 ✓）
```
【原输入 IN-1（逐字）】docs/V112-input-substitution.md §②-1 逐字：“**IN-1 验证高度区**（‘RH 验证至 T’＝
   在 γ≤T 内**无**离轴零点）｜**不存在性信息（纯否定）**” ✓ —— **这是“零自由信息”的档案名** ✓
【原输入的计数身影】BL1 行 12 逐字：“误差预算 B(n)（按 D-037 (4.1) 形状 ✓，**用经典显式计数界** ✓，**T=√n+1** ✓）”
   ⟹ “经典显式计数界” ＝ N(T)／F(T)／Z₂(T)（F、Z₂ 皆为 N(T) 的显式版本 ✓）
【新输入 IN-3（逐字）】V112 §②-1 逐字：“**IN-3 零密度上界 N(σ,T)**｜计数型（平均）”；
   CONV2 §2 逐字：“**正是 N(σ,T) 型零密度定理** ✓…经典零密度定理（Ingham 等）**无条件成立**”；
   改进档 ＝ E30 §4（**A11 Guth–Maynard** ✓）
【对照式】原 ＝ ｛显式计数界 N(T)（T≤T₀）｝⊕｛验证高度 T₀（零自由 IN-1）｝；
          新 ＝ ｛N(σ,T) ≪ T^{3(1−σ)/(2−σ)+ε}（Ingham）／GM 改进｝
```

### ②-4 ★ 对象识别定案：(c) 两者 —— 但【目标】与【机制】不同轴
```
【判定】§4C 替换的是【零自由信息】(IN-1)；IN-1 在 BL1 以两个身份出现 ⟹ **(c) 两者**：
   ① 喂给预算的输入：行 12 逐字（B(n) 用经典显式计数界，T=√n+1）⟹ **(a)**；
   ② 范围终点的输入上限：§4C 逐字（“突破 n ≤ (T₀−1)² 这一**输入上限**”）⟹ **(b)**。
⚠️ 识别差异（★ 关键）：行 12 说的是“**计数界**”，§4C 说的是“**零自由**”——**两者非同一物**：
   计数界（IN-2/3）答“**有多少个**零点”；零自由（IN-1）答“**有没有**离轴零点”。
   **方法要的是 IN-1（非存在性），替换给的是 IN-3（计数）** 〔本方推论〕
⟹ 对三关的影响方向：
   **(a)-读法**只改 B(n) 的**量** ⟹ 只动“余量/交叉点”（BL1 §2 逐字：预算在 n≈300–1000 后**已不约束**）⟹ **无范围收益**；
   **(b)-读法**才动范围终点 (T₀−1)² ⟹ **是 §4C 的登记目标**。
⟹ **本档据此：§4C 的目标读作 (b)，命名机制读作 (a)——错位本身即 Gate 3 的判死点（§⑤）。**
```

## ③ Gate 1（方向）

### ③-1 判据（**先写死** ✓）
```
(G1-1) B(n) 随 n 须有**有用的单调/递减结构** B(n+1) ≤ B(n) − Δ(n)，Δ(n) > 0；
(G1-2) Δ(n) 须在覆盖论证中**实际被消费**（须说明：谁消费、怎么消费，附逐字出处）；
(G1-3) 替换后的新输入须**保持**该可用结构（显式常数、可算 Δ(n)）。三条**全过**才算 Gate 1 过。
```

### ③-2 判定
```
(G1-1) ✗ —— B(n) **递增**，不是递减：BL1 §2 表逐字：B(10³)=1.742e3 → B(10⁴)=6.444e3 → B(10¹²)=1.494e8。
   有用的结构**不在 B 本身**，而在 **margin(n) := S_∞(n) − B(n)**（同表 S_∞(10³)=2.324e3 > B ✓；
   S_∞ ≍ (n/2)log n 快于 B ≍ √n log n ⟹ margin **递增**）。
(G1-2) ✓（部分）—— margin>0 被消费于“S_∞ 主导 ⟹ λ_n ≥ 0”；消费主体 ＝ **预算交叉判据**：
   BL1 §2 逐字“**预算交叉点 n≈300–1000 ⟹ 此后预算不再约束 ⟹ 范围完全由【输入】决定**” ⟹ Δ(n)（＝margin 增量）
   被“交叉 ⟹ 结论稳健”消费（ALIGN §3 逐字：“**‘范围由输入决定’这一结论【稳健】** ✓”）。
(G1-3) ✗（关键）—— 新输入 N(σ,T) ≪ T^{a} 为**渐近**界（**无显式常数**）：V112 §① 逐字
   “`FRONTIER-PRIMEGAP-SURVEY-2026-09.md`（**新零密度界未显式化**）” ⟹ 给不出可算 Δ(n) ⟹ 替换后 (G1-2) 的消费**断链**。
⟹ **Gate 1：原输入“过（但结构在 margin 不在 B）”；替换后“不过”** ⟹ **对 §4C 不过** ✗
```

## ④ Gate 2（秩）

### ④-1 判据（**先写死** ✓）
```
(G2-1) 把所有允许的覆盖对象按预算排序 B₁(n) ≥ B₂(n) ≥ ⋯，须成立 **budget ordering ⟹ admissible-cover ordering**；
(G2-2) 若最终只得**统一大小界** B(n) ≤ C·n^α、**不能排除候选间 swap** ⟹ 与 F1 同 **P-Type、无选择力** ✗。
```

### ④-2 判定
```
(G2-1) ✗ —— 零密度型输入给的是**单一统一上界档**（N(σ,T) ≪ T^{3(1−σ)/(2−σ)+ε} **无条件**成立，V112 §③-F1 逐字
   “**✓ 是**：Ingham 型…**无条件**”）；它**不区分候选间的位置/距离**（N29 逐字：“测试函数变量的正性【位置盲】”）。
   ⟹ budget ordering ⇏ admissible-cover ordering。
(G2-2) ✗ —— swap **不可排除**：计数上界对“把零点从离轴搬到轴上”这一 swap **无感**（只数个数、不看位置）。
⟹ **Gate 2 不过** ✗ —— **P-Type 命中**，与 F1 同型（V112 §③-F2 逐字“**P-Type**（点式 ⇏ 聚合）”；
   V113 §⑦ 逐字“P-Type ✓ **命中**（点式⇏聚合）⟹ 只到‘平均/密度一’ ✗”）。
```

## ⑤ Gate 3（截断）★ 最重要

### ⑤-1 判据（**先写死** ✓）
```
(G3-1) 若证明只用 n ≤ N₀ 的预算 ⟹ 须查 sup_{n>N₀} B(n) 或尾预算 Σ_{n>N₀} B(n) 是否**重新吞掉**前面的区分量；
       否则“有限截断有效 ⇏ 全局覆盖有效” ✗。
(G3-2) 须**正面处理** BL1 行 23／35 的“**预算交叉后不再约束**”：若预算在大部分区域**本就不约束**，
       那“弱化输入”改变的到底是**预算**还是**输入极限**？
```

### ⑤-2 判定
```
(G3-1) —— 尾部**不吞**，但结论反噬：B(n) 递增但**慢于** S_∞(n) ⟹ margin **递增** ⟹ 尾部安全 ✓（无尾预算吞没）。
   ⚠️ **但**：正因为尾预算**不吞**区分量，**就没有区分量落在预算轴上** ⟹ **(a)-读法在此区域零效果**
   ⟹ “有限截断有效”在此**推不出**“全局覆盖有效”（全局覆盖的约束**不在预算轴**）✗。
(G3-2) ⭐⭐【判死点】—— 正面处理：BL1 行 23／35 逐字“**预算交叉点 n≈300–1000 ⟹ 此后预算不再约束
   ⟹ 范围完全由【输入】决定**”⟹ 在决定范围的区域（n ≳ 1000 直到 9×10²⁴），**预算是“非约束”**。
   故“弱化输入”若要有效，**只能**改**输入极限**（(b)），而**输入极限 ＝ (T₀−1)² 由验证高度（零自由 IN-1）给出**。
   ⟹ **自相矛盾**（两读皆使 §4C 失效）：
      ① 若信 §4C 逐字“**预算 B(n) 本身已把可能的离轴零点计入 ⟹ 算术框架可容忍离轴零点**”
         ⟹ **不需**零自由 ⟹ 无输入上限、**无需替换**；
      ② 若信 §2 逐字“**范围完全由【输入】决定**”（输入 ＝ 零自由信息）
         ⟹ 零自由仍是约束，而**零密度（计数）不能替代非存在性**（IN-1 逐字：“**不存在性信息（纯否定）**”）
         ⟹ 替换**不成立** ✗。
⟹ **Gate 3 不过（判死点）** ✗ —— 有限截断（n≤1000）的预算有效性 **⇏** 全局（n≤9×10²⁴）覆盖有效性。
```

## ⑥ K2-E″ 应用

```
【K2-E″（唐先生定义，逐字 ✓）】
   “B(n) 的新输入若最终只提供 **绝对大小界／可求和性／截断界**，而**不改变覆盖秩**，则**立即 NO-GAIN**”
【逐条对号】
   绝对大小界 ✓ **命中**：N(σ,T) ≪ T^{a} 正是“绝对大小界”（V112 §③-F1 逐字：“Ingham 型…无条件”）；
   可求和性 ✓ **命中**（部分）：CONV2 方案本就以“收敛性”为目标（V113 标题逐字：“离轴加权和【能界住】”）；
   改变覆盖秩 ✗ **未命中**：Gate 2 已判 **不改秩**（④-2；N29 位置盲）。
⟹ **K2-E″ 命中 ⟹ 立即 NO-GAIN** ✓✓（与 V113 §⑤-3 K3-E “无增益”同向 ✓）
```

## ⑦ 总判定

```
★ **CLOSED（作为“突破输入上限”的路线判死）** ＋ 残量 **LIVE\***（预算轴子问题）

【逐条依据】
   Gate 1 ✗（方向）：原输入过但结构在 margin 不在 B（③-2 G1-1）；替换后渐近界无可算 Δ(n) ⟹ 断链（G1-3）
   Gate 2 ✗（秩）：零密度只给统一上界档 ⟹ budget⇏cover ⟹ swap 不可排除 ⟹ **P-Type、无选择力**（④-2）
   Gate 3 ✗（截断，⚰）：(a)/(b) 两读皆使 §4C 失效（⑤-2 G3-2 自相矛盾）；有限截断有效性 ⇏ 全局覆盖有效性
   K2-E″ ✓（命中）：新输入只给大小界／可和性，不改秩 ⟹ **NO-GAIN**（⑥）

【下一步可判定动作（若不甘判死）】
   NP1（纸面，可判）：取 D-037 (4.1) 的**显式** N(T) 常数，重算 margin(n)=S_∞(n)−B(n)；
      验证“预算交叉点 n≈300–1000”是否仅属**首项**而完整预算会**改交叉点**（BL1 行 57 逐字：“α 由预算内部平衡决定
      （我的计算只取了首项 ✗）”）—— 但这只动 (a) 轴 ⟹ **预计仍是 NO-GAIN**（本档预测 ✗）。
   NP2（纸面，可判）：判定“**是否存在一条不经过零自由（IN-1）而能得 n>(T₀−1)² 的输入替换**”＝
      判定“计数型输入能否替非存在型输入” ⟹ **本档已判：不能**（IN-1 逐字“不存在性信息（纯否定）”）✓。
【判死理由一句话】**零密度型（计数）不能替代零自由（非存在）；且预算在决定范围的区域本就不约束 ⟹ 改 B(n) 无范围收益。**
```

## ⑧ 对 V109／V112／V113 的影响

```
【V112（换输入立项）】§③ 行 121 逐字：F1 ＝“**LIVE（最强候选 ✓✓）**”；§④-1 逐字：“**存活族 F1（零密度型）—— 最强候选**”
   ⟹ **本档下调 F1：LIVE → CLOSED（作范围突破）**；F1 的 G-1 论证（V112 §④-1 逐字：“G-1：BL1 逐字‘若能用
   【零密度型】信息替代…则可能突破 n≤(T₀−1)²’”）**不被支持**（目标 (b) 不成立）。F1 仅在**预算轴 (a)** 保留 **LIVE\*** 残量。
   注：V112 §⑤-7“若‘换输入’亦关闭 ⟹ 项目进入什么状态（预先写死 ✓）” ⟹ 本档为该条款提供**一个触发实例**（但仅 F1 族，
   非全换输入支 ⟹ F5（Selberg 积分型）仍 ⚠️ 部分存活，见 V112 §④-2）。
【V113（F1 悬点判定）】§⑤-3 逐字：“**K3-E 触发** ⟹ 按 §⑥ 归档 ✓（如实归档，不强行推进 ✗）”；§⑦ 逐字 P-Type“命中”。
   ⟹ **本档为 V113 的“无增益”结论提供第二独立确认（对象识别 + 三关）**；且本档指出 V113 未做的两件：
   ① 区分 (a)/(b)；② 正面处理“预算交叉后不约束”（V113 仅算至“能界住”与 T² 平衡）✓
【V109（覆盖论证章程）】本档**不是**覆盖论证（对象 ＝ 单条输入替换的**一步判定**，同 V113 §⑦ 逐字“本档对象是
   【某判据的输入替换候选的【一步】判定】，不是新载体/新正性”）。⟹ 对 V109 中档 M 靶**零影响** ✓。
   〔本方推论〕但 V109 §⑦“与三条活路的关系”中若“换输入”为一条，则本档**减一候选**（F1）⟹ 杠杆估计下降。
   同时本档**未**触发 V109 §⑥-5 任何 KILL（K1-A 换对象型 ✗ 不适用：未改称 T5/未搬 N31 版判据）✓。
```

## ⑨ 分类／门槛／形态

```
【宪法 §1 硬禁止表】不命中 ✓：N2（Weil 正性变体）⚠️ 潜在未判（同 V112 §③-F1“若其实现最终 ≡ 显式公式 residue 记账”）；
   N29（位置盲）✓ 命中为“**范围限制器**”（非判死）：本档结论完全落在“比例/密度通道”（与 V113 §③-1 同）；
   N31 ✗ 不命中（本路线不产生 √-正性、不引有限性）。⟹ **无硬 NO-GO，故本档未早停归档** ✓。
【§3 门槛】P-Type ✓ 命中（点式⇏聚合／无选择力，④-2）；P-Scale ⚠️ 沾边（height 侧量，无增益）；
   P-SqrtPos／P-Basis／P-Dual／P-Lock*／A_min ⟹ **不适用**（该路线无常数族产物）；非循环性 ✓；非 zero-encoding ⚠️（依赖 N(T) 定义）。
【§4 形态】**非 RH 路线**（同 V113 §⑦ 方框逐字）；不属 L1–L4 任一种；§4.1 A–H 全不过 ✓
   （无独立载体/无新正性/无选择力）。
```

## ⑩ 证据等级 + 未证清单

```
【证据等级】
   【核验】BL1 全文 81 行（主档）✓；D-037 的 B(n) 逐字（A1-ROUTE §5 行 56；ALIGN §3 行 44）✓；
            BL1 §2 数值表（B(n) 与 S_∞(n)）✓；V112 §②-1/§③-F1/§④-1、V113 §⑤-3/§⑦、V109 §⑥-5、宪法 §1/§2 逐字 ✓
   【档案级】IN-1/IN-3 定义（V112 §②-1）✓；N29 修正案（V113 §③-1引）✓
   〔本方推论〕：(a)/(b)/(c) 错位判定；自相矛盾判定；P-Type 认定（无 swap 排除）；Gate1-3 的 margin 论证
【未证清单】
   u1  D-037 三个未知项定义未取得（40M₀／2(e⁴+1)[7H+3.3]／I_I）—— ALIGN §4 逐字自陈“✗”
   u2  B(n) 精确常数未知（Rosser 系数 vs D-037 更优系数 A=0.9,B₀=1.0）—— ALIGN §3 逐字“⚠️ 高约 2.2 倍”
   u3  对象识别 (a)/(b)/(c) 依赖 BL1 措辞；BL1 **未明言** ⟹ 若唐先生裁定为纯 (a)，Gate 3 自相矛盾仍发生，总判定**不变**；
       若裁定为纯 (b)，则 Gate 1/2 判定不变，**总判定仍为 CLOSED** ⟹ 【口径待定】不改变结论 ✓
   u4  零密度型 N(σ,T) 的**显式常数**未显式化（V113 §① 逐字“新零密度界**未显式化**”）⟹ Gate 1(G1-3) 仅以“无显式常数”判 ✗
   u5  “预算容忍离轴零点”（§4C）与“范围由输入决定”（§2）的相容性**未证** —— 本档判为自相矛盾，但 BL1 **未自陈**此张力 ⚠️
   u6  本档**未跑任何数值/脚本**（纯纸面 ✓）；所有数值取自 BL1/ALIGN 已入库逐字（无新数据 ✗）
```

## 提交链
```
BL1（主档）—→ V112（换输入立项，F1 LIVE）—→ V113（F1 悬点：能界住但无增益）
   —→ **V114（本档：§4C 三关终审 ⟹ CLOSED）** —→ 待唐先生裁定对象口径 (a)/(b)/(c)（u3）
```
