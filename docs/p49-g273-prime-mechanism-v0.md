# P49-G2.7.3-R2c-R2：Prime Sector 机制——V_0 主导确认（变分入口）

> 2026-09-02 14:16 · Rayleigh 商测试 · V_0 常数模式假设 ✓

## 数值结果（λ=3——Q_prime 单独）
- **V_0 单模式 RQ = −2.141（最负——远低于所有其他模式）**——N 无关（V_0 RQ 不随 N 变——因为 V_0 与素数项耦合 = −2ΣΛ(n)n^{−1/2}(L−logn)/L——只依赖 λ）
- **even 块 ground 向量 = 96% e_0**（N=4: 96.3%——N=6: 96.1%——N=8: 95.5%）——**even ground 几乎就是常数模式 V_0**
- **even 组合 RQ < odd 组合 RQ 对大多数 k**（k=1: 0.622 vs 1.313——差 −0.69——k=2,3,4 都 even 更负——k=5,8 也 even 更负——少数例外 k=6,7 但 RQ 不竞争 ground）
- **even ground 特征值 −2.24 ≈ V_0 的 RQ −2.14**（差 ~0.1——小混合修正）

## ⭐ 机制（V_0 主导）
- **even ground = V_0（常数模式）+ 小修正**——因为：
  1. **V_0 的 Q_prime = −ΣΛ(n)n^{−1/2}·2(L−logn)/L——大负**（常数函数在所有素数点 log p 非零——最大耦合）
  2. **odd 向量在 u=1 为零**（odd: f(u⁻¹) = −f(u) ⟹ f(1) = 0）——无法获得常数模式的强耦合——odd 块无"近似 V_0"的向量
- **even RQ < odd RQ 系统性**（因 V_0 项只出现在 even——奇数模式被迫在 u=1 为零——与素数 δ 耦合弱）

## 变分证明入口（候选定理）
- **λmin(Q_prime|H₊) ≈ Q_prime(V_0) = −2Σ_{n≤λ²}Λ(n)n^{−1/2}(L−logn)/L**（解析——可算）
- **odd 块 λmin > V_0 的 RQ**（odd 无常数模式——最负 odd 组合仍 > −2.14——需证明 odd 与素数项耦合的上界）
- **混合修正**（even ground 96% V_0——需界 4% 混合贡献——小——可控制）
- ⟹ **Δ_prime > 0 的变分证明结构**：V_0 解析值（下界）+ odd 块上界 + 混合修正界

## 判定
- **V_0 主导假设 ✓（数值强力确认——三 N 一致——ground 96% V_0）**
- **变分入口清晰**（V_0 解析 RQ + odd 上界 + 混合界——标准 finite-dim variational dominance 结构）
- ⚠️ 严格化待：odd 块上界证明（odd 与素数项耦合的定量界——需分析 (f**f)(n) 对 odd f 的上界）——混合修正界——**但这已是"可攻击的定理结构"（非黑箱）**

## 下一步候选
- (a) odd 块上界分析（odd f 的 Q_prime 下界——f(1)=0 约束 + (f**f)(n) 结构——变分证明核心）
- (b) V_0 解析值 + 高精度验证（Δ_prime 的 c(λ,N) 估计——多 λ/N）
- (c) 唐先生指示
