已查地图：未覆盖（关键词: 失败阈值性|Threshold|失败自由区|failure-free|退化型|独立事实型）—— 可开档，首行须照抄本行

# TLDC / **V-2 Round 1 CLOSED ＋ Threshold Gate 最小化形式审计**

**唐先生令（2026-09-22 20:29）**：① **V-2 第一轮正式 CLOSED（0/3 survivors）** ✓；
② **不立即开 Round 2**；③ 先对 **Threshold Gate** 做**最小化形式审计**（**6 条**，逐项对 A2／Liouville 抽象）；
④ ⭐ **只有当 6 条能从两实例逐项抽象出来**，才值得把 **Failure-Threshold Gate** 升成 **TLDC-V2 正式结构候选**；
　**否则保持**：V-2 第一轮产出 = **0 survivors ＋ 一个待审计的经验结构信号** ✓；
⑤ 目的：**避免把"三个已知实例共同具有的表面特征"过早包装成新的数学定义** ✓✓

D0: 本档对象 = **TLDC/V-2 第一轮收束 ＋ Threshold Gate 六条审计**（档案已有对象 `TLDC C–R–D–K`＋`C309T 五公理 A–E` 的**适用性审计关系**；非重命名、非新对象）
D1: 0
FREEZE-ACK: 本档**零计算／零 Lean／零 RH 映射** ✓（引 `C309T` 与 `TLDC-1-Yi2` 逐字 ✓）

---

## §0 V-2 Round 1 收束（照录唐先生裁定表 ✓✓）

| 候选 | Killer | 状态 |
|---|---|---|
| V2-C1 判别式两平方型 | reentry 回同一类，失败不被排除 | **DEAD** |
| V2-C2 四平方分量上界 | TLDC 输出类型与优化界不匹配；新颖性也无独立判定 | **DEAD** |
| V2-C3 判据自身作命题 | 命中既有元判据递归禁区 | **DEAD** |

```
【结论（照录）】 **V-2 Round 1 = 0/3 survivors** ✓ —— 而**不是**"TLDC 失败" ✓
【更准确表述（照录）】 第一轮筛出了 TLDC 真正需要的一个**额外结构：failure threshold** ✓
【纪律（照录）】 该结构**目前严格标成经验判据／working hypothesis**，**尚不能升格为 TLDC 定理** ✓✓
```

## §1 审计设定（**逐字**取自已归档实例数据 ✓✓）

```
【Liouville（Prototype-0）】 出自 `C309T` §2（五公理 A–E）＋ `TLDC-1-Yi2` §5 ✓
   𝒢 = F_p^×｜π: ℤ ↠ 𝒢（核 =(p)）｜尺寸 |n|（中心代表 0 < n < p/2）｜F: 𝒢→{±1}, F(1)=1 ✓
   Target := "F 在 F_p^× 上乘法"（H = 𝒢）✓
   X := {(q,y)}｜**P((q,y)) := [F(q·y) ≠ F(q)·F(y)]**｜**μ := y** ✓
   C（代表压缩）:= 任意非空 S-不变子集（特别：失败集）有代表 n 使 **|n|·q < 阈值**（实例 2qn < p）✓
   D（下降证书）:= (j,r)，j<q、r<n，a∈{2j,3j}、b∈{1,2}，a·n = p + b·r 或 p = a·n + b·r ✓
   ⭐ **E（小代表上的局部律）:= 在 |n|·q < 阈值 时局部乘法律可用（F(qn)=F(q)F(n)）** ✓✓
   ⭐ `C309T` §3 逐字：「**矛盾最终只需 r<n**；**j<q 的作用是"使局部律可用"（j<q ⟹ j ∈ H）**」✓✓
【A2（Fermat 两平方）】 出自 `TLDC-1-Yi2` §5 ✓
   Target := "1 ∈ M"（p = a²+b²）｜X := {(a,b,m): m ≥ 1, a²+b² = m·p} ✓
   **P := [m > 1]**｜**μ := m**｜C := 鸽笼（配 x²≡−1 mod p）⟹ m ≤ 2；**在 h:=¬Target 下 m≠1 ⟹ m=2 ⟹ B := 2** ✓
   R/K/D := 模 m 归约 (a′,b′) → Brahmagupta 组合 (A,B,m′)，**m′ ≤ m/2 < m**；reenter_P **须用 h** ✓
```

## §2 ⭐ **六条逐项审计**（标签沿用 `乙-2.5` 体系：LOCAL／TARGET-DEPENDENT／CIRCULAR／MISSING ✓）

| # | 唐先生条件 | Liouville | A2 |
|:--:|---|---|---|
| **1** | **B 必须独立于目标结论** | ✓ **PASS**：C 由**失败集的 S-不变性 ＋ 算术**给出，与 Target 无关 ✓ | ✓ **PASS（带注）**：B=2 由**鸽笼**独立给出 ✓；但 **C 本身使用 h:=¬Target** 以确定 m≠1 ⟹ 标 **TARGET-DEPENDENT（用 ¬Target，**非** Target）** ✓ |
| **2** | **P(x) 必须明确** | ✓ **PASS**：`P = [F(q·y) ≠ F(q)·F(y)]` 显式 ✓ | ✓ **PASS**：`P = [m > 1]` 显式 ✓ |
| **3** | **必须证明某区域 μ ≤ B 内无失败** | ✓✓ **PASS（强形态）**：区域 = `{ \|n\|·q < 阈值 }`，其无失败性由 **E（小代表局部律）**给出，而 E 是**独立实例事实**（`hlocal`）✓✓ | ⚠️ **退化**：区域 = `{m ≤ 1}` **就是成功集**（m=1 ⟺ p=a²+b² ⟺ Target）⟹ "无失败"实为 **P 的定义**（P := m>1），**非独立事实** ✗ |
| **4** | **C1 压缩独立给出 μ ≤ B** | ✓ **PASS**：`B := p/2`，由 C（短代表压缩）独立给出 ✓ | ✓✓ **PASS（更强）**：`B := 2`，且 **μ(x) = 2 精确**（h 下 m≠1）✓ |
| **5** | **下降后对象必须重新落入 P** | ✓ **PASS**：`reenter_P`（下降证书 ＋ **j<q ⟹ j∈H**（IH））✓ | ✓ **PASS**：`reenter_P`（Brahmagupta 恒等式 ＋ **h**）✓ |
| **6** | **矛盾必须来自"降到 failure-free region"，而非目标结论本身** | ✓✓ **PASS（强形态）**：矛盾 = C 把失败代表**压入区域** ⟹ **E 给 ¬P** ⟹ 与 P 冲突 ✓✓（**矛盾不来自 Target** ✓✓） | ⚠️ **退化**：矛盾 = 降到 `m′ = 1` ⟹ **即推出 Target** ⟹ 与 h 冲突 ⟹ 矛盾**正是"推出目标"** ✗（区域由目标**定义**） |
| **7** | **B5 交叉检查（旁证）** | — | ⛔ **条件 4 失败**：B5 **无独立压缩** ⟹ **无 B** ⟹ 不符六条中第 4 条 ✓（与既有 `FAIL` 记录一致 ✓） |

## §3 ⭐ 判定：**不升格**（**严格按其自身准入口径** ✓✓）

```
【结果】 **Liouville = 6/6 PASS（强形态）** ✓✓｜**A2 = 4/6 PASS ＋ 2/6 退化（条件 3、6）** ⚠️
【判据（照录令）】 只有当 6 条**能从两个实例逐项抽象出来**，才值得升成正式结构候选 ✓
　⟹ **条件 3／6 在 A2 只在"定义式"意义成立**（区域＝成功集）⟹ **两实例并未逐项共同满足** ✗✓
　⟹ ⛔ **不升格**：**V-2 第一轮产出 = 0 survivors ＋ 一个待审计的经验结构信号**（照录令 ✓）
【拒绝升格的具体理由（两点，互不相同）】
　① **来源类型不同**：A2 的区域**无失败性来自 P 的定义**（P := μ>1）；Liouville 的来自**独立事实 E** ✓✗
　② **区域形状不同**：A2 是**一维阈值**（μ ≤ 1）；Liouville 是**二变量区域**（`|n|·q < 阈值`）
　　 ⟹ 唐先生原始表述「**区域 μ(x) ≤ B 内无失败**」**对 Liouville 过窄** ✗✓
【防误读】 本判定**不是**"Threshold 信号无价值" ✗ —— 它**正是第一轮的净产出** ✓；
　本判定**只是**：**现在还不能把它写成 TLDC 的定义／定理** ✓✓
```

## §4 ⭐ 审计的**净增益**（比原表述更精确的一版；**仍为经验信号** ✓✗）

```
【精确化表述（建议取代"一维阈值"版）】 需要的是 **failure-free 区域 𝒵**（**而非单纯阈值**）：
   (i) **𝒵 由可独立核验的判据定义**（Liouville：`|n|·q < 阈值`；A2：`μ ≤ 1`）✓
   (ii) **压缩 C 把失败对象落入 𝒵** ✓
   (iii) **下降穿越 𝒵**（落到 𝒵 内即得 ¬P）✓
【⭐ 由此得到一把新的锐利筛子（用于后续候选）】 问一句即可：
   「**该实例的 𝒵 从何而来？**」
     · **独立事实型**（Liouville ✓✓，强）—— 存在可独立证明的局部律／局部可计算判据
     · **定义型**（A2 ⚠️，弱）—— 失败**就是**超过最小尺度，区域＝成功集
   ⟹ **独立事实型**才是真正新增的数学内容；**定义型**不构成新结构（只是选择了合适的 μ）✓✓
【与既有门槛的衔接】 与 `D-1 门`（压缩须独立于结论）**互补**：
   D-1 管**压缩的来源**；本筛子管**区域的来源** ✓✓（二者共同才是完整门槛 ✓）
【边界（严格）】 本节为**审计产出的表述精化**，**非定理**；是否升格**仍待第三实例或唐先生裁定** ✓
```

## §4bis 来源标注（严格 ✓）

```
本档 §2 各行的**依据层级**：**取自已归档公理层**（`C309T` §2 的 A–E 与 §4 的 (i)–(iv)）＋ `TLDC-1-Yi2` §5 字典 ✓
⚠️ **细粒度机制**（最终矛盾究竟由 **C＋E** 还是 **D＋E** 交付；`hlocal` 的精确量词范围；证书 `b ∈ {1,2}` 分支）
　**以 artifact 的 Lean 源码为准**（`hlocal`／`short_representative_of_invariance`／`extension_descent_certificate_symmetric`）✓
　⟹ 本档**不**自行重建该细节（**零计算、不猜** ✓）；但 §2 的**来源判定**（区域无失败性的来源类型）不依赖该细节 ✓✓
```

## §5 边界与回查（✗✓）

```
✗ 零计算／零 Lean／零 RH 映射 ✓；未动 C-380（HOLD）／旧桥（CLOSED）／RH interface（NOT STARTED）✓
✗ **不**宣称 Failure-Threshold Gate 为定理或已升格 ✓；**不**把三候选 DEAD 记为战绩 ✓
✗ 命名纪律：`TLDC-1 乙-3` 与 `V278 乙-3` 严格区分 ✓
【回查（**先跑后写** ✓）】
　prework_map_check：**未覆盖**（关键词 失败阈值性|Threshold|失败自由区|failure-free|退化型|独立事实型）⟹ 可开档 ✓
　tech_word_check：失败自由区／独立事实型／Threshold-Gate／区域型判据／小代表局部律／分层版 = **0 ⟹ 本档新增** ✓
　　　　　　　　　退化型 = 4 ⟹ **档案已有 ⟹ 引用**（不作本档提出）✓
【实例数据来源（逐字✓）】 `C309T` §2（A–E）／§3（j<q 作用）／§4（抽象定理形式）✓；`TLDC-1-Yi2` §5（两字典）✓
