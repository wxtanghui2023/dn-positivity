已查地图：命中（`F1-dual-audit-and-G2p5-gate`／`F2-structural-feasibility-verdict-A`／`P1-3-unresolved-finite-problem-pool`）⟹ 引用，不开新案
D0: 本档对象 = `F1` **降级记录**（上界已更新至 `81` ＋ 已有专门 SAT 团队）＋ `F5`/`F3` 双审计 ⟹ **`G2.5` 为当前瓶颈门**
D1: 0 （[REVIEW] 轮次：审计，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`F1` 降级 ＋ `F5`/`F3` 审计：`G2.5` 已成瓶颈**

## §0 `F1` 降级（照录）

```
【状态已过时】 新预印本（`2026-09-19`）把上界推进到 $$\boxed{33\le\mathrm{ES}(7)\le81}$$（原 `113`），并给出完整有限证书＋声称 Lean 形式化；`\mathrm{ES}(8)`：`65\le\mathrm{ES}(8)\le319` ✓
【且已被专门攻击】 `Dumitru` 的 `33` 点 SAT 编码直取该问题，已获**anchored subfamilies 的 `UNSAT` 证书**；其运行时间**heavy-tailed**，瓶颈正是**编码改进** ✓✓
【⟹ 判定】**`F1` DOWNRANK** ✓：仍开放，但**已是专门 SAT 团队的直接战场** ⟹ **我们极易沦为"同一问题换一个 solver"** ✓✓
【⟹ 新教训（照录并升格）】 三点确认**必须再加一问**：$$\boxed{\text{我们是否会是第五支做同一件事的 SAT 搜索队伍？}}$$ ✓✓
```

## §1 `F5`（差族/循环设计）审计

```
【状态】 该域**成熟**（`La Jolla` 型表、`Bitan–Yin` 型存在性结果）；**确有未决参数**，但**须逐参数钉死 `(v,k,\lambda,\text{group})`** ✓
【⭐ 规模账 —— 结构上不利】 循环差集/差族的**自然对称群只有平移 `\mathbb Z_v`（阶 `v`）＋乘子群** ⟹ 压缩因子 `\sim v\cdot m`，而搜索空间为 `\binom{v}{k}` ✓✓
$$\frac{\binom{v}{k}}{v\cdot m}\ \gg\ 1\ \text{（如 }v=111,k=11:\ \binom{111}{11}\approx10^{15},\ /v\approx10^{13}\text{）}$$ ✓✓
【⟹ 对比】 图问题有 `S_n`（`n!`）级对称；**差集的群作用量级仅 `v`** ⟹ **`G2.5`（可压缩性）在此族\textbf{结构性失败}**，除非参数很小 ✓✓
【竞争】 存在性表＋计算工作长期活跃 ⚠️
【⟹ 判定】**`F5-A`（就中参数而言）**；仅**小参数**（`\binom{v}{k}\lesssim10^{9}`）才可能过 `G2.5`，而小参数**多半已被解出** ✓
```

## §2 `F3`（`5`-色单位距离图）审计

```
【状态】 记录 `509`；社区（`de Grey`／`Parts`／`Exoo` 型）**长期专门搜索** ⚠️
【规模账】 待判定问题＝"是否存在 `K<509` 顶点的 `5`-色单位距离图" ⟹ 空间为 `\sim500` 顶点图，**天文量级**；证书（显式图＋着色校验）**很小** ✓ 但**搜索侧无压缩** ✓
【⟹ 判定】**`F3-A`／竞争受限** ✓（照您纪律：除非找到**明确仍未解决的小参数子问题**，否则退出）
```

## §3 ⭐ 池状态与结构性结论

```
$$\begin{array}{c|c}\text{F9}&\text{CLOSED（已解决型）}\\\text{F2}&\text{CLOSED（F2-A：}\sim10^{79}\text{证书）}\\\text{F4}&\text{DOWNRANK（gap 大口径）}\\\text{F1}&\text{DOWNRANK（专门 SAT 战场）}\\\text{F5}&\text{结构性弱（群作用仅 }v\text{）}\\\text{F3}&\text{竞争受限}\end{array}$$ ✓
【⭐ 结构性结论（本档核心）】 当前**瓶颈门是 `G2.5`（可压缩性）＋ 竞争结构**：
　**(i)** 我方资产（`D`：穷尽＋证书＋独立校验）**提供的是"验证能力"，不提供"压缩能力"** ✓
　**(ii)** **压缩必须来自问题自身的结构**（大对称群／强传播／高维约束（如 chirotope））—— 而我方资产**不能凭空造出它** ✓✓
　**(iii)** 符合 `G2.5` 的问题类型画像：**大对称群**（图/超图同构类）＋**强约束传播**＋**证书小** → 图论型；**但**这类问题恰好**竞争最激烈**（`SAT`/`nauty` 社区即在此）✓✓
【⟹ 诚实判定】 `P1-3` 池按当前门串（`G1∧G2∧G2.5∧G3∧G4`）**与竞争审计**，**尚未产生可投候选**；**约束不在"是否开放"，而在"我们能否比现有队伍更早压到可证书化量级"** ✓✓
【⛔ 纪律】 本轮**未计算**；`n=38`／`TARGET-L9`／RH 未作筛选依据 ✓
【边界】 §0 的 `81`／`319`／`Dumitru`／heavy-tailed 为**您提供的档级信息**（未读原文）；§1–§2 群作用与量级为**本档纸面估计**；§3 结论为**本档推导**；未制造候选／未启动搜索／未碰 RH。
