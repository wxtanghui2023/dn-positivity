# 第⑮关：Diagonal-Origin Audit —— q 究竟如何从"计数"变成"谱平方尺度"

**日期**：2026-09-10 ｜ 承接 `3a33990` ｜ 结果：q 之所以能变 √q，是因为【三重锁定】；char 0 的断裂点正在此处

---

## 0. 先登记 ⑭ 的收紧（唐先生）
原文"乘法 ⇒ log、加法 ⇒ √X"**不升格为定理**。保留版本：
$$\boxed{\text{在【自然 Haar/计数归一化】下，纯乘法侧的自然质量通常呈 }\log/\text{height 尺度}}$$
（避免把"自然测度"偷换成"所有可能构造"；防止以后反例被门槛误杀。）

---

## 1. 有限域里 q 到底怎么变成 √q（逐件拆）

**(a) 内禀自同态**：$F:x\mapsto x^q$ 是 $\mathbb F_q$ 自身的内禀映射。
**(b) 计数 = 不动点计数**：$\#\mathbb F_q=q$；更一般 $\#X(\mathbb F_{q^n})=\#\mathrm{Fix}(F^n)$（Lefschetz）
⟹ **计数尺度与自同态的幂次是同一个参数**。
**(c) 权重梯与中项**：上同调权重为 $\{1,\ q,\ q^2\}$（$H^0,H^1,H^2$），Poincaré 对偶交换 $w\leftrightarrow2-w$
⟹ **自对偶权重 = 中项 = $q^{1/2}$** ✓
**(d) ⭐ 锁定**：**同一个 $q$** 同时是
```
(i)   底域基数（计数尺度）
(ii)  自同态的乘子（x ↦ x^q 中的 q）
(iii) 权重单位（权重梯 {q^i} 的底）
```
$$\boxed{\sqrt q\ \text{不是"某个谱半径"，而是这三重性锁定时【权重梯的中项】}}$$

## 2. char 0 审计：三件中哪一件断了

| 部件 | char 0 是否可得 | 说明 |
|---|---|---|
| 计数尺度 $X$ | ✓ | $\#\{n\le X\}\sim X$ |
| 权重梯 + 自对偶点 | ✓ | **阿基米德分级**给出的梯的自对偶点正是 $s=1/2$（= 函数方程中心） |
| **乘子 = 计数尺度** | **✗** | 见下三例 |

**三例检查**：
```
· 乘 n 映射（ℤ/n 或乘法群）：乘子 = n ≠ 计数尺度 X（且被 gcd/模结构破坏）
· 移位/倍化映射（{1..X} 或 ℝ/ℤ 上 x↦2x）：
   乘子 = 2（熵型）⟹ 尺度是 e^{-n} 型（Perron–Frobenius），不是 X^{1/2}
· 分圆塔 ℚ ⊂ ℚ(ζ_{p^n})：层参数 n 与尺寸参数 disc ~ p^{n(1+o(1))} 【解耦】
   ⟹ 层与尺度的锁定在 char 0 【不存在】（正是 char p 中 q 既是基数又是乘子的对照）
```

$$\boxed{\text{⟹ char 0 的断裂点 = 不存在"计数尺度 = 自同态乘子 = 权重单位"的锁定对象}}$$

## 3. ⭐ 本关裁决：quartet 被解释为【锁定的四个侧面】

在 char p，锁定一旦成立，四件事**同时**出现（不是四个巧合）：
```
(i)   整性    ⟸ 有限型 + 自同态（特征多项式 ∈ ℤ[T]）
(ii)  对偶    ⟸ Poincaré 对偶（α ↔ q/α）
(iii) 独立阳性 ⟸ Hodge 指标（|α| ≤ √q，来自几何正性）
(iv)  共轭同步 ⟸ Galois 作用与锁定相容
```
在 char 0：**没有锁定** ⟹ 四者无法同时装配 ⟹ 与第⑨⑬⑭关的结论完全一致，并给出**根因**。

$$\boxed{\text{第⑮关答案：}q\to\sqrt q\ \text{的全部秘密是【三重锁定】；char 0 缺的正是这个锁定}}$$

## 4. 死亡测试（唐先生列的）+ 本关的对应解释
```
F_X = Fourier          ⟹ 只给 Q2（对称性），不给 Q3（正性）        ⟹ 死（⑧关）
F_X = Frobenius 替身   ⟹ 尺度挂到 p-adic 型（δ/Buium）             ⟹ 死（⑩关）
positivity = Weil 型   ⟹ ⟺ RH                                     ⟹ 死（⑬关）
X 只是外加权 w_X = X·w ⟹ 违反内禀性                                ⟹ 死（本关判据）
```

## 5. 新增门槛
```
P-Lock（本关）：任何 char 0 候选必须回答：
   其【计数尺度】、【内禀自同态的乘子】、【权重单位】是否为同一个参数？
   三者不同 ⟹ 无法导出 √X ⟹ 预筛淘汰
```

## 6. 诚实边界（最后一毫米）
```
· "三重锁定"是对函数域证明的结构性解读，不是定理
· "char 0 不存在锁定对象"是【未找到】，不是不存在定理
· 若存在带锁定的 char 0 对象而未被识别，本关结论需推翻（判据已给出，可检验）
· 未写程序；RH 本身未动
```

## 7. 障碍链最终形式（十五关收敛）
$$\boxed{\text{RH}\to\beta=\tfrac12\to\sqrt X\ \text{自对偶权重}\to
\text{Duality}+\text{独立正性}\to\textbf{计数/乘子/权重单位的锁定}\to
\text{char 0 目前无此对象}}$$

## 8. 提交链
```
3a33990 ⑭ → 本篇 ⑮
```
