已查地图（所查：`V249-external-search-triage-major-developments.md`（**外部搜索分流档**；含 `§1` 67.2% 与 `§2` `𝔽₁` 构造）、`CLOSED-ROUTES-MAP.md:1915` §4（**(a) GMC／log-correlated**：Saksman–Webb 2020／Arguin–Belius–Bourgade–Radziwiłł 证 FHK 主阶／Harper；**(b)** `H²(Dirichlet)` 再生核＝ζ（Hedenmalm–Lindqvist–Seip）；**(c)** **Kakeya sticky** 模板；**(d)** AI×数学；**(e)** 算术随机性）、`CLOSED-ROUTES-MAP.md:1702`（**`V237-D`＝Arithmetic dynamics ＋ intrinsic pairing ＋ nontrivial defect**，含硬门 `A`–`E` 与要求 1–8）、`V192` §③、`DIMENSION-QUESTION-answer.md`、`E19-model-alignment.md`、`V219`）。**结论**：唐先生所问三块（**量子混沌／对数气体／算术动力学**）**均已在本项目档案内登记**；本轮外部检索**未发现档案之外的新对象或新机制** ✓；三块在"**两面**"二分下的归宿明确：**量子混沌／GUE ⟹ γ-侧统计 ⟹ `β`-盲**；**对数气体／GMC ⟹ `|ζ(½+it)|` 的值分布 ⟹ 值面 ⟹ 不含 `β`**；**算术动力学 ⟹ 已登记为 `V237-D`（defect 形式）**，经典文献（heights／canonical heights／rational maps）**无 ζ-零点输入** ✓✓ ⭐ 并标出**一条同名陷阱**（对数气体的 `β`＝系综**温度参数** ≠ 零点实部 `β`）✓✓

# C-96 · **外部扫描三块（量子混沌／对数气体／算术动力学）——登记核对 ＋ 过滤判词**

> **时间**：2026-09-18 14:06 唐先生：搜索这三块的研究成果，看有无新启发与方向
> **纪律**：**先查地图**（`C-91` §4 硬过滤器）→ 再检索 → 再过滤 ✓；⚠️ 检索项一律 `untrusted`、**未逐条核原文** ✓

---

## §0 结论（先行）

$$\textbf{(1)}\ \textbf{三块均已在档案登记}：\text{量子混沌／GUE（16／74 档命中）};\ \text{对数气体／Coulomb gas（9／5 档）};\ \text{算术动力学（2 档＋`V237-D`）}✓$$
$$\textbf{(2)}\ \text{本轮外部检索}\ \textbf{未发现档案之外的新对象／新机制}✓\（\text{检索所得几乎全部为档案已登记项或其经典前身}）$$
$$\textbf{(3)}\ \text{三块在}\ \textbf{两面二分} \text{下的归宿}：$$
$$\qquad \text{量子混沌／GUE} \Longrightarrow \gamma\text{-侧统计} \Longrightarrow \boxed{\textbf{`β`-盲}}✓\（\text{`V192` §③：实谱实现只看见}\ \gamma，\ \beta\ \text{只经重数}）$$
$$\qquad \text{对数气体／GMC} \Longrightarrow |\zeta(\tfrac12+it)|\ \text{的值分布} \Longrightarrow \boxed{\textbf{值面}} \Longrightarrow \text{不含}\ \beta✓$$
$$\qquad \text{算术动力学} \Longrightarrow \text{已登记为}\ \text{`V237-D`}（\text{defect 形式}）;\ \text{经典文献无 ζ-零点输入} \Longrightarrow \textbf{无新输入}✓$$
$$\textbf{(4)}\ ⭐\ \textbf{同名陷阱（本档新标）}：\text{对数气体的}\ \beta\ \text{＝}\textbf{系综温度参数};\ \text{我方的}\ \beta\ \text{＝}\textbf{零点实部} \Longrightarrow \textbf{同名不同量}✗✓✓$$

---

## §1 档案登记状态（先查地图的结果）

| 块 | 档案登记 | 已登记内容（关键）|
|:--|:--|:--|
| **量子混沌** | 16 档（`CONNES2026-read-1.md` 等）＋`GUE` 74 档 | `V192` §③ **谱实现族封印**：实谱实现只看 `γ`，`β` 只经**重数／退化** ⟹ 撞 **`0.6818287`** 天花板；`V249` §1：该通道下界 `41.6% → 67.2%`（Anthropic／Claude，Lean 验证）⟹ 与上界 `68.18%` **夹在 1% 内** |
| **对数气体** | 9 档＋`Coulomb gas` 5 档 | `DIMENSION-QUESTION-answer.md`：**＝ 1D Coulomb gas（`β=2`）⟹ 一维对象，只给 `γ` 分布，不碰 `β`**；`E19`：能复现 **GUE 配对相关但符号相反** |
| **算术动力学** | 2 档＋`Arithmetic dynamic` 3 档 | `CLOSED-ROUTES-MAP:1702`（**`V237-D`**）：RH 改写为 **defect 消失**；硬门 **`A`–`E`** ＋要求 **1–8**；⚠️ 预注册判死：**若 `F` 只是乘法／卷积算子 ⟹ `F*BF−B` 落 convolution／adjoint／correlation／二次型／trace／positivity ⟹ 全部已进 `V185`／`V188`／`V199`／`V200`** |
| **（附）GMC** | `CLOSED-ROUTES-MAP:1915(a)` | Saksman–Webb（Ann. Prob. **48**(6) 2680–2754, 2020）`ζ(½+iωT+it) →` **复 GMC**；Arguin–Belius–Bourgade–Radziwiłł 证 **FHK 猜想主阶**；Harper（log-correlated／BRW）⭐ **临界 GMC 参数恰为 2** |
| **（附）Kakeya sticky** | `CLOSED-ROUTES-MAP:1915(c)` | Wang–Zahl（2025-02）3 维 Kakeya；2026-01 与 Guth 简化；Lean 形式化；⭐ 方法＝归约到 **sticky** ＋关键引理 **跨尺度严格下降**、可跨多尺度迭代（`V230`／`V240` 想要的模板，**已证可行**）|

## §2 本轮外部检索所得（⚠️ 全部 `untrusted`、检索级）

$$\text{(i)}\ \text{FHK 线}：\text{Fyodorov–Hiary–Keating 猜想（`|\zeta(\tfrac12+it)|` 在短区间上的极大值属 **log-correlated 普适类**）}\ \textbf{已被证明主阶}（\text{Arguin–Bourgade–Radziwiłł}）✓;\ \text{Harper 另有独立模型复现}✓$$
$$\text{(ii)}\ \text{量子混沌／谱形因子}：\text{标准表述仍是}\ \textbf{Pólya–Hilbert 谱解释 ＋ Montgomery 配对相关 ＋ Berry 半经典 ＋ 谱形因子三条分类};\ \text{检索所得}\ \textbf{无 2025–2026 新的 ζ-}\beta\ \text{通道}✓$$
$$\text{(iii)}\ \text{算术动力学}：\text{检索命中以}\ \textbf{经典理论} \text{为主}（\text{heights／canonical heights／rational maps／dynamical zeta functions，Silverman／Benedetto／Ingram}）;\ \text{与 ζ 零点的}\ \textbf{直接联系文献未见}✓$$
$$\qquad ⚠️\ \text{（仅见 arXiv:2202.01064 "A Dynamical Systems Framework for Generating the Riemann Zeta Function"——}math.GM\ \text{类、通用数学投稿，}\textbf{可信度低}，不采用）✓$$

## §3 过滤（对象 → 结构公理 → 零点性质 ＋ `V199` 三门 ＋ `C-91` 硬过滤器）

| 块 | 对象 | 结构公理 | 推出的零点性质 | 过滤判定 |
|:--|:--|:--|:--|:--|
| 量子混沌／GUE | `{γ_ρ}` 的**统计** | 随机矩阵普适性 | **只约束 `γ`** | **门①失败（`β`-盲）**；且 `V192` §③ 已封 |
| 谱形因子／配对相关 | 同上（二阶统计） | — | 同上 | **门②失败**（二次型 ⟹ `⟺`RH，`POS1` 第三行）|
| 对数气体／GMC | `|\zeta(\tfrac12+it)|` 的**值分布** | 1/f-噪声／冻结转变 | **值面**（不含 `β`）| **门①失败（值面）** |
| 算术动力学（经典）| rational maps／heights | 高度理论 | **无 ζ-零点联系** | **门①失败（无算术来源到零点）** |
| 算术动力学（`V237-D` 形式）| `(F_X,B_X)` ＋ defect | **内部配对非平凡缺陷** | `|\alpha|^2−1 = C/B` | ⚠️ **唯一带 `β` 的形式**；但预注册判死：`F` 若为乘法／卷积 ⟹ 落回 `V185`/`V188`/`V199`/`V200` |

$$\Longrightarrow\ \text{三块}\ \textbf{全部}\ \text{命中同一壁垒}：\text{要么}\ \gamma\text{-侧（}\beta\text{-盲）},\ \text{要么值面（不含}\ \beta）,\ \text{要么已预注册其判死条件}✓✓$$

## §4 ⭐ 同名陷阱（**本档新标**；与 `V219` 纪律同类）

$$\text{对数气体／GMC 文献里的}\ \beta\ \text{＝}\textbf{系综逆温度}（\text{如 "临界 GMC 参数恰为 2"、"}\beta=2\ \text{即 GUE／Coulomb gas"）✓$$
$$\qquad ⚠️\ \text{而}\ \text{我方}\ \beta\ \text{＝}\text{零点实部}\ \Re\rho✓✓$$
$$\Longrightarrow\ \boxed{\text{两个}\ \beta\ \textbf{同名不同量}} \Longrightarrow \textbf{严禁} \text{把"}\beta=2\ \text{临界"读作"}\tfrac12\ \text{特殊"}\ ✗✓$$
$$\qquad \text{同类前例}：\text{`V219`}\ \text{的"}\textbf{同一个}\ \tfrac12\ \ne\ \textbf{同一个零点机制}}✓✓$$

## §5 唯一值得记一笔的线索（**已在档案**）

$$\text{Kakeya/sticky 模板（`CLOSED-ROUTES-MAP:1915(c)`）}：\textbf{跨尺度严格下降 ＋ 可多尺度迭代} \Longrightarrow \text{与}\ \text{`V230`／`V240`}\ \text{所求"}\textbf{严格尺度缺陷可迭代} \text{"模板同形}✓$$
$$\qquad ⚠️\ \text{但其为}\ \textbf{方法模板} \text{（非对象）} \Longrightarrow \text{按}\ \text{`C-91` §4}\ \text{硬过滤器：}\textbf{必须}\ \text{先在 ζ 上}\ \textbf{实例化} \text{出"跨尺度严格下降量"方可重开}✓$$

## §6 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-18 14:0x）`[纪律]`（先跑后写）

```
技术词 同名同形    命中文件数=0
技术词 值面命中    命中文件数=0
技术词 跨尺度严格下降  命中文件数=3  :: ./V249-external-search-triage-major-developments.md ./CLOSED-ROUTES-MAP.md ./MASTER-STATUS-AND-CLOSURES.md
技术词 sticky       命中文件数=3  :: ./V249-external-search-triage-major-developments.md ./CLOSED-ROUTES-MAP.md ./MASTER-STATUS-AND-CLOSURES.md
```
**读数**：`同名同形`／`值面命中`＝**0 档 ⟹ 本档新增** ✓；`跨尺度严格下降`／`sticky`＝**3 档 ⟹ 档案已有**（`V249` 等）⟹ 本档**引用** ✓

## §7 边界

- `[档案]` §1 各项均为**档内逐字／指针** ✓；`[检索]` §2 为 `untrusted` 检索级，**未逐条核原文** ⚠️
- `[本档]` §3 过滤表、§4 **同名陷阱**、§5 判据 ✓
- **不声称**：三块"已死" ✗；**不声称**检索穷尽 ✗；不证 RH ✗
- **纪律**：先查后判（R-1 ✓，**先跑后写** ✓）；**未用 RH 作推导** ✓；**零数值** ✓

```
⚠️ 唐先生 14:06：搜索量子混沌／对数气体／算术动力学三块，看有无新启发与方向
✅ 先查地图（关键）：三块**均已在本项目档案登记** ——
   量子混沌 16 档＋GUE 74 档；对数气体 9 档＋Coulomb gas 5 档；算术动力学 2 档＋V237-D；
   档案已有专门的外部扫描分流档 V249（唐先生 2026-09-15 21:22 委托"不预设方向、扩大搜索"）
   且 CLOSED-ROUTES-MAP:1915 §4 已登记 (a) GMC/log-correlated（Saksman–Webb 2020；Arguin–Belius–Bourgade–Radziwiłł 证 FHK 主阶；
   Harper；临界 GMC 参数恰为 2）(b) H²(Dirichlet) 再生核＝ζ（Hedenmalm–Lindqvist–Seip）(c) Kakeya sticky 模板
   （跨尺度严格下降，可多尺度迭代，V230/V240 所求模板，已证可行）(d) AI×数学 (e) 算术随机性
   CLOSED-ROUTES-MAP:1702 = V237-D（Arithmetic dynamics + intrinsic pairing + nontrivial defect；硬门 A–E 与要求 1–8；
   预注册判死：F 若只是乘法/卷积 ⟹ F*BF−B 落 convolution/adjoint/correlation/二次型/trace/positivity ⟹ 全部已进 V185/V188/V199/V200）
✅ 本轮检索：未发现档案之外的新对象或新机制；FHK 已证（值分布定理）；量子混沌检索无 2025–2026 新 ζ-β 通道；
   算术动力学检索以经典理论为主（heights/canonical heights/rational maps），与 ζ 零点直接联系的文献未见
   （仅 arXiv:2202.01064，math.GM 类、可信度低，不采用）
⭐ 过滤判词（对象→结构公理→零点性质 + V199 三门 + C-91 硬过滤器）：三块全部命中同一壁垒 ——
   量子混沌/GUE ⟹ γ-侧统计 ⟹ **β-盲**（V192 §③）；谱形因子/配对相关 ⟹ 二次型 ⟹ 门②（⟺RH）；
   对数气体/GMC ⟹ **值面**（|ζ(1/2+it)| 值分布）⟹ 不含 β；算术动力学（经典）⟹ 无 ζ-零点输入（门①）；
   算术动力学（V237-D 形式）= 唯一带 β 的形式，但已预注册判死条件
⭐ 同名陷阱（本档新标）：对数气体文献的 β = **系综逆温度**（"临界 GMC 参数恰为 2"、β=2 即 GUE/Coulomb gas）；
   我方 β = **零点实部** ⟹ 同名不同量 ⟹ **严禁**把"β=2 临界"读作"1/2 特殊"（同类前例：V219 的"同一个 1/2 ≠ 同一个零点机制"）
⚠️ 唯一记一笔的线索（已在档案）：Kakeya/sticky 模板（跨尺度严格下降 + 可多尺度迭代）与 V230/V240 所求同形，
   但为**方法模板**（非对象）⟹ 按 C-91 §4 硬过滤器须先在 ζ 上实例化出"跨尺度严格下降量"方可重开
✅ 净产出：①三块登记状态核对 ✓；②检索所得登记 ✓；③过滤判词表 ✓；④同名陷阱 ✓；⑤线索与判据 ✓
```
