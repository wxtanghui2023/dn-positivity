已查地图（所查：`RESEARCH-CONSTITUTION.md` **§8.1–§8.6**（`D0`–`D4` 受理制；`\boxed{\text{口径}＝\text{连续 }D1=0\text{ 轮数}；\ \text{连续 3 轮} \Longrightarrow \text{冻结／重设坐标系}}`；`§8.2` Round 2 `D1=0`；`§8.5` Round 3 `D1=0`（2/3）；`§8.6` 冻结审计 `FZ-1`：46 NO-GO ⟹ 6 母机制／独立障碍 12）、`FREEZE-2-coordinate-system-audit-novelty-ratio-and-retrieval-blind-spot.md`（本会话：`C*.md` 108 档中**自称"本档新增"68 档（63%）** vs **提及 `D1` 仅 15 档（14%）**）、`scripts/git-hooks/pre-commit`（规则 3 载体）、`ERRATUM-T10` §5（NEWNESS RULE 依据））。**结论**：本档为 **`D0/D1` 账本的可读载体＋机器可读种子**，把已存在但曾失效的 `§8.1` 规则接到**不会忘的机器**上（git hook）✓✓。**这不是新规矩**，而是**已写下、又曾失效的规矩的强制执行** ✓

FREEZE-ACK: 本档即冻结期内的账本补丁（依 `§8.1`；不产候选，不新增 `N` 项）

# D0/D1 账本（Ledger）· **规则 ＋ 历史 ＋ 机器种子**

> **立档**：2026-09-18 19:13 唐先生**「写」**（批准把 `D0/D1` 声明接入 pre-commit）
> **依据**：`RESEARCH-CONSTITUTION.md` §8.1（口径＝连续 `D1=0` 轮数；3 轮 ⟹ 冻结）— **规则原本就有，本档只做"接机器"**

---

## §1 规则（强制字段）

**任何新增 `docs/*.md` 必须在档首（`已查地图` 行之后）写两行**：

```
D0: 本档对象 = 档案已有 <哪个对象> 的 <什么关系>   （重命名／新对象／无对象）
D1: 独立自由度 = <…>   或   D1: 0
```

$$\textbf{`D0`}（\text{对象新颖性}）\ \textbf{逐字依宪法}：\mathcal X\ \textbf{非已知 RH 对象重命名}✓✓$$
$$\textbf{`D1`}（\text{独立自由度}）：q_{\rm new}\notin{\rm closure}(N0\text{–}N46)✓$$
$$\textbf{升格}：\text{连续}\ D1=0\ \text{达}\ 3 \Longrightarrow \text{新档必须含}\ \texttt{FREEZE-ACK:}\ \text{令牌}✓✓$$

## §2 机器种子（hook 读取）

```
LEDGER-SEED: 5
```

**含义**：本行数值 ＝ **规则上线前的连续 `D1=0` 计数**（由 `FREEZE-2` 的统计核定）。hook 会把它与 git 扫描的"最近新增档连续 `D1=0`"相加，得到当前计数并打印。

**当前状态**：连续 `D1=0` ＝ **5**（`C-125`／`C-126`／`C-127`／`C-128`／`C-129`）⟹ **已达阈值 ≥ 3** ⟹ **冻结期**：所有新增 docs 档须含 `FREEZE-ACK:` ✓

**重置规则**：当某档**确实**给出非零、可检验的独立自由度（`D1: <值>` ＋ `FREEZE-ACK: D1-ESCAPE <理由>`）⟹ 由该档作者**把本行改为 `LEDGER-SEED: 0`** ✓

## §3 历史（为什么需要它）

| 时期 | 状态 |
|:--|:--|
| `RESEARCH-CONSTITUTION` §8 时期 | ✅ **在位**：`D0`–`D4` 受理制 ＋ 账本 ＋ 阈值冻结；`Round 1` `D1`✓／`Round 2` `D1=0`／`Round 3` `D1=0` ⟹ **冻结已执行**（`FZ-1`–`FZ-4`）|
| 其后（`C` 系列 108 档） | ⚠️ **失效**：**63%** 宣称"本档新增"，**仅 14%** 提及 `D1`，**2%** 记录 `D1=0` |
| `FZ-1` 自己的审计 | 46 条 NO-GO ⟹ **6 个母机制**（独立障碍 12）⟹ **真正独立 ≈ 13%** |
| 根因（`C-129` §4 ＋ `FREEZE-2` §3） | **"新性宣称"与"新性判据"脱钩**；替补手段（`grep` 新词）**必 0 命中** ⟹ 结构上发现不了重命名 |

$$\Longrightarrow \textbf{教训}：\text{规矩曾写下、执行过一次、然后失效};\ \text{失效原因}\ \textbf{不是没规矩}，\ \text{而是}\ \textbf{规矩挂在"记得查"上}✓✓$$

## §4 本档的 `D0`/`D1`

```
D0: 本档对象 = 档案已有 §8.1 规则的【强制执行载体】（git hook ＋ 种子行）—— 关系 = 工程化，非新对象
D1: 0
```

## §5 边界

- ⚠️ 本档**不产候选**、**不新增 `N` 项**、**不改任何已有判词** ✓
- ⚠️ 本档**不改变**任何数学结论；只把**已存在的规则**接到机器上 ✓
- ⚠️ 种子数值为**核定值**（依 `FREEZE-2` §2.1 的关键词命中口径），**不是**逐档内容判定 ✓
- **不声称**：RH；不声称 `D0/D1` 覆盖一切新性形式 ✓

## §6 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-18 19:1x）`[纪律]`（先跑后写）

```
技术词 账本种子          命中文件数=1  :: ./D0-D1-LEDGER.md（本档）
技术词 接机器           命中文件数=1  :: ./D0-D1-LEDGER.md（本档）
技术词 机器可读         命中文件数=1  :: ./D0-D1-LEDGER.md（本档）
```
**读数（按实测）**：三项均＝**1 档（仅本档）⟹ 本档新增措辞** ✓
