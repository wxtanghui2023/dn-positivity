# B3：中心层 → 表示 conductor（**构造优先，未算 1/2**）

**依据**：唐先生 B3 ①→②→③（构造优先、审计后置；**禁止预设** $1/2$、$r\mapsto(r-1)/2$、平方根、临界指数）｜ **L2 冻结不动**
**脚本**：`scripts/B3_central_layer.py`｜所有陈述标注【计算】/【引用】/【结构性论证】

---

## ① $V_7=G_7/G_8\cong\mathbb Z/2$ 在 2 维不可约表示中的显式矩阵作用【计算】
$$\rho(\sigma)=\begin{pmatrix}0&-1\\1&0\end{pmatrix}=:R,\qquad \rho(\tau)=\begin{pmatrix}1&0\\0&-1\end{pmatrix}=:S$$
**自检（全部通过）**：
```
S R S = R^-1  ✓   |   R^4 = I  ✓   |   S^2 = I  ✓     （D4 关系成立）
ρ(σ^2) = R^2 = [[-1,0],[0,-1]] = -I_2  ✓
ρ(σ^3) = R^3 = [[0,1],[-1,0]]
```

## ② Schur：$\sigma^2\mapsto-I_2$ 是否**必然**【结构性论证 + 计算】
```
【计算】Z(D4) = {1, σ^2}，σ^2 中心 ✓
         χ_ρ(1) = 2，⟨χ_ρ,χ_ρ⟩ = 1  ⟹ ρ 不可约 ✓
         ker ρ = {1}  ⟹ ρ【忠实】（⚠️ 更正：不是 ⟨σ^2⟩；忠实比"看见中心层"更强——它看见【所有】层）
【论证】σ^2 中心 ⟹ 由 Schur 引理，ρ(σ^2) = λ·I，且 (σ^2)^2 = 1 ⟹ λ = ±1
         若 λ = +1，则 ρ 通过 D4/⟨σ^2⟩ ≅ (Z/2)^2 分解，而 (Z/2)^2 的不可约表示全部是 1 维
         ⟹ ρ 可约，与 ⟨χ,χ⟩=1（dim 2 不可约）矛盾
⟹ λ = -1 【被强制】：ρ(σ^2) = -I_2 必然成立
```

## ③ 与 conductor $a(\rho)=8$ 的**精确（结构性）对应**【计算 + 结构性解释】
$$a(\rho)=\sum_{i\ge0}\frac{1}{[G_0:G_i]}\operatorname{codim}V^{G_i}\qquad\text{（按"层"（$G_i$ 恒定的块）分组）}$$
| 层（块） | 指标 $i$ | 权重和 | $\operatorname{codim}V^{G_i}$ | 贡献 |
|---|---|---|---|---|
| $G_0=G_1=D_4$ | 0,1 | 2 | 2 | **4** |
| $G_2=G_3=\langle\sigma\rangle$ | 2,3 | 1 | 2 | **2** |
| $G_4..G_7=\langle\sigma^2\rangle$ | 4–7 | 1 | 2 | **2** |
$$\sum=4+2+2=\mathbf 8=a(\rho)\quad\checkmark$$
**⭐ 精确对应的内容（不是数值对应）**：
```
codim V^{G_i} = dim V = 2 在【每一层】成立；而在中心层，其原因是【恰为】ρ(σ^2) = -I_2：
  不动向量 = ρ(σ^2) 的特征值 +1 的特征向量；-I_2 【没有】这样的特征向量 ⟹ 不动空间 = 0 ⟹ codim = 2 = dim ρ
⟹ 中心层的贡献 = (权重和 1) × (codim = dim ρ = 2) = 2，即中心层恰好贡献 dim(ρ)
   而 dim(ρ) = 2 本身又由 σ^2 ↦ -I_2 经不可约性强制
```
**权重和 1 与 upper-break 区间长度的对应**：upper breaks = 1, 2, 3（B1 已验证）⟹ 区间 $[0,1),[1,2),[2,3)$ 长度各为 1 ✓（与后两层的权重和 1 一致 ✓）

## ④ 是否存在"中心层 → conductor"的天然传递量
```
是：每层的天然传递量为二元组 (ℓ_k, codim_k)，其中
   ℓ_k     = 该层在 upper numbering 中的区间长度
   codim_k = 不动子空间的余维
   层的贡献 = ℓ_k · codim_k           （总：a(ρ) = codim V^{G_0} + Σ_k ℓ_k·codim_k = 2 + 6 = 8 ✓）
中心层：(ℓ, codim) = (1, 2)，且 codim = dim ρ 【由 ρ(σ^2) = -I_2 造成】✓
【结构性原因】ρ 忠实 ⟹ 每个非平凡子群都有元素无特征值 1 的特征向量 ⟹ codim ≡ 2 于【所有】层
```

## ⑤ 能否与 A′ 的双尺度对象形成**同一个**递推
```
形状上【是】：两套独立算法给出同一总量 ——
    ramification 侧 Σ(|G_i|-1) = 24 ｜ representation 侧 Σχ(1)a(χ) = 24
   这与 A′ 的"守恒总量"（S_+ + S_- = log|G|）【同形】，且其一致性是定理（conductor–discriminant）✓
但【不是】层对层的配对：
   2 维表示同时涉及【全部三层】，故两侧无法逐层配对；两侧的总量一致是【整体】性质
⟹ 逐层的双尺度递推【没有】从本构造中自然产生
```
**关键事实（如实报告，但【不作为输入】）**：
```
ramification 侧的【块值】为 |G_i|-1 = 7(×2), 3(×2), 1(×4) ⟹ 块值序列 (7, 3, 1)
其来源是群阶 (8, 4, 2)（即 upper breaks 等距为 1,2,3 的直接后果）—— 这是【几何事实】，
【不是】把 r↦(r-1)/2 作为目标输入（遵守约束 6）
```
$$\text{representation 侧每层数据}:\ (ℓ_k,\operatorname{codim}_k)=(1,2)\ \text{在全部三层【一致】} \Longrightarrow \text{无层间变化} \Longrightarrow \text{无 } F \Longrightarrow \text{无不动点可查}$$
**（遵守约束 7：没有 $F$，因此本轮【不检查】任何不动点）**

## 本轮结论（遵守约束 9：只记正结构，不判死）
$$\boxed{\textbf{B3-positive structure}:\quad \text{中心层}\ \langle\sigma^2\rangle\ \longleftrightarrow\ a(\rho)=8\ \text{的} 2\ \text{贡献}\ \text{（经}\ \rho(\sigma^2)=-I_2\ \text{的形式对应）}}$$
```
· ① 显式作用成立；② σ²↦-I₂ 被 Schur + 不可约性【强制】；③ 对应是结构性的（codim = dim ρ 由 -I₂ 造成）
· ④ 天然传递量存在：每层 (ℓ_k, codim_k)，中心层为 (1, 2)
· ⑤ 与 A′ 只是【形状】相同（两套算法同总量）；层对层递推【未】自然产生
· 未获得 F ⟹ 未检查不动点 ⟹ Layer 3/4 仍【不】重开（约束 8 的条件未满足）
```

## 边界（严格遵守唐先生 10 条）
```
· 计算级：①②③④ 的全部矩阵、Schur 数据、per-level 分解、块值 (7,3,1) 及其来源
· 引用（非我证明）：Artin conductor 的离散公式；Schur 引理；(Z/2)^2 的不可约表示全为 1 维
· 结构性论证：σ²↦-I₂ 的强制性；codim = dim ρ 的结构原因；形状相同但无逐层配对的判断
· 【未做】约束 6：未预设 1/2、未用 r↦(r-1)/2、未预设平方根、未预设临界指数
· 【未做】约束 7：无 F ⟹ 未做不动点检查    ｜ 约束 10：L2 未改（保持 VERIFIED 冻结）
· 未做审计；未声称与 ζ 连接
```

## 提交链
```
a479721 B1+B2 → 本篇（B3：中心层 → 表示 conductor，B3-positive structure）
```
