# E2：二阶尺度传输 —— **精确核验结果**（含 completion 三元收口的登记）

**依据**：唐先生 §11–§20（transport 失配 / 二阶耦合 / 有限全平坦 + 无限 anomaly）｜**约束**：无 $1/2$ 输入、无人为权重、L2 未动
**脚本**：`scripts/E2_second_order_transport.py`（**先校准后核验**）｜**标注**：【核验】= 本轮精确计算；【引用】= 经典结果

---

## §0 登记：completion 三元收口（同意）
$$\text{profinite}\ \cup\ \text{Archimedean}\ \cup\ \text{adelic}\qquad\text{（Ostrowski 型约束；与 E1 的"}\widehat{\mathbb Z}\times\mathbb R_{>0}\text{"结论一致）}$$
⟹ **Arithmetic Completion 本身不是活路** ✓（同意）

## §1 核验设置（校准先通过）
```
校准（已知答案，全部通过）：Ω(12)=3｜ω(12)=2｜d(12)=6｜σ(12)=28｜φ(12)=4｜π(100)=25｜Ω(1)=0｜d(1)=1
网格：1266 组 (n,p,q)，p<q 为不整除 n 的不同素数
判据：Δ_{p,q}L(n) = L(pqn) − L(pn) − L(qn) + L(n)
```

## §2 【核验】结果表（每一行 1266/1266 全部匹配）
| 类别 | $L$ | $\Delta_{p,q}L(n)$ 实算 | 判定 |
|---|---|---|---|
| **加性** | $\log n$ | **$0$** ✓ | 平坦（无二阶耦合） |
| **加性** | $\Omega,\ \omega,\ v_2$ | **$0$** ✓ | 平坦 |
| **乘性** | $d,\ \sigma,\ \phi$ | $L(n)\bigl(L(p)-1\bigr)\bigl(L(q)-1\bigr)$ ✓ | **非零但【只依赖局部数据】** |
| **分解决定（非乘性）** | $\Omega^2$ | $2$（常数）✓ | 仍只依赖局部数据 |
| **分解决定（非乘性）** | $d^2$ | $9\,d(n)^2$ ✓ | 仍只依赖局部数据 |
| **加法结构** | $n-1$ | $n(p-1)(q-1)$ ✓ | 只依赖局部/多项式数据 |
| **非局部** | $\pi(n)$ | $\Delta\neq0$（400/400）✓ | **【不】可因式分解（0/400）** ⟹ 真非局部 |

## §3 ⭐ 二分结论（结构性；在测试电池上核验）
$$\boxed{L\ \text{由 }n\text{ 的素因子分解决定}\iff \Delta_{p,q}L(n)\ \text{只依赖局部数据}\Longrightarrow \text{唐先生 §13 死因}}$$
$$\boxed{L\ \text{非分解决定}\Longrightarrow \text{自然候选}=\text{计数/误差函数}\ (\pi,\psi,M,\Delta(x),\text{类数})\Longrightarrow \text{显式公式 / L-值类}\Longrightarrow \text{唐先生 §20 排除}}$$
**要紧的推论**：唐先生的 §13 判死条件 **⟺ "L 是分解决定的"** —— 这把该路线的活动范围精确限定为"**非分解决定的 L**"，
而非分解决定的 canonical L **恰好就是**显式公式/L-值类 ⟹ **§20 排除项自动命中** ✓

## §4 ⭐⭐ 更强的一条：他的 §14/§15 形状有**经典名字**
$$\boxed{\text{"每个有限层严格平坦，但整体出现残余"}= \text{局部-整体 obstruction 的经典形状}}$$
| 经典实例 | 形状 | 其"anomaly"由什么测度 |
|---|---|---|
| $\mathrm{Sha}$（Tate–Shafarevich） | 处处局部平凡、整体非平凡 ✓ | **L-值**（Tate 对偶 / BSD 型）【引用】 |
| 类群 $Cl_K$ | 处处局部平凡、整体非平凡 ✓ | **L-值**（类数公式）【引用】 |
| Brauer–Manin obstruction | 局部处处有解、整体无解 ✓ | 对偶定理 + L-系数【引用】 |
$$\boxed{\Longrightarrow\ \text{canonical 的"有限平坦 + 无限 anomaly"实例【全部】由 L-值测度}\ \Longrightarrow\ \text{§20 排除项全部命中}}$$

## §5 结论（诚实，含规模判断）
```
· §20 的门槛 ⟺ "存在一个【不由 L-值测度】的局部-整体 obstruction" —— 【无已知实例】
· 且该存在性将是【远超 RH 的独立大结果】（它会推翻算术中"局部-整体 obstruction 由 L-值测度"的
  普遍预期）⟹ 这条新路线的门槛【至少与一个著名猜想同级】，不是在 RH 之上的"小改进"
· 因此：按唐先生 §20 自己的判死条款，本代应判死（在已识别 canonical 来源内）
· 但必须承认：§14/§15 的【表述】比前几轮更锋利（"有限全平坦 + 无限 anomaly" 是很干净的判据）✓
```

## §6 边界
```
· §2 表格为【核验】（1266×7 组，逐组精确匹配；π 的 400 组全部非零且不可因式分解）
· §3 二分为结构性结论 + 电池核验（非全称定理）
· §4 表格中 Ш/类群/Brauer–Manin 与 L-值测度为【引用】
· 【未做】未输入 1/2；未构造模型；未改 L2；未声称与 ζ 连接
```

## §7 提交链
```
034ae0a E1 → 本篇（E2：二阶传输精确核验 + completion 收口 + Ш 形状识别）
```
