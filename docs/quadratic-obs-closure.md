# 二次观测攻击——闭环确认（所有组合收敛——）

> 2026-09-07 22:15 · 唐先生指向的"二次/相位消除观测"门

## 测试对象
|G(t)|²——相关函数——等权变体——（多零点模态叠加的二次量——）

## 结果
### 普通二次（|G(t)|²——系数 1/ρ——）
- 对角项 Σe^{2δ_ρt}/|ρ|²——在线常数——离轴增长但 **1/|ρ|² 衰减（1/γ²——）**
- 交叉项 e^{i(γ−γ')t}——需长时间平均消除（分辨——）
- 数值：带离轴（γ=100——）不明显（t=20 反而小——交叉振荡——）

### 微分+二次（等权模态——|x^{1/2}ψ'(x)|²——）
- 对角 Σe^{2δ_ρt}（无 1/γ²——）——离轴增长 e^{2δt}
- **但**——交叉项消除需长时间平均——平均后 = 已知判别（ψ' 的 L²——RH 等价——）

## ⭐ 闭环确认（唐先生的——）
```
单模态 |f_ρ|² = e^{2δt}（γ 消失——O(1)——）✓ 理想
实际观测 = 多模态叠加——交叉项 e^{i(γ_ρ−γ_ρ')t}
消除交叉 → 需要频率分辨（长时间/平均/滤波——）
分辨 → 恢复零点谱信息 → 判别已知（RH 等价——）→ 证明循环
⟹ 即使最强组合（微分+二次——）也收敛到已知判别——需分辨
```

## 结构观察
- 二次量的"权重衰减"来源：模态系数（1/ρ——显式公式结构性——）或交叉项（需分辨——）
- **没有组合能同时：zero-blind + γ-blind + δ-sensitive + 无需分辨**
- Amplification ⟺ Resolution（唐先生的命题——数值/结构支持——）

## P51-T5 状态更新
Amplification-Resolution Dichotomy——**候选结构定理——获得多路支持**：
- 微分放大（ψ'——）：放大 = 去 1/ρ = 更高分辨（频率可读——）
- 二次观测（|G|²——）：δ 提取需对角 = 需分辨
- 所有组合收敛：放大 ↔ 分辨——无"免费午餐"

## 剩余（未闭合——）
- "分辨能力 ⟹ 恢复零点谱"的严格算子化（POC——）
- 是否存在非模态叠加的观测形态（不经显式公式——）？
- 数值仅支持（非定理——）——需算子级证明

## 文件
- scripts/quadratic_obs_attack.py——测试
