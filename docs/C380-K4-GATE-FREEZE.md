# C380-K4-GATE-FREEZE — K4 Gate 冻结规格（**纸面设计**；通过前不得由 HOLD → ACTIVE）

已查地图（**先查后写** ✓）：`C380-MASTER-LEDGER-FREEZE-2026-09-22.md`（**四分类 ＋ 五条防重审规则** ✓✓）、`C380-S1-STEP0.md`（**S1-A FALSE 裁定；near-collision 过滤器** ✓✓）、`C380-221-COMPUTATION-SPEC-FREEZE.md`（**封面证书模板：§15 执行协议／§16 dry-run** ✓✓）、`C3899c`（**k=4 = OPEN；退化内点锚 Q={1,2,4,9,10}** ✓✓）、`C3899o`／`p`（**二参数路线永久停止** ✓✓）、`C380-221-CERTIFICATE-RESULT.md`（**已验证的仪器与复核链** ✓✓）

D0: 本档对象 = **C-380-132：K4 Gate 冻结规格**（档案已有对象 `K4` 的**规格关系**：OPEN → OPEN＋Gate 条件；非重命名、非新对象）
D1: 0
FREEZE-ACK: 本档即冻结审计（连续 D1=0 ＝ 35 ≥ 3；本档为**规格登记**，未引入新自由度）

---

## §0 裁定登记（可引用 ✓✓）

```
S1-A   = **FALSE / CLOSED**（结构性反例族：k=5、max_r|F_{2r+1}| = 0.0664397 ≪ 1/2、
         40 位复核、开集性稳健；机制 = **近碰撞对 ＋ π/2 边界节点**）✗✗
S1-B   = **DECLINED / 不立项**（域改 E_even ⟹ 陈述即原目标 γ^(13) > 1/2 ⟹ 失去"独立机制"资格 ✗；
         **不得**日后包装成 S1 重开 ✓）
S1     = **PRIMARY → ARCHIVE / DEAD** ✓✗
K4     = **OPEN**，并取得下一阶段**唯一主动资源资格** ✓
C-3900 = **HOLD**（Gate 未冻结前不得 ACTIVE）✓✓
【方法过滤器（永久资产）】 **support separation  ≠  spectral separation** ✓✓
   z_i ≠ z_j 只排除**精确同 φ**，**不控制 |φ_i − φ_j|**；而奇层对**近碰撞**极敏感 ✓
   ⟹ 今后任何 `k >= 4 ⟹ odd-layer 下界` 路线，**必须先支付 separation / anti-cancellation cost** ✓
```

---

## §1 问题 1：k = 4 原系统的精确定义（✓✓）

```
节点（5 个，含 1 个二重 ＋ 3 个单重；k=4 的唯一分割 5 = 2+1+1+1 ✓）：
   互异值 t_1..t_4 ∈ [−1,1]，权重 (2,1,1,1)   （二重节点可**重标号固定为 t_1** ✓ WLOG）
偶层约束：  F_q(t) := 2T_q(t_1) + T_q(t_2) + T_q(t_3) + T_q(t_4)  <=  1/2 ,   q = 1..12     ✓
证书用 θ-形式： θ_i := arccos t_i ∈ [0,π] ⟹ T_q(t_i) = cos(qθ_i) ✓
   ⟹ **目标**：  ∀ θ ∈ [0,π]^4 :  ∃ q ∈ {1..12} :  2cos(qθ_1) + cos(qθ_2) + cos(qθ_3) + cos(qθ_4)  >  1/2   ✓✓
【若证书成立】 k = 4 层不可行 ⟹ 配合 **[k <= 3] = CLOSED** ⟹ **k_min = 5** ⟹ support-lower-bound 主线收官 ✓✓
【闭包红利】 闭盒上的证书同时覆盖退化（t_i 相撞）⟹ 将**独立重证** [k <= 3]（相容、冗余、可作一致性检验 ✓）
```

## §2 问题 2：可利用的 CLOSED 结果（白名单 ✓✓）

```
① [k <= 3] = CLOSED —— **仅用于最终蕴含 k_min = 5** ✓✓
② (2,2,1) 证书**方法学与仪器**：CosMin/CosMax 逐箱见证 ＋ 独立复核器 ＋ 60 位薄区复核 ＋ dry-run 六项 ＋ 反查八项
   （执行器 `scripts/c380_90_221_certify.py` 可**直接扩展**到 4 变量权重 (2,1,1,1) ✓✓）
③ k=4 数值锚（**仅作实现测试，不作证据**）：退化**内点** KKT、活跃集 Q = {1,2,4,9,10}（**5 > 4 维**）、
   λ_min = 0.0438 > 0、t ≈ 0.60780729 ✓✓
④ 弧交/区间方法（C3899l／m）—— 作互补工具 ✓
⑤ 不可用：S1 任何产物（DEAD ✗）｜四节点模型与 two-level（ARCHIVED ✗）｜P/N ＋ σ 分区（永久停止 ✗）
```

## §3 问题 3：仍被禁用的工具（✗✓）

```
① 固定系数的**非负线性组合**证书：**对 k=4 同样不可能** ✓✗
   理由（分层延拓）：均值恒为零 —— ∫_{−1}^{1} T_q(t) dt = 0（q >= 1）⟹ 任何 Σ_q c_q [F_q − 1/2] 的域均值为 −(1/2)Σc_q < 0 ✓
   ⟹ 必须用**分层特异**的非线性/覆盖/消元论证 ✓✓
② 大规模 resultant／通用 CAD（4 变量 × 12 条 Chebyshev 约束 ⟹ 实践上不可达）✗
③ P/N 对撞 ＋ 有限 σ 分区（**永久停止**）✗✗
④ 任何 S1 产物／把 S1-B 包装重开 ✗✗
⑤ H1/Fejér 迁移、多项式型偶×奇耦合、旧四节点/two-level ✗
⑥ 重跑已永久结束的实验（(2,2,1)；**预算剩余不是理由**）✗✗
⑦ 禁止**把数值最小值当证明**（本轮 k=4 的已知数值 ≈ 0.6078 只是**资源依据** ✓）
```

## §4 问题 4：证书目标形态（三选一，须在 Gate 内定版 ✓✓）

```
【T-a 主选】 **直接覆盖证书**（沿用 (2,2,1) 模板）：
     ∀θ ∈ [0,π]^4, ∃q: F_q > 1/2 ⟸ Θ 被有限闭盒覆盖，每盒附见证 q 与严格下界 L_q^certified(B) > 1/2 ✓✓
【T-b】 精确半代数排除（quantifier elimination / CAD）—— 规模上不可行 ✗（仅登记为不可用）
【T-c 后备】 **混合**：先用退化 KKT 结构**降维分层**（活跃集分层），再逐层区间/Sturm 封闭 ✓
     ⚠️ 风险：降维若依赖**数值**活跃集识别 ⟹ 必须为其本身出**可复核证书**（否则整链不成立）✗✓
【定版规则】 由 §6 的 **scale probe** 决定 T-a 是否在预算内；若否 ⟹ 转 T-c（仍须 pre-frozen）✓✓
```

## §5 问题 5：证书对象（✓✓）

```
【T-a 的证书对象】 与 (2,2,1) 同形：
  ① 表 A：盒端点（有理/dyadic）｜见证 q｜**certified lower bound** > 1/2｜深度 ✓
  ② 表 B：认证盒数／残集／最大深度／**认证最小值**／耗时 ✓
  ③ 声明段：域定义、权重 (2,1,1,1)、外向膨胀、精度、依赖 ✓
  ④ **独立复核器**（独立实现 cos 值域路径；精确 dyadic 体积账；非重叠；随机包含）✓
  ⑤ 薄区（margin < 0.05）**全部** 60 位复核（**复核 ≠ 证书本身**）✓
  ⑥ 反查八项 ＋ 红旗规则（"通过得太容易 = 可疑"）✓✓
【T-c 的证书对象】 有限**分层**证书：活跃集分层清单（每层附严格不等式或空性证据）＋ 逐层区间/Sturm 封闭 ✓
```

## §6 问题 6：最坏规模估计（**Gate 的核心不确定性** ✓✗）

```
【解析界】 F_q 在边长 h 的盒上的变差 <= (2+1+1+1)·q·h = 5·12·h = 60h ✓
           证书需 60h < margin_est ⟹ h* < ~1.8e-3（取已知数值 margin ≈ 0.1078）✓
   ⟹ **朴素 4 维均匀网格：(π/1.8e-3)^4 ≈ 9e12 盒** ⟹ **远超一切预算** ✗✗
【自适应经验（(2,2,1) 先例）】 朴素 3 维估计 ~1.5e8 ⟹ 自适应实际 **2737 盒**（约 5e-5 倍）✓✓
   ⟹ 对 k=4 的乐观外推：1e4 ~ 1e7 盒（**不确定性大** ⚠️）
【结论】 规模**不可先验判定** ⟹ **必须先做 scale probe**（§7）✓✓
```

## §7 问题 7＆9：预算与中止点（冻结 ✓✓）

```
【scale probe（先决，极小）】 <= **15 min**（1 批）：固定低深度（如 depth 10）跑覆盖，
   量测：认证比例／残集规模／残集维数估计／每深度新增盒数 ⟹ **外推到全额预算** ✓
   ⚠️ probe **不得**当作证明，只用于"是否值得全额投入"的判断 ✓
【主运行（probe 通过后）】 <= **6 CPU-hours**，<= **3 batches × 2h** ✓（沿用 (2,2,1) 模板）
【第二块】 须书面理由 ＋ 唐先生批准 ⟹ 否则不得追加 ✗
【中止点（硬）】 ① probe 外推 > 6 CPU-h ⟹ 立即 **NO-GO**（或转 T-c）✗
              ② 主运行认证盒数 > **2e6** ⟹ 中止并报告 ✓
              ③ 残集在深度 > **20** 仍无收敛趋势 ⟹ 中止并报告 ✓
              ④ 任何时刻出现 **S3 盒**（认证出 F_q <= 1/2 全 12 条且箱内确有可行点证据）⟹ **立即 STOP ＋ 报告** ✓✓
【铁律】 **固定预算 = 限制研究，不是花完** ✓✓
```

## §8 问题 8：四出口（冻结 ✓✓）

```
【PASS】 全覆盖证书 ＋ 独立复核通过 ＋ 薄区 60 位通过 ＋ 反查八项通过 ⟹ **k=4 不可行** ⟹
         （配合 [k<=3] CLOSED）⟹ **k_min = 5** ⟹ support-lower-bound 主线收官 ✓✓
         **PASS 即刻 STOP**（不得追更漂亮 margin）✓
【GAP】 得到非平凡新机制但缺最后闭合 ⟹ 写清缺口与"若有 X 则可闭合" ⟹ 归还资源 ✓
【NO-GO】 撞旧墙或规模超预算且 T-c 亦不可行 ⟹ 指认墙 ＋ 说明为何不可绕 ＋ **不用预算榨干** ✓
【S3】 认证出 k=4 的**可行**区域 ⟹ **立即 STOP ＋ 报告**（⟹ k_min = 4，主问题结论改变 = 方向变化）✓✓
```

## §9 边界与流程（✗✓）

```
- 本档 = **纸面设计**；**不启动任何计算** ✓；**C-3900 继续 HOLD**，Gate 未冻结前不得 ACTIVE ✓✓
- **不**碰 S1（DEAD）、**不**重开 §3 任一旧墙、**不**把数值锚当证据 ✓
- 下一步（须唐先生发令）：① 由本档定版 T-a/T-c 与各参数（或要求修改）⟹ ② 冻结后执行 **scale probe** ⟹ ③ 依 probe 结果决定全额投入/转 T-c/NO-GO ✓✓
```

---

## §10 SCALE PROBE 执行结果（2026-09-22 18:05；**仅 probe，未做全域覆盖** ✓）

```
脚本： scripts/c380_92_k4_scale_probe.py     日志： out/out_c380_92_k4_probe.txt
对象： F_q(θ) = 2cos(qθ₁) + cos(qθ₂) + cos(qθ₃) + cos(qθ₄)，θ ∈ [0,π]⁴，q = 1..12 ✓
方法： 逐箱单一见证 q ＋ 外向膨胀 1e-12 ＋ 严格判据 L > 1/2 ＋ 未认证箱按固定规则(最长边, θ₁→θ₄)二分 ✓
参数： N0 = 8（初始 4096 盒，h = π/8）；depth cap 16；time cap 840 s；box cap 2e6 ✓
```

**输出 1（初始盒数／深度）**：N0 = 8 ⟹ **初始 4096 盒**，初始边长 π/8；最大深度 16（本轮**深度上限触顶**，非时间触顶）✓

**输出 2（subdivision 后盒数增长曲线）**：

| level ✓ | leaves ✓ | certified ✓ | residual ✓ | min cert margin ✓ | edge ✓ |
|---:|---:|---:|---:|---|---|
| 0 ✓ | 4096 ✓ | 1855 ✓ | 2241 ✓ | 0.530734 ✓ | 3.93e−01 ✓ |
| 3 ✓ | 15316 ✓ | 6886 ✓ | 8430 ✓ | 0.501321 ✓ | 4.91e−02 ✓ |
| 6 ✓ | 50877 ✓ | 34622 ✓ | **16255（峰）** ✓ | 0.500081 ✓ | 6.14e−03 ✓ |
| 9 ✓ | 92121 ✓ | 84298 ✓ | 7823 ✓ | 0.500035 ✓ | 7.67e−04 ✓ |
| 12 ✓ | 112538 ✓ | 110342 ✓ | 2196 ✓ | 0.500035 ✓ | 9.59e−05 ✓ |
| 14 ✓ | 116213 ✓ | 115138 ✓ | 1075 ✓ | 0.500035 ✓ | 2.40e−05 ✓ |
| **16** ✓ | **117816** ✓ | **117630** ✓ | **186** ✓ | 0.500035 ✓ | 5.99e−06 ✓ |

**输出 3（最薄 margin 的位置与量级）**：
```
min cert margin = **0.5000354136517904**  ⟹ 高出 1/2 **3.541e−05** ✓
所在盒（level 3）： [0.392699, 0.490874] × [1.276272, 1.374447] × [2.454369, 2.552544] × [2.159845, 2.258020] ✓
注：该量级远薄于 (2,2,1) 的 6.4e−4 ⟹ 薄区需更深分辨率（但总量仍很小，见输出 4）✓
```

**输出 4（完整证书规模估计 ＋ 推断依据）**：
```
残集演化： 2241 → 峰 16255(level 6) → **衰减至 186(level 16)** ⟹ 薄集被**有效解消**（非指数爆炸）✓✓
推断依据①（经验趋势）：残集在 level >= 6 后**单调衰减**，每层约减 ~15–30% ⟹
     外推几层内归零 ⟹ **完整证书规模 ~1.2e5–3e5 盒** ✓
推断依据②（解析分辨率）：最薄 margin/60 ⟹ 所需边长 h* = 5.90e−7 ⟹ 所需 level d* ≈ 19.3 ✓
     （本轮已达 level 16，仅差 3–4 层；而 16 层总耗时 **7.5 s**）✓✓
推断依据③（硬闸门对照）：远低于**中止点 2e6 盒**；远低于**预算 6 CPU-h**（已用 7.5 s）✓✓
⚠️ 诚实说明：负斜率(≈−0.23 bits/level)使"有效维数"线性外推**不具意义**（残集已进入衰减段），
   故以经验趋势 ＋ 解析分辨率**双依据**给出量级，而非单点外推 ✓
```

**异常出口检查（S3）**：**0 命中** ✓（本轮未出现"∀q ≤ 12, sup_B F_q ≤ 1/2"之盒 ⟹ 无潜在反例区域 ✓）

**Probe 判定（按 Gate 冻结的分类）**：
```
status = maxdepth-reached（深度上限触顶，非时间） ｜ elapsed = **7.5 s**（上限 840 s）｜ levels = 16 ｜ S3 = 0
VERDICT = **<=1e5 : stable growth -> T-a justified** ✓✓
⟹ 结论：**T-a（直接覆盖证书）具预算级可行性**；T-c 暂不需要 ✓
```

**边界（✗✓）**：
- 本 probe **不是证明**；**未**做正式全域覆盖（按令：`K4-FULL = NOT STARTED`）✗✓
- **未**碰 C-3900／resultant／CAD；**未**追最终 margin ✓
- **未**因"仅用 7.5 s"而自动追加预算或提前启动正式覆盖 ✓✓（Gate 纪律）
- 下一步（须唐先生发令）：正式 **K4 T-a** 运行（≤6 CPU-h／≤3 批 × 2h；硬中止点已冻结）✓
