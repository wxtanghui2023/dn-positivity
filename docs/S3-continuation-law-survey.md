# S3 continuation-law 普查：什么算术原则决定"下一项为什么必须这样构造"？

**日期**：2026-09-10 18:40+ ｜ 依据：唐先生裁定①（fork B 不批准）②（AC-quotient 批准，G4 重述）｜ 代码 `scripts/s3_continuation_law_survey.py` ｜ 输出 `/tmp/s3_cont_out.txt`

---

## §0 裁定登记（唐先生）
### ① fork B 的"alternation"**不批准**作为 primitive generating principle
```
理由：+→x→+→x… 是【人为写入的有限状态机】；它没有说明为什么算术结构必然产生这个二态转移律
状态：S3-B′【通过】（历史有效性）；但 **primitive legitimacy【未通过】**
fork A = CLOSED｜fork B = NOT YET ADMITTED｜**Phase 2 = NOT STARTED**
```
**⚠️ 警示**：否则极易落入
```
人为选择 continuation law → 人为产生跨尺度行为  ⟹ 重入 GPS 的"先设计、后解释"陷阱
```

### ② AC-quotient **批准**，但 G4 须重述
$$\boxed{\text{状态} = \text{derivations 的【AC-等价类】}}$$
而非"完整语法树"（人工句法信息膨胀），也非"值"（多对一、丢构造信息）✓ 已在 GPS §1 的 G4 条款更新。

### ③ 更深的定位（唐先生）
$$\boxed{\text{S3 的真正 primitive 不是"项"，而是 continuation law}}$$
`operand → operation` 只是**表示层方向**；决定有无新数学内容的是 $[T]\longmapsto\{\text{允许的下一步构造}\}$。

---

## §1 普查：非人为候选只剩两类

### L1 算术恒等式重写（AC + 分配律）
```
AC 与分配律所代表的是【同一算术项】的不同句法代表 ⟹ 值保持且【可逆】
⟹ 这是一个 GROUPOID，【没有内生箭头】（"展开 vs 收缩"是选择 ⟹ G1 失败）
⟹ 若朝"规范形"定向则系统【终止】⟹ 无渐近、无临界指数
```

### L2 FTA-强制的分解（唯一的箭头携带者）
```
素数 p     -> p = 1 + (p-1)              （加法腿）
合数 n     -> n = p x (n/p)，p = 最小素因子 （乘法腿，由 FTA 强制）
state = 推导的 AC-类；S(n) = 规范树节点数
```

### L3 素性强制的加法腿（p → 1+(p−1)）＝ L2 的组成部分
### L4 其余（alternation / 权重 / 优先级 / 枚举顺序）
$$\boxed{\text{人为 ⟹ primitive legitimacy 失败（唐先生裁定①）}}$$

---

## §2 ⭐⭐ 尺度实测（L2 的自然尺度）—— **LOG SCALE**

| $X$ | $\max_{n\le X} S(n)$ | $\max\Omega$ | $\log X$ | $\log\log X$ | $S_{\max}/\log X$ |
|---|---|---|---|---|---|
| 100 | 21 | 6 | 4.605 | 1.527 | **4.560** |
| 1 000 | 32 | 9 | 6.908 | 1.933 | **4.632** |
| 10 000 | 44 | 13 | 9.210 | 2.220 | **4.777** |
| 20 000 | 47 | 14 | 9.903 | 2.293 | **4.746** |
| 50 000 | 53 | 15 | 10.820 | 2.381 | **4.898** |

**尺度拟合（PART 3）**：
```
X 1000→5000 :  S 32→40   X-指数 b=0.1386   (log X)-指数 c=1.0654
X 5000→10000:  S 40→44   b=0.1375           c=1.2182
X 10000→20000: S 44→47   b=0.0952           c=0.9090
X 20000→50000: S 47→53   b=0.1311           c=1.3577
```
$$\boxed{S_{\max}\simeq 4.8\,\log X\ \Longrightarrow\ \text{【线性于 }\log X\text{】，不是 }X\text{ 的幂}\ (b\simeq0.1\approx0)}$$
$$\boxed{\text{且}\ \max\Omega \approx 0.9\text{–}0.98\cdot\log_2 X\ \Longrightarrow\ \text{纯乘法侧同样是 }\log\text{ 尺度}}$$

**⟹ 这是已注册 E3 墙的【第二个独立实例】**：
> E3 当时发现"筛法/Mertens 的自然临界指数住在 $\log$ 变量"；
> 现在从**构造/语法侧**独立得到同一结论：**唯一非人为的箭头携带 continuaton law 的尺度是 $\log X$。**

---

## §3 ⭐⭐⭐ 本轮抽出的精确二分（S3 的诚实结论）
```
纯乘法分解（FTA）        ：完全非人为、无选择 ✓ —— 但【可逆】（由素因子多重集可重组 n）
                            ⟹ GROUPOID ⟹ 【无内生箭头】⟹ G1 失败（且尺度仍是 log）
混合树（含加法腿）        ：有箭头 ✓（不可逆：素因子多重集不决定推导）
                            —— 但加法腿 p = 1+(p−1) 的"规范性"来自"最小加数"这一【选取规则】
                            ⟹ 属【轻度人为】（与 alternation 同类性质，只是更自然）
```
$$\boxed{\text{箭头需要【不可逆性】；半环中的不可逆性只能来自【加法】；而加法的规范步骤需要【选取规则】}}$$
$$\boxed{\Longrightarrow\ \text{仅凭算术恒等式，得不到【完全非人为】且【携带箭头】的 continuation law}}$$
**且全部候选都落在 $\log$ 尺度 ⟹ 不可能给出 $X$-power 临界指数。**

---

## §4 裁决（依据唐先生既定条件）
$$\boxed{\text{S3 的 primitive legitimacy = 未取得（OPEN），且其自然候选已【实测】失败}}$$
```
① fork A  = CLOSED（历史装饰）
② fork B  = NOT ADMITTED（人为 alternation）
③ L1      = 无箭头 / 终止
④ L2 纯乘法 = 无箭头（可逆）
⑤ L2 混合  = 有箭头但需选取规则（轻度人为）；且尺度 Θ(log X)
⑥ L4      = 人为
⟹ 未找到【完全非人为 + 携带箭头 + 非 log 尺度】的 continuation law
```
**按唐先生指示**：若找不到，**S3 整类应诚实地回到 GPS 边界**，而不是靠 alternation 把它"做活"。
⟹ **本轮结论：S3 归入 GPS 边界（且给出了量化的理由）**；Phase 2 从未启动。

**⚠️ 边界保留（不得写成全称不可能定理）**：
```
本轮普查覆盖的是【半环恒等式 + FTA + 素性】这三类自然来源；"非人为"的空间【未被证明穷尽】。
重启条件：须提交一个【新的算术原则】（不得是 balance / g / 当前值类型 / 人为 alternation /
人为权重 / 枚举全部 continuation / 已有群-半群作用），且必须解释"为什么算术结构必然产生该转移律"。
```

## §5 诚实边界
```
· §0 三条裁定（fork B 不批准；AC-quotient 批准 + G4 重述；"真正 primitive 是 continuation law"）、
  §4 的"找不到就回 GPS 边界"—— 均为唐先生本轮指定
· §1 的 L1–L4 分类、§2 的尺度实测与拟合、§3 的二分（箭头↔不可逆↔加法↔选取规则）、§4 的裁决 —— 小灵本轮
· 【计算】级：§2 表与拟合（S(n) 由最小素因子优先 + 加法腿的规范树定义，$n\le5\times10^4$，memoized）；
  $\max\Omega$ 与 $\log_2X$ 的比较
· 【结构性论证】：§3 的二分（尤其"可逆⟹无箭头"、"加法腿的规范性是选取"）—— 非定理
· ⚠️ 未检验：其他可能的自然来源（如模结构、二次型、代数数论对象）未纳入本次普查；故"只剩两类"限于【本次普查范围】
· ⚠️ L2 混合树的加法腿用 1 作原子（1 非素数）：本次以"1 = 乘法单位"为由允许，但**这本身是一个应记录的选择**
· 未使用 ζ / Mellin / 零点 / 函数方程；未使用 Λ；无统计拟合
```

## §6 提交链
```
509718a S3-ENTRY 自证 → 本篇（continuation-law 普查 + S3 归入边界）
```
