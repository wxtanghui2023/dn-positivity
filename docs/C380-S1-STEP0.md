# C380-S1-STEP0 — Step 0：S1 域判定（纸面检查 ＋ 结构性 sanity scan）

已查地图（**先查后写** ✓）：`C380-S1-FREEZE.md`（**§8 Step 0；§1 双形式目标；§4 禁用旧墙** ✓✓）、`C3899k`（**S1 独立性审计** ✓✓）、`C3899i`（**奇层能量恒等式（约定 A）** ✓✓）、`C380-PHASE4-LOOP1-CLOSURE-AND-K4-VS-S1-GATE.md`（**分流令** ✓✓）

D0: 本档对象 = **C-380-131：S1 Step 0 域判定**（档案已有对象 `S1` 的**域关系**判定；非重命名、非新对象）
D1: 0
FREEZE-ACK: 本档即冻结审计（连续 D1=0 ＝ 35 ≥ 3；本档为**域判定登记**，未引入新自由度）

---

## §1 第 1 步：纸面零计算检查（**不碰**能量证书／Sturm／覆盖 ✓）

**1.1 全轨迹域的精确写法**

```
节点（5 个，原问题即 5 节点 ✓）：  φ_j ∈ (0, π/2)，j = 1..5
u_j := cos 2φ_j ，z_j := u_j^2 = cos^2 2φ_j
支撑数： k := #{ 互异 z_j } = #{ 互异 |u_j| }   （注意：**不是**求和项数 ✓）
全轨迹域： T_{>=4} := { (φ, σ) : φ ∈ (0,π/2)^5, σ ∈ {±1}^5, k(φ) >= 4 }     ✓
奇层：     F_n(φ,σ) := Σ_{j=1}^{5} σ_j cos(n φ_j)      （**5 项求和** ✓）
目标频率： n = 2r+1，r = 0..12  ⟹ n ∈ {1,3,...,25}（13 个）✓
```

**1.2 域内允许项（逐条澄清，按令 ✓）**

| 项 ✓ | 判定 ✓ |
|---|---|
| 求和下标 ✓ | **Σ_{j=1}^{5}**（5 节点）；`k` 是**支撑数**（互异 z 个数）⟹ 记号 `Σ_{j=1}^k` **不可用**（会与支撑数混淆）✗✓ |
| σ_j ✓ | 全体 {±1}^5 ✓；\|F\| 在**全局翻转** σ → −σ 下不变（F → −F）⟹ 可商化到 **16 层** ✓ |
| φ_j ✓ | **(0, π/2) 开区间**；闭包 [0, π/2]（δ 与 0 为边界值）✓ |
| 节点重复／碰撞 ✓ | **允许**（φ_i = φ_j ⟹ z_i = z_j ⟹ k 下降）⟹ 碰撞使构型**离开** k >= 4 域（见 1.4 ✓）✓ |
| 端点 φ = 0 或 π/2 ✓ | 边界值：二者**都给出 z = 1**（u = cos 0 = 1；u = cos π = −1）✓；φ = π/2 对应 u = −1 ⟹ z = 1 ✓（**注意**：u = 0 出现在 φ = π/4 ⟹ z = 0 ✓，两者不同 ✗✓） |
| **k > 5 是否需要单独处理？** ✓ | **不需要**：5 节点 ⟹ k ∈ {1,...,5} ⟹ **k <= 5 恒成立** ✓；参数域为有限维（10 连续 ＋ 离散符号）✓ ⟹ **无需归约/截断** ✓✓ |

**1.3 第 2 步：S1-direct 的量词检查（按令，**不得偷加 E_even** ✓）**

```
S1-direct：  ∀ (φ, σ) ∈ T_{>=4} :   max_{0<=r<=12} |F_{2r+1}(φ,σ)|  >  1/2        ✓
量词结构：   全称量化于 **(φ, σ)**；约束仅 **k >= 4**；**无**任何偶层不等式 ✗✓
E_even 的角色：**只出现在"嵌回原问题"的桥**（E_even ⟹ k >= 4，由 [k<=3 排除] = CLOSED 供给）✓
             —— **S1 本体的陈述与证明都不使用偶层**（C3899k 独立性审计 ✓✓）
```

**1.4 L0 精细化（纸面，本档新增两点 ✓✓）**

```
【L0 原骨架】 奇层恒等为 0 ⟹ k <= 3：
   ① 单节点条件：cos(nφ) = 0 对全部奇 n 成立 ⟹ n = 1 已给 cos φ = 0 ⟹ **φ = π/2** ✓
   ② 一般抵消：需 **同 φ 异 σ** 成对（F 中两节点项完全相同、符号相反）✓
   ③ 计数：p 对抵消 ⟹ 互异 z 数 <= 5 − p ⟹ k >= 4 允许 **p <= 1** ✓
      ⟹ 2 对 ＋ 1 节点（φ = π/2）型 ⟹ k <= 3 ⟹ **被 k >= 4 排除** ✓✓

【本档新增 ①】 φ = π/2 属**边界**值 ⟹ 在**开域** (0,π/2)^5 内，该"单节点零贡献"机制**不可用** ✗✓
    ⟹ 开域内奇层要恒为 0，**只能**靠**异 φ 的奇异三角关系** ✓（= L0 的缺口，仍待证 ⚠️）

【本档新增 ②】 奇层**消失机制的连续性**：φ → π/2 时 cos(nφ) → 0（n 奇）✓
    ⟹ 存在 φ_5 靠近 π/2 的构型使该节点贡献 **任意小** ✓
    ⟹ 于是 T_{>=4} 上 **inf max_r |F_{2r+1}|** 的**判定必须把"近 π/2 节点"家族当作一等公民** ✓✓
       （其余 4 节点须在全部奇 n 上把 |Σ| 压在 1/2 以下 —— 这正是 §2 扫描的 F2 家族）
```

---

## §2 第 3 步：结构性 sanity scan（**只问"是否存在 ≤ 1/2 候选"，不是证明** ✓）

```
脚本： scripts/c380_91_s1_step0_scan.py     日志： out/out_c380_91_step0.txt
覆盖（按令，**结构性优先** ✓）：
   F1 同角抵消（恰好 1 对同 φ 异 σ ＋ 3 个单点；>=2 对已被 k >= 4 排除）✓
   F2 φ = π/2 型节点（闭包家族；含"近 π/2"局部极小化）✓
   F3 共谱角家族 φ_j = 2πm/M（M = 9, 13, 18, 25, 26, 39, 52），**逐 M 穷举多重集 × 16 符号层** ✓
   F4 随机起点 ＋ 局部极小化（最坏情形搜寻）✓
   ＋ 随机基线采样 ✓
判据： 记录 k >= 4 且 **z-间隙 > 1e-6**（非退化）的构型；跟踪 **max_r |F| 的最小值** ✓
```

### 扫描结果（**COUNTEREXAMPLE-CANDIDATE** ✓✗）

```
脚本耗时 336.7 s；共 3.2e6 次随机/局部评估 ＋ 逐 M 穷举共谱家族 ✓
【全局最优】 v = max_r|F_{2r+1}| = **0.066439705** ⟹ **远小于 1/2** ✗✗
   φ* = [0.058093752, 1.570796326, 1.052567951, 0.060395155, 1.052171803]
   σ* = [1, 1, −1, −1, 1]      k = **5**      min z-gap = 6.94e−4（**非退化** ✓）
   判据 v <= 1/2 的候选数 = **100** ✓✗
```

### 候选核验（`out/out_c380_91b_verify.txt` ✓✓）

```
① float64：max_r|F| = 0.066439703；z 值 = {0.2586646, 0.2593588, 0.9854805, 0.9865611, 1.0}
   ⟹ **5 个互异 z 值** ⟹ 确实落在全轨迹域 T_{>=4} 内 ✓✓
   F 向量 = [0.00048, 0.00120, 0.00167, 0.00894, 0.01036, 0.01171, 0.02545,
             0.02633, 0.02756, 0.04632, 0.04487, 0.04449, 0.06644]  ⟹ **全部 << 1/2** ✗✗
② 偶层（**仅记录，不用于救回**）：max_q F_4q = **4.511505** ⟹ **偶层严重不可行** ✓
   ⟹ 该候选**不在** E_even 内 ⟹ **主目标 γ^(13) > 1/2 未被触及** ✓✓
③ mpmath dps=40 复核：max = **0.066439702515619507569** <= 1/2 ✓（**非浮点伪影** ✓）
④ 稳健性（开集性）：
   eps = 1e−6 / 1e−5 / 1e−4 ⟹ **300/300** 满足（max <= 1/2 且 k >= 4），k 恒为 5 ✓
   eps = 1e−3 ⟹ **300/300**（k ∈ {4,5}）；eps = 1e−2 ⟹ 251/300 ✓
   ⟹ **开集性现象**（非刀刃巧合）✓✓
⑤ 机制（结构性读法）：
   近碰撞对 1：(φ_1,φ_4) = (0.058094, 0.060395)，σ = (1,−1) ⟹ 近抵消 ✓
   近碰撞对 2：(φ_3,φ_5) = (1.052568, 1.052172)，σ = (−1,1) ⟹ 近抵消 ✓
   边界节点：φ_2 = π/2 ⟹ 对**全部奇频**贡献 ~0 ✓
   ⟹ **机制 = 近碰撞（近同 φ 异 σ）＋ 边界节点**；z 仍互异 ⟹ k = 5 ✓✗
```

---

## §3 Step 0 最终出口格式（**只允许这一张表** ✓✓）

| 项目 ✓ | 结果 ✓ |
| --------------- | ------------------------------ |
| 全 k ≥ 4 域定义 ✓ | **PASS**（T_{>=4} 精确写法见 §1.1；k ≤ 5 恒成立、无需归约 ✓） |
| L0 同角机制 ✓ | **PASS**（同 φ 异 σ 成对 ⟹ k 下降；p ≥ 2 被 k ≥ 4 排除 ✓） |
| 异角奇层抵消 ✓ | **COUNTEREXAMPLE**（**近碰撞机制**，开集性稳健 ✓✗） |
| sanity scan ✓ | **发现候选**（100 个 v ≤ 1/2 候选；全局最小 0.06644）✓✗ |
| **S1 域** ✓ | **A 全轨迹 = 假** ⟹ 唯有 **B（E_even）** 才有资格 ✓✗ |
| S1-direct ✓ | **改为条件目标**（必须带 E_even 条件，否则为假）✗✓ |
| 下一步 ✓ | **立即 S3-类 STOP ＋ 报告**（**不得**用偶层"救回" S1-direct ✗）✓✓ |

**本档不产生任何"证明 S1"的中间结论** ✓✓

---

## §4 ⭐ 结构性发现与影响（本档最重要的产出 ✓✓）

```
【发现】 **近碰撞（near-collision）机制**：φ_i ≈ φ_j（近同 φ、异 σ）＋ 边界节点 φ = π/2
        ⟹ 奇层被**近乎抵消**（max_r|F| ≈ 0.066）⟹ 而 **z 仍互异 ⟹ k = 5** ✗✗
        且该现象**开集性稳健**（eps <= 1e-3 时全部仍满足）⟹ 非退化巧合 ✓✓

【直接推论 1】 **支撑信息（k >= 4）本身不蕴含奇层定量非退化** ✗✗
   —— 即 "support information ⟹ odd-layer quantitative obstruction" **作为独立机制为假** ✓
   （原因：k 只禁止**精确同 φ**，不禁止**近同 φ**；而近同 φ 已足以灭掉奇层 ✓）

【直接推论 2】 **S1-A（全轨迹版）= 假**（已证候选）；
   而 **S1-B（E_even 版）** 是该机制上唯一可能的版本 ✓
   ⚠️ 但 S1-B 的域 = E_even ⟹ 其陈述**就是**原目标 γ^(13) > 1/2 ✓
   ⟹ **S1 作为"绕开偶层约束的独立机制"实质失效** ✗✗

【不给"救回"】 候选偶层不可行（max F_4q = 4.51）✓ —— 但这**只作记录**，
   **不得**用作"S1-direct 仍可由偶层约束救回"的论证 ✗✗（按令）

【与既有档案相容】 该候选 even-INFEASIBLE ⟹ 不触及 E_even 上 γ^(13) ≈ 0.8688 的既有数值 ✓✓
   （也再次印证：奇层小的区域恰是偶层不可行的区域 ✓）
```

---

## §5 边界（✗✓）

- **不**把扫描当作证明（域判定依据 ✓）；
- **不**偷加偶层条件进 S1-direct ✗；
- **不**用偶层约束"救回" S1-direct ✗✗（按令）；
- **不**碰 K4／C-3900（继续完全 HOLD）✓✓；
- **不**重开 §4 旧墙；**不**在此 STOP 后自行推进任何 S1-B 工作（等发令）✓

---

## §6 待唐先生裁定（STOP 已执行 ✓✓）

```
① S1-A（全轨迹）**判为假** ⟹ 是否正式把 S1 域改为 **S1-B = E_even**？✓
② 若改 B：则须**重新冻结工具集**（域内含 12 条偶层不等式 ⟹ 证书方法学需重新设计，成本显著上升）✓
③ 是否据此把 S1 从 PRIMARY 降级（因其"独立机制"实质失效）⟹ 重新做资源分流？✓
④ K4／C-3900 **继续完全 HOLD**（窄门条件未变更 ✓）
```

---

## §7 裁定登记（唐先生 2026-09-22 17:53 ✓✓）

```
【S1-A】 = **FALSE / CLOSED** —— 结构性反例族（非孤立点）：
   k = 5、max_r|F_{2r+1}| = 0.0664397… < 1/2；5 个 z_j 互异；非精确碰撞；40 位复核；扰动稳定 ✓✗
   正式记录：  **k >= 4  ⟹̸  γ^(13) > 1/2** ✓✓
   Failure mechanism： **near-collision cancellation ＋ boundary-near-zero odd contribution** ✓
【S1-B】 = **DECLINED / 不立项**（**不是** GAP，**不是** HOLD）✗✗
   理由（方法论身份已变）：S1-A 价值 ="不用偶层、仅凭 support 得 odd obstruction"；
   S1-B = E_even ⟹ γ^(13) > 1/2 已把决定性信息放回 12 条偶层约束 ⟹
   **不再是 independent bridge，而是原 C-380 可行域上 γ^(13) 的直接证明** ⟹
   违反 S1 分流初衷；**不得**因 A 被击穿就把 B 当"备用 S1"续烧资源（= 改问题定义以保存路线）✗✗
   防重开：日后**不得**把 S1-B 包装成 S1 重开 ✓✓
【S1】 = **PRIMARY → ARCHIVE / DEAD** ✓✗
【K4】 = **OPEN**，取得下一阶段**唯一主动资源资格**（但**非**立即启动 C-3900）✓
   ⟹ 启动须先满足原冻结窄门第二条件：**已有预先写死、有限预算、明确出口的 k=4 排除方案** ✓✓
【C-3900】 = **HOLD**（Gate 未冻结前不得 ACTIVE）✓✓
【资源重组】 原 S1 60–65% ／ K4 ≤ 20% 配置**即刻作废**；新结构见 `C380-K4-GATE-FREEZE.md` ✓
【S3 STOP 评注（照录）】 本次 STOP **完全正确**：未用偶层"救回"反例、未偷换目标、
   **未因已投入 336.7 秒而继续烧预算** ✓✓

⭐【方法过滤器（**永久资产**）】
   **support separation  ≠  spectral separation** ✓✓
   z_i ≠ z_j 只排除**精确同支撑坐标**，**完全不能控制 |φ_i − φ_j|**；
   而 odd layer 对近碰撞极敏感：σ_i = −σ_j、φ_i ≈ φ_j 时
        cos(nφ_i) − cos(nφ_j)   可在 n = 1,3,…,25 **同时很小** ✓
   ⟹ 今后任何 `k >= 4 ⟹ odd-layer 下界` 路线，**必须先支付 separation / anti-cancellation cost** ✓✓
   （定性：**不是**新主线，而是**方法过滤器** ✓）
