已查地图：命中（本仓自档 `KH-4-POSITION-TO-EIGENVALUE`／`KH-4-EXPONENT-TO-SPECTRUM`／`C347` 低阶表）⟹ **引用，不开新案** ✓

# **`WS-J1` 第一次落地推导**：位置（`t=\sqrt x`）能否以谱的语言重新表达？

**唐先生指示（2026-09-23 12:37）**：① ✅ **`J-1` 比 `J-4` 更接近真正"等号两边接不上"的原型**（`1/2` 分别以**位置／指数**与**谱值**两种**不相容的身份**出现，且"相似变换保持谱不变"**明确排除了**把两者接起来的路 ✓）；
　② ⭐ **不要问"缺什么运算"，要问更基础的**：**"`t\mapsto x/t` 这个对合 ＋ `相似变换保持谱不变`，是不是预设了一个太窄的对象类别？有没有一种更大的等价关系（不要求可逆、或半共轭），在此之下"位置"与"谱值"可能被统一看待？"** ✓✓
　③ 具体要求：**把 `J-1` 的式子重新摆在纸上，推导一次**（不是抽象规划）✓

D0: 本档对象 = **`WS-J1` 落地推导（Mellin 恒等式 ＋ 相似性限制的放宽检验）**（引既有档；**未开 bridge 案** ✓）
D1: 0 （`[RESEARCH]` 轮次；结论为**结构定位 ＋ 一条显式恒等式 ＋ 一处自纠** ✓）
FREEZE-ACK: D1=0 ✓
[RESEARCH]

---

## §1 **把式子摆上纸：`\sigma_x` 在 Mellin 侧的显式形式（✓ 已用测试函数核验）**

```
【约定】 `\hat f(s)=\int_0^\infty f(t)\,t^{s-1}\,dt`；`\sigma_x f(t):=f(x/t)`；乘法 Haar 测度 `dt/t` ✓
【⭐ 显式恒等式（本档推出 ＋ 核验 ✓✓）】 $$\boxed{\widehat{\sigma_x f}(s)=x^{s}\,\hat f(-s)}$$ ✓
　核验（取 `f(t)=t^{-1/2}e^{-t}`）：左端 `=x^{s}\Gamma(-s-\tfrac12)` ＝ 右端（`\hat f(-s)=\Gamma(-s-\tfrac12)`）✓✓ —— **逐项吻合** ✓
【两条独立算子事实（同一侧）】 反演 `t\mapsto1/t` ⟹ `s\mapsto -s`（乘子 1）｜伸缩 `t\mapsto\lambda t` ⟹ 乘子 `\lambda^{-s}` ✓
　故 `\sigma_x=D_{1/x}\circ I` ⟹ 乘子 `x^{s}` 与反射 `s\mapsto -s` 的**组合** ✓（自检：`\widehat{\sigma_x^2 f}(s)=x^{s}\cdot x^{-s}\hat f(s)=\hat f(s)` ⟹ `\sigma_x^2=\mathrm{id}` ✓✓）
```

## §2 ⚠️ **一处自纠（重要 ✓）**：两个 `1/2` **不是同一个 `1/2`**

```
【原先的错误说法（应撤回 ✗）】 我此前暗示"除子对称的中心 `1/2`"与"临界线 `\mathrm{Re}\,s=1/2`"在该归一化下**是同一个参数** ✗✗
【更正（本档 ✓✓）】 在上述约定下：**除子对合 `t\mapsto x/t` 在 Mellin 侧是 `s\mapsto -s`，其中心是 `s=0`** ✗≠`1/2`；
　而算术函数方程的反射是 **`s\mapsto 1-s`，中心才是 `1/2`** ✓ ⟹
　$$\boxed{\text{除子对称的 }1/2\（=x^{1/2}\text{ 的指数}\）\ \text{与}\ \text{函数方程的 }1/2\ \text{在裸 Mellin 下\textbf{不同心}}}$$ ✓✓
　⟹ 因此"把位置读成谱值"**不可能靠直接等同完成**，**必须插入权重**（这正是本刀要检验的东西 ✓）
```

## §3 **按令放宽"相似变换"：一个更弱但仍自然的等价（✓ 本档检验）**

```
【观察 ✓】 上述 `\sigma_x` 在 **Haar（酉）归一化**下乘子模长为 1 ⟹ 位置只以**相位／平移**出现 ⟹
　而平移共轭**保持谱不变** ⟹ `\operatorname{Spec}(\sigma_x)=\{+1,-1\}`（对一切 `x` ✓）⟹ **位置在酉层面是"纯相位"，本质不可谱读** ✓
　⭐ 结论：**"相似变换保持谱不变"不是人为限制，它就是"位置是平移参数"这一事实本身** ✓✓
【放宽（按令：不要求可逆 / 半共轭）】 改在**加权**实现上做 —— 以**单项式权重** `t^{-r}` 共轭（＝把 Mellin 线由 `\mathrm{Re}\,s=0` 移到 `\mathrm{Re}\,s=r`）✓
　该共轭在 `r\ne0` 时是**无界相似**（`t^{-r}` 无界）⟹ **不是相似变换** ⟹ **谱可以改变** ✓✓
　加权后乘子获得因子 `x^{r}` ⟹ $$\boxed{\text{位置以\textbf{模长（谱半径）}形式出现：}\ \rho=x^{r}}$$ ✓
【⭐ 取 `r=1/2` ⟹ `\rho=x^{1/2}=\sqrt x`】 ⟹ **位置确实可以变成谱数据** —— 但是以**半径**而非**本征值**的形式 ✓✓
```

## §4 ⚠️ **诚实风险与其唯一判据（本档关键 ✓✓）**

```
【风险（必须自己先说 ✓）】 权重 `r` 是**我们挑的**；挑 `r=1/2` 就是为了得到 `x^{1/2}` ⟹ **＝把答案塞进参数** ✗
　（此即本仓 `KH-4` 的 `G3` **去参数化测试**所排除的"人工谱编码"同型风险 ✓）
【⭐ 唯一合法判据（本档给出，一轮内可判定 ✓✓）】 把 `r` 换成**任意** `r`，要求**算术结构自己**把 `r` 逼到 `1/2`：
　$$\boxed{\text{是否存在一个\textbf{不含 }1/2\text{ 的}算术自洽条件（例如加权算子的自对偶／正性／除子恒等式）}\ \Longrightarrow r=\tfrac12}$$ ✓
　**若存在** ⟹ 位置→谱值的转换**是被逼出来的**（＝真有结构，非编码）✓｜**若不存在** ⟹ 该路线＝**人工编码，CLOSED** ✓
【⛔ 未做的事】 未宣称 1/2 已被导出；未宣称与 RH 有接口；未把权重当"新运算"包装 ✓
```

## §5 边界

```
✗ 未证 RH／未接 ζ｜⛔ 未把 localization·transport·realization 当候选机制 ✓
【证据等级】 **档级**（一条显式恒等式，已用测试函数 `t^{-1/2}e^{-t}` 逐项核验 ✓；一处自纠 ✓）
【无新性主张】 未宣称"新" ⟹ 无需【技术词回查】✓
```
