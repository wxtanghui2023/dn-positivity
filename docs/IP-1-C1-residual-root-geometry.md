已查地图：未覆盖（关键词: residual root r(a) Möbius 对合 T-system 零点几何）—— 可开档 ✓

# **`IP-1C1: Residual Root Geometry`** —— 符号消元 ＋ 区间定律 ＋ **判死门：标准几何 ⟹ 干净关上**

**唐先生令（2026-09-23 10:06）**：① ⭐ 把 `d_V` 与 `ec` 两条线**彻底拆开** ✓；② 反例对**干净**（非 Sturm bug、非端点口径假象 ✓）；
　③ ⭐ **不许说"出现新 invariant"** —— 目前已证的是更弱但干净的事实：$$\boxed{\text{rank flag 控制消失阶，但不控制消失多项式的剩余根位置}}$$ ⟹
　两层：`\underbrace{\text{evaluation/rank structure}}_{\text{控制 }d_V}` ＋ `\underbrace{\text{ambient real geometry}}_{\text{控制额外根是否进入 }[-1,1]}` ✓✓
　④ 下一刀命名 **`IP-1C1: Residual Root Geometry`**，核心对象＝**第三根的位置** `r(a)`；对 `S=\{-1,a\}` 做 **`C1-a` 符号消元**／**`C1-b` 求临界点 `r(a)=\pm1`**／**`C1-c` 比较几何区间** ✓；
　⑤ ⭐ **判死门**：`r(a)` 若只是**已有 Chebyshev 根公式／插值公式／导数公式／标准极值理论**的直接推论 ⟹ $$\boxed{IP\text{-}1C1 \to \text{标准零点几何}}$$（虽解释 `ec`，**不能称新机制**）✓✓
　⑥ ⛔ 禁止：**继续扩样**、**重新研究 `d_V`**、**给 `ec` 换名字** ✓

D0: 本档对象 = **`IP-1C1` 符号消元与判定**（`IP-1-G0` 族内深挖，非改名；**未开 RH 案** ✓）
D1: 0
FREEZE-ACK: D1=0（本档不主张新自由度）；§8.1 冻结债务见 `IP-1-G0` 档 §7 ✓
【计算授权】 依令执行一次符号计算（`sympy`，零浮点）＋ 格点交叉验证；⛔ 未扩样 ✓

---

## §1 **符号消元（`C1-a` ✓✓）**

```
【族】 `V=\operatorname{span}\{T_1,T_2,T_3\}`（`q=1,r=2`）；`S=\{-1,a\}`；核维数 = **1** ✓
【消失元（精确 ✓）】 $$p_a(x)=\frac{4\,(x-a)(x+1)\,(2ax-a-x+1)}{2a-1}$$ ✓
【残根（`C1-a` ✓✓）】 消去已知因子 `(x+1)(x-a)` ⟹ $$\boxed{r(a)=\frac{a-1}{2a-1}}$$ ✓✓
【验证 ✓】 `r(1/4)=3/2`（反例 A ✓）｜`r(-3/4)=7/10`（反例 B ✓）—— **与两组反例逐一吻合** ✓✓
```

## §2 **结构性质与临界点（`C1-b` ✓✓）**

```
【⭐ 对合】 `r(r(a)) - a = 0` ⟹ **`r` 是对合（involution）** ✓✓；其不动点满足 `2a^2-2a+1=0` ⟹ 判别式 `-4<0` ⟹ **无实不动点** ⟹ `r` 是**椭圆型 Möbius 对合** ✓
【单调性】 $$\frac{dr}{da}=\frac{1}{(2a-1)^2}>0$$ ⟹ 在 `(-\infty,\tfrac12)` 与 `(\tfrac12,\infty)` 各段**严格递增** ✓
【临界点（`C1-b`）】 $$\boxed{r(a)=1 \Longrightarrow a=0},\qquad \boxed{r(a)=-1 \Longrightarrow a=\frac23}$$ ✓✓
【⭐ 极点 `a=1/2` 的结构读法（本档 ✓）】 `a=1/2` 时 `\operatorname{span}\{T_1,T_2\}` 在 `S` 上**已有核**（核维数 1）⟹ **前缀已降秩** ⟹ `d_V=q+1=2` ⟹ **flag 类别改变** ✓
　⟹ 极点**正是 flag 边界的投影** ✓✓（这也解释为何该点须从 `(1,2,2)` 类中排除 ✓）
```

## §3 **区间定律（`C1-c`）＋ 逐例交叉验证（数据驱动 ✓✓）**

```
【定律（`C1-c`）】 对 `a\in[-1,1]`、`a\ne\tfrac12`：$$\boxed{ec=1 \iff a\in[-1,0]\cup[\tfrac23,1]}$$ $$\boxed{ec=0 \iff a\in(0,\tfrac23)}$$ ✓✓
【交叉验证（格点 `k/4`，逐例实测 vs 定律）】
　`a`: `-3/4 → r=7/10 (ec 1✓)`｜`-1/2 → 3/4 (1✓)`｜`-1/4 → 5/6 (1✓)`｜`0 → 1 (1✓)`｜`1/4 → 3/2 (0✓)`｜`3/4 → -1/2 (1✓)`｜`1 → 0 (1✓)` ⟹ **一致 7／7，不一致 0** ✓✓
　并**同时复现** §1 的两组反例（`a=1/4 ⟹ ec=0`；`a=-3/4 ⟹ ec=1`）✓✓
```

## §4 ⛔ **判死门（承令 ⑤ ✓✓）**

```
【判定对象】 `r(a)` 的**数学身份** ✓
【事实】 `r(a)=\dfrac{a-1}{2a-1}` ＝ **Möbius 变换**（分式线性）＋ **对合** ＋ **无实不动点** ⟹
　即 `\mathbb P^1` 上由**交比／射影结构**决定的**椭圆型对合**——**T-system／Chebyshev 零点几何的标准对象** ✓
【并入结论】 因此 `ec` 的差异**被标准几何完整解释**（额外根是否落入 `[-1,1]` ＝ 该 Möbius 对合下区间像的位置 ✓），**未出现新机制** ⟹
　$$\boxed{IP\text{-}1C1 \to \text{标准零点几何}}$$ ✓✓（**依令：干净关掉** ✓）
【⛔ 依令的诚实措辞】 ⛔ **不得**称 `r(a)` 为"新 invariant"／"新机制"；⛔ 不得把 `ec` 包装成新量 ✓
```

## §5 **判定与线内收口（承令 ①②）**

```
【`IP-1/d_V`】 ✅ **B（CLOSED-LEANING）**：恒等式 `d_V=q+\min\{j:\operatorname{rank}(\mathrm{prefix}_j)<j+1\}` 8208/8208 ⟹ **rank-flag complete** ✓（**不再研究** ✓）
【`IP-1/ec`】 ✅ **CLOSED（标准几何解释）**：`ec` 由 **Möbius／T-system 零点几何**决定（本族给出**显式** `r(a)` 与**两区间**判据）✓
【⭐ 本档真正得到的】 一条**显式初等定律**（`p_a` 因式分解 ＋ `r(a)` ＋ 两区间判据），**完整解释了** §2 的反例对 ⟹
　它把"`C` 候选"从**悬而未决**变成**已解释** ✓✓ —— 这是**有内容的收口**，**不是**新机制 ✓
【⚠️ 诚实边界】 ① 阈值 `0`／`2/3` 与 `r(a)` 的**具体形式是本族（`q=1,r=2`、含强制节点 `-1`）专有** ✓；
　② **不声称**一般 `(q,r)` 的对应律（**未计算、不扩样** ✓）；③ 不声称与 RH 有任何接口 ✓
```

## §6 边界与归档

```
【脚本】 `scripts/ip1_c1_residual_root.py`（符号消元＋临界点＋对合验证）｜`scripts/ip1_c1b_crossvalidate.py`（逐例交叉验证）✓
【日志】 `out/out_ip1_c1.txt`｜`out/out_ip1_c1b.txt` ✓
【R7】 结论数字驱动（核维数、一致 7/7、`r` 值、`ec` 值）✓｜【R1】 脚本头含目的／输入／输出／纪律 ✓
✗ 未扩样／未重研 `d_V`／未给 `ec` 换名字／未称新 invariant／未证 RH／未接 ζ／未开案 ✓
```
