已查地图：命中（`G2p5-R-executable-compression-spec`／`F1-downrank-F5-F3-audit-G2p5-is-binding`／`F2-structural-feasibility-verdict-A`）⟹ 引用，不开新案
D0: 本档对象 = `H/M` 型候选发现 R1：4 个具体有限目标 ＋ `G2.5-R` 四源审计 ＋ `N_eff` ＋ 竞争排除（**零 SAT／零实现**）
D1: 0 （[REVIEW] 轮次：候选发现与审计，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`H/M` 候选 R1（4 个具体有限目标）**

## §0 外部现实约束（照录）

```
**`Hadamard\ 668` 已于 `2026-08` 出现构造并被独立精确复核** ⟹ **"矩阵正交＋大对称＋exact certificate"本身不再构成候选优势** ✓✓
【⟹ 纪律】 优先序 `M>H>P>S`；**最后才考虑 Hadamard／普通设计**（已高度工业化）✓
【⚠️ 已知反例警示】**`OM(4,8)` 已完整分类** ⟹ **不得直接拿来当目标**（照录）✓
```

## §1 四个候选

```
**【`C1`｜rank-4 定向拟阵可实现性（uniform）】** `(P,\ \text{有限参数},\ \text{未知命题})`＝`(rank\text{-}4\ \text{chirotope 可实现性},\ (r,n)=(4,n),\ \text{"该 chirotope 是否可由点集实现"})` ✓
　`\mathcal C_{\rm rank}` ✓✓ **高阶**：chirotope 必须满足 **3-term／Grassmann–Plücker 型一致关系**（非普通 SAT）✓
　`\mathcal C_{\rm cert}` ✓ 表示＝**显式点集/矩阵（短）**；不可实现＝**代数一致性证书** ✓
　`\mathcal C_{\rm sym}` ⚠️ 中（`S_n` 作用，但 chirotope 层 orbit 计数不便宜）｜`\mathcal C_{\rm prop}` ⚠️ 中
　**竞争** ⚠️ **低–中**（`rank\ 3` 已枚举到 `n\approx11`；**`rank\ge4` 远不完整** ⟹ **相对空位**）✓
　`N_{\rm eff}` ⚠️ 中（`n=9`–`10` 量级可估；大 `n` 不可）
　**`G4`** ✓ "**该 `(4,n)` chirotope 可实现/不可实现**" ⟹ 干净命题 ✓
**【`C2`｜拟阵有限域可表示性】** `(\text{给定小拟阵 }M,\ \text{有限域 }\mathbb F_q,\ \text{"}M\text{ 是否 } \mathbb F_q\text{-可表示"})` ✓
　`\mathcal C_{\rm rank}` ✓✓ **Plücker 向量 ＋ 二次关系**（真高阶）✓｜`\mathcal C_{\rm cert}` ✓ 表示矩阵（短）／非表示性代数证书 ✓
　**竞争** ⚠️ **中–高**（`Oxley` 系表与可表示性数据库长期存在）⟹ **须逐 `(M,q)` 核** ✓
　`N_{\rm eff}` ✓ 小（单个小拟阵 → Gröbner/SAT 规模可控）｜**`G4`** ✓
**【`C3`｜抽象多胞形（rank `\ge4`）存在性分类】** `(\text{给定 Schl\"afli 型 }\{p,q,r\},\ \text{有限参数},\ \text{"是否存在该型局部射影规则多胞形"})` ✓
　`\mathcal C_{\rm rank}` ✓ **低阶局部数据（面incidence）＋ 高阶兼容（局部射影性/diagram）** ✓
　`\mathcal C_{\rm sym}` ✓（大对称：型群 `[p,q,r]`）｜`\mathcal C_{\rm cert}` ✓ 构造/非存在证书（短）
　**竞争** ⚠️ `McMullen–Schulte` 系已分类大族 ⟹ **须找具体未决 `\{p,q,r\}`** ✓
　`N_{\rm eff}` ⚠️ 视参数｜**`G4`** ✓ "**存在/不存在**"✓
**【`C4`｜`k`-集结构可实现性（抽象凸性结构）】** `(\text{给定抽象 3-set/凸性结构},\ \text{有限参数},\ \text{"是否存在点集实现"})`
　`\mathcal C_{\rm rank}` ✓✓（**凸位性只依赖 chirotope**）｜**竞争** ⛔ **高**（与 `\mathrm{ES}(7)`/order-type 社区重叠）⟹ **降级**
```

## §2 `G2.5-R` 四源评定（需 `\ge3/4`）

| 候选 | `\mathcal C_{\rm sym}` | `\mathcal C_{\rm prop}` | `\mathcal C_{\rm rank}` | `\mathcal C_{\rm cert}` | 计数 | 竞争 | 判定 |
|:--|:--:|:--:|:--:|:--:|:--:|:--|:--|
| **`C1`** | ⚠️ 中 | ⚠️ 中 | **✓✓** | **✓** | **2.5/4** | **低–中（相对空位）** | **`C1-B` 边缘候选** ⚠️ |
| **`C2`** | ⚠️ 弱 | ✓ | **✓✓** | **✓** | **3/4** | ⚠️ 中–高 | **`C2-B`（须逐案核竞争）** ✓ |
| **`C3`** | **✓** | ⚠️ 中 | **✓** | **✓** | **3/4** | ⚠️ 中 | **`C3-B`（须找未决型）** ✓ |
| **`C4`** | ⚠️ | ⚠️ | ✓✓ | ✓ | 2/4 | ⛔ **高** | **退出** ✗ |

## §3 ⭐ 本轮结构性结论（重要，且是负形）

```
【观察】 **`H` 型在有限几何里同样高度工业化**：order types 枚举（小 `n` 已穷尽）、`rank\ 3` OM 枚举（到 `n\approx11`）、可表示性表、`Hadamard\ 668`（`2026-08` 已解）⟹ **"高阶一致性"优势并不自动等于"无人占位"** ✓✓
【⟹ 相对空位】 仅 **`rank\ge4` 定向拟阵／抽象多胞形 rank `\ge4`／具体未决 `(M,q)`** 三类仍是**相对空位**（因枚举成本高、工具不成熟）✓✓
【⟹ 但与此对应的代价】 这三类的 `N_{\rm eff}` **更难估**，且**工具链未工业化** ⟹ **我方 `D` 也不占便宜**（无现成 canonical 生成器）✓
【⟹ 诚实判定】 `C1`/`C2`/`C3` 均**未同时满足**"`G2.5-R\ge3/4` ＋ `N_{\rm eff}\lesssim10^{8-10}` ＋ 竞争机制不同" ⟹ **本轮 0 命中，需下一轮收窄参数** ✓✓
【⛔ 纪律】 零 SAT／零实现／未计算；`n=38`／`TARGET-L9`／RH 未作筛选依据 ✓
【边界】 §0 的 `Hadamard\ 668`（`2026-08`）与 `OM(4,8)` 归类为**您提供的档级信息**；§1–§2 各门评定与 `N_{\rm eff}` 为**本档纸面估计**（**未核 `C1`–`C3` 的具体未决参数**）⚠️；§3 结论为**本档推导**；未制造候选／未启动搜索／未碰 RH。
