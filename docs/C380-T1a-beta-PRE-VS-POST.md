已查地图：未覆盖（关键词: PRE-VS-POST|事后认证|rank-collapse|短证书）—— 可开档，首行须照抄本行
　（`短证书` 1 档／`低次消失元` 3 档 ＝**档案已有 ⟹ 引用** ✓；`PRE-VS-POST`／`rank-collapse` = 0 ⟹ 本档新增 ✓）

# **`T1a-β / PRE-VS-POST`**：前置推导 vs 事后认证（**只审 `rank E₂ ≤ 2`／低次 annihilator 的结构性来源**）

**唐先生令（2026-09-23 09:20）**：
① **只做这一刀** ✓；目标钉死为：**在尚未使用 `C3890` 已给出的具体二次多项式之前**，能否从 `T1a` 结构假设推出
　$$\exists\,0\ne p,\ \deg p\le2,\ p|_{\operatorname{supp}\mu}=0$$ ✓
② **两档**：**`P0` 真前置**（不用 `C3890` 数值根/系数回查的定理，如 `\dim\mathcal P_2|_{\operatorname{supp}\mu}<|\operatorname{supp}\mu|`，更强 `\ker(E_2)\ne\{0\}`）✓｜
　**`P1` 伪前置／事后认证**（先搜支撑数值 → 算出 `p` → 验证 `p(y_i)=0`；或用 `C3890` 两根已落 `[-1,1]` 反推）✓
③ ⚠️ **关键提醒（照录）**：`\dim\mathcal P_2=3` **本身不能**推出"存在零点包含整个支撑"；**只有先证 `\operatorname{rank}E_2\le2`** 才有 `\ker E_2\ne0`
　⟹ **真正该追的是：为什么 `T1a` 的结构强制 `\operatorname{rank}E_2\le2`？** ✓✓
④ **审计顺序**：① 冻结 `C3890` 多项式与两根 ② 从原始约束张量写出 `E_2` ③ 明确约束数量/独立性/rank 上界 ④ 查是否有**结构性 rank-collapse／recurrence／annihilator** ⑤ 若有，给不依赖数值见证的证明 ⑥ 若无，记 `T1a-β` ＝ **目前仅有事后认证** ✓
⑤ **反循环标记**：证明中首次出现 `-13/32`、`1/4`、`C3890` 两根区间或其派生量 ⟹ 标 **POST-C3890 DATA**；**若核心存在性结论依赖这些数据 ⟹ `P0` 自动失败** ✓✓
⑥ **禁项**：**禁止重新数值找根** ✓

D0: 本档对象 = **`T1a-β` 的前置/事后判定（`rank E₂ ≤ 2` 的结构性来源审计）**（引既有 `C3890`／`C3862`；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算／零数值／未写程序**／未做数值实验／**未重新找根**／未算零点／未接 ζ／未找 `1/2`／未证 RH ✓

---

## §1 **冻结声明**（依令 ① ✓✓）

```
【冻结对象（全部标 POST-C3890 DATA ✓）】
　`(m,s) = (-\tfrac18,\tfrac{7}{16})`｜`a = \tfrac{3\sqrt3}{8}`｜`y_{1,2} = m \pm a`｜`y_1y_2 = -\tfrac{13}{32}`｜`y_1+y_2 = -\tfrac14`｜`p_2(y) = y^2+\tfrac14y-\tfrac{13}{32}` ✓
【状态】 ⛔ **本轮不得作为推导输入**（仅可作"事后认证"的见证）✓✓
```

## §2 **写出 `E_2`**（依令 ②③ ✓；含**定义层歧义登记** ⚠️）

```
【约束张量（`C3890` §6① 逐字）】 $$\operatorname{span}\{T_{13},T_{19},T_{12},T_{16},T_{28},T_{36}\}$$（六元、**次数 12–36**）✓
【评价映射】 $$E_2:\ \mathcal P_2 \longrightarrow \mathbb R^{\operatorname{supp}},\qquad p\longmapsto (p(y_i))_{i\in\operatorname{supp}}$$，`\dim\mathcal P_2 = 3` ✓
【rank 基本事实（本档）】 对**相异**支撑点：$$\operatorname{rank}E_2 = \min(3,\ \lvert\operatorname{supp}\rvert) \qquad\Longrightarrow\qquad \boxed{\ker E_2\ne0 \iff \lvert\operatorname{supp}\rvert\le2}$$ ✓✓
【⚠️ 定义层歧义（本档登记，依 `AGENTS.md`「Spec 不清先问不猜」✓）】
　· **读法 A**："低次" ＝ 多项式变量中次数 `\le2` ⟹ 但约束张量诸元次数 `\ge12`，且**相异次 Chebyshev 多项式线性无关** ⟹
　　**该 span 中不存在次数 `\le2` 的非零元** ✗ ⟹ 此时"低次消失元"**不在约束张量内** ⚠️
　· **读法 B**："低次" ＝ 该 span 内**可用次数中较低者**（如 `T_{12}`，次数 12 ≪ 36）⟹ 这是一个**真正的线性代数问题** ✓
　⟹ **本档结论对两读法分别给出**（见 §3／§4）✓✓
```

## §3 **`P0` 判定（读法 A：目标次数 `\le2`）** ⛔ **FAIL**

```
【逻辑链】 要"存在 `\deg\le2` 且零点 `\supseteq\operatorname{supp}` 的非零元"，只需 `\lvert\operatorname{supp}\rvert\le2`（由 §2 的 `3>2` 维数计数）⟹ **存在性自动** ✓
【⛔ 但】**`\lvert\operatorname{supp}\rvert\le2` 正是待证的支撑界本身** ⟹
　$$\boxed{\text{利用低次消失元去证 }\lvert\operatorname{supp}\rvert\le2\ \text{是}\ \textbf{循环}}$$ ✓✓
　（即：`\ker E_2\ne0 \iff \lvert\operatorname{supp}\rvert\le2` ⟹ **存在性命题与支撑界命题等价** ⟹ **不能用作推导** ✓）
【⭐ 对本刀核心问题的回答（依令 ③）】
　$$\boxed{\text{为什么 }T1a\text{ 的结构强制 }\operatorname{rank}E_2\le2？\ \text{——档案定义内}\ \textbf{无可用的独立机制}}$$ ✓✓
　· 唯一可得的 rank 上界来自**支撑大小本身**（`\operatorname{rank}E_2=\min(3,\lvert\operatorname{supp}\rvert)`）⟹ **等价式** ✓
　· **未发现**结构性的 **rank-collapse／recurrence／annihilator**（即：不依赖 `\lvert\operatorname{supp}\rvert` 的、由约束张量自身结构给出的 rank 压制）✗
【依令 ⑤】 ⟹ 若日后出现**独立机制**（例如：约束张量的低次部分维数受限且与支撑无关 ⟹ 直接给 `\ker\ne0`），**本判定可重审** ✓
```

## §4 **`P0` 判定（读法 B：span 内"较低次数"消失元）** ⚠️ **未达（同为等价型）**

```
【问题】 span 内是否存在较低次（如次数 12）的非零元，其**零点 `\supseteq\operatorname{supp}`**？
【结构分析】 span 内诸元次数 `\ge12` ⟹ 若某元 `q` 在支撑上消失，则 `\lvert\operatorname{supp}\rvert \le \deg q` ⟹ 仍**只给"支撑 ≤ 次数"型界** ✓；
　要得到**有用**的界（支撑 ≤ 2），必须 `\deg q\le2` ⟹ 回到读法 A ⟹ **循环** ✓✓
【⛔ 结论】 读法 B 亦**不能**给出独立支撑界；其价值仅在于**证书成本**（由 36 降至 12）⚠️ ⟹ **非 `P0`** ✓
```

## §5 **判词**（依令 ④⑥ ✓✓）

```
【判定】 ⛔ `P0` **FAIL**（两读法皆失败：读法 A 循环；读法 B 仍为"支撑 ≤ 次数"型、无独立界）⟹ **落 `P1`**：
　$$\boxed{\text{T1a-}\beta = \text{目前仅有事后认证}}$$ ✓✓
【准确表述（照录要素）】 `C3890` 的 `p_2(y)=y^2+\tfrac14y-\tfrac{13}{32}` 与"两零点 `\in[-1,1]`"是**短证书（两行）** ✓；
　其**存在性**在"**两层结构**"（`C-3862` 已结构推导 ✓）输入下**由维数计数自动得到**；但该证书**不能给出独立支撑界** ⟹ **事后认证** ✓✓
【`POST-C3890 DATA` 标记（依令 ⑤）】 本档 §1 六项**全部**标 `POST-C3890 DATA`；且 ⭐ **核心存在性结论确实依赖这些数据**
　（"存在"的**见证**即它们；"先验理由"则需**支撑界输入**）⟹ 依令 ⟹ **`P0` 自动失败** ✓✓
【⛔ 非 DEAD（依 `E-13` 五出口 ✓）】 依令：**保留其在出口 `E`（独立 Chebyshev／矩／稀疏表示问题）上的价值** ⟹ 作**事后认证资产**保留 ✓✓
```

## §6 五出口再评（依 `E-13`；`B/E` 未置空 ✓）

| 出口 | 状态 |
|:--|:--|
| **A RH interface** | ⛔ **空**（已裁定不得表述为 RH 进展 ✓） |
| **B 提高 `2/3`** | ⛔ **无路径** ✓ |
| **C 有限离轴零点** | ⛔ **无** ✓ |
| **D 其它素数问题** | ⚠️ **弱** ✓ |
| **E 其它猜想** | ⚠️ **保留**：等价刻划 `\ker E_2\ne0 \iff \lvert\operatorname{supp}\rvert\le2` 属**Chebyshev 系统／矩问题**的独立命题面；"两行短证书"作为**证书结构**有独立价值 ✓✓ |

## §7 边界与回查（✗✓）

```
✗ 零计算／零数值／未写程序／未做数值实验／**未重新找根**／未算零点／未接 ζ／未找 `1/2`／未证 RH／**未开案** ✓
✗ ⛔ 未碰 `T1b`／`T1c`／`T2` ✓；⛔ 未把 `C3890` 数据作推导输入 ✓；⛔ 未用数值搜索包装成"低次结构" ✓
⚠️ 本档 §2 登记**定义层歧义**（"低次"两读法）⟹ 建议唐先生裁定：目标次数是 **`\le2`** 还是 **span 内较低次** ✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 PRE-VS-POST 命中文件数=0 ⟹ **本档新增** ✓｜rank-collapse 命中文件数=0 ⟹ **新增** ✓
　技术词 短证书 命中文件数=1 ⟹ **档案已有（`C3890`）⟹ 引用** ✓
　技术词 低次消失元 命中文件数=3 ⟹ **档案已有（`C3890`／台账／`C380-T1a-local-audit`）⟹ 引用** ✓
【地图回查】 关键词 4 项 = **未覆盖** ⟹ 可开档 ✓
【档级引用】 `C3890`（`T1a-β` 探针；`§6①` 约束张量；`§0③` 定理更正；`§0④` 短证书）✓｜`C3862`（两层结构／4 条偶频取等 ＋ tie）✓｜`C380-T1a-local-audit.md`（本线第一刀）✓
