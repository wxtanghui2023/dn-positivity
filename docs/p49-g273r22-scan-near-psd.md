# P49-G2.7.3-R2.2：Full-QW 扫描——Mtot 近半正定 + Δ 在噪声区

> 2026-09-02 15:04 · λ≈0 异常澄清 · 双近零 · 需高精度判定

## N=1 核对澄清（之前 audit 误读）
- even 2×2 = [0.0344, 0.0490; 0.0490, 0.0699]——**特征值 [1.0e-6, 0.104]（半正定）**
- odd 1×1 = 0.000564
- **N=1：λ₊ = 1e-6（even）——λ₋ = 0.0006（odd）——Δ = +0.0006 > 0——even ground（极弱）**
- ⚠️ 之前 audit 报告"even 块最小 0.036"是误读（把对角元 Mpar[0,0] 当特征值）——修正：even 最小 ≈ 1e-6

## R2.2 扫描（λ=3——trapz dt=0.01 矢量化）
| N | λ₊ | λ₋ | Δ_N | 注 |
|---|---|---|---|---|
| 1 | 1e-6 | 5.6e-4 | +5.6e-4 | even（弱） |
| 2 | 0 | 1e-6 | +1e-6 | even（极弱） |
| 4-16 | ~0 | ~0 | ~0 | 噪声区 |

- **N≥2：λ₊ ≈ λ₋ ≈ 0（even/odd 双近零）——Δ 在 1e-6 以下——float64 噪声区**
- **‖Mtot‖：N=1-4 ~0.04——N≥6 跳 2.25**（⚠️ N≥6 的 Mtot 有大幅值但 sector 最小仍 ≈0——需查——可能高 k 模式的 Mtot 值大但 even/odd 最小在低 k 近零方向）

## ⭐ 关键发现
- **Mtot（λ=3 Weil 截断）近似半正定**（特征值 ≥ 0——even [1e-6, 0.104]——odd 0.0006）——**与 RH 的 Weil 正性方向一致**（QW ≥ 0 ⟺ RH）
- **但——even/odd 的 ordering（simple-even 核心）在 1e-6 级——数值精度以下——float64 不足判定 Δ 符号**
- 三 sector 大项（MA 4.85/MR 2.68/MP 2.14）抵消后 Mtot 小（0.04）且近半正定——**V₀ 抵消是真实的（Numerical structural observation——唐先生标签）**

## ⚠️ 诚实判断
- **R2.2 扫描不能判定 Δ_N 符号**（双近零——1e-6 噪声区）——**需 C1-C3 + 高精度 eigh（mpmath）**
- N≥6 的 ‖Mtot‖ 跳变需排查（可能高 k 模式——但 sector 最小不受影响？——待查）
- **不是 even 稳定也不是 odd 穿透——是"双近零"——competition 在精度以下**

## 判定
- R2.1 PASS（管线干净）——R2.2 部分执行（发现 Mtot 近半正定 + Δ 噪声区）
- **Δ_N 符号判定需高精度（C1-C3 + mpmath eigh——float64 不够）**
- ⚠️ Mtot 近半正定本身是发现（Weil 正性数值方向——λ=3 截断）

## 下一步候选
- (a) C1-C3 + mpmath 高精度 eigh（小 N——N=2,4,6——判定 Δ 符号——决定性）
- (b) N≥6 ‖Mtot‖ 跳变排查（先确认矩阵正确）
- (c) 唐先生指示
