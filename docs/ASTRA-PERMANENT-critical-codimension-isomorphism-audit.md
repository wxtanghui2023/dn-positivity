已查地图：命中（`ZF-RPC-1-natural-instance-enumeration-and-CLOSED` ＋ `ZF-G5G6-DIM-screen-and-height-collapse` L22）⟹ Astra 批次同构审计，不开新案
D0: 本档对象 = **Astra「Permanent 式 critical-codimension」与既有 C-系列／ZF 系列资产的同构审计** ＋ **判定（包装 vs 新线）** ＋ **`NEW-INFO GATE`（AMEND-25）** ＋ Astra 批次处置
D1: 0（审计型，零计算；结论为同构／降级）
[REVIEW]

# **`Astra` Permanent 式 critical-codimension：同构审计**

## §0 待审计命题

```
$$\text{Astra 机制（Permanent）}:\ \text{构造 object-specific 临界轨迹}\operatorname{Crit}(P)=V(\partial_1P,\ldots,\partial_mP),\ \text{使 }\operatorname{codim}\ \text{远超 determinant 可达规模};\ \text{再以 codimension ＋ degree bound ＋ Bézout 得下界}$$
$$\text{其 RH 映射（候选）}:\ \boxed{\rho\ \text{离轴}\ \Longrightarrow\ \operatorname{codim}\operatorname{Crit}(F_\rho)>\text{universal bound}}$$
$$\textbf{审计问题}:\ \text{这是}\boxed{\text{新的不可满足关系}}\ \text{还是}\boxed{\text{已知骨架的换名}}?$$
```

## §1 档案检索（显式路径，计数）

```
$$\begin{array}{c|c|l}
\text{关键词}&\text{命中档数}&\text{命中性质}\\\hline
\text{Bézout}&1& \texttt{ZF-RPC-1}\ \text{（resultant／消去，}\textbf{该路线已 CLOSED}\text{）}\\
\text{codimension}&5& \text{全在别处（覆盖码协维／自由度计数／C07）};\ \boxed{\texttt{IP-1-G0.2}\ \text{已把「codimension」列为\textbf{改名即 FAIL}}}\\
\text{Jacobian rank}&2& \texttt{C3851}／\texttt{C3792}:\ \text{用于\textbf{注入性／次式}，非临界轨迹}\\
\text{Fredholm}&25& \text{有限容量线（四路线）}\\
\text{relative Morse}&1& \text{四路线之一}\\
\text{multi-Fredholm}&1& \text{四路线之一}\\
\end{array}$$ ✓
$$\textbf{四路线状态（}\texttt{ZF-G5G6-DIM-screen-and-height-collapse.md}\ \text{L22 逐字）}:\ \text{Fredholm kernel locus}\ \boxed{CLOSED\ as\text{-}is};\ \text{moduli boundary}\ \boxed{CLOSED};\ \text{relative Morse/Floer}\ \boxed{OPEN}\ (\text{缺 canonical finite anomaly theorem});\ \text{MULTI-COMPATIBILITY/ISOLATED-DEGENERACY}\ \boxed{OPEN+}$$
```

## §2 ⭐ 决定性判词（`ZF-RPC-1` 逐字）

```
$$\texttt{ZF-RPC-1}\ \text{标题逐字}:\ \text{「自然实例枚举与检验 ⟹「以 resultant／ramification 供 }S_{\rm exc}\text{」路线}\ \boxed{\textbf{CLOSED}}\text{（人为并置）」}$$
$$\text{【⛔ 检验结论】逐字}:\ \text{“要求「离轴零点 ⟹ 固定对象发生}\textbf{退化／坏约化}\text{」⟹ 这正是本线一直在找的}\ \boxed{\textbf{zero-to-obstruction 定理}}\text{（您早前 §97 的最终缺口）⟹ Class A 并未提供新来源，}\boxed{\text{只是把同一 GAP 换个名字}}\ \text{✗”}$$
$$\text{【统一结论】逐字}:\ \boxed{\text{三类均无独立新来源；全部归约到同一个 residual GAP}}\ \Longrightarrow\ \text{RPC-1 自然实例路线 CLOSED（人为并置），不再继续枚举／包装}$$
```

## §3 判定

```
$$\textbf{Astra Permanent 模板的 RH 版本}\ \textbf{必然包含}:\ \boxed{\rho\ \text{离轴}\Longrightarrow\text{某固定对象退化／碰撞／坏约化}}\ \text{这一步}$$
$$\text{而这一步}\ \textbf{逐字等于}\ \texttt{ZF-RPC-1}\ \text{已判定的 residual GAP（zero-to-obstruction，§97 缺口）}$$
$$\Longrightarrow\ \boxed{\textbf{判定}:\ \text{同构 ⟹ 包装（rename），}\textbf{不是新攻击线}}$$
$$\qquad \text{关键差别（诚实标注）}:\ \text{Astra 的 codimension 超界来自 permanent}\ \textbf{固有结构};\ \text{但 RH 版要起作用，仍须}\boxed{\text{离轴假设}\Rightarrow\text{退化}}\ \text{这一条件步骤 —— 而该步骤在本线}\textbf{无独立来源} ✓$$
$$\qquad \text{且}\ \texttt{IP-1-G0.2}\ \text{早已把「codimension」列入}\textbf{改名即 FAIL}\ \text{清单} \Longrightarrow\ \text{本条路线在本线\textbf{双重命中}禁区}$$
```

## §4 唯一保留的残余子问题（诚实标注，不得升级为路线）

```
$$\text{残余问题}:\ \text{是否存在}\ \boxed{\text{ζ-专属对象 }F,\ \text{其临界轨迹}\ \textbf{无条件}\ \text{余维超界}}\ \text{（不需离轴假设）};\ \text{且该结论能}\textbf{独立于零假设}\ \text{连到零点}?$$
$$\text{若不能独立连到零点} \Longrightarrow\ \text{与 ZF 无关（纯几何陈述）};\ \text{若能} \Longrightarrow\ \text{即回到 GAP}\ \Longrightarrow\ \text{无出口}$$
$$\Longrightarrow\ \boxed{\text{该残余子问题不构成新入口}}$$
```

## §5 `NEW-INFO GATE`（`AMEND-25`，照先生 A–E）

```
$$\textbf{Gate A（新变量不算）};\quad \textbf{Gate B（新组合不算）};\quad \textbf{Gate C（新表示不算）}$$
$$\textbf{Gate D（硬门槛）}:\ \text{必须给出新的不可满足关系}\ \boxed{\mathcal O_{\rm new}\not\Leftarrow\mathcal O_{\rm old}}\ \text{并对目标构型产生新限制}$$
$$\textbf{Gate E（同构检查）}:\ \text{是否最终等价于下列之一}:\ \text{局部量}=f(\gamma);\ \text{所有局部量共享 }\beta;\ \beta=\tfrac12;\ \text{positivity}\Leftrightarrow RH;\ \text{谱实现}\Leftrightarrow RH$$
$$\textbf{本轮新增（同构屏，来自本次审计）}:\ \boxed{\text{zero-to-obstruction 屏}}\ ——\ \text{凡「离轴 }\Rightarrow\text{ 某固定对象退化／碰撞／坏约化」型候选，}\textbf{一律视为同一 GAP 的换名}，除非\textbf{另行提供该事件的独立来源} ✓✓$$
```

## §6 Astra 批次处置

```
$$\boxed{\text{整批降级为}\ \textbf{方法学参考}，\textbf{不作为新攻击线}}$$
$$\text{保留资产（仅两项）}:\ \text{① “独立新约束长什么样”的}\textbf{真实样本}（Permanent 是唯一没落入旧骨架的，但经本审计仍归约到 GAP）;\ \text{② root-of-unity 消去交叉项的技术装置 —— 但注意}\ \texttt{ZF-RPC-1}\ \text{显示 resultant 型装置在本线已撞同一 GAP}$$
【⛔ 纪律】 未开新案、未推导、未跑计算 ✓
【边界】 全部为检索级＋逐字引档；判词取自档案原文 ✓
