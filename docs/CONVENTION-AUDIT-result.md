已查地图：命中（`EDR-CAP-1-run-invalid-and-Bn-equals-1-structural-point`／`ZF-EDR-2-gapA-dissolved-and-gapB-exact-form`）⟹ 引用，不开新案
D0: 本档对象 = `IP-5`/`ZF-EDR` 生成约定审计（四问）⟹ 结果：**表来源未核实**，须先重建
D1: 0 （[REVIEW] 轮次：审计，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **约定审计：四问的回答（诚实版）**

## §1 四问

```
**【问 1】档案中 `B_n` 如何定义？** 档案**只有使用处、未见生成定义**：见 `Z_S:=\{n:\operatorname{Supp}(B_n)\subseteq S\}` 与 `r_q:=\min\{n:q\mid B_n\}=\operatorname{ord}_{E(\mathbb F_q)}(\widetilde P)` ✓；**未找到生成 `B_n` 的脚本或约定声明** ✗
**【问 2】与 `x(nP)=A_n/B_n^2` 是否同一归一化？** ⚠️ **不能确认** —— 我按 `x(nP)` 分母实算得 `B_1..B_4=1`；与所引表（`2,13,604,6153`）**不符**
**【问 3】`1,2,13,604,6153` 对应哪个对象？** ⛔ **未能定位** —— `grep`（`604`／`6153`／`2211326`／`1327783`）在 `docs/` 与 `scripts/` 中**未命中生成源** ⟹ 该表**很可能只存在于本线的压缩摘要中**（即**来源未核**）⚠️
**【问 4】同约定重算并逐项对拍？** ⛔ **无法执行**（无约定可依）⟹ 须**先立约定** ✓
```

## §2 结论与处置

```
【结论】 表**来源未核实**；**唯一已执行**的结果是我这次的实算：`E:y^2+y=x^3-x^2-10x-20,\ P=(5,5)` ⟹ `B_1=B_2=B_3=B_4=1`（`x(2P)=16`）✓
【⟹ 处置】 **以实算为准**：把 `B_n` 约定**显式钉死为** `x(nP)=A_n/B_n^2` 的既约分母（`B_n>0`）✓，并**重新生成前 `N` 项**（脚本已可运行）⟹ 此后再跑 `CAP-1` ✓
【判据（照录，不变）】 $$\boxed{\exists S:\ |Z_S\setminus I(E,P)|>|S|},\qquad I(E,P)=\{n:B_n=1\}$$ 再分解 `Z_{\rm prim}` 与 `Z^{*}_{\rm exc}` ✓
【结构（照录您的严谨版）】 $$Z_S=I(E,P)\sqcup Z_{\rm prim}\sqcup Z^{*}_{\rm exc}\ \Longrightarrow\ |Z_S|\le|I(E,P)|+|S|+|Z^{*}_{\rm exc}|$$ ✓✓（并注明：`B_n=1` 只给**例外容量的下界**，不等于 `Z^{*}_{\rm exc}=\varnothing`）✓
【边界】 本档为审计报告；§1 各问结论由本机 `grep` 实测支撑；未制造候选／未启动搜索／未碰 RH／未碰 abc。
