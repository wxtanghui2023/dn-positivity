# TESTING.md —— dn-project 的**可执行回归测试**（2026-09-13 建 ✓）

> **背景** ✓：唐先生 2026-09-13 要求对"⑥ 零测试"做深入分析 ✓。分析结论见 §1 ✓ —— 项目此前
> **没有可执行的测试**（无 `tests/`、无 `conftest.py`、无 CI ✓），只有**探究脚本**与**协议检查器** ✓。

---

## 1. 分析：此前的状态（被测事实核实 ✓）

```
【实测 ✓】`python3 -m pytest --collect-only scripts/` ⟹ **收集到 0 个用例** ✓
【实测 ✓】仓库内**无** `tests/`、`conftest.py`、`.github/`（CI）、`Makefile` ✓
【⚠️ 自我纠正 ✓】我在汇报里说过"项目从未存在过任何测试" ✗ —— **说法不精确**，需更正 ✓：
   · 确实存在 **20+ 个**文件名含 `*_test.py`（如 `affine_crt_test.py`、`discriminant_test.py`）✓
   · 确实存在 **4 个**函数名含 `def test_*`（`PAPERA_sigma_verify.py` 等 ✓）
   · 但**它们不是测试** ✗：例如 `def test_mod_pq(primes, conn, name, n_pairs=10, n_per=8)`
     —— **带必填参数**（pytest 无法调用 ✓）、**只 print 不 assert** ✗ ⟹ 实为**探究/数值检验函数** ✓
【精确结论 ✓】缺口 = **无可执行的回归测试** ✓（不是"没有脚本"，而是"没有测试套件" ✓）
【已有的准测试 ✓（但都不是套件）】① `scripts/check_archive.py`（协议**检查器** ✓，需手动跑 ✗）
   ② `lean/` 的 16 文件编译（我们手动巡检过 ✓，非自动化 ✗）③ 若乾脚本内的 `assert`（局部自检 ✓）
```

## 2. 建成内容（`tests/`，2026-09-13 ✓）

```
tests/test_e44_enclosure.py     ✓ E44 审计的**关键数字**（精确有理算术 ✓）
   · K = 2^140；256 对包络；每对宽度 1 ✓
   · **255 行**每对在端点夹住 j·2^132 ⟹ 行证书偏差 == 256/K = 2^−132 ✓ ⟹ 满足 τ = 3e−40 ✓
   · 边界界：由表算出的 D(1) 落在记录界 82395317/10^8 之下 ✓ 且**符号为正** ✓，余量 ~9.3e−9 ✓
   · 天花板常数 p₀ == 记录的精确分数 ✓ ≤ 0.6818287 ✓ 分母 == 2^43·5^39 ✓
   · 定理常数步 1/(6·256²) + τ/512 ≤ 2.5431316e−6 ✓
tests/test_e45_exact_ramp.py    ✓ E45 两个**精确法**（Q(√2)/Q(√3) 内精确算术 ✓）
   · 权重构成概率律 ✓（Σu = 1、Σt = 0、全非负 ✓）
   · **斜坡逐位精确**：Σ w_c|F_c(j)|² = j 对 j = 1..N（**含边界行** ✓）
   · 由斜坡推出 C(1) = (N+1)/(2N)、**D(1) = 1/(2N)** ✓ ⟹ d₁ = 1/8（N=4）或 1/12（N=6）✓
tests/test_project_invariants.py ✓ 项目不变量
   · `check_archive.py` 退出码 0 ✓（R1–R6 协议 ✓）
   · `data/lawN256_encl_v1.0.json` 可解析 ✓ 且**迁移源 sha256 与记录一致** ✓
```

## 3. 如何跑（✓ 快，0.2 秒）

```bash
cd ~/.openclaw/workspace/dn-project
python3 -m pytest tests/ -q          # 预期：14 passed, 1 skipped
```
⚠️ **不要在仓库根目录直接跑 `pytest`** ✗ —— 它会遍历整个仓库（含 `fn_backup/` 约 26 万文件 ✗）而卡住 ✓。

## 4. ⚠️ **Lean 的算力纪律**（唐先生 2026-09-13 11:47 指令 ✓ —— **强制**）

```
【指令 ✓】"Lean 昨天就已经确认能跑了 … **所有论文最终定稿后再跑 Lean，不要浪费算力**" ✓

【因此 ✓】① **不再**把 Lean 编译放进常规测试套件 ✗（已从 `tests/` 移除 ✓，只留一个 skip 桩以留痕 ✓）
   ② **不再**在常规会话里重跑 `lean/*.lean` 巡检 ✗
   ③ Lean 只在**论文定稿里程碑**上跑 ✓

【记录在案的事实 ✓（不重复验证，仅备查）】
   2026-09-13 巡检：**16 个 .lean 文件全部 exit 0 / 零 error** ✓（含 `E45-NearCUE-Instance.lean` ✓）；
   同日晚些时候的带 Lean 测试运行：`15 passed in 85.31s` ✓（即又跑了一遍，纯属浪费 ✗ —— 已按指令停止 ✓）
【里程碑命令 ✓（定稿时才用）】
   export PATH="$HOME/.elan/bin:$PATH"
   cd ~/mathlib4 && for f in ~/.openclaw/workspace/dn-project/lean/*.lean; do
       lake env lean "$f" || echo "FAIL $f"; done
【背景 ✓】本机 `~/mathlib4` 是 v4.33.0 正式版，而前沿仓库 pin 的是 v4.33.0-rc2 + 特定 Mathlib rev ✗
   ⟹ **无法** import 其 `ceiling_nearCUE` ✓（详见 `docs/E45-ceiling-law-construction.md` §12 ✓）
```

## 5. 本文件**不**主张什么（✓）

```
✗ 不主张测试已"完备" ✗ —— 目前**只锁定** E44/E45 两处关键数字与三条不变量 ✓；
   139 个其它脚本的**回归保障仍为空** ⚠️（见 §6 待办 ✓）
✗ 不主张 Lean 已验证过的东西现在不成立 ✗ —— 只是**不重复跑** ✓（算力纪律 ✓）
✗ 不主张探究脚本（`*_test.py`）没有价值 ✗ —— 它们有价值 ✓，只是**不是测试** ✓
```

## 6. 待办（✓ 若要继续补测试）

```
① 把 E44/E45 其它脚本的**关键输出**也做成断言 ✓（例如 E45 步 5–9 的定论值 ✓）
② **golden-file** 测试 ✓：scripts/*.txt 与重跑输出逐字比对 ✓（能抓"无意改动输出" ✓）
③ ⚠️ **成本评估**：① ② 需按"值 → 成本"排序 ✓；**不得**引入需数分钟的测试 ✗（算力纪律 ✓）
```
