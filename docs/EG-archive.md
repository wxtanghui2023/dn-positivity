# E-G 路线归档（2026-09-08 21:00——唐先生定案）

> ⚚ TP₅ 读数撤回（2026-09-12）：本文若把 TP₅ 的七个负子式称为【新的负面结果】✗，该读数**已撤回** ✓ —— 后续审计发现**对象错误**：与 RH 相关的对象是 **1/Ξ 的双边 Laplace 逆变换** ✓，而非本文所测的核 Φ ✗ ⟹ **数值事实仍成立**（30/60/120 位逐位相同 ✓；高斯对照验证装置 ✓），但**不再作为该路线的负面结果** ✓。详见 `E23-lee-yang-dqpt-report.md`。

## 判定：(a) 归档——已完成边界识别的路线封存
**不是失败归档**——E-G 把"FE 对称 + 水平集 + 单调性"整类几何路线的有效边界测出来了。

## 路线链条（完整——）
```
FE 对称 → Im Λ/Im ξ 关于 σ=½ 奇 → F_t(½)=0
→ 数值：唯一零线（σ=½）+ 单调（到 γ~72000 含 Lehmer 对——）
→ 解析刚性缺失（G4 未闭合——）
→ 【ARCHIVED】
```

## ⚠️ 唐先生技术修正（必须入库——）
**G1 表述过强——需要降级**：
- Λ(s)=χ(s)^{-1/2}ζ(s) 的平方根**不能无条件在整个复平面取单值**——存在 branch 问题
- 严格表述：**在所选的局部/条带分支上**数值验证了 FE 对称与共轭关系
- 不能把 Λ 当作全局 entire、实系数函数
- ξ(s)=½s(s−1)π^{-s/2}Γ(s/2)ζ(s) 才是自然的全局对象

## 核心归档结论
1. **Λ-路线没有逃出 ξ-路线**（交叉审计：Im ξ 也奇对称+单调——）
2. **E-G 找到的是 RH 的漂亮等价几何——不是证明 RH 的新刚性来源**
3. 留下的结构命题：RH ⇐ 临界带内 Im ξ(σ+it) 的唯一零线性质
   ——但"唯一零线性质"本身承载 RH 的全部困难
4. 继续证明 F_t' 恒不变号 ≈ 把 RH 重新包装成 Im Λ' 的符号定理（循环——）

## 禁止的循环（正式——）
```
更多 t → 更多单调数值 → 猜测全局单调 → 尝试 Hadamard → 出现零点 → 循环
```
扩大到 10^6/10^7 对寻找新数学机制的边际价值极低——禁止。

## 复活条件（resurrection criterion——严格——）
**只有找到一个不依赖零点位置的正性/变差控制定理才重新启动**：
- 例：F_t'(σ) = ∫₀^∞ K_{σ,t}(x)dx 且 K≥0——正性不偷偷等价 RH
- 或：F_t'(σ) = P_{σ,t}——某种未使用过的正算子/变差界/完全单调/总正性结构
- **核心：证明 F_t' 的符号而不使用零点信息**
- 没有这种机制——不投入

## 评级总结
| 项 | 状态 |
|---|---|
| G1（Λ 定义——） | 局部/选支意义下成立（非全局——） |
| G2（F_t 零点——） | 强数值支持 |
| G3（F_t' 不变号——） | 强数值支持 |
| "精确线性" | 已明确否证（a₃≠0——） |
| G4（解析证明——） | 未闭合 |
| Λ vs ξ 交叉审计 | 无新本质自由度 |
| 新机制 | 未产生 |

## 下一轮范式禁令
**避开"从 ξ/Λ 的零点几何再提取一个符号定理"这一范式**
——去寻找 E-G 完全没有使用的结构来源。
