# ⚠️ 更正：二次端点的"绿灯"用错了 B ✓（2026-09-12 21:1x）

> 来源 ✓：论文 v2 结构子代理的发现 ✓ + **我的独立复算**（下表 ✓）
> 性质 ✓：**我的错** ✗ —— 我在 `PAPERA-quadratic-gap-located.md` / roadmap 里说的"余量 1.69 ✓"
> 用的是 **B_{T₀} 的真值** ✗，而不是**论文能证的显式界** ✓ ⟹ **结论要改** ✓

---

## 一、复算（数字 ✓）

```
T₀ = 1.1325×10⁶ ✓｜N = 2,001,052 ✓｜n = T₀² = 1.2825×10¹² ✓｜S_n = 2.001269×10⁶ ✓

  B_{T₀} 真值（表内 + 解析尾）  = **9.2065×10⁻⁷** ✓
  论文显式上界 B^exp            = **3.4015×10⁻⁶** ✗ —— **比真值松 3.69 倍** ✗
  （依据 ✓：论文 SUMMARY 明写 `B_T ≤ 3.4×10⁻⁶（T = 1.13×10⁶）` ✓）

  用【真值 B】    ：n·B = 1.1808e6 ✓ ⟹ 余量 **1.6949** ✓ ✅ 过
  用【论文 B^exp】：n·B = 4.3625e6 ✗ ⟹ 余量 **0.4587** ✗ ❌ **不过**
  再用【B^exp × 被丢掉的 e^{n/(2T₀²)} = e^{0.5} = 1.6487】：
                    ⟹ 余量 **0.2782** ✗ ❌ **不过**
```

## 二、更正后的结论 ✓

```
❌ **撤回**：我之前写的"二次端点处余量 1.69 > 1 ⟹ 绿灯" ✗
   —— 那是用**真值 B** ✓，而论文只能给出 **B^exp** ✗ ⟹ **不可证** ✗
✅ **更正**：**用论文现有的显式常数，二次端点不过** ✗（余量 0.459 ✗；含 e 因子则 0.278 ✗）
```

## 三、⭐⭐ 但差距是【显式常数量级】，不是结构障碍 ✓✓

```
【需改善约 3.59 倍】✓
【而显式界恰好比真值松 **3.69 倍** ✓】⟹ ⭐ **把 B^exp 收紧到接近真值，几乎正好补上缺口** ✓✓
【外加因子 e^{n/(2T₀²)} = 1.65 ✗】—— 论文为 n ≤ 2T（线性 ✓）合法地丢掉了它 ✓，
   但在 n ~ T₀² 处它是 O(1) 的 ✗ ⟹ **二次范围必须把它放回来** ✓
⟹ ⭐ **结论 ✓：二次范围【不】因结构性障碍而失败 ✗，而是因【两个常数级的松弛】**
   （① B^exp 松 3.69 倍 ② 丢掉的 e 因子 1.65 倍）✓✓
   ⟹ **这是一个【具体、可攻、有限】的目标** ✓✓（不是"需要新机制" ✗）
```

## 四、下一步（明确 ✓，两条并联 ✓）
```
① **收紧 B_T 的显式界** ✓ —— 从 3.4×10⁻⁶ 收到接近真值 9.2×10⁻⁷ ✓
   手段 ✓：更好的显式 N(T) 界 / 更细的尾部 Abel 估计 ✓（我方有现成 Abel 机器 ✓）
② **把 e^{n/(2T₀²)} 因子放回** ✓ —— 改写 Lemma 的推导，不在 n ~ T₀² 处丢弃 ✓
   （论文为 n ≤ 2T 丢弃是合法的 ✓；扩到 T₀² 必须保留 ✓）
⟹ 若 ①② 都成 ⟹ **二次端点的余量** ≈ 1.69 / 3.69 × ... hmm：
   收紧后 B ≈ 真值 ⟹ 余量回到 ~1.69 ✓ ⟹ 再除以 e 因子 1.65 ⟹ **~1.02** ✓ ⚠️ **勉强** ✓
   ⟹ ⚠️ 诚实说：**刚好在边缘** ✓ —— 需要 ① 收得比"接近真值"更好一点 ✓
```

## 五、本文件**不**主张什么 ✓
```
✗ 不主张二次范围已可达 ✗ —— 更正后**更保守** ✓：需两个常数改进 ✓ 且结果在边缘 ✓
✗ 不主张真值 B 可用于证明 ✗ —— 真值只对**已列表的零点**成立 ✓，证明需要**对所有构型**成立 ✓
✗ 不主张 B^exp 一定能收紧到真值量级 ✗ —— 那是一个**待做**的显式估计 ✓
✅ 主张：**① 我更正了自己的错 ✗ ② 缺口定位为两个【常数级】松弛 ✓ ③ 目标具体可攻 ✓**
```

## 六、对几份既有文档的影响（后续需同步 ✓）
```
· `PAPERA-quadratic-roadmap.md` §三"端点判据"：**1.69** 需改成"**真值 1.69 / 可证 0.46**" ✓
· `PAPERA-delta-N-attack.md`、`PAPERA-quadratic-gap-located.md` 同处 ✓
· `scripts/PAPERA_avgcase_probe.py` 输出里的 margin 1.6949 ✓ 需加注"用真值 B" ✓
（本次先记录更正 ✓，同步修改留待论文改写时一并做 ✓ —— 避免反复改动 ✓）
```
