已查地图（**先查后写**）：`C-328`（外部池总结：0 条达操作级 ✓）、`C-327`／`C-326`／`C-325`／`C-323`（四条链 ✓）、`C-320`（**证书闭合 ≠ 新算术机制 ≠ RH bridge** 边界已钉死 ✓）、`C-287`／`C-288`（禁止元判据递归 ✓）。回查见 §5 ✓

D0: 本档对象 = **C-329：路线收束决议（状态决策登记）**，**零计算**
D1: 0
FREEZE-ACK: 本档即冻结审计

---

## §0 决议三条（✓✓，唐先生口径 ✓）

$$\textbf{① 主动外部候选枚举}\ \textbf{停止}✓✓：\textbf{不}再主动枚举第五、第六个「著名独立问题」✗（\text{预期信息增量低}✓）$$
$$\textbf{② 未来外部输入}\ \textbf{只接受自然出现者}✓✓：\text{仅当外部文献／新定理}\ \textbf{自然出现} \text{下列结构时才重开外部线}✓：$$
$$\qquad \boxed{\text{两个本来独立的数学结构} \;+\; \text{已有非平凡 compatibility} \;+\; \text{一个可量化的异常／约束}}✓$$
$$\qquad \textbf{且}✓✓：\text{新信息}\ \textbf{必须先从数学文献中出现}✓，\textbf{不}\ \text{由我们继续设计候选筛选器产生}✗$$
$$\textbf{③ } `M=5 certificate`\ \textbf{保持唯一}\ \textbf{OPEN/AUDIT} \text{技术线}✓✓：\text{但其}\ \textbf{完成} \text{不得自动解释为}\ RH\ \text{bridge}✗✓（\text{C-320 边界}✓）$$

## §1 四条链共同信息（✓✓）

$$\text{层级图景已稳定}✓：\text{Mahler} \to \textbf{映射／等价}✓；\text{LR} \to \textbf{等价网络}✓；\text{Barker／Littlewood} \to \textbf{恒等式级联合约束}✓；\textbf{操作级非平凡 compatibility} \to \textbf{四条均未及}✗✓$$
$$\textbf{定性}✓✓：\text{这}\ \textbf{不是} \text{「不存在 compatibility」}✗；\text{但足以说明}\ \textbf{再主动枚举预期信息增量很低}✓（\text{故 C-328 的「停止枚举」直接生效}✓）$$

## §2 线路状态表（✓✓）

| 线路 | 状态 | 下一动作 |
|---|---|---|
| **Mahler／`L`-值** | **PAUSED** ✓ | 不继续内部挖掘 ✗ |
| **Barker** | **CLOSED** ✓ | 不重开 ✗ |
| **Littlewood** | **CLOSED** ✓ | 不重开 ✗ |
| **Lonely Runner** | **CLOSED** ✓ | 不重开 ✗ |
| **外部问题主动枚举** | **STOPPED** ✓✓ | 不再扩池 ✗ |
| **M=5 certificate** | **OPEN/AUDIT** ✓✓ | 可继续既有证书审计 ✓ |
| **RH 主线** | **未产生新 bridge** ✓ | 不人为制造 ✗ |

## §3 输入模式变更（✓✓）

$$\boxed{\text{外部问题} \longrightarrow \textbf{被动输入}}✓ \quad \text{而}\textbf{非}\quad \text{我们主动搜索一个新问题}✗✓$$
$$\textbf{重开条件}✓✓（\text{全部满足才重开}✓）：\text{① 两独立结构}✓；\text{② 已有非平凡}\ compatibility✓；\text{③ 可量化异常／约束}✓$$
$$\textbf{与}\ C\text{-328 纪律一致}✓：\textbf{不}\ \text{增加}\ F9／F10✗，\textbf{不}\ \text{设新判据}✗✓$$

## §4 M=5 的两个层次（✓✓，须分清 ✓）

$$\textbf{即使最终得到}✓：\forall B \in [0,\pi]^5,\ \max_{1 \le k \le 25} q_k(B) > \tfrac12✓（\text{完整证书}✓）\ \Longrightarrow \text{首先仍是}\ \boxed{\textbf{certificate closure}}✓$$
$$\qquad \textbf{而}\ \textbf{不是}\ \boxed{\textbf{new RH mechanism}}✗✓（\text{C-320 已钉死}✓✓）$$
$$\textbf{下一步问法}✓✓：\textbf{不}\ \text{再问「这个结果怎么硬接}\ RH\ \text{」}✗；\textbf{先把}\ \text{「M=5 到底能否数学闭合」这个事实本身弄清楚}✓$$
$$\textbf{防线}✓✓：\textbf{不}\ \text{因它是「目前唯一}\ OPEN\text{」就包装成新的}\ RH\ \text{机制}✗✓$$

## §5 减法定性 ＋【技术词回查】输出（**先跑后写**✓）＋ 边界

$$\boxed{\text{候选生成} \cancel{\longrightarrow} \text{候选生成} \cancel{\longrightarrow} \text{候选生成}}✓ \quad \Longrightarrow \quad \boxed{\text{已有 OPEN 技术问题} \;+\; \text{等待真正新的数学输入}}✓✓$$
```
技术词 路线收束决议 命中文件数=0    :: 
技术词 被动输入     命中文件数=0    :: 
技术词 候选生成循环 命中文件数=0    :: 
```
- 本档新增 ✓：`路线收束决议`／`被动输入`（依上表判 ✓）
- **零计算** ✗；未读 pending ✗；未改他档正本 ✓（仅追加 ✓）；未动 v4 ✗；`C-181` 的 `u<=5` 仍为 **GAP-A** ✓
- **不得**写成：外部路线不可能 ✗（仅信息增量低 ✓）；操作级机制不存在 ✗；M=5 可接 RH ✗；档案已结案 ✗
