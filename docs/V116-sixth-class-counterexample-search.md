# V116 — 第六类反例搜索（方向 (iii) 首轮）

> 口径：本方文件为**纸面/档案**判定，默认不写代码。
> 领号：`bash scripts/id_claim.sh audit sixth-class-counterexample-search` ⟹ **V116**
> 日期：2026-09-14 ｜ 触发：唐先生 12:12「继续」（＝按 `V115` 重排后首位执行）

## §① 编号与查重

- 编号：**V116**（脚本输出，见上）。
- 查重（**先领号再建文件 ✓**）：
  ```
  $ bash scripts/id_claim.sh audit sixth-class-counterexample-search
  V116
  $ ls docs/ | grep -i "V116"
  V116-sixth-class-counterexample-search.md          # ← 仅命中本档（新建）；无既有 V116-* ✓
  ```
- 上游档：`V115`（`docs/V115-representation-contraction-r1r2r3.md`）＝本方向重排序来源 ✓。
- 状态：**完成**（①–⑧ 逐节落盘 ✓）。

---

## §② 自建「单点隔离」版判据（**先写死 ✓；三态规则写在正文之前 ✓**）

> ⚠️ **合规前置（`V109` §⑤-3 逐字要求 ✓）**：本判据 **须自建**、**禁搬 `N31` 版** ✗。
> `N31` 版（`V107` §⑤-2 S1–S3）的谓词＝**「√-尺度正性」**；`V108` §④-3 逐字：该谓词
> 「**不直接适用**覆盖论证 ✗ —— **搬用会【换对象】（N28 陷阱 ✗）**」。
> ⟹ **本判据的谓词 ＝ 载体的「选择原理（Σ）之装置类型」，与「正性尺度」无关** ✓（〔本方拟定义〕）。

### ②-0 被判对象的形式（先固定，不发明 ✓）

```
候选机制 M 一律写成三元组：  M ＝ ( D , Iso , Σ )
  · D   ＝ 输入数据（整数／素数／理想／Galois／adelic／global ⟹ 落在 `E102` §C1 允许域内 ✓）
  · Iso ＝ 隔离对象（M 在 D 上产出的对象／极限／集合）
  · Σ   ＝ **选择原理**：把候选集收缩到单点的**规则**（＝「canonically 单点隔离」的机制内核 ✓）
           〔逐字锚〕`E105` §5 标题逐字：「"canonically 单点隔离"的已知机制 ✓ —— 五类」✓
```

五类装置 **A1–A5（＝ `E105` §5 逐字表，原样搬运「五类叙述」，非搬运其判据 ✓）**：

| 编号 | `E105` §5 逐字叙述 | 选择原理 Σ 的形态 | `E105` §5 逐字子判决 |
|:-:|:--|:--|:--|
| **A1** | ① 方程（零点集 $F=0$） | 「满足等式 E(x)=0」 | ⟹ 类 IV／T1 ✗ |
| **A2** | ② 不等式／极值（$\arg\max$） | 「取到极值／上下确界」 | ⟹ 箱 5 ✗（计数/熵） |
| **A3** | ③ 对称／不动点（$g\!\cdot\!x=x$） | 「是不动点／唯一不变对象」 | ⟹ 仍是方程 ✗ |
| **A4** | ④ 可定义性（"最小满足 P 的点"） | 「序极小／最小定义元」 | ⟹ 类 VI ✗ |
| **A5** | ⑤ 动力／发生构造（迭代极限） | 「迭代／发生过程的极限」 | ⟹ 箱 5 ✗ |

### ②-1 ⭐ 三条规则（**写死 ✓：通过／不通过／无判定**）

```
【判据 V116-C（「单点隔离」版 · 三态）】输入 M＝(D,Iso,Σ) ⟹ 输出**恰一个**：

 R-W（良构闸 · 前置）
   若 M 不满足 admissible：C1 ∧ C★（`E102` §1 逐字／`V115` §②-3 逐字），
   或**以** {ρ}／L-值／解析延拓／Euler 解析规则／AFE 为构造或复原步骤（`V109` §⑥-5 K1-C 逐字）
   ⟹ 输出 **无判定**（理由：**非 𝒞 成员／循环**）×──「不通过 R-W」≠「不通过分类」✗

 R-A（归约见证 ⟹ **属五类** ＝ 三态之「通过」）
   若**能显式给出**一个 canonical、有限、可核对的翻译 τ：Σ ↦ A_k（k∈{1..5}）
   ⟹ 输出 **属五类**，并注 k 与 τ。**见证须显式**（不得只写「疑似」✗）。

 R-B（不可归约证书 ⟹ **属第六类** ＝ 三态之「不通过」）
   若 R-A 不成立 **且** 持有「Σ 不可能 canonical 地归入 A1–A5」的**证明**（不可归约证书）
   **且** Iso 确实**单点隔离一个零点**
   ⟹ 输出 **属第六类**。⚠️ 三者缺一 ⟹ **不得**输出属第六类 ✗。

 R-D（默认 ⟹ **无判定**）
   若 R-A 不成立 **且** 无 R-B 证书（或 R-W 未决）⟹ 输出 **无判定**，并**逐条列缺什么**（不得含糊 ✗）。
```

**三条硬约束（与规则同处正文之前，写死 ✓）**

```
【C-1 禁搬 N31 版 ✓】谓词 ＝ Σ 的**装置类型**（A1–A5），**不是** `V107` §⑤-2 的「√-尺度正性」；
    引用 `V107` 只引其**结论／孔位**，**不引其 S1–S3 判据** ✓（`V108` §④-3 N28 陷阱 ✓）。
【C-2 不预设 RH ✓】A3 只用「载体的**内在**对称」（如 $s\leftrightarrow1-s$，`E105` §5③ ✓）；
    不得以「ζ 零点在临界线」为输入或隐设 ✓（`V109` §⑥-5 K1-C ✓）。
【C-3 未找到 ≠ 不存在 ✓】R-A 失败**单独**只能给 R-D（无判定）；
    **不得**因「未找到 τ」而输出「属第六类」 ✗、**不得**因「未找到反例」而输出「孔已堵」 ✗
    （宪法 §0 逐字；`E105` §7 逐字「未宣布 T5 绝对死亡 ✗」✓）。
```

### ②-2 ⚠️ 判据的两条已知盲区（**先声明，防后文误用 ✓**）

```
B-1 「属五类」**不等于**「判死」 ✓：须再接 `E105` §5 的**子判决**（①②③④⑤ ⟹ 类IV／箱5／仍是方程／类VI／箱5）。
    —— 但**方向 (iii) 的靶只需「是否属五类」**：属五类 ⟹ **不产生表外结构 ⟹ 不反驳覆盖论证** ✓
    （`E105` §5 逐字「五类皆已排除或归类 ✓」；唯一残余＝**清单穷举性未证** ✗ ✓）。
B-2 五类装置表本身的**穷举性未证** ✗（＝ `E105` §5 末逐字「但"清单穷举性"未证」／`E105` §7 逐字「唯一残余」✓）。
    ⟹ ⚠️ **凡用「任何单点钉扎必落 A1–A5」来做归约的推理，都会暗中假设 u1 ⟹ 循环** ✗。
    ⟹ 本判据**只允许显式 τ 见证**（R-A）；**禁止**用「穷举 ⟹ 必属五类」充作见证 ✓（§④ 据此把 content 半判为 R-D ✓）。
```

---

## §③ 孔-1 ＝ Selberg 类改造检查（判据 V116-C 逐条）

### ③-0 孔-1 的逐字内容

```
`V107` §⑤-2 逐字：⚠️ 孔-1＝**Selberg 类改造**（②-3-c）：能否把"谱实型"正性**改造成**"X 对角型"
   而不落 L1 的 N0 转译？ —— 若"能" ⟹ 反驳 N31 的 C-α；若"不能" ⟹ 为 α 半提供一例强证据 ✓（**未证 ＋ 未反驳** ✗）。
`V107` §②-3-c 逐字：Selberg 谱正性**输出 ＝ 谱实** ⟹ **不在 N31 类内** ⟹ **不构成反例** ✓✓
   （两者是**不同轴**：L1 轴＝谱实几何 vs Q3 轴＝对角量 √。此点与 `E130 §结论四` 逐字一致 ✓）。
```

### ③-1 判据检查（**两读穷尽 ⟹ 两读皆落 A1／A2** ✓）

```
【读法 I（Selberg 原样 · 不改造）】
  判据 W0：Selberg 类机械 ＝ Euler 积 ＋ 函数方程 ＋ Ramanujan（**局部／Euler 因子型** ✓）。
  `E101` §8.4 逐字：「任何**局部分解型**载体在 $\sigma\le1$ **结构性看不见零点** ✗」✓
  ⟹ 违反 C2（global visibility，`E102` §1 逐字）⟹ **非 𝒞 成员** ⟹ R-W ⟹ **无判定**（理由：非载体）✓
  ⚠️ 注意：**这不是「不是第六类」** ✗ —— 是「**根本不在被判对象的域内**」（C-3 ✓）。

【读法 II（改造为 global ⟹ 使其真能「看见 global zeros」）】
  `E101` §8.4 二择一逐字：① 若「看见 global zeros」⟹ 必然已蕴含「global 正性/极化」⟹ (β) **覆盖** ✓；
    ② 若存在「看见 global zeros」而**无需**极化的 canonical 全局算子 ⟹ **缺口真** ✓✓
  ⟹ **两读出尽**（`E101` §8.5 逐字「YES ⟹ 覆盖 ✓ 缺口假 ✗｜NO ⟹ 表外新类 ✓ LIVE」✓）。
  现对两读分别给 τ：
   · 读法 II-①（含全局正性/极化）：单点隔离的 Σ ＝ 正定型的**核／极小元** ⟹ ⟨Xv,v⟩=λ_min
     ⟹ 核 ker(X−λ_min) ⟹ **τ：Σ ↦ A1（方程）** ✓〔本方拟定义·见证〕（亦可视作 A2 的极值侧 ✓）
   · 读法 II-②（无极化而看见零点）：要「看见某个零点 ρ」仍须**评估**某个全局对象在其处的退化
     ⟹ 单点性来自「$F(\rho)=0$」 ⟹ **τ：Σ ↦ A1** ✓（若经 Deninger 型谱 ⟹ 特征方程 ⟹ 仍 A1 ✓）
  ⟹ 两读皆 **R-A 成立（显式 τ）** ⟹ 输出 **属五类**（k＝1；读法 II-① 兼 k＝2）✓
```

### ③-2 孔-1 判定

```
✅ **在孔-1 内【不能】构造出不属于五类的「canonically 单点隔离」机制** —— 就**可核对的 τ 存在**而言 ✓；
   **且**其自然改造形态（谱实型／X 对角型）**恰落在 A1／A2** ✓〔本方推论〕。
⚠️ **不得**据此宣称「孔-1 已被证明属五类」 ✗ —— 因为：
   (i) τ 为〔本方拟定义〕的**显式见证**，但**未覆盖**「孔-1 是否存在**第三种**改造」这一全称问题 ⟹ 第三种读法仍走 R-D（无判定）✓；
   (ii) 「任何单点钉扎必落 A1–A5」若用作推理 ⟹ **暗中假设 u1 ⟹ 循环** ✗（§②-2 B-2 逐字禁令 ✓）。
⟹ 孔-1 的**三态输出**：**读法 I ＝ 无判定（非 𝒞 成员）**；**读法 II-①／II-② ＝ 属五类**；
   **「是否存在第三种改造」＝ 无判定**（R-D）✓。
⚠️⚠️ **换对象诊断 ✓**：孔-1 的原问（`V107` §⑤-2）是「**反驳 N31 的 C-α**」——**谓词是正性尺度，不是隔离装置** ✗。
   ⟹ 即便孔-1「改造成功」，其**产物**按 `V109` §③-2 方框须先满足 C2（真入谱），而据 `E101` §8.4 这要求**全局性** ⟹ 落 A1/A2 ⟹ **仍属五类** ✓
   ⟹ **孔-1 作为「覆盖论证」的靶，其强度【低于】作为「N31」的靶** ⚠️（〔本方推论〕）✓
```

---

## §④ 孔-2 ＝ R_CS／X6 的 content 半检查（**同型检查** ✓；⚠️ 非 `N43` 型 defect ✓）

### ④-0 孔-2 的逐字内容

```
`V107` §⑤-2 逐字：⚠️ 孔-2＝**R_CS／X6 的"对合半"**（`E130` §③ 族A 逐字：「对合半部分 canonical 可得
   （范围对换，不动点内生＝√X）」）：其**不动点恰为 √X** ⟹ **满足 (H3)** ✓，
   且范围对换 **canonical ⟹ 无人为外权 ⟹ 疑似 β-盲** ⚠️；
   **唯独 content 半崩塌于 N43** ✗ ⟹ 若 "content 半"能被**非 N43 型**修复
   ⟹ **即得类内非有限性成员 ⟹ 反驳 N31** ✓。—— **这是当前最锐的反例搜索靶** ✓
   （`E130` §⑧② 逐字亦定为 "精确靶 ＝ 非 N43 型的 defect" ✓）。
```

### ④-1 判据检查（**对合半 vs content 半分开走三态 ✓**）

```
【对合半 Σ_ι】
  ι²=id；Fix(ι) 由「x − ι(x) = 0」给出 ⟹ **显式 τ：Σ_ι ↦ A1（方程）** ✓；
  同时按 `E105` §5③ 逐字「对称／不动点（$g\!\cdot\!x=x$）」⟹ **亦 ↦ A3** ✓（子判决逐字「仍是方程」✓）。
  ⟹ **R-A 成立（显式见证、**非**循环） ⟹ 输出【属五类】（k＝1／3）** ✓
  ⚠️ **但**：`E130` §③ 逐字「不动点内生＝√X」 ⟹ Fix(ι) 是**一维族（尺度线）**，
     **不是单点** ✗ ⟹ 违反 R-B 前提「Iso 确实**单点隔离一个零点**」 ⟹ **对合半是「半机制」，不能单独构成候选** ✓。

【content 半 Σ_c】—— **这才是真正把点钉死的那个 Σ** ✓
  现状（`V107` §⑤-2 逐字「 content 半崩塌于 N43」✗）⟹ M 不完整 ⟹ 不满足 R-B 前提 ⟹ **无判定** ✓
     （理由：**content 半的精确形式未获取** ⟹ τ 不可构造 ⟹ 只能 R-D ✓）。
  若被「**非 N43 型**」修复，则修复须在 √X 族内**再给一个标量条件**。两支：
   · 支二（修复**依赖外权**）：「范围对换 ⟹ 无人为外权」是 canonical 的**全部理由**（`E130` §③ 逐字 ✓）；
     若修复必须引入**外在权重** ⟹ 违 C★（内在性，`E102` §1 逐字）⟹ **R-W ⟹ 无判定（非 𝒞 成员）** ⟹ **不作反例** ✓。
   · 支一（修复**仍 canonical**）：则 Σ_c ∈ {A1（c(x)=c₀）／A2（极值）}（〔本方推论〕）——
     ⚠️ 但该推论的**唯一依据**是 A1–A5 表**穷尽** ⟹ **暗中假设 u1 ⟹ 循环** ✗
     ⟹ 依 §②-2 **B-2 逐字禁令**（「只允许显式 τ 见证」）⟹ **不得**据此输出「属五类」 ✗ ⟹ 只能 **无判定** ✓。
  ⟹ **content 半：两支皆不出「属第六类」** ✓，但**支一也不出「属五类」**（循环禁用）⟹ **孔-2 ＝ 无判定** ✓。
```

### ④-2 孔-2 判定 ＋ ⚠️ 换对象诊断（**本轮实质发现之一**）

```
⚠️⚠️ **孔-2 的谓词是「有限性来源」，不是「隔离装置」** ✗：
  `V107` §⑤-2 逐字把本靶的**目标**写作「**即得类内非有限性成员 ⟹ 反驳 N31**」；
  `E130` §⑧② 逐字亦定为「精确靶 ＝ **非 N43 型的 defect**」—— **defect 是 content（有限性）侧的对象** ✓。
⟹ 即便 content 半修复成功：其**隔离**仍走 A1／A3（对合半的不动点）⟹ **不构成第六类** ⟹
   **不能反驳覆盖论证** ✓（只能反驳 N31 的 α 半 ✗）。
⟹ ⚠️ **把「N31 靶」当作「覆盖论证靶」＝ 换对象** —— 与 `V109` §⑥-5 K1-A 逐字所禁的「搬用 N31 版 S1–S3 判据于本靶」
   属**同一风险族** ⚠️（〔本方推论〕；因未「改称 T5」故**不严格触发** K1-A ✓，但同型 ✗）。
⟹ ⭐ **故 `V109` §⑤-3 把 CS-2／CS-3 列为 (iii) 的「现成可判定判据」，在「隔离装置口径」下【不成立】** ⚠️
   —— 二者是**N31 口径**的靶（`V107` §⑤-2 原句自己就写着「反驳 N31」✓；`V109` §⑤-3 引之而未改谓词 ⚠️）。
```

---

## §⑤ 三关 ＋ K2-E″ 逐条（`MASTER-NOGO-AND-LIVE-PATHS.md` 末段 2026-09-14 新增判据，**逐字 ✓**）

### ⑤-0 判据逐字（先引，后判 ✓）

```
【⭐ 判据 K2-E″（"是而空"型）】若某替换／界／输入只提供【绝对大小界／可求和性／截断界】，而**不改变覆盖秩**
   ⟹ **立即 NO-GAIN** ✗（不计为进展 ✓）⟹ 模式：Input strength ≠ selection strength ✓
【⭐ 负结果三关】
 · Gate 1 方向：该量随 n 的变化方向是否与覆盖所需一致？须有有用的单调/递减结构…且 Δ(n) 被实际消费 ✗（仅 B(n)<∞ 不算过 ✗）
 · Gate 2 秩：budget ordering ⟹ admissible-cover ordering 是否成立？若只有统一界 B(n)≤Cn^α 且不能排除 swap ⟹ P-Type、无选择力 ✗
 · Gate 3 截断：若只用 n≤N_0，须查 sup_{n>N_0}B(n) 或 ∑_{n>N_0}B(n) 是否重新吞掉区分量 ✗
 ⟹ 任一关"不过" ⟹ 该路径**不得**声明为进展 ✓（与 K2-E″ 并用 ✓）
```

### ⑤-1 逐条出具（**对两个对象分列：本轮自身 ／ 预判的「界型修复」** ✓）

| 关 | 本轮自身（纸面分类动作） | 预判：孔-2 的「非 N43 型修复」若走**界型路线** |
|:--|:--|:--|
| **K2-E″** | **不命中** ✓（未提交任何替换／界／输入；动作＝**分类**，产出＝三态判定 ✓） | ⚠️ **命中** ✗ —— 绝对大小界／可求和性／截断界**皆不改覆盖秩** ⟹ **立即 NO-GAIN** ✓（同 `V113`／`V114` 两次独立确认 ✓） |
| **Gate 1 方向** | **不适用** ✓（无随 n 变化的量） | **不过** ✗ —— 只有 $B(n)<\infty$ ⟹ 逐字「**不算过**」✗ |
| **Gate 2 秩** | **不适用** ✓（无 budget） | **不过** ✗ —— 统一界 ⟹ 不能排除 swap ⟹ **P-Type、无选择力** ✓ |
| **Gate 3 截断** | **不适用** ✓（无 $N_0$） | **未查** ⟹ 逐字「不得声明为进展」 ✓ |

```
⟹ 结论 ✓：**三关对「界型修复」皆不过（Gate 1／2 明确不过，Gate 3 未查） ⟹ 该路线不得计入进展** ✓；
   而**本轮自身不提交界型提案 ⟹ K2-E″ 不命中** ✓。
⚠️ 但必须记下**这条预判**：孔-2 的修复若「只给界不给秩」⟹ 立刻 NO-GAIN ⟹ **该孔的最自然修法已被 K2-E″ 预先堵死** ✓
   —— 这实际上把 §④ 的「支一」进一步压窄：**canonical 且改秩**的修复才是唯一可行分支（未找到 ✗）。
```

### ⑤-2 附带自检（硬 NO-GO ／ K1 ✓）

```
【硬 NO-GO 自检 ✓（宪法 §1）】本轮**不构造对象**（只做**分类判定** ✓）⟹ 不预先构造 ⟹ **不命中硬表** ✓。
【K1 自检 ✓（`V109` §⑥-5 逐字）】
  · K1-A（换对象型）—— 本轮**未**改称 T5／**未搬 N31 版判据**（自建 V116-C ✓）⟹ **未触发** ✓；
    ⚠️ 但**指出了**孔-2 原表述本身的换对象风险（§④-2 ✓）—— 这是**诊断他档**，非本方换对象 ✓。
  · K1-B（候选枚举型）—— 本轮**未再枚举**任何载体／候选 ✓（只在两孔**内**做分类 ✓）⟹ **未触发** ✓。
  · K1-C（循环型）—— **未**以 ζ 的解析延拓／Euler 解析规则／AFE 为构造步骤，**未**以 {ρ}／L-值为输入 ✓ ⟹ **未触发** ✓。
  · K1-D（"不可判定"即结论）—— 若 A–I 无法裁定 ⟹ 出「当前不可判定」，**如实归档不强行推进** ✓
    ⟹ **本轮正是依此取 (c)** ✓（§⑥）—— **K1-D 【已命中并被遵守】** ✓。
```

---

## §⑥ 判定（**三选一 ✓**）

```
【结果汇总 ✓（判据 V116-C 逐项）】
  · 孔-1：读法 I（Selberg 原样）＝ **无判定**（非 𝒞 成员 · 违 C2）；
          读法 II-①／II-②＝ **属五类**（显式 τ ↦ A1／A2）；
          「是否存在第三种改造」＝ **无判定**（R-D）。
  · 孔-2：对合半 ＝ **属五类**（显式 τ ↦ A1／A3，**非循环** ✓）但**不单点** ⟹ 半机制；
          content 半 ＝ **无判定**（形式未获取 ⟹ τ 不可构造；且「修复必落 A1／A2」之推论**循环依赖 u1 ⟹ 依 B-2 禁用** ✗）。
```

$$\boxed{\ \textbf{判定 ＝ (c) 未定}\ }$$

```
【为何不取 (a)】**未找到**任何「属第六类」机制 ✗ —— 但依 **C-3（宪法 §0：未找到 ≠ 不存在 ✓）**，
   「未找到」**恰恰不能**写成「不存在第六类」 ✗，**也不能**因「未找到反例」而取 (b)。
【为何不取 (b)】两孔**皆未被无循环地判为「属五类」** ✗：
   · 孔-1 的「无第三读法」未证；
   · 孔-2 的 content 半归约**必须用 u1 ⟹ 循环** ⟹ **禁用**（§②-2 B-2 ✓）。
   ⟹ **不能**宣称五类表在两孔处封闭 ✗。
【取 (c) 的依据 ✓】`V109` §⑥-5 **K1-D 逐字**：「若……仍无法裁定 ⟹ 出『当前不可判定』，**如实归档，不强行推进** ✗」
   —— 且 𝒞 本身非唯一确定（`V109` §③-4 逐字；`V115` §②-4 逐字 ✓）⟹ **未定** ✓。
```

### ⑥-1 ⭐ 本轮的结构性发现（＝「为何 (c)」的真正原因 ✓）

```
⚠️ **方向 (iii) 的两个「现成可判定判据」（CS-2／CS-3）不是独立入口** ✗：
  其对「隔离装置」的判定力 **≡ u1（𝔐₅ 穷举性）的局部版** ——
   · 用 u1 ⟹ 得出「属五类」但**循环** ✗；
   · 不用 u1 ⟹ 只能「无判定」 ✗。
⟹ **同型于 `V115` §③-4／§⑥**：那边发现「R 的完备存在性 **≡** u1（＝§⑥「空转／包装」结论）」；
   本轮发现「CS-2／CS-3 的判定力 **≡** u1（局部版）」⟹ **再次印证 V115 §⑥ 的结构结论** ✓✓
   并把它**上推一层**：**在「隔离装置」口径下，(iii) 也并不是与 (i) 平行的独立方向** ⚠️。
```

### ⑥-2 明写缺什么（**不得含糊 ✗**）

| 缺项 | 具体缺什么 | 卡住哪一步 |
|:--|:--|:--|
| **缺1 · u1** | 𝔐₅ 穷举性（`E105` §5 末／§7 逐字「唯一残余」✗） | **孔-2 content 半归约**：用 ¾循环、不用 ¾只能无判定 |
| **缺2 · 资料** | `E130` §③ 族A 的 **content 半精确形式**（本轮**未读** `E130` ✗，仅有 `V107` 转引 ✓） | τ 不可构造 ⟹ R-A 不可能 |
| **缺3 · 工具** | 「选择原理 **正规形** 定理」（`E106` §2 仅 partial；B 分支未分类，`V115` §⑧-u4 逐字 ✓） | R-A／R-B 的**可判定性本身**未立 |
| **缺4 · 裁定** | `V109` §③-4 **A／B／J**（`V115` §②-4 逐字；J＝𝔐 外延两读） | 𝒞 外延未定 ⟹ R-W 闸不可判定 |

```
【条件性路径（供唐先生抉择 ✓，不得自行替裁 ✗）】
 · 若裁定「任何单点钉扎必落 A1–A5」（＝u1 局部版）为**真** ⟹ 孔-1／孔-2 皆 **属五类** ⟹ 得 **(b)**。
   ⚠️ 但该命题**正是待证的 u1** ⟹ **不得先取 (b)** ✗（否则＝把结论当前提）。
 · 若给出 R-B 的**不可归约证书**（需 缺3 的正规形）⟹ 得 **(a)**。**当前无此证书** ✗。
⟹ **本轮不得代唐先生裁定；出 (c) 并归档** ✓（K1-D ✓）。
```

### ⑥-3 （b）若采时的**加强后表述**（**仅条件性 ＋ 不得宣称已证 ✓**）

```
“∀ I ∈ 𝒞（C1–C4∧C★，`V115` §②-3 逐字），若 M(I) 的隔离机制落**两孔之一**
 （孔-1 Selberg 类改造 ／ 孔-2 R_CS·X6 content 半），则 M(I) ∈ 𝔐₅ —— **两处已知出口封闭**。”
⚠️ **未**断言 ∀I∈𝒞 成立 ⟹ **不构成覆盖论证之证明** ✗（宪法 §0；`E105` §7 逐字「未宣布 T5 绝对死亡」✓；
   `V115` §⑧ 逐字「不得写成『覆盖论证已证』✗」✓）。
```

---

## §⑦ 与 `V109`／`V115`／`E146` 的关系（**对覆盖论证强度的增量**）

```
【对覆盖论证强度的增量 ＝ **零（未增）** ✗】—— 因为两孔**未被无循环地堵**（(c)）
  ⟹ 强度**不变** ✓；增量落在**路线图／依赖结构**，不在论证本体 ✓（诚实标注 ✓）。
```

| 档案 | 本轮与其的关系 | 增量 |
|:--|:--|:--|
| **`V109` §⑤-3** | 本轮＝方向 (iii) **首轮执行** ✓；**满足**其「须自建『单点隔离』版判据，禁搬 N31 版」要求（V116-C ✓） | ⚠️ **对 CS-2／CS-3 提独立性反驳**（判定力≡u1 局部版）⟹ **建议 T10 勘误**：CS-1（`E101` §8.4）为**唯一独立**判据，CS-2／CS-3 降为「u1 下游」✗〔本方推论〕 |
| **`V109` §⑥-5 KILL** | 逐条自检：K1-A／-B／-C **未触发** ✓；**K1-D 已触发并遵守**（出未定）✓ | 无（合规记录 ✓） |
| **`V115` §⑥／§⑦** | **确认**其「R 存在性 ≡ u1」结论 ✓；**执行**其重排序（(iii) 置首） | ⚠️ **二次修正建议**：因 (iii) 两靶需 (i) 的 normal form 才能判 ⟹ **(i) ≻ (iii) ≻ (ii)**〔本方推论〕（V115 把 (iii) 置首仅因「唯一现成可判定判据」；本轮发现该「现成」不成立） |
| **`E146` §0** | 其判 (ii) **第三侦测型只能是非自伴谱型**（＝总册 L1，已 NO-GO ✓），逐字「**已知类型穷尽 ✓，但无定理 ✗**」 | **两表正交**（侦测方式轴 vs 隔离装置轴）⟹ **合流**：已知枚举面（E146 三类侦查 ＋ 𝔐₅ 五类隔离）**两处皆「穷尽但无定理」** ⟹ **支持**「覆盖论证当前不可证」之判读 ✓ |
| **覆盖论证（中档 M，`V109` §②-2）** | — | **强度不变** ✗；但**依赖结构**被修正（(iii) 依赖 (i)）⚠️ |

```
【⚠️ L 编号口径（强制标 ✓，`V115` §⑥ 逐字）】
  上文引 **L1** 时口径＝**【总册】第四部分**（`MASTER-NOGO-AND-LIVE-PATHS.md` ✓，同 `E146` 自标「＝总册 L1」✓）；
  若引 **L1–L4 四形态**则口径＝**宪法 §4.2** —— 二者互换已由 `V108` §⑨-u8 登记 ⚠️，**本档照标、未消解** ✗。
```

---

## §⑧ 证据等级 ＋ 未证清单（诚实边界 ✓）

```
【档案级 ✓（逐字锚）】`E105` §5／§7；`E106` §2；`E101` §8.4／§8.5；`V107` §②-3-c／§④-3／§⑤-2／§⑨-u4-u5；
   `V109` §③-2／§③-4／§④-3／§⑤-3／§⑥-5；`V115` §②-3／§②-4／§③-4／§⑥／§⑦／§⑧；`MASTER` 末段（K2-E″＋三关）；`E146` §0
   —— 均**引其原文** ✓；**未逐字复核**者显式标注 ✗（见 u4 ✓）。
【结构性（非定理级）✓】**V116-C 三态判据** ＋ **孔-1／孔-2 的归约见证** ＋「CS-2／CS-3 非独立」之论断
   —— 皆**本方结构性判读** ✗，非定理级，可被新证据推翻 ✓。
【本方拟定义（明确标注 ✓）】三元组 (D,Iso,Σ) 的 **Σ（选择原理）** 、**A1–A5 → Σ 形态的映射**
   、**R-W／R-A／R-B／R-D 四规则** —— 皆**非档案原文**，为**本档为可判定化而拟** ✓；每条附锚或标为候选 ✓。
【本档自陈】**零代码** ✓｜**仅新建本档 1 份** ✓｜**未改任何既有文件** ✓｜只用**显式路径 ＋ `docs/` 内定向 `grep -n`／`sed`** ✓
   （未全盘 find／grep ✗；未触 `fn_backup／fupeng／github／quant-system／.git` ✓）｜**未用 RH** ✓｜**未预设 RH** ✗｜**未跑 Lean** ✓｜**零计算** ✓。
```

── 未证清单 ──

```
u1 ⚠️ **𝔐₅ 的穷举性未证** ✗（`E105` §5 末／§7 逐字；`CLOSED-ROUTES-MAP` §E.4；`E102` §7）
   —— 本轮**未推进**；且**发现**孔-2 的归约**必需**其**局部版** ⟹ 新登记“u1 的局部版是 (iii) 的隐形前提” ⚠️。
u2 ⚠️ **V116-C 判据未获唐先生批准** ✗（〔本方拟定义〕；`V109` §⑤-3 只要求“自建”，未要求批准 ✓）。
u3 ⚠️ **孔-1 的“改造能否成立”＝未证＋未反驳** ✗（承 `V107` §⑨-u4 逐字 ✓）—— 本轮**未推进其本身**，只判其**产物口径** ✓。
u4 ⚠️ **孔-2 content 半的精确形式未获取** ✗ —— 本轮**未逐字读** `E130` §③ 族A（仅经 `V107` 转引 ✓）⟹ τ 不可构造（**资料缺口，非结论** ✓）。
u5 ⚠️ **“CS-2／CS-3 非独立（判定力≡u1 局部版）”为〔本方推论〕** ✗ —— 非定理；可被“显式非循环归约”推翻 ✓。
u6 ⚠️ **`V109` §③-4 A／B／J 未裁定** ✗（同 `V115` §②-4 ✓）⟹ 𝒞 外延两读皆不可用 ⟹ R-W 闸不可判 ✓。
u7 ⚠️ **引 L 编号口径**（宪法 §4.2 vs 【总册】第四部分）**已互换、未消解** ✗（照标 ✓；同 `V108` §⑨-u8／`V115` §⑧-u7 ✓）。
u8 ⚠️ **`E146` 仅取 §0 标题级**（未读其 §1–§4 全文 ✗）⟹ “两表正交”为**结构判读**，非同角度定理级对比 ✓。
```

---

## 附：提交链与指针

- 领号脚本：`bash scripts/id_claim.sh audit sixth-class-counterexample-search` ⟹ **V116**
- 上游：`docs/V109-coverage-argument-charter.md` §⑤-3（方向 iii）｜`docs/V115-representation-contraction-r1r2r3.md` §⑥／§⑦｜`docs/V107-N31-formalization.md` §⑤-2（孔-1／孔-2）
- 判据锚：`docs/E105-relational-limit-necessity.md` §5（五类）／§7（第六种）｜`docs/E106-CACC-sharpening.md` §2（normal form）｜`docs/E101-purity-coverage-audit.md` §8.4（二择一）｜`docs/MASTER-NOGO-AND-LIVE-PATHS.md` 末段（K2-E″＋三关）
- 下游待办（本轮产出）：⚠️ ① 请唐先生裁定 `V109` §③-4 A／B／J；② 若维持方向 (iii)，须**先建“选择原理正规形”**（＝接 (i)）；③ 建议 T10 勘误 `V109` §⑤-3 的 CS-2／CS-3 定位。

