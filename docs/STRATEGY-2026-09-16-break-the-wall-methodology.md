# STRATEGY-2026-09-16 · **破墙方法论：先破哪道、怎么破**（索引级策略档；不占 V 编号）

> 前提 ✓ 唐先生 2026-09-16 12:12：**"1（攻承重墙本体），为了突破 RH，墙必须破。但是怎么破，先破哪道墙，需要方法论，需要技术手段，需要策略。"** ✓
> 纪律 ✓ 目标＝**RH 的理解与突破**；论文＝副产品；**不得**把"可发表"当进展度量（锚页四要）✓

---

## §1 墙的清单与**依赖图**（先破哪道：答案唯一）

$$\text{承重墙（`V181` 交回）}\ ＝\ \text{T}\log\text{T}\ ＋\ \text{Weil 正性};\qquad \text{核心缺口}\ ＝\ \textbf{局部算术结构}\ \not\Longrightarrow\ \textbf{全球谱定位} ✓$$
$$\text{与之并行的三道"墙"}：\quad \text{① 值面墙（`V283`）}\quad \text{② 阿基米德泄漏（`V172` §5a）}\quad \text{③ 正性循环（`POS1`／`POS2`）} ✓$$

$$\boxed{\textbf{本档第一个结论}：\text{这四者}\ \textbf{不是四道独立的墙}，\text{而是}\ \textbf{同一道墙的三面}} ✓✓✓$$
$$\qquad \text{依据（今日链条整合）}：\text{"局部}\not\Longrightarrow\text{全球"之所以成立，}\textbf{是因为} \text{唯一已知的桥}\ ＝\ \text{显式公式};$$
$$\qquad \qquad \text{而显式公式}\ ＝\ \textbf{值面}（`V283` §1：argument principle ⟹ 全素数求和＋archimedean）;\ \text{其中的 archimedean 部分}\ ＝\ \textbf{A-leak}（`V172` §5a）;$$
$$\qquad \qquad \text{若改走"正性"路线，则}\ \text{Weil 正性}\ \textbf{等价于 RH}（`POS1`／`POS2`）＝ \text{循环} ✓$$
$$\Longrightarrow \boxed{\text{故"先破哪道"}\ \textbf{不是选择题}：\text{要破的是}\ \textbf{"桥"这一环} \text{本身}} ✓✓✓\ \text{（其余三面随之解除）}$$

---

## §2 "桥"的精确形态与**已知失败模式**（＝破墙必须绕开的四条封锁线）

$$\text{桥}\：＝\ \text{把}\ \textbf{局部／算术结构} \text{转成}\ \textbf{全球谱信息} \text{的映射}\ \Phi ✓$$
$$\text{四种已封的实现（今日所得，}\textbf{直接作为排除判据}）：$$
$$\qquad \text{(E1) }\Phi\ \text{连续或可计算} \Longrightarrow \textbf{cylinder} ⟹ \text{落 C0（无分离）}\（`V277`-A）✓$$
$$\qquad \text{(E2) }\Phi\ \text{经过零计数／}\beta \Longrightarrow \textbf{本性属值面}\（`V283` §1）✓$$
$$\qquad \text{(E3) }\Phi\ \text{非圆柱} \Longrightarrow \text{不连续} \Longrightarrow \textbf{不可计算} ⟹ \text{成分归}\ `V211`\ \text{八类（全封）}\（`V277`-B）✓$$
$$\qquad \text{(E4) }\Phi\ \text{为聚合／上同调型} ⟹ \text{落}\ `V234`\text{–}`V236`／`V193`\text{–}`V198` ✓$$
$$\Longrightarrow \text{破墙}\ ＝\ \text{找一个}\ \Phi\ \textbf{同时避开 E1–E4};\ \text{这与}\ `V211`\ \text{§5 的"UNINSTANTIATED"}\ \textbf{是同一对象} ✓✓$$

---

## §3 ⭐ 策略：**三轨并行**（先做什么、各轨产物、为何这样排）

### Track I（近战·**先做**）：审计"我们自己量出的墙"含不含可移假设
$$\text{对象}：\text{比例天花板}\ 0.682\ \big|\ \textbf{T}^2\ \text{律／预算越界}\ \big|\ \text{第三矩／高相关需 support}>1\（`V102`／`V162`／`V217b`）✓$$
$$\qquad \text{问法}：\text{这些}\ \textbf{是定理} \text{还是}\ \textbf{方法的产物}？\ \text{逐一检查其推导的隐含假设（是否可移）}✓✓$$
$$\qquad \text{两种结果都有价值}：\text{① 可移} ⟹ \textbf{预算墙变矮}（新空间）;\quad \text{② 不可移} ⟹ \textbf{升格为定理}（＝理解，且永久封死一条捷径）✓✓$$
$$\qquad \text{为何先做}：\text{它是}\ \textbf{有限、可核验、不预设 GRH／反例} \text{的；且它正是墙的}\ \textbf{定量刻度} ✓✓$$
$$\qquad ⚠️\ \text{纪律}：\text{这是}\ \textbf{审计我们自己的推导}（"结论太顺时先怀疑自己"）；\text{不是常数优化}（≠ 被否的"预算优化"）✓$$

### Track II（主攻·**并行**）：把"桥"**分类化**（＝把 NO-GO 地图升格为定理）
$$\text{动作}：\text{把}\ \Phi\ \text{公理化（有限读／连续性或可计算性／非上同调／独立性闸门／不引零点），然后}：$$
$$\qquad \text{(i) 证}\ \textbf{"任何桥必经值面"} \Longrightarrow \text{完备性} \text{（＝"为什么难"的定理）};\qquad \text{(ii) 或找到}\ \textbf{逃逸者} ⟹ \textbf{突破种子} ✓✓✓$$
$$\qquad \text{为何值得}：\text{① 把开放搜索变成}\ \textbf{有确定答案的分类问题};\ \text{② 两种结果都不是"小论文"};\ \text{③ 不预设 GRH／反例}\（满足硬条件）✓✓$$
$$\qquad \text{与}\ `§E.4`\ \text{的关系}：\text{这是}\ `§E.4`\ \text{的}\ \textbf{定理化} \text{（此前只到"类表是否完整"的}\ \text{元问题层}）✓$$

### Track III（长期·机会主义·**只在 Track II 出现逃逸迹象时投入**）：非阿基米德正性载体
$$\text{想法}：\text{所有"韵脚"（char-}p\ \text{Weil／Hodge 指标；Selberg 曲面}\ \lambda_1）\ \text{的}\ \beta\text{-刚性}\ \textbf{全来自}\ \text{代数对应＋正定配对（自伴性）} ✓$$
$$\qquad \text{char 0 的障碍}（`AOB4`）\：\text{无典范 similitude 元素；极化}\perp\text{元素性；}\ \text{Buium}\ \delta\text{-几何}\ ＝\ `G10`\ \text{已封} ✓$$
$$\qquad \text{唯一还开着的形态}：\textbf{保住"配对"而放弃"元素性"} \text{—— 即用一个}\ \textbf{无典范平方根的正性} \text{（＝`V247`／`V248` 的第三型正性）} ⚠️✓$$

---

## §4 技术与判据工具箱（今日已建，可直接用于三轨）

| 工具 | 用处 |
|:--|:--|
| `V277`-A／B | 判定候选桥是否"连续／可计算"（是 ⟹ 立即 cylinder ⟹ 不必再审）|
| `V283` §1 | 判定候选桥是否"经零计数"（是 ⟹ 值面）|
| `V286`-A（REF） | 单向驳倒：某候选若含已知 off-line 数据 ⟹ 立即死 |
| `V279` 六类商 | 判定候选是否落已审计的生成方式 |
| 三闸门（方向／秩／截断） | 负结果必须三闸全过，否则不算进展 |
| `E30`-2 突破判据 ＋ T²／log 律 | **任何"突破"主张的先验检验** |
| `N1`–`N13` 防升级／防漂移清单 | 防止把局部失败升成不可能、把台账二手描述当事实 |

---

## §5 验收与止损（**什么算破墙** / **何时收手**）

$$\textbf{算破墙}：\text{产出}\ \Phi\ \textbf{同时避开 E1–E4} \text{且过}\ \textbf{独立性闸门} \text{与突破判据};\ \text{并至少给出}\ \text{一条}\ \textbf{可核验的定量推论} ✓✓$$
$$\textbf{早停信号}：\text{① 候选逐一落入 E1–E4;}\ \text{② 需引入 GRH／off-line 反例作输入;}\ \text{③ 只能给"常数改进";}\ \text{④ 只能给"等价重述"（③类）} ✓$$
$$\qquad ⟹ \text{出现①–④} \Longrightarrow \text{转入 Track II 的}\ \textbf{完备性证明} \text{（＝把失败变成定理）} ✓✓$$
$$\qquad ⚠️\ \text{允许的负结果}：\text{"在此公理集下任何桥必经值面"}\ \text{—— 这是}\ \textbf{理解}，\text{符合锚页①} ✓$$

---

## §6 **不做的事**（防漂移清单）

```
① 不做常数优化（0.682／T²／第三矩 的"提高精度"——除非 Track I 发现假设可移）✗
② 不写"小论文"作为目标（论文＝副产品；只在真懂了时写）✗
③ 不用 GRH／off-line 反例作输入（硬条件 (H1)(H2)）✗
④ 不把"已审计族中失败"升成"不可能"；不把"未找到"升成"不存在"（N1／N2）✗
⑤ 不依赖 terse 台账行（N13：须核对已验证原文的档）✗
⑥ 不以"又关掉一条路"为战绩（锚页①：要理解，不要战绩）✗
```

---

## §7 立即可执行的五步（下一步）

```
第 1 步｜Track I 启动：取 `V102`／`V162` 的 T² 律与 0.682 天花板，逐行审计其**隐含假设**
        （目标：判定"可移"还是"定理"）✓
第 2 步｜把 E1–E4 四条封锁线**写成统一公理集** B1–B5（"桥公理"）✓
第 3 步｜对存量候选（Arakelov 高度／δ-几何／MZV-周期／F_1-位形／第三型正性）**逐条对表 E1–E4**，
        记录"落哪条"，并标出唯一未落者（若有）✓
第 4 步｜对唯一未落者做**独立性闸门 + 突破判据**双检；通过 ⟹ 全力攻；不过 ⟹ 记 ④ ✓
第 5 步｜若第 3 步无未落者：正式开 Track II 的完备性证明（＝把 NO-GO 地图定理化）✓
```
