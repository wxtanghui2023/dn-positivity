已查地图：未覆盖（关键词: 分母独立性|路径无关|异常零点审计|碰撞稳定坐标）—— 可开档，首行须照抄本行

# **C-380-L3-1D**：分母的四门证书（**同号 ＋ `R_r(x) ≠ 0` ＋ 非零 `Φ` 尺度**）

**唐先生令（2026-09-22 22:16）**：
① ✅ **`C380-L3-1` → CLOSED** ✓，四条锁定（照录）：
　**(1)** 域内非零**只做点态见证**，**不升级为一致非零**（`F_1(X) = Σ_j |c_j| > 0` 只证存在，**不能排除某些符号向量使 `F_1 = 0`**）✓；
　**(2)** 奇层**确实是度-1 符号字符**（`F_{2r+1} = Σ_j ε_j w_{r,j}`）⟹ **二次符号对象没有奇部** ⟹ **`degree 2 ⇏ 新的 odd information`** ⟹
　　　**平方、两两乘积整族从 L3-2 候选中直接删掉** ✓✓；
　**(3)** ⭐ **碰撞处的阶数分裂是最有价值产出**：`ε_i = ε_j ⟹ ν_N = 0`；`ε_i = −ε_j ⟹ ν_N = 1` ⟹
　　　**反号字符的排除具有结构依据**（分母若随同一 `δ` 消失 ⟹ 落入已明禁的路径依赖型 `0/0` 稳定假象）✓✓；
　**(4)** ⟹ **L3-2 入口严格收缩为**：
　　$$\boxed{\text{同号碰撞字符} + \text{非零 } \Phi\text{-型尺度}}$$ 而**不再**枚举全部 `2^5` 个符号字符 ✓✓
② **L3-1D ＝ 分母的严格四门证书**（照录表 ✓）：
　**D1** `D_Φ` 在整个候选域**有限** ⟹ *finite*｜**D2** `D_Φ ≢ const` ⟹ *nonconstant*｜
　**D3** 同号碰撞 `δ → 0` 时 `D_Φ → D_0 ≠ 0` ⟹ *collision-stable*｜
　**D4** 任意碰撞路径、任意高阶趋近方式均给**同一极限** ⟹ *path-independent* ✓
　（⭐ **D3 是硬门，D4 是真正的封口门** ✓；⛔ **不能只验证比值存在，必须先验证 `D_Φ(0) ≠ 0`** ——
　 `N = aδ + …, D = cδ + …` 虽给 `N/D → a/c`，但它**已属本档明确排除的"共同消失阶"结构** ✗✓）
　⭐ 若严格得到 `N(δ) = N_0 + O(δ)`、`D_Φ(δ) = D_0 + O(δ)`、`N_0, D_0 ≠ 0` ⟹ **才真正获得 L3-2 所需的碰撞稳定坐标** ✓✓
③ ⚠️ **必须保留的边界例外**：`R_r(x) = 0` 时**同号亦可能降阶** ⟹ 正式命题写成
　$$\boxed{\varepsilon_i = \varepsilon_j,\ R_r(x) \neq 0 \Longrightarrow \nu_N = 0}$$ 而
　$$\boxed{R_r(x) = 0\ \text{单独列入 exceptional-zero audit}}$$ ✓✓
　（否则下一档若把"同号"当**充分条件**，容易把**真正的零点退化**重新伪装成碰撞效应 ✗✓）

D0: 本档对象 = **L3-1D 分母四门证书 ＋ 异常支线分离**（档案**零覆盖** ⟹ 新档；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算（解析）／零数值／未写程序**／未碰 RH／未检索文献／未做数值搜索 ✓

---

## §1 入口与候选（依令 ① (4) ✓）

```
【入口】 `同号碰撞字符` ＋ `非零 Φ-型尺度`（不枚举 `2^5` ✓）
【numerator】 `N := F_{2r+1}`（度-1 字符 ✓），且取**同号**：`ε_i = ε_j`（`(i,j)` 为碰撞对 ✓）
【denominator 候选】 `D_Φ := w_{r,i} = √(x_i)·R_r(x_i)`（**与 numerator 同 `r`、取碰撞对之一** ✓）
　· `D_Φ` 只依赖 `{x_j}`（**sign-invariant** ⟹ **被 `Φ` 固定** ✓✓）
　· 备选（同族）：`w_{r,i} + w_{r,j}`、`√(x_i)R_r(x_i)·√(x_j)R_r(x_j)` 等对称化版本 ✓
```

## §2 四门证书（逐门判定 ✓✓）

| 门 | 必须证明 | 本档判定 |
|:--|:--|:--|
| **D1 有限** | `D_Φ` 在候选域有限 | ✅ **成立**：`x_j ∈ (0,1]`、`R_r` 连续 ⟹ `w_{r,i}` 有限 ✓（⚠️ **零节点** `x_i = 0` 除外 ⟹ 记入异常支线 ② ✓） |
| **D2 非常数** | `D_Φ ≢ const` | ✅ **成立**：`w_{r,i} = √x·R_r(x)` 随构型变化 ✓（⚠️ **在单个纤维上它是常数** —— 这正是它**被 `Φ` 固定**的体现 ✓，故 D2 应读作"**在整个域上不恒为常数**" ✓） |
| **D3 碰撞稳定（硬门）** | 同号碰撞 `D_Φ → D_0 ≠ 0` | ✅ **成立（在主支线内）**：`D_Φ = g(x_i) → g(x) = √x·R_r(x) ≠ 0`，**前提 `R_r(x) ≠ 0`** ✓✓（**不取 gap ⟹ 无 `0/0`** ✓） |
| **D4 路径无关（封口门）** | 任意路径、任意高阶趋近给同一极限 | ✅ **成立**：`D_Φ` **仅依赖 `x_i`** 且连续 ⟹ 任何路径（含高阶趋近）给同一极限 `g(x)` ✓✓ |

```
【四门结论】 ⟹ **在非同异常支线内，四门全过** ⟹ **获得 L3-2 所需的碰撞稳定坐标** ✓✓
【关键对照】 ⛔ 若把 `D` 取作 **gap**（`|x_i − x_j|`）：`D3` **必败**（gap 在碰撞处为零 ⟹ `0/0` ⟹ 路径依赖 ✗）✓✓
```

## §3 **三条支线严格分离**（依令 ③；异常**不得混进主证明** ✓✓）

```
【主支 M】 `ε_i = ε_j` ＋ **`R_r(x) ≠ 0`** ⟹ `ν_N = 0`；四门全过 ✓（正式命题之一半 ✓）
【异常支线 E1 · exceptional-zero audit】 **`R_r(x) = 0`**（`R_r` 在 `(0,1)` 内的根，低阶表已给 ✓）：
　⟹ 同号亦**降阶**（`g(x) = 0` ⟹ `ν_N ≥ 1`）⟹ ⛔ **不得被"同号 ⟹ `ν_N = 0`"的简写吞掉** ✓✓
　⟹ 单列审计：在 `R_r` 的根处，判定 `ν_N` 的真实阶数与分母退化的相互作用 ✓
【异常支线 E2 · 总抵消（本档新增精度 ⚠️，属我的分析）】 同号时**碰撞对**贡献 `2g(x)`（阶 0 ✓），
　但**其余节点**的贡献 `Σ_{k≠i,j} ε_k g(x_k)` **可能恰好抵消**（`g(x) ≠ 0` 不足以排除）⟹ **`N(0) = 0` 的"意外抵消"** ⟹
　⛔ 建议同样**单列**（`accidental-cancellation audit`），**不**并入主支 ✓✓
【统一纪律】 支线 M／E1／E2 **各自独立判定**；**不得**用主支结论覆盖异常支线 ✓✓
```

## §4 下一步（**L3-2**）与本节**未**证之事

```
【本档只证 D 门】 ⚠️ **未**证二阶稳定（`Ψ(X_δ) = Ψ(X_0) + O(δ²)`）—— 那是 **L3-2** 的活 ✓
【L3-2 待做（在已得的碰撞稳定坐标上）】 ① 有限 ② 非恒定 ③ 近碰撞稳定 ④ **二阶稳定**（＋路径无关复核 ✓）
　⛔ 仍需防"比值有限但 `Ψ(X_0)` 依赖路径"的伪稳定（令 ① 已明禁 ✓）
【出口语义】 四门过 ⟹ **碰撞稳定坐标已建立**（L3-2 的前提 ✓）；二阶稳定过 ⟹ 才登记 **KERNEL-HUNT witness** ✓
```

## §5 边界与回查（✗✓）

```
✗ 零计算（解析）／零数值／未写程序／**未碰 RH／未检索文献／未做数值搜索**／**未开案** ✓
✗ ⛔ 不把"同号"当**充分条件**（须 `R_r(x) ≠ 0` ＋ 无意外抵消 ✓）；⛔ 不预设 denominator；⛔ 不把比值存在当稳定 ✓
⚠️ E2（总抵消）标为**本档新增分析**（非档案结论 ✓）；主支依赖 `R_r` 低阶表（**档级引用** ✓）
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 碰撞稳定坐标 命中文件数=0 ⟹ **本档新增** ✓
　技术词 异常零点审计 命中文件数=0 ⟹ **本档新增** ✓
　技术词 分母独立性   命中文件数=0 ⟹ **本档新增** ✓
　技术词 四门证书     命中文件数=2 ⟹ **档案已有（异义）⟹ 不主张** ✓
　技术词 路径无关     命中文件数=5 ⟹ **档案已有（异义）⟹ 不主张** ✓
【地图回查】 `分母独立性|路径无关|异常零点审计|碰撞稳定坐标` = **未覆盖** ⟹ 可开档 ✓
【档级引用】 `C-347` §1／§2（奇偶层分离；`R_0..R_3` 低阶表）✓｜`C380-FINAL-STAGE-REPORT`（`E_even ⊆ {k=5}` 无碰撞）✓｜
　`C380-L3-1`（ν_N 分裂 ＋ 反号字符出局 ＋ 新筛子）✓
