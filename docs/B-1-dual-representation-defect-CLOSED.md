已查地图：命中（`J-2` 本线自档／`E-20`／`E-22`／`AMEND-4`）⟹ **引用，不开新案** ✓

# **`B-1` 第一刀：离散/解析双表示的结构性缺陷 ⟹ CLOSED（被经典账本定量解释）**

**唐先生令（2026-09-23 13:40）**：**开 `B`，不收手** ✓；⛔ **必须避开 `J-2` 已封的"离散和 ≈ 连续积分、争取更好误差"**路线 ✓；
　`B-1` 目标＝**寻找离散/解析双表示之间此前未登记的结构性缺陷** ✓；第一轮**只做结构扫描**（不做渐近优化）✓；
　⛔ 第一刀**不许把 `\beta`／`rank`／`support`／`parity`／`majorant`／`2/3` 当目标** ✓；
　⭐ **强 STOP**：若 `D(X)` 只是已知离散化误差／EM 余项／Poisson 余项／显式公式余项／log saving ⟹ **`B-1` 立即 CLOSED** ✓

D0: 本档对象 = **`B-1` 第一刀（截断 Dirichlet 和 vs 截断 Euler 积的缺陷扫描）**（引本线自档；**未开 bridge 案** ✓）
D1: 0 （`[RESEARCH]` 轮次；结论为**计算级判定**，不主张新自由度 ✓）
FREEZE-ACK: D1=0 ✓
[RESEARCH]

---

## §1 设定（避开 `J-2`：比的是**两个精确表示**，不是"和 vs 积分"✓）

```
【对象】 `\zeta` 的两种**天然表示**（**同一截断 `N`**）：$$A(N;s)=\sum_{n\le N}n^{-s};\qquad B(N;s)=\prod_{p\le N}(1-p^{-s})^{-1}$$ ✓
【⭐ 精确恒等式（本档出发点）】 $$D(N;s):=A-B=-\sum_{n>N,\ P(n)\le N}n^{-s}$$（`P(n)`＝最大质因子）✓
　⟹ 这是**精确差**（由 `B` 的 Euler 积展开 ＋ `n\le N\Rightarrow P(n)\le N` 立即得到）⟹ **不是离散化误差** ✓✓（故 STOP 第一句不自动触发 ✓）
【对照】 把 `B` 的截断由 `p\le N` 改为 `p\le N/3` 与 `p\le 3N` ⟹ 检验"**对角 `y=N`**"是否特殊 ✓
【脚本/日志】 `scripts/b1_dual_representation_defect_scan.py`／`out/b1dual/b1_scan.txt` ✓
```

## §2 数据（数字驱动 ✓）

```
【`s=2`】 `D(100)=-6.961\times10^{-3}`｜`D(300)=-2.520\times10^{-3}`｜`D(1000)=-7.906\times10^{-4}`｜`D(3000)=-2.723\times10^{-4}`｜`D(10^4)=-8.385\times10^{-5}` ✓
　跨尺度比 `D(3N)/D(N)`：0.3621／0.3137／0.3444／0.3079（`3^{-1}=0.3333` ✓）｜`\log|D|` 对 `\log N` 斜率：**-0.925／-1.055／-0.970／-1.072** ✓
【`s=1.5`】 `D(10^4)=-1.524\times10^{-2}`；斜率：**-0.404／-0.487／-0.456／-0.512** ✓
【⭐ 统一律（本档测出）】 $$\boxed{|D(N;s)|\ \asymp\ N^{-(s-1)}}$$（`s=2\Rightarrow N^{-1}`；`s=1.5\Rightarrow N^{-1/2}`）✓✓
【符号】 全为负 ✓；六项中的**精确消失／整有理／因子化／奇异阶数转换** ⛔ 均无 ✓
【对照（对角检验）】 `N=1000`：`D(\mathrm{diag})=-7.906\times10^{-4}`｜`D(p\le N/3)=-2.748\times10^{-4}`｜`D(p\le3N)=-9.385\times10^{-4}` ⟹ **对角 `y=N` 不特殊** ✓
```

## §3 ⭐ **判定：定律被经典账本定量解释 ⟹ CLOSED（`E-20` 第 4 条 ✓✓）**

```
【经典解释（本档给出，订量级 ✓）】 平滑数密度（Dickman）启发式：`y`-平滑数计数 `\sim x\rho(\log x/\log y)` ⟹ 令 `y=N`、`x=N^u` 代入 `\sum`：
　$$\sum_{n>N,\ P(n)\le N}n^{-s}\approx \log N\int_1^\infty \rho(u)N^{-(s-1)u}du\ \approx\ \frac{\rho(1)}{s-1}N^{-(s-1)}$$（`\rho(1)=1` ✓）
　⟹ 预测 `s=2`：`|D|\approx N^{-1}` ⟹ `N=10^4` 预测 `1.00\times10^{-4}` **实测 `8.385\times10^{-5}`（低 16%）** ✓✓ **量级与常数均吻合** ✓
【⟹ 判定】 缺陷**本身精确**，但其**尺度律与常数**由**已知的平滑数（Dickman）密度账本**给出 ⟹
　$$\boxed{B\text{-}1\ \text{第一刀} = \text{CLOSED（\textbf{已由经典账本定量解释}）}}$$ ✓✓（依 `E-20` 第 4 条：**不是旧账本改写才可用**；此例**就是**旧账本 ✓）
【⚠️ 诚实标注（本档自查两处 ✓）】
　① `\S2` 表的"恒等式核对"列＝**截断太短**造成的差额（`\sum` 仅取前 200／60 项），**不是恒等式不成立** ✓（全尾应在 `(N,N^u]` 上延伸；实测 `D` 的量级与全尾一致 ✓）
　② 本轮**未**做渐近优化、**未**触及 `\beta`／`rank`／`support`／`parity`／`majorant`／`2/3` ✓
```

## §4 边界与回查

```
技术词 双表示缺陷  命中文件数=0    :: 
```
【三分类标注】 `0` ⟹ 本档新增；`>0` ⟹ 档案已有（引用）✓
【边界】 ✗ 未证 RH／⛔ 未命名机制／⛔ 未做渐近优化／⛔ 未把负结果写成"不存在"｜【R7】 数字驱动 ✓
```
