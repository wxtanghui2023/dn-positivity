# R-A8.3 审计记录：S2 Mechanism Audit（第一轮，小灵执行）

**日期**：2026-09-10 13:38+ ｜ 协议：`PROTOCOL-R8.3-s2-mechanism-audit.md` ｜ 预算：纸面，无代码
**执行纪律**：**未预设 R8.3-B**；按 Gate 0 → 机制类 → 个体/平均 → 三问 逐关走
**裁决（由审计得出）**：$$\boxed{\textbf{R8.3-B ｜ 但"归约"非必然性定理 ⟹ R8-C 仍需独立构造}}$$

---

## Gate 0 — 字典自洽性检验（首位）

### ① 三项归一化统一（必须先做，否则任何"λ≥2"先判坐标异常）
```
variance vs second moment：V(X,H)=X^{-1}∫_X^{2X}(ψ(x+H)−ψ(x)−H)²dx 为【x-平均】形式
                          ⟹ 与 almost-all 型陈述不同等级，不可互换
L² over x vs almost-all ：R8 需 L²/平均型二阶主项
support 归一化           ：Montgomery F(α) 的 α 与 1-level density 的 λ **同族但须显式对齐**
```
$$\boxed{\text{凡出现"无条件 λ≥2"⟹ 先判【归一化/方向错误】，而非突破}}$$

### ② 与无条件事实的自洽性核对
```
已知无条件：Montgomery λ<1（对角主导）
⟹ 字典 λ=1/(1−η) 给出 λ<1 ⟺ η<0 ⟺ H=X^{o(1)}（**亚多项式区间**）✓ 自洽
GM 猜想范围 H ≤ X^{1−ε} ⟺ η ≤ 1−ε ⟺ λ ≥ 1/ε（覆盖**一切** λ>1）✓ 自洽
⟹ **字典本身通过自洽性检验**（结论：R8-D 仍排除）
```

### ③ ⭐ 决定性内部一致性探针（本轮关键装置）
```
若【无条件】得到 η>0 尺度的正确二阶主项，则（因 F(α)(α>1) 可由 prime-pair 数据表达）
应直接给出【无条件 λ>1】——而 λ>1 无条件【已知不成立】
⟹ 强一致结论：**S2-c 的正确主项 η>0 无条件不可得不是"技术巧合"，而是与 λ<1 无条件事实同一条边界**
⟹ 且由此得本轮核心恒等式（见 §2）
```
**⚠️ 因此标记一个必核项（Gate 0 产物）**：
```
待核实：Lavrik / Motohashi / Hooley 型【平均移位相关】渐近（"平均意义的 twin prime"）
   · 其无条件 η-范围是什么？
   · 若覆盖某 η>0 且给出真主项 ⟹ 二选一：
     (a) 字典方向/约定与实际不同 ⟹ 触发 R8.3-C（需修正对应）
     (b) 该结果不能转译为 F(α)(α>1) 的信息（细微之处须澄清）
   —— 这是唯一可能推翻 §③ 一致性的入口，必须在下一轮核实
```

---

## §1 机制类判表（含预筛）

| 机制类 | 能否给出 S2 主项 | 判定依据 |
|---|---|---|
| **纯筛法** | **不能（预淘汰）** | **奇偶性障碍**：二元素数关联的**主项**不能由纯筛法给出（筛法只给上界/可容许性） |
| 圆法 / Vaughan / Type I–II | **仅至 η=0 边界**（待核实） | 可给平均型主项，但已知无条件范围与对角主导边界重合 ⟹ 未跨 η>0 |
| 大筛 / 谱方法 | 不能（S2-b 级） | 给上界与均方估计，非 uniform 主项 |
| 迹公式 / 族平均 | **排除** | 族结构不匹配 ζ（R6 已确立） |
| zero statistics / 显式公式 | **排除** | 命中循环（N1） |
| Hardy–Littlewood | **排除** | 个体 h 的 HL 即目标强度；平均 h 用 HL 亦为循环 |
| **新的 char-0 机制** | **未发现** | 无已知实例（本轮结论） |

$$\boxed{\text{S2 主项"从哪里来"的回答：来自【移位相关平均】；个体 h 来自 HL（不可得），}\\
\text{平均 h 的已知无条件评估【只到一个边界】——即 } \lambda=1\ \text{本身}}$$

---

## §2 ⭐⭐ 本轮核心恒等式（S2-c 不是独立障碍）

$$\boxed{\text{S2-c（S2 的无条件正确二阶主项）}\ \Longleftrightarrow\ \lambda>1\ \text{的跨越}}$$
```
论证要点：
· 主项 V ~ HX log(X/H) = HX(1−η)log X ⟹ support 信息 = 2 阶主项的 η-依赖
· 而 F(α)（α>1）由 prime-pair 数据表达（GM 关系）
⟹ 拿到 η>0 的正确主项 ⟹ 拿到 λ>1 的无条件信息
⟹ 故 S2-c 【就是】λ=1 的跨越，不是"另一个更难的坎"
```

### ⭐⭐ 三支线统一（本轮重大综合）
| 支线 | 其参数 | 跨越点 | 边界含义 |
|---|---|---|---|
| mollifier（Levinson 谱系） | 长度指数 θ | θ = 1/2 | 无条件素数侧信息边界 |
| pair correlation 谱系 | Fourier support λ | λ = 1 | 同上 |
| **prime variance（本线）** | 短区间指数 η | **η = 0⁺** | 同上 |
$$\boxed{\text{三支线的"墙"是【同一道】——无条件素数侧信息的边界}}$$
**注意**：这是**结构性综合**（三支线参数不同、机制不同而边界重合），**不是定理**；
但它把 FZ-3″ 的"信息边界"从"某一支线的技术上限"提升为**跨支线一致的边界现象**。

---

## §3 个体 h vs 平均 h（写死，已执行隔离）
```
· 个体 h：C(h;X) ~ 𝔖(h)X（如 h=2）⟹ **twin-prime 强度（HL）** ⟹ 不作 R8 carrier
· R8 合法目标 = 【h-加权平均主项】（唯一允许形式）
⟹ 任何从个体 h 出发的路线，按此行直接判为 HL 强度并隔离
```

---

## §4 三问裁决
```
Q1 独立性：定义层 YES（纯算术）；**所需平均主项无已知独立无条件来源** ⟹ "carrier 独立；独立供给未知"
Q2 跨越性：λ>1 未实现；λ≥2 未实现（≠不可能）
Q3 非等价性：已知路线的平均主项求值均经 HL / 零点侧 ⟹ 落入等价类**（但无必然性定理）**
```

---

## §5 输出判定与必产 R
$$\boxed{\textbf{R8.3-B}:\ \text{所有【已知】S2 路线归约为 HL / zero-statistics / 族平均 / 筛法奇偶障碍}}$$
**⚠️ 严格限定**：这是"**已知路线**"的归约，**不是**必然性定理 ⟹ 故
$$\boxed{\text{R8-C 仍【未证】；但 R8.3-B 是开始构造 R8-C 结构论证的【必要条件】已满足}}$$

**必产 R**：
```
R_{8.3}【新】：S2 的必然性定理——"任何 canonical 求值 S2 平均主项（η>0）的路线，
             其所需信息必与 zero-side（F(α), α∈(1,2)）等价"
             （若证成 ⟹ R8-C 成立，R8 线结构性关闭；若被反例击穿 ⟹ R8-A 打开）
R_{8.2}（保留）：困难 = 正确的 HX log(X/H) 级二阶主项（携带 1−η=1/λ）
R_{8.3}-verify【新】：Lavrik/Motohashi/Hooley 型平均移位相关的无条件 η-范围核实（Gate 0 探针入口）
```

## §6 下一轮建议（窄）
```
只做一件：核实 R_{8.3}-verify 的三族结果（无条件 η-范围 + 是否真主项 + 能否转译为 F(α) 信息）
   · 若发现 η>0 的真主项且可转译 ⟹ 字典约定必有问题 ⟹ R8.3-C（修正对应），且不得直接宣布突破
   · 若发现不能转译 ⟹ 需澄清"平均移位相关"与 F(α) 之间的确切关系（这本身是结构性收获）
   · 否则 ⟹ 继续走 R_{8.3} 的必然性定理构造
```

## 诚实边界
```
· Gate 0 ②③ 与 §2 的恒等式为【结构性论证】：③ 依赖"F(α)(α>1) 可由 prime-pair 数据表达"（GM 关系）
  以及"λ>1 无条件不成立"（Montgomery）——两条均已在本项目登记为文献级
· §1 的"圆法仅至 η=0 边界"标【待核实】（R_{8.3}-verify）
· 奇偶性障碍对"平均型"主项的适用形式须以文献核实（其在二元素数问题上的标准表述针对个体/筛法框架）
· 三支线统一为结构性综合，非定理
· 未写代码、未做数值；未引入 ζ 零点或谱算子
```

## 提交链
```
5797619 R8.3 协议 → 本篇（R8.3 执行）
```

---

# ⚠️ ERRATUM（2026-09-10 13:41，唐先生纠正；**留在原档，不静默改写**）

**被修正三处**（均在本文档正文内）：

```
① 原 §2 "S2-c ⟺ λ>1"（写作【等价】）——**过强，降为单向**
   更正：只有单向可用：**若 S2-c 在目标归一化下成立 ⟹ λ>1**
   反向（"η>0 的任何无条件主项 ⟹ RH"）**不得声称**

② 原 §1/§5 "所有【已知】S2 路线归约为…／只到 η=0 边界"——**证据不足，撤回该表述**
   原因（唐先生核实）：**Hooley 已展示 prime-specific off-diagonal 可在某些平均问题中被
   无条件压到主项以下**（误差改善至 QN log N + O(QN) + O(N²(log N)^{−A})）
   ⟹ "已知路线全部撞 η=0 墙"不成立为表述；只能写"**尚未打开 S2-c**"

③ 原 §2 三支线统一（θ=1/2 / λ=1 / η=0⁺ 为【同一道墙】）——**降级为待核**
   原因：按本项目自身 **N28（对象混淆）**，三者对象【未对齐】：
     · Lavrik    = 频率平均二阶量 ∫|S(θ)|²dθ
     · Motohashi = 算术函数（除数函数族）在 arithmetic progressions 中的 variance
     · Hooley    = 素数 AP variance 的 off-diagonal 改进
     而 R8 要的是【fixed X、短区间 H=X^η 的 prime-pair 二阶主项】——**不是同一对象**
   ⟹ 统一表述须先过【对象对齐检验】（Gate 0 精神），否则只能记为"形式相似"
   ⚠️ 特别：**不得因二者同具 QN log N 量级结构就把它们放在同一 λ 坐标轴上**
```

**本轮最大正面收获（唐先生指定登记）**：
$$\boxed{\text{Hooley 型 prime-specific off-diagonal cancellation = R8.3 必须排除的【真正反例模板】}}$$
> 若连这种**非 HL、非 zero-statistics、非简单筛法上界**的 off-diagonal 机制，
> 都被证明**不能**携带 $H=X^\eta$ 的连续主项 ⟹ R8-C 的结构论证才开始"有牙齿"；
> 反之若它能携带 ⟹ R8-A/C 的分界被真正打开。**这比继续泛查 variance/short interval 更重要。**

**当前状态更正为**：
$$\boxed{\textbf{R8.3-B'}:\ \text{已知路线未打开 S2-c，但"必然归约到 zero-side"【尚未证明】}}$$
