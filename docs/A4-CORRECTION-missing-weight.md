# ⚠️⚠️ **重大更正：NB3/NB4/NB5 的核漏了 `(mn)^{−1/2}` 权重** ✗✓（第 16 次自查 ✓）

> ⚚ 数值更正（2026-09-12，第 16 次自查 ✓）：本文若出现 **d_N² ≈ 0.68**（或 0.6803 / 0.6819 / 0.6821）**已作废** ✗ —— 根因：核漏了 **`(mn)^{−1/2}`** 权重 ✗（线性型有权重而核没有 ✗ ⟹ 归一化不一致 ✓）。**修正后的值**：**d_N²(N=40) = 0.0519493** ✓、**d_N²(N=160) = 0.0489309** ✓（与 Burnol 常数 C 之比从 ~75 倍降到 **~5.4 倍** ✓）。核与线性型的权重一致性**已列为提交前自检** ✓；距离类计算一律用 **SVD 正则化**或 **n ≤ N/2** ✓。详见 `A4-CORRECTION-missing-weight.md`。

**发现**：E26 执行批（子代理 ✓）+ **父方读码结构性确认** ✓｜**日期**：2026-09-12

---

## §1 **错误**
```
【定义 ✓（Báez-Duarte）】d_N² = inf_a (1/2π)∫ |1 − ζ(½+it)·Σ_{n≤N}a_n n^{−1/2−it}|² dt/(1/4+t²) ✓
   ⟹ |Σa_n n^{−1/2−it}|² = Σ_{m,n} a_m a_n **(mn)^{−1/2}(m/n)^{−it}** ✓
   ⟹ 核必须是 **K(m,n) = (mn)^{−1/2}·g(log(n/m))** ✓✓
【✗ 我写的】`K[m-1,n-1] = g(log n − log m)` ✗ —— **没有 (mn)^{−1/2}** ✓
   而线性型 `lv[m-1] = …/sqrt(m)` ✓ **有** ✓ ⟹ ⭐ **核与线性型【归一化不一致】** ✗✓
⟹ 求解的**不是** BCF 变分问题 ✗ ⟹ **所有记录的 d_N² 值都是错的** ✗✓
【⭐ 此前的征兆（我当时已察觉但未追根 ✗）】NB5 的系数 a_n ≈ **0.055·μ(n)** ✓
   —— 我当时写"**c 远小于 1，可疑** ⚠️" ✓ 但**归因为"归一化待确认"** ✗（**未查码** ✗✓）
```

## §2 **修正与验证（✓✓）**
```
【✓ 修补】三处核行均加 `/sqrt(m·n)` ✓（`NB3` ✓ / `NB4` ✓ / `NB5` ✓）
【✓✓ 交叉验证（决定性 ✓）】E26A **直接按定义**（不组装核 ✓）算得 N=40: **0.051949** ✓✓
   ≈ 修补后 NB3 的 **0.0519493307** ✓✓（**一致** ✓）；且 E26A 显示 convention A
   **复现定义积分到 3.4×10^{−13}** ✓✓ 而 convention B（旧 NB3）**完全不能** ✗
【修正后的值 ✓】
   · **d_N²(N=40) = 0.0519493** ✓（原记 0.682 ✗）｜**d_N²(N=160) = 0.0489309** ✓（原记 0.6803 ✗）
   · **d_N²·log N**：N=2: 0.229 → N=160: **0.248** ✓ ⟹ 与 C 之比 **4.96 → 5.38** ✓
   ⟹ ⭐ **与 Burnol 常数 C 的差距从"~75 倍"缩到"~5.4 倍"** ✓✓（**认识大幅改善** ✓）
【✓ 副产品改善】NB5 的系数尺度 **0.055 → 0.367** ✓（**接近 μ 量级** ✓）；
   M2 残差 0.479→**0.388** ✓；M3（拟合 x = 3.33N）残差 **0.209** ✓；**r 0.85 → 0.892** ✓✓
【✓ Möbius 校验仍通过】NB4: **97/98** ✓（SVD 正则化时 **98/98** ✓）；⚠️ 边缘 n≈N 不稳 ✓
   ⟹ **稳健做法 = SVD 正则化 或 限制 n ≤ N/2** ✓（列为制度 ✓）
```

## §3 **E26 三计算的结果（诚实 ✓）**
```
【A（与文献显式多项式对照）】**成功判据未达** ✗：R(N) 在**两种**约定下都**漂移** ✗ ——
   但**正确定位了缺陷** ✓✓（即 §1 的权重缺失 ✓）；⭐ 另发现 **I(V_N)·log N 单调【下降】**
   （1.33→0.89 ✓，约 19–29C ✓，且**几乎与 T 无关** ✓）⟹ **文献的显式 BCF 多项式【确实】呈现
   1/log N 速率** ✓✓，而**最优 d_N² 不呈现** ✗
【B（更大 T / N ≤ 400）】**判据 L2（单调降入 [C,3C]）未达** ✗ —— **干净否定** ✓：对**每个** T
   都**单调上升** ✗（4.0 → 7.0 ✓）；且 **d_N² 随 T 上升** ✗（N=160: 0.0489→0.0532→0.0555 ✓）
   ⟹ T=400 的值**未收敛** ✓（**诚实标注** ✓）
【C（DFMR 逐点距离 → 零自由区）】**判据未达** ✗ —— 唯一可得的 λ-无关输入
   （DFMR I Prop. 7.5: d_r² < 1 ✓）**恰好就是【非空阈值】** ✓ ⟹ 半径 0 ✓、盘为空 ✓
   ⟹ **未作任何零自由声明** ✓（**代理盘含已知零点 ✓ 已明确标注为非合法** ✓）
```

## §4 **制度更新（✓）**
```
【① 核与线性型的【归一化一致性检查】必须成为提交前自检 ✓✓】（本次教训 ✓）
【② 距离类计算一律用【SVD 正则化】或 n ≤ N/2 ✓】（边缘不稳 ✓）
【③ 用【直接按定义算】作交叉验证 ✓✓】（本次即靠它定案 ✓）
【④ 教训 ✓】**"异常但未追根"是危险的** ✗ —— 我此前见 a_n ≈ 0.055μ 只说"可疑" ✓
   而未**查码** ✗ ⟹ **数值异常必须追到根因** ✓（与"结果不理想先怀疑自己"同源 ✓）
```
