已查地图：命中（`EXP-A-1-partner-odd-kill`／`B2-2-ratio-pairing-odd-Laplace`）⟹ 引用，不开新案
D0: 本档对象 = 自检：`EXP-A-1` 判死定理 与 `B2-2` 一阶项是否冲突 ⟹ 判定「不矛盾」＋ 两条边界
D1: 0 （[REVIEW] 轮次：自检，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **自检：判死定理 与 B2-2 不冲突**

## §1 为什么不冲突（关键：`t`-变量下的镜像＝复共轭）

```
【`t`-变量】 `t=(s-\frac12)/i`；离线点 `t=\gamma-i\delta`，其镜像 `1-\bar s` 给 `t=\gamma+i\delta=\overline{t}` ⟹ **伙伴交换 ⟺ 复共轭** ✓
【伙伴奇 ⟺】 `h(\bar t)=-h(t)` ✓ —— 该条件**仅在移动中心 `a` 为实数时成立**
【`B2-2` 的载体】 中心 `a=\gamma-i\delta` **非实** ⟹ **不是伙伴奇** ⟹ **不在判死定理适用域** ✓✓ ⟹ **无矛盾** ✓
```

## §2 但由显式公式（恒等式）⟹ 一条必须承认的后果

```
【恒等式】 零点侧 ≡ 素数侧＋arch＋pole ⟹ **素数侧不是独立见证，是改写** ✓✓
【⟹ 一阶项必然被素数侧复现】 零点侧一阶 `=2i\delta/r+O(\delta^3)` ⟹ **素数侧必须给出同一一阶** ⟹ `P_1+A_1+E_1=2i/r` ⟹ **不存在"信号 vs 背景"之争**（我上一刀 `C_1=-2/r-P_1-A_1` 的框架**作废**）✗✓
【⟹ `EXP-A` 的真实性质】 `\partial_\delta\mathcal S|_{\delta=0}` 是一个**可由素数侧数值计算的诊断量**（不需输入零点）⟹ 结论上限为 **PARTIAL（数值可判，非不等式）** ✓
```

## §3 判死定理仍然成立（须限定）

```
【限定版】 仅当载体**伙伴奇**（`a\in\mathbb R`）时，零点侧恒零 ⟹ 素数侧亦恒零 ⟹ `\boxed{\text{一切伙伴奇载体在恒等式下恒为零}}` ✓✓（`T1` 为其特例）
【⟹ 唯一的非平凡入口】 载体**必须带非实中心**（`a=\gamma-i\delta`）或**外部形变参数** ⟹ 前者给**数值诊断**，后者（＝`C` 路线）才可能给**不等式** ✓✓
```

## §4 下一刀（不变，仅换判据）

```
把 `B2-2` 的 `\mathcal S_r` 化为**显式可算积分核**；判据从"`\mathcal V>0`"改为：$$\boxed{\text{素数侧数值 }\partial_\delta\mathcal S|_{\delta=0}\ \text{能否在已知模型（`DH`／人工配对）上区分出离线对？}}$$ ⟹ PASS（可区分）/ CLOSED（不可区分）✓
【边界】 §1 的 `t`-变量镜像＝共轭、§2 恒等式后果、§3 限定版为**本档自行推导**；未制造候选／未启动搜索／未碰 RH 总攻。
