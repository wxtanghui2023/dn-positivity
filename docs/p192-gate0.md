# P19.2：Gate 0——Boundary-Phase Geometry 是否只是 Weil criterion？

> 2026-09-01 · 唐先生 P19.2 指令 · Gate 0 审计（三件事）

## P19.2 框架（唐先生）
**纠正**：P19.1 的"相位跳变平滑化"不是 ζ 特有刚性——是任意解析函数"零点从边界移入内部"的 Poisson-kernel 效应——**但给新入口**：
- 法向相位导数 ∂_x arg F_δ(s)——边界上 = Poisson 核 P_δ(t−γ) ~ δ/(δ²+(t−γ)²)——δ=0 集中/δ>0 扩散——总质量守恒
- **Level 3（新机制）**：零点 = 电荷——临界线 = 边界——**把零点从边界向内部移动——是否必然增加某个全局几何代价——不能被其他零点重新排列抵消？**
- **Boundary Transport Defect**：W₂(μ_Λ, μ₀)²——global optimal transport defect——唯一极小在全部在线
- **Gate 0**：T(Λ) ≠ linear functional of explicit-formula type + T(Λ) ≥ 0 + T(Λ) = 0 ⟺ β=0

## Gate 0 三件事结果
### ① Θ'(t) 的 Poisson 表示（完成）
- Θ'(t) = Im[Ξ'/Ξ(½+it)]——在线：−1/(t−γ)（奇点）——离轴：−β/(β²+(t−γ)²)（Poisson 展宽）
- 数值验证：Θ'(14.6) 在线 0.334 vs 离轴 0.032——**确认平滑化 = Poisson 效应（唐先生纠正正确）**

### ② 泛函分类
- Level 1（总质量 ∫Θ' = 2πN）——辐角原理——杀掉
- Level 2（线性 ∫wΘ'）——**Weil 类（Σw(γ) = 素数侧）——自适应——判循环**
- Level 2b（二次 ∫∫KΘ'Θ'）——对关联类——**实际离轴 ζ 满足（真的）——循环**
- Level 3（W₂/transport/熵）——非线性——见 ③

### ③ W₂（transport）——循环
- W₂(μ_β, μ₀)² > 0（离轴展宽增加 transport——数值）
- **但——"W₂ = 0 ⟺ μ_Λ = μ₀ ⟺ 配置相同"（唯一性——循环）**
- **"合法类"（算术边界条件——渐近 Γ）不约束中间形状——"实际离轴 ζ 的 μ"（中间展宽——合法——真的）——循环**

## ⭐ Gate 0 判定
- **线性类退化成 Weil（判循环）**
- **二次类（对关联）——实际离轴 ζ 满足（真的）——循环**
- **W₂（transport）——"合法类"不排除离轴（ζ 真的）——循环**
- **除非——"合法类"含"中间形状约束"（非渐近——如——μ 的某种凸性/极值——从 Euler 积/函数方程的全局——未找到——可能不存在或等价 RH）**

## 有价值的新对象（记录）
**Θ'(t) 的 Poisson 表示**（边界相位测度 = ΣPoisson 核 + Γ 背景）——"零点 = 电荷——临界线 = 边界"的精确框架——**transport 视角（Level 3）是正确的方向**（避开局部负性/Euler 流保持/HP 构造三个陷阱）——但——"合法类"（不含中间形状约束）——循环

## 下一步
- (a) 寻找"合法类的中间形状约束"（μ 的凸性/极值——从 Euler 积/函数方程的全局——非渐近）
- (b) 接受 Gate 0 初步收口（线性 Weil/二次对关联/transport 合法类——循环——除非中间形状约束）
- (c) 唐先生指示
