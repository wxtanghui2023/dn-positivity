已查地图：命中（`E-36`／`E-35`／`AMEND-6/7/8`／`SOURCE-CARD-TEMPLATE` 本线自档）⟹ **引用，不开新案** ✓

# **搜索形状冻结 ＋ 下一张卡 7 问协议**（治理级；唐先生 2026-09-23 14:23 ✓✓）

D0: 本档对象 = **搜索形状冻结（`SOURCE_new\not\sim` 共同递推型）＋ 下一轮 `SOURCE CARD` 7 问协议 ＋ 四出口起点纪律**（引本线自档；**未开 bridge 案** ✓）
D1: 0 （`[REVIEW]` 轮次：治理级冻结，不主张新自由度 ✓）
FREEZE-ACK: D1=0 ✓
[REVIEW]

---

## §1 **账本固定（照录 ✓✓）**

| 项目 | 状态 |
|:--|:--|
| `Card 1` | **CLOSED — RUN-1 NEGATIVE** |
| `Card 2` | **HOLD — `\mathcal R_{\rm dens}` 未机械化** |
| `Card 3` | **CLOSED — S4 exact absorption** |
| `Card 4` | **CLOSED — RUN-2 NEGATIVE** |
| `linear-recurrence SOURCE` | **CLOSED / EXHAUSTED（仅已注册 `Card 1+4`）** |
| `D_{\rm new}` | **0** |
| `COMPUTATION` | **LOCKED** |
| RH target | **OPEN** |

## §2 ⭐⭐ **搜索形状冻结（照录 ✓✓）**

```
$$\boxed{\text{SOURCE}_{\rm new}\not\sim\text{sequence/correlation}\ \xrightarrow{\ \rm common\ low\text{-}order\ recurrence\ }\ \text{OBJECT}}$$ ✓
【含义（照录）】 下一张卡**不能只是** $$\text{另一种算术序列}\longrightarrow\text{共同递推}\longrightarrow F$$ ——
　**即使换成新的算术来源，这仍是同一个 discovery channel** ✗ ✓
【⟹ 下一张合法卡至少同时改变两件事（照录 ✓✓）】
　$$\boxed{\text{SOURCE 的数学生成机制改变}\quad+\quad\text{OBJECT 的数学类型改变}}$$ ✓
【⭐ 反改名字句（照录 ✓✓）】 第二项"OBJECT 类型改变"须**真正从线性动力学对象跳出去**，
　⛔ **不是**把递推写成**矩阵／核／生成函数／差分算子**等**重新命名** ✗ ✓
```

## §3 **下一轮不 `RUN`：只做 `SOURCE CARD` ＋ `S4-PRE-EXACT`，且必须先答 7 问（照录 ✓✓）**

```
**1.** `SOURCE` 自身是什么**独立数学问题**？
**2.** 第一项真正要计算的 `Q_1,\dots,Q_N` 是什么？
**3.** 若出现异常，**被迫出现的 `OBJECT` 到底是什么类型**？
**4.** 这个 `OBJECT` **为什么不是**递推／秩／核／支撑／有限差分／Hecke／Chebotarev 等**旧结构的换名**？
**5.** `OBJECT` 在**第二个尺度／第二层**是否仍被迫出现？
**6.** 它是否有**实验域之外**的数学定义？
**7.** 若计算结果是**平凡的**，什么明确条件**立即 `STOP`**？
```

## §4 **四出口（照录 ✓✓）**

```
$$\boxed{\begin{array}{c}\text{online zero proportion}\\\text{finite off-axis exclusion}\\\text{other prime problems}\\\text{other conjectures}\end{array}}$$
【⭐ 起点纪律（照录 ✓✓）】 **任何一项都必须从 `OBJECT` 本身出发**，⛔ **不是**计算完成后再寻找 RH 联系 ✗ ✓
```

## §5 ⭐ **最值得保护的资产（照录 ✓✓）**

```
$$\boxed{\text{不要再寻找"另一个会产生共同递推的 SOURCE"}}$$ ✓✓
【`Card 2`】 **保持 `HOLD`** ✓；⛔ **不能因为 `Card 1`／`Card 4` 都是负结果，就反向降低它的 `\mathcal R_{\rm dens}` admission 标准** ✓✓
【边界】 ✗ 未计算／⛔ 未降标准／⛔ 未开新卡／⛔ 未把"域内耗尽"写成"数学上排除"／⛔ 未把改名当类型改变 ✓
```
