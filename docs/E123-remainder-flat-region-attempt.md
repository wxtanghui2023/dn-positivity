# E123（编号更正 ⚠️ 原误编 E93 —— 与 `E93-remainder-ledger.md` 冲突）· 余项"平坦区/非共振重写"尝试

> 委托 ✓ 唐先生（E92 §3 剩余墙 $R$；方法学来源 ＝ 四色定理 2026 平坦区／局部重写）｜执行 ✓ 小灵
> 纪律 ✓ **未用 RH** ✓；**未跑 Lean** ✓；**纸面阶段：未写代码 ✓、未改动任何既有文件 ✓**（只新建本文件 ✓）
> 前作 ✓ `E92-abel-stationary-audit.md` ✓｜封档参照 ✓ `E96-E93-closure.md` ✓｜方法学 ✓ `EXT-4CT-2026-method-transfer.md §1,§3` ✓
> 基线参照 ✓ `E82-uniform-supremum.md` ✓｜截断/常数 ✓ `E94-verbatim-verification.md` ✓
> ⚠️ **全文标【推导】✗** —— 非定理、非数值验证 ✓；结论**不超出所证** ✓

---

## 0. 结论（先给判定 ✓，后附推导 ✓）

```
⛔ **未闭合** ✗ —— 「平坦区／非共振」重写把「逐点上界 $2.62\times10^{6}$」**降为【单量】条件** ✓，
   但该量对**标准显式公式截断**恒为 $+\infty$ ✗ ⟹ **本轮尝试失败** ✓（失败点在【聚合步】✓，非在【单频步】✗）
```

⭐ **唯一正面产出（重要 ✓）**：本方法把 E92 §3 的墙**精确重写为一条可判死活的单量条件** ✓：

$$\boxed{\ \text{（本方法内）闭合}\ \Longleftrightarrow\ M_R:=\sum_j|b_j|\ \le\ M_R^\star=1.4139\times10^{5}\ }\qquad(\{b_je^{i\lambda_jt}\}=\text{余项谱展开},\ \lambda_j\ge\log X)$$

$$\text{而标准截断下}\quad M_R=\sum_{m>X}\frac{\Lambda(m)}{\sqrt m\,\log m}=\sum_{p>X}\frac{1}{\sqrt p\log p}=\mathbf{+\infty}\ \ ✗$$

```
⭐ **单频增益 ~$10^{5}$ 倍 ✓✓**（每个频率分量的非共振把 $2.62\times10^6$ 压到 $\le C_0=23.6$ ✓）；
⛔ **但聚合（跨频率求和）发散** ✗ —— 因余项谱**不是绝对可和**的（$M_R=\infty$ ✓，且 $L^2$ 质量
   $\sum_{m>X}|a_m|^2\sim\log\log\infty$ 也发散 ✓）⟹ **逐频非共振【不能相加】** ✗✓
```

---
## ① 分类（命中 §1 硬 NO-GO？／是否循环？）

```
✅ **不命中 `RESEARCH-CONSTITUTION` §1 硬 NO-GO** ✓ —— §1 九条（N0/N10/N36/N26/N27/N45/N42/N20-A）
   **逐条不符** ✓：本机制**不引入新载体**、**不用形式事实冒充算术事实**、**不用输出熵**、
   **不是 CRT 局部-整体**、**不是边界幺正性** ✓（它是【对一条已知估计算法的常数改进】✓，非新路线）
⚠️ **但按 `E97` 的既有定位入【⑫关】=「全阶非共振／相干，不在 RH 关键路径」** ✓（差 NO-GO 总册 ⑫ 逐字 ✓）
   ⟹ **本机制属【条件可见性工具】✗，非 RH 路线** ✓ —— 这解释了整条 E92／E93／E93R 轨迹 ✓
⚠️ **边缘风险两处（须标注 ✓，未越线 ✗）**：
   ① `N1 显式公式重包装` ✗：本方法**用**显式公式做估计（✓），**不**把判据重写为显式公式 ✓；
      但若结论退化为「余项收敛 ⟺ 显式公式余项收敛」，则有重包装风险 ⚠️ ⟹ 本文件**不落入该退化** ✓（有独立常数 $C_0$ ✓）
   ② `零侧编码` ✗：E92 §3 的措辞是「零侧尾项」⚠️ —— 本文件**只用素数侧频率表示** ✓（$m^{-it}$，$\log m\ge\log X$ ✓），
      **不读零点位置** ✓；「零侧」在此仅作**对偶的语言** ✓（$\log X\approx\gamma_1$ ✓），非零点编码 ✓
✅ **非循环（相对 RH）** ✓：全程未用 RH ✓、未把结论当前提 ✓；$D_I^{rem}=D_I-D_I^{main}$ 是**恒等拆分** ✓，
   不是「假设结论 ⇒ 结论」✗。⚠️ 但存在**一处自一致风险**（见 §5.6 ⚠️）：频率前提 $(\text{freq}(R)\ge\log X)$
   与「余项可展开」是**同一件事的两面** ✓ —— 本文件**显式标注**该前提为【输入假设】✗，不当定理用 ✓
```

---
## ② 十八条门槛逐条过筛（§3.1 ✓）

> ⚠️ 说明 ✓：本对象是**既有链条内的技术引理**（$E82\to E92\to E93$ ✓），**非新载体** ✗ ——
> 故多条门槛**判为「不适用（N/A，非载体型）」** ✓；仍在适用的条目上如实过筛 ✓。

| # | 门槛 | 判定 | 依据（一句 ✓） |
|:--|:--|:--:|:--|
| 1 | **P1″** 具体位置约束 | ⚠️ **部分** | 约束的对象 = 具体积分 $\int_IRK_n$ ✓；但**未给出任何位置约束** ✗（不触谱位置 ✓） |
| 2 | **P0′** 不读未来 prime label | ✓ | 只用 $m\le X\le T_0$（$\le$ 当前高度 ✓），无未来标签 ✓ |
| 3 | **P-Info** 三量分离（申报信息预算） | ⚠️ **申报但不独立** | 预算 = 显式公式（素数侧 ✓）＋**一个前提**（freq$(R)\ge\log X$ ✗）；后者**非独立信息** ✓ |
| 4 | **P-Type** 点式⇏聚合 | ✗ **命中** | ⭐ 本方法**正是**「逐频（点式）⇒ 聚合」✗ —— 而**聚合步发散** ✓ ⟹ **正是此关杀本方法** ✓ |
| 5 | **P-Phase** 须携带相位信息 | ✓ | 机制核心 = chirp 相位 $e^{i\phi(t)}$ ✓（非模长型 ✓） |
| 6 | **P-Basis** 申报基底＋误差族 | ⚠️ | 基底 = 素数频率 $\{m^{-it}\}_{m>X}$ ✓（**非同一余型** ✓）；误差族 = **$\ell^1$ 谱质量** ✓（已申报 ✓，但 = ∞ ✗） |
| 7 | **P-Exponent** A/B/C/D（仅 C 可继续） | N/A | 本机制**无 $X$-指数参数** ✗（不是幂律型判据 ✓） |
| 8 | **P-Dual** 申报自对偶类别 | N/A | 无自对偶结构 ✗（不触 (b)(c) ✓） |
| 9 | **P-Scale** 申报阳性尺度（height/log 淘汰） | N/A | 无正性、无尺度 ✓ |
| 10 | **P-Diag** 申报对角尺度 | ✓ 已申报 | 对角 = **单频非共振** ✓；**非对角 = 跨频干涉** ✓ ⟹ ⭐ **非对角【不消】** ✗（发散源 ✓） |
| 11 | **P-SqrtPos** 正性须独立于零点 | N/A | 未用正性 ✗ |
| 12 | **P-Lock\*** 申报 $\Phi(C,M,W)$ | N/A | 无三重锁 ✗ |
| 13 | **P-Basis-2** 非同余基底须申报误差族（1/4 或 1/2） | ⚠️ | 基底为**非同余**（素数频率 ✓）⟹ **须申报** ✓：误差族 = **非 $1/4$／$1/2$ 型** ✗（是 $\sum|b_j|$ 型 ✓，不受此关约束 ✓） |
| 14 | **A_min** 四要素齐备 | N/A | 非载体 ✗（缺整性/对偶/阳性/共轭同步 ✓ 本就不要求 ✓） |
| 15 | **非循环性** | ✓（附风险 ⚠️） | 未用 RH／零位／结论 ✓；⚠️ §5.6 的自一致风险已标注 ✓ |
| 16 | **非 zero-encoding** | ✓ | 只用素数侧 ✓，未读零点 ✓ |
| 17 | **非重包装** | ⚠️（未越线） | 用显式公式做**估计** ✓；结论**不** $\equiv$ 显式公式 ✓（含独立常数 $C_0=23.611$ ✓），但**边界附近** ⚠️ |

```
⭐ **逐条小结 ✓**：**唯一硬命中 = #4（P-Type 点式⇏聚合）** ✗ —— 这**正是**本方法死因的结构性名字 ✓；
   #3/#6/#13/#15/#17 为**须申报／有风险但未越线** ⚠️；其余 N/A ✓（非载体型 ✓）。
⟹ **按第 0 层协议第②步：未过筛（#4✗）⟹ 应停** ✓ —— 本文件**据此停止**并如实归档 ✓（未强行写代码 ✗）
```

---
## ③ 形态（L1–L4 哪个？）

$$\boxed{\textbf{不属于 L1–L4 任一}\ ✗\ \text{—— 本对象是【技术引理】，不是【载体】}}$$

```
【依据 ✓】§4.2 的四种合法形态（L1 三重锁载体／L2 非自伴谱刚性／L3 边界—内部锁定／L4 非标准几何载体）
   **都是【载体级】结构** ✓ —— 而本对象是**一条估计**（对 $\int_IRK_n$ 的非共振界 ✓），
   寄生在既有链条（$E82$ 的分段账 ✓）之内 ✗，**不产生新 $\mathcal X$** ✗
⟹ **按第 0 层协议第③步：不属于 L1–L4 ⟹ 应停** ✓ —— 与 ②#4 同向 ✓
⚠️ **且 §4.1 协议 A–H 直接不适用** ✓：A（独立载体）✗、B（内生动力学）✗、C（三重锁）✗、
   D（独立正性）✗、E（约束谱位置）✗、H（选择性）✗ ⇒ **A–H 六条当场不满足** ✓
✅ **H（选择性）尤其 ✗**：本机制**不排除 $\beta\ne1/2$** ✗（它是一条【无条件常数改进】✓）
```

---
## ④ 纸面否决判据（**先写死 ✓**）＋ 一轮预算

> **纪律 ✓**：判据**在推导之前**写定 ✓（`RESEARCH-CONSTITUTION §3.2`：纸面否决判据先写死 ✓）；触发即关闭 ✓。

```
【K-1（结构性 · 主判据 ✓）】**若余项的绝对谱质量 $M_R:=\sum_j|b_j|$ 发散（$=+\infty$）**
   ⟹ **本方法【判死】** ✗（聚合步不可用 ✓）—— 归档为「未闭合」✓，不得改换名字续命 ✓。

【K-2（定量 · 判活阈值 ✓）】**若 $M_R<+\infty$ 且 $M_R\le M_R^\star=1.4139\times10^{5}$**
   ⟹ **本方法【闭合】** ✓（则 $|\int_IRK_n|\le(C_0/\pi)M_R\le1.0627\times10^{6}$ ✓ 达账 ✓）。

【K-3（部分增益 · 不算闭合 ✓）】**若 $C_0\cdot\mathbb{E}[\,]$（单频）小 ✓ 但 $M_R=\infty$**
   ⟹ **只记【单频增益 $\sim10^5$】** ✓，**不得**据此宣布任何闭合 ✗（照 §0 纪律 ✓）。

【K-4（自检 ✓）】任何一步若用到「$\sum_j$ 与 $\int_I$ 可交换」✗，**必须**先核 $M_R<+\infty$ ✓ ——
   否则该交换非法 ✗（= #4 P-Type ✗ 的精确形态 ✓）。
```

```
【一轮预算 ✓】**本轮 = 1 轮纸面推导（本文件）** ✗；不含代码 ✗、不含数值 ✗、不含新档案 ✗。
   · 触发条件 ✓：本文件给出 $M_R$ 的**渐近**（§5.5）；若 $M_R\to\infty$ ⟹ 触发 K-1 ⟹ **立即停** ✓
   · 备用（**不在本轮** ✗）：若要救活，须**换表示**（使 $M_R<\infty$ ✓）—— 列为 §9 的下一步判断 ✓，**本轮不做** ✗
```

---
## ⑤ 推导（逐步、常数显式、可独立核对 ✓）

### 5.0 记号与已知量（全部取自 E92／E93，**独立可核** ✓）

$$\phi(t)=2n\arctan\frac1{2t},\qquad \phi'(t)=-\frac{4n}{4t^2+1}=:-a(t)\ (<0),\qquad a(t)=\frac{4n}{4t^2+1}>0$$
$$\text{带 }I=[t_0,t_1]=[\sqrt{n/11},\ \sqrt n],\qquad n=T_0^2,\quad T_0=1.1324906587\times10^{6}$$

| 量 | 值 | 来源／核对 |
|:--|:--|:--|
| $T_0=\sqrt n=t_1$ | $1.132490659\times10^{6}$ | E92 ✓ |
| $t_0=\sqrt{n/11}$ | $3.4145878\times10^{5}$ | $\sqrt n/\sqrt{11}$ ✓ |
| $H=t_1-t_0$ | $7.9103188\times10^{5}$ | E92 $H=7.910319\times10^5$ ✓ |
| $a(t_1)=\lvert\phi'\rvert_{\min}$ | $1.0$（$=1-O(n^{-1})$ ✓） | $\lvert\phi'\rvert=1/u^2$，$u=1$ ✓ |
| $a(t_0)=\lvert\phi'\rvert_{\max}$ | $11.0$（$=11-O(n^{-1})$ ✓） | $u=1/\sqrt{11}$ ✓ |
| $\int_I\lvert\phi'\rvert=\int_Ia$ | $\sqrt n\,(\sqrt{11}-1)=\mathbf{2.623556\times10^{6}}$ | E92 ✓（本文件独立复核 ✓） |
| 允许 | $\mathbf{1.062658\times10^{6}}$ | $0.8N-\text{deep}$，E93 ✓ |
| $\Lambda_0=\log X=\log T_0$ | $\mathbf{13.939930}$ | E92 ✓ |
| 频率间隙 $\delta_0=\Lambda_0-\lvert\phi'\rvert_{\max}$ | $\mathbf{2.939930}$ | $13.939930-11$ ✓ |

```
⭐ **核心结构事实（E92 给定 ✓，本文件当【输入假设】用 ✗）**：
   $$\text{freq}(R)\ \ge\ \Lambda_0=\log X=\log T_0=13.94\ >\ \lvert\phi'\rvert_{\max}=11$$
   ⟹ 余项谱与 kernel 谱**不重叠** ✓（间隙 $\delta_0=2.94>0$ ✓）—— 这就是「平坦区／非共振」的落点 ✓
```

### 5.1 目标量（E92 §3 的墙 ✓）

$$D_I^{rem}:=\frac1\pi\int_I R(t)\,K_n(t)\,dt,\qquad K_n(t)=\phi'(t)\,e^{i\phi(t)}=-a(t)e^{i\phi(t)}$$
$$\textbf{逐点上界（E92 §3 ✓，本文件复核 ✓）}：\ \Bigl|\int_IRK_n\Bigr|\le\lVert R\rVert_\infty\int_I\lvert\phi'\rvert=1\cdot 2.623556\times10^{6}\ \ ✗\ (\text{超允许 }2.47\times)$$

### 5.2 平坦区重写：从「逐点上界」到「逐频非共振」（**单频步 ✓**）

$$\text{写余项谱展开}\quad R(t)=\sum_j b_j\,e^{i\lambda_j t},\qquad |\lambda_j|\ge\Lambda_0\ \ \text{(输入假设 }\star\text{)}$$
$$\Longrightarrow\quad \int_IRK_n\,dt=\sum_j b_j\,\widehat W_j,\qquad \widehat W_j:=\int_I\phi'(t)\,e^{i(\phi(t)+\lambda_jt)}\,dt=\int_I\phi'\,e^{i\Phi_j}\,dt$$
$$\Phi_j(t)=\phi(t)+\lambda_jt,\qquad \Phi_j'(t)=\phi'(t)+\lambda_j=-a(t)+\lambda_j$$

$$\textbf{非共振（关键观察 ✓）}：\ a(t)\in[1,11]\ \Longrightarrow\ |\Phi_j'|=\lvert\lambda_j-a(t)\rvert\ge\lvert\lambda_j\rvert-11\ \ge\ \delta_0=2.939930\ >\ 0$$
```
⟹ **每个频率分量都【无驻点】（non-stationary ✓）** —— 「平坦区」= 谱间隙区 ✓；
   这正是 4CT §1「在无结构区找局部可归约结构」的同型落点 ✓（此处「局部结构」= 单频非共振 ✓）
```

### 5.3 单频界（**显式常数，可独立核对 ✓**）

$$\text{分部积分 ✓}：g=\phi'=-a,\ \mu_j=|\Phi_j'|\ge\delta_0\ \Longrightarrow\ \Bigl|\int_Ig\,e^{i\Phi_j}\Bigr|\ \le\ \frac{2\sup|g|}{\mu_j}+\frac{\operatorname{Var}(g)}{\mu_j}+\frac{\sup|g|\cdot\operatorname{Var}(\Phi_j')}{\mu_j^{2}}$$

$$\sup_I|g|=\sup_I a=a(t_0)=11,\qquad \operatorname{Var}_I(g)=\operatorname{Var}_I(\Phi_j')=a(t_0)-a(t_1)=11-1=10$$

$$\Longrightarrow\quad \boxed{\ |\widehat W_j|\ \le\ \frac{32}{\mu_j}+\frac{110}{\mu_j^{2}}\ \le\ \frac{32}{\delta_0}+\frac{110}{\delta_0^{2}}=\mathbf{23.6114}\ =\:C_0\ }\qquad(\forall j)$$

```
⭐ **单频增益 ✓**：从 $\int_I\lvert\phi'\rvert=2.62\times10^6$ 到 $\le C_0=23.6$ ⟹ **增益 $=2.62\times10^6/23.6=\mathbf{1.11\times10^{5}}$ 倍** ✓✓
   —— **这才是 4CT §1「局部重写 > 逐点上界」在本问题的真实兑现** ✓（单频层面 **完全兑现** ✓✓）
```

### 5.4 聚合步（**★ 死在此处** ✗）

$$\Bigl|\int_IRK_n\Bigr|\le\sum_j|b_j|\,|\widehat W_j|\ \overset{\text{5.3}}{\le}\ C_0\sum_j|b_j|=C_0\,M_R,\qquad M_R:=\sum_j|b_j|$$
$$\Longrightarrow\quad \Bigl|\frac1\pi\int_IRK_n\Bigr|\ \le\ \frac{C_0}{\pi}\,M_R\ =\ \mathbf{7.5157}\cdot M_R$$

$$\textbf{闭合条件（= K-2 ✓）}：\ 7.5157\,M_R\le 1.062658\times10^{6}\quad\Longleftrightarrow\quad \boxed{M_R\le M_R^\star=\mathbf{1.4139\times10^{5}}}$$

```
⚠️ **此步【非法】的充要条件 ✓**：$\sum_j$ 与 $\int_I$ 交换 ✓ —— 它要求 $M_R<+\infty$ ✗（K-4 ✓）。
   ⟹ 聚合步的合法性 = $M_R$ 有限 ✓ —— 这正是 #4（P-Type）✗ 的具体形态 ✓
```

### 5.5 ⛔ **$M_R$ 的实际值：标准截断下 $=+\infty$**（**判死 ✓**）

$$\text{余项（标准截断，}X=T_0\text{）的素频谱质量：}\quad M_R=\sum_{m>X}\frac{\Lambda(m)}{\sqrt m\log m}\ \supseteq\ \sum_{p>X}\frac{1}{\sqrt p\,\log p}$$

$$\textbf{渐近 ✓（用 }\pi(u)\sim u/\log u\Rightarrow d\pi=du/\log u\text{ ✓）}：$$
$$\sum_{X<p\le N}\frac{1}{\sqrt p\log p}\ \approx\ \int_X^N\frac{1}{\sqrt u\log u}\cdot\frac{du}{\log u}\ =\ \int_X^N\frac{du}{\sqrt u\,\log^2u}\ \sim\ \frac{2\sqrt N}{\log^2N}\ \xrightarrow{\ N\to\infty\ }\ \mathbf{+\infty}$$

```
⛔ **故 $M_R=+\infty$** ✗ ⟹ **触发 K-1 ⟹ 判死** ✓
```

⭐ **为什么「$\lVert R\rVert_\infty\le1$（有限）」与「$M_R=\infty$」不矛盾** ✓（关键澄清 ✓）：

$$\textbf{模型 ✓}：\ R(t)=\sum_{n=1}^{\infty}\frac{\sin(nt)}{n}\ \Longrightarrow\ \lVert R\rVert_\infty<\infty\ (\text{锯齿，有界 ✓})\quad\text{而}\quad M_R=\sum_{n}\frac1n=\infty\ \ ✗$$
```
⟹ **有界 ⇏ 绝对谱可和** ✓ —— 「谱间隙」控制**每个分量的积分** ✓，但**不控制分量个数×幅度之和** ✗。
   ⟹ 只有当余项属**绝对收敛谱类**（$M_R<\infty$ ✓）时，逐频非共振才能相加 ✓；标准截断**不属** ✗。
```

⛔ **$L^2$ 加权聚合也救不了** ✗（C–S in $\ell^2$ 同样发散 ✓）：

$$\Bigl|\int_IRK_n\Bigr|\le\sqrt{\sum_j|b_j|^2}\cdot\sqrt{\sum_j|\widehat W_j|^2}\ \le\ M_{R,2}\cdot C_0\sqrt{\#\{j\}}\ \ ✗\ \text{（}\#\{j\}=\infty\text{）}$$
$$\text{且}\quad \sum_{m>X}|a_m|^2=\sum_{p>X}\frac{1}{p}\ \sim\ \log\log N\ \xrightarrow{}\ \infty\ \ ✗\quad(a_m=\Lambda(m)/(\sqrt m\log m))$$

```
⟹ ⭐ **两条聚合路线（$\ell^1$ 与 $\ell^2$）** 都发散 ✗ —— 死因是**结构性的**（余项谱既非 $\ell^1$ 也非 $\ell^2$ 可和 ✓）。
```

### 5.6 等价表述（$u$-坐标 ✓）＋ 自一致风险标注 ⚠️

$$\textbf{换元 }u=\phi(t)\ (\text{E91 §4 路线 B ✓})：\ \int_IR\,\phi'\,e^{i\phi}\,dt=\int_{J}R(t(u))\,e^{iu}\,du,\qquad J=\phi(I)\approx[\sqrt n,\ \sqrt{11}\sqrt n]$$
$$|J|=\int_I|\phi'|=\sqrt n(\sqrt{11}-1)=2.623556\times10^{6}\ \ \checkmark\ (\text{墙常数出现两次 ✓ 自一致 ✓})$$

```
【u-坐标下的频率间隙 ✓】余项在 u-坐标的频率 $=\lambda_j\,t^2/n=\lambda_j u^2\ge\Lambda_0/11=1.267$ ✓，
   而 kernel $e^{iu}$ 的频率 $=1$ ⟹ 间隙 $0.267$（相对间隙 21% ✓）。
⟹ **同一结构、同一死因** ✓（聚合仍发散 ✗）—— 换坐标**不改变**结论 ✓（排除「只是坐标没选对」的侥幸 ✗）
```

⚠️ **自一致风险（必须标注 ✓）**：
```
【风险 ✓】输入假设 $(\star)$「$\text{freq}(R)\ge\Lambda_0$ 且 $R=\sum b_je^{i\lambda_jt}$」**字面上成立**
   ⟺ 余项谱**绝对收敛**（$M_R<\infty$ ✓）—— **而这恰是本方法要证的东西** ⚠️。
⟹ **故 $(\star)$ 不能当【已知定理】用** ✗ —— 本文件把它当【输入假设】✓；一旦 $M_R=\infty$ ✗，
   假设 $(\star)$ **失效** ✓ ⟹ 整个 5.2–5.4 **不成立** ✓（这正是 K-1 触发 ✓）。
⚠️ **不循环（相对 RH）** ✓：$(\star)$ 与 RH **无关**（是显式公式的性质 ✓）；但**方法内部**存在
   「要证的东西出现在前提里」的**结构自指** ⚠️ —— 故本文件**不宣布任何闭合** ✗。
```

### 5.7 与既有档案的关系（✓ 一致性与路线定位）

```
⭐ **与 E96 的更窄开放问题一致 ✓**：E96 逐字 ✓「能否【独立构造】$S=P_x+R_S$ 使 $\lVert R_S\rVert_2^2\lesssim H/\log T$」✓
   ⟹ 本文件给出的**新形态** ✓：不是 $\lVert R_S\rVert_2$ 型 ✗，而是 **$\ell^1$ 谱质量** $M_R\le1.41\times10^5$ ✓ ——
   **更硬或更软取决于表示** ⚠️（见 §9 判断 ✓）。
⭐ **与 E93/E96 的死因不同（须区分 ✓）**：E93 死于**误用 $S_1$ 级 Theorem 4**（$S$ vs $S_1$ 对象不匹配 ✗）；
   本文件死于**谱质量发散**（聚合非法 ✗）—— **两个独立死因** ✓。
⚠️ **与 E119 的结论一致 ✓**：E119 逐字 ✓「窗口【不能平均相位】⟹ 局部 $L^2$ 路线【不能绕开】点态墙」✓
   ⟹ 本文件的「平坦区＋局部」混合版（若走局部 $L^2$ ✓）**同样不能绕开** ✗ ⟹ **不重复尝试** ✓。
⚠️ **与 E97 的定位一致 ✓**：本机制入【⑫关】（非共振／相干，不在 RH 关键路径 ✓）⟹ **^条件可见性工具** ✗。
```

---
## ⑥ 【技术手段】＋【对我们的启发／推进】

```
【技术手段 ✓（可迁移 ✓）】
  ① **平坦区识别 ✓**：把「kernel 频率 $\le11$ ＜ 余项频率 $\ge13.94$」读作**谱间隙区**（$\delta_0=2.94$ ✓）——
     这正是 4CT §1「零曲率／无结构段」的算术对应物 ✓。
  ② **局部重写 ✓**：在谱间隙区用**分部积分（非驻相 ✓）**替代逐点上界 ✓ —— 单频增益 $1.11\times10^5$ 倍 ✓✓。
  ③ **常数显式化 ✓**：$C_0=32/\delta_0+110/\delta_0^2=23.6114$ ✓（$\sup|g|=11$、$\operatorname{Var}(g)=\operatorname{Var}(\Phi')=10$ ✓）——
     一切常数可独立重算 ✓（无隐藏 $O$ ✓）。
  ④ **判据先写死 ＋ 单量归约 ✓**：把「能否闭合」压成**一个数** $M_R$ ✓ —— 这本身是可复用的**归约技术** ✓。

【对我们的启发／推进 ✓】
  ✅ **正面 ✓**：本方法**确实**把一条「看似吞掉全部收益的逐点墙」（$2.62\times10^6$ ✗）
     化为「**一条单量条件**」$M_R\le1.41\times10^5$ ✓ —— **这正是 4CT 方法学的『目标』**（局部重写替代逐点上界 ✓）。
  ⛔ **负面（诚实 ✓）**：该单量对**任何有限截断**都是 $+\infty$ ✗ —— 因为余项 $R=S-P_X$
     **继承了计数误差 $S$ 的跳跃结构**（$S$ 在每个零点处跳 $1$ ✓）⟹ 其谱系数 $\sim1/\text{频率}$
     ⟹ $\ell^1$ 质量 $=\infty$ ✓（**与表示无关** ✓，换坐标／换截断皆无效 ✓，§5.6 已验证 ✓）。
  ⭐ **结构性教训（入档案 ✓）**：「谱间隙 ⟹ 逐频非共振」是**真机制** ✓，但**可乘性不可默认** ✓ ——
     要相加必须 $M_R<\infty$ ✓（`EXT-4CT §4 自检项 #9：叠加性必须被证明，不得默认` ✓ **再次生效** ✓）。
```

---
## ⑦ 证据等级 ＋ 未证清单（**失败在哪一步、卡在哪个量** ✓）

$$\boxed{\textbf{证据等级 ＝【推导级】✗（非定理、非数值验证 ✓；输入假设 $(\star)$ 未证 ✗）}}$$

```
⛔ **失败在【第 5.4 步 · 聚合】** ✗ —— **不是**在单频步（5.2–5.3 成立且强 ✓），
   **不是**在坐标选择（5.6 排除 ✓），**不是**在截断选择（对任意有限 $X$ 同死 ✓）。
⭐ **卡在的量 ✓**：$\boxed{M_R=\sum_j|b_j|=\sum_{m>X}\Lambda(m)/(\sqrt m\log m)}$ —— **绝对谱质量，$=+\infty$** ✗
   （等价的 $L^2$ 量 $\sum_{m>X}|a_m|^2\sim\log\log N$ 亦发散 ✗ ⟹ **两条聚合路线皆堵** ✗）
```

**未证清单 ✓（逐条 ✓）**：

| # | 未证项 | 状态 |
|:--|:--|:--|
| 1 | 输入假设 $(\star)$：$\text{freq}(R)\ge\log X$ 的**严格**陈述（含绝对收敛 ✓） | ⚠️ **未证**（E92 作启发用 ✓） |
| 2 | $M_R<\infty$（余项谱绝对可和 ✓） | ⛔ **已判为假**（$\forall$ 有限截断 ✓，§5.5 ✓） |
| 3 | 逐频非共振的**可加性**（$\sum_j$ 与 $\int_I$ 交换 ✓） | ⛔ **非法**（=#4 P-Type ✓） |
| 4 | 闭合（把 $2.62\times10^6$ 降到 $\le1.06\times10^6$ ✓） | ⛔ **未达成** ✗ |
| 5 | 是否存在**完全不同的**聚合机制（如零点对相关 ✓） | ⚠️ **未探**（超本轮 ✓；且属 E117/E121 的对相关类输入 ✗，不在本轮预算 ✓） |

```
⚠️ **不宣布**「余项墙永久不可过」✗ —— 只宣布：**本题材的『逐频非共振』方法【判死】** ✓（K-1 触发 ✓）；
   以及**精确刻画出**何种表示才能救活（须 $M_R<\infty$ ✓）—— 而该表示**不存在于有限截断族**内 ✓。
⚠️ **不宣布** T1 已证／未证 ✗（不涉 T1 状态 ✓）；**不改动** E92／E93／E96 任何档案 ✗ ✓。
```

---
## ⑧ 若找到可相消结构：**条件性机制 ＋ 常数**（⚠️ **本文件未找到，仅给出【唯一可行形态】** ✗）

> **诚实声明 ✓**：标准截断下**未找到**可相消结构 ✗（$M_R=\infty$ ✓）。以下给出**唯一能使 2.62e6 降到 $\le1.063e6$ 的机制形态** ✓ —— **条件性** ✗（须 $M_R\le M_R^\star$ ✓），**非闭合** ✗。

$$\boxed{\textbf{条件性机制（若可构造 }\widetilde R=S-\widetilde P_X\text{ 使 }M_{\widetilde R}<\infty\text{ ✓）}：\quad \Bigl|\frac1\pi\int_IRK_n\Bigr|\ \le\ \underbrace{7.5157}_{=C_0/\pi}\cdot M_{\widetilde R}\ \le\ 1.0627\times10^{6}\ \Longleftarrow\ M_{\widetilde R}\le M_R^\star=1.4139\times10^{5}}$$

**其中的常数（逐项显式 ✓，可独立核对 ✓）**：

| 常数 | 值 | 来源 |
|:--|:--|:--|
| $C_0$（单频非共振上界 ✓） | $\mathbf{23.6114}$ | $32/\delta_0+110/\delta_0^{2}$ ✓ |
| 其中 $\delta_0$（谱间隙 ✓） | $\mathbf{2.939930}$ | $\log T_0-11$ ✓ |
| $C_0/\pi$（聚合系数 ✓） | $\mathbf{7.5157}$ | ✓ |
| $M_R^\star$（闭合阈值 ✓） | $\mathbf{1.4139\times10^{5}}$ | $1.062658\times10^6/(C_0/\pi)$ ✓ |
| 单频增益（vs 逐点 ✓） | $\mathbf{1.11\times10^{5}}$ 倍 | $2.62\times10^6/23.6$ ✓ |
| 墙常数（$\int_I\lvert\phi'\rvert$ ✓） | $2.623556\times10^{6}$ | $\sqrt n(\sqrt{11}-1)$ ✓ |

```
⛔ **无法闭合成因 ✓**：$M_{\widetilde R}<\infty$ 需余项谱**绝对可和** ✗ —— 而 $R=S-P_X$ 继承 $S$ 的**跳跃结构** ✓
   （$S$ 于每零点跳 $1$ ⟹ 谱系数 $\sim1/\text{频率}$ ⟹ $M_{\widetilde R}\ge\sum 1/\text{频率}=\infty$ ✓），
   **任何有限 $X$、任何坐标皆然** ✗（§5.6 ✓）。⟹ **该表示【不存在于有限截断族】** ✓ ⟹ **机制不可兑现** ✗。
⚠️ **唯一理论逃逸口 ✗**：允许**跨频相消**（非 $\ell^1$ 聚合 ✓）⟹ 需**零点对相关** ✓
   （与 `E117`「控制量＝对相关」✓、`E121`「窗口不能平均相位」✓ 同源 ✓）⟹ **属 N21 类已关输入** ✗，**本轮不探** ✓。
```

---
## ⑨ 判定汇总（给唐先生 ✓）

$$\boxed{\textbf{E93R ＝ 未闭合（方法判死）}\ ✗\quad\text{单频非共振成立（增益 }1.11\times10^5\text{ 倍 ✓）}\ \text{但聚合发散（}M_R=\infty\text{ ✗）}}$$

```
① 分类 ✓：不命中 §1 硬 NO-GO ✓；按 E97 入【⑫关】（非 RH 路径 ✓）；非循环（相对 RH ✓）。
② 门槛 ✓：**唯一硬命中 = #4（P-Type 点式⇏聚合）** ✗ ⟹ **按第 0 层协议应停** ✓（已停 ✓）。
③ 形态 ✓：**不属于 L1–L4**（技术引理，非载体 ✓）⟹ **同向应停** ✓。
④ 判据 ✓：K-1 触发（$M_R=\infty$ ✓）⟹ **判死** ✓；一轮预算已尽 ✓；**未写代码** ✓。
⑤ 推导 ✓：单频界 $C_0=23.6114$ ✓（显式 ✓）；聚合阈值 $M_R^\star=1.4139\times10^{5}$ ✓；$M_R\sim2\sqrt N/\log^2N\to\infty$ ✓。
⑥ 手段／启发 ✓：谱间隙＝平坦区 ✓；局部重写＝非驻相 ✓；**教训＝可加性不可默认** ✓。
⑦ 证据 ✓：推导级 ✗；失败在**聚合步** ✓；卡在 **$M_R$（绝对谱质量）** ✓。
⑧ 条件机制 ✓：若 $M_R\le1.4139\times10^5$ 则闭合 ✓ —— 但**该表示不存在于有限截断族** ✗。
```

```
⚠️ **未宣布**：余项墙永久不可过 ✗｜T1 生死 ✗｜任何闭合 ✗
✅ **已宣布**：**【逐频非共振】这一具体方法（对本题材）判死** ✓，及其**精确数量化死因**（$M_R=\infty$ ✓）
✓ **纪律** ✓：未用 RH ✓；未跑 Lean ✓；**未写代码 ✓**；**未改动任何既有文件 ✓**；只新建本文件 ✓
✓ **与 E96 兼容** ✓：E96 的更窄开放问题（能否构造 $S=P_x+R_S$ 使 $\lVert R_S\rVert_2^2\lesssim H/\log T$）**仍未解** ✓，
   本文件**不声称**解决了它 ✗，只**补充了另一条等价/更硬的形态**（$M_R\le1.41\times10^5$ ✓）。
```

---
*（本文件为纸面推导尝试 ✓，**失败归档** ✗；按 `RESEARCH-CONSTITUTION §3.2 流程纪律`：触发即关闭 ✓，不改换名字续命 ✓。）*
