# P26-A21：谱对象的替代——"合并发生的地方"（无自然候选）

> 2026-09-01 · 继续探索 · Θ 的替代（编码 1−ρ 与 ρ̄ 对齐的宿主）

## P26-A21 结果

### ① F_q 的"合并编码"——特征多项式的结构
- 特征多项式 P(t) = det(1−t·Fr)——有限维——"合并"（q^w/α 与 ᾱ 同一根）编码在：
  - (a) 系数在有限域（Galois 不变——根集共轭闭合）
  - (b) 对称关系（Poincaré——P(t) ~ ±t^d P(1/(q^w t)) 类）
- **"编码"（两种对称性（共轭+对偶）在 P(t) 的结构中）——合并是 P(t) 的结构事实**

### ② Spec Z 的对应——什么"编码"共轭-FE 对齐？
- 候选：(a) Hadamard 积（根集——对称轨道——但"允许"离轴——不强制合并）(b) 函数方程（零点反射——不编码对齐）(c) 实系数（零点共轭——不编码对齐）
- **⚠️ ζ 的"对称性编码"（Hadamard/FE/实系数）——都给"轨道"——不编码"对齐/合并"**

### ③ ⭐ 关键——ζ 缺"合并的编码"
- F_q：P(t) 的结构编码合并（Galois + Poincaré 对齐）
- Spec Z：ζ 的结构只给"轨道"（Wigner——A19）——不编码合并
- **"为什么 F_q 有而 Spec Z 无"**：F_q 有限维（特征多项式——线性代数结构）——Spec Z 无限维（无特征多项式）——**"特征零无 Fr"（THH 审计——谱约束无来源）**

### ④ 候选盘点
| 候选 | 结构 | 编码合并？ |
|---|---|---|
| Hadamard 积 | 根集——对称轨道 | ✗ 只给轨道 |
| 函数方程（ξ） | 零点反射 | ✗ 不对齐 |
| 实系数 | 零点共轭 | ✗ 不对齐 |
| 模形式（Laplace） | 自伴实谱（Selberg） | ✓ 但 P3 封口 |
| 散射（幺正性） | 连续谱极点 | ✗ P3 封口 |
| Θ（Deninger） | 谱——合并 | ✓ 未构造 |

### ⑤ ⭐ 诚实评估
- "合并发生的地方"候选（Hadamard/FE/实系数/模形式/散射）——全部不编码——模形式（Selberg——自伴实谱——但 Riemann ζ 是连续谱散射——P3）——Θ（唯一编码合并——未构造）
- **"谱对象的替代"——无自然候选——与 THH/特征零无 Fr 一致——重数强制（Spec Z）无宿主——循环确认**

### ⑥ ⭐ 深层——"编码"的本质（信息论）
- "合并"（1−ρ = ρ̄——在线）——"信息冗余"（同一对象）
- F_q：特征多项式的代数结构编码冗余——Spec Z：解析结构不编码（离轴——无冗余）
- **"冗余的编码"需要"代数对象"（特征多项式类）——Spec Z 无（无限维——无特征多项式）**
- 物理：规范冗余（β 规范——1−ρ 与 ρ̄ 同一物理态——在线——规范等价）——编码（规范结构）Spec Z 无

## ⭐ P26-A21 判定
- 谱对象的替代——无自然候选——合并编码（Spec Z）不存在（除 Θ 未构造）
- 与 THH/特征零无 Fr 一致——重数强制的信息论形态（Spec Z 缺"冗余编码"——无线性代数结构）

## 下一步
- (a) 接受 A21（谱对象的替代无自然候选——合并编码不存在——与 THH 一致——循环确认）
- (b) 转向整理（P26 系列完整图景——A3-A21——"为什么 RH 难"的完整结构解释——存档/汇报）
- (c) 唐先生指示
