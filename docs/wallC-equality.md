# 墙 C —— 等号条件（主定理"⟺"方向）

> 日期：2026-08-30 19:44
> 状态：核心确认（由 L5 正项和 + 唯一性保证——正性结构）——正式化书写（依赖墙 A/B 的严格估计与 Weil 框架——🟡）
> 依赖：墙 A（交换——Σδ_ρ|H''| < ∞）+ 墙 B（统一配对——Q = ⟨S,H_0⟩）

## 等号条件（主定理"⟺"）

$$Q'_{\rm zeros} = Q'_{\rm RH} \iff \text{RH（所有零点 } \beta_\rho = \tfrac12\text{）}$$

**充分性（RH ⟹ 等号）**：
- RH 时全在线（δ_ρ = 0）——Q'_zeros = Σ_ρ w_H(γ_ρ, 0) = Σ_ρ w_target(γ_ρ) = Q'_RH（——w_H(γ,0) = w_target 精确——）∎

**必要性（等号 ⟹ RH）**：
- 墙 A/B 后：Q'_zeros = ⟨S, H_0⟩ = Σ_ρ w_H(γ_ρ, |δ_ρ|)（统一配对——逐项交换）
- Q'_zeros − Q'_RH = Σ_ρ [w_H(γ_ρ,|δ_ρ|) − w_target(γ_ρ)] = Σ_ρ diff(γ_ρ, |δ_ρ|)
- 每项 diff ≥ 0（L5——γ_ρ ≥ 14——|δ_ρ| ∈ (0,½)）
- **正项和 = 0 ⟺ 每项 = 0 ⟺ 每项 |δ_ρ| = 0（L5 零点唯一性——δ=0 唯一零点）⟺ RH** ∎

## 正性结构（墙 C 机制）

$$\langle S, H_0\rangle = \sum_\rho \delta_\rho^2\, w_H(\gamma_\rho, |\delta_\rho|)$$

- **w_H(γ,|δ|) > 0（恒正——L5——γ≥14——细扫确认）**——
- 正项和——⟨S,H_0⟩ = 0 ⟺ 每项 δ_ρ²w_H = 0 ⟺ 每项 δ_ρ = 0 ⟺ RH——
- **正性结构（w_H 恒正）——无需谱解释**——

## 数值验证

- 在线等号精确：Q'_zeros(在线) = Q'_RH = 3.657464e-5（差 0.0000e+00）
- 离轴偏离严格正：|δ|=0.01: +7.6e-5~+3.7e-6——|δ|=0.3: +1.5e-3~+1.1e-4（单调）
- w_H 恒正（γ≥14——|δ|∈(0,½)——细扫 True）

## 依赖（墙 A/B——正式化待补）

| 依赖 | 状态 |
|------|------|
| 墙 A：Q'_zeros = Σw_H 的交换（Σδ_ρ|H''| < ∞） | 🟢 核心闭合——严格估计待补 |
| 墙 B：统一配对 Q = ⟨S,H_0⟩（Weil 合法） | 🟢 核心障碍解除——测试空间待归属 |
| L5：diff > 0 + 唯一性 | ✅ 解析证明（N(x)——∂N/∂x > 0——γ≥2） |

## 墙 C 状态

- **核心确认**：等号条件由 L5 正项和 + 唯一性保证——正性结构（w_H 恒正）——
- **比墙 A/B 简单**（等号条件的核心已建立——无需新机制）——
- **剩余**：墙 A/B 正式化完成后——等号方向的完整落笔（逻辑结构已就绪）——
