已查地图：命中（`CAS-1` 本线自档／`E-20`／`E-22`）⟹ **引用，不开新案** ✓

# **`CAS-1B` 高阶消失阶判别实验（`k\le6`）：命中 STOP 条件 ⟹ 立即收手**

**唐先生令（2026-09-23 13:33）**：选 **(甲)**，但**限定为"一轮高阶消失阶判别实验"**（`k=0,\dots,6`），⛔ **不是**开启无限参数搜索 ✓；
　条件固定：`h=2\delta`、`\delta=0.05`、`T_0\in\{500,1000,2000\}`、`W=100`，仍跑 `Z` 与对照 `C` ✓；判据升级为 `R_k`／`\kappa_k`／`A_k` ＋ **ζ 特异跨尺度锁定** ✓；
　⭐ **STOP 条件**：若"所有 `k` 满秩 ∧ 谱随 `k` 平滑 ∧ ζ 与 `C` 差别仍只是尺度增长 ∧ 模式可归入已知账本" ⟹ **立即收手**，⛔ **不得**因"`k=6` 还没发现"而继续 `k=8,10,20,\dots` ✓✓

D0: 本档对象 = **`CAS-1B` 高阶消失阶判别（`k\le6`）**（引本线自档；**未开 bridge 案** ✓）
D1: 0 （`[RESEARCH]` 轮次；结论为**计算级判定（STOP）**，不主张新自由度 ✓）
FREEZE-ACK: D1=0 ✓
[RESEARCH]

---

## §1 口径与设定

```
【脚本】 `scripts/cas1b_high_order_vanishing_scan.py`｜【日志】 `out/cas1b/cas1b_scan.txt` ✓
【口径声明（本档约定，因唐先生记号有歧义 ⚠️）】 `Q^{(k)}` := 全 `7\times7` Gram 的 **`(k+1)\times(k+1)` 主子矩阵**（阶 `0..k`）✓
【数值健全性补充（本仓加 ✓）】 因高阶 `Q_{kk}` 量级差极大，**另报归一化相关阵** `C_{kl}=Q_{kl}/\sqrt{Q_{kk}Q_{ll}}` 的特征值 ✓
【对象】 `Z`＝RS 实值函数｜对照 `C=\sum_{n\le60}\cos(t\log n)/\sqrt n` ✓
```

## §2 数据（逐项打印 ✓）

```
【T_0=2000, Z】 `R_k` = 1.0000, 3.66e-2, 9.77e-4, 2.23e-5, 4.23e-7, 6.88e-9, 9.87e-11（`k=0..6`）；`\kappa_k` 至 9.75e+9 ✓
【T_0=2000, C】 `R_k` = 1.0000, 5.68e-2, 2.14e-3, 1.03e-4, 3.40e-6, 1.63e-7, 5.20e-9 ✓
【`A_k=\mathrm{tr}Q^{(k+1)}/\mathrm{tr}Q^{(k)}`】 `Z`: 1.038338, 1.002411, 1.000179, 1.000014, 1.000001, 1.000000（**平滑 → 1**）✓
【相关阵特征值（`Z`, T_0=2000）】 2.466e-4, 2.486e-3, 2.474e-2, 1.486e-1, 4.610e-1, 2.975, 3.388 ⟹ **最小特征值远离 0** ⟹ 无退化 ✓
【跨尺度（关键）】
　`R_k(Z)` 三个尺度：`k=1`: 2.40e-2／2.83e-2／3.66e-2｜`k=2`: 4.06e-4／6.36e-4／9.77e-4｜`k=3`: 5.10e-6／1.08e-5／2.23e-5｜`k=6`: 3.86e-12／1.92e-11／9.87e-11 ⟹ **随 `T_0` 单调漂移** ✓
　`R_k(C)` 三个尺度：`k=1`: 5.58e-2／6.04e-2／5.68e-2｜`k=2`: 1.97e-3／2.26e-3／2.14e-3｜`k=3`: 9.22e-5／1.17e-4／1.03e-4 ⟹ **基本平坦**（±15–30%）✓
```

## §3 **三项目标现象逐项判定**

```
【① 有限阶消失（`R_k\to0` 于某特定 `k`）】 ⛔ **无**：`R_k` 对 `Z` 与 `C` **均呈平滑几何衰减**（`Z` 每阶约 ×30–50，`C` 每阶约 ×25–30）⟹
　这是**有限差分本身的标度**（`\Delta_h^k` 的固有量级），**不是"某阶消失"** ✓
【② 阶数突变（`A_{k+1}/A_k` 非平滑）】 ⛔ **无**：`A` 比值平滑（`Z`: 0.9445 / 0.9945 / 0.9993 / 0.9999 / 0.99999）✓
　⚠️ **诊断设计瑕疵（如实记录 ✓）**：`A_k=\mathrm{tr}Q^{(k+1)}/\mathrm{tr}Q^{(k)}` 在本归一化下**被 `Q_{00}` 主导**（`h=0.1` ⟹ 高阶差分量级 `\sim h^k` ≪ `Z`）⟹ `A_k\approx1` **近乎平凡** ⟹
　　**该诊断在本归一化下无判别力** ✗ ⟹ 正确替代＝在**相关阵迹**或**仅 `k` 阶对角元**上算 `A_k` ✓（此瑕疵**不改变** ①③ 的判定 ✓）
【③ ζ 特异跨尺度锁定】 ⛔ **无**：`Z` 的 `R_k` **随尺度漂移**；`C` 的 `R_k` **平坦** ⟹
　即**差别仍是"ζ 随尺度增长 vs 对照平稳"**（与 `CAS-1` **同型**）⟹ **不构成锁定** ✓
```

## §4 **判定（命中 STOP ⟹ 收手 ✓✓）**

```
【STOP 条件逐条核对】 所有 `k` **满秩**（最小相关阵特征值 2.47e-4 ≫ 0 ✓）｜谱随 `k` **平滑** ✓｜
　ζ 与 `C` 差别**仍只是尺度增长** ✓｜模式可归入**已知账本**（有限差分标度 ＋ 零点密度／`\log T` 局部相关）✓
【⟹】 $$\boxed{CAS\text{-}1B = \text{命中 STOP 条件} \Longrightarrow \text{立即收手}}$$ ✓✓
【⛔ 依令禁止】 **不得**因"`k=6` 还没发现"而继续 `k=8,10,20,\dots`（那**不再是 discovery，而是无止境参数扫描**）✓✓
```

## §5 唯一系统性对比（**如实记录，不升级** ✓）

```
【观察】 `Z` 与 `C` 的 `R_k` 之比随阶**几何衰减**：`k=2`: 0.206｜`k=3`: 0.0553｜`k=4`: 0.0178｜`k=5`: 0.00357｜`k=6`: 0.00107 ✓
【判定】 该对比**平滑、单调**，且属"**谱随 `k` 平滑变化**"类 ⟹ ⛔ **不是异常** ⟹
　依 STOP 规则**不做机制审计**（阶段二仅在出现"某有限阶精确／近精确消失且 `C` 无"时开启 ✓）；此处记录以备将来 ✓
```

## §6 【技术词回查】＋ 边界

```
技术词 高阶消失阶扫描 命中文件数=0    :: 
技术词 跨尺度锁定  命中文件数=0    :: 
```
【三分类标注】 `0` ⟹ 本档新增；`>0` ⟹ 档案已有（引用）✓
【边界】 ✗ 未证 RH／⛔ 未做机制命名／⛔ 未继续扩大 `k`／⛔ 未把负结果写成"不存在" ✓
【R7】 结论由数字驱动（`R_k`／`\kappa_k`／`A_k`／相关阵特征值 逐项打印 ✓）
```
