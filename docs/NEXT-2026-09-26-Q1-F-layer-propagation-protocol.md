已查地图：已跑 scripts/prework_map_check.sh K(10,1) Q=1 forced 传播 容量 ⟹ 执行自 Q1-LOCAL-CLASSIFICATION-2026-09-25 档；本档为**下轮操作协议＋停止判据**（唐先生 2026-09-25 23:47 指定）；不开新研究方向。
D0: 本档对象 = `Q=1` 分支的 forced 非码字传播表协议（既有对象；非新对象）
D1: 0（无新独立自由度；产出为协议、停止判据与硬目标重述）

# NEXT-2026-09-26 · Q=1 的 F₁→F₄ 去重传播表（协议）

## §1 目标（唯一）

```
对 **A/B 两分支分别**构造: `F_i` := 由局部结构**严格推出**的 L_i(z) 非码字集合（i=1..4）
$$\text{产出}: \begin{cases}\text{逐层 }|F_i|\ ✓\\ \text{交叉重叠 }|F_i\cap F_j|\ \text{（全部对）}\ ✓\\ \text{真正并集 }|F_{\le4}|\ ✓\\ \text{每点**原因标签**}\ ✓\end{cases}$$
```

**⛔ 明令禁止的算法**：不先把各层 forced 数**直接相加** ✗ —— 层间高度重叠，直加会制造**假突破** ✓（唐先生 ①）

## §2 方法（8 条硬规矩）

```
① 只做**严格推出**的集合（不含启发/概率）✓
② 逐层算 |F_i| ✓；③ 算全部交叉重叠 F_i∩F_j ⟹ 真并集 ✓；④ A/B **分开**，绝不混算 ✓
⑤ 每点贴**原因标签**: {b=2 | distance-1 | distance-2 | 上一层传播} ✓
⑥ 若**大量同型传播**出现 ⟹ 抽成**递推**，停止枚举 ✓
⑦ **理想结果不是 |F|>905**，而是某一层出现**容量超额**: 某局部纤维所需码字数 > 该层实际可供数 ✓
⑧ 种子（已备）: A 分支 L₃(z) 已有 **22** 个 forced 非码字 ✓；B 分支已有 **17** 个 ✓
⑨ **重点看 L₄**: 该层第一次让来自不同 y_ij / w 的禁止区域发生**非平凡交汇** ✓
```

## §3 ⛔ 停止判据（唐先生 ⑩–⑫，本档固化）

```
若 A/B 两分支都只得到**有限的局部禁集**且**远小于 905** ⟹ **立即停止该线** ✗（不得再堆层数 ✗）
⟹ 正式结论写成: **「Q=1 的局部结构不能提供全局容量矛盾」** ✓
⟹ 主战场**正式回到上界**: $$\boxed{A_1+A_2\le143}$$
（目的：**不再陷入"多算几层也许突然矛盾"的循环** ✓）
```

## §4 当前硬目标（不变）

```
$$\boxed{\textbf{硬目标}:\ A_1+A_2\le143\ (\text{等价于 }Q\ \text{的上界})}\quad\text{—— 若能证 }A_1+A_2\le143,\ \text{则与 }A_1+A_2\ge143\ \text{并合即得 }Q=1\ \text{且刚性}$$
```
**Q=1 / T=1 / 路径-匹配结构** 三者已基本完成其**结构性职责** ✓（本线可暂置 ✓）

## §5 边界

- 本档为**协议**，非结果 ✓；不含新数学主张 ✓；未跑程序 ✓
- 明日若触发 §3 停止判据，须**显式记录**"该线信息量不足"，不得留模糊状态 ✓

## 【技术词回查】（定稿前逐字输出）

```
技术词 传播表        命中文件数=0    :: 
技术词 容量超额     命中文件数=0    :: 
技术词 局部纤维     命中文件数=0    :: 
技术词 去重传播     命中文件数=0    ::
```

- **本档新增**（命中数=0）：`传播表`、`容量超额`、`局部纤维`、`去重传播`
- **档案已有（引用，不列为提出）**：—
- **通用词（不计）**：—
