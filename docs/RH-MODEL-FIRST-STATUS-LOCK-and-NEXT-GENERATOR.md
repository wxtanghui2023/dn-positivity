已查地图：命中 6 处 —— 先逐条判 已DEAD/已封/已登记；命中即引既有条目，不得开新案
　判定：6 命中皆为 **RH 侧既有条目**（如 `V244` U(1) 相位接口、`V258` §7 反例搜索）；**候选独立领域（刚性理论／
　唯一着色／编码界刚性等）本身不在 RH 地图覆盖范围内**（地图覆盖的是 **RH 搜索空间**）✓
　⟹ 本档为**状态锁 ＋ 新搜索生成器登记**（**非新案** ✓；**未开案** ✓）

# RH-MODEL-FIRST · **状态锁（MAP EXHAUSTED）＋ 新生成器（反转）**

**唐先生令（2026-09-22 20:47）**：
① 核验结果**接受**，且「**比"再找一个模型"更有价值**」✓；
② 结构性原因进一步定位到**四重必要条件（缺一不可）**：
　⭐ **非定义式局部律 ＋ 局部律的真实缺陷 ＋ 严格弱于 RH 的 `P` ＋ 独立下降 `D`** ✓✓；
③ ⭐ 关键锁定（照录）：「**RH 侧目前缺的不是一个漂亮的 failure predicate，而是能够产生 failure predicate 的"非平凡局部燃料"**」✓✓；
④ 状态锁：**RH-MODEL-FIRST = MAP EXHAUSTED FOR CURRENT CATEGORIES**；**不再继续生成 RH 四元组** ✓；
⑤ **生成器反转（照录）**：
　`RH → 找模型` **反转成** `独立结构 → 非平凡局部律 → 新全局不变量 → RH 接口` ✓✓
　且⚠️ **"RH relevance" 必须后置**：先问「**这个独立模型本身能否产生新数学结构？**」——**若不能，连 RH 都不碰** ✓✓；
⑥ 与既有原则一致：**独立问题 > adjacent asset > RH relevance** ✓

D0: 本档对象 = **RH-MODEL-FIRST 的状态锁与新生成器登记**（档案已有对象 `四重必要条件／独立问题优先` 的**状态与生成器变更**；非重命名、非新对象）
D1: 0
FREEZE-ACK: 零计算／零 Lean／**未开案** ✓

---

## §1 状态锁（**逐字** ＋ 边界 ✓）

```
【锁】 **RH-MODEL-FIRST = MAP EXHAUSTED FOR CURRENT CATEGORIES** ✓
　　　⟹ **不再继续从 RH 内部枚举载体／生成 RH 四元组** ✓
【更准确的状态陈述（照录）】 不是"RH 没有模型"，而是：
　　「**尚未发现满足四重必要条件的 RH-native model fuel**」✓✓
【锁的**准确含义**（防误读 ✗）】
　✗ 不是"RH 不可能有原生模型"（未证，也不该这样说 ✓）
　✗ 不是"这些载体已被证明无用"（是**已成覆盖**，非**已证不存在** ✓）
　✓ 是：**继续换载体大概率重新进入 `V194`／`V195` 的 repackaging 区** ✓✓
```

## §2 诊断：**瓶颈 = 缺燃料，不是缺候选** ✓✓

| | **Liouville／A2（有燃料）** | **RH 侧目前看到的结构** |
|---|---|---|
| 独立局部律 | ✓ 短程乘法 / 鸽笼定量界 | ⚠️ 大量局部关系是**定义／无条件恒等式**（有限处 `α_p ≡ 1` 等 ✓） |
| 可识别失败 | ✓ 失败集非空且**可压缩** | ⚠️ `P` 容易**直接变成 RH 强度** |
| 压缩 | ✓ 短代表／鸽笼给出定量界 `B` | ⚠️ **尚无独立压缩** |
| 严格下降 | ✓ `r<n`／`m′ ≤ m/2` | ⚠️ `D3/D4` 既有墙 |
| 最终矛盾 | ✓ failure-free 区（独立事实型 `E`） | ⚠️ **尚无新的 failure-free 区** |

```
【三条路暴露同一结构问题（照录）】
　· **Weil**：想从负性见证**向下压缩**，缺**算术侧恢复力**；补非局部/热/变分又触及**既有循环墙** ✓
　· **Nyman–Beurling**：自然的 `N`-尺度方向**已与逼近改善方向一致** ⟹ 向下压缩**无判别力** ✓
　· **Li 系数**：离轴数据把**首次失败位置锁住**；要下降必须改 `(β,γ)` ⟹ **失败谓词不再保持** ✓
【压缩诊断（照录）】 `RH-native descent` **≠** "不存在下降形式"，而是：
　「**尚无一个同时具备**｛非定义式局部律／严格弱于 RH 的失败对象／失败保持／独立下降·再入｝」✓✓
【与 TLDC 的区别（照录）】 TLDC **本身没有失败**（它已自证可迁移性）；**真正缺的是 RH 的输入结构** ✓
```

## §3 新生成器（**反转**；本档登记 ✓）

```
【旧生成器（停用）】 `RH 的缺口 → 找载体 → 问能否迁移 TLDC` ✗
【新生成器（启用）】 ⭐ `独立结构 → 非平凡局部律 → 新全局不变量 → （**RH 接口后置**）` ✓✓
【准入四条件（= 四重必要条件，作用于**独立**模型）】
　① **非定义式局部律**：该局部律必须**可独立证明**且**非"按定义成立"** ✓
　② **失败对象**：局部律的真实缺陷，且**不是目标的重编码** ✓
　③ **压缩＋失败保持＋严格下降/再入**：一个未被既有墙吸收的 `D` ✓
　④ ⭐ **新结构优先**：**先答"这个独立模型本身能否产生新数学结构？"**——
　　 **不能 ⟹ 连 RH 都不碰** ✓✓
【顺序（照录）】 独立问题 > adjacent asset > RH relevance ✓
```

## §4 首批候选**独立领域**（first pass ✓；**每个都须先地图核验**才可开案 ✓）

> ⚠️ 以下为本档**首批提议**（零计算、纸面）；**未开案**；每一项标注"**燃料是否可证／非定义式**"——**这正是新门槛的核心** ✓

```
【C-α】刚性理论（Laman／全局刚性）——⭐ 燃料最像的一个
　局部律：边数/次约束（`|E| = 2|V|−3`，且每子图 `≤ 2|V′|−3`）——**可证、非定义式** ✓✓
　失败对象：满足局部计数但**非刚性**的图（存在违反子图）✓
　压缩/下降：**pebble game／逐边归纳**（去掉一条边 ⟹ 更小图，仍在同类 ⟹ **再入** ✓✓）
　⭐ 新结构问题：更高维全局刚性（开放）／带对称性（群作用下的刚性）／曲面上的刚性 ✓✓
　⟹ 燃料 = ✓✓；新结构 = 可能 ✓ —— **建议列为第一核验对象** ✓

【C-β】唯一着色／局部识别（图着色局部-整体）
　局部律：局部邻域结构 ⟹ 全局唯一性（唯一 k-可着色图的刻画）——**可证、非定义式** ✓
　失败对象：非唯一可着色（存在"局部一样但全局不同"的着色对）✓
　压缩/下降：局部同构下的约化（尚未见标准下降）⚠️
　⭐ 新结构问题：唯一着色的**定量**判据／在特定图类上的刻画 ✓

【C-γ】编码界刚性（Hamming／MDS／完美码）
　局部律：球填充/局部界（Singleton、Hamming）——**可证、非定义式** ✓
　失败对象：达到界但**非完美/非 MDS** 的码（"界被触碰"型缺陷）✓
　压缩/下降：删列/缩短（**再入同类** ✓✓）—— 有真实下降结构 ✓
　⭐ 新结构问题：界**几乎**达到时的分类（近乎完美的码）／在非标准度量下的刚性 ✓✓

【C-δ】障碍类下降（Hasse／Brauer–Manin 型局部-整体）
　局部律：局部可解性（**可验证、非定义式** ✓）
　失败对象：局部处处可解而**全局不可解**（障碍类 `[T] ≠ 0`）✓
　压缩/下降：**descent**（真实且成熟 ✓✓）
　⚠️ 注意：**RH 侧对应（Brauer／K₂／H¹）已由 `V196`–`V198` 关闭** ✓ ——
　　本项**只作为独立问题**考察（**不带 RH 接口** ✓）；其"新结构"须自证 ✓
```

## §5 流程（依既有纪律；本档**不开案** ✓）

```
每个候选：**先地图核验 → 十三门槛 → （若涉 RH 才谈 L1/L2/L3）→ 再决定开案** ✓
⛔ 未过地图 ⟹ 不进入十三门槛（照录唐先生 20:43 令 ✓）
⛔ **不写程序**先于门槛；门槛未过 ⟹ 不写程序 ✓
⛔ 不得以"换载体／换失败对象／换模型名"制造新案 ✓（须**同时换生成器** ✓）
```

## §6 边界与回查（✗✓）

```
✗ 零计算／零 Lean／未开案／未写程序 ✓
✗ **RH relevance 已后置**（§4 各候选**不带** RH 接口；`C-δ` 明确标注 RH 侧对应已闭 ✓）
✗ 不声称 §4 各领域"一定能产生新结构"（那是**待答问题**，非结论 ✓）
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 燃料准则        命中文件数=0 ⟹ **本档新增** ✓
　技术词 非定义式局部律  命中文件数=0 ⟹ **本档新增** ✓
　技术词 生成器反转      命中文件数=0 ⟹ **本档新增** ✓
　技术词 独立模型优先    命中文件数=0 ⟹ **本档新增** ✓
　技术词 燃料            命中文件数=11 ⟹ **档案已有 ⟹ 引用**（如 ASSETS-REGISTRY／E30-2 等），不列为本档提出 ✓
【回查（先跑后写 ✓）】
　prework_map_check（刚性理论|Laman|唯一着色|局部-整体|Hasse|完美码|MDS）：命中 **6**，皆 RH 侧既有条目 ⟹ 非新案 ✓
　tech_word_check：燃料准则／非定义式局部律／生成器反转／独立模型优先 = **0 ⟹ 本档新增** ✓；燃料 = 11 ⟹ **档案已有 ⟹ 引用** ✓
【引用的既有条目】 `V194`（F1 两问／2×2）｜`V195`（生成器重置·机制优先）｜`V196`–`V198`（局部-整体 已闭）｜
　`C309T` §6（模板天然适配类 ①② ／RH 侧缺 `D3/D4`）｜`p26a17-meta-conclusion`（β 墙最深层）｜`DOOR5`｜`DOOR1` ✓
