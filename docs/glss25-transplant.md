# GLSS25 工具移植：S 矩 → A_j 结构（2026-08-23 20:00）

> 唐先生方向：GLSS25（PCC ⟹ 100% 临界线——S 矩桥梁）
> 状态：✅ 部分成功——发现 ΣΔu·(S−½) = O(1) 稳定结构——严格化卡相位均匀性

## 1. GLSS25 方法回顾

- **Proposition 1（GM78/Fujii——无条件）**：∫(Δ_U N)²dt = T(UL)² + ∫(Δ_U S)²dt + O(...)
- **关键恒等式**：Δ_U N − UL ≈ Δ_U S——短区间零点计数偏差 = S 增量
- **A_j（δ 累积）= −短区间计数偏差**——A_j 的矩 = Fujii 的 ∫(ΔS)² 矩（无条件）

## 2. 移植：Σwδ → Abel → S 加权和

```
Σwδ = −Σu_k·ΔS_k（u_k = w_k/N₀'——δ = −ΔS/N₀' 精确——corr 1.0）
Abel：Σu·ΔS = u_N·S_N − u_0·S_1 − ΣΔu_k·S_{k+1}
     = 主项（mean(S)=½）+ ΣΔu·(S−½)（残差）
```

## 3. 数值验证（关键发现）

### |Σ(S_j−½)| ≤ 0.75（mean(S)=½ 强形式）
```
k=1e2:   0.003
k=1e3:   0.174
k=1e4:   0.004
k=1e5:   0.745
k=1e6:   0.042
→ 不随 k 增长——O(1)（理论 O(log²γ) 不紧——数值强得多！）
```

### ΣΔu·(S_{k+1}−½) = O(1)（所有 n 稳定！）
```
n=100:  +0.39
n=500:  +0.35
n=1000: +0.26
→ 不随 n 增长——非常稳定
```

### Abel 恒等式精确（1e-16）✓

## 4. 严格化障碍

- Σ|Δ²u| = O(n)（n 依赖——Δ²u ~ n/(γ⁴log²γ)——k 求和收敛但常数 O(n)）
- |ΣΔu·(S−½)| ≤ Σ|Δ²u|·0.75 = O(n)——**不够 O(1)**
- **需要带符号抵消**（Δ²u 的振荡 × Σ(S_j−½) 的振荡）——**相位均匀性 = RH**

## 5. 结论

1. **GLSS25 工具部分移植成功**：
   - ✅ 发现 ΣΔu·(S−½) = O(1) 的**稳定结构**（数值——所有 n）
   - ✅ |Σ(S_j−½)| ≤ 0.75（mean(S)=½ 强形式——潜在新定理）
2. **严格化仍卡相位均匀性**（带符号抵消——Δ²u × Σ(S_j−½)）
3. **新认知**：mean(S)=½ 的误差实际 O(1)（不是 O(log²)）——**这是潜在的无条件新定理**（|Σ(S_j−½)| 有界）——可能可证（用 Selberg 矩——GLSS25 的工具）！

## 6. 下一步（候选）

1. **证明 |Σ(S_j−½)| = O(1)**（mean(S)=½ 强形式）——用 Selberg 矩/GLSS25 方法——**潜在无条件新定理**！
2. **ΣΔu·(S−½) = O(1) 的严格化**——需要带符号抵消（相位均匀性）

## 7. 文件

- scripts/glss25_transplant.py（移植验证）
- scripts/du_resid_proof.py（Abel 严格化路径）
