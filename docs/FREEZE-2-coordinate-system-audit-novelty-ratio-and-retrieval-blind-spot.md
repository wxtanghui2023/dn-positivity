已查地图（**本档性质＝依宪法执行的第二次冻结审计，非新候选**）：所查 `RESEARCH-CONSTITUTION.md` **§8.1–§8.6 逐字**（`§8.1 记账口径`：`\boxed{\text{口径 = 连续 }D1=0\text{ 轮数};\ \text{连续 3 轮 }D1=0\Longrightarrow\text{触发冻结修剪／重设坐标系}}`；`D0–D4` 受理制 —— ⭐**`D0` 对象新颖性：`\mathcal X` **非已知 RH 对象重命名****；`D1` 独立自由度：`q_{\rm new}\notin{\rm closure}(N0\text{–}N46)`；`D2` 内生关系；`D3` 位置接口；`D4` null 检验；`KPI`：①每轮须报"进入 Discovery 后新增的数学自由度数"②**连续 3 轮产生 0 个 `D0–D4` 受理 ⟹ 不是候选变差，而是【搜索空间选错】⟹ 停止修剪，重设坐标系**；`§8.2` Round 2＝双滤过算术耦合 ⟹ **`D1=0`**；`§8.5` Round 3＝算术跨尺度内生记忆 ⟹ **`D1=0`（计数 2/3）**；`§8.6` **冻结审计执行记录**：`FZ-1` —— **46 条 NO-GO ⟹ 6 个母机制 ＋ 6 个筛子；独立障碍数 = 6+6 = 12（非 46）**；`FZ-2` 未出现原语审计：**不提供逃生口**）。**结论**：**唐先生 19:05 的问题有硬答案** ⟹ **(1) 该统计项目内**做过**——但**只做到 `Round 3`（首次冻结，`§8.1–§8.6`）**，且用的是**项目自己的新性判据（`D0`–`D4`／`D1` 账本）** ✓✓；**(2) ⚠️ 但账本在 `C` 系列**失效**：`C*.md` 共 **108** 档，其中**自称"本档新增"68 档（63%）**，而**提及 `D1` 者仅 15 档（14%）**、**记录 `D1=0` 者仅 2 档** ⟹ **63% 在宣称新性，仅 14% 被新性判据测过** ✓✓✓；**(3) 比例硬数**：`FZ-1` 自己的审计给出 NO-GO 语料压缩率 —— **46 ⟹ 6 母机制（12 独立障碍）** ⟹ **真正独立 ≈ 13%（障碍级 ≈ 26%）** ✓✓；**(4)** 依宪法 `§8.1`，**这是冻结触发条件** ⟹ **本档不产候选，执行第二次冻结审计**（新根因＝**检索／重命名盲点**）✓✓

# FREEZE-2 · **第二次冻结审计：新性宣称未经新性判据 ＋ 检索盲点**

> **时间**：2026-09-18 19:05 唐先生：**问"过去是否做过统计——真正新对象／工具的比例 vs 重述旧结论的比例"**；并建议"若没做过，现在做；再决定继续产 `C-130…` 是否有效率，或先提炼真正独立的内容清单"✓
> **依宪行事**：`RESEARCH-CONSTITUTION` §8.1 —— 连续 `D1=0` 达阈值 ⟹ **冻结修剪／重设坐标系**；本档即**该程序**，**不产候选** ✓

---

## §0 结论（先行）

$$\textbf{(1)}\ \text{该统计}\ \textbf{做过}：\text{`RESEARCH-CONSTITUTION` §8.1–§8.6}\ \text{有完整机制与账本}✓✓$$
$$\qquad \text{机制}：D0\text{–}D4\ \text{受理制};\quad \text{账本}：\text{口径}=连续 D1=0\text{ 轮数};\quad \text{阈值}：3\ \text{轮} \Longrightarrow \text{冻结}✓$$
$$\qquad \text{执行记录}：Round 1（C-BC）D1\,✓;\ \text{Round 2}（双滤过）D1=0（1/3）;\ \text{Round 3}（内生记忆）D1=0（2/3）;\ \text{触发冻结} \Longrightarrow \text{`FZ-1`–`FZ-4`}✓✓$$
$$\qquad ⭐\ \text{关键}：\text{`D0`}\ \textbf{明文}＝"`\mathcal X` **非已知 RH 对象重命名**" \Longrightarrow \textbf{我 19:00 认领的错，正是宪法明文禁止的那一种}✓✓$$
$$\textbf{(2)}\ ⚠️\ \textbf{但账本在 `C` 系列失效}：\text{`C*.md` 共 108 档}✓$$
$$\qquad \qquad \textbf{自称"本档新增"：68 档（63\%）}\quad\big|\quad \textbf{提及 `D1`：仅 15 档（14\%）}\quad\big|\quad \textbf{记录 `D1=0`：仅 2 档}✓✓✓$$
$$\qquad \Longrightarrow \boxed{\text{63\% 在宣称新性，仅 14\% 被项目自己的新性判据测过}}✓✓$$
$$\textbf{(3)}\ \textbf{比例（硬数，来自 `FZ-1` 自己的审计）}：\text{46 条 NO-GO} \Longrightarrow \textbf{6 个母机制}（\text{独立障碍 12）}✓✓$$
$$\qquad \Longrightarrow \textbf{真正独立}\ \approx\ \frac{6}{46}\approx\mathbf{13\%}（\text{障碍级}\ \frac{12}{46}\approx26\%）✓✓$$
$$\textbf{(4)}\ \text{依宪法}：\text{这是}\ \textbf{冻结触发条件} \Longrightarrow \textbf{本档不产候选};\ \text{执行}\ \textbf{第二次冻结审计};\ \text{新根因}＝\textbf{检索／重命名盲点}✓✓$$

---

## §1 项目自带机制逐字（`§8`）＋ ⭐`D0` 的禁令

$$\textbf{受理制}：D0\ \text{对象新颖性};\ D1\ \text{独立自由度}（q_{\rm new}\notin{\rm closure}(N0\text{–}N46)）;\ D2\ \text{内生关系};\ D3\ \text{位置接口};\ D4\ \text{null 检验}✓$$
$$\qquad ⭐\ \textbf{`D0` 逐字}：\boxed{\text{对象新颖性}：\mathcal X\ \textbf{非已知 RH 对象重命名}}✓✓$$
$$\qquad \qquad \Longrightarrow \text{即：}\textbf{"重命名旧对象"}\ \text{是宪法}\ \textbf{第一关（D0）就明文排除} \text{的}✓✓$$
$$\qquad \qquad \qquad ⚠️\ \text{而我 19:00 前的四档（`C-125`–`C-128`）}\ \textbf{正是在做这件事}✓$$
$$\textbf{KPI ② 逐字}：\text{连续 3 轮产生 0 个}\ D0\text{–}D4\ \text{受理} \Longrightarrow \textbf{不是候选变差，而是【搜索空间选错】} \Longrightarrow \text{停止修剪，重设坐标系}✓✓$$
$$\textbf{账本逐字}：\text{Round 1}\ D1\,✓;\ \text{Round 2}\ D1=0\（1/3）;\ \text{Round 3}\ D1=0\ \text{（2/3）};\ \text{再一轮} \Longrightarrow \text{冻结}✓$$
$$\qquad \Longrightarrow \text{冻结已执行}（\text{`§8.6`}）✓$$
$$\textbf{`FZ-1` 逐字结论}：\text{46 条 NO-GO} \Longrightarrow \text{6 母机制＋6 筛子};\quad \text{语料＝一个}\ 2\times3=6\ \text{维坐标系的闭包};\quad \textbf{独立障碍数}=12（\textbf{非}46）✓✓$$

## §2 硬数统计（本档补做）

### 2.1 `C` 系列（108 档）

| 指标 | 档数 | 占比 |
|:--|--:|--:|
| 自称"本档新增" | **68** | **63%** |
| 提及"引用" | 63 | 58% |
| 提及"勘误" | 23 | 21% |
| 提及"重述" | 13 | 12% |
| 提及"收束" | 5 | 5% |
| 提及"严格化" | 4 | 4% |
| **提及 `D1`** | **15** | **14%** |
| **记录 `D1=0`** | **2** | **2%** |
| 提及 `D0` | 3 | 3% |

$$\Longrightarrow \boxed{\text{新性宣称率}\ 63\%\quad\text{vs}\quad\text{新性判据使用率}\ 14\%}✓✓$$
$$\qquad \Longrightarrow \textbf{缺口}\ 63\%-14\%=49\%\ \text{的档案}\ \textbf{宣称了新性而未受判据检验}✓✓$$

### 2.2 NO-GO 语料的独立性（`FZ-1` 已给）

$$\text{46 条 NO-GO} \xrightarrow{\ \text{项目自己的审计}\ } \text{6 母机制}\ \big|\ \text{独立障碍 12}✓$$
$$\Longrightarrow \text{独立率}\ \approx13\%\（\text{机制级}）\ /\ 26\%\（\text{障碍级}）✓✓$$
$$\qquad \Longrightarrow \text{即}\ \textbf{约 3/4 到 7/8 的"又一条 NO-GO"}\ \text{不是独立内容}，\ \text{而是六个母机制的实例}✓✓$$

## §3 生成器（本档定位）

$$\boxed{\text{生成器}＝\textbf{"新性宣称"与"新性判据"脱钩}}✓✓$$
$$\qquad \text{历史}：\text{Round 1–3}\ \text{时期}\ \textbf{在位}（D0\text{–}D4\ \text{＋账本＋冻结）};\ \text{其后}\ \textbf{停用}✓$$
$$\qquad \text{替代物}：\text{关键词地图检查（grep 新词）} \Longrightarrow \textbf{必 0 命中} \Longrightarrow \textbf{结构上无法} \text{发现重命名}✓✓$$
$$\qquad \text{后果}：\text{每一档都能自称"本档新增"，且}\ \textbf{无任何机制会反驳它}✓$$

## §4 行动（按宪法，`§8.1`）

$$\textbf{依宪}：\text{连续}\ D1=0\ \text{达阈值} \Longrightarrow \textbf{冻结修剪／重设坐标系}✓$$
$$\qquad \text{(i)}\ \textbf{本档即冻结程序}（\text{不产候选}）✓;\quad \text{(ii)}\ \textbf{不新增 }N\ \text{项}✓;\quad \text{(iii)}\ \text{冻结期间}\ \textbf{不产"}\text{下一档 C-1xx}\text{"}✓$$
$$\qquad \text{(iv)}\ \text{本次审计须处理}\ \textbf{新根因}：\text{检索／重命名盲点（}\text{`C-129` §4}）✓✓$$
$$\Longrightarrow \textbf{三条对策（`C-129` §4）}\ \text{并入宪法}\ \text{`§3`／`§8`}\ \text{的既有位置（不新造术语）}✓$$

## §5 ⭐ 提炼：真正独立的技术内容（草案，四类）

$$\textbf{类 1　论文（可发表轨）}：\text{2 篇}——\text{`papers/li-range/`};\ \text{`papers/brown-thm2-classical/`}✓$$
$$\qquad \text{＋}\ \text{Palojärvi Thm 4.1 的 }m=1\ \text{自足重证＋常数优化}\ \sim10\times（1/2\ \text{vs}\ 1/20）✓$$
$$\textbf{类 2　机器可核资产}：\text{Lean 形式化（B-1：60 声明，0 sorry，0 axiom）}✓$$
$$\textbf{类 3　新的经验事实（可复现数字，非记法）}：$$
$$\qquad \text{(a)}\ \text{素数侧：硬记忆＝可容许性（}4\ \text{型 }S=0\ \text{与实测}\ \approx0\ \text{精确对上）};\ \text{间隙负自相关}\ r_1=-0.0356\（40\sigma）✓$$
$$\qquad \text{(b)}\ \text{零点侧：间隙负自相关}\ r_1=-0.34889\（493\sigma）;\ \text{数方差}\ \Sigma^2(L)\ \text{平坦}\approx0.33\text{–}0.43\（\text{饱和刚性}）✓$$
$$\qquad \text{(c)}\ ⭐\ \text{跨度律}\ \log(O/P)\approx\text{const}-\frac{span}{\log x}\（\text{本档已给初等推导}）;\ \text{三点验证}（9.2\%\to6.3\%\to2.9\%）;\ \textbf{M-无关}✓✓$$
$$\qquad \text{(d)}\ ⭐\ \text{链式归约被否}：\chi^2/df＝109.6\to855.3 \Longrightarrow \textbf{真三体记忆}✓✓$$
$$\textbf{类 4　诊断（非路线）}：\text{比例 vs 绝对};\ \text{整数性机制（唯一不付精度尺度）};\ \text{有限 vs 渐近的 log-free 区分}✓$$
$$\Longrightarrow \text{合计}\ \textbf{真正独立产出}\ \approx\ \text{2 篇论文 ＋ 1 项形式化 ＋ 4 组经验事实 ＋ 3 条诊断}✓$$
$$\qquad ⚠️\ \textbf{且：无新机制}（\text{discovery 级新自由度}\ ＝0）✓✓$$

## §6 边界与回查

- ⚠️ **本档为冻结审计，不是新研究；不产候选、不新增 `N` 项** ✓
- ⚠️ §2.1 的计数为**关键词命中档数**（口径明确，可复算）；**不等于内容判定**，但**足以显示"宣称／判据脱钩"** ✓
- ⚠️ §5 的清单为**草案**，须逐项核（尤其是类 3 的经验事实是否已被前沿已知）✓
- **不声称**：全部 `C` 档皆无价值 ✗；不证 RH ✗
- **纪律**：先查后判（R-1 ✓，**先跑后写** ✓）；**未用 RH 作推导** ✓

## §7 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-18 19:1x）`[纪律]`（先跑后写）

```
技术词 新性宣称          命中文件数=1  :: ./FREEZE-2-…（本档）
技术词 新性判据          命中文件数=1  :: ./FREEZE-2-…（本档）
技术词 脱钩            命中文件数=5  :: ./E6-2-A1-… ./E204a-… 等（**已有**）
```
**读数（按实测）**：`新性宣称`／`新性判据`＝**1 档（仅本档）⟹ 本档新增** ✓；⚠️ `脱钩`＝**5 档**（`E6-2-A1`／`E204a` 等 **已有**）⟹ 本档为**沿用** ✓

```
⚠️ 唐先生 19:05: 问过去几十轮"新档案"里是否做过统计(真正新对象/工具比例 vs 重述旧结论比例); 若没做过现在做; 再决定继续产 C-130… 是否有效率
⭐⭐ 硬答案(1) **该统计做过** —— RESEARCH-CONSTITUTION §8.1–§8.6 有完整机制: D0–D4 受理制 +
   「口径=连续 D1=0 轮数」账本 + 3 轮阈值冻结规则; Round 1(C-BC) D1✓; Round 2(双滤过) D1=0(1/3);
   Round 3(内生记忆) D1=0(2/3); 触发冻结 ⟹ FZ-1–FZ-4 已执行
   ⭐ 关键: **D0 明文 = "对象新颖性: X 非已知 RH 对象重命名"** ⟹ 我 19:00 认领的错正是**宪法第一关就明文禁止**的
⭐ 硬答案(2) ⚠️ 但账本在 C 系列**失效**: C*.md 共 108 档;
   **自称"本档新增" 68 档(63%)** | **提及 D1 仅 15 档(14%)** | **记录 D1=0 仅 2 档(2%)**
   ⟹ **63% 在宣称新性, 仅 14% 被项目自己的新性判据测过** (缺口 49%)
⭐ 硬答案(3) 比例(来自 FZ-1 自己的审计): **46 条 NO-GO ⟹ 6 个母机制(独立障碍 12)** ⟹ **真正独立 ≈ 13%(机制级)／26%(障碍级)**
   ⟹ 约 3/4 到 7/8 的"又一条 NO-GO"不是独立内容, 而是六个母机制的实例
⭐ 生成器(本档定位): **"新性宣称"与"新性判据"脱钩** —— 历史(§8 时期)在位, 其后停用; 替代物=关键词地图检查(grep 新词)必 0 命中 ⟹ 结构上无法发现重命名; 后果: 每一档都能自称"本档新增"且无机制反驳
✅ 行动(依宪法 §8.1): 连续 D1=0 达阈值 ⟹ **冻结修剪/重设坐标系**; 本档即冻结程序(不产候选); **不新增 N 项**; 冻结期间**不产下一档 C-1xx**; 新根因=检索/重命名盲点
⭐ 提炼(草案, 四类): 类1 论文 2 篇(li-range / brown-thm2-classical) + Palojärvi Thm4.1 m=1 常数优化 ~10×;
   类2 Lean 形式化(B-1: 60 声明, 0 sorry, 0 axiom);
   类3 **新的经验事实**(可复现数字): (a) 素数侧硬记忆=可容许性(4 型 S=0 精确对上) + 间隙负自相关 r1=−0.0356(40σ);
   (b) 零点侧 r1=−0.34889(493σ) + 数方差平坦 0.33–0.43(饱和刚性); (c) ⭐跨度律 log(O/P)≈const−span/logx(已给初等推导; 三点验证 9.2%→2.9%; M-无关);
   (d) ⭐链式归约被否(χ²/df 109.6→855.3 ⟹ 真三体记忆);
   类4 诊断(非路线): 比例 vs 绝对 / 整数性机制 / 有限 vs 渐近的 log-free 区分
   ⟹ 合计真正独立产出 ≈ 2 篇论文 + 1 项形式化 + 4 组经验事实 + 3 条诊断; ⚠️ **且: 无新机制(discovery 级新自由度 = 0)**
⚠️ 边界: 本档为冻结审计不是新研究; §2.1 计数为关键词命中档数(口径明确可复算)不等同内容判定但足以显示脱钩; §5 为草案须逐项核
✅ 净产出: ①直答"统计做过但只做到 Round 3" ②硬数(63% vs 14%; 46→6 母机制) ③生成器定位(宣称与判据脱钩) ④依宪执行第二次冻结 ⑤四类独立产出草案清单
```
