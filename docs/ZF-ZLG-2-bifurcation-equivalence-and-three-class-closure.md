已查地图：命中（`ZF-ZAI-2`（局部几何封口）／`ZF-G5G6-DIM/CAN/PL/VI` 四门／`E-44`（inertia 只给 extensive）／`§89`（分支方向检查）／`S6`／第一断裂 本线自档与既有封存）⟹ **引用，不开新案** ✓
D0: 本档对象 = 「离散算术分岔」与「canonical anomaly」的等价定理 ＋ 方向检查 ＋ 三类穷尽 ＋ 自生分岔的归约
D1: 0 （`[REVIEW]` 轮次：等价性与分类推导，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`ZF-ZLG-2`：分岔路线 ＝ anomaly 结构的换壳（等价定理＋三类穷尽）**（本档全为自行推导 ✓✓）

## §1 **采纳您的 `ZLG-1` 封口（落档 ✓）**

```
【封口内容】 「局部 jet → 离散算术」CLOSED：`J_k` 有真离轴敏感性｜`\operatorname{FP}\xi'/\xi` 有精确零点接口｜residue/argument principle 可产整数 —— 但**三者无法同时**给出 $$\boxed{\beta\text{-sensitive finite-capacity arithmetic event}}$$ ✓✓
【三接口表（落档）】 `J_1`（`\mathbb C`，β✓，容量✗）｜`\operatorname{Re}J_1`（`\mathbb R`，β✓，✗）｜residue `f=1`（`\mathbb Z`，β✗）｜residue `f=s`（`\mathbb C`，β✓，✗）｜argument principle（`\mathbb Z`，弱，✗）｜**有限素数截断**（β✓，有限输入但不精确）｜**无限素数展开**（β✓，无限）⟹ $$\boxed{\text{有限}\Rightarrow\text{不精确};\ \text{精确}\Rightarrow\text{无限}}$$ ✓
```

## §2 ⭐⭐⭐ **等价定理（本档核心之一）**

```
【命题】 「连续参数 `s\mapsto` 离散算术结构发生分岔」**等价于**「canonical anomaly locus（`\mathcal D`）」 ✓
【证明（两步）】 **(1)** 分岔的定义＝存在**局部常值**的离散不变量 `\operatorname{Inv}(\mathcal X(s))`，且其跳跃轨迹 `Z` 非空 ✓
　**(2)** 令 `\mathcal M`＝该对象族（参数空间），`F`＝判别式/跳跃条件，则 `Z=F^{-1}(0)=\mathcal D` 正是**异常轨迹** ✓ ∎
【⟹ 结论】 您要的"分岔"表述与 `§95–§118` 的架构**完全同构** ⟹ $$(\mathcal M,\overline{\mathcal M},\tau,\mathcal D,F,\Phi)$$ 五元组**直接适用** ⟹ $$\boxed{\text{「分岔」不是新入口，而是同结构的\textbf{换壳}}}$$ ✓✓
```

## §3 ⭐⭐ **方向检查（本档；与自然几何反向）**

```
【算术对象的跳跃轨迹形态】 离散不变量（秩/阶/Euler 数/分支型）在参数空间上**局部常值**，跳跃轨迹是**Zariski-闭**的真子簇（典型为**除子**，余维 1）✓
【而要求是】 `Z` 须**含全部离线零点**、**避开全部在线零点** ⟹ 即须一个**代数集分离两个零点族** ✓
【⛔ 非自然性】 而这与 `§89` 的**分支方向检查**同为反向：自然实现中"奇异侧"通常是**在线/退化**侧，而要求是**离线侧为奇异** ⟹ 与自然几何**相反** ✓✓
【⟹ 增量结论】 故「分岔」表述在**方向**上同样要求一个反自然结构 ⟹ 与 `RPC` 三类同型（人为并置）✓
```

## §4 ⭐⭐ **三类穷尽（本档核心之二）**

```
**(i) anomaly / special-point 类**（Zilber–Pink/André–Oort 型）：`DIM`✓ `CAN`✓ `PL`✓，`VI`＝**GAP** ✓
**(ii) inertia / rank 类**（Weil 形式、不定型→惯性→秩）：**已封** —— `E-44`：只给 extensive 预算（有计数、无有限预算）✗ ✓
**(iii) monodromy / branch 类**：**已封** —— `DIM` FAIL（分支轨迹＝除子）＋ `PL` FAIL（单值性沿族）✗ ✓
【⟹ 穷尽结论】 「连续参数 → 离散算术结构」范式下的**全部自然实例化**落入上述三类 ⟹ $$\boxed{\text{无新类}}$$ ✓✓
```

## §5 ⭐⭐ **"自生分岔"（不用外部对应）的归约**

```
【唯一真正未攻击的形态】 若放弃外部对应 `\Phi`，要求 `\mathcal X(s)` **自身**在 `\zeta(\rho)=0` 处分岔 ⟹ 则需
　$$\boxed{\text{一个\textbf{算术}条件，其消失轨迹恰为 } \{\zeta=0\}\cap\text{带内}}$$ ⟹ 即需**零点的算术刻画** ✓✓
【⟹ 与既有墙同向】 `S6`（局部可容许性不能全局锁定）＋**第一断裂**（`\alpha_p\equiv1`、无内生算术动力学）⟹ **无已知的零点算术刻画** ⟹ 非不可能，但须**新的零点算术描述** ⟹ **同一 GAP** ✓✓
【⟹ 净判定】 「自生分岔」不提供捷径；它把 GAP 归约回"零点的算术描述"——比原 GAP **更强**（要求更多）✓
```

## §6 **结论与账本**

```
【本档判定】 $$\boxed{\text{ZLG-2：分岔路线 ＝ anomaly 换壳 } \Rightarrow\ \text{无新入口};\ \text{residual GAP 被包围但未闭合}}$$ ✓✓
【residual GAP 的现状】 已被**三面围住**：右侧（`event\Rightarrow`预算）干净 ✓；左侧两条自然入口（显式公式、局部几何）判死 ✓；「分岔」范式**三类穷尽且全部落回** ✓ ⟹ ⟹ 唯一未攻击形态＝「自生分岔」＝**零点的算术描述**（比原 GAP 更强）✓

| 层 | 状态 |
|:--|:--|
| `EDR` / `RPC-1` 引擎 | **PASS / 保留** |
| 离轴 → 稀有算术集（三类） | **CLOSED** |
| 显式公式 / `\psi(x)-x` | **CLOSED** |
| 局部 jet（`ZLG-1`） | **CLOSED** |
| **分岔范式（本档）** | **CLOSED（等价换壳；三类穷尽）** |
| 自生分岔（零点算术描述） | **OPEN（比原 GAP 更强）** |
| finite event → finite zeros | **已有预算工具可接** |

【边界】 ⚠️ §4 的 `(ii)` 引 `E-44`、`(i)` 引 `DIM/CAN/PL/VI` 均为本线既有档（档级）；⛔ 未制造候选／未启动搜索／未改状态；⭐ §2 等价定理、§3 方向检查、§4 三类穷尽、§5 归约均为**本档自行推导** ✓
```
