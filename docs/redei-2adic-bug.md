# Rédei 2-adic 规范化——严格实现进展与关键 bug 定位

> 2026-09-07 · 唐先生指导后——O/4O 严格实现

## 已完成（严格版——）
- O_a/4O_a 有限环运算（ω 基——ω² = ω−c——真平方测试——）
- 单位测试（模 2：split → F₂×F₂——inert → F₄——）
- 结果：多 t = 0（唯一性 OK——）但"无 t" 21/40——仍高

## 关键 bug 定位（新——）
**"twist by t=2" = 平方类乘 [2]——不是坐标乘 2！**

- 类运算：[4] = 1——所以 [2β] = [2]·[β]——"乘 2"在类层面 = "除 2"的效果
- 我的实现（坐标乘 2——）在 β 被 2 整除时方向反了（2β 更被 2 整除——）

## 具体反例（关键多解测试——）
(a,b,c) = (3457, 2081, 3361)——a ≡ 1 mod 8（split——）：
- β₁ = (49449, 841, 8)——ω 坐标 (48608, 1682)——除 2 到 (24304, 841)——(0,1) mod 2
  - split 单位条件：u 与 u+v 都奇——u=0 偶——非单位——不能再除（841 奇——）
  - ⟹ 我的实现给 t = []（无——）
- β₂ = (5901, 100, 11)——t = [1]
- **两解不一致——规范化仍未正确**

## 数学难点（待解决——）
split 情形（a ≡ 1 mod 8——）的**双 2-adic valuation**：
- 2O = q₁q₂——v_{q1} + v_{q2} = 2·v₂(z)——共轭交换——奇偶相同
- 单位化需同时归零两个 valuation——不是简单"坐标除 2"
- β₁ 的 ω 坐标 (24304, 841)——(0,1) mod 2——q₁ 非单位（u=0——）q₂ 单位（u+v=1——）
  - 但共轭对称应该 v1 ≡ v2 (mod 2)？——(0,1) 的 valuation (≥1, 0)——奇偶不同？
  - 矛盾？——除非 β₁ 的"类"需要别的单位化——或者我的 φ₁/φ₂ 映射方向错

## 请求指导
1. split 情形（a ≡ 1 mod 8）的 valuation parity 正确计算方法（v_{q1}, v_{q2}——用 φ₁: ω→0, φ₂: ω→1——？——是否正确——？）
2. "类乘 [2]"的正确实现（如何对 [tβ] 找单位代表——当 t 改变类的 [2]-部分——）
3. 或——直接数值验证"F^t 在 2 未分歧"（局部域判别式——）替代代数单位化？

## 文件
- scripts/redei_tmin_strict.py——O/4O 严格版（多 t=0——但无 t 仍 21/40——）
- external_refs/stevenhagen_redei.pdf——原文（已给唐先生路径——）
