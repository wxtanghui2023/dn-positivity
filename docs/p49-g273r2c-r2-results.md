# P49-G2.7.3-R2c-R2：三分块 Sector Decomposition 数值结果（第一版）

> 2026-09-02 14:12 · QW_λ 精确重建（3.19）· 方向性发现 · 精度待核对

## QW_λ 精确结构（论文 3.19——已确认）
- **QW_λ(f,f) = ∫|f̂(t)|²·2∂_tθ(t)/2π dt + 2Re[f̂(i/2)f̂̄(−i/2)] − Σ_{1<n≤λ²}Λ(n)⟨f|T(n)f⟩**
- W_{0,2}(F) = f̂(i/2) + f̂(−i/2)（**"0,2" = s = ±i/2——非 0 和 2**）
- −W_R = W∞ = ∫|f̂|²·2∂_tθ/2π（∂_tθ 乘子——Riemann-Siegel——θ(t) = −(t/2)logπ + ℑlogΓ(¼+it/2)）
- −Σ_p W_p = −ΣΛ(n)T(n)（von Mangoldt——n^{−1/2}[(f**g)(n)+(f**g)(n⁻¹)]）
- T(n)：⟨V_k|T(n)V_m⟩ = n^{−1/2}[(V_k**V_m)(n)+(V_k**V_m)(n⁻¹)]——(V_k**V_m)(n) = e^{2πim·logn/L}(1−e^{2πi(k−m)logn/L})/(2πi(k−m))（k≠m——区间 [logn, L]）——k=m: e^{2πik·logn/L}(L−logn)/L

## 数值结果（三分块——λ=3）
| N | W_{0,2} Δ | −W_R Δ | −ΣΛT Δ | total Δ |
|---|---|---|---|---|
| 4 | −0.404 (odd) | ≈0 | **+1.699 (EVEN)** | **+0.0004 (EVEN)** |
| 6 | −0.424 (odd) | ≈0 | **+1.342 (EVEN)** | **−0.018 (odd)** |

## ⭐ 方向性发现
1. **素数项（−ΣΛT）单独给强 EVEN ground（Δ = +1.7/+1.3——远大于其他项）——情况 ① 的初步证据**（单独 prime sector 已有 Δ>0——纯算术 variational inequality 有希望！）
2. **W_{0,2}（秩 2 交叉项）给 odd ground（Δ ≈ −0.4）**——抵消素数项的 even 倾向
3. **−W_R（∂_tθ 乘子）几乎 sector 对称**（Δ ≈ 0）
4. **total 不稳定**：N=4 EVEN（+0.0004 极微弱）——N=6 odd（−0.018）——⚠️ 翻转

## ⚠️ 诚实限制（total 不稳定的可能原因）
1. **M2 积分精度**（∂_tθ × sin²(tL/2) 振荡——trapz 细网格——近极点区 t≈2πk/L 需精确——可能精度不足）
2. **W_{0,2} 极化约定**（2Re[f̂(i/2)f̂̄(−i/2)] 的矩阵元——我实现 2·f̂_k(i/2)·conj(f̂_m(−i/2)) + Hermitian 化——需核对因子/共轭约定）
3. **total Δ 小（0.018 vs 项 ~2）——灾难性抵消**（P34 J2+J3 教训——需高精度）
4. **N=6 翻转**可能是精度伪影——非结论

## 判定
- **方向性结果**：素数项 even 倾向（Δ>0 稳定——两 N 都 +1.3~1.7）——**这是"情况 ①"（纯算术 variational inequality）的初步证据——CCM 的 even ground 可能主要来自素数项（Weil 显式公式的算术侧）**
- **total 未定**（精度限制——需高精度 M2 积分 + W_{0,2} 约定核对）
- ⚠️ 不宣称为结论——标记"初步——精度待核对"

## 下一步候选
- (a) 精度提升（M2 的解析积分——∂_tθ 缓变 + sin² 振荡——用振荡积分技巧/半周期平均 + 近极点精确——W_{0,2} 极化核对——重跑 total）
- (b) 接受"素数项 even 倾向"方向性发现（情况 ① 候选——但——total 待定——需高精度确认）
- (c) 唐先生指示
