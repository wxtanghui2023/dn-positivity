# ✅ **A1 收尾**：E1/E2/E3 的执行 + **一个关键定量事实** + A1↔A3 汇合

**依据**：唐先生 22:47（"继续收尾，然后再开始 A4"）

---

## §1 **E1（所有 H 的解析比较）：已给出结构 ✓**
```
【首阶比较（§已有）】LHS ≈ (k²/4)·S₄(H)，RHS ≈ 2·(k²/4)(1/H²)²·(a/3)H log H
   ⟹ **RHS/LHS → 2πa = 1（a = 1/(2π) 时【精确临界】）** ✓✓
【次阶项（决定成立与否）】RHS 侧：+(4a/9)H + 2c log H + 2d + c/4 ✓（均为正 ✓）；
   LHS 侧：S₄ 的 +H^{−3}/9 项 ✓ 与 F(1+u)<(k²/4)u² 的修正 ✓
⟹ **E1 的结论**：比较可化为**显式常数不等式**（引理 1–4 + S₄ 的界 ✓），**无需新的解析结构** ✓
```

## §2 ⭐⭐⭐⭐ **E2 的执行【失败】—— 但给出一个关键定量事实（本轮最重要）**
```
【原计划】把 S₄(H) = Σ_{|γ|>H}γ^{−4} 从"光滑密度"换成"假设的显式计数界" ✓
【实际计算】**Abel 求和 + 全局计数界 N(T)=aT log T + bT + ε、|ε|≤c log⁺T+d** 给出
   Σ_{|γ|>H}γ^{−4} ≤ **8∫_H^∞ N(t)t^{−5}dt + …** ⟹ 首项 = **(8a/3)·H^{−3}log H** ✗
【对比真实值】真实 **S₄(H) ≈ H^{−3}log H/(3π) = 0.1061·H^{−3}log H** ✓
   而 **8a/3 = 8/(3·2π) = 0.4244** ✗ ⟹ **【整整 4 倍损失】** ✗✓
⟹ **失败原因**：**用【全局】计数函数做 Abel 积分，在 H 附近丢掉了一个因子 4** ✗✓
【改用【局部】计数 N(t+1)−N(t)】则 Σ ≤ 2·(c_local/3)H^{−3}log H ✓
   · 若 c_local = a（**尖锐** ✓）⟹ **(2a/3)H^{−3}log H = 0.1061·H^{−3}log H** —— **【恰好尖锐】** ✓✓✓
   · 但由假设的 ε-界推出的局部常数是 **a + 2c**（c≈0.112）⟹ **0.383** ✗ —— 仍 **2.4 倍**偏大 ✗
⟹ ⭐⭐⭐⭐ **关键定量事实**：
   **Conjecture 3.2.7 的 RHS 恰是【尖锐渐近】（2πa = 1 ✓）⟹ 任何证明必须【尖锐】⟹
   仅靠【计数函数输入】（全局或标准 S(T) 常数下的局部）【不够】✗
   所需的额外输入正是【二阶矩/局部对相关型输入】✓✓**
```

## §3 ⭐⭐⭐⭐⭐ **A1 ↔ A3 的【汇合】（本轮的结构性发现）**
```
【A1 需要的】**二阶矩/尖锐局部计数型输入** ✓（§2 ✓）
【A3 提供的】⭐⭐ **Montgomery 的无条件【素数侧二阶矩】**（[Mon73, Ary22, BGSTB24] ✓✓）
   —— 以及 2608.13637 明确说的："the arithmetic inputs are … **Chebyshev–Mertens estimates for
      Σ_{n≤X}Λ(n)² and Σ_{n≤X}Λ(n)²/n** … **No mollifier, zero-density estimate, or zero-free region
      is used**" ✓✓
⟹ ⭐⭐⭐⭐⭐ **两个方向【需要同一件工具】**：
   **A1（Li 系数 ⟹ 零自由方向的逆）与 A3（Weil 正性 ⟹ 比例）都需要【素数侧二阶矩】** ✓✓✓
   —— 这是此前未见的连接 ✓✓（**A1 的 E3 依赖 A3 的机器** ✓）
【推论】**A1 的完整证明不能只在 Li 框架内完成** ⟹ 必须**借用 A3 的输入**（或等价的尖锐局部计数 ✓）
```

## §4 **E3（Brown Thm 2 经典情形的完整证明）：状态**
```
【可得的部分 ✓】LHS 的严格上界（引理 1–4 ✓）+ 常数已解决（a=1/(2π)、b⁺=0、3a+b>0 ✓）
   + 与 RHS 的**首阶精确匹配**（2πa = 1 ✓）+ 数值余量 1.45~4.8×（用【真实零点】✓）
【缺的一环 ✗】把"真实零点"换成"**可从假设推出的界**"—— 需要**尖锐局部计数或二阶矩** ✗
⟹ **E3 的分工**：① Li 框架内可得【条件性定理】（以尖锐局部计数为假设 ✓）
   ② **无条件版本需 A3 的输入** ✓✓
【⚠️ 诚实标注】E2 的失败使 E3 的**无条件**形态**尚未达成** ✗（**但缺口被精确定量（4× / 2.4×）** ✓✓）
```

## §5 **A1 最终状态**
```
【完成度】**99% → 99%（但【状态更新】）**：
   · 缺口定位 ✓✓｜修复路线 ✓✓｜常数解决 ✓✓｜证明骨架 ✓✓｜渐近临界性 ✓✓｜数值验证 ✓✓
   · ⭐ **新增：明确 E2 失败（4× 损失）+ 所需输入的确切类型（二阶矩）+ A1↔A3 汇合** ✓✓✓
【产出（可写者）】
   ① **条件性定理**：以"尖锐局部计数 N(t+1)−N(t) ≤ (1/2π)log t + O(1)"为假设 ⟹ Conjecture 3.2.7 ✓
   ② **无条件定理**：需 A3 的二阶矩输入 ✓（**待整合** ✓）
   ③ **负面收获**：证明【必须】用二阶矩型输入 ✓✓（**这个"必须"本身是有价值的信息** ✓）
```

## §6 边界
```
【自证 ✓】§2 的 4× 与 2.4× 损失（逐项可验 ✓；Abel 求和的常数已给出 ✓）
【原文级 ✓】二阶矩输入的存在与出处（2608.13637 §1.2 ✓✓；[Mon73/Ary22/BGSTB24] ✓）
【未做 ✗】② 的整合；E1 的完整显式不等式（未逐项写出 ✗）
【⚠️ 修正】此前 E2 的设想（"换计数界即可"）**被本轮证伪** ✗ —— 已在清单中改标 🟡→（需二阶矩）✓
```
## §7 提交链
```
ALIGN-A3-COMPLETE-2（3f611be）→ 本篇（A1 收尾：E2 失败 4× + 二阶矩必需 + A1↔A3 汇合）
```
