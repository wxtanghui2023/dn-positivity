# η 交错表示探索（2026-09-08 21:10——E-G 归档后第一方向）

## 动机
"避开 ξ/Λ 零点几何范式"——η(s)=Σ(−1)^{n−1}n^{−s} 是唯一不含解析延拓的
临界带表示（σ>0 收敛级数——）——t=0 给出纯算术的零-free 证明（Leibniz——）
——罕见的"不依赖零点的正性机制"——解剖它的机制与失效点

## 发现
### t=0 机制（纯算术——）
- 配对 (2k−1, 2k)：贡献 (2k−1)^{−σ} − (2k)^{−σ} > 0（−σ 递减——）
- 每对正——累计 η_N 单调增——η(σ)>0——ζ(σ)=η(σ)/(1−2^{1−σ})——(0,1) 无零点 ✓
- **正性来源 = 实数的序**（相邻整数的大小——）

### t≠0 失效（精确结构——）
- 配对变 (2k−1)^{−σ}e^{−it log(2k−1)} vs (2k)^{−σ}e^{−it log(2k)}
- **两个不同方向的单位向量之差——无固定符号**
- 正性来自"实数的序"——复变量丢失序
- η_N 部分和振荡（σ=0.5——t=γ₁——|η_N|~0.07-0.2 收敛慢——）

## 结论
- η 揭示：**"正性需要序——序只在实轴"**——t≠0 无配对正性
- 这是 RH 困难的缩影（复变量配对无正性——）——非新逃逸通道
- η 方向低价值（标准工具——已知困难——）

## 状态
- 探索完成——无突破——不深入
- E-G 归档后方向选择仍开放——待唐先生指引
