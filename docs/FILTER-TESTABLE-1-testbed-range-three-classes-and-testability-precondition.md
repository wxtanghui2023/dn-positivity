已查地图（所查：`C-114` §2（**检验床协议逐字**：候选须在**仅用素数侧数据**下于已知 `\beta\ne\tfrac12` 对象上**报火**；检验床＝**Davenport–Heilbronn**（有 FE、无 Euler 积、已证有离线零点）＋**Epstein `\zeta`**（有 FE 却有离轴零点，Potter–Titchmarsh）；判死逻辑＝若在 `\zeta` 与检验床上**输出同形** ⟹ 未报火 ⟹ 不是检测器）、`CLOSED-ROUTES-MAP:2357` **逐字**（**已知 off-line 实例（Davenport–Heilbronn／Epstein／Beurling 型）全无 Euler 积 ⟹ 撞约束⑤ ⟹ 该角＝"Selberg 类成员有可证 off-line 零点"级问题**；⚠️ 不得把"无已知实例"升成"不存在"）、`ATTACK-S2-pre-search-DBN-heatflow-ALREADY-CLOSED-and-my-proposal-illposed.md:28,40` **逐字**（`\texttt{(R2)③}` **直接否决**：`H_\lambda\ (\lambda\ne0)` **无 Euler 积** ⟹ **素数侧无定义** ⟹ 输入 `\sum_{n\le T}\Lambda(n)n^{-s}` **不存在**；`\boxed{\text{我的 S2' 提法}\ \textbf{ill-posed}}`）、`C68-SPEC` §3（`F1`–`F8` ＋ 四交付物）、`POS3` §4（等值判据族：Weil 正性／Li／Robin／Nicolas／Lagarias／Nyman–Beurling）、`C-46`（单离轴零点 ⟹ 大 `n` 的 `\lambda_n<0`）、`FREEZE-2`／`D0-D1-LEDGER`／`RESEARCH-CONSTITUTION` §8.1）。**结论**：**唐先生 19:23 的三问框架档案已有（`F1`–`F8` ＋ 检验床，`C-114`）** ✓；**本档补两点**：**(1) 直答"手头有没有具体新对象定义"——没有** ✓✓；**(2) 检验床的真实射程比看上去窄——它是"三分类"而非"二分类"，且附带一条**可测性前提**（`TESTABLE-1`）；二者均为**筛子**（冻结期产物，依 `§8.6` 先例），**非候选** ✓✓

FREEZE-ACK: 本档即冻结期内的筛子补丁（不产候选，不新增 `N` 项）

D0: 本档对象 = 档案已有「检验床协议」（`C-114` §2）＋ 已有「无 Euler 积限制」（`:2357`）的**射程修正与前提化**——关系 = 筛子细化，非新对象
D1: 0

# FILTER-TESTABLE-1 · **检验床的射程（三分类）与可测性前提**

> **时间**：2026-09-18 19:23 唐先生：三问检查点（是否隐含 RH／聚合 vs 逐点／对接桥梁）＋ 建议先做"坏函数压力测试"，并问：**手头有没有具体的新对象定义可先做这个测试？** ✓

---

## §0 结论（直答）

$$\textbf{直答}：\boxed{\textbf{没有}}——\text{我手头}\ \textbf{没有} \text{任何具体的"新算术对象"定义可拿来做压力测试}✓✓$$
$$\qquad \text{理由不是"还没想好"}，\ \text{而是}\ \text{`FREEZE-2`}\ \text{已核定}：\text{五原语全部落地、}\textbf{无新机制}（\text{discovery 级新自由度}=0）✓$$
$$\qquad \qquad \text{且}\ \text{`C-128`}\ \text{的"候选形状"经核实＝}\textbf{已审对象的重新发明}（\text{离轴零点有限}）⟹ \text{已勘误}✓✓$$
$$\Rightarrow\ \text{按}\ \text{`§8.1`}\ \text{我}\ \textbf{正处于冻结期};\ \textbf{产候选} \text{正是被暂停的动作}✓$$

---

## §1 三问框架：档案已有，本档确认

$$\text{唐先生三问} \equiv \text{档案}\ \text{`F1`–`F8`}\ ＋\ \text{检验床}（\text{`C-114` §2}）✓$$
$$\qquad \text{(1) 是否隐含 RH／等价物} \leftrightarrow \text{`F5`（含独立算术输入，不与 RH 等价）＋ `F7`（Epstein 检验）}✓$$
$$\qquad \text{(2) 聚合 vs 逐点} \leftrightarrow \text{`V188` §2 饱和定理（线性＝聚合＝饱和；反演到逐点需无界精度）}✓$$
$$\qquad \text{(3) 对接桥梁} \leftrightarrow \text{BRIDGE-ONLY／`V193` §③（唯一已知桥梁＝显式公式，且饱和）}✓$$
$$\Longrightarrow \text{故}\ \textbf{框架无须重建}，\ \text{本档只补}\ \textbf{射程} \text{与}\ \textbf{前提}✓$$

## §2 ⭐ 检验床的真实射程：**三分类**，不是二分类

$$\textbf{协议}（\text{`C-114` §2 逐字要点}）：\text{候选须}\ \textbf{仅用素数侧数据} \text{在已知}\ \beta\ne\tfrac12\ \text{对象上}\ \textbf{报火}✓$$

| 类 | 表现 | 判定 | 现有实例数 |
|:--|:--|:--|:--:|
| **(i)** | **不报火** | `\beta`-**盲** ⟹ **直接死**（零成本）| 多（`C-114` 的"八条"）|
| **(ii)** | 报火 ＋ 决定性步**可证**（无 RH 强度输入）| **目标** | **0** |
| **(iii)** | 报火 ＋ 决定性步 ⟺ **RH 等价** | **等值判据类**（仍在墙上）| **多** |

$$\textbf{⭐ 关键示例（本档补）}：\text{Li 判据}\ \lambda_n\ge0\ \textbf{在 Davenport--Heilbronn 上会报火}✓✓$$
$$\qquad \because\ \text{单个离轴零点} \Longrightarrow \text{大}\ n\ \text{的}\ \lambda_n<0（\text{`C-46` 机制}）⟹ \textbf{它会"报警"}✓$$
$$\qquad \Longrightarrow \text{Li}\ \textbf{通过} \text{检验床}，\ \text{却仍是}\ \textbf{RH 等价}（\text{`POS3` §4 等值判据族}）✓✓$$
$$\Longrightarrow \boxed{\text{检验床只能把 (i) 从}\ \{(ii)\cup(iii)\}\ \text{分开};\ \textbf{通过它很便宜}}✓✓$$
$$\qquad \Longrightarrow \text{真正难的是}\ (ii)\ \text{与}\ (iii)\ \text{的区分} \equiv \textbf{可证性}（\text{`F5`}），\ \textbf{不是检测能力}✓✓$$

## §3 ⭐ 可测性前提（本档新增，`TESTABLE-1`）

$$\text{`CLOSED-ROUTES-MAP:2357` 逐字}：\text{已知 off-line 实例（Davenport--Heilbronn／Epstein／Beurling 型）}\ \textbf{全无 Euler 积}✓✓$$
$$\qquad \Longrightarrow \text{检验床}\ \textbf{只对"不依赖 Euler 积"的候选可执行}✓$$
$$\qquad \qquad \textbf{(a)}\ \text{不依赖 Euler 积的候选} \Longrightarrow \textbf{可测}✓$$
$$\qquad \qquad \textbf{(b)}\ \text{依赖 Euler 积的候选} \Longrightarrow \textbf{不可测};\ \text{而该类}\ \textbf{恰是 canonical 类} ⟹ \text{`FZ-3` ①}\ \beta\ \text{盲}✓✓$$
$$\boxed{\text{`TESTABLE-1`}：\text{候选的定义必须能在"}\textbf{有函数方程、无欧拉积}\text{"的函数上}\ \textbf{原样写出}}✓✓$$
$$\qquad \text{否则}\ \textbf{压力测试无法执行} \Longrightarrow \text{该候选}\ \textbf{不接受检验床筛子} \Longrightarrow \textbf{不予进入} (ii)\ \text{评估}✓$$

$$\textbf{⚡ 已有先例（这条前提曾真实咬过一次）}：\text{`ATTACK-S2`}\ \textbf{逐字}：$$
$$\qquad \texttt{(R2)③}\ \textbf{直接否决}：H_\lambda\ (\lambda\ne0)\ \textbf{无 Euler 积} \Longrightarrow \textbf{素数侧无定义} \Longrightarrow \text{输入}\ \sum_{n\le T}\Lambda(n)n^{-s}\ \textbf{不存在}✓$$
$$\qquad \boxed{\text{我的 S2' 提法}\ \textbf{ill-posed}}✓✓$$
$$\Longrightarrow \text{即：}\text{`TESTABLE-1`}\ \text{不是想象出的规矩};\ \text{它}\ \textbf{已经}\ \text{杀掉过我自己的一个提案}✓$$

## §4 本档性质与用法

$$\text{本档}\ ＝\ \textbf{筛子}（\text{冻结期产物}），\ \textbf{非候选}✓$$
$$\qquad \text{依}\ \text{`§8.6` 先例}：\text{首次冻结的产物就是}\ \text{`46 NO-GO} \Longrightarrow \text{6 机制}\ ＋\ \text{6 筛子}\ ⟹ \text{冻结产出＝筛子，是既定做法}✓✓$$
$$\text{用法（二步）}：\qquad \textbf{步 1}\ \text{`TESTABLE-1`}（能否在无欧拉积对象上写出）;\qquad \textbf{步 2}\ \text{检验床三分类}✓$$
$$\qquad \textbf{步 1 不过} \Longrightarrow \textbf{停止};\quad \textbf{步 2 判 (i)} \Longrightarrow \textbf{停止};\quad \textbf{步 2 判 (iii)} \Longrightarrow \text{归入等值判据族（已知墙）}✓$$

## §5 边界

- ⚠️ 本档**不产候选**、**不新增 `N` 项**、**不改任何判词** ✓
- ⚠️ `TESTABLE-1` 是**前提／筛子**，**不是**关于"存在性"的断言 ✓
- ⚠️ §2 的"Li 会报火"为**结构性判断**（依 `C-46` 的单离轴机制），**本档未跑数值** ⟹ 标 `[待核·可零成本测]` ✓
- ⚠️ 依 `:2357` 的纪律：**不得**把"无已知实例"升成"不存在" ✓
- **不声称** RH；**不**声称检验床完备 ✓

## §6 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-18 19:2x）`[纪律]`（先跑后写）

```
技术词 可测性前提          命中文件数=1  :: ./FILTER-TESTABLE-1-…（本档）
技术词 三分类           命中文件数=9  :: ./RESEARCH-CONSTITUTION.md 等（**已有**）
技术词 报火            命中文件数=3  :: ./C114-… ./C116-…（**已有**）
```
**读数（按实测）**：`可测性前提`＝**1 档（仅本档）⟹ 本档新增** ✓；⚠️ `三分类`＝**9 档**、`报火`＝**3 档**（**已有**）⟹ 本档为**沿用** ✓
