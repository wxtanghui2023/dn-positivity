已查地图：已跑 scripts/prework_map_check.sh K(10,1) excess 二阶 近邻对 van Wee ⟹ 执行自 ALIGN-2026-09-25 与 PLAN-2026-09-25 两档；本档为**纯数学推导**（唐先生 2026-09-25 23:32 指定"先不跑程序，把二阶计数/excess→近邻对压力往下推一层"）；不开新研究方向。
D0: 本档对象 = `K(10,1)` 格的**二阶计数链**（文献 van Wee excess counting 框架内；非新对象）
D1: 0（无新独立自由度；产出为链条核验、层-0 表达、层预算恒等式与结构定位）

# SECOND-ORDER-2026-09-25 · `M=119` 的二阶计数 → 近邻对压力链

## §1 唐先生的链：逐步核验（**全部正确** ✓，并有两处加强）

```
(0) b(x):=|N[x]∩C|, δ(x):=b(x)-1≥0;  Σ_x b = 11M ⟹ E:=Σδ = 11M-1024;  M=119 ⟹ **E=285** ✓
(1) P:=Σ_x C(b(x),2);  C(1+δ,2) = δ + C(δ,2)  ✓ ⟹ **P = E + Q**,  Q:=Σ_x C(δ(x),2) ✓
(2) |N[c]∩N[c']| = 2 (d≤2) / 0 (d≥3) ⟹ **P = 2(A₁+A₂)** ✓  [已数值验证: 120-码 P=398=2·199 ✓]
(3) ⟹ **A₁+A₂ = (285+Q)/2** ⟹ 左边整数 ⟹ Q 奇 ⟹ **Q ≥ 1** ⟹ **A₁+A₂ ≥ 143** ✓
(4) c∈C ⟹ δ(c)=d_C(c) ⟹ Σ_{c∈C}δ = 2A₁ ✓;  ζ:=Σ_{x∉C}δ = 285-2A₁ ≥ 0 ⟹ **A₁ ≤ 142** ✓
(5) (3)+(4) ⟹ **A₂ ≥ 1** ✓
(6) C(1+δ,3) = C(δ,3)+C(δ,2)  ✓ ⟹ **T:=Σ_x C(b(x),3) = Q + Σ_x C(δ(x),3)** ✓ 且 T ≥ Q ✓
    ⟹ **T = Q ⟺ max_x δ(x) ≤ 2** ✓（因 C(δ,3)=0 ⟺ δ≤2 ✓）
(7) 2A₂ = Σ_x C(δ(x)+1-1_{x∈C}, 2) ✓ —— 核验：|C∩N(x)| = b(x)-1_{x∈C} = δ(x)+1-1_{x∈C} ✓，
    而 N(x) 内两码字必相距 2（x 的两个不同邻居 ✓）⟹ 该式恰好以"共同开邻域中心"计距离-2 对，每对计 2 次 ✓ ✓
```

**⟹ 你的链（0)–(7) 我逐步核验：无错 ✓**

**加强 ①（奇偶 → 模 4）**：由已验证恒等式 （120-码校验通过 ✓）

```
Σ_x δ(x)² + Σ_x δ(x) = 4(A₁+A₂)
```
⟹ `Σδ² = 4(A₁+A₂) − 285` ⟹ **⟢ Σδ² ≡ 3 (mod 4)** ✓（比"Q 奇"更强：285≡1 mod 4 ✓）
⟹ 由 `Σδ = 285`、`Q=(Σδ²−285)/2` ⟹ Q 奇 ✓（与 (3) 一致 ✓），且 **Σδ² ≥ 287** ✓
⟹ **最小构型唯一**：`283×{δ=1} + 1×{δ=2} + 740×{δ=0}` ✓（因 ΣC(δ,2)=1 ⟹ 恰一个 δ=2，其余 δ∈{0,1} ✓）

**加强 ②**：`Q ≥ 1` 的机制是**模约束**而非纯计数 ✓ ⟹ 若将来能证明 `Q ≡ 0 (mod 3)` 之类，即可与 mod 2 咬合 ✓（见 §7）

## §2 层-0 表达（你点名要的那一步）

```
δ_0(x) = δ(x)（因 δ_0(x)=Σ_{y∈L_0(x)}δ(y)=δ(x) ✓）⟹ 全部 pair-level 数据只依赖 **层-0 profile + 指示函数**：
$$\boxed{Q=\sum_x\binom{\delta_0(x)}{2}},\quad \boxed{A_1=\tfrac12\sum_{x\in C}\delta_0(x)},\quad \boxed{2A_2=\sum_x\binom{\delta_0(x)+1-\mathbf1_{x\in C}}{2}}$$
⟹ ⚠️ **结构性结论**：层 i≥1 的 δ_i **不直接进入** pair 层 ✗ ⟹ **pair 链自身无法闭合** ✓
⟹ 必须补上 **profile ↔ 指示函数 的耦合** ✓ —— 正是 ALIGN-2026-09-25 定位的 surfeit/pair-level 缺项 ✓✓（两条独立路线汇合 ✓）
```

## §3 新增：**层预算恒等式**（本档新）

```
固定 y：x 使 y∈L_i(x) ⟺ d(x,y)=i ⟹ 恰有 C(10,i) 个这样的 x ✓
$$\boxed{\sum_{x\in V}\delta_i(x)=\binom{10}{i}\,E}\quad(\forall i=0..10)$$
M=119 ⟹ Σ_xδ_0=285, Σ_xδ_1=2850, Σ_xδ_2=12825, Σ_xδ_3=34200, …  ✓
⚠️ 对均匀分数点 `z=1/11`：δ≡0 ⟹ 预算等式两边同为 0 ✓ ⟹ **不构成切割** ✗（但作为合法线性约束可与其它 cut 联用 ✓）
⟹ 与 §1 的 `Σ_iδ_i(x)=E (∀x)` 合起来：**层-0/层-k 的边际分布被完全钉死** ✓（每 k 的全局总量 = C(10,k)E ✓）
```

## §4 三阶核（你的 §6/§7）

```
T = Σ_x C(b(x),3) = Σ_{三元组{c₁,c₂,c₃}} |N[c₁]∩N[c₂]∩N[c₃]| ✓（三重球交）
κ-核（Q₁₀ 闭球几何, 纯组合）:
  · 星形（同一中心 x 的一邻域内三点 {x⊕e_i}i∈I, |I|=3）：交 = {x}∪{x⊕e_i} ⟹ κ=4 ✓
  · 中心+两邻居（{x, x⊕e_i, x⊕e_j}）：交 = {x, x⊕e_i, x⊕e_j} ⟹ κ=3 ✓
  · 链形（x, x⊕e_i, x⊕e_i⊕e_j）：交 = {x⊕e_i} ⟹ κ=1 ✓
⟹ T 给出**第三组对偶约束** ✓（是 pair 层之外的新几何信息 ✓）
```

## §5 `Q=1` 分支的刚性（你的 §8/§9）

```
Q=1 ⟹ profile 完全锁定: #{δ=2}=1, #{δ=1}=283, #{δ=0}=740 ✓（283+2=285, 284+740=1024 ✓）
唯一异常中心 x₀（δ(x₀)=2, b(x₀)=3）的局部二分:
  (i) x₀∉C ⟹ 三个码邻居 ∈ N(x₀) ⟹ 两两距离 2（星形）⟹ 产生 C(3,2)=3 个距离-2 对 ✓
  (ii) x₀∈C ⟹ 自身 + 两个码邻居；两邻居距离 2 ⟹ 1 个距离-2 对 ✓
⟹ 该分支 = **单一异常中心的局部禁形分类** ✓（从 1024 点全局问题压缩为局部问题 ✓ —— 你的 难点 D ✓）
```

## §6 ⚠️ 结构定位（避免重复劳动，重要）

```
本链 ≡ **van Wee 型 excess counting 框架** ✓ —— Σ_x C(b(x),2) 与距离分布 A_j 的关系正是其核心 ✓
⟹ 单独使用时其强度 = 2^n/n 量级（n=10 ⟹ ⌈1024/10⌉ = 103）✓ ⟹ **不足以排除 119** ✗（119 ≫ 103 ✓）
⟹ 与上轮的阶梯定位一致: 94（球覆盖）< 103（van Wee）< **107**（混合码 2004）≪ 120 ✓
⟹ 新杠杆只能来自: ① **同余**（Habsieger 型；6|n 与 n≡−1 mod 3 两个好情形都不含 n=10 ✗ ⟹ 须自导）
                    ② **混合码精细化**（107 的实际来源 ✓）③ **高阶**（pair/triple/SDP ✓）
```

## §7 下一步的**明确目标**（你的 难点 C）

```
需要的是 **A₁+A₂ 的上界**（等价于 Q 的上界）✓ —— 注意逻辑地位:
  若能证 `A₁+A₂ ≤ 143` 且 Q≥3 ⟹ 与 (3) 的 ≥144 矛盾 ✓ ⟹ **即已证 119 不可能** ✓
  若能证 `A₁+A₂ ≤ 143` 且 Q=1 ⟹ 落入 §5 的唯一异常中心分类 ✓
⟹ 所以"上界"就是真正的 crux ✓（不是辅助引理 ✓）——三条具体子路线:
  ① Q 的模约束: 已有 mod 2 / mod 4 ✓；找 mod 3 / mod 5（用 δ_i 层结构或 κ-核 ✓）
  ② 混合/三进制精细化: 借 107 的机制 ✓（需 L5 原文 ✓）
  ③ 三阶 κ-核 LP: 用 §4 的 κ∈{1,3,4} 建第三组约束 ✓
```

## §8 边界（诚实标注）

- 全程为**必要条件**推导 ✓；**未**排除 119 ✗；**未**主张任何"不存在" ✓
- §1 的恒等式在 120-码上已数值验证（`Σδ²+Σδ=4(A₁+A₂)` ✓、`P=2(A₁+A₂)` ✓、profile 与 Q 一致 ✓）；**本轮未新跑程序** ✓（遵"先不跑程序" ✓）
- §4 的 κ-核为纯组合推导（未程序枚举 ✓）；§5 的两分支为**结构性陈述**，未做完全分类 ✓

## 【技术词回查】（定稿前逐字输出）

```
技术词 层预算        命中文件数=0    ::
技术词 pair-level       命中文件数=4    :: ./ALIGN-2026-09-25-...md ./E18-NOGO-ALIGNMENT-2.md ./HG-D-quadratic-closure-audit-r1.md
技术词 二阶压力     命中文件数=0    ::
技术词 三阶核        命中文件数=0    ::
技术词 近邻对压力  命中文件数=0    ::
```

- **本档新增**（命中数=0）：`层预算`、`二阶压力`、`三阶核`、`近邻对压力`
- **档案已有（引用，不列为提出）**：`pair-level`（3 档，含 `E18-NOGO-ALIGNMENT-2.md`、`HG-D-quadratic-closure-audit-r1.md` ✓）
- **通用词（不计）**：—

---

## §9 ⚠️ **勘误（2026-09-26 追加）：§4 的 κ-表全错**

```
原写 "星形 κ=4 / 中心+两邻居 κ=3 / 链形 κ=1" ✗ **错误**
正确：**两个不同闭球交 ≤ 2 点** ⟹ 三元组至多有 1 个公共中心 ⟹ **κ ∈ {0,1}**，且 κ=1 ⟺ 共覆盖
⟹ `T = Σ_x C(b(x),3) = #(共覆盖三元组)` ✓（§4 的"κ-核"内容不存在 ✗）
⟹ §6 的"第三组对偶约束"应改为：**三阶只给出共覆盖三元组的计数** ✓（仍有效，但更弱 ✓）
（更正依据：Q1-CENSUS-2026-09-25 §1 勘误②）
```
