已查地图：命中（`TOPIC-ASSET-MATRIX-v1-and-framework-reset`／`TOPIC-SEARCH-R1-candidates-seven-field`／`INDEPENDENT-PROBLEM-HUNT-round1`）⟹ 引用其门制与七字段，不开新案
D0: 本档对象 = **课题总清单 v1**（唯一入口）：资产表／类型层／逐条清单／依赖链／运行规则／当前指针
D1: 0 （[REVIEW] 轮次：清单与规则，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **课题总清单 v1（唯一入口）**

> **用法**：**每次开工先看本表**；条目**只在过门后进入**；**不凭记忆发明候选**；每轮结束**必须更新"当前指针 ＋ 下一动作"**。

## §1 门制与格式（引用，不重述）

```
**五门**：`G1` 独立性 → `G2` 资产天然适配（强映射）→ `G3` 必须产生新量 → `G4` 明确 gap 写成 `(P,X,K,G)` → `G5` 外溢价值（最后才问）✓
**七字段**：$$(P,\ X,\ K,\ G,\ A,\ N,\ O)$$ ✓ —— **不满足者不入表** ✓
**可登记成果形态**：精确值／界／分类／构造／反例／新算法／新证书／结构定理 ✓（**换符号不算**）✓
```

## §2 资产（A–H）

```
$$A\ \text{有限几何}/\mathbb F_2\ \text{（配置、交、对撞、穷举+证明）}\quad B\ \text{加法}\times\text{乘法容量}\quad C\ \text{机制审计}\quad D\ \text{exact/certificate（含失败证书）}$$ ✓
$$E\ \text{秩–迹–惯性}\quad F\ \text{零点–素数数据}\quad G\ \text{跨尺度/最小反例}\quad H\ \text{新颖性审计}$$ ✓
```

## §3 类型层与资产映射

```
$$P1\ \text{认证/审计型}\Rightarrow D+H\ (\text{接 }A/C/G)\qquad P2\ \text{有限几何表/配置缺口}\Rightarrow A+D\qquad P3\ \text{结构矩阵/惯性}\Rightarrow E+D$$ ✓
$$P4\ \text{形式化/泛算法}\Rightarrow \textbf{REJECT（不入池）}$$ ✓
```

## §4 ⭐ 逐条清单

### 4.1 活跃（每次只允许一条）

```
**【L-1】** `z_L(5,5)` 精确值（limited augmented Zarankiewicz）｜类型 `P2+P1`｜资产 `D✓✓ A G`
$$\text{缺口逐字}:\ \text{作者自述需"over all extremal }C_4\text{-free graphs on }5\times5\text{ 的三/四元组穷举"，"beyond the scope of this paper"，值仍 open；现有下界 }15$$
**状态**：**活跃（三点确认中）** ｜**下一动作**：**(1)** 核是否仍 open ｜**(2)** `z_L` 定义逐字 ｜**(3)** extremal `C_4`-free `5×5` 图是否已完备枚举（决定"有限穷举"还是"又一堵墙"）
```

### 4.2 暂缓（门槛未过／等输入）

```
**【L-2】** `\pm`-rank of `(0,\pm1)`-matrices（尤其 ASM）｜类型 `P3`｜资产 `E D`｜**暂缓**：`G` 与 `N` 未锁定 ⟹ 七字段不全 ✓
**【L-3】** `U_{2,3}={}</*`群论分支`*/`｜类型 `(?)`｜资产映射 `U_{2,3}\to\{A,B,C,D,E,G\}` **未完成**｜**暂停**（沉没成本不决定选题）✓
**【L-4】** 有限几何表缺口（`PG(n,q)` 饱和集／cap／arc 的小参数精确值）｜类型 `P2`｜资产 `A D`｜**暂缓**：文献活跃度高，**须先证"未被他方收割"** ✓⚠️
```

### 4.3 REJECT（附理由，不再重开）

```
**【R-1】** `P4` 形式化/泛算法：**形式化本身不是数学结果**；无具体对象即成"工具漂亮但无课题" ✓
**【R-2】** `11/8` 猜想（`b_2\ge\frac{11}8|\sigma|`）：`G2` **失败**（`E` 仅主题相邻，无 exact/有限接口）✓
**【R-3】** Astra 型"数值缝隙"（Hadamard 668、`R(5,5)` 等）：**已被计算密集型 AI 队收割** ✓
```

### 4.4 已关（附死因，禁止重走）

```
**【X-1】** EDS/CAP：已关（含**反例 `|T(S)|>|S|`**；**不可派生证书**）✓
**【X-2】** Lemma R `P1`/`R2`：**prior art** ✓
**【X-3】** II-D `\delta^*`：**改名**（= 已知分辨率阈值）✓
**【X-4】** TARGET-L9/`\tau`：**结构 STOP**（`\tau` = `g`/shear 的忠实重编码）✓
**【X-5】** `Bridge-C-1`／`(5)`／`(TR3')`／`d^2=1`：**作废**（`c^2` 居中 + `b^{-1}` 记账）✓
**【X-6】** 旧四节点模型／`P_4` universal／`M3`-`\alpha`／双参数路线：**作废或停止**（见各 closure 档）✓
```

### 4.5 类型级待填（只有类型，尚无具体问题）

```
**【S-1】** `P1` 认证/审计型：**待填具体公开有限断言**（要求：可认证、显式 gap）✓
**【S-2】** `P2` 有限几何表缺口：**待填具体小参数**（要求：未被他方收割）✓
**【S-3】** `P3` 惯性/秩迹：**待填具体矩阵族 + 明确界缺口** ✓
```

## §5 逻辑链（谁吃谁）

```
$$\text{资产}\ \to\ \text{类型}(P1/P2/P3)\ \to\ \text{具体问题}\ \to\ (P,X,K,G)\ \to\ \text{七字段}\ \to\ \text{攻击}\ \to\ \text{可登记成果}$$ ✓✓
【反链（禁止）】$$\text{手头某个推导}\ \to\ \text{顺手继续算}\ \to\ \dots$$ ✗（`U_{2,3}` 案例）✓
```

## §6 运行规则（防瞎跑）

```
**规则 1**：**每次只允许一条活跃条目**，其余冻结 ✓
**规则 2**：**任何新想法先过七字段**，否则**不入表**（不得以"似乎有潜力"入表）✓
**规则 3**：**RH 相关性不是入场券**；不得以"以后也许能接 RH"作为入表理由 ✓
**规则 4**：**手工共轭/取逆链必须先做小商反例筛**（`S_3`/`D_{12}`）——已触发 4 次同型错误 ✓✓
**规则 5**：条目状态**只允许四值**：`活跃`／`暂缓`／`REJECT`／`已关` ✓
**规则 6**：**每轮结束更新"当前指针 + 下一动作"**；**指针为空 = 违规状态** ✓
**规则 7**：**不许把"关掉一条路"当战绩**（`STRATEGY` §6⑥）✓
```

## §7 当前指针（唯一）

```
$$\boxed{\textbf{活跃条目}:\ L\text{-}1\ (z_L(5,5))\qquad \textbf{下一动作}:\ \text{三点确认}\ \{(1)\text{是否仍 open};\ (2)z_L\ \text{定义逐字};\ (3)\text{极端 }C_4\text{-free }5\times5\ \text{图是否已完备枚举}\}}$$ ✓✓
$$U_{2,3}\ \text{暂停};\quad \text{其余条目冻结};\quad \text{新候选须过七字段方可入表}$$ ✓
```

## §附 【技术词回查】（补录）
```
技术词 课题清单     命中文件数=0    :: 
技术词 已关           命中文件数=97   :: ./V167-five-device-audit-double-obligation-structure.md ./E67-true-scale-constants.md ./RH-YM-obstruction-parallel.md 
```
