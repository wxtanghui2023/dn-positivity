已查地图：未覆盖（关键词: 整数复杂度 数值半群 因子分解计数 代价模型）—— 可开档，首行须照抄本行

# **`E-10` 第二轮再生：新候选（`N0′` 优先筛选）**

**唐先生令（2026-09-23 08:49）**：**先冻结本轮成果，再开 `C-B`/`C-C` 之前做一次全新候选生成** ✓；
　**新顺序**：`N0′ → N1–N5 → N6/N7` ✓；**宁可暂停，不硬推** ✓

D0: 本档对象 = **E-10 第二轮再生候选的 `N0′` 快筛**（档案 0 覆盖 ⟹ 新档；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算／零数值／未写程序**／未做数值实验／未算零点／未接 ζ／未找 `1/2`／未证 RH ✓

---

## §1 三个新候选（**先过 `N0′`**）

| 编号 | 对象 | `\theta` | 独立问题 | `N0′` 判定 |
|:--|:--|:--|:--|:--|
| **D-1** | **整数复杂度** `\|n\|`（用 `1,+,×` 表示 `n` 的最少 `1` 个数） | **代价模型 `(a,b)`**（加法代价 `a`、乘法代价 `b`） | **Selfridge 缺陷猜想**、`\|n\|` 渐近 ✓✓ | ⚠️ **未决（须快审）** |
| **D-2** | **数值半群**（互素生成元生成的 `\mathbb N` 子半群） | 重数／嵌入维数／Frobenius 数／亏格 | Wilf 猜想（2023 已解）＋ Sylvester 波等 ⚠️ | ⛔ **FAIL**（参数**全部**是**标准命名不变量**） |
| **D-3** | **有序因子分解计数**（factorisatio numerorum） | 因子个数 `k` | `h_k(n)` 的渐近／分布 ⚠️ | ⛔ **FAIL**（`h_k(n)` 为**已有命名对象**：Kalmár／Hille 线） |

## §2 `N5`（仅供 D-1 参考；D-2/D-3 已止于 `N0′`）

```
【D-1 可证结构 ✓✓】 (i) **乘法次加性**：`\|mn\| \le \|m\| + \|n\|`（精确、可证 ✓）；
　(ii) **上界** `\|n\| \le 3\log_3 n`（可证 ✓）；(iii) `2^a 3^b` 型的**精确值**已知 ✓
【但】 `N0′` 未决 ⟹ **依新顺序不得进入 `N1–N5` 展开**（此处仅登记其结构以备后用）✓
```

## §3 判定与建议

```
【本轮结果】 ⛔ **无候选干净通过 `N0′`**：
　`D-1` 未决（代价模型参数是否已被"代价加权复杂度"文献命名 **须快审**）⚠️；`D-2`／`D-3` **明确 FAIL** ✗✓
【⭐ 依令的建议】 $$\boxed{E\text{-}10\ \text{暂停}}$$ —— **宁可暂停，不硬推**；`D-1` 留作**可选快审项**（若唐先生要，则先做 `N0′` 快审，过了再进 `N1–N5`）✓✓
【⛔ 禁项】 不以"候选数量"为由硬推 `C-B`／`C-C`／`D-2`／`D-3` ✓
```

## §4 边界与回查（✗✓）

```
✗ 零计算／零数值／未写程序／未做数值实验／未算零点／未接 ζ／未找 `1/2`／未证 RH／**未开案** ✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 参数独立性 命中文件数=0 ⟹ **本档新增** ✓｜整数复杂度=0 ⟹ **新增** ✓｜数值半群=0 ⟹ **新增** ✓｜代价模型=0 ⟹ **新增** ✓
【地图回查】 关键词 4 项 = **未覆盖** ⟹ 可开档 ✓
【文献依据（untrusted，未逐字核）】 整数复杂度：Selfridge 缺陷猜想线；数值半群：Wilf 猜想（2023 解）；因子分解计数：Kalmár／Hille 线 ✓
```
