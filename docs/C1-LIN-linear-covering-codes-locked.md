已查地图：命中（`HUNT-R2-OPEN-MATH-POOL-round1`（`C1` 覆盖码）／`II-D-CLOSED-and-why-RH-question`（资产池=0））⟹ 引用，不开新案
D0: 本档对象 = `C1-LIN` 锁定：线性覆盖码 `K_{\rm lin}(n,R)` 的对象/证书/目标形状/候选排序规则
D1: 0 （[REVIEW] 轮次：锁定与规格化，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`C1-LIN`：线性覆盖码（锁定规格）**

## §0 为何改锁线性族（本档定稿）

```
【关键结构】**线性码 `C\le\mathbb F_2^n` 由**奇偶校验矩阵 `H`（`n` 列 `n-k` 行）完全决定；覆盖半径 `R` 等价于**有限综合征条件**：$$\forall s\in\mathbb F_2^{\,n-k}:\quad s=\textstyle\sum_{j=1}^{R}h_{i_j}\ \ (\text{至多 }R\ \text{列之和})$$ ⟹ **验证＝穷举 `2^{\,n-k}` 个综合征** ✓✓
【⟹ 与本线能力的匹配】 **证书＝`H` 本身** ＋ **独立穷举校验器** ⟹ **秒级可验、完全有限** ⟹ 命中我方最擅长的"有限可验证证书" ✓✓
【⚠️ 不是重复 unrestricted 问题（照录您的证据）】 `(n,R)=(10,2)`：已有工作严格证明**二元线性族**最小为 `32`，**并明确声明这不蕴含** unrestricted `K(10,2)=32` ⟹ $$K_{\rm lin}(n,R)\ne K(n,R)\ \text{是真实的结构层}$$ ✓✓
【⚠️ 排除项（防撞车）】 **(i)** 2026-08 新工作给出 `q=6,7` 的 9 个新上界（含显式构造＋独立穷举验证）⟹ 那些参数**已被扫过**，**不得选作候选** ✓；**(ii)** 2025 SDP 下界工作已把大量二元覆盖码下界推到新整数界（如 `K_2(13,1)\in[607,704]`、`K_2(15,4)\in[22,32]`）⟹ **纯"数字差小"的项会撞上已很强的下界工具** ❌ ⟹ 判据须改（见 §2）✓
```

## §1 ⚠️ 修正"gap=1"判据（本档技术点）

```
【问题】 线性族中 `K_{\rm lin}=2^{\,k}`，故"大小差 1"几乎不可能；正确的整数量是 $$k_{\min}(n,R):=\min\{k:\ \exists\ [n,k]\ \text{线性码，覆盖半径}\le R\}$$ ⟹ **"差 1"应定义在 `k` 上**（大小上是**因子 2** 的差距）✓✓
【⟹ 目标形状（最干净）】 已构造出 `k_U` 的 `H`（上界 `K_{\rm lin}\le2^{k_U}`），而下界仅给 `K_{\rm lin}\ge2^{k_U-1}` ⟹ $$\boxed{\text{构造出 }k=k_U-1\ \text{的线性码}\ \Longrightarrow\ K_{\rm lin}\ \text{被减半、上下界闭合}}$$ ✓✓ —— **纯构造型、秒级可验、量真变** ✓
```

## §2 候选排序规则（照录您的三条 ＋ 本档细化）

```
**【G1 整数逼近】** `k` 上差 `1`（大小上因子 `2`）；优先已有下界**恰好**卡在 `2^{k-1}` 者 ✓
**【G2 构造空间可搜索】** 要求候选具备**可搜索结构**：线性／循环／**低维商空间**／**直和拼接**／**已知码有限修改**／**高对称轨道**；⛔ 排除纯 `2^n` 无结构子集搜索 ✓✓
**【G3 排除近期已扫点】** 剔除 2026-08 新上界覆盖的 `(q,n,R)` 与 2025 SDP 已强下界的项（除非我们目标正是**线性子族**而 SDP 界是 unrestricted）✓
```

## §3 `O1`–`O6`（`C1-LIN` 填表）

```
**`O1`** 线性覆盖码 `K_{\rm lin}(n,R)`（`\mathbb F_2` 及各 `q`）的精确值／`k_{\min}` ✓
**`O2`** 未闭合量＝`k_{\min}(n,R)`（整数）✓
**`O3`** 已知上界＝显式 `H`（构造）；下界＝sphere-covering／SDP／枚举证书 ✓
**`O4`** gap＝构造侧与下界侧在 `k` 上差 1（大小因子 2）✓
**`O5`** 有限可验证：**证书＝`H`**，校验＝穷举 `2^{n-k}` 综合征，**独立校验器当日可写** ✓✓
**`O6`** 独立价值：推进公开线性覆盖码表；且与 unrestricted 的差异本身有价值 ✓
【量变】 `\boxed{\text{量变}=k_{\min}\ \text{下降 1（码字数减半）}}` ✓✓
【四道硬门】 `NO equality` ✓｜`NO repackaging` ✓（量是 `k_{\min}`，非重命名）｜`NO method-without-problem` ✓｜`NO RH` ✓ ⟹ **全过** ✓
```

## §4 下一刀（照录您的裁示）

```
$$\boxed{\text{做"线性覆盖码表的精确候选扫描"：列出所有小 gap 项，再按"可构造性 ＋ 未被近期搜索覆盖"排序}}$$ ✓✓
【首action 细则】 取表 → 过滤为**线性族**可用项 → 按 `k_U-k_L=1` ＋ G2 结构 ＋ G3 未扫 三条打分 → **输出 `TOP1–3`**（每个附：`(n,R)`、`k_U`、下界来源、可用结构、预期证书形态）✓
【⛔ 纪律】 先扫表再动手；`O5` 不成形则 **PASS 保留**，不强行接机制；**不碰 RH** ✓
【边界】 §0 的三条文献为**档级**（arXiv 2608.19872 题录、SDP 下界工作、`(10,2)` 线性 `32`），**未逐字核原文**；§1 判据修正、§2 规则、§3/§4 为**本档自行推导/照录**；未制造候选／未启动搜索／未碰 RH。
