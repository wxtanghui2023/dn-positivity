# M(T) 振荡机制：几乎周期展开模型（2026-09-09 构造+验证）

## 核心发现（NS 涡旋方法的迁移——构造+验证——）
M(T) = ∫S̄ 的完整数值行为被"几乎周期积分模型"复现：
M(T) ≈ Σ_{p,k} (a_{p,k}/(k log p))·(1 − cos(kT log p)) + ...（有界——）

## S̄ 的几乎周期展开（完备性——）
S̄（零点区间平均 S——）= Σ_{p,k} a_{p,k} sin(k log p · t) + 残差
- 基频（k=1——）p≤199：R² = 0.927
- 加谐波（k=2,3——）：R² = 0.993
- 全频率（k·log p ≤ log 300——79 频率）：R² = 0.9973（残差 std 0.012——）
- 残差随频率上限指数衰减 → S̄ 几乎=展开（完备——）

## 系数结构
- 基频 a_{p,1}：p=2:0.215, p=3:0.167, p=5:0.119——衰减 ~1/p^0.7
  ——与 S 的 Titchmarsh 系数 1/(π√p log p) 偏离（比值 0.66→1.55——非恒定——）
- 谐波 a_{p,2}（k=2——）：2·log2 幅度 0.071——衰减比理论 1/(2πp) 快
  ——（区间平均修正/抹平——）

## M(T) = O(1) 的机制（模型——）
- M 主项 = Σ(a_{p,1}/log p)(1−cos(T log p))——Σa/log p ≈ 0.67 ≈ M mean 0.64 ✓
- M 完整 = 基频+谐波的 (a/(k log p))(1−cos)——相关 0.905（数值——）
- 有界性：Σ a_{p,k}/(k log p) 收敛（a 衰减快——基频 ~1/√p——谐波更快——）
- M 的范围（mean 0.64——[0.08,1.24]——）被模型复现

## 残差分析
- 非尾部（与 p>199 sin 相关 ~0）——非 δ（相关 ~0）
- 早期误判为"非周期成分"——实为谐波（2·log2 主导——k=2 项——）
- 全频率拟合后残差 std 0.012——弱相关（ρ(1)~-0.09——）——接近噪声
- ∫残差 ~1.36（数值——有限数据或极低频——待查——）

## 波包结构（独立确认——）
- 波包面积完美交替 = 定义平凡（符号段——）
- |面积| 平稳 AR(2)（特征根 0.564/0.390——）——统计
- b_k 变差线性（Dirichlet 检验不适用——）
- 振荡部分交替和有界（max 1.19——）——被几乎周期模型解释（波包 = sin 组合的符号段——）

## 严格化路径（下一步——）
1. S̄ 的展开定理（S̄ = Σa_{p,k}sin(k log p·t)——从 log ζ 展开 + 区间平均——）
2. 系数界 a_{p,k}（理论——）
3. M 积分模型收敛性（Σa/(k log p)——）
——均未完成——数值基础扎实（R² 0.997——相关 0.905——）——
