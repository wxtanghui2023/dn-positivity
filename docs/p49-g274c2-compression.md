# P49-G2.7.4-C2：Low-Energy Transform Compression——结果 B（无压缩）

> 2026-09-02 17:15 · κ_K > 1 所有 N · transform 不压缩 P_⊥ · Bridge-Ib FAIL 增强

## 结果（λ=3——κ_K = sup_z‖T_z P_⊥‖/sup_z|T_z ξ₀|）
| N | r | κ_K (K1=[10,40]) | κ_K (K2=[13,15.5]) | f* 与 ξ₀ 重叠 |
|---|---|---|---|---|
| 6 | 4 | 6.72 | 8.74 | 0.0000 |
| 8 | 5 | 6.94 | 9.30 | 0.0118 |
| 10 | 5 | 2.24 | 2.41 | 0.1735 |
| 12 | 5 | 2.92 | 3.14 | 0.0000 |

## 判定（唐先生三结果）
- **不是 A（强压缩——κ ≪ 1）**
- **是 B（无压缩——κ > 1 所有 N——transform 对 P_⊥ 更敏感——失配不能由近核自由度解释）**
- **不是 C（f* ≉ ξ₀——无特殊 ground 方向被选出）**

## ⚠️ 观察
1. **κ_K > 1 所有 N（2.2-9.3）——P_⊥（非 ground 低能方向）的 transform 响应比 ground 更大**——无压缩——反而放大
2. **"近零簇 freedom 在 observable 层消失"的 Case B 机制未出现**——T_z 不是压缩映射
3. **f*（T_K 最大方向）≉ ξ₀**——ground 不是 transform 最大方向——低能簇里其他方向产生更大 ξ̂
4. 与 II-A.2b（ground vs k 方向）+ η 崩一致——**ground 在 transform 域也不是特殊的**

## 结论倾向
- **ξ̂ 与 k_λ 的失配不能由近核自由度解释**——**Bridge-Ib FAIL candidate 增强**
- ground（ξ₀）不是 canonical transform 方向——**若 canonical direction 存在（f*）——需查 f* vs k̂（C 变体——canonical ≠ ground 但 ≈ k）**

## ⚠️ Caveat
- r 选择（近零簇边界——N=6: r=4——N=8-12: r=5）
- κ_K 的 K 依赖（K1/K2 略不同——但都 > 1）
- 单 λ（3）——需 λ=4,6 扫描（压缩是否出现）
- f* vs k̂ 未直接测（当前 f* vs ξ₀）

## 下一步候选
- (a) f* vs k̂ 重叠检查（canonical direction ≈ k_λ?——C 变体）
- (b) κ_K 随 λ 扫描（λ=4,6）
- (c) 唐先生指示
