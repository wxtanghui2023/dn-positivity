# 抵消恒等式家族探索——诚实修正（2026-08-23 22:50）

> 唐先生：继续抵消恒等式家族
> 结果：❌ 分解是恒等重排（无新无条件）——缺口不变（M(T)=O(1)——RH 深度）

## 1. 诚实修正

```
r(n) = 2K − 2Σcos − 主项（f_n = 2−2cos——代数恒等）
"分解"（主项抵消 + 2S(T) − 2∫cos·dS）是恒等重排——非新无条件结果
∫S·d(cos) 与 Σcos 通过分部积分循环相关（ΣSΔcos = [S·cos] − ΣcosΔS——回到 Σcos）
→ 缺口不变：相位均匀性（RH）
```

## 2. 确认的事实

1. **M(T) = +0.68（max 1.51——O(1）——数值）**——8/23 上午强猜想（M(T)=O(1））——RH 深度——**所有路径汇聚点**
2. ∫S·d(cos(2nθ₁)) = O(1）（数值——但与 Σcos 循环——非独立）
3. Σcos(2nθ₁) − ∫cos·N₀' = O(1）（数值——Weyl 差——r(n) 的另一种表述）
4. S 的跳跃影响（右极限 vs 中点——差 0.09——Stieltjes 处理细节）

## 3. 为什么"分解"不给出新结果

- 分部积分只是重排（ΣSΔcos ↔ ΣcosΔS——循环）
- "灾难性抵消"的每一项（Σcos、∫cos·N₀'）都是 ~N 量级——差 O(1）——需要相位均匀性
- 没有"解析恒等式"（类似 mean(S)=½ 的精确和）——只有恒等重排

## 4. 缺口汇聚（所有路径）

```
M(T) = O(1）← 8/23 上午强猜想
  ← mean(S)=½ 强形式（Σ(S_j−½)=O(1））
  ← ∫S·N₀' = O(1）
  ← r(n) = O(1）（灾难性抵消）
  ← 相位均匀性
  ← RH
M(T) = O(1）的证明：需要 ∫S 的精细界——Titchmarsh + van der Corput——N₀' 增长（端点主导）——失败
→ M(T) = O(1）是最终的墙（RH 深度）
```

## 5. 下一步（候选）

1. **M(T) = O(1）的直接攻击**（8/23 上午——数值 max 1.33——需要新方法——N₀' 增长问题）
2. **接受现状**：无条件成果（Σδ=O(1）、短程守恒、加权压缩）已足够一篇论文
3. **换物理视角**（BKT/超 GUE 的严格化——p>0.5——恢复力模型）

## 6. 文件

- scripts/stieltjes_check.py（分部积分验证——不匹配）
- scripts/rn_decomposition2.py（分解——恒等）
