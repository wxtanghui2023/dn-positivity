# E-G 候选测试结果（T1-T4——2026-09-08 20:20）

## T2：u=0 分支上 |ζ'| 远离 0（关键——）
```
零点1 分支（γ=14.1）: |ζ'| ∈ [0.79, 0.81]（几乎常数——远离 0——）
零点2 分支（γ=21.0）: |ζ'| ∈ [1.13, 1.18]
零点3 分支（γ=25.0）: |ζ'| ∈ [1.36, 1.55]
——u=0 分支上【无 ζ' 临界点】——
```

## T1：分支结构（非闭合环——是"弧"——）
```
从零点出发——σ 增——到拱顶（零点1: ~0.516——）消失/回头
不是数学意义的闭合环（v 不能在闭合环上单调——）
是"弧"（u=0 曲线的一段——从 σ<½ 上来——过零点——到拱顶）
拱顶 = Im ζ'=0 点（|ζ'|=u_σ≠0——非 ζ' 零点——）
```

## v 沿分支单调（确认——）
```
零点1: v: +0.0006 → +0.1123（σ: 0.500→0.514——严格单调——）
|ζ'| ≈ 0.79（几乎常数）——dv/dℓ = |ζ'| ✓（唐先生恒等式——）
```

## ζ' 零点位置（扫描——）
```
σ∈[0.40,0.70]——t∈[2,40]——|ζ'| 最小 ~0.16（t~4.8——非零点——）
σ∈[0.50,0.85] 的扫描：无 |ζ'|<1 的深极小
——ζ' 零点不在 σ≥0.5 的常见区（或需更细网格——）
```

## E-G 核心状态
```
① dv/dℓ = ±|ζ'| 严格（CR——唐先生——）✓
② Rolle：同分支两零点 ⟹ 中间 ζ'=0 ✓（数学——）
③ 数值：u=0 分支上 |ζ'| ≥ 0.79（远离 0——）
   ——支持"分支无 ζ' 临界点"（T2 通过——数值——）
④ 若分支无 ζ' 零点——分支最多一个 ζ 零点（Rolle——）
   ——但"每分支一零点"≠ RH（零点可在 σ>½ 的分支——）
⑤ 真正需要：分支底部在 σ=½ 的证明（或离轴 ⟹ 分支异常——）
```

## 下一步（唐先生的 T4 等——）
- T4：FE 配对零点是否同分支？（真实 ζ 无离轴——需玩具/推理——）
- 分支与 ζ' 零点位置的全局关系（ζ' 零点在 σ 何处——）
- "每分支一零点" → 分支计数 vs 零点计数（如果分支数 = 零点数——）
