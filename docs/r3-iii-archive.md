# R3-III 归档（修正版）——跨-a 兼容性窄归档

> 2026-09-07 16:45 · 唐先生逻辑修正：删除伪反证——保留窄结论——R3 拆 R3-A/R3-B

## 归档状态表
| 子路线 | 状态 |
|---|---|
| 任意零点移动数值攻击 | ❌ 关闭（ambient ⊋ admissible——） |
| Bernstein/完全单调性 | ❌ 关闭（核非 Stieltjes 型——） |
| 单点变分 | ✅ 已有 |
| 全 a 逐点正性 | ✅ 已有 |
| 跨-a 兼容性（谱读出型——） | ❌ 关闭（≈ 显式公式重写——） |
| 结构型饱和（R3-B——） | 🟡 未攻击——R3 核心开放 |

## ⚠️ 逻辑修正（唐先生——本轮最重要审计结论）
**删除伪反证**："若存在零盲 L 使 L[D]≤0 ⟹ RH——不可能——故不存在"
- ❌ 错误：这恰恰可能是 RH 证明——"如果成功就是 RH" ≠ "不可能"
- 否则所有可能证明 RH 的方法都被同一逻辑排除

**III 真正能归档的窄结论**：
- "通过直接读取 γ 谱产生饱和" ≈ 显式公式重写——NO-GO ✓
- 但不能扩大为"所有 zero-blind saturation 都不可能"——未证明

## R3 拆分
### R3-A：谱读出型刚性（基本关闭——）
prime data → Σ_ρ f(ρ) → D=0 → RH——易退化显式公式/C 类

### R3-B：结构刚性（开放——核心——）
完全不直接计算零点位置——存在对象 X：
```
X ∈ C（RH-free）
F(X) ≥ 0（正性）
G(X) = 0（独立恒等式）
F(X) = 0 ⟹ X 刚性结构 ⟹ supp X ⊆ {Re s = ½}
```
**刚性来自对象自身结构——不是计算 γ**

## 优先级修正（唐先生——）
**V > IV > II**
- V：反演 + 正性 + 解析唯一性（Hardy/de Branges/reproducing kernel——）——先理论考古
- IV：矩确定性——次之（防止又走成"矩重构零点"——）
- II：不能简单宣布死——但避开完整 HP

## 反教训（唐先生——核心——）
**不要因为"如果成功就是 RH"判死候选**——那排除所有真正突破点。
真正判死的是：循环、重述、逻辑上无法产生额外约束的机制。
