已查地图：本档属**独立问题线**（`IP-2`／Card 3），不涉 RH/ZF 地图条目 ⟹ 按唐先生 2026-09-23 15:07 令执行 ✓
D0: 本档对象 = Card 3（`D(n)`-四元组）的 FIRST THEOREM 锁定尝试（新对象：独立问题候选的具体命题）
D1: 0 （`[REVIEW]` 轮次：文献核验与命题精确化，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`IP-2`：Card 3（`D(n)`-四元组）FIRST THEOREM 锁定尝试**（⛔ 零计算；⛔ 不碰 Card 2/4、ZF/RH）

**唐先生令（15:07 ✓✓）**：优先顺序 $$\boxed{Card\ 3>Card\ 2>Card\ 4}$$（＝**进入下一轮 FIRST-THEOREM 精确化的优先顺序**，**不是价值排名**）✓；
　⛔ 暂缓 Card 2（其 `I3`「`B` 上界下 LCM 的显式 2-进/同余约束」**离已有 covering-system necessary-condition 机器太近**）｜⛔ Card 4（独立性强但 `I3` 有危险：新同余类排除易退化为**已知局部障碍**）✓；
　⭐ **流程**：Card 3 → 锁定具体 `n_0`/同余族 → **一手核验已有覆盖** → 写 `FIRST THEOREM` → **才允许计算** ✓✓；
　⚠️ 最不能犯的错：$$\boxed{\text{一个看起来很弱、很好算的问题}\ \neq\ \text{一个值得证明的新问题}}$$ ✓

---

## §1 **一手核验结果（本档新做 ✓✓；⚠️ 外部内容按不可信标注）**

```
✔ 已核实（两条独立来源）：
　(a) **`n\equiv2\pmod4` ⟹ 不存在 `D(n)`-四元组** —— 原文："In [3], [16] and [18], it was shown that if `n\in\mathbb Z`, `n\equiv2\pmod 4`, then there does not exist a `D(n)`-quadruple" ✓；另一来源："Dujella (1993) showed there exist no Diophantine quadruples `D(4k+2)`" ✓✓
　(b) **该障碍的机制** ＝ "`n` 是 `4k+2` 型 ⟺ `n` **不是两个整数平方之差**" ✓✓（文献原文给出此等价）⟹ 属**平方/奇偶型**（2-进）障碍 ✓
✔ 部分核实：`Dujella, Open problems on Diophantine m-tuples and elliptic curves (2026)` **确实存在**（12 页）✓；⚠️ 但 `Problem 5.1`／`5.2` 的**逐字措辞未能核**（以下按**您所引为准** ⚠️）
✔ 既有机器（⚠️ 档级）：椭圆曲线延拓 $$E:\ y^2=(ax+n)(bx+n)(cx+n)$$ ＋ `regular quadruple` 猜想 ＋ **最大元/尺寸界**（`Dujella–Petričević`, Indag. Math. 2019）＋ 尺寸界（`Glas. Mat.` 2004）✓
【⭐ 结构推论（本档新增 ✓✓）】 您所引的未决小参数集 $$\{-3,3,5,8,12,20\}$$ **恰好全不被 `2\bmod4` 障碍排除**（`-3\equiv1`, `3\equiv3`, `5\equiv1`, `8\equiv0`, `12\equiv0`, `20\equiv0`） ⟹ **内部一致性检验通过** ✓✓（该集合 = 2-进障碍**未覆盖**的最小 n ✓）
```

## §2 **`C3-A`／`C3-B`／`C3-C` 逐条风险（照录 ＋ 本档分析 ✓）**

```
【`C3-A` 固定参数（不存在 `D(n_0)`-四元组）】 命题极干净 ✓；⚠️ **风险**：须确认**未被**"最大元/尺寸界"机器与既有计算范围覆盖；且须先知道该 `n_0` 的**方向**（存在？不存在？**未知**）✓
【`C3-B` 参数族（新同余族 `n\equiv a\pmod m` ⟹ 无四元组）】 数学价值更高 ✓；⚠️ **风险**：新族必须**不被** `n\equiv2\bmod4` 蕴含，且须有**不同于"两平方差"的机制**——而**奇数素数位的类比不明显**（`\mathbb Z_p` 上"两平方差"是通有性质）⚠️ ⟹ 撞已有模障碍概率高 ✓
【`C3-C` 有限性弱化（`\#\{D(n)\text{-四元组}\}\le2\Rightarrow n\in S`，`S` 有限/可算）】 一旦成立即**有限性定理** ✓；⚠️ **风险**：须处理所有 `n` ⟹ **非一轮可判** ⚠️ ⟹ 宜作**长期目标**而非第一轮 ✓
```

## §3 ⭐ **本档新增方向 `C3-D`：把 `FIRST THEOREM` 写成"方向无关的双向命题"（✓✓）**

```
【动机】 对 $\{-3,3,5,8,12,20\}$ 而言，开放问题是**存在性**（"是否存在四元组？"）⟹ 第一轮**不必**先选方向 ✓✓：
　**(+)** 给出 **exact certificate**（显式四元组 `\{a,b,c,d\}` ＋ 全对 `a_ib_j+n` 为平方的精确验证）⟹ **一轮可判** ✓✓，且**直接解答 Dujella 开放问题的一部分** ✓
　**(−)** 证明"**最小元素 `\le A` 的 `D(n_0)`-四元组不存在**"（`A` **事前固定**，需 exact 证书或结构论证）✓
【⭐ 判定】 正方向＝**一轮可判、可精确验证、独立价值明确**；负方向＝须结构论证但**仍有界可判** ⟹ `FIRST THEOREM` 宜写成 $$\boxed{\text{对 }n_0\text{：给出四元组证书，或证明存在显式界 }A\text{ 使其不可能}}$$ ✓✓
```

## §4 **尚未解决的一手核验（决定是否可锁定 `n_0`；本档⛔不锁定 ✓✓）**

```
`Q1` 对每个 $$n\in\{-3,3,5,8,12,20\}$$：`D(n)`-四元组**已知存在／已知不存在／未知**？（决定 `C3-A` 与方向）✓
`Q2` 除"两平方差"（2-进）外，是否存在**已知的障碍机制**？（决定 `C3-B` 是否可能不撞车）✓
`Q3` "最大元/尺寸界"机器（`Dujella–Petričević` 2019 等）**是否已排除小 `n` 的四元组**（在既有计算范围内）？（决定 `C3-A` 是否已被覆盖）✓
【⛔ 本档不锁定 `n_0` 的理由（照录纪律 ✓✓）】 在 `Q1`–`Q3` 之前锁定，正是您警告的错误：**看起来弱好算 ≠ 值得证明** ✓
```

## §5 **`FIRST THEOREM` 候选 ＋ 预先声明的 `STOP`**

```
【候选陈述（待 `Q1`–`Q3` 后定稿）】 $$n_0=\text{（待定，取自 }\{-3,3,5,8,12,20\}\text{）}:\quad \text{给出 }D(n_0)\text{-四元组证书}\ \ \text{或}\ \ \text{证明显式界 }A\text{ 下不存在}$$ ✓
【`STOP`（任一条命中即 CLOSED）】
　① 命中项落在**已知计算范围**内（⟹ 退化为重复验证）✗
　② 新同余/结构条件被 `n\equiv2\pmod4`（两平方差）**蕴含** ✗
　③ 只能得到**已被最大元/尺寸界蕴含**的推论 ✗
　④ 需先假设 RH 或任何 RH 等价物 ✗
【边界】 ✗ 零计算／⛔ 未碰 Card 2/4／⛔ 未连 ZF／⛔ 未连 RH／⚠️ 外部文献项按不可信标注 ✓
```
