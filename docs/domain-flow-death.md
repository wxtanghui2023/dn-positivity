# Arithmetic Domain Flow：Spec_ess(J_ω) 测试——标量化+退化（2026-09-03 12:00）

## 对象
x_{k+1} = 1/(p_k+x_k)——θ_k=x_k——a_k=1/θ_k——b_k=θ_k+θ_{k+1}——J_ω Jacobi（ℓ²(N)——）
判据：Spec_ess(J_ω) 是否依赖 ω（携带不可约 prime-history——）还是相同/标量化（死）

## 数值（N=400 截断谱）
| 环境 | 谱范围 | 理论（固定点 x*(p)——） |
|---|---|---|
| 全2 | [−4.000, 5.657] | x*=√2−1≈0.414——[b−2a,b+2a]=[−4, 5.657] ✓精确 |
| 全3 | [−6.000, 7.211] | x*=(√13−3)/2≈0.303——[−6, 7.21] ✓精确 |
| 全5 | [−10.000, 10.770] | ✓匹配 |
| 自然素数（递增） | [−5312, 5312] | **无界退化**（x_k→0——a_k=1/x_k→∞） |
| 交替2,3 | [−5.000, 6.455] | 周期带 |
| 2,3,5循环 | [−6.805, 8.092] | 周期带 |

## 判断：结构性判死（ess spec 载体）
1. **Spec_ess(J_ω) 依赖 ω——但通过"尾渐近类"（标量极限/周期/符号集）**：
   - 常序列：ess spec = F(x*(p))——单参数（p——固定点——）——**T2 死（有限参数 deformation）**
   - 周期序列：周期 Jacobi 带（依赖周期模式——有限参数——）——T2 死
   - 递增素数（自然——）：a_k→∞——无界退化（非有用信息）
2. **ess spec 尾不变（Weyl 定理——紧扰动/有限前缀差不改变 ess spec）**——**ess spec 从定义上不可能携带"完整 prime-history"**（结构性——前缀信息被 Weyl 抹掉）
3. 依赖只是"尾统计"（固定点标量 x*(p)——周期——符号集支撑——）——可压缩成有限参数——**标量化**
4. **素数序列的自然尾（递增——）退化**（系数发散——无界谱——）——除非人工归一化（外部尺度——）

## 深层
- ess spec 不是"历史进入谱"的正确载体（尾不变性——）
- 需要"非尾谱对象"（精细结构——Weyl m 函数奇点/共振——）或"非平凡尾"（遍历/随机——Lebesgue 谱——）——但素数自然尾（递增——）退化
- 按唐先生预告（"可压缩成有限参数/trace-class/标准 Jacobi family——封掉"）——**此层（ess spec 载体）可压缩——倾向封档**

## 文件
scripts/domain_flow_test.py, docs/domain-flow-death.md
