# 核不匹配发现：f̃ 核 ≠ r(n) 核（2026-08-23 17:00-17:30 展开讨论成果）

> 状态：数值确凿 + 恒等式清晰——8/23 ε_m 机制链的关键审视
> 影响：定理 1（Abel 求和）不直接证明 r(n) = O(1)——缺口仍是相位均匀性

## 1. 数值铁证（精确每区间积分——2M 零点）

| n | f̃ 核黎曼和差 | f_n 核黎曼和差 | r(n) 参考（8/22 三重验证） |
|---|---|---|---|
| 100 | **+0.048** | +0.682 | **1.38** |
| 500 | **−0.078** | +2.035 | **−0.07** |

**f̃ 核黎曼和差（±0.1）≠ r(n)（±2.5）——差一个数量级。**

## 2. 核的恒等式（为什么不同）

```
r(n) 的真实核（8/22 权威——rn-o1-final-summary.md）：
  λ_n = Σ_γ 4sin²(nθ₁(γ))，θ₁ = arctan(1/2t)     （β=½ 下——|1−1/ρ|=1）
  r(n) = λ_n − ½nlogn − cn = Σ4sin²(nθ₁) − ∫4sin²(nθ₁)N₀' + O(1)
  f_n(t) = 4sin²(nθ₁) = 2 − 2cos(2nθ₁)

8/23 ε_m 核（epsilon-sum-explicit-proof.md——定理 1 对象）：
  f̃(t) = 2sin(cθ)sin(θ/2)，θ = π−2arctan(2t) = 2θ₁，c = n+½
  f̃(t) = cos(2nθ₁) − cos((2n+2)θ₁)     （望远镜）
```

**f_n = 2 − 2cos(2nθ₁)  vs  f̃ = cos(2nθ₁) − cos((2n+2)θ₁)——不同核。**

## 3. 望远镜连接（部分补救）

```
f_n 核 Abel 项 = Σ 4sin²(nθ₁)N₀'δ = 2ΣN₀'δ − 2Σcos(2nθ₁)N₀'δ
cos(2nθ₁) = Σ_{j=n}^∞ [cos(2jθ₁) − cos((2j+2)θ₁)] = Σ_{j=n}^∞ f̃_j
⟹ f_n 核 Abel 项 = 2ΣN₀'δ − 2·Σ_{j≥n}(f̃_j 核 Abel 项)
```

**r(n) = O(1) 需要**：
1. **f_n 核 Abel 项 = O(1)**——Σ 4sin²(nθ₁)N₀'δ——**未证**（需要 δ 强抵消 = 相位均匀性 = RH 深度）
2. 望远镜版本：**Σ_{j≥n}（f̃_j 核 Abel 项）= O(1) 均匀于 n**——比定理 1（单 j）强——**未证**
3. 2ΣN₀'δ 项的无界部分（~S(T)）被主项吸收——需仔细处理

## 4. 诚实结论

1. **定理 1 正确且无条件**（Σ f̃ N₀'δ = O(1)——f̃ 核 Abel 项）——但它是"临界线投影"的控制
2. **f̃ 核 ≠ f_n 核**——8/23 机制链的"Σε_m → Sf(n) → r(n)"连接**存疑**
3. **r(n) = O(1) ⟺ Sf(n) = O(1)**（Lagarias——Sf = 1−r(n)——无条件恒等式）——Sf 是 Laguerre/素数对象（ΣC(n,j)η_{j−1}）
4. **真正缺口**：f_n 核（4sin²(nθ₁)）的 Abel 项 + EM 项 = O(1)——相位均匀性 = RH
5. 唐先生宏观确认成立：**所有路径（机制链/η_j/Euler 积/Abel/反证法）→ 相位均匀性 = RH**——没有工具绕过

## 5. 今天的真实进展（修正后）

| 成果 | 状态 |
|------|------|
| Abel 求和定理（f̃ 核——临界线投影） | ✅ 无条件严格——新定理（独立价值） |
| 反证法（离轴 ⟹ 发散） | ✅ 无条件严格 |
| 完整等价链 r(n)=O(1) ⟺ RH | ✅ 逻辑清晰 |
| **核不匹配发现**（f̃ ≠ f_n） | ✅ **重要修正**——8/23 ε_m 机制链需重新审视 |
| r(n) = O(1) 正面证明 | ❌ = 相位均匀性 = RH——无免费午餐 |

## 6. 下一步候选

1. **验证 f_n 核 Abel 项**（Σ4sin²(nθ₁)N₀'δ）的数值行为——确认它是否 O(1)（如果数值 O(1)——目标确认；如果增长——r(n) = O(1) 的数值基础也需要重新检查）
2. **检查 8/23 ε_m 机制链的 Sf(n) 连接**——ε_m（f̃）与 Sf(n)（Laguerre）的真实关系
3. **Sf(n) = ΣC(n,j)η_{j−1} 的 Laguerre 路径**（Coffey——8/23 受阻）——无条件对象——反证法闭环
4. Guinand 小定理（快振荡——独立）

## 7. 文件

- scripts/compare_kernels.py（f̃ vs f_n 核黎曼和差）
- scripts/precise_rsd.py（精确每区间黎曼和差）
- scripts/abel_riemann_sum.py（Abel 求和验证）
- docs/guinand-theorem-framework.md（Guinand 框架）
- docs/phase-locking-guinand.md（相位锁定）
