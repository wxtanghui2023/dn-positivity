已查地图：本档为 **会话存档／交接档** ✓（非新研究档），引用 `C-380-22`…`C-380-45` 既有结论 ✓

D0: 本档对象 = 档案已有对象的 **会话级汇总**（C-380 支 Level 2 闭合链）；**非新对象、非重命名** ✓
D1: 0
FREEZE-ACK: 本档即冻结审计

---

# 会话存档 · 2026-09-21（C-380-22 → C-380-45）

**主题**：two-level 族上的 `Q_1..Q_5 ≤ −1/2` 不可行性（`Level 2`）→ **CLOSED**

## 一、最终账本（冻结）

| 对象 | 状态 |
|---|---|
| two-level 五约束（`Q_1..Q_5 ≤ −1/2`） | **CLOSED** |
| A／B／C ＋ `s = m²` 端点穷尽 | **CLOSED** |
| global coverage（two-level 内） | **CLOSED** |
| `F_0`（一般四点） | **OPEN** |
| Level 3 入口 | **GAP** |
| Level 3 主证明 | **FROZEN** |
| `inf Q_5 = 341/128` | **不作为已证结论** |

## 二、核心链（可复用）

```
two-level: X = {m+a, m+a, m-a, m-a}
  → (m,s) 二维压缩，s = m² + a²
  → 精确边界点 (m,s) = (−1/8, 7/16)，Q_5 = 341/128，Q_6 = Q_2·W
  → S_m = [max(m²,A,s_-), min(7/16,s_+)]（四条区间条件）
  → 端点穷尽：{7/16, A(m), s_±(m)}；第五类 s = m² 解析为空
  → A 支：解析闭合（Q_3/Q_4 容许性排除 P 变号区）
  → B 支：单变量 P_B 正性 + Sturm（窗口 I_B = [1/8, 11371/40000]）
  → C 支：精确有理认证（低带）；高带 u ≥ u_2 解析杀（A > 7/16 与 s ≤ 7/16 矛盾）
  → Q_5 > 341/128 > −1/2 ⟹ 五约束集空 ⟹ Level 2 CLOSED
```

## 三、关键公式速查

- `Q_1 = 4m`；`Q_2 = 8s − 4`；`Q_3 = 4m(12s − 8m² − 3)`；`Q_4 = 4(−32m⁴ + 32m²s + 8s² − 8s + 1)`；`Q_5 = 4m(−64m⁴ + 40m² + 80s² − 60s + 5)`
- `Q_6 = Q_2 · (16a⁴ + 224a²m² − 16a² + 16m⁴ − 16m² + 1)`
- `A(m) = (3 + 8m² − 1/(8m))/12`（`Q_3 = −1/2` 边界，`m = −t`：`A(−t) = (64t³ + 24t + 1)/(96t)`）
- `s_±(m) = 1/2 − 2m² ± √(512m⁴ − 128m² + 7)/8`（判别式 `D = 512t⁴ − 128t² + 7`）
- `P_B(t) = 131072t⁶ − 122880t⁴ − 5120t³ + 23040t² − 2109t − 40`，`Q_5 − 341/128 = P_B/(1152t)`
- `R_B(t) = 2048t⁵ − 512t⁴ − 1408t³ + 224t² + 52t − 1`（条件 ②），B 支真实上端 `t_B = 0.28425257…`
- `R_C(u) = 1024u² − 448u + 27`，`u_1 = 7/32 − √22/32 ≈ 0.072175`，`u_2 = 7/32 + √22/32 ≈ 0.365325`
- 等号点唯一：`(m,s) = (−1/8, 7/16)`，`a = 3√3/8`，余量 `15/128`

## 四、两条永久审计原则

1. `constraint symmetry ≠ solution symmetry`（对称约束不强制对称解）
2. **不**把 `Δ_4 ≈ 0.000878` 解释成趋于零的数值噪声（不得当作"近似坍缩"）

## 五、下一步（未开，须唐先生发令）

- **停止令**：不再做 C-380 支的"补丁式"计算
- **唯一值得重开的问题**：`Q_5(X) − 341/128 ≥ Ψ(Δ_4)`，`Ψ(δ) > 0`（`δ > 0`）
  - 比严格 `Δ_4 = 0` 弱得多，可能绕过 `C-380-44` 的坍缩 GAP
  - ⚠️ **须先审计是否有独立数学来源**；无则寻找真正的旁路问题
- Level 3 主证明：**FROZEN**

## 六、纪律复述（新会话必读）

- 每档 doc：`已查地图` ＋ `D0/D1` ＋ `FREEZE-ACK` ＋ 先跑 `scripts/tech_word_check.sh`
- 花括号/引号零缺陷；`git add` 显式路径；**绝不** `git add -A`
- 端点为**窗口化**结论，须与**覆盖性**配对；不得把"边界被杀"跳成"整体被杀"
- 扫描只用于**定位机制**，不作结论
