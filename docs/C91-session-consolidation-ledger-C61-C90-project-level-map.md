已查地图 + **四级结构固化**（底本：`AUDIT-WALLS-AND-DIFFICULTIES-20260917.md`（`W1`–`W12`／`D1`–`D10` 逐字）、本会话 `C-61`–`C-90` 逐档、`V199`（三条件硬门＋三锥源）、`V254`、`V191`、`V192`、`SUPPORT-1-WALL-IDENTIFICATION-closure.md`、`CLOSED-ROUTES-MAP.md`（2836 行）、`MASTER-STATUS-AND-CLOSURES.md`（707 行）、`ASSETS-REGISTRY.md`（245 行））。**本档＝唐先生 13:41 指令的 (A)：把 C-61～C-90 的状态变化固化成项目级地图**，结构为 **墙 → 叶子 → 已攻击技术族 → 精确剩余缺口** ✓✓

# C-91 · **本会话收束台账（`C-61`–`C-90`）＋ 项目级四级地图**

> **时间**：2026-09-18 13:41 唐先生：「直接开 **(A)**」，并指定四项要求（四级结构／两个 `≠` 升级／`B` 门精确限定语／禁止重开条件＋叶子性质区分）
> **性质**：**过程性／负面判据类**（`D` 类自用资产）✓ **非突破级** ✗；**不证 RH** ✗

---

## §0 项目级主图（固定于此层级）

$$\boxed{W6=\textbf{原子墙}}\ \longrightarrow\ \boxed{\{UQRL,\ D10(a),\ W8(b)\}}\ \longrightarrow\ \begin{cases}UQRL & \textbf{四族已关}\\ D10(a) & \textbf{新对象未发现}\\ W8(b) & \textbf{收敛到}\ V199\ \text{三源（逐源待核）}\end{cases}$$
$$\text{四级读法}：\textbf{墙}\ (W6)\to\textbf{叶子}\ (\{UQRL,D10(a),W8(b)\})\to\textbf{已攻击技术族}\to\textbf{精确剩余缺口}✓$$

## §1 四级结构表（墙 → 叶子 → 技术族 → 剩余缺口）

| 级别 | 对象 | 内容 |
|:--|:--|:--|
| **① 墙** | `W6` | **原子墙**：无条件三阶矩 at `X≍T` ≡ prime-pair ≡ support>1（`C-86` §5；不可再分）|
| **② 叶子** | `UQRL` | 定量墙：`sup_{n≲T₀²}|E(n,T₀)|→0` |
| | `D10(a)` | 对象墙：须找到 `C-68` 四件套＋`ARS1–6` 要求的**新对象** |
| | `W8(b)` | 结构源墙：非筛局部权重族 |
| **③ 技术族** | `UQRL` 下 **4 族** | `Burnol`／`FINT`／`large sieve`（＋频率正则性）／`decoupling` |
| | `D10(a)` 下 | 无（需新对象，非技术族问题）|
| | `W8(b)` 下 | = `V199` 三源（已知正性机制类型）|
| **④ 剩余缺口** | `UQRL` | 命题本身开放；**已知技术族穷尽** |
| | `D10(a)` | 候选数 **0** |
| | `W8(b)` | 逐源最后核验未做 |
| **①（承重）** | `W6` | 需 prime-pair 型输入（著名开放问题级）|

## §2 `UQRL` 四族关闭表（＋精确限定语）

| 技术族 | 关闭原因 |
|:--|:--|
| `Burnol` | **α 参数失配**（`λ→0` 逼近参数 vs `n≲T₀²` 频率一致；`C-88`）|
| `FINT` | **α＋β**（频率＝`log n` 且 `n` 不作截断参数；定量内容＝框架／范数等价 `L²` 型；`C-89`）|
| `large sieve`（＋频率正则性）| **β**（天然 `L²`／平均；`C-89`）|
| `decoupling` | **A＋B：对象／曲率**（`C-90`）|

$$\textbf{⭐ `B` 门的}\ \textbf{精确限定语}（\text{唐先生指定；}\textbf{不得} \text{写成"decoupling 原则不可能"}）：$$
$$\qquad \boxed{\text{在}\ \textbf{当前候选转换及当前曲率模型} \text{下，所需的}\ \textbf{非退化曲率条件没有得到满足};\ \text{现有实测的二阶差分符号混合及 gap 跳变}\ \textbf{不能支持}\ \text{所需的标准曲率估计}}✓✓$$
$$\qquad ⚠️\ \text{故}\ \textbf{不写}：\text{"decoupling 需要二阶导定号（作为普遍命题）"}\ ✗;\ \textbf{不写}：\text{"decoupling 数学上不可用"}\ ✗✓$$

## §3 两个 `≠` 升级（必须保留，`[本档]`）

$$\boxed{UQRL\ \textbf{开放}\ \ne\ UQRL\ \textbf{不可解}}✓$$
$$\boxed{\text{四族}\ \textbf{已知工具关闭}\ \ne\ \textbf{所有可能机制不存在}}✓$$
$$\qquad \text{可写的强度}：\text{"在}\ \textbf{已知技术族内} \text{穷尽"／"当前候选转换下未满足"}✓;\ \textbf{不得} \text{升级为普遍否定}✗$$

## §4 **禁止重开条件**（硬过滤器，`[本档]`）

$$\textbf{对}\ UQRL\ \text{重新开案}：\boxed{\text{必须出现}\ \textbf{新的对象} \text{或}\ \textbf{新的机制}}✓$$
$$\text{以下}\ \textbf{不构成} \text{重开理由}（\text{逐条禁止}）✓：$$
$$\qquad \text{(1) 再换一个 exponential-sum theorem};\quad \text{(2) 再换一个}\ L^p;\quad \text{(3) 再把 zero sum 写成 Dirichlet polynomial};$$
$$\qquad \text{(4) 再换 Fourier／Hilbert 表示};\quad \text{(5) 再把}\ \textbf{平均估计包装成 uniform estimate}✓✓$$
$$\Longrightarrow\ \text{用途}：\text{今后}\ \textbf{筛选候选文献的硬过滤器} \text{（对照}\ \text{`C-89` §3 两条失配}\ (\alpha)(\beta)\ \text{即可快速判死）}✓$$

## §5 三个叶子的**性质区分**（`[本档]`）

$$\textbf{(1)}\ W6：\textbf{原子墙}（\text{结构层承重点，}\textbf{不是} \text{普通"候选路线"}）✓$$
$$\textbf{(2)}\ UQRL：\textbf{定量墙}（\sup_{n\lesssim T_0^2}|E(n,T_0)|\to0）;\ \text{已完成}\ \textbf{四族技术审计};\ \text{命题}\ \textbf{仍开放}✓$$
$$\textbf{(3)}\ D10\text{(a)}：\textbf{对象墙}（\text{不是再找估计，而是必须找到}\ \text{`C-68` 四件套＋`ARS1`–`ARS6`}\ \text{要求的}\ \textbf{新对象};\ \#\{\text{候选}\}=0）✓$$
$$\qquad ⚠️\ \textbf{不得} \text{与}\ UQRL\ \text{的"已有技术族失败"混为一类}✓$$
$$\textbf{(4)}\ W8\text{(b)}：\textbf{结构源墙}（\text{已收敛到}\ V199\ \text{三源};\ \text{下一步＝}\textbf{逐源最后核验}，\ \textbf{不是} \text{重新展开整个}\ W8\ \text{文献空间}）✓$$

## §6 本会话状态变化清单（`C-61`–`C-90`，逐档）

| 档 | 净变化 |
|:--|:--|
| `C-61` | (ISO) 五项强制对审判词；新发现"检测工具预设结论＝新形式循环" |
| `C-62` | 8/23 算术侧输入逐字取出；**窗口空洞**；三形态皆不可用 |
| `C-63` | 8/23 第二环两条候选输入**全不可用**（循环）；措辞"无条件"过强 |
| `C-64` | `E118`/`E119` 簿记冲突候选定位（窗权重不统一）|
| `C-65` | **定位坐实**：𝒩 vs 𝓔 混用；恒等式 𝓔＝`N`·𝒩；差因子 `N` |
| `C-66` | `A3` 结论：目标 `o(hN)` 落在区间内部 ⟺ 满相干被压制 |
| `C-67` | **Droll `Conjecture 3.2.7` 逐字**＋与论文 B `Theorem 1` 逐项对齐（身份断言 TRUE）|
| `C-68` | 模板四件套＋八过滤器（`ARS1`–`ARS6`／污染／压力测试）规格 |
| `C-69` | 识别侧规格卡：**可调**（两处已成功调整）；四界 `B1`–`B5` |
| `C-70` | 识别侧 8 候选逐项审计：仅 #5 `ARS`／#7 逆谱几何为**真接口形式** |
| `C-71` | 识别侧五步决策程序；**接口类＝空集**（后经 `C-72` 修正）|
| `C-72` | ⚠️ 勘误：`C-71` §4"接口类空集"**过度陈述**；第三通道**存在**但上限＝输入 |
| `C-73` | 收束索引（`C-61`–`C-72`）：12 档状态表；9 条净结论；8 条判据工具箱 |
| `C-74` | `Lemma R` 链逐字核实；**两处勘误**：方向单侧化（`R≤1 ⟹ RH`）＋端点退化回正性 |
| `C-75` | A 重跑闸门：目标定量化 `η≥0.04⟹0.70`；**两轴判别** |
| `C-76` | 取回 RS 失效步骤（`Second Lemma`）；support ⟺ 乘积界 |
| `C-77` | `Lemma 3.2` 勘误确认（二手源转录误差）；**分叉解决**；`SQ1` 定向判定 |
| `C-78` | `SQ1` 第一层：MV **不 sharp** 但只剩常数空间（≤1.475×）|
| `C-79` | ⚠️ **自我更正**：`SQ1` 关闭过早（第二层＝稀疏权算子范数仍开）|
| `C-80` | 历史链补正 `Schur→MV→Preissmann→Yangjit→Rodgers`；**写法纪律** |
| `C-81` | `Q★` 闭合：**量的翻转**（非界不 sharp）；回溯解释 `C-30` |
| `C-82` | `DEC-1` 判死（不分解＋Mertens 强度）；⭐**值 vs 界**统一理由 |
| `C-83` | "带号量"＝`V254`（**按类闭合**）；第 8 次同址 |
| `C-84` | 正性锥源穷举 10 类过三门 ⟹ **分类闭合** |
| `C-85` | 破墙阶段收束总图＋**终端障碍陈述**（带限定词）|
| `C-86` | 22 项拆解：**原子墙 1／归一化 2／已封 8／工具 3／可攻叶子 4** |
| `C-87` | `D3(b)` 第一刀：块法成立（光滑项）；缺口命名"定量 RL" |
| `C-88` | 三处改正（术语严格／撤回第二箭头／改名 `UQRL`）＋ Burnol 台账第一刀 |
| `C-89` | `FINT-1`：不闭合；**两条失配**（α 参数／β `L²` vs 一致）|
| `C-90` | `DEC-1` 桥接审计＝**(III)**；曲率假设实测失效（符号 50/50）|

## §7 勘误与**引用完整性**台账（本会话）

| 项 | 内容 | 状态 |
|:--|:--|:--|
| 8/23 "反证法（无条件）" | 措辞过强（`C-63`）| 已登记 |
| `C-71` §4 "接口类＝空集" | 过度陈述（`C-72`）| 已勘误 |
| `C-78` `SQ1` 判"关闭" | 过早；分两层（`C-79`）| 已更正 |
| `C-87` 第二箭头 | "离散测度 ⟹ 只能 √"**不成立**（`C-88`）| 已撤回 |
| **`Burnol` "门⑤逐字"** 引用 | "the uniformity as `A→0`" **未能复核**（摘要无／正文未全取）| ⚠️ **待核，不得当已核使用** |
| `C-77` | 曾**漏提交** | 已补（`3e3e4f8`）|
| 花括号平衡 | 7 处 `\text{}` 收尾错位 | 已修，复扫 0 |

## §8 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-18 13:4x）`[纪律]`（先跑后写）

```
技术词 禁止重开条件  命中文件数=0
技术词 硬过滤器    命中文件数=0
技术词 定量墙      命中文件数=3    :: ./E208-full-pool-gamma-local-selfconsistent-fixedpoint.md ./E6-5-A3-shen1-normalized-strength-coordinates-and-feasibility-domain.md ./E6-6-yiyi1-exponent-ledger-rebuild-and-reverse-implication-check.md
技术词 对象墙      命中文件数=0
```
**读数**：`禁止重开条件`／`硬过滤器`／`对象墙`＝**0 档 ⟹ 本档新增** ✓；⚠️ `定量墙`＝**3 档 ⟹ 档案已有**（`E208` 等）⟹ 本档**引用**，不列为提出 ✓

## §9 边界

- `[逐字]` §2 四族关闭原因（各指向 `C-88`／`C-89`／`C-90`）✓；§6 逐档变化（指向本会话各档）✓
- `[本档]` §0 主图、§1 四级表、§3 两个 `≠`、§4 禁止重开条件、§5 叶子性质区分 ✓
- **不声称**：四族之外机制不存在 ✗；`UQRL` 不可解 ✗；不证 RH ✗；**不覆盖**任何原档 ✓
- **纪律**：先查后判（R-1 ✓，**先跑后写** ✓）；**未用 RH 作推导** ✓；**零数值** ✓

```
⚠️ 唐先生 13:41「直接开 (A)」+ 四项要求：①四级结构固化（墙→叶子→技术族→剩余缺口）②保留两个 ≠ 升级
   ③ B 门写精确限定语（不得写成"decoupling 原则不可能"）④ 加禁止重开条件＋区分三个叶子性质
✅ 已执行：①主图固定 W6 原子墙 → {UQRL, D10(a), W8(b)}；四级结构表（§1）；②两个 ≠：UQRL 开放 ≠ 不可解；
   四族已知工具关闭 ≠ 所有可能机制不存在（§3，且写明"可写的强度"边界）；③ B 门精确限定语（§2 逐字）：
   "在当前候选转换及当前曲率模型下，所需的非退化曲率条件没有得到满足；现有实测的二阶差分符号混合及 gap 跳变不能支持所需的标准曲率估计"
   ⚠️ 不写"decoupling 需要二阶导定号（普遍命题）"；不写"decoupling 数学上不可用"；④禁止重开条件（§4）：UQRL 重开必须出现
   新对象或新机制；逐条禁止(1)再换 exponential-sum theorem (2)再换 L^p (3)再写成 Dirichlet polynomial (4)再换 Fourier/Hilbert 表示
   (5)再把平均估计包装成 uniform estimate ⟹ 今后筛选候选文献的硬过滤器；⑤叶子性质区分（§5）：W6 原子墙 / UQRL 定量墙 /
   D10(a) 对象墙（候选数 0；不得与"技术族失败"混为一类）/ W8(b) 结构源墙（收敛到 V199 三源；下一步逐源核验）
✅ UQRL 四族关闭表（§2）：Burnol=α 参数失配 | FINT=α+β | large sieve=β(L²) | decoupling=A+B(对象/曲率)
✅ 另含：本会话 C-61–C-90 逐档净变化表（§6）、勘误与引用完整性台账（§7，含 Burnol 门⑤ 引用标待核、C-77 漏提交已补、花括号 7 处已修）
✅ 净产出：①项目级四级地图 ✓；②四族关闭表＋精确限定语 ✓；③两个 ≠ 升级 ✓；④禁止重开条件（硬过滤器）✓；⑤叶子性质四分 ✓；⑥逐档变化＋勘误台账 ✓
```
