# P51-T1：Canonical Finite Realization——数值结果（离轴 ψ 偏差尺度）

> 2026-09-02 19:30 · 截断显式公式 · 离轴效应可分离 · M_T 定义自由度定位

## 数值结果
### 1. 截断显式公式重建 ψ
- Nz=20-100（γ ≤ ~240）：x=2000+ 匹配 ~0.2%——低 x（100-500）需更多零点（2-5%）
- 截断收敛：x=5000 时 Nz 20→500 差 ~1-5（O(1) 级）

### 2. ⭐ 离轴效应（同截断 ψ_W(δ) − ψ_W(0)——纯离轴）
- **振荡模式**（cos(γ log x) 相位——非单调——x=2000 负——x=10000 正——x=20000 负）
- **绝对幅度随 x 增长**：delta=0.2: x=2000: −37.8——x=10000: +135.6——x=20000: −139（~x^{½+δ} 包络）
- **远超截断误差**（~5）——**可分离** ✓
- **相对 x：不衰减到 0**（x=10000: 1.36%——x=20000: 0.70%——波动）——**我的理论 x^{δ−½} 衰减预测错误**（振荡因子 Σcos(γlogx)/γ 不衰减）

### 3. 尺度分析
- 单零点移动贡献差 ~ x^{½}(x^δ − 1)·cos(γ log x)/γ——绝对 ~ x^{½+δ}·(振荡)——增长
- 相对 x ~ x^{δ−½}·(振荡)——**振荡因子 ~ √(loglog x) 级？——不衰减——rel 不趋于 0**

## M_T 定义自由度（关键定位）
- ψ_prime(x) 对 x ≤ T **精确已知**（素数表）——容忍度 ε_T 原则上可任意小
- W 的 ψ_W 计算有截断误差（高零点）——ε_T 下限 = 截断误差
- **移动零点的离轴 W_δ：ψ_Wδ − ψ_prime ~ x^{½+δ}（绝对增长）**
- **核心问题（T3）**：能否构造**任意**离轴配置 W_T（非移动在线——每尺度调整）匹配 ψ_prime 到"在线级"（x^{½}）？——反问题——非平凡

## 状态
- T1 数值基础完成——离轴效应可分离（绝对 x^{½+δ} 增长）
- M_T 定义的自由度 = 容忍度 ε_T(x) 的选择——在线级（x^{½}——循环风险）vs 自适应（无排除）
- **真正的 T3 = 反问题**：给定 ψ_prime——离轴配置能否匹配到在线级？——非平凡

## 下一步候选
- (a) T3 反问题探测（数值：给定 ψ_prime——能否找到"匹配"的离轴零点配置——用 Mellin 算子/零点反演——看离轴解是否存在）
- (b) M_T 定义的形式化（容忍度 ε_T 的算术来源——非循环）
- (c) 唐先生指示
