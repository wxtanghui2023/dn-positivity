已查地图：命中（`A5-CORRECTION-17-is-reduced-reps-not-orbits`／`A5-FORMAL-SPEC-r9-n38`）⟹ 引用，不开新案
D0: 本档对象 = 追 `gen_asets.c → 17` 定义链：**额外条件＝"只用 `S_4`（24 个坐标置换）＋含基归一化"的弱归约**，非数学新约束
D1: 0 （[REVIEW] 轮次：源链核对，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`gen_asets.c → 17` 链：`2` vs `17` 的完整解释**

## §1 代码实际做的事（逐行，`F=4,\ a=7`）

```
`F=4` ⟹ `NV=16`；`a=7` ⟹ **`b=NV-1-a=8`（`|B|=8`）** ✓
**【分支 1】** `if (b>=F)` ⟹ `k=b-F=4`：取 `B=\{e_1..e_4\}\cup\{`从 `rest` 中选 `4` 个`\}`，`rest`＝非基非零向量（`15-4=11` 个）⟹ **枚举 `\binom{11}{4}=330` 个** ✓
　对每个 `B`：**只用 `nperm=4!=24` 个坐标置换**（`gen_perms()`＝Heap 算法）取掩码最小者，**仅当自身即最小者才打印** ✓✓
　打印 `full & ~m` ＝ **`A`-mask**（即 `A=V\setminus(B\cup\{0\})`）✓
**【分支 2】** `for d=1..F-1`：`np=2^d-1`；`if (b>np) continue` ⟹ `b=8>2^3-1=7` ⟹ **`d\le3` 全部跳过** ✓✓
【⟹ 结论】**对 `(F,a)=(4,7)`，代码只走分支 1** ✓
```

## §2 ⭐⭐ 额外条件的真相（本档判定）

```
【不是数学约束】**除 `|A|=7` 与 `\{0\}\cup A\cup(A+A)=V` 之外，代码没有对 `A` 加任何数学条件** ✓✓
【而是"归约群更弱"】 代码用的群是 **`S_4`（24 个坐标置换）**，而**不是 `\mathrm{GL}(4,2)`（`20160`）** ✓✓✓
　逐字（`gen_asets.c` 头注）："to be **lexicographically minimal under the coordinate permutations `S_F`** (which stabilise `\{e_1..e_F\}`)" ✓
【⟹ `2` 与 `17` 完全相容】 群更小 ⟹ **轨道更细 ⟹ 代表更多**：$$\underbrace{S_4\ (24)}_{\text{代码归约}}\ \Rightarrow\ 17\ \text{个代表}\ \supsetneq\ \underbrace{\mathrm{GL}(4,2)\ (20160)}_{\text{您的轨道分解}}\ \Rightarrow\ 2\ \text{个轨道}$$ ✓✓
【⟹ 头注"possibly more than one representative per orbit"正是此意】**多个代表落在同一轨道** ⟹ `17` **收敛到 ≤2 个轨道** ✓✓
【⟹ 含基归一化这一半是真正的 `\mathrm{GL}` 步骤】**任意 `|B|=8`、`\mathrm{rank}(B)=4` 的 `B` 都可由 `\mathrm{GL}(4,2)` 映到"含 `e_1..e_4`"** ⟹ 该列表**覆盖完整**，但**只用了 `\mathrm{GL}` 的一小部分（`S_4`）来去重** ✓
【`\mathrm{rank}(B)=4` 自动成立】 若 `\mathrm{rank}(B)\le3` 则 `|B|\le2^3-1=7<8` ⟹ **`\mathrm{rank}(B)=4` 恒真** ⟹ 无漏项 ✓✓
```

## §3 对 `A5` 的最终口径（A-侧定稿）

```
**(a)** A-侧的**本质对象＝`\mathrm{GL}(4,2)`-轨道集 ＝ `2` 个**（您的独立穷举；我方**待自核**）✓
**(b)** `17` ＝ **`S_4`-级代表清单**（覆盖完整、可重复），**是运行清单**，不是本质类数 ✓✓
**(c)** 三处口径必须分开写：**(i) 数学条件**（`|A|=7` ＋ 1-saturating）；**(ii) 归约群**（代码＝`S_4`；本质＝`\mathrm{GL}(4,2)`）；**(iii) 覆盖性**（代表覆盖全部轨道，去重不完整）✓✓
**(d)** `248` incidence／shear 规范化 `104` bit／三层群作用分离 —— **不变** ✓
```

## §4 对攻击的直接含义

```
【搜索面更小】**A-侧本质只有 2 种结构** ⟹ "17 类"这一说法**高估了本质复杂度** ✓✓
【⟹ 穷尽策略】 逐**轨道**（2 个）做 `g`-侧穷尽；`17` 行退火数据在轨道层面**只看 ≤2 组** ✓
【⟹ 未闭合量重心】 `g`-侧（shear 规范后 `104` bit，再商 `\mathrm{GL}(5,2)` 与 `A` 的稳定子）✓
【待办】 **(i)** 我方自核 `5040`／`2`-轨道；**(ii)** 用 `gen_asets.c 4 7` 实跑，核对其输出行数是否为 `17`（若是 ⟹ 链闭合 ✓）✓
【边界】 §1 为**逐行/逐字**源读；§2 判定、§3 口径、§4 含义为**本档自行推导**；未制造候选／未启动搜索／未碰 RH／未跑 SAT。
