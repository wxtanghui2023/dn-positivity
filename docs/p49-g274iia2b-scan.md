# P49-G2.7.4-II-A.2b：三联扫描——重叠随 N/λ 下降（倾向 FAIL/OPEN）

> 2026-09-02 15:56 · vM bug 修复后 · O 不保持 · λ=3 97.7% 是 N=4 特例

## 修复后结果（vM 素数完整）
| λ | N=4 O | N=6 O | N=8 O | O_low(N=8) | α 比 |
|---|---|---|---|---|---|
| 3 | 0.977 | 0.892 | 0.727 | 0.805 | 1.29 |
| 4 | 0.921 | 0.596 | 0.537 | 0.602 | 1.62 |
| 6 | 0.544 | 0.430 | 0.395 | 0.471 | 0.85 |

- **统一模式：O 随 N 降（所有 λ）——且随 λ 增整体降**
- N=4: 0.977(λ3) → 0.921(λ4) → 0.544(λ6)——N=8: 0.727 → 0.537 → 0.395
- **O_low 同步降**（不是"都进 V 但方向分离"——V 内方向也在分离——O_low 0.98→0.47）
- **leak_ξ 小**（0.003-0.03——QW ground 低-k 集中 ✓ 所有 λ）
- **k 分布随 λ 变宽**（leak_k: 0.019 → 0.07 → 0.17）——Gaussian α 比不一致（1.29/1.62/0.85）

## ⚠️ 判定（唐先生三档）
- **不是 PASS+**（O 不趋 1——反降）
- **不是 PASS**（0.95+ 不稳——N=8 已 0.73/0.54/0.40）
- **倾向 FAIL/OPEN**：O(3,N=4) ≈ 0.98 但 λ=4,6 明显降——**λ=3 的 97.7% 是有限尺度现象（N=4 小截断）——II-A 不能据此推进**

## ⚠️ Caveat（诚实）
1. **k 截断**：k 分布随 λ 变宽（λ=6 leak 0.17——Nk=15/N=8 截断丢尾）——O 可能被低估——但 N=8 的 V_k 覆盖频率到 14（够 k 的 ~6.3 物理宽度）——相对可靠
2. **QW ground 低-k 集中保持**（leak 小）——但 V 内方向随 λ 漂移——与 k 不一致
3. 需更大 N（10,12）确认 O 是否继续降或平台——但趋势（降）已clear

## II-A.2 状态
- **Bridge-I numerical evidence：λ=3,N=4 强（97.7%）——但不稳定（N/λ 增降）——倾向有限尺度**
- **II-A 的"共同 ground direction"在 λ 增大时不保持**——"共同 Gaussian 低-k profile"的结构证据未确立
- ⚠️ 但——k 的截断 caveat + 需更大 N——不能完全定论 FAIL

## 下一步候选
- (a) 更大 N（10,12）确认 O 趋势（平台 or 继续降）+ k 截断修正（Nk 增大）
- (b) 接受 FAIL/OPEN 倾向（λ=3 特例——II-A 需重新考虑——或——转 II-A.4（ε_λ↔1−χ 缺陷同源）或其他）
- (c) 唐先生指示
