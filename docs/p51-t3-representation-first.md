# P51-T3：Representation Compatibility——第一轮（高零点逃逸 + D restriction）

> 2026-09-02 19:50 · ψ 匹配层面 · 高零点离轴逃逸可行 · representation 层未测

## 数值结果（Nz=400——γ 到 680——移动零点 δ=0.2）
### 匹配误差（相对——ψ_W vs ψ_prime）
- **基线（无移动）**：0.003-0.225%（x=100: 0.225%——低 x 截断高——x 大衰减）
- **低零点 k=0 (γ=14)**：x=100: 1.34%（vs 0.23%——破坏）——x=500: 0.11%（vs 0.22%——不破坏）——x=2000: 0.87%（vs 0.16%——破坏）——**振荡——非单调**
- **高零点 k=199 (γ=396)**：x=100: 0.24%（vs 0.23%——几乎同）——x=2000: 0.16%（vs 0.16%——同）——**高零点移动几乎不改变匹配**

## ⚠️ 关键现象
1. **高零点离轴"逃逸"数值可行**——移动 γ=396 的零点 δ=0.2——匹配误差几乎不变（基线振荡主导）——**M_∞（容忍匹配）含高零点离轴配置——δ_∞ ≠ 0 可逃逸到高处**
2. 低零点移动——**部分 x 区间**破坏匹配（振荡——非单调"失败区间"概念——非简单 L）
3. 基线截断振荡（0.003-0.225%）——低 x 需更多零点

## D 的 restriction 自然性（初步分析）
- 对"高零点离轴"W_{T₂}——r（观察等价类——匹配 [2,T₁]）≈ 在线低零点（高零点贡献 < 基线）
- **D_{T₁}(rW_{T₂}) ≈ 0 < D_{T₂}(W_{T₂})（高零点离轴 P_γ 被 r 抹掉）**——D 单调减（平凡——r 删贡献）
- P_γ(δ) ~ δ²/γ⁴ 类（γ 大衰减）——高零点逃逸的 D 贡献小
- **⚠️ 但——D(W_∞) = Σ_all P_γ > 0（正项和——逃逸零点严格 δ≠0）**——除非 δ 随 γ 衰减到 0

## 核心开放（representation 层）
- ψ 匹配层面：高零点逃逸可行（M_∞ 含 δ_∞≠0）
- **representation 框架（R_T——完整算术实现——relations ≤ T——FE 配对/共轭/边界数据）可能排除**（高零点离轴破坏某种 relation？——未测）
- **"高零点离轴是否真的算术可实现"是唐先生 representation 框架的核心问题**

## 状态
- P51-T3 第一轮（ψ 匹配层）：高零点逃逸可行——低零点部分破坏——M_∞ ⊄ ker D（ψ 容忍版）
- representation 层（R_T/relations）未测——这是下一步关键
- GO/NO-GO 未决——取决于 representation 层是否排除逃逸

## 下一步候选
- (a) representation 层构造（R_T 的 relations——FE 配对/共轭对高零点离轴的约束——测逃逸是否被 relations 排除）
- (b) D 的 restriction 自然性正式化（r 抹掉高零点离轴——D 单调——projective D 的定义）
- (c) 唐先生指示
