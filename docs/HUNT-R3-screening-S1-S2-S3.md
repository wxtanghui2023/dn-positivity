已查地图：命中（`TARGET-L9-CLOSURE-tau-consumed`／`HUNT-R2-OPEN-MATH-POOL-round1`／`E-GATE-and-Lemma-R-R2-CLOSED`／`INDEPENDENT-PROBLEM-HUNT-round1`）⟹ 引用，不开新案
D0: 本档对象 = `S1/S2/S3` 三种子过筛（`E-gate`→`P1`–`P6`→`SURVIVOR-5`→新硬门槛），纯纸面
D1: 0 （[REVIEW] 轮次：过筛，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`S1`/`S2`/`S3` 过筛（0 通过）**

## §0 新硬门槛（复核用）

```
$$\boxed{\text{新方向必须先产生一个原变量中没有的量}}$$ ✓（原变量＝`A,\ B,\ g,\ \tau,\ (C_u)_u`）
```

## §1 逐项判定

```
**`S1`｜不相交"点覆盖线系"包装数 `\kappa`**
　**`E-gate`** ✓ PASS（极值量，非等号分类）｜**`P1`** ✓（`\kappa`:=\max\{\text{两两不相交且各覆盖全部 31 点的线系个数}\}）｜**`P2`** ✓｜**`P3`** ✓（枚举/ILP 可直接作用）｜**`P4`** ✓（构造给下界、穷尽/对偶给上界）｜**`P5`** ✓｜**`P6`** ✓
　**新硬门槛** ✓ **PASS**（`\kappa` 只依赖 `\mathrm{PG}(4,2)`，**不是 `g/\tau/A/B` 的函数**）✓
　**`SURVIVOR-5`**：① **须 30 分钟文献核查** ⚠️｜② ✓｜③ ✓｜④ ⛔ **FAIL** —— **其机制＝源 `Lemma B` 的"跨不相交覆盖计线"同一机制，仅把常数改锐**（`|B|\le\lfloor155/c(5)\rfloor=14` 更锐化）⟹ **量未变、机制未变** ✓
　**⟹ 判定：REJECT（④ 不通过）** —— 只能当"必要条件型检验"，**不构成独立新问题** ✓
　【⚠️ 补注】即使算出 `\kappa`，也只在 `\kappa<8` 时才有信息；而 `155` 线、每系 `\ge11` 线 ⟹ `\kappa` 上界 `14`，**大概率不杀** ⟹ 收益亦有限 ✓

**`S2`｜`c(M)`（覆盖全部点的最少线数）小 `M` 值**
　**新硬门槛** ✓（几何量）｜**`SURVIVOR-5`** ① **FAIL**：`c(M)` 是**已知量**（文献久有）⚠️；④ ⚠️
　**⟹ 判定：REJECT（已知量 + 与 `Lemma B` 同机制）** ✓

**`S3`｜`C`-profile 的像**
　**新硬门槛** ⛔ **FAIL**：`(C_u)_u` 是 **`\tau` 的函数** ⟹ 属"原变量中已有"的量（只是它的像的性质）✓
　**⟹ 判定：REJECT（违新硬门槛）** ✓
```

## §2 本轮结论

```
【结果】**`0/3` 通过** —— `S1` 死于 `SURVIVOR-5` ④（同机制、仅锐化常数）；`S2` 死于"已知量"；`S3` 死于新硬门槛（`\tau` 的函数）✓✓
【⟹ 状态（照录我方既有定位）】 $$\boxed{\text{资产池}=0\ \ne\ \text{问题池}=0}$$ —— **本轮不是"没有数学可做"，而是"三颗自产种子都被自家门砍掉"** ✓✓
【⟹ 结构性诊断（沿用 `HUNT-R1`）】 自产种子仍倾向落在三类：**(i) 同机制锐化**（`S1`）｜**(ii) 已有量**（`S2`）｜**(iii) 原变量的函数**（`S3`）⟹ **与 `HUNT-R1` 的诊断同型** ⟹ **自产路径再次验证不可靠** ✓✓
【⛔ 红线（未碰）】 `r=10,n=49`｜`r=11` order `11/17/23`｜`q10` order-7｜族外搜索｜SAT/exact｜`g`-穷尽｜轨道枚举 ✓
【边界】 §1 各条判定为**本档自行推导**（`S1` 的 `04` 条、`S2` 已知性、`S3` 门槛判定）；`c(M)`／`\kappa` 文献状态**未查**（标 ⚠️）；未制造候选／未启动搜索／未碰 RH。
