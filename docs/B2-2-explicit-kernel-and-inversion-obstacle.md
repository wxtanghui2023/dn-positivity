已查地图：命中（`B2-2-ratio-pairing-odd-Laplace`／`B2-2-selfcheck-no-contradiction`）⟹ 引用，不开新案
D0: 本档对象 = `B2-2` 显式核的数值算例 ＋ 判定：真障碍是**反演**不是可算性
D1: 0 （[REVIEW] 轮次：算例与判定，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`B2-2`（续）：核可算且非退化；真障碍＝反演**

## §1 显式核与算例

$$F_1(\gamma,r)=\sum_{n\ne m}\frac{\Lambda(n)\Lambda(m)}{\sqrt{nm}}\log\frac nm\; e^{-\frac{r^2}{4}[(\log n)^2+(\log m)^2]}\,e^{-i\gamma\log(n/m)}$$
【算例（素数 `\le100`，`r=0.5,\ 1.0`；`\gamma=2,5,10`）】 见 `out`：`|F_1|>0` 且有限 ⟹ **核非退化** ✓

## §2 ⭐ 关键结构性判定（本档核心）

```
【事实】 `F_1` 只依赖**探针参数** `(\gamma,r)`，**不依赖零点** ✓✓
【⟹ 单次求值不能"检测"任何东西】 素数侧是 `(\gamma,r)` 的函数；恒等式说它**等于**零点侧 ⟹ 要检测离线对必须**反演恒等式**（从 `F_1(\cdot,r)` 解出零点位置）✓✓
【⟹ 结论】 `B2-2` 的障碍**不是可算性、也不是符号**，而是**反演**：
　$$\boxed{\text{从可算的素数侧函数反解零点位置——这在结构上就是"求零点"本身}}$$ ✓✓ ⟹ `EXP-A/B2` **上限维持 PARTIAL**，且**不能**产生不等式（与 `§自检` 结论一致）✓
【⟹ 唯一可能出不等式的入口】 **外部形变参数**（`C` 路线）：只有形变才能让 `\pm\delta` 伙伴不同时出现 ⟹ 才有一阶可用 ✓✓
```

## §3 结论与下一步

```
【判定】 `B2-2`：核可算 ✓／符号可控 ✓／**但检测＝反演** ⟹ **在本架构下不能成为不等式** ✗ ⟹ 记为 **PARTIAL，不升级** ✓
【下一步】 转 `C`：设形变族（`t` 参数）上的量 `P_t(\rho)`，要求 `P_t` **对 `|β-\frac12|` 单调**且 `0\le P_t\le1` ⟹ 判 `P_{\Lambda t}-P_t\ge c\,\Phi(|\delta|)` ✓
【边界】 算例为本机数值（素数 `\le100`，非严格证书）；§2 判定为**本档自行推导**；未制造候选／未启动搜索／未碰 RH 总攻。
