# **逐篇读全文**：Voros / Palojärvi / 2-3 论文 —— 三篇都直接命中我们

**依据**：唐先生 2026-09-11 19:47（"逐篇读全文"）｜**标注**：【外部·全文（分块抽取，部分细节缺 ⚠️）】【推导】

---

## §1 **Voros, arXiv:math/0506326v2**（"Sharpenings of Li's criterion"）
```
【主定理·两择一渐近】n→∞ 时：
   **RH 真 ⟺  λ_n ∼ n(A log n + B)**（A>0，B 显式；亦适用于一般 zeta/L）✓✓
   **RH 假 ⟹ λ_n 呈"非温和振荡型"，双侧振幅【指数增长】** ✓✓
【⭐⭐ 无条件显式趋势（他的 eq.24）】
   S_n ∼ (n/2)(log n − 1 + γ_E − log 2π) + 3/4 − Σ_k B_{2k}/(4k)·n^{1−2k}   **无条件** ✓✓✓
   并指出：**这确认了 Maślanka 的两条经验猜想**（S_n 就是 RH 下 λ_n 的"趋势"）✓
【机制】RH 真时唯一主鞍点在 σ_r(n) ∈ (1/2,1)，由 **Z(σ) 在 1/2 的双极点**（由全体零点生成）塑形，
   故 **σ_r(n) ∼ 1/2 + 1/log n** ✓✓（**"1/2"是鞍点的极限位置，不是输入** ✓）
【积分表示】(12)：λ_n = ((−1)^n n/iπ)∮_C I(σ)dσ，I(σ)=Γ(σ+n)Γ(σ−n)/Γ(2σ+1)·Z(σ)，C 绕 σ=1..n ✓
【Oesterlé 的另一证明】零点全在线时 λ_n = 2∫₀^∞[1−cos nθ(T)]dN(T)，θ(T)=2arctan(1/2T)，
   分部积分 ⟹ n^{−1}λ_n = 2∫₀^π sin(nθ)·N((1/2)cot(θ/2))dθ ✓✓
【评注】该渐近判据"实用性未必更强，但**锐度**有趣：排除了 λ_n → 0 这类临界情形" ✓
```
$$\boxed{\text{我们"门①缺的背景"= Voros 的 }S_n\text{，而且【无条件】✓✓✓}}$$

## §2 ⭐⭐⭐ **2/3 论文 arXiv:2608.13637v2**（"More than two thirds…simple and on the critical line"）
```
【结果】无条件 ≥2/3（带重数）简单且在线；≥5/6 相异；用 Montgomery–Taylor 窗 ⟹ 0.6725 / 0.8362
   （此前无条件记录：5/12 与 0.6603 ✓）⟹ 可推广到 primitive Dirichlet L ✓；**Lean 4 形式化** ✓
【⭐⭐ 方法核（摘要原文）】把 **Montgomery 1973 的推导变成无条件**：
   把"需 RH 才能把零点侧读成实纵坐标上的正和"替换为
   **对 Weil Hermitian 型的【有限压缩】施加 rank–trace 不等式，用【Sylvester 惯性律】处理离轴对** ✓✓✓
【线性代数引理】3.1（拉回）：**n₊(A*Q₀A) ≤ n₊(Q₀)** ✓｜3.4（Weyl）：‖E‖≤θ ⟹ n₊(A) ≥ #{λ_i(A+E)>θ} ✓
【块结构】Prop 4.1：P ⪰ 0，rank P ≤ N₀*(I′)，tr P ≤ N₀(I′) ⟹ **n₊(Q) ≤ ½·#off** ✓✓
【测试族】{λ_r} = {**log n : n ≤ X 为素数幂**} ✓，间距条件 log(n′/n) ≥ 1/(2n) ⟹
   δ_n^{−1} ≤ 2n，Σδ_n^{−1}|x_n|² ≤ 2Σ n|x_n|² ✓
【输入】Weil 显式公式、R–vM、N(t,t+1)≪log t、Stirling、Chebyshev–Mertens、**Montgomery–Vaughan 不等式**
   —— **不用 mollifier、不用零密度、不用无零点区域** ✓✓
【⭐⭐⭐ 出处叙事（对我们极其关键）】"E2 智能体工作 3.5 小时后发现：
   **预期的上界路线是空的 —— 由素数计算，任何有限压缩的【负指标恒为 0】，界不住任何东西** ✗，
   于是**把同一套记账【掉转过来】：对偶计数（正特征值，经 Sylvester 惯性律 + 特征值上的 Cauchy–Schwarz）
   似乎无条件地认证了至少 1/2 的零点在临界线上。编排模型的第一反应是【不敢相信】"** ✓✓✓
```
$$\boxed{\text{那条"空路线"= 我们 P34/P36/P49 的结论（素数侧核 ⟹ 无负性 ⟹ 界不住）✓✓\ 我们的死胡同【被文献独立确认】，而【掉转记账】是解法}}$$

## §3 **Palojärvi, arXiv:1807.01506v3**（"Explicit Zero-Free Regions and a τ-Li-type Criterion"）
```
τ-Li 系数：λ_F(n,τ) := Σ_ρ (1 − (ρ/(ρ−τ))^n)，τ ∈ [1,2) ⟹ 非负 ∀n ⟺ GRH ✓
【第一主结果】**显式 N₁, N₂**：若 τ-Li 系数实部在 **N₁ ≤ n ≤ N₂** 全非负 ⟹ **函数在某区域外无零点** ✓✓✓
【第二主结果】若某 n ∈ [n₁,n₂] 的实部为负 ⟹ **至少存在一个在该区域外的零点** ✓（检测方向 ✓）
【相关】Brown：有限多个系数非负 ⟹ 临界带内零点数上界 ✓；趋势 "n(a log n + b)" ✓
```
$$\boxed{\text{门①的"转换"在文献中【已存在且显式】（Palojärvi 2018/2020）✓\ 我们的门定位正确，砖已有人烧好}}$$

## §4 ⭐⭐⭐ **组合结论（3 条）**
```
①【门① 完成度大幅提升】**缺的两块砖现在都有了出处**：
   (a) 背景 = **Voros S_n（无条件显式 ✓）**；(b) 转换形式 = **Palojärvi 的显式 N₁,N₂（✓）**
   ⟹ 仍缺的只是**(c) 在线零点相位的分散界**（= 2/3 论文所用的**无条件统计输入**这一类 ✓）
   ⟹ **三方组合路径：Voros 趋势 + Palojärvi 转换 + Montgomery 型无条件相位统计**
②【我们的"死胡同"被文献确认，且解法已知】素数侧 ⟹ 负指标恒 0（✗ 我们早就发现）⟹ **掉转记账**
   （正指标 + 拉回 + Sylvester）⟹ **比例界** ✓✓ ⟹ 我们 P27–P33 的惯性机器**正是这套工具**，只是用法不同 ✓
   ⭐ **我们有他们没有的东西**：具体的惯性计算（n₋(K_ρ)=1、n₋=N、S(t) 核、阈值律 n*≈6.6γ²/|δ|）
      ⟹ 可尝试给出**不同的预算切分**（⚠️推测，未验证）
③【方法学再确认】他们的"1/2 是意外出现、编排模型不敢信" ⟹ 与我们的"1/2 不该被输入、应由机制涌现"一致 ✓
```

## §5 边界
```
【外部·全文】§1 Voros：摘要+eq.(12)(22)(23)(24)+Oesterlé 证明+评注（**原文分块，中段细节缺** ⚠️）
   §2 2/3 论文：摘要+Lemma 3.1/3.4+Prop 4.1+测试族+输入清单+出处叙事（**§1.2 与 §4–6 未读全** ⚠️）
   §3 Palojärvi：摘要+引论+参考文献（**显式 N₁,N₂ 的具体数值未取到** ⚠️）
【推导】§4 的三条组合判断（②③ 含推测成分 ⚠️）
【未做】未输入 1/2；未构造模型；未改 L2；未声称任何证明
```
## §6 提交链
```
LATEST1（0c61987 组合报告）→ 本篇（逐篇全文）
```
