已查地图：本档属**独立问题线**（`IP-3`／Card 3），不涉 RH/ZF 地图条目 ⟹ 按唐先生 2026-09-23 15:09 令执行 ✓
D0: 本档对象 = Card 3 的 theorem selection（`literature-coverage matrix` ＋ 四条件筛）
D1: 0 （`[REVIEW]` 轮次：覆盖矩阵与筛选，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`IP-3`：Card 3 theorem selection（覆盖矩阵 ＋ 四条件筛）**（⛔ 零计算）

**唐先生令（15:09 ✓✓）**：授权 `Q1`–`Q3`；**本轮仍不计算** ✓；
　⭐ **`Q1` 修正（照录）**：`Problem 5.1` 原始列为六项 $$\{-3,3,5,8,12,20\}$$，但**经已知约化后**整数情形的真正剩余为
　$$\boxed{S_{\rm true}=\{-3,3,5,8,20\}}$$（`D(-1)` 已有非存在性结果 ⟹ 从 `S'=\{-3,-1,3,5,8,20\}` 中剔除；`12` 虽在最新统一表述中，**整数情形可进一步缩减**）⟹ ⛔ **六个不可未经说明地当成同等状态** ✓✓
　⭐ **`Q2` 修正（照录）**：已有障碍远超 `n\equiv2\pmod4` —— 对 $$n\not\equiv2\pmod4,\ n\notin S=\{-4,-3,-1,3,5,8,12,20\}$$ **已有存在性结果** ⟹ 现剩余集本身是"**全局分类＋局部障碍＋已知构造**"筛过一轮后的**残差** ⟹ $$\boxed{C3\text{-}B\ \text{（再找同余障碍）暂不进入 FIRST THEOREM}}$$（非不可能，而是**新颖性门槛过高**：须证明**不属于现有 2-进/`\bmod 4,8,16` 障碍**的机制）✓✓
　⭐ **`Q3` 修正（照录）**：`Dujella–Petričević` 2019 研究**最大元**可到多大/多小（构造 `|n|^3`、`|n|^{2/5}` 族）；更近 errata（`Yip` 2025）给的是 **tuple size 上界** $$B_n=O(\log\log|n|),\ M_n\le(2+o(1))\log|n|$$ ⟹ 二者**都不能推出**"四元组不存在"：
　$$\boxed{\text{"元素个数 }m\text{ 的上界"}\ \neq\ \text{"四元组最大元素的上界足以穷尽"}}$$ ⟹ $$\boxed{Q3=\text{未发现直接覆盖};\ C3\text{-}A\ \text{尚未被尺寸界封死}}$$ ✓✓
　⭐ **`C3-D` 修正（照录 ✓✓；本档接受）**：原"给出证书 **或** 证明 `A` 下不存在"**不是合格 FIRST THEOREM** —— **两个方向完全不同的命题，不能把未指定的 disjunction 当作预注册定理** ✗ ⟹ 须 **`C3-D'`**：**先固定 `n_0`，再固定一个方向**：
　　$$\boxed{\text{FIRST THEOREM}(n_0):\ D(n_0)\text{-quadruple exists}}$$（允许 exact certificate `(a,b,c,d)` ＋ 逐项验证六个平方条件 ✓）**或**
　　$$\boxed{\text{FIRST THEOREM}(n_0):\ D(n_0)\text{-quadruple does not exist}}$$（须**预先规定证明机制**，如"某个明确的有限局部障碍 ⟹ 不存在"；⛔ **不得先算、发现某 `A`、再把 `A` 写成 theorem**）✓✓

---

## §1 **`literature-coverage matrix`（`n\in S_{\rm true}=\{-3,3,5,8,20\}`；⚠️ 外部项按不可信标注）**

| `n` | 已知局部障碍 | 已知构造 | 尺寸/个数界 | 椭圆曲线约化 | **剩余精确缺口** |
|:--|:--|:--|:--|:--|:--|
| `-3` | ⚠️ 待核（`S'` 成员；`D(-1)` 已有非存在结果 ⟹ 相邻情形机制不同） | ⚠️ 无（`n\in S` ⟹ 构造族不覆盖） | `M_n\le(2+o(1))\log|n|`（✅ 同一机器） | `E:y^2=(ax+n)(bx+n)(cx+n)` ✓ | **存在性未知** |
| `3` | ⚠️ 待核 | ⚠️ 同上 | 同上 | 同上 ✓ | **存在性未知** |
| `5` | ✅ 已有 `\bmod4/8` 结构结果（`n\equiv5\pmod8` ⟹ 元素须满足特定同余条件 ✓） | ⚠️ 同上 | 同上 | 同上 ✓ | **存在性未知** |
| `8` | ⚠️ 待核 | ⚠️ 同上 | 同上 | 同上 ✓ | **存在性未知** |
| `20` | ⚠️ 待核 | ⚠️ 同上 | 同上 | 同上 ✓ | **存在性未知** |

```
【⭐ 矩阵结论（本档 ✓）】 五项"剩余精确缺口"**形式完全相同**（皆为"存在性未知"）⟹ **矩阵本身不产生区分度** ✓；
　⛔ 故**不能**据矩阵"挑一个看起来有希望"的 `n_0`（正是纪律所禁）✓
```

## §2 **四条件筛（照您给定 ✓✓）**

```
进入 `FIRST THEOREM` 者须同时满足：**(i) 有明确缺口**；**(ii) 不是已有模障碍**；**(iii) 不是已有尺寸界**；**(iv) 一轮可以严格判定** ✓
【方向 `(-)`（证明不存在）】 (i)✓ ｜**(ii) ⚠️ FAIL 风险极高**（须机制不属于现有 2-进/模障碍；`Q2` 已示残余集是筛后残差）｜(iii)✓ ｜(iv) ⚠️ 须**预先规定**有限局部障碍，且该障碍极易与既有 `\bmod4/8` 结果重合 ⟹ **四条件不全过** ✓
【方向 `(+)`（给出证书）】 (i)✓ ｜(ii)✓（不涉模障碍）｜(iii)✓（尺寸界不阻证书）｜**(iv) ✅ PASS**（有界搜索 ＋ exact certificate ＋ 全对平方验证 ⟹ **一轮严格可判** ✓✓）
　⭐ 且若命中 ⟹ **直接解答 `Problem 5.1` 在该 `n_0` 处的存在性** ✓（独立价值明确，无需 RH/invariant/ZF ✓）
【筛查结果】 $$\boxed{\text{只有方向 }(+)\text{ 通过四条件};\ \text{方向 }(-)\text{ 暂不通过}}$$ ✓；`C3-B` HOLD；`C3-C` 长期目标 ✓
```

## §3 **判决与下一步（⛔ 未锁定 `n_0`；理由照录纪律 ✓✓）**

```
【⭐ 一个重要事实（照录 ✓✓）】 $$\boxed{\text{Problem 5.1 本身就是一个非常干净的独立母问题}}$$ —— **不需要 RH、不需要新 invariant、不需要把 `ZF` 搬回来** ✓✓
【`n_0` 选择必须由**事前声明的规则**决定（本档新增 ✓✓）】 因矩阵无区分度，不可凭感觉挑 ⟹ 候选规则（须您裁）：
　**R1** `|n|` 最小者 ⟹ `n_0=-3`（`|n|=3`）✓
　**R2** 已知部分结构最多者 ⟹ 目前仅 `5` 有已核实的 `\bmod4/8` 元素同余结构 ✓
　**R3** 您直接指定 ✓
【诚实标注 ⚠️】 若走 `(+)` 而搜索在事前界内未命中 ⟹ **不产生 theorem**（该失败**不是**结果，只是搜索耗尽）⟹ 需在 `STOP` 中写明 ✓
【`STOP`（预声明）】 ① 命中项落在已知计算范围内（重复验证）✗｜② 条件被 `n\equiv2\pmod4`（两平方差）蕴含 ✗｜③ 只得已被尺寸界蕴含的推论 ✗｜④ 需假设 RH 或等价物 ✗｜⑤ 走 `(-)` 而机制与既有 `\bmod4/8` 结果重合 ✗ ✓
【边界】 ✗ 零计算／⛔ 未碰 Card 2/4／⛔ 未连 ZF／⛔ 未连 RH／⛔ 未凭感觉锁定 `n_0`／⚠️ 外部文献项按不可信标注 ✓
```
