已查地图：命中（`INDEPENDENT-PROBLEM-HUNT-round1`（生成器诊断）／`II-D-CLOSED-and-why-RH-question`（资产池=0）／`E-GATE-and-Lemma-R-R2-CLOSED`）⟹ 引用，不开新案
D0: 本档对象 = `HUNT-R2` 首轮：**从外部开放问题池生成候选**（非从资产、非从 RH），逐项 `O1`–`O6` ＋ 四道硬门
D1: 0 （[REVIEW] 轮次：候选生成与筛选，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`HUNT-R2`：开放问题池首轮**

## §0 新发生器（照录您的裁示）

```
旧：$$\text{我方资产}\to\text{变形}\to\text{候选}$$ ✗（已证只产三类，且全被自家门杀）
新：$$\boxed{\text{外部开放问题池}\to\text{问题结构化}\to\text{找未闭合量}\to\text{再看我方机制能否进入}}$$ ✓✓
【状态定义（照录）】 $$\boxed{\text{资产池}=0\ \ne\ \text{问题池}=0}$$ 两集合**彻底分离** ✓✓
【四道硬门】**NO** equality-only｜**NO** repackaging｜**NO** method-without-problem｜**NO** RH-required；**且必须写明"量变在哪里"** ✓
【本轮偏好】 $$\boxed{\text{中等难度}\ +\ \text{明确 gap}\ +\ \text{可计算}\ +\ \text{独立价值}}$$ 不碰大猜想 ✓
```

## §1 候选（`O1`–`O6`）

| # | `O1` 开放问题 | `O2` 未闭合量 | `O3` 当前最好上下界 | `O4` gap 在哪 | `O5` 有限可验证攻击？ | `O6` 独立价值 |
|:--|:--|:--|:--|:--|:--|:--|
| **C1** | **覆盖码 `K(n,R)`** 小参数表（覆盖半径 `R`、长度 `n` 的最小码字数） | `K(n,R)` 的精确值 | 表内有**已知上界**（构造）与**下界**（LP/枚举），若干 `(n,R)` **未闭合** | 上下界差 `1\sim` 小常数，需**新构造**或**更强下界证明** | ✓ **证书＝码本身**，秒级可验；下界可用 LP/枚举证书 | ✓ 直接推进公开表（表扩展有独立价值） |
| **C2** | **覆盖设计 `C(v,k,t)`** 小参数精确值（Turán 型） | `C(v,k,t)` 精确值 | 部分已知、部分带 gap | 同上，差 `1\sim` 小数 | ✓ 证书＝设计块集合 | ✓ 组合设计表 |
| **C3** | **Butson／skew Hadamard** 小阶存在性缺口 | 最小未构造阶 | 各子族表内**有缺口**（主族 `668` 已被关） | 特定阶缺构造 | ✓ 证书＝矩阵本身 | ✓ 矩阵/编码表 |
| **C4** | **差族／循环设计** `(v,k,\lambda)` 存在性表缺口 | 存在性 | 表内混合（已知＋未知） | 特定参数缺构造或非存在性证明 | ✓ 证书＝差族 | ✓ 设计理论表 |
| **C5** | Erdős 问题中**"上下界均有、gap 未闭合"**且**有限子情形可算**者 | 该具体界 | 逐题不同 | 需先逐题核实 | ⚠️ 视题而定 | ✓ 依题 |

## §2 硬门判定

```
**C1**：`NO equality` ✓（是数值界）｜`NO repackaging` ✓（量＝`K(n,R)`，非重命名）｜`NO method-without-problem` ✓（有明确问题）｜`NO RH` ✓ ⟹ **PASS** ✓✓；**量变=上下界移动 `1` 以上** ✓
**C2**：同上 ⟹ **PASS** ✓
**C3**：同上 ⟹ **PASS** ✓（⚠️ 须先核该子族缺口是否已被 2026 年 AI 团队一并关掉）
**C4**：同上 ⟹ **PASS** ✓
**C5**：⚠️ **待核**（须逐题看是否有"可计算的未闭合量"，否则易滑回"方法无问题"）⟹ **HOLD** ✓
```

## §3 `TOP`（本轮唯一建议先动者）

```
$$\boxed{\text{TOP-1}=C1\ (\text{覆盖码 }K(n,R)\text{ 表缺口})}$$ ✓✓
【理由】 **(i)** 量明确且**是数字**（`K(n,R)`）；**(ii)** **证书＝对象本身**，秒级独立可验 ✓；**(iii)** 下界可用 **LP/枚举证书**（我方盒覆盖/证书传统直接相关）✓；**(iv)** 中等难度、有 gap、独立价值明确；**(v)** **完全不涉 RH** ✓✓
【第一动作（不是推导）】 30 分钟查**现表**（Östergård 型覆盖码表）：**列出所有 `(n,R)` 中上下界差最小者**，取一个"只需构造一个更小码即可改进"的实例 ✓
【⛔ 纪律】 不先接我方旧资产；`O5` 过不了就 **PASS 保留为独立问题**，**不强行接机制** ✓
【边界】 §1 各 `O3` 依**档级**（未逐字核表）；§2/§3 判定为**本档自行推导**；未制造候选／未启动搜索／未碰 RH。
