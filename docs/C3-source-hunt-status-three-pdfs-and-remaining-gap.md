已查地图：命中（`C3-RGPF-II-free-source-extracted`／`C3-modular-coverage-range-and-free-next-stop`／`C3-pdf-full-scan-what-it-does-and-does-not-contain`）⟹ 引用，不开新案
D0: 本档对象 = `C3` **取源收口**：已获三份免费 PDF ＋ 系列定位（`Part I` 无 arXiv 版）＋ **唯一剩余缺口**（已决 universal 清单）
D1: 0 （[REVIEW] 轮次：收口，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`C3` 取源收口**

## §1 已到手的免费源（三份，均已归档）

```
**(1)** `Schulte` ＆ `Weiss`，**"Problems on Polytopes, Their Groups, and Realizations"**，`arXiv:math/0608397`（`2006`，`25` 页）✓ ⟹ **`Problem 16/17` 陈述 ＋ 参数空间** ✓
**(2)** `Monson` ＆ `Schulte`，**"Locally Toroidal Polytopes and Modular Linear Groups"**，`arXiv:0805.3479`（`2008`，`21` 页）✓ ⟹ **modular 覆盖范围**（`q=(d,0)`、`r=(d,0)` 或 `(d/3,d/3)`，`d\ge3`）✓
**(3)** `Monson` ＆ `Schulte`，**"Reflection Groups and Polytopes over Finite Fields, II"**，`arXiv:math/0601502`（`2006`，`30` 页）✓ ⟹ **素模构造的具体类**（`(1,1)`／`(3,0)`／`(p,0)` 型，群阶样本 `2592`／`3888`／`432`／`1944`）✓
```

## §2 系列定位（本轮检索所得）

```
`Part II` ＝ `arXiv:math/0601502`（已有）✓｜`Part III` ＝ **`arXiv:0707.4007`**（`2007`，`19` 页，免费，**未下载**）✓｜**`Part I`（`2004`）无 `arXiv` 版** ✗（`Part I` 含 `Table 1`／`§5.6`，即文献反复引用的参数表 ⟹ **只能走期刊/机构途径**）✓
```

## §3 ⛔ 唯一剩余缺口（未变）

```
$$\boxed{\text{"\textbf{已决 universal 情形}清单"}\ \in\ \{§11E,H\ \text{（书）},\ 1992\ CMH\ 77\text{–}118\}}$$ ✓✓
【为何三份 PDF 都不够】 **(1)** 只给**问题陈述**（明写指向 `[42, §11E,H]`）；**(2)** 给 **modular 覆盖**（"有限成员存在"，**不移除 `D`**）；**(3)** 给**素模具体构造**（同上，**不移除 `D`**）✓✓
【⟹ `D` 仍不可算】 无"已决清单" ⟹ 无从取补集 ⟹ **最小未决 `(s_*,t_*,u_*,v_*)` 与两硬问全部 `OPEN`** ✓
```

## §4 诚实结论与两条路

```
**【结论】** 免费 `arXiv` 路线**已用尽**（三份到手；`Part I` 无 arXiv 版；`export.arxiv.org` 被墙）⟹ **决定性清单在书或 `1992` 期刊论文里** ✓
**【路 A（最直接）】** `1992 CMH` 论文 —— **`e-periodica` 免费**，**浏览器可下**（我自动抓 `403`）⟹ **您下、丢 `sources/`** ✓✓
**【路 B】** 书 `[42] §11E`（`pp.417`–`422`，`6` 页）—— `Cambridge` 挡下载；若有机构/图书馆通道亦可 ✓
**【路 C（低成本备用，我下轮可做）】** 抓 **`arXiv:0707.4007`（Part III）**，看是否含 rank-4 汇总（概率低但不贵）✓
【⛔ 纪律】**不计算、不实现**；`C2` 暂停 ✓
【边界】 §1–§2 为**本地验证过的下载/检索结果**（文件已归档）；§3–§4 为**本档判定**；未制造候选／未启动搜索／未碰 RH。

## §5 【技术词回查】（补录）
```
技术词 universal polytope 命中文件数=6    :: ./C3-modular-coverage-range-and-free-next-stop.md ./C3-parameter-table-spec-and-status.md ./C3-RGPF-II-free-source-extracted.md 
```
