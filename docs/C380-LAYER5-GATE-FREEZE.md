# C380-LAYER5-GATE · FREEZE — 五层分离门槛（**门卫 spec**；零计算）

已查地图（**先跑后写** ✓）：`C380-SUPPORT-CLOSURE-AND-LAYER5-GATE.md`（**硬收官 ＋ 门槛登记** ✓✓）、`K4-T-a-CERTIFICATE-RESULT.md`（**k=4 证书** ✓✓）、`C257`／`C258`／`C259`／`C287`（**`SURVIVOR-5` 五条件判据体系 ＋ **元判据递归警告** ✓✓）、`CLOSED-ROUTES-MAP.md`（**NO-GO 地图** ✓）、`C3793`（**higher-order moment 反重复墙** ✓）

D0: 本档对象 = **C-380-136：`C380-LAYER5-GATE` 门卫 spec 冻结**（档案已有对象 `SURVIVOR-5`／`k_min` 的**规范关系**；非重命名、非新对象）
D1: 0
FREEZE-ACK: 本档即冻结审计（连续 D1=0 ＝ 35 ≥ 3；本档为**规格登记**，未引入新自由度）

---

## §0 定位（照录唐先生 2026-09-22 18:41 ✓✓）

```
【性质】 本冻结 **不是**预先承诺某一种数学路线，而是 **"门卫 spec"** ✓✓
【两个核心目的】
   ① 防止"5 层分离"退化成**又一个数量性结论** ✗
   ② 在计算之前先**强制回答**：**究竟要产生什么新量，为什么它可能携带算术/谱信息** ✓✓
【与硬收官的一致性（照录）】 C-380 已证"**五层必须存在**"；
   现在**唯一**值得研究的是"**五层为什么会迫使某个此前不存在的数学量出现**" ✓✓
【本步真正产物】 **不是**"找到下一条路线"，而是把**什么才有资格成为下一条路线**先锁死 ✓✓
```

---

## G0｜输入事实 **LOCKED**（✗✓）

```
输入： **E_even ⊆ {k = 5}**，即五个 |u_j| **严格互异**（无 |u| 碰撞）✓✓
纪律： **不得重新证明、不得优化、不得重新计算这一事实** ✗✗
      （证书已闭合：不追 margin、不跑 B2、不"漂亮化"、不因预算剩余而重跑 ✓）
```

## G1｜目标（✓✓）

```
寻找一个由五层分离**必然产生**的、**此前主线没有的**可证明量
      **𝒬( five layers )** ✓
满足**至少一个**实质条件：
   ① 是**新的算术对象** ✓
   ② 或是**新的谱对象** ✓
   ③ 或建立两者之间**此前不存在的可证明接口** ✓
⛔ **仅**得到"某个量 > 0"而**没有后续接口** ⟹ **不能过门** ✗✗
```

## G2｜必须明确"新量到底是什么"（**先量后验，禁止倒造** ✗✓）

```
候选必须在纸面上先写出链条：
   五层分离  ⟶  **𝒬**  ⟶  可证明性质  ⟶  RH 接口        ✓✓
⛔ 尤其禁止：**先算一个漂亮常数，再反过来给它寻找解释** ✗✗
```

## G3｜独立性门槛（**直接 NO-GO，无需计算** ✓✗）

```
候选**不得**只是下列任一者（否则立即 `ARCHIVE / NO-GO`，**无需计算** ✗）：
   ① K4 证书的**重新编码** ✗
   ② **k=5 的计数本身** ✗
   ③ 五个节点的 **Vandermonde 非退化** ✗
   ④ 已知**正定/Gram/SOS** 结构换一种记号 ✗
   ⑤ 旧**偶层约束的线性组合** ✗
   ⑥ **S1 的变体** ✗
   ⑦ **β-blind 的频率量** ✗
```

## G4｜RH 接口必须**后置但具体**（✗✓）

```
不要求一开始就证明 RH ✓；但**必须**能写出**明确的接口形态**，例如
   𝒬  ⟹  某个 arithmetic invariant  ⟹  某个 zero/spectral statement        ✓✓
⛔ 若只能写成"**可能与 RH 有关**" ⟹ **不启动** ✗✗
```

## G5｜`SURVIVOR-5` **只作外部验收标准**（**严格沿用档案原义，不重新定义** ✓✓）

```
本门槛**自己只负责**判断：  五层分离  ⟹?  新增可证明量        ✓
过门**之后**才接受档案既有的 **N ∧ NR ∧ A ∧ Q ∧ R** 与 **Scale Gate** ✓✓
（引用 `C257`／`C258`／`C259`／`C287`；`C259` §5：SURVIVOR-5 ⟹̸ 突破 ⟹ 必须再过 scale gate ✓）
```

## §1 三出口（冻结 ✓✓）

```
【EXIT-A · PROMOTE】 找到满足 **new mechanism ＋ new quantity ＋ independent value ＋ RH interface**
   且存在**现实的可验证闭合路径**的候选 ✓✓
【EXIT-B · ARCHIVE】 证明五层分离**目前只产生**已知结构、重新编码、或**没有 RH 接口** ⟹ 归档 ✓✗
【EXIT-C · GAP/HOLD】 确实存在**新的数学量**，但其**关键桥接尚未闭合**，且**值得保留为独立资产** ✓
   ⚠️ **HOLD ≠ 允许无限期继续烧预算** ⟹ 必须**明确写出剩余缺口是什么** ✓✗
```

## §2 ⭐ STOP 条款（**立即 STOP**，冻结 ✓✓）

```
① 纸面预检发现  **𝒬 = 旧量的重命名 / 投影 / 线性组合**  ⟹ **立即 STOP** ✗✗
② 发现 **五层分离只增加"非退化性"，没有新增算术内容**  ⟹ **立即 STOP** ✗✗
③ RH 接口仍然需要  **local finite data → global zeros**  ⟹ **立即 STOP** ✗✗
④ 或重新撞上 **β-blind / Weil 正性墙**  ⟹ **立即 STOP** ✗✗
（另：**不得**用本门槛包装 G3 任一形态；**不得**在 STOP 后自行换名续走 ✓）
```

## §3 内部一致性注记（⚠️ 引 `C287` §2，防**元判据递归** ✓✓）

```
`C287` §2 实测结论：档案馆已有 **≥10 套判据体系**（FZ-1/FZ-2/FZ-3、六筛子、N46、Rule T／Rule F、
SURVIVOR-5、B1–B5、检验床、V188 四通道）；**再加第九套且零新维度 = 元判据递归** ✗✓
⟹ 因此 `C380-LAYER5-GATE` **不是**新的"判据体系"，**仅是**针对**单一问题**（五层分离 ⟹ 新增量？）
   的**门卫 spec** ✓✓
⟹ 硬约束：**不得**把本门槛外推用于其它问题；**不得**据本门槛生成新的候选生成器 ✗
   （呼应 `C287` 的行为约束：**停止优化候选生成器** ✓✓）
```

## §4 执行纪律（冻结 ✓✓）

```
· 本刀 = **纸面 spec freeze**；**零计算、零搜索、零候选优化** ✓✓
· 目标 **不是**"找到路线"，而是**锁死资格** ✓✓
· **不**触碰已闭合的支持下界证书；**不**重开任何 CLOSED 对象／ARCHIVED 路线 ✓✗
· **不**把本档读作 RH 进展（负边界沿用 `C380-SUPPORT-CLOSURE-AND-LAYER5-GATE.md` §5）✓✓
· 下一步（须发令）：对**具体候选**执行 G0–G5 ＋ §2 STOP 的**纸面预检**（仍零计算），
   出口按 §1 三态判定 ✓
```

## §5 【技术词回查】输出（`scripts/tech_word_check.sh`，**先跑后写** ✗✓ —— 本档初稿误为"先写后跑"，已按官方输出改正）

```
技术词 门卫           命中文件数=1    :: ./C380-LAYER5-GATE-FREEZE.md 
技术词 spec             命中文件数=303  :: ./B-SERIES-INDEX.md ./C3880-... ./grh-goldbach-paper-draft-v2.md 
技术词 独立性门槛  命中文件数=2    :: ./p2p-anticircular-audit.md ./C380-LAYER5-GATE-FREEZE.md 
技术词 RH 接口        命中文件数=8    :: ./C262-critical-line-proportion-audit-structural-seal-and-classA-closure.md ./ASSETS-REGISTRY.md ./C261-Lambda-upper-bound-four-layer-audit-exit-type-2-quantitative-asset.md 
技术词 元判据递归  命中文件数=6    :: ./C305-FSD-blind-spot-audit-program-four-classes-dual-ledger-five-rounds.md ./C287-closure-ledger-AB-closed-C-not-reopened-and-C288-five-condition-crosscheck.md ./C329-route-consolidation-decision-record.md 
技术词 SURVIVOR-5       命中文件数=10   :: ./C257-mechanism-space-search-third-family-quantity-change-outside-FZ1-grid.md ./C380-MASTER-LEDGER-FREEZE-2026-09-22.md ./C260-C142-classB-enumeration-complete-three-gates-no-mainline-candidate.md 
```

**分类（按检查器要求逐项标注）：**

```
本档新增：       ① `门卫`（命中 1，**仅自命中**）⟹ 本档首次命名 ✓
                   ② `门卫 spec`（以 `门卫` 计）⟹ 同上 ✓
通用词（不计）：  `spec`（命中 303，通用词，**不计**）✗✓
档案已有（引用，不列为提出）：
   ③ `独立性门槛`（2）—— 另有 `./p2p-anticircular-audit.md` ⟹ **引用** ✓
   ④ `RH 接口`（8）—— 另含 `C261`／`C262`／`ASSETS-REGISTRY.md` 等 ⟹ **引用** ✓✓（档案词汇已有先例）
   ⑤ `元判据递归`（6）—— 含 `C287`／`C305`／`C329` ⟹ **引用** ✓
   ⑥ `SURVIVOR-5`（10）—— 含 `C257`／`C260` 等 ⟹ **外部验收标准，不重定义** ✓✓
```

⚠️ **纪律失误记录（本档内自纠）**：§5 初稿系"**先写后跑**"（凭印象填 0 命中），与实测不符 ✗；
已按 `TOOLS.md`（2026-09-18 同型教训）**重跑官方脚本、逐字粘贴、逐条分类**后改正 ✓。
**硬规矩重申**：凡"本档新增 / 首次命名 / 0 档"类**新性主张**，**一律先跑回查再写该节** ✓✓
