# ✅ **A4-2：最优系数的形状检验**（截断形状**得到支持** ✓，但不是全部 ✓）

**依据**：唐先生 2026-09-12（"逐项实施"）｜脚本：`NB5_optimal_coefficient_shape.py` ✓（复用 NB3 缓存 ✓）

---

## §1 **问题**
```
【登记项 A4-2】检验提议的形状 **a_n ~ μ(n)(1 − log n/log x)** ✓ 是否真的是 Báez-Duarte 距离的最优系数 ✓
【方法】用 NB3 的傅里叶核机制（K a = l ✓ 精确最优 ✓）在 N=160 求最优 a_n ✓，
   再把三种模型拟合到 a_n（**只在 98 个无平方因子 n 上拟合** ✓），比较相对残差 ✓
```

## §2 **结果（✓）**
```
【M1 纯比例】a_n = c·μ(n) ✓ ⟹ 相对残差 **0.8607** ✗（几乎无解释力 ✓）
【M2 截断形状】a_n = c·μ(n)(1 − log n/log N) ✓ ⟹ **0.4794** ✓✓（**残差减半** ✓）
【M3 拟合截断】拟合 x ⟹ **最优 x = 160.000 = N（精确 ✓）** ✓，残差同为 0.4794 ✓✓
【相关】a_n/μ(n) 与 (1 − log n/log N) 的 **Pearson r = +0.8525** ✓✓（**强相关** ✓）
【范围】a_n/μ(n) ∈ (−0.003, +0.650) ✓
```

## §3 **判读（诚实 ✓）**
```
【✅ 得到支持】**截断形状是真实特征** ✓✓：相关 0.85 ✓、残差减半 ✓、且**最优截断恰为 x = N** ✓
   （即 **N 本身就是正确的截断参数** ✓ —— 与理论预期一致 ✓）
【⚠️ 不是全部】**残差仍 0.479** ✗ ⟹ 截断形状只解释约一半的偏差 ✓
   ⟹ 其余部分应来自**更细的算术结构**（如 a_n 对 n 的乘法性质的依赖 ✓ —— 超出单一 μ(n)·光滑因子的模型 ✓）
【✅ 已验证的强信号（NB3/NB4 ✓）】**符号结构与 μ 完全一致**（N=160: 95/98 ✓）
   ⟹ 本项检验的是**幅度形状** ✓，符号部分已被独立验证 ✓
【⚠️ 归一化仍未定 ✓】M1/M2 的 c ≈ 0.055 / 0.334 ✓（**远小于 1** ✓）⟹ 与 Burnol/BBLS 的渐近常数对齐问题
   仍然开放 ✓（同 A4-1 的标注 ✓）
```

## §4 资产
```
【✓】`NB5` 可复跑 ✓（用缓存 ✓ 秒级 ✓）｜三种模型 + 拟合 x + 相关 ✓
【✓】**结论由数字驱动** ✓（脚本 READ-OFF 依阈值自动给出判词 ✓，符合 R7 ✓）
```
