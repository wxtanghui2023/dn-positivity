# 🎯 **下一轮宪章**：非-Gram 不定型搜索 —— **zero-sensitive vs cylinder-blind 作第一刀**

> 依唐先生 14:05：D-GRAM 全线关闭；下一轮**不得**从 Gram／窗／$\partial_\sigma$／reflection／Toeplitz 变体出发；直接从**非-Gram 不定型**开始，且把零点敏感性检查**放在第一刀**（不是最后才查）✓

---

## §1 D-GRAM 净结论（措辞收紧后，可引用）
$$\text{已封死类}\ =\ \boxed{\{\text{正测度 Gram}\}\cup\{\text{monomial／}\partial_\sigma\ \text{闭包}\}\cup\{\text{整数可计算 cylinder 数据}\}}✓✓$$
$$\text{类内}：\ \boxed{\text{要么 PSD；要么即使人为制造不定性，也没有零点全局定位信息}}✓✓$$
$$\text{要得到真}\ (1,1)\ \text{零点块} \Longrightarrow \textbf{必须进入 Weil／显式公式的值面结构}✓✓$$

## §2 两条因果链（照录并钉死）
$$\boxed{\text{integer-only}\Longrightarrow\text{cylinder}\Longrightarrow\text{zero-configuration blind}}✓✓$$
$$\boxed{\text{zero-pair sensitivity}\Longrightarrow\text{global spectral data}\Longrightarrow\text{value-surface／Weil}}✓✓$$
$$\Longrightarrow \text{下列手段}\ \textbf{不能再搭桥}：\text{换窗}\ \big|\ \partial_\sigma\ \big|\ \text{复}\ s\ \big|\ J_x\ \big|\ \text{非正 Toeplitz}✓✓$$

## §3 ⭐ 关键保留（唐先生原话的推广）
$$\boxed{\text{不定}\ \not\Longrightarrow\ \text{有零点信息}}✓✓✓$$
$$\Longrightarrow \text{此前"找到一个 indefinite matrix"}\ \textbf{本身没有意义}；\text{真正需要的是}$$
$$\boxed{\text{signature}\ \textbf{必须是 zero-configuration functional}}✓✓\qquad(\text{否则只是 cylinder 上的人工谱})✓$$

## §4 搜索树（照录）
$$\begin{array}{c}\text{non-Gram indefinite form}\\\downarrow\\\text{是否 zero-sensitive？}\\\begin{cases}\text{否}&\Rightarrow \textbf{V181}\\\text{是}&\Rightarrow \text{审计 HS}\\\ &\begin{cases}\text{integer energy}&\Rightarrow\boxed{\text{真正突破候选}}\\\text{prime correlation}&\Rightarrow\textbf{SUPPORT-1}\end{cases}\end{cases}\end{array}$$
$$\textbf{判死标准}：\text{若零点敏感性最终可被消去（退化成 cylinder 数据）}\Longrightarrow \text{直接归}\ \textbf{V181}；\ \text{若保留，则须查 HS 是否}\ \textbf{不可避免地} \text{产生 prime-side correlation}✓✓$$

## §5 未决引理 $\Lambda_{HS}$（本档登记，唯一把"已审计类"升级为"绝对"的缺口）
$$\boxed{\Lambda_{HS}：\text{任何}\ \textbf{zero-sensitive}\ \text{不定 Hermitian 型}\Longrightarrow\|\\cdot\|^2_{HS}\ \text{必须重新承载显式公式的素侧信息}}✓✓$$
$$\qquad \textbf{未证}✓\qquad(\text{证成} \Longrightarrow \text{中间地带}\ \textbf{绝对} \text{为空}；\ \text{证否} \Longrightarrow \text{突破候选})✓✓$$

## §6 第一刀的正式定义（下一轮开工即用，**先判这两条，不先算 HS**）
$$\textbf{(ZS) zero-sensitive}：\tilde G\ \text{的 inertia}\ (n_+,n_-)\ \text{是}\ \textbf{zero-configuration 的函数}✓$$
$$\qquad\text{可操作形式}：\text{由显式公式}\ \textbf{可证}\ n_-\ \ge\ \#\{\text{window 内 off-line pairs}\}\ \text{（或等价）}✓✓$$
$$\textbf{(CB) cylinder-blind}：\tilde G\ \text{的条目}\ \textbf{由整数数据单独决定} \Longrightarrow \text{归}\ \textbf{V181}✓$$
$$\textbf{第一刀}：\text{对每个候选先判 (ZS)／(CB)}；\ \text{两条都不成立者}\ \textbf{不进入 HS 审计}✓✓$$

## §7 被禁止的出发点（照录唐先生）
$$\boxed{\text{不得从}\ \text{Gram／窗／}\partial_\sigma／\text{reflection／Toeplitz 变体}\ \text{出发}}✓✓$$

## §8 边界
$$\text{(i)}\ §1--§4\ \text{照录唐先生 14:05；}\ §5\ \Lambda_{HS}\ \text{为本档登记（未证）}✓\quad\text{(ii)}\ \textbf{未用 RH}；\ \textbf{零计算}✓$$
