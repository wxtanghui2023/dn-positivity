# REVIEW-2026-09-16 · **决策地图：V262–V284**（**复盘档**；本档**不推数学**，只做状态归档与防原地打转）

> 用途 ✓ 唐先生 2026-09-16 11:35 指示：**"只做复盘，不再推数学"**；把 V262–V284 固化成决策地图；尤其要把**四种状态严格分开**，避免以后再把"局部失败"升级成"全局不可能" ✓
> 级别 ✓ 本档为**索引级复盘档**（与 `MASTER-STATUS-AND-CLOSURES`／`CLOSED-ROUTES-MAP` 同级，**不占 V 编号**）✓

## 【状态标签legend】（全文严格使用）

| 标签 | 含义 | 使用纪律 |
|:--:|:--|:--|
| **①不可能** | **已证**不可能（定理级／定义级／集合论恒等式） | 可直接引用 ✓ |
| **②族内失败** | **在已审计族中失败**（枚举非穷尽） | **不得**升成为"不可能" ✗ |
| **③⟺RH** | **等价于 RH 本身**（重述／循环） | **不计为进展** ✗ |
| **④未决** | **尚未解决**（无已知实例／无已知构造／概念未形式化） | 可保留为靶 ✓ |

---

## 1. 今日主线与目标变化（**元目标链**）

```
"找 RH 机制" → 机制／判据之分（V270）→ 证书（V271-A）→ D1_C vs D1_P（V278）
→ C0（V274-A／V275）→ 唯一格（V275 §2）→ C0 ⟺ FQS（V279 §5）→ **一比特（C0 是否为空）**
```
$$\boxed{\text{今日主要产出}\ ＝\ \text{目标被}\ \textbf{逐次精确化} \text{本身};\ \text{而非新机制}} ✓✓$$

---

## 2. V262–V284 时间轴（一行一档）

| 编号 | 一句话 | 状态 |
|:--:|:--|:--:|
| `V262` | 逆极限墙：有限非空＋任意 π ⟹ lim← ≠ ∅；紧致不逃逸；空 ⟺ 有限见证 ⟹ 四逃逸口收缩为**非紧致｜非投射** | **①** |
| `V264`／`V265` | N1／N2 与 GAP(乙) 坐标**查重**：均已被 `E108`／`ATTACK-PnQ`／`POS3` 执行过 | **②** |
| `V267` | (乙) 第三型锥三情形：算术可判定 ⟹ 排除；含 archimedean ⟹ A-leak；非有限可判定 ⟹ `V211` §5 | **②**＋**④** |
| `V268` | (甲) R1：**反自对偶免费**（Φ=f(s)/f(1-s)）；局部乘性幂族反自对偶 ⟹ 平凡 | **①** |
| `V270` | 判据／证书之分；**V270-A**：类级＋ζ-carrier＋cylinder ⟹ 不能正确判定（乘子构造） | **①** |
| `V271` | **V271-A：NC ⟹ 不能给证书**；(III) 候选分类五行 ⟹ 第⑤行无实例 | **①**＋**④** |
| `V272` | **H1 三分不成立**（cylinder／NC 定义级穷尽，(II) ⊆ (I)∪(III)）⟹ 唯一实质项＝(II) 支裁决类型 | **①**＋**④** |
| `V273` | **V273-A：真 global 障碍（Ш 型）⟹ 裁决必为 NC**；逐实例：Ш／Selmer／BM ⟹ NC；已审计 (II) ⟹ 常数 ⟹ 空层 | **①**＋**②** |
| `V274` | **勘误（Robin 是 NC 不是 cylinder）**；**V274-A：唯一格 ⟺ 非 ζ-local 有限证书** | **①** |
| `V275` | **Certificate Barrier（条件式）**：封口段＋等价段 ⟹ 唯一未封出口＝非 ζ-local 有限 cylinder carrier；唯一未决比特 **C0** | **②条件式**＋**④** |
| `V276` | C0 ⟺ C0-A ⟺ C0*；**A₁ 已在档案**（`E103` Lemma A 等）；A₂ 曾是判断（**已被 `V281` 降级**） | **①**＋**②** |
| `V277` | **V277-A：有限值＋连续（或可计算）⟹ cylinder**；**V277-B：非 cylinder ⟹ 不连续 ⟹ 不可计算** | **①** |
| `V278` | **$(D1_P\Rightarrow D1_C)$ 为假**（Con 型反例，Gödel 第二）⟹ 第 5 行分裂 C／U／P | **①** |
| `V279` | **C0 ⟺ ¬FQS**（集合论）；D1_C 三分（存在／canonical 可描述／非循环可证）；商生成方式六类 | **①**＋**②** |
| `V280` | **轨道不变量 ＝ 词-字符 span**（经典）；$k\ge2$ 部分之 `V258` 接管**已降级为待证**（`V281` §0） | **①**＋**②** |
| `V281` | 勘误（含 conductor ⟹ 非单射 ≠ 同纤维）；**C0 的真卡点**＝"是否存在 Euler-class 内有限局部同纤维其 off-line 状态可不同" | **①**＋**④** |
| `V282` | **$\mathrm{L3^\star}$ 的 E1–E5 分层**：E1／E2 假（Beurling，非算术）｜level 1 真而平凡（Hamburger）｜其余未决 | **①**＋**②**＋**④** |
| `V283` | **$N(\sigma,T)$ 本性属值面**（argument principle ⟹ 全素数求和＋archimedean）⟹ 甲 ＝ case B（截断显式公式） | **①**＋**②** |
| `V284` | 五族候选（S 线／Hecke／θ–Euler／Selberg／δ-Frobenius）**全数失败**；**(III) ⊥ (V) 原理性互斥** | **②** |

---

## 3. 每条路线的**真正死因**（逐条对应，不合并）

| 路线 | 死因（精确） | 状态 |
|:--|:--|:--:|
| 非满射制造全球障碍（口 C） | `V262`-A：有限非空＋任意 π ⟹ 极限非空（**满射条件多余**） | **①** |
| 紧致系统制造"每层可延拓却突然失败"（口 A） | `V262`-B：紧致 ⟹ 空 ⟺ **有限见证** | **①** |
| "有限窗口可逼近"整类 | `V133` Theorem A（极限盲性） | **①** |
| 有限局部数据决定 ζ 的 off-line 性（类级） | `V270`-A 乘子构造（同层数据、相反判定） | **①** |
| NC 机制给证书 | `V271`-A（证书必 cylinder ⟹ 与 NC 矛盾） | **①** |
| (II) 支作为"第三种裁决类型" | `V272` §2：cylinder／NC 定义级穷尽 ⟹ (II) 只是**语言** | **①** |
| 真 global 障碍（Ш／Selmer／BM） | `V273`-A：**处处局部平凡 ⟹ 全层同数据 ⟹ NC** | **①** |
| 连续／可计算的有限压缩 | `V277`-A：⟹ cylinder（预层开覆盖＋紧性） | **①** |
| 非圆柱压缩 | `V277`-B：⟹ 不可计算 ⟹ 成分归 `V211` 八类 | **②** |
| $D1_P\Rightarrow D1_C$（有限证明给有限数据证书） | `V278`-A：**Con 型反例**（Gödel 第二） | **①** |
| 有限商分离（把分离当机制） | `V279` §5：C0 ⟺ ¬FQS（集合论）；且分离存在≠可描述 | **①** |
| 轨道型商（非聚合非谱者） | 已审计实例**退化**（`V241`-D／`V177` 作用平凡）；RF\* 裸算术可得 ρ ⟹ ζ-local ⟹ L1 | **②** |
| 尾部变形移动零点（同一 ζ） | `V126`-L3（B 层不可实现；强制约束＝显式公式）—— **但 L3 只能当前提于"非算术类"** | **②** |
| L3 作为封口前提 | `V282`：E1／E2 假（但非算术）｜level 1 **真而平凡**｜其余**未决** | **②**＋**④** |
| $N(\sigma,T)$ 作有限谱判据（甲′） | `V283` §1：**本性属值面**（argument principle）⟹ 只能截断＋误差 ⟹ 已审计预算墙 | **①**＋**②** |
| 0.682／T²／第三矩的预算优化 | 只在 value-face 上提高常数（唐先生 11:31 判定） | **②** |
| 算术一致性 ⟹ 谱位置刚性（V284） | 五族全败 ＋ **(III) ⊥ (V) 互斥**；韵脚中 β-刚性均来自 positivity 而非闭合 | **②** |
| positivity 路线（Weil／Li） | 充分 ⟹ **等价于 RH**（`POS1`／`POS2`） | **③** |
| S 线（算术反自对偶平衡因子） | `V177` 必 coboundary ⟹ 平凡；`V181` **结构性关闭**（唐先生 09-15 12:11 拍板） | **①**＋**②** |
| δ-几何／Buium | `G10` 已封 | **②** |
| commutator 路线（V206–V208／`V241`） | 归约为熟悉算术量；holonomy 平凡 | **②** |

---

## 4. **已经不能再重复的方向**（防原地打转清单·甲）

```
① 正性类六机制再枚举（POS3 §2 已穷尽枚举）／不等式族（Robin 型）—— 均为**判据**，给不出证书 ✗
② 锥分离／第三型正性作**加锐工具**（`V248` 已判：判别锥必自对偶 ⟹ 回角 I）✗
③ higher associator／"更高阶兼容条件"（`V269`-A′：仍属 cylinder ⟹ 不逃逸）✗
④ G3 作为"第三种机制"（`V273` §7：G3 ＝ 障碍的结构语言）✗
⑤ 轨道型／词-字符型（`V280`-A：不变量＝词-字符 span；RF* 收口）✗
⑥ S 线／δ-几何／commutator／乘子 F_σ 证 ζ 本身（各已封或仅证对象类层面）✗
⑦ 把 L3／"Euler 全局约束"当**前提**（`V282` §3：须按类分层）✗
⑧ 把局部谱／零点计数当"零点位置"（`V283`：N(σ,T) 属值面；N(T) 不含横向信息）✗
⑨ 优化 0.682／T²／第三矩预算（value-face 上的常数优化）✗
⑩ 含糊使用 "certificate"（必须写明 D1_C 或 D1_P）／含糊使用 "canonical"（未形式化）✗
⑪ 跳过"先查重／先找反例"（`E107` 规则）＝ 本轮之前多次踩坑的唯一来源 ✗
```

---

## 5. **仍然开放的接口**（全项目现存，逐条标注）

| # | 开放项 | 形态 | 若能解决会得到什么 |
|:--:|:--|:--|:--|
| **O1** | **C0**（唯一格是否为空） | 单点／全局存在性；**⟺ 非 ζ-local 有限证书存在性** | 空 ⟹ Certificate Barrier 封口；非空 ⟹ **直接得证书** |
| **O2** | **(II) 支裁决类型**（未审计实例） | 逐实例判定 cylinder／NC | NC ⟹ 由 L2 挡死；cylinder 非 ζ-local ⟹ 进 S／S′ 审计 |
| **O3** | **$\mathrm{L3^\star}$ 在 E3($q>1$)／E4／E5 内的真值** | 弱化版（$\beta$ 整数部分／零点计数）才可能非 GRH 型 | 可分离 ⟹ 打开 C0；刚性 ⟹ 封口 |
| **O4** | `V211` §5 UNINSTANTIATED（非有限缺陷） | 非加性／非上同调／非 index／非 $\Pi^1_1$／非选择 | 实例出现 ⟹ 新结构类别 |
| **O5** | **canonical $\rho$ 概念的形式化** | 元定义层 | ⚠️ 价值有限：`V281`／`V284` 表明形式化后仍落 L1 或 P1 |
| **O6** | `§E.4` 类表完备性 | 其命运绑定于"∃-型 RH 等价判据是否存在" | 完备 ⟹ 空间真正关闭 ⟹ 改变目标 |

$$\Longrightarrow \boxed{\text{对"执行型算术机制"目标，}\textbf{只剩 O1 一比特};\ \text{其余为元层或 GRH 型}} ✓✓$$

---

## 6. **D1_C 与 D1_P 的最终边界**

| | **D1_C** | **D1_P** |
|:--|:--|:--|
| 定义 | 验证器读取**输入的有限数据**（∃S: $D=D_S\circ\pi_S$） | **有限形式证明**（∃π: $\pi\vdash$RH，$|\pi|<\infty$） |
| 与 cylinder | **⟹ cylinder**（`V277`-A） | **不自动**（`V278`-A） |
| 靶 | **C0**（⟺ FQS） | **可证明性**（标准问题，**非机制问题**） |
| 与 RH 的关系 | 递进：D1_C ＋ 可证等价 ⟹ **RH 真值可计算**（`V274`-B） | 证明的**存在性**不给数据条件；可搜索性只有半可判定 |
| 边界更正 | **Certificate Barrier ⊂ D1_C**（`V278` §6） | 若把 D1 读作 D1_P，`V275` 封口段**不成立** |

$$\boxed{\text{唐先生原始目标（"从算术结构内部产生有限、可执行、可检查、非循环的机制"）}\ =\ \mathrm{D1_C}} ✓✓\ \text{（故 D1 := D1_C 为正当目标澄清）}$$
$$\qquad ⚠️\ \text{但}\ \textbf{不得} \text{声称封住了"RH 有有限形式证明"这条路线}（＝标准可证明性问题）✗$$

---

## 7. 对下一轮研究的**决策意义**

```
① 若目标是"执行型算术机制"：**只剩 O1 一比特**；且其两角已被定位 ——
   分离角需"合法类中可证 off-line 的算术对象"；非分离角 ⟺ "RH 不可有限认证" ⟹ 两角当前都不可攻 ✓
② 若目标是"RH 的数学进展"：应**离开 C0／证书框架**，转真实数学问题（如论文线：Brown／Li 两篇 ✓）
   或按 `§E.4` 第二条出路**改变目标**（承认"找不到且能论证完整性 ⟹ 空间真正关闭"）✓
③ 若目标是"确认边界"：**现在已足够**（今日已把每个接口定位到具体已知墙或具体缺口）⟹ 可停止推数学 ✓
④ 无论哪条：先执行 §8 的防升级清单 ✓
```

---

## 8. ⚠️⚠️ **绝不能升级的清单**（防原地打转清单·乙；本档最重要）

| # | 禁止 | 反例／正确的写法 |
|:--:|:--|:--|
| **N1** | 把**"已审计族中失败"**升成**"不可能"** | 六族生成方式／五族操作对／八类机制／E1/E2 无刚性 —— 均**枚举非穷尽** ⟹ 写"在已审计范围内失败" ✗→✓ |
| **N2** | 把**"未找到实例"**升成**"不存在"** | 唯一格／UNINSTANTIATED／NC 对象／canonical ρ ⟹ 写"无已知实例" ✗→✓ |
| **N3** | 把**条件式结论**当**无条件定理** | Certificate Barrier（依赖 D1–D3／P1–P3／L1／L2／S1）；V284 互斥（依赖 `V283` §1）⟹ 须带假设清单 ✗→✓ |
| **N4** | 把**"等价于 RH 本身"**当**进展** | 判据族（Robin／Li／Weil／Nyman–Beurling）；G3 的"存在性"问题 ⟹ 标 **③**，不计进展 ✗→✓ |
| **N5** | 把**局部谱／计数**当**零点位置** | $N(T)$ 不含横向信息；$N(\sigma,T)$ **本性属值面** ⟹ 不得作"横向判据" ✗→✓ |
| **N6** | 用 **$F_\sigma$ 乘子构造**证明关于 **ζ 本身**的结论 | 它只证"**对象类层面**"（`V259`-A 的适用范围）⟹ 不得移用于 ζ 本身 ✗→✓ |
| **N7** | 自动给障碍**贴 G3／上同调标签** | 须**显式**给出群／纤维化／链复形结构；否则只是重命名（`V272` §6(II)、`V273` §7）✗→✓ |
| **N8** | **含糊使用 "certificate"** | 必须写明 **D1_C** 或 **D1_P**（二者不同阶，`V278` §4）✗→✓ |
| **N9** | **含糊使用 "canonical"** | 该概念**尚未形式化**（`V280` §5 自标）⟹ 须标 [结构性] ✗→✓ |
| **N10** | **跳过"先查重／先找反例"** | `E107` 规则；本轮多数前期失误正来源于此 ⟹ 顺序固定为 **领号 → 查重（含"该线是否已关"）→ 反例搜索 → 写** ✗→✓ |
| **N11** | 把**"判据存在"**当**"证书存在"** | 判据 ∈ **③**；证书须过 D1_C ＋ 独立性闸门 D2 ✗→✓ |
| **N12** | 把**"该格空虚"**当**"封口成功"的证明** | 𝒩=∅ 时 C0 为"**空虚的真**"（$A_S=X_S$ 撞 P3）⟹ 必须要求分离由**真子集**实现（`V281` §5）✗→✓ |

---

## 9. 归档

```
本档 ＝ `docs/REVIEW-2026-09-16-decision-map-V262-V284.md`（索引级复盘档，**不占 V 编号**）✓
指针已登记于 `MASTER-STATUS-AND-CLOSURES.md`（非 V 条目行）✓
【今日全线】V262–V284（23 档）＋ 勘误 6 处（V270／V275／V280／V281 各若干）＋ 自查累计 12 次 ✓
【本档承诺】不推数学；只做状态归档与防升级纪律 ✓
```

---

## 10. 补记（V285–V287；把 O1 更新为最新口径）

$$\textbf{O1（原表述）}：\mathrm{C0}\ \text{是否为空} \iff \text{非 ζ-local 有限证书是否存在} ✓$$
$$\textbf{O1（更新后·等价形式）}：\boxed{\text{O1 的达成}\iff\text{在合法算术类内 settle GRH（任一方向）}} ✓✓✓$$
$$\qquad \text{依据}：\text{打开}\ \mathrm{C0}\（\text{证}\ \mathrm{FQS}）\ \text{需}\ \textbf{双向} \text{——可证 on-line 对象 ＋ 可证 off-line 对象，}\textbf{共享有限数据}（`V286` §3／`V287` §5）✓$$
$$\qquad \qquad \text{封口}\ \mathrm{C0}\（\text{证}\ \neg\mathrm{FQS}）\ \text{需"某层有限数据}\ \textbf{决定} \text{状态"} ⟹ \text{即一条可证的}\ \textbf{有限判据}（`V279` §5／`V287` §5(b)）✓$$
$$\qquad \qquad \text{逐候选驳倒（`V286`-A）}\ \text{只需}\ \textbf{单向}（\text{off-line 侧}）⟹ \text{可执行、可计算，但仅命中}\ \mathrm{REF}\ \text{内已知者} ✓$$
$$\textbf{三处新增状态标签（沿用四态）}：$$
$$\qquad \text{① `V286`-L：}\textbf{有局部数据}\ne\textbf{有 Euler 积}（b_p^2-b_{p^2}=2uv(1-\Re\chi(p)^2)\ne0）⟹ \text{off-line 对象}\ \textbf{可获有限局部数据} ✓$$
$$\qquad \text{② V287-A：甲}\iff\textbf{合法类内 GRH 反例} ⟹ \text{记}\ \textbf{④}（\text{目标＝开问题}），\textbf{不是} \mathrm{②} ✓$$
$$\qquad \text{③ V287-B：off-line 零点必落}\ \big(\tfrac12,1-\tfrac{c_d}{\log C}\big]\ \text{且}\ q(2+|t|)\gtrsim e^{c_d/(1-\beta)} ⟹ \textbf{唯一可攻窗口} ✓$$

---

## 11. ⚠️ 勘误（V288；更正 §10 的 O1 描述）

$$\text{§10 原写} ✗：\mathrm{O1\!-\!1}\ \text{达成}\iff\text{在合法类内 settle}\ \mathrm{GRH}（\text{任一方向}）\ ⚠️$$
$$\qquad \textbf{错在}：\text{把两个蕴含合并} ⟹ \text{经逐行审计（`V288`）：}\textbf{二者并不同真，而是}\ \textbf{互斥} ✓✓✓$$
$$\boxed{\text{更正后的正确关系}：\mathrm{O1\!-\!1}\Longrightarrow\neg\mathrm{GRH}_{\mathcal C};\qquad \mathrm{GRH}_{\mathcal C}\Longrightarrow\neg\mathrm{O1\!-\!1}（\text{互斥}）} ✓✓✓$$
$$\boxed{\text{正确分解}：\mathrm{O1\!-\!1}\iff\big[\neg\mathrm{GRH}_{\mathcal C}\big]\wedge\big[\text{有限可分性实现（P1–P3）}\big]\quad（＝\textbf{"}\neg\text{GRH-plus"}\ \text{，}\textbf{不是} \text{"GRH-plus"}）} ✓✓$$
$$\qquad \text{机制}：\mathrm{GRH}_{\mathcal C}\ \text{真} \Longrightarrow \mathcal R=\mathcal C \Longrightarrow \pi_S(\mathcal R)=X_S \Longrightarrow A_S=X_S \Longrightarrow \textbf{撞 P1（真子集）} ⟹ \mathrm{O1\!-\!1}\ \text{假} ✓$$
$$\qquad \qquad \text{即：}\mathrm{O1\!-\!1}\ \text{被锁定在}\ \neg\mathrm{GRH}_{\mathcal C}\ \text{的世界里}（\text{＝}\ `V281`\ \text{§5"空虚真"的排除代价}）✓$$
【定位改写】$\mathrm{O1\!-\!1}$ **不是**通往"证明 RH／GRH"的路（与 $\mathrm{GRH}_{\mathcal C}$ 互斥）；应改写为"**若类内存在可证 off-line 成员且分离可有限描述，则得机制**" ✓

---

## 12. O1-1 **最终状态归档**（V289–V290；结构性封口，非全局数学封口）

$$\boxed{\mathrm{O1\!-\!1}\ ＝\ \big[\text{合法类内 off-line 锚点}\big]\ +\ \big[\text{有限纤维分离}\big]} ✓✓$$
$$\qquad \text{两墙（分别锁定）}：\big[\text{off-line 锚点}\big]\longleftrightarrow\textbf{GRH 反例问题};\qquad \big[\text{有限纤维分离}\big]\longleftrightarrow\textbf{on-line 伪装排除问题} ✓✓$$
$$\qquad \text{两难的机制（`V289`）}：\text{有锚点的类}\ \mathrm{FS-}\ \text{成立（认证死）};\qquad \text{认证有可能的类}\ \textbf{无锚点} ✓✓$$
$$\boxed{\text{定性}：\textbf{结构性封口，非全局数学封口}}$$
$$\qquad \textbf{允许}：\text{"现有两类锚定路线均已被精确封锁"};\ \text{"锚定策略存在结构性二难"} ✓$$
$$\qquad \textbf{禁止}：\text{"O1-1 不存在"} ✗;\ \text{"已全局数学封口"} ✗;\ \text{"}\mathrm{GRH}_{\mathcal C}\ \text{已证"} ✗ ✓$$
$$\qquad \text{剩余出口}：\text{唯一}\ ＝\ `V287`\ \text{的靶（合法算术类内可证 off-line 成员）},\ \text{且}\ \textbf{该出口本身已触及 GRH} ✓$$
$$\qquad \text{且}：\text{继续在 O1-1 内换}\ S／\sim_S／\text{有限系数}\ \textbf{会重新落入 `V289` 两难} ✓$$
$$\Longrightarrow \boxed{\text{O1-1}\ \textbf{完成了它作为搜索工具的使命}} —— \text{把"找机制"精确化为两堵已知墙的合取} ✓✓$$

---

## 13. ⚓ **目标锚定**（唐先生 2026-09-16 12:09）

> 唐先生逐字：**「读文献可以，但我们的目标不是几篇小论文，请务必牢记」**

$$\textbf{与锚页同旨}（`00-ANCHOR-purpose-and-discipline.md`，唐先生 2026-09-12 14:18 定）：$$
$$\qquad \text{"我们和那 25 位数学家的目的是一样的，不是用 AI 做拔苗助长的事，而是认真踏实做 }\mathrm{RH}\ \text{以及其它猜想的证明，并不会为了一两个论文去急于求成"} ✓✓$$
$$\qquad \text{四条"要"}：\text{① 要理解，不要战绩}\ \text{② 要可核验，不要自述}\ \text{③ 要慢，不要抢（}\textbf{不为凑论文而写；只在真懂了时写}\text{）}\ \text{④ 要少而精，不要多而杂} ✓✓$$

$$\boxed{\textbf{对今日的修正}：\text{本档 §7 与 `V290` §3 的"}\textbf{开论文线}\text{"框架}\ ＝\ \textbf{漂移}} ⚠️✓$$
$$\qquad \text{论文线（P1–P8／A1-3／A1-4）的}\ \textbf{正确位置}\ ＝\ \textbf{副产品}（\text{可交付}\ \ne\ \text{目标}）;\qquad \text{A1-3 已降级（`V291`）} ✓$$
$$\qquad \textbf{目标侧唯一尚有数学内容的靶}：\ `V181`\ \text{交回的}\ \textbf{承重墙}：$$
$$\qquad \qquad \boxed{\text{承重墙}\ ＝\ \text{T}\log\text{T}\ ＋\ \text{Weil 正性};\qquad \text{核心缺口}\ ＝\ \textbf{局部算术结构}\ \not\Longrightarrow\ \textbf{全球谱定位}} ✓✓$$
$$\qquad \qquad ⟹ \text{攻此靶的两种可能产物}：\textbf{(i) 结构定理（＝理解）}\ \text{或}\ \textbf{(ii) 突破（RH 级）};\ \text{两者}\ \textbf{都不是"小论文"} ✓✓$$
$$\qquad \textbf{任何"突破"主张} \Rightarrow \text{须先过}\ \textbf{突破判据}（\text{档案既有资产}：`E30-2`\ \text{机制与精化判据};\ `E30-UNIFIED-CONVERSION-LAW`\ \text{的 T}^2\ \text{律＋log 律}）✓✓$$

$$\textbf{纪律}：\text{读文献只用于}\ \textbf{锁定事实}（\text{如 `V291` 的核验}）,\ \textbf{不得} \text{把"可发表"当作进展度量} ✓✓$$
