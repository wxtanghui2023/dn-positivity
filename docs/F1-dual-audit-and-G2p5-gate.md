已查地图：命中（`F2-structural-feasibility-verdict-A`／`P1-3-unresolved-finite-problem-pool`／`TARGET-L9-q11-source-closure`（源仓 ES(7) 记录））⟹ 引用，不开新案
D0: 本档对象 = 登记新门 **`G2.5`（可压缩性）** ＋ `F1`（`\mathrm{ES}(7)`）**现状＋规模双审计** ⟹ 三态 **`F1-B`（可能可行）**
D1: 0 （[REVIEW] 轮次：审计与估计，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`G2.5` 登记 ＋ `F1` 双审计**

## §0 ⭐ 新门（照录）

```
$$\boxed{G2.5\ \text{可压缩性}:\ \text{不是"有限"就算可攻击；必须存在合理的结构压缩，使有效搜索空间从不可计算量级降到可证书化量级}}$$ ✓✓
【完整门串】 $$\boxed{G1\land G2\land G2.5\land G3\land G4}$$ ✓（**先判可攻击，再判 open**）✓
```

## §1 `F1` 现状（三点确认）

```
【问题】 `\mathrm{ES}(7)` ＝"任意 `N` 个一般位置点中必含 `7` 点凸位"的最小 `N` ✓
【已知】 `\mathrm{ES}(5)=9`、`\mathrm{ES}(6)=17`（`=2^{4}+1`）；**`33\le\mathrm{ES}(7)\le113`** ✓（源仓 README 记录）✓
【`ES` 猜想】 `\mathrm{ES}(n)=2^{n-2}+1` ⟹ **`\mathrm{ES}(7)=33`** 即该猜想在 `n=7` 的断言 ✓
**(1) 开放** ✓｜**(2) 参数未知** ✓｜**(3) 无后续解决** ✓（源仓记录逐字："its full `33`-vertex run returned **UNKNOWN** after `300` seconds, **so no bound changed**"）✓✓
```

## §2 ⭐⭐ 规模账（本档核心）

```
【关键性质：凸位性只依赖 chirotope（抽象序型）】**"`7` 点凸位"只由四元组定向决定** ⟹ **`\mathrm{ES}` 类问题是抽象序型层面的问题** ⟹ **可用 chirotope／signotope 编码，无需真实坐标** ✓✓（这也是 `\mathrm{ES}(6)=17` 类计算的既有路线）✓
【变量量级】 三元定向 `s(i,j,k)`：`\binom{33}{3}=5456`；若四元组编码：`\binom{33}{4}=40920` ⟹ **`10^3`–`10^4` 量级** ✓
【约束量级】 "无 `7` 点凸位" ⟹ 对每个 `7` 子集禁掉凸位模式：`\binom{33}{7}=4\,272\,048` 条 ⟹ **`10^6` 量级（可行）** ✓✓
【对称群】 `S_{33}`（`33!`）—— **可破**：固定 `3`–`4` 个点消除大部分对称 ⟹ 未见 `F2` 式灾难 ✓
【证书规模（两种出口各一）】 **(i) 若 `SAT`（存在 `33` 点无 `7` 凸位）** ⟹ **证书＝显式配置** ⟹ **极小**；校验＝`\binom{33}{7}\approx4.3\times10^6` 个子集凸位检查 ⟹ **秒级–分钟级** ✓✓；**(ii) 若 `UNSAT`** ⟹ 证书＝`DRAT` ⟹ 规模取决于证明树（**未知，但不必然 `10^{79}` 级**）⚠️ ✓
【⟹ `G2.5` 判定】**满足**：有效空间被 chirotope 编码＋强约束（`4.3\times10^6` 条禁模式）＋对称破缺压到**可证书化量级** ✓✓
```

## §3 ⭐⭐ 决定性优点：**两个出口都是新结果**

```
$$\text{SAT}\ \Longrightarrow\ \mathrm{ES}(7)\ge34\ (\textbf{提高下界})\qquad\qquad \text{UNSAT}\ \Longrightarrow\ \mathrm{ES}(7)=33\ (\textbf{落实猜想在 }n=7\text{ 的断言})$$ ✓✓✓
【⟹ `G4` 自动满足】 两个出口均为**写死的整数命题**（`\mathrm{ES}(7)\ge34` 或 `\mathrm{ES}(7)=33`）⟹ **不是审计、不是 Lean 化** ✓✓
【⟹ 且不与"已解决"冲突】 现有记录 `33\le\mathrm{ES}(7)\le113` ⟹ **两端都还开着** ✓
```

## §4 三态判定与风险

```
$$\boxed{F1-B:\ \text{可能可行}}$$ ✓✓
【首个原型参数】 不必碰 `112`（上界侧）；**直接打 `n=33` 的判定问题**（"是否存在 `33` 点无 `7` 凸位"）✓✓ —— 这正是源仓试过但只给了 `300` 秒的那一问 ✓
【风险 1（建模）】 必须证编码**可靠且完备**：凸位性 ⟺ 四元组定向模式；`7` 点凸位 = 全部 `\binom{7}{4}` 个四元组一致 ✓（**须逐条核**）⚠️
【风险 2（难度）】 `4.3\times10^6` 条约束的 `UNSAT` 可能极难；**但"难"≠"不可行"**，且源仓 `300` 秒 `UNKNOWN` **不构成不可行证据** ✓
【风险 3（竞争）】 需查是否已有 `n=33` 的专门尝试（**未查**）⚠️
【⛔ 纪律】 本轮**未投计算、未跑 SAT**；`n=38`／`TARGET-L9`／RH 未作筛选依据 ✓
【边界】 §1 的 `33\le\mathrm{ES}(7)\le113` 与"`UNKNOWN` after `300` s"为**源仓 README 逐字**；§2 的变量/约束/对称/证书量级为**本档纸面估计**；§3–§4 判定与风险为**本档自行给出**；未制造候选／未启动搜索／未碰 RH。
