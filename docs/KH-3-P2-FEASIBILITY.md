已查地图：命中 1 处 —— 先逐条判 已DEAD/已封/已登记；命中即引既有条目，不得开新案
　判定：⭐ **实质命中 ＝ `ASSETS-REGISTRY` `P-3`**（**已判 ✓**），其逐字已含**统一负面洞察**：
　「**临界指数 `1/2` 的算术来源恰好是那些已知／已封闭的来源**（归一化＝**插入**；函数方程＝completion **同墙 `E4-1 S1`**；显式公式＝**已知判据**）
　⟹ **新规格：`1/2` 必须作为自洽方程／流的临界指数动态导出 ⟹ 精确指向 `P2`**」✓✓
　＋ `③-B-2` 构造 `\mathcal O_X = \int_1^X (\psi(t)-t)/t^2 dt`（**良定义、算术生成、逼近率 `\asymp X^{\beta_{\max}-1}` 确实读出 `β_max`**）但**三条同时失败**（无盲性／阈值靠插入／指数来源已封闭）⟹ **②FALSE** ✓
　⟹ ⛔ **本档不得把"指数来源全堵"当新结论**（**引用 `P-3`** ✓）；本档**新增者仅为"流层面"的 G1–G3 枚举与谱计算** ✓

# **KH-3 / P2-FEASIBILITY**（可行性审计：G1–G3 表 ＋ 最小障碍）

**唐先生令（2026-09-22 22:41）**：
① 选 **(乙) `P2` 可行性审计**，**暂不做 B1**（理由照录：KH-3 已把"熟悉对象"压到只剩 A1/B1，而 **C1 已证明真正的结构瓶颈是 `1/2` 的合法算术来源**；
　若 `P2` 三条硬要求本身不可同时满足，则 B1 即使构造出漂亮的 `\mathcal E(p,q)`，**仍易在 `P5` 再次撞上显式公式墙**）✓✓
② **只做可行性筛选，不做长证明** ✓；**顺序**：
　**P2-G1 枚举"算术流"的允许形态**（只收**以整数/素数乘法结构为原生状态**者：`n\mapsto an`、`n\mapsto n^k`、`p^a\mapsto p^{a+1}`，
　及由其生成的 divisor/lattice flow、valuation flow、乘性半群流、prime-power transition、convolution-induced flow）——
　⛔ **硬门**：**若"流"第一步就把 ζ／零点／显式公式／`1/2` 塞进生成元 ⟹ 立即 P2-DEAD** ✓✓；
　**P2-G2 不动点必须"被强制"**（须有**独立于 RH 的机制**使 `x_* = T(x_*)` 不是人为挑出；**若只能"选取 `x_*`"再算线性化 ⟹ 直接 G2 FAIL**）✓；
　**P2-G3 `1/2` 必须是导出的谱量**（目标形 `\lambda_* = \beta_{\max} - 1/2`，其中 **`1/2` ＝由算术流不动点线性化导出的本征量**，**而非"ζ 的临界线参数"**）✓；
　**五问**：**1** 建立前 `1/2` 是否**完全没出现**？**2** 是否由 `T` 的**不动点方程**产生？**3** 是否由 **`DT(x_*)` 的谱**产生？**4** ⭐ **若去掉 ζ，整个 `1/2` 是否仍然定义**（**Rule T 的硬测试**）？**5** 最后才把该谱量与 β 接起来？✓✓
③ **三出口**：`G1 有候选且 G2/G3 均可行 ⟹ SURVIVOR`（才进 B1/P5）｜`G1 有候选但 G2 或 G3 卡死 ⟹ GAP`（**精确定位卡点，不扩散搜索**）｜
　`所有算术流都在 G1/G2/G3 之一失败 ⟹ P2-FEASIBILITY CLOSED（本候选族）` ✓
④ ⭐ **审计顺序（照录）**：**不要先问"它与 RH 有什么关系"**；先问
　`算术流 → 强制不动点 → 线性化谱 → 1/2` **能否在无 RH、无显式公式、无零点输入下独立成立**；**能成立，才值得接到 `β_max`** ✓✓

D0: 本档对象 = **`P2` 可行性（算术流的强制不动点与线性化谱）**（引既有 `P-3`／`P3-B`／`P3-B2`；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算（解析）／零数值／未写程序**／未做数值搜索 ✓

---

## §1 **P2-G1**：允许的算术流（⛔ 硬门检查 ✓）

| 流 | 原生状态 | 生成元（**须纯算术** ✓） | 硬门 |
|:--|:--|:--|:--:|
| divisor involution `d \mapsto n/d` | 除数集 | 整数乘法 ✓ | ✅ |
| `n \mapsto an` | `\mathbb N` | 乘法 ✓ | ✅ |
| `n \mapsto n^k` | `\mathbb N` | 乘法 ✓ | ✅ |
| `p^a \mapsto p^{a+1}` | 素数幂集 | 乘法 ✓ | ✅ |
| **valuation flow** `v_p`（系数向量） | `\bigoplus_p \mathbb N` | 乘法 ✓ | ✅ |
| ⭐ **卷积流** `f \mapsto f \star g` | 算术函数**环** | Dirichlet 卷积 ✓ | ✅（⚠️ **须取 `g = \mu`（纯算术）**；⛔ 若取 `g = 1/\zeta` 即**把 ζ 塞进生成元 ⟹ DEAD** ✓✓） |
| **筛法流**（Buchstab／Dickman 递推） | 计数函数 | **素数整除性** ✓ | ✅ |

```
⟹ **G1 ✅ 有候选**（七类，皆**算术原生** ✓）；⛔ 硬门**已守**：ζ **不在**任何生成元内，
　在卷积流中 ζ **涌现**（作为 `\mu` 卷积的谱）而非**输入** —— 这一点是 Rule T 的关键 ✓✓
```

## §2 **P2-G2**：不动点是否"被强制"（✓ 关键表）

| 流 | 不动点 | 是否**被强制**（算术闭包/整数性/乘法结构） | 线性化 |
|:--|:--|:--|:--|
| `n \mapsto an`（`a \ge 2`） | **无**（`\mathbb N` 内）✗ | — | — |
| **`n \mapsto n^k`** | `1`（**乘法单位元**） | ✅ **被乘性闭包强制** ✓ | `DT(1) = k` |
| divisor involution | `\sqrt n`（`n` 为平方） | ⚠️ **依 `n`、非唯一** ⟹ 弱 | `DT = -1` |
| `p^a \mapsto p^{a+1}` | **无** ✗ | — | — |
| **valuation 除法流** | `1`（幺元） | ✅ **被乘法幺元强制** ✓ | **平移 ⟹ `\lambda = 1`** |
| ⭐ **卷积流** `f \mapsto f \star \mu` | **`\delta_1`（卷积单位元）** | ✅✅ **被环代数强制**（单位元／幂等 ✓） | **谱 ＝ Dirichlet 级数像之闭包** ✓✓ |
| 筛法流 | 一般**无**不动点（递推非自映射）⚠️ | — | 其"谱"由**零点**控制 |

```
⟹ **G2 ✅ 部分成立**：**三类**具**被强制的固定点**（`n \mapsto n^k` 的 `1`；valuation 的 `1`；卷积流的 `\delta_1` ✓✓）
　—— 皆由**代数结构**强制，**非人为选取** ✓；但**其线性化谱**见 §3 ✓
```

## §3 **P2-G3**：`1/2` 是否**导出的谱量**（五问 ✓✓）

| 流 | 1 建立前 `1/2` 出现? | 2 由不动点方程? | 3 由 `DT` 谱？ | **4 去掉 ζ 后 `1/2` 仍定义?** | 5 可接 β? | 判定 |
|:--|:--:|:--:|:--|:--|:--|:--|
| `n \mapsto n^k` | 否 ✓ | 否 | **`\lambda = k \in \{2,3,\dots\}`** ✗ | — | — | ⛔ **FAIL**（谱非 `1/2`；取逆得 `1/k` 但 `n^{1/k} \notin \mathbb N` ⟹ **破 G1**） |
| valuation 除法流 | 否 ✓ | 否 | `\lambda = 1` ✗ | — | — | ⛔ **FAIL**（平移谱） |
| ⭐ **卷积流** | 否 ✓ | 否 | **谱 ＝ `1/\zeta(s)` 值域闭包** ⟹ **产出零点（`\gamma` 侧）** ✓✓ **但不产出 `1/2`** ✗✗ | ⚠️ 要谈"临界半平面"须用**零点自由区**：无条件仅 `\mathrm{Re}\,s \ge 1`（Wiener 型）；**改进到 `1/2` 属 RH 级** ⟹ **循环** ✗ | — | ⛔ **FAIL（G3 卡死）** |
| p 进伸缩 `\|2x\|_2 = (1/2)\|x\|_2` | ⚠️ **`\lambda = 1/2` 可得** ✓ | 否 | `\lambda = 1/2`（仅 `p = 2`） | ✓ | ⛔ **无机制连接 β** | ⛔ **FAIL**（**纯 `1/2` 但缺机制 ⟹ 记 flag，不记候选** ⚠️） |
| 筛法流（Buchstab） | 否 ✓ | 否 | 振荡由**零点**控制 ⟹ 衰减率 `1/2` **须先知道零点位置** ✗ | ✗ | — | ⛔ **FAIL**（`1/2` 来自**零点输入**） |

```
【G3 谱枚举结果】 五种算术流可得的谱量 ＝ `\{k \ge 2\}\ \cup\ \{1\}\ \cup\ \{\text{Dirichlet 级数像}\}\ \cup\ \{1/p\}\ \cup\ \{\text{零点控制的衰减}\}` ✓
　⟹ **`1/2` 只在两种情形出现**：(a) **p 进度量 `p = 2`**（**无机制连接 β** ⚠️）；(b) **零点输入**（**循环** ✗）✓✓
```

## §4 ⭐ 最小障碍（本档核心 ✓；**与档案 `P-3` 逐条对应**）

```
【障碍陈述】 算术流的**线性化谱自然取值集合** ＝ `\mathbb Z_{\ge 1}`（幂映射）`\cup \{1\}`（平移）`\cup`
　`\{\text{Dirichlet 级数像}\}`（卷积）`\cup \{1/p\}`（p 进度量）—— **`1/2` 不在其中**（除 `p = 2` 的度量为 `1/2`，**但缺连接机制**）✓✓
　⟹ 要得到 `1/2`，**只能**引入：**ζ 的泛函方程**（**已知 ⟹ 同墙 `E4-1 S1`**）／**零点的实部信息**（**循环**）／**人工归一化**（**插入**）✓
【⭐ 独立复现档案结论】 上述三条**与 `ASSETS-REGISTRY` `P-3` 逐字条目一一对应**
　（归一化＝插入；函数方程＝completion 同墙；显式公式＝已知判据）⟹ **本档从"流／谱"侧独立复现 `P-3` 的"临界指数来源全堵"** ✓✓
【新增（相对档案）】 `P-3` 是**缺陷来源级**的结论；本档补的是**流级**枚举：**哪些流有被强制的固定点**、**其线性化谱落在哪个集合** ⟹ **解释了"为什么堵"** ✓✓
```

## §5 出口（依令 ③ ✓）

```
【出口】 ⭐ **GAP**（`G1` ✅ 七类候选；`G2` ✅ 三类被强制的固定点；**`G3` 卡死**）✓✓
【精确定位卡点（不扩散 ✓）】 $$\boxed{\text{算术流的线性化谱给出 }\gamma\text{（零点位置）}，\ \textbf{不给 }1/2}$$ ✓✓
　（即：**能动态产出"零点在哪"，不能动态产出"实部为何是 `1/2`"** ✓）
【⛔ 不宣布 `P2-FEASIBILITY CLOSED`】 依"**未找到 ≠ 不存在**"：本档**未穷尽**"非幂／非平移的非线性算术流"，
　亦未排除"由**多个**流耦合产生的自洽方程"（如筛法流 ＋ 卷积流的耦合）⟹ 保持 **GAP** ✓✓
【⛔ 不扩散】 暂不开新对象（B1 亦缓）✓；本节**不做长证明** ✓
【对 B1 的结论性提示（依令 ① 的逻辑 ✓）】 因 `1/2` 的结构性缺口在**流／谱层**，
　**B1 即使构造出漂亮的 `\mathcal E(p,q)`，其 `P5` 仍极可能撞同一墙** ⟹ **B1 优先级下调** ✓✓
```

## §6 边界与回查（✗✓）

```
✗ 零计算（解析）／零数值／未写程序／未做数值搜索／**未开案** ✓
✗ ⛔ **不把"指数来源全堵"当新结论**（**引 `P-3`** ✓）；⛔ 不宣布"不存在此类算术流"✓；⛔ 守 `V290` 禁语 ✓
⚠️ 卷积流"谱 ＝ Dirichlet 级数像之闭包"与 Wiener 型零点自由半平面为**标准事实**（**凭记忆** ⚠️，未逐字核档）；
　"`\|2x\|_2 = (1/2)\|x\|_2`"为**定义级**事实 ✓（但其与 β 的**无机制**是本档判定 ✓）
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 强制不动点 命中文件数=0 ⟹ **本档新增** ✓
　技术词 P2可行性   命中文件数=0 ⟹ **本档新增** ✓
　技术词 算术流     命中文件数=14 ⟹ **档案已有 ⟹ 引用**（不作本档提出）✓
　技术词 线性化谱   命中文件数=1 ⟹ **档案已有（异义）⟹ 不主张** ✓
　技术词 临界指数来源 命中文件数=3 ⟹ **档案已有（含 `P3-B2`）⟹ 引用** ✓
【地图回查】 命中 1 ＝ `ASSETS-REGISTRY` **`P-3`** ⟹ **实质性命中 ⟹ 引用** ✓（本档仅补"流级"枚举 ✓）
【档级引用】 `ASSETS-REGISTRY` **`P-3`**（③-B 结构性二分＋临界指数来源全堵～新规格指向 P2）✓｜`P3-B`／`P3-B2` ✓｜`V262`（紧致 ⟹ 无障碍）✓
