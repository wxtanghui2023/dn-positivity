已查地图：命中 24 处 —— 先逐条判 已DEAD/已封/已登记；命中即引既有条目，不得开新案
　判定（逐条分类）：命中项**全部**为**既有杀点**，分别在 §2 逐候选引用 ✓✓
　　　　　　　　　　（`序数测度 μ_γ 的 Hankel 矩 = β-盲`／`R2：Hankel-rank 做不到`／
　　　　　　　　　　 `第一断裂：有限处 α_p ≡ 1`／`T7：正性依赖显式公式或 Weil 型 ⟹ 循环` ✓）
　⟹ 本档为**四元组候选的筛查登记**（**非新案** ✓）

# RH-MODEL-FIRST · **候选四元组筛查**（`(𝒳, ℒ, P, D)`；本档**零计算**）

**唐先生令（2026-09-22 20:39）**：① **RH-MODEL-FIRST = NOT A NEW CASE** ✓；
② 纪律升级（照录）：「**不能再以"换载体／换失败对象／换模型名称"制造新案；必须同时改变生成器**」✓✓；
③ TLDC → **External Reference Model**（**不是** RH 候选机制）✓；
④ 唯一保留入口 = **等待具体四元组** `(𝒳, ℒ, P, D)`：`𝒳` 具体 RH 载体｜`ℒ` 具体可验证局部律｜
　 `P` **不是"β>1/2"的简单改名**而是具体 failure predicate｜`D` **尚未被现有 D3/D4 墙吸收**的下降/再入变换（`P(x) ⟹ P(y)`，`μ(y)<μ(x)`）✓✓；
⑤ 流程：**等待四元组 → 先地图核验 → 13 门 → L1/L2/L3 → 再决定是否开案** ✓

D0: 本档对象 = **RH 侧四元组候选的筛查**（档案已有对象 `L1/L2/L3 ＋ 十三条门槛 ＋ 既有杀点` 的**归属关系**；非重命名、非新对象）
D1: 0
FREEZE-ACK: 零计算／零 Lean ✓（仅筛查＋引既有条目 ✓）

---

## §1 回答「可能的四元组是啥？」——**五个自然候选**（各死在不同关口 ✓✓）

### C1 多重度载体（γ-多重集）
```
𝒳 := 纵坐标多重集（**含重数**；`V192` 载体：离轴对 = **二重（退化）点**，在线零点 = 单点 ✓）
ℒ := 该多重集的**局部规格**（例：局部单重性 / 局部密度的形状）
P := 「存在重数 ≥ 2 的点」——⚠️ 与"离轴对"**双射** ⟹ **＝ β≠1/2 的重编码** ✗✗
D := ？（无自然下降）
【杀点】 ① `V192`：**纵坐标退化封印（谱实现族一次性封）** ⟹ 载体已封 ✓
　　　　 ② **P 是目标的重编码** ⟹ 违反唐先生硬条件（不得"β>1/2"简单改名）✗✓
【判定】 **DEAD**
```

### C2 自守／局部-整体载体
```
𝒳 := adelic 自守数据（局部因子族 ＋ Hecke 本征值）
ℒ := 局部 Hecke 关系 / 局部函数方程 / 局部 Langlands（**成立且无条件** ✓——但⚠️**是定义式/结构性**的，非"非平凡可证新律"）
P := 「局部数据无法装配成所需全局对象」（局部-整体障碍）
D := 导子下降 / 表示下降（导子是自然度量 ✓）
【杀点】 ① `V196`–`V198`：**Brauer／K₂／H¹ 型局部-整体 档案已闭**（canonical ⟹ `[T]=0` 或 `Br[N]`）✓
　　　　 ② **P 是全局解析属性**（L 函数零点），**不是 ℒ 的缺陷** ⟹ 不合四元组形态 ✗✓
【判定】 **DEAD**
```

### C3 矩序／Hankel 载体
```
𝒳 := 矩序 `(M_n)`（＝序数测度 μ_γ 的矩）✓
ℒ := 生成函数**逐项**关系（`GORZ`: `(−1+4z²)（**按档案逐字**：原文即 `(-1+4z^2)` ✓）Λ(1/2+z) = Σ γ(n) z^{2n}/n!` ✓）
P := 「Hankel 行列式非正 / 矩问题不定」
D := Hankel 秩下降（原始度量 = 秩 ✓）
【杀点】 ⭐ **已刊且致命**：`序数测度 μ_γ 的 Hankel 矩 = β-盲（门①②）` ✓✓
　⟹ **该载体看不见 β** ⟹ `P` **无法**检测目标 ⟹ 失效谓词恒空 ✗✓
　＋ `R2：Hankel／递推表示给 R 上界` **已判"做不到"** ✓
【判定】 **DEAD**
```

### C4 函数域／Frobenius 载体
```
𝒳 := 函数域曲线 ζ（Frobenius 载体）✓
ℒ := 局部 RH（Weil，**函数域已证** ✓✓——最强的一个 ℒ）
P := 「char-p 类比失败」（⚠️**空**：函数域已证 ⟹ 无失败对象）
D := Frobenius 下降（真实存在 ✓）
【杀点】 ⭐ 已刊 **第一断裂**：`ζ 局部因子 (1−p^{−s})^{−1}，有限素数处 α_p ≡ 1
　　⟹ 无函数域型 α_p = √p·e^{iθ_p} 内部谱` ✓✓
　＋ **L2 否决判据**：ζ **只能经显式公式进入** ⟹ 关闭 ✓
【判定】 **DEAD**（D 真实存在，但**无 char-0 实现** ✗）
```

### C5 显式公式配对载体（**最"具体"的一个** ✓）
```
𝒳 := (检验函数空间, 算术核) 的配对（Weil 显式公式）
ℒ := 显式公式的**逐位分解**（每个 place 一个局部项 ✓——真局部、可写 ✓）
P := 「∃ f：W(f) < 0」——⭐ **具体见证函数**（**不是**"某个零点"，故非简单改名 ✓✓）
D := ？（需在函数空间内把负方向**压小并再入**同一类）
【杀点】 ⭐ 已刊 **T7**：「正性**是否依赖零点位置／显式公式／Weil 型？若是 ⟹ 循环**」✓✓
　＋ 门槛 **P-SqrtPos**：`正性须独立于零点位置` ✓
　＋ 且 **D 的存在性强度 = RH**（若有 D ⟹ 无负方向 ⟹ RH）⟹ **F1b 失败** ⟹ 按 `V194` F1 2×2 判 = **REPACKAGED** ✗✓
【判定】 **DEAD**（但为**最有教益**者：它把缺口精确定位到 `D` 上 ✓）
```

## §2 ⭐ **共同结构原因**（对既有墙的**统一读法**；⚠️**非定理，标为读法** ✗✓）

```
【观察（读法）】 RH 侧「**局部律**」**天然是定义式／无条件的**：
　· 有限处：`α_p ≡ 1`（**已刊第一断裂**）＋ 局部因子／Hecke 关系**按定义**成立 ✓
　· 阿基米德处：Γ 因子与局部因子**同为定义式** ✓
　⟹ **不存在"非平凡且独立可证的局部律"** 可作燃料 ✗
　⟹ 任何非平凡失败**只能**落在**全局零点装配**处 ⟹ **P 被迫是 RH 强度** ⟹
　　 `F1b`（能否以**严格弱于 RH** 的输入无条件证明？）**失败** ⟹ 按 `V194` F1 判 = **REPACKAGED** ✗✓
【与 TLDC 的关键反差（解释"为何 TLDC 不能迁移"）】
　TLDC 引擎**恰恰**需要**非平凡且独立可证**的局部律作燃料
　（Liouville：`|ab| < T ⟹ F(ab)=F(a)F(b)` **短程乘法** ✓；A2：鸽笼定量界 ✓）
　⟹ **该燃料在 RH 侧天然缺席** ✓✓
【一致性】 本读法与既有 `T1 局部⇏全局` 墙、`L3`「全局类比**全部循环**」、**第一断裂**、`C0 紧性` 完全一致 ✓✓
```

## §3 若要有**活**四元组：必要条件（**待等** ✓）

```
【必要条件（由 §2 推出）】
　(i) `𝒳` 必须使 `ℒ` **非定义式**：可**独立证明**且**非平凡**（不是"按定义成立"的局部因子）✗✓
　(ii) `P` 必须是 `ℒ` 的**真实缺陷**（局部律在该对象上失效），而非全局解析属性 ✓
　(iii) `D` 必须**未被 `D3/D4` 墙吸收**：不是"坏对象必有更小坏对象"的改名 ✓
　(iv) 且 `P` **严格弱于 RH**（否则 `F1b` 失败 ⟹ REPACKAGED ✗）
【现状】 **L1／L2／L3 三者皆无此类 `(𝒳, ℒ)`** ⟹ **无活四元组** ✓
【正确状态（依令）】 **等待四元组出现** ⟹ 先地图核验 ⟹ 13 门 ⟹ L1/L2/L3 ⟹ 再决定是否开案 ✓
```

## §4 边界与回查（✗✓）

```
✗ 零计算／零 Lean ✓；**未新开案** ✓；未动 C-380（HOLD）／旧桥（CLOSED）／RH interface（NOT STARTED）✓
✗ §2 为**读法**而非定理（不写"RH 侧不可能有活四元组" ✗，只写"目前 L1/L2/L3 皆无" ✓）
✗ 不把"五个候选皆 DEAD"记为战绩（依既有纪律：不以"又关掉一条路"当成果 ✓）
【回查（**先跑后写** ✓）】
　prework_map_check：命中 **24**（四元组|局部律缺陷|Hankel|显式公式配对|函数域类比|多重度载体）
　　⟹ 命中项**全部**为既有杀点（§2/§3 逐条引用）⟹ 非新案 ✓
　tech_word_check：局部律缺陷／定义式局部律／反演谱几何／多重度载体／配对载体／四元组候选 = **0 ⟹ 本档新增** ✓
【引用的既有条目】 `V192`（谱实现族一次性封）｜`V196`–`V198`（局部-整体 档案已闭）｜
　`序数测度 μ_γ Hankel 矩 = β-盲`（`MASTER-STATUS-AND-CLOSURES` L773）｜`R2`（L958）｜
　**第一断裂**（`CLOSED-ROUTES-MAP` L311）｜`T7`（`MASTER-NOGO-AND-LIVE-PATHS`）｜`P-SqrtPos`｜`V194` F1 2×2 ✓
