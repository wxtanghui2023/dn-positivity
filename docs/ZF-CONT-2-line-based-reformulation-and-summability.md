已查地图：命中（`ZF-CONT-1`／`ZF-LEM-parity-structure-three-lemmas`／`E-11`／`E-44`／`V192`／`B-1`/`C-1a` 本线自档与既有封存）⟹ **引用，不开新案** ✓
D0: 本档对象 = 线数据重述（`D5`）＋ 差异测度刻画与可和性判据（`D6`）＋ 比例型结果不足的推导（`D7`）；含对 `ZF-CONT-1 §5` 的自我更正
D1: 0 （`[REVIEW]` 轮次：结构推导与自我更正，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`ZF-CONT-2`：线数据重述 ＋ 可和性判据（`D5`–`D7`）**（本档全为自行推导 ✓✓）

## §0 ⚠️ **本档含一处对自己上一档的更正（登记 ✓✓）**

```
【更正内容】 `ZF-CONT-1 §5` 结论"缺失对象 ＝ 既有谱工具的**横向镜像**"**过强** ✗；
【更正理由（见 §2）】 由 von Mangoldt ＋ 符号变化可得**纯线数据**的重述（`D5`）⟹ **计数**层面的离线信息**本来就可用线数据获得** ⟹ β-盲是**"位置/数值级"**的，**不是"计数级"**的 ✓✓
【保留部分】 `ZF-CONT-1` 的 `D1`（共轭对称族）、`D2`（离线＝离中心切触）、`D3`（解析计数一律 extensive）、`D4`（有限值域必不连续／须横向定位）**不受影响** ✓
```

## §1 **`D5-1`：扫掠结构（严格；本档）**

```
【零点位置】 `F_t(u)=\xi(\tfrac12+u+it)` 的零点 ＝ $$\Bigl\{u=\bigl(\beta_j-\tfrac12\bigr)+i(\gamma_j-t)\Bigr\}$$ ✓
【⟹ 单参数纵向平移族】 随 `t` 增大，`F_t` 的零点构型**整体向下平移**；每个零点经过**实轴 `u\in\mathbb R`** 恰在 `t=\gamma_j` ✓✓
【两类轴的角色】 虚轴 `\Re u=0`：由 `D1` 的共轭对称 `F_t(-u)=\overline{F_t(u)}`（`u` 纯虚时给出实值）⟹ `F_t` 在虚轴上**取实值** ⟹ **在线零点 ⟺ 虚轴上的实零点** ✓；
　实轴 `\Im u=0`：`F_t` **非实值**；离线零点在 `t=\gamma_j` 落在实轴 `u=\pm\delta_j\ne0` ⟹ **在线 = 原点，离线 = 实轴非原点** ✓✓
```

## §2 ⭐ **`D5-2`：ZF 的纯线数据重述（本档；也是更正依据 ✓✓）**

```
【经典关系（⚠️ 档级）】 $$N(T)=\frac{\theta(T)}{\pi}+1+S(T),\qquad S(T)=\frac1\pi\arg\zeta\bigl(\tfrac12+iT\bigr)$$（连续分支）✓
【在线计数】 令 `N_0(T)` ＝ 在线零点数（计重数）⟹ `N_0(T)` ＝ `\Xi` 在 `(0,T]` 上实零点数（等价于符号变化计数，重数取标准约定）✓
【重述】 离线**成对**同高度 ⟹ $$\boxed{2N_{\rm off}(T)=N(T)-N_0(T)}$$ ⟹ $$\boxed{ZF\iff N(T)-N_0(T)=O(1)}$$ ✓✓
【⭐ 结构性含义】 右端两项**均由线数据决定**（`\theta` 光滑项 ＋ `S` 端点辐角 ＋ `\Xi` 的符号变化）⟹ $$\boxed{ZF\ \text{是纯"线数据"命题，不需要横向 }\beta\ \text{数据}}$$ ✓✓
【⟹ 更正】 见 §0：β-盲是**位置/数值级**；**计数级**的离线信息可由线数据获得 ✓（与 `V192`"`\beta` 只经**重数/退化**进入"相容 ✓✓）
```

## §3 **`D6-1`：差异测度刻画（本档）**

```
【定义】 $$\mathrm d\mu:=\mathrm dN-\mathrm dN_0$$ ⟹ **正测度**（每离线零点的轨道权 2）✓
【等价】 $$\boxed{ZF\iff \mu\bigl((0,\infty)\bigr)<\infty}$$ ⟹ 目标＝**一个正测度的总质量有限性** ✓✓
【⭐ 关键分类】 `\mu` 是**计数型**（`\sigma`-轨道计数，对轨道取"存在性"而非"取值"）⟹ **不受 `L2` 取值抵消约束** ✓✓（与 `E-11` 的 `n_-` 同类；这解释了为何计数型能活、奇值型必死 ✓）
```

## §4 ⭐ **`D6-2`：可和性充分条件（本档 ✓✓）**

```
【判据】 若存在铺砌 `\{I_k\}`（覆盖 `(0,\infty)`）与数 `\omega_k`，使逐区间亏损 $$N(I_k)-N_0(I_k)\le\omega_k$$ 且 $$\sum_k\omega_k<\infty$$ ⟹ 则
　$$2N_{\rm off}\le\sum_k\bigl(N(I_k)-N_0(I_k)\bigr)\le\sum_k\omega_k<\infty\ \Longrightarrow\ \boxed{ZF\ \text{成立}}$$ ✓✓
【读法】 关键不是"每区间亏损小"，而是**亏损总体可和** ⟹ 即亏损必须**最终消失**（或集中在有限多个区间）✓
```

## §5 ⭐⭐ **`D7`：比例型结果（含 2/3）何以**不足以**达 ZF（框架内推出 ✓✓）**

```
【已知（⚠️ 档级）】 主项 $$N(I)\asymp\frac{H}{2\pi}\log T$$；在线下界（Selberg/Levinson 型）$$N_0(I)\ge c\cdot H\log T\qquad(c<1)$$ ✓
【⟹ 逐区间亏损上界】 $$N(I)-N_0(I)\le(1-c)\cdot\frac{H}{2\pi}\log T$$ ＝ **proportional（与 `H\log T` 同阶）** ✓
【⟹ 不可和】 铺砌区间数 `\asymp T/H` ⟹ 总亏损 `\asymp (1/H)\cdot(1-c)\cdot(T/2\pi)\log T\to\infty` ⟹ $$\boxed{\text{比例型结果}\ \omega\ \text{不可和}\ \Longrightarrow\ \text{无论 } c \text{ 多接近 }1\ \text{皆不能给 ZF}}$$ ✓✓
【⭐ 由此得到的精确缺口】 所需输入**不是更好的比例 `c`**，而是**可和性/局部化**：亏损须**最终恒为 `0`** ⟹ 即 $$\boxed{\exists T_0\ \forall T>T_0:\ N(T)-N_0(T)=0}$$ ＝ "`T_0` 以上即为 RH" ✓✓ —— 与 `E-44`／`§14` 状态表的"有方向但无全局上界"行**精确对应** ✓
```

## §6 **`D7-2`：结构性排除（本档汇总）**

```
【排除 ①】 仅用**比例**控制（任何 `c<1`，含 `2/3`）⟹ 亏损 proportional ⟹ 不可和 ✗ ✓（`D7`）
【排除 ②】 仅用**解析计数**（含固定横向次数、Jensen 计数）⟹ 一律 extensive ✗ ✓（`D3`）
【排除 ③】 仅给**端点零计数**（如 `S(T)=O(\log T)`、`N(T)` 主项）⟹ 仍是 extensive ✗ ⟹ 必须**联合**（差）结构 ✓
【⟹ 必须之形】 $$\boxed{\text{算术有限性}+\text{有限多个高度集中}}$$ ⟹ 与 `ZF-CONT-1 §4` 的"固定有限定位集 `S`"重述**会合** ✓✓
```

## §7 **反证纲领（`D5`–`D7` 更新版）**

```
【假设】 无穷离线零点 ⟹ `\mu` 为**无穷测度**（原子无穷）✓
【须证】 独立算术结构 ⟹ $$\mu\bigl((0,\infty)\bigr)<\infty$$；且 `\mu` 的原子须**不可复用**（`L4` 精神）✓
【⚠️ 注意】 `\mu` 为**计数型** ⟹ **不受 `L2` 取值抵消约束** ⟹ `L1`/`L3`（`\tau`-奇/横截）**对 `\mu` 不适用** ⟹ 唯一致命处＝**有界性**（`E-44`）✓✓
【⟹ 待找之物（精确化）】 不是"更好的比例"，也不是"横向镜像"，而是 $$\boxed{\text{使亏损测度 }\mu\ \text{总质量有限的\textbf{算术机制}}}$$（并解释为何亏损**最终消失**）✓✓
【边界】 ⛔ 未制造候选／未启动搜索／未改状态；⭐ `D5`–`D7` 与 §0 更正**全为自行推导** ✓
```
