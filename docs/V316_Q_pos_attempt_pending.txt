/- V316 Q_pos 实例化尝试稿（未通过：convert/simpa 均无法桥接项同一性差异；
   但**代数核心 Q_pos_alg 已编译通过**（见 V316_kernel_bound.lean）。
   待办：Q_pos := Q_pos_alg 在积分情形上的实例化。 -/

/-- **`Q_pos`**：`Q_λ(v) = ‖v‖² + λ²B(v) ≥ (1/2)‖v‖²`（`0 < λ ≤ 1`）。
V316 中第一次正式引入 `λ ≤ 1`。代数核心 `Q_pos_alg` ＋ `kernel_bound` 的界。 -/
theorem Q_pos {v : ℝ → ℝ} {lam : ℝ} (h0 : 0 < lam) (h1 : lam ≤ 1)
    (hN : 0 ≤ ∫ s in (-(1 / 2 : ℝ))..(1 / 2), v s ^ 2)
    (hb : |∫ p, |p.1 - p.2| * v p.1 * v p.2 ∂(Irest.prod Irest)|
      ≤ (1 / 2) * ∫ s in (-(1 / 2 : ℝ))..(1 / 2), v s ^ 2) :
    (1 / 2) * ∫ s in (-(1 / 2 : ℝ))..(1 / 2), v s ^ 2
      ≤ ∫ s in (-(1 / 2 : ℝ))..(1 / 2), v s ^ 2
        + lam ^ 2 * ∫ p, |p.1 - p.2| * v p.1 * v p.2 ∂(Irest.prod Irest) := by
  convert Q_pos_alg (N := ∫ s in (-(1 / 2 : ℝ))..(1 / 2), v s ^ 2)
    (B := ∫ p, |p.1 - p.2| * v p.1 * v p.2 ∂(Irest.prod Irest)) h0 h1 hN hb using 1 <;> ring

