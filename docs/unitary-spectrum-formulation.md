# 幺正谱表述：RH 的 Möbius/单位圆形式（2026-09-04）

## 状态
模类观察（modular-observation-hp-fix.md）的深挖修正——缩放因子问题的化解——RH 的"幺正谱"表述。

## 问题
函数域：F*F = qI（缩放 q——）——Frobenius 的代数缩放——char 0 无 F_q——无代数缩放——e/π 的缩放是分析的（判死——）——缩放因子问题。

## 关键发现：Möbius 映射化解缩放问题
不需要"指数映射"（e^{-ρ}——模 e^{-½}——非单位圆——）——用 Möbius 把临界线送到单位圆：

```
λ(s) = ((s−½)−a)/((s−½)+a)（a 实——）
s = ½+it ⟹ λ = (it−a)/(it+a)——|λ| = 1（单位圆——）
⟹ RH ⟺ 所有零点的 λ(ρ) 在单位圆（模 1——）
λ(ρ) = e^{iθ}——θ(γ) = 2 arctan(γ/a)——零点的相位表示
```

## 三个好处
1. 缩放因子问题消失（单位圆——模 1——不需要 e^{-½}）
2. 物理幺正性直接适用（T*T = I——量子演化/S 矩阵类——）
3. 谱是"相位"（θ(γ) = 2 arctan(γ/a)——单位圆上的点——）

## 靶心更新（修正模类观察——）
**RH ⟺ 存在幺正算子 T——谱 = {λ(ρ)}（Möbius 相位——）——不经 ζ 独立构造——**
- L4 靶心从"缩放幺正（e^{-½}——）"修正为"幺正（单位圆——）"
- 更物理（幺正性——守恒——量子演化——）

## 连接
- θ = 2 arctan(γ/a)——arctan 出现在早期工作（望远镜恒等式——θ = π − 2 arctan(2t)——g_n = cos(nθ)−cos((n+1)θ)——）——零点的相位/三角表示——可能与幺正谱表述相关（待回看——）

## 障碍（诚实——）
- T 的独立构造（谱 = {λ(ρ)}——不经 ζ——）不变（L4——）
- 但靶心更准（幺正——模 1——物理机制成熟——）

## ⚠️ 诚实标注（2026-09-04 深挖后——）
**幺正谱表述 = HP 的 Cayley 变换（数学等价——无新机制）**：
- 任何自伴 H（谱 = {γ}——）可 Cayley 变换成幺正 T（谱 λ(γ) = (γ−a)/(γ+a) 类——单位圆——）
- Möbius 只是"翻译"（零点 ↔ 单位圆——）——不是新机制
- 物理的幺正（守恒——）的生成元（H——）还是要谱 = {γ}——循环
- Möbius 参数 a 无自然选择
- **真正的靶心不变**：L4——独立构造（谱 = 零点——不经 ζ——）
- 保留方向：守恒律作为机制（谱在单位圆的"原因"——Noether 类——）——但 char 0 的守恒未知（L4）

## 文件
本文档——dn-project/docs/unitary-spectrum-formulation.md（2026-09-04 深挖存档——）
关联：docs/modular-observation-hp-fix.md（模类观察——被本文档修正——）
