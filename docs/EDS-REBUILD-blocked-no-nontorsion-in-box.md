已查地图：命中（`EDS-REBUILD-partial-EZ-equals-torsion-on-11a1`／`EDS-INPUT-INVALID-P-is-5-torsion`）⟹ 引用，不开新案
D0: 本档对象 = EDS 重建**第二次尝试**：有界搜索仍只命中挠点 ⟹ 数据层未建；给出**收口方案**
D1: 0 （[REVIEW] 轮次：失败报告与方案，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **EDS 重建：第二次尝试（未成功）＋ 收口方案**

## §1 实算（本机）

```
【搜索盒】 `x=A/B^2`，`B\le12`，`|A|\le400` ⟹ **命中 26 个有理点，全部 `x\in\{5,16\}`** ⟹ **全部为挠点** ⟹ **非挠候选 = 0** ✓
【结论】 `11a1` 的**非挠生成元落在本搜索盒之外**（高度更大）⟹ **仍未取得合法 `P`** ⟹ **`B_n` 数据层未建、四项审计未跑、`CAP-1` 未跑** ✓✓（不含糊）
【已确认的正向结果（保留）】 `E(\mathbb Z)=E(\mathbb Q)_{\rm tors}\cong\mathbb Z/5` ⟹ 对非挠 `P` **`B_n=1` 永不发生** ⟹ `I(E,P)=\varnothing` ⟹ **`B_n=1` 污染源在本曲线彻底排除** ✓✓
```

## §2 收口方案（不再原地扩盒）

```
**【方案 `W`】** `P` 从**可信数据库取值**（LMFDB `11.a1` 的 generator 有理坐标）—— 比我扫盒可靠 ✓
**【方案 `S`】** 若坚持自算：**扩大搜索盒**（`B\le60`、`|x|\le2000` 量级），但**必须换策略**（用 `2Q` 回到整点的两倍点关系做 θ 搜索，而非直接扫 `(A,B)`）✓
**【方案 `T`】** **换曲线**：取一条 `E(\mathbb Z)\ne tors` 的曲线（如 `37a1`）⟹ `I(E,P)` 非空 ⟹ 反而更接近原 `CAP` 命题（能同时测 `I` 与容量两层）✓✓
【我的建议】 **优先 `W`（快且可信）**；若 `W` 不可用则 `T`；`S` 最后 ✓
【⛔ 纪律（照录）】 不追 abc／不追 `N_0`；首轮无 exceptional surplus ⟹ **降级 EDS 线**；**不为保住 CAP 强行换约定** ✓
【边界】 §1 为本机实算（`out/eds_layer_11a1.txt`）；§2 方案为**本档自行推导**；未制造候选／未启动搜索／未碰 RH。
