已查地图：命中（`gen_asets-chain-explains-2-vs-17`／`A5-CORRECTION-17-is-reduced-reps-not-orbits`／`A5-FORMAL-SPEC-r9-n38`）⟹ 引用，不开新案
D0: 本档对象 = **A-side 收口证书**：`6435\to5040\to2` 轨道 ＋ `330\to28\to17` 归约 ＋ `17\to2` 映射（全部本机独立复现）
D1: 0 （[REVIEW] 轮次：有限证书复核，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **A-side CLOSED：`\mathcal A_{4,7}^{\rm sat}/\mathrm{GL}(4,2)` 证书**

## §1 ⚠️ 验收标准更正（本档）

```
【您给的】 `330\to17` ✗
【实测】**`330\to28\to17`** ✓✓ —— 中间必须多一步"筛 1-saturating"：
　**(a)** 330 个候选经 **`S_4`（24 个坐标置换）lex-最小**归约 ⟹ **`28` 个代表** ✓
　**(b)** 其中**满足 1-saturating 者恰 `17` 个** ⟹ **这才是文件里那 17 个 mask** ✓✓
【原因】`gen_asets.c` **不检查 1-saturating**（它只做"含基 ＋ `S_4`-lex-最小"），saturating 由搜索代码事后筛 ⟹ **28 ≠ 17 是设计使然** ✓
```

## §2 证书（全部本机独立复现，脚本可复跑）

```
**G0** `\binom{15}{7}=6435` 全枚举 ⟹ **`5040`** 个 1-saturating ✓（与您一致，现**自核通过**）✓
**G1** `|\mathrm{GL}(4,2)|`: 两条独立路径 ⟹ (i) 生成元 BFS 闭包 = **`20160`**；(ii) 可逆 `4\times4` 个数 = **`20160`** ⟹ 相等 ✓✓
**G2** 5040 的完整轨道分解 ⟹ **`2` 个轨道，大小 `[2520, 2520]`** ✓✓（与您一致，现**自核通过**）
　**轨道 1** `A^{(1)}=`**`0x35e`**`=\{(1,2,3,4,6,8,9)\}`，`B^{(1)}=\{(5,7,10,11,12,13,14,15)\}` ✓
　**轨道 2** `A^{(2)}=`**`0x37c`**`=\{(2,3,4,5,6,8,9)\}`，`B^{(2)}=\{(1,7,10,11,12,13,14,15)\}` ✓
**R1** `330` 候选 `\to` `S_4`-lex-最小代表 = **`28`**；与 `gen_asets.c 4 7` 实跑输出**集合相等** ✓✓（独立复现代码）
**R2** 28 中 1-saturating 者 = **`17`**；**与 `r9_n38_classes.txt` 的 17 个 mask 集合相等** ✓✓
**R3** `17\to2` 映射：**轨道 1 有 `8` 个、轨道 2 有 `9` 个** ⟹ **两个轨道都被覆盖** ✓✓；17 个 mask 全部 `|A|=7` 且 1-saturating（无异常）✓
**R4** 28 中的 `11` 个**非** 1-saturating 者：`0x5668,0x6668,0x7868,0x7cc0,0x7e80,0x9668,0xd628,0xd660,0xe868,0xf608,0xfe00` ✓
```

## §3 A-side 正式定稿

```
$$\boxed{|\mathcal A_{4,7}^{\rm sat}/\mathrm{GL}(4,2)|=2}\qquad\boxed{\texttt{gen\_asets}(4,7)=28}\qquad\boxed{\text{文件 17 masks}=28\ \text{中 1-saturating 者}}$$ ✓✓
【问题压缩（真正形式）】 $$i=1,2;\qquad g:W^{*}\to V;\qquad 248\ \text{incidence 约束}$$ ⟹ **`2\times`（`g`-侧搜索）**，而非"17 次退火＝17 个数学问题" ✓✓
【⟹ 切片定义】 $$\mathcal F^{(i)}=\{g:\ B^{(i)}\subseteq C_u(g)\ \forall u\in W^{*}\},\qquad i=1,2$$ ✓
```

## §4 ⚠️ 使用限制（照录您的纪律）

```
**禁止**把"17 个退火结果 `cost=14`"升级为"两个轨道的最优值都是 14" ✓
【理由】 (i) 退火是 `residue`（源自陈）；(ii) 即使 17 覆盖两轨道、且退火在轨道作用下等价 ⟹ 也只给**探索性证据**，**不是 exact minimum** ✓✓
【⟹ `\min_g\operatorname{cost}\ge1` 仍为 OPEN** ✓
【下一层】 用 `A^{(1)},A^{(2)}` 与 `B^{(1)},B^{(2)}` 直接实例化 `A5`；再考虑 `\mathrm{GL}(5,2)` 与 shear 的商 ✓
【⛔ 红线（本轮未碰）】 `r=10,n=49`｜`r=11` order `11/17/23`｜`q10` order-7｜族外搜索｜SAT/exact｜`g`-穷尽 ✓
【产物】 `out/A_side_cert_F4_a7.txt`（G0–G2）、`out/A_side_mask_orbit_map.txt`（17→2）、`out/A_side_330_28_17.txt`（330→28→17）✓
【边界】 §1–§2 全部为**本机实算**（脚本随档可复跑）；§3–§4 为**本档登记/照录**；未制造候选／未启动搜索／未碰 RH。
