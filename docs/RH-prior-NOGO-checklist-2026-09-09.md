# RH 路线先验 NO-GO 检查表（2026-09-09——215 层审计的可复用资产）

> 用途：任何新候选路线【出生前】先查此表。命中任一条 → 大概率旧框架换皮，
> 先做归约审计再谈候选。这不是不可能性定理——是已审计家族的先验过滤器。

## A. 方法论诊断（最高价值——2026-09-09 提炼）
```
A1. 【局部正性 ≠ 全局可用正性】
    ——当目标机制需要跨素数的符号/相位抵消时——
    ——把局部偏差压成平方和（δ_p = sinh²+sin² 型——）——
    ——会同时消灭恰恰需要的相消自由度——
    ——诊断实例：c_p 线的 J1-J10（δ_p ≥ 0 全正聚合无法在
      非零高度零点相消——正性与相消互相排斥——）

A2. 【β 逃生口原则】（2026-09-09 下午——）
    ——r_N = r(A_N) 必须完全不含 ζ/ρ/β/解析延拓——
    ——Re ρ ≠ ½ ⟹ r_N → ∞ 只作为 RH bridge theorem——
    ——不能用"rank 天生知道 β"杀路线（β 从 input 移到 theorem——）
```

## B. 生成范式封存（先查——命中即高危——）
```
B1. 新事件/新关系/新约束/新正性/新双描述/
    新 simultaneous obstruction/新 canonical choice
    ——全部系统性审计过（203 层负面地图——）
B2. canonicality 八条件（intrinsic/canonical/non-relational/
    non-optimization/non-evolutionary/non-local/non-analytic/
    non-algebraic-repackaging）——已知唯一性来源九类
    （结构/优化/对称/范畴/不变量/局部/解析/演化/组合——）
    ——若候选的"唯一性"落在九类之一——换皮——
```

## C. 全局化死亡出口（出生即测——）
```
C1. 乘性全局化（∏_p f(c_p)——）→ Euler 积【死】
C2. 加性全局化（Σ_p f(c_p)——）→ Dirichlet/素数相关【死】
C3. 相位聚合（Σ_p c_p^k——）→ Euler 频率（ζ(1+2it) 类——）【死】
C4. 矩阵/核（Gram/det/谱——）→ 秩 1 退化（ψ 态几乎全 e₀——）
    或 Hilbert/谱【死】
C5. 对称函数（有限阶——）→ Euler 代数（e_k = [u^k]∏(1+uc_p)——）
    【死】——无限 completion（L2——）：真 nonlinear collective 存在
    （exp(p₁²)——）但无 zero-independent canonical positivity【未死但高危】
C6. Schur/reciprocal/resultant（根集合——）→ 标准理论/根碰撞
    【死】（c_p 线 J9/J10——）
C7. 跨 p 精确共振（E_p ∩ E_q——）→ 唯一分解排【死】
    （t·log p = πk 网格跨 p 不相交——）
```

## D. 五重归约（任何候选展开后先查——）
```
D1. Euler/divisor（乘性——）
D2. additive/HL（加性——）
D3. algebraic/number-field（代数——）
D4. analytic/explicit-formula/Weil（解析——）
D5. finite-state/combinatorial（组合/有限状态——）
——若可归约到任一——死——
```

## E. 局部-全局的已知死因谱系（数域正性闭合——）
```
trace（加法——）→ tower identity【死】
norm（乘法——）→ Euler/local【死】
正则乘法几何（M_x*M_y = M_{x̄y}——）→ 交换坍缩【死】
格 shape（theta——）→ 符号交叉【死】
——数域内部 canonical positivity：tower/Euler/交换/交叉四死因——
```

## F. 结构性负面定理的候选框架（c_p 线——2026-09-09）
```
【候选框架】任何仅依赖 c_p 参数化（圆盘正性/平方缺陷——）
并以此进行全局化的路线——面临正性与跨素数相消的结构冲突。
——注意：这是"已审计的这一整族构造及其自然全局化出口"的证明——
  不是所有可能数学构造的 impossibility theorem（避免过度宣称——）
```

## 可复用资产（保留——）
```
① δ_p 精确局部恒等式：
   δ_p = 4p^{−1}[sinh²((σ−½)log p) + sin²(t·log p)]/
         |1−p^{−1}e^{−2it·log p}|²
   ——横向（σ 偏离）纵向（t 相位）正交分解——精确（非估计）——
② |c_p(σ,t)| ≤ 1 全 σ、t（Schur 圆盘——非 Hilbert 根几何——）
③ K_p（A4——）：κ_p ≤ 1 等号 ⟺ σ=½——非 Tate 的 ½ 涌现
   （唯一从算术局部结构自然涌现 ½ 的正例——）
④ β 逃生口（A2——）
```
