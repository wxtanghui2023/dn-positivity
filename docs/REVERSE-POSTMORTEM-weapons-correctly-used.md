已查地图：命中（`CORRECTION-cases-show-move-not-compute`／`ASTRA-TYPE-OPEN-PROBLEM-TABLE`／`V186`／`META-OBSTRUCTION`）⟹ **引用，不开新案** ✓
D0: 本档对象 = 五案例**反向复盘表**（原始问题→中间对象→被控量→关键不等式/构造→计算角色→Lean 核心）＋ 横向模式 ＋ `Rule T`
D1: 0 （`[REVIEW]` 轮次：复盘与提取，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **反向复盘：武器是如何被正确使用的**

## §0 ⚠️ **来源纪律**

```
【可核实】 2/3 在线比例（arXiv:2608.13637，且本线 `V186` 已逐条审计）；Liouville–Goldbach 对数平均（Tao 2015／Tao–Teräväinen 2018）✓
【⛔ 未核实】**NS／Lorenz 多项式／素数间隙 186** —— **我无可靠来源，故留空、不编造**（待您提供论文或授权检索）✓
```

## §1 **表（六列）**

| 案例 | 原始问题 | 中间对象 | **被控制的量** | 关键不等式/构造 | 计算角色 | Lean 核心 | 我们已有对应武器？ |
|:--|:--|:--|:--|:--|:--|:--|:--|
| **2/3 在线比例** | 临界线上单纯零点**比例下界** | **测试族（窗 `\psi` ＋ Gabor 压缩）＋ 有限维矩阵 `P,Q`** | **`\operatorname{rank}`／惯性 `n_-` ＋ `HS` 范数（＝素数侧二阶矩）** | **`rank–trace` 不等式「惯性替代正性」＋ 优化测试族** | **有限维优化**（测试族参数），**非巨量枚举** | `rank–trace` ＋ Sylvester 惯性（**我们已实现过**） | ✅ 有（`V186` 已审） |
| **Liouville–Goldbach（对数平均）** | 相关和 `o(x)` | **乘性函数 ＋ 短区间平均** | **熵／互信息** | **熵递减论证 ＋ 短区间乘性抵消** | 小 | 信息论不等式＋短区间引理 | ✅ 概念层有（未形式化） |
| NS ／ Lorenz ／ 186 | ⚠️ 待核 | ⚠️ | ⚠️ | ⚠️ | ⚠️ | ⚠️ | ⚠️ |

## §2 ⭐⭐ **横向模式（本档提取，仅基于可核实两行＋本线经验）**

```
**`P1`** 中间对象**都是"有限维／有限状态且可优化"的**（测试族；短区间尺度）✓
**`P2`** 武器**从不单独使用**：**一条不等式**（`rank–trace`／熵递减）**＋ 一个可优化族** ✓
**`P3`** **压缩方向一致**：$$\text{巨大对象}\ \to\ \text{低维参数}\ \to\ \text{有限维极值/不等式}$$ ✓
**`P4`** 计算角色＝**有限维优化或校验**，**不是巨量枚举** ✓ ✓（＝您"不需要大算力"的判断，在可核实两行上成立）
```

## §3 ⭐⭐⭐ **`Rule T`：Translation-before-launch（本档正式化）**

```
【规则】 不先发射武器；**先选载体**。形式化：给定武器 `W` 与目标 `T`，先找载体 `C` 使 $$\boxed{W\ \text{在}\ C\ \text{上\textbf{尖锐}（成为等式/极值可达）}}$$ 再问 **`C` 能否由 `T` 产生** ✓
【本次具体翻译提案（待检验，非候选）】 把 **positivity 的载体**从"函数值／核／Gram 矩阵"换成 **"有限个离轴零点诱导的离散对象"**：取有限离轴零点集 `S`（`|S|=n` 小），构造**互作用矩阵 `M_S`**（entries＝可计算的算术配对量），问：是否存在**由 `S` 产生、且 positivity 在其上可判**的载体？判据三条：**(a)** `n` 小 ⟹ 低维 ✓；**(b)** `M_S` entries 可计算 ✓；**(c)** positivity 在 `M_S` 上有**非平凡结论**（非常数）✓
【第一步（可当天做）】 写出 `M_S` 的一个**具体候选定义**，并在**已知有离轴零点的模型（如 `DH`）**上检查其行为（是否存在、是否可算、是否有符号结构）✓
【⛔ 纪律】 不先问"我们有什么"；不谈 RH 结论；不把提案当候选 ✓
【边界】 ⚠️ 三案例留空（未核实）；`P1`–`P4` 与 `Rule T` 为**本档自行推导**；`M_S` 为**待检验翻译提案** ✓；⛔ 未制造候选／未启动搜索／未改状态／未碰 RH 结论 ✓
```
