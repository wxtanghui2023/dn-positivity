已查地图：命中（`C380-MASTER-LEDGER-FREEZE`／`C3890` 等既有条目 ⟹ **引用，不开新案** ✓）
　档级引用：`C3890`（`T1a-β` 探针；`§6①` 约束张量；`§6②` `M0` 收尾）✓｜`C380-MASTER-LEDGER-FREEZE-2026-09-22.md`（活项表）✓｜`C380-T1a-beta-PRE-VS-POST.md` ✓

# **`C-380/T1a-β` 收口（`CLOSED AS STRUCTURAL ROUTE`）＋ 活项清单审计**

**唐先生令（2026-09-23 09:27）**：
① 选 **(a)**，但**不是重审数学结论**，而是**先裁定定义层歧义**，再做一次**极窄的术语一致性回查** ✓
② ⭐ **裁定（照录）**：$$\boxed{\text{Low-degree annihilator} := \{p\ne0:\ \deg p\le2,\ p(x)=0\ \forall x\in\operatorname{supp}\}}$$ ⟹ **`低次` ＝ 次数 `\le2`** ✓✓
　理由三条：**（1）** 原探针已写"零点 `\supseteq` 支撑、且**零点数 `\le2`**"✓；**（2）** `P0` 核心对象为 `E_2:\mathcal P_2\to\mathbb R^{\operatorname{supp}}`、`\dim\mathcal P_2=3`，
　改读法属"**改变 Spec 后再审**"✗；**（3）** `T_{13},\dots,T_{36}` 次数 `\ge12`，"span 内较低次" **不**等价于"存在次数 `\le2` 的 annihilator" ⟹ **两命题必须严格分开** ✓✓
　⟹ **今后统一写「次数 `\le2` 消失元」，不再使用"低次"裸词** ✓
③ **`P0` 结论不需重审**（闭环干净）：`\operatorname{rank}E_2=\min(3,|\operatorname{supp}|) \Longrightarrow \ker E_2\ne0 \iff |\operatorname{supp}|\le2` ⟹
　$$\boxed{\text{次数}\le2\ \text{消失元}\ \iff\ |\operatorname{supp}|\le2}$$ ⟹ 目标即后者 ⟹ 用前者证后者是**同义循环** ✓✓；`C3890` 的 `p_2` **只是短证书**，不改变逻辑地位 ✓
④ **收口判词（照录）**：$$\boxed{\texttt{C-380/T1a-}\beta = \text{CLOSED AS STRUCTURAL ROUTE}}$$（**比"β-DEAD"更准确**，因 `E-13` 仍保留其独立数学问题面 ✓）
⑤ **下一主线顺序（照录）**：$$\boxed{\text{T1a-}\beta\ \text{收口} \to \text{M0 收尾} \to \text{审计 }C\text{-}380\ \text{其它明确 OPEN 项} \to \text{再决定 T2/T3}}$$（**T2/T3 暂不启动**）✓

D0: 本档对象 = **`T1a-β` 收口 ＋ Spec 锁定 ＋ 活项清单审计**（引既有台账/`C3890`；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算／零数值／未写程序**／未重新找根／未算零点／未接 ζ／未找 `1/2`／未证 RH ✓

---

## §1 **术语一致性回查**（极窄 ✓）

```
【回查命令】 `grep -rn "低次" docs/*.md`（全档）✓
【结果】
　· **本项目语境（探针链）**：`C3890`（原探针）｜`C380-MASTER-LEDGER-FREEZE-2026-09-22.md`｜`C380-T1a-local-audit.md`｜`C380-T1a-beta-PRE-VS-POST.md` ⟹ **四处** ✓
　· **无关语境（异义）**：`C232-active-set-structure-verdict-generic-no-arithmetic.md` 中的"次数 `\le10`"（半径代数性）⟹ **非本议题，不归并** ✓
【规范化动作】 ① **本档为"次数 `\le2` 消失元"的正式 Spec 出处** ✓；② 台账与 `C3890` 追加**日期注记**指向本档 ✓；
　③ ⛔ **不静默改写**他档正文（保留 provenance）✓
【锁定的正式定义】 $$\boxed{\text{次数}\le2\ \text{消失元} := \{p\ne0:\ \deg p\le2,\ p|_{\operatorname{supp}}=0\}}$$ ✓✓
```

## §2 **收口判词**（八项，照录 ✓✓）

```
· **`P0`：FAIL** ✓（两读法皆然；同义循环）｜· **前置结构推导：未发现** ✓
· **事后证书：保留** ✓（`p_2(y)=y^2+\tfrac14y-\tfrac{13}{32}` 两行短证书 ✓）
· **A（RH interface）：空／至多间接** ✓｜· **B（提高 `2/3`）：无** ✓｜· **C（有限离轴零点）：无** ✓｜· **D（其它素数问题）：弱** ✓｜· **E（其它猜想）：保留** ✓
· ⭐ **重开条件：出现独立的 rank-collapse／recurrence／annihilator 机制** ✓✓
【状态】 `CLOSED AS STRUCTURAL ROUTE`（**非 DEAD**）✓
```

## §3 **`C-380` 活项清单审计**（依令 ⑤：按「独立机制 → quantity 是否改变 → 五出口 → 值得投入」排序 ✓✓）

| 项 | 状态 | 独立机制？ | quantity 改变？ | 五出口 | 值不值得再投入 |
|:--|:--|:--|:--|:--|:--|
| **`M0` 收尾**（奇偶族 T-system **逐字判别** ＋ **精确定理号定位**；`C3890` `§6②`） | 未做 | ⛔ **否**（文献/定理定位型） | ⛔ **否** | A 空／B–D 无／E 弱 | ⭐ **值得**（**卫生项**：`C-380` 收口必需，**成本低**）✓ |
| **`S1` 本身**（奇侧定量非退化；**需证书，非纯纸面**；台账 OPEN 项 3） | **OPEN** | ⚠️ **待评估** | ⚠️ **可能**（奇频分离量） | A 空／**E 待评** | ⭐⭐ **最实质的 OPEN 项**（唯一可能带新 quantity 者）✓ |
| **`k=4`**（台账记 OPEN，余量 +0.108） | ⚠️ **已被后续证书取代** | — | — | — | ⛔ **不投入**（`k=4` 已由 `T-a` 证书证不可行、`k_min=5` ✓）✓ |
| **`C-3900`／K4 大型消元** | **HOLD** | — | — | — | ⛔ **不投入**（其目标已由 K4 证书达成 ✓）✓ |
| **`T1b`／`T1c`** | **CLOSED** | ⛔ 否 | ⛔ 否 | — | ⛔ 不投入（重开判据未满足）✓ |
| **`T2`／`T3`**（旧四节点模型系） | **ARCHIVED** | — | — | — | ⛔ **不启动**（依令 ⑤ ✓） |
| **`T1a-β`** | **`CLOSED AS STRUCTURAL ROUTE`**（本档） | ⛔ 否 | ⛔ 否（同义循环） | **E 保留** | ⛔ 不投入（重开条件见 §2）✓ |

## §4 判定与下一刀

```
【本档结论】 `T1a-β` 收口 ✓；Spec 锁定 ✓；术语回查完成 ✓；活项清单按四问排序完成 ✓
【⭐ 下一刀（依令 ⑤ 顺序，最优先且成本最低）】 $$\boxed{M0\ \text{收尾}}$$ ——
　**奇偶族 T-system 的逐字判别 ＋ 精确定理号定位**（`C3890` `§6②` 原话 ✓）⟹ 属**卫生项**：不改 quantity，但为 `C-380` 收口所必需 ✓
【次优先】 `S1` 本身的**独立机制／quantity 评估**（唯一可能带新 quantity 的 OPEN 项 ✓）
【⛔ 不启动】 `T2`／`T3`；⛔ 不投入 `C-3900`／`k=4`／`T1b`／`T1c` ✓
```

## §5 边界与回查（✗✓）

```
✗ 零计算／零数值／未写程序／未重新找根／未证 RH／未接 ζ／未开案 ✓
✗ ⛔ 未静默改写他档正文（仅追加日期注记 ✓）；⛔ 未把"低次"裸词继续用于本议题 ✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 术语一致性 命中文件数=1（**即本档自身**）⟹ **本档新增** ✓
　技术词 次数≤2消失元 命中文件数=1（**即本档自身**）⟹ **本档新增** ✓
　技术词 低次 命中文件数=4（`C3890`／台账／`C380-T1a-local-audit`／`C380-T1a-beta-PRE-VS-POST`）⟹ **档案已有（本议题四处）⟹ 已登记规范化** ✓
　⚠️ 标签 `CLOSED AS STRUCTURAL ROUTE` 含空格 ⟹ 脚本按词拆分（`CLOSED` 386／`AS` 343／`STRUCTURAL` 5／`ROUTE` 184，皆**通用词**）⟹
　　**词级不可判**；**标签级**记为本档首次命名 ✓（依"紧签名"原则，不把通用词当新性证据 ✓✓）
　⚠️ **自纠**：本节首版**预填**了前三项数字（皆写 0），实测与之一致性存疑（本档自身计 1）⟹ 已按实测改正 ✓
【地图回查】 命中既有条目（台账／`C3890`）⟹ **引用、不开新案** ✓
