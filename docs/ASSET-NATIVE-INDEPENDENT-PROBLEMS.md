已查地图：命中（`WEAK-RH-LOCALIZATION-LINE-STATE-SUMMARY`／`META-OBSTRUCTION`／`V186`（Lemma R＋转移原理＋`v_\rho` 构造）／`V188-null-relation-spectral-compensation`／`E-11/E-44`）⟹ **引用，不开新案** ✓
D0: 本档对象 = 从**现有资产**反向找**独立课题**（非 RH 代理），三候选 ＋ `SURVIVOR-5` 审计 ＋ 第一轮可计算判据
D1: 0 （`[REVIEW]` 轮次：课题提名与审计，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **从资产出发的独立课题提名（三候选）**

## §1 **资产清单（我们真正拥有的，逐项可查）**

```
**(A1) Lemma R（两矩阵 rank–trace 不等式，已证\bf{紧}，已形式化）** ✓
**(A2) 转移原理（`V186 §4 收2`）**：一阶 ＋ 二阶 ＋ 正惯性上界 ⟹ 计数下界 ✓
**(A3) Gabor 压缩机制**：`v_\rho=(\hat\phi(\gamma_\rho-\alpha_k))_k`、压缩阵 `\widetilde G`、重叠积分式 `(2.11)` ✓
**(A4) 宇称机制**：`T1`（σ-对称一阶响应 `=0`，`O(\delta^2)`）、`T3`（σ-反对称逃逸）、`L1`–`L4`、水平线自关系 `G_\gamma(x)=\chi(x+i\gamma)\overline{G_\gamma(1-x)}` ✓
**(A5) 空关系/盲读资产**（`V188`、`W4-1e`）＋ `PL`/分辨率判据 ✓
**(A6) 算术有限容量引擎（条件型）**：`EDR`（`N\le2^{|S|}`）、`RPC`（`N\le\omega(R)`）、`D=\prod_{q\in S}q` 规范形式 ✓
```

## §2 ⭐⭐⭐ **课题 I（首推）：自对偶函数中的「宇称检测」——以 Davenport–Heilbronn 为模型**

```
【原生资产】 `A4`（`T1`／`T3`／σ-配对／水平线自关系）✓✓
【独立问题】 取满足 Riemann 型功能方程却**可证违反其 RH 类比**的自对偶函数（`DH` 型线性组合，`\warn` 经典：`DH` 有无穷多离线零点 ✓）
　问：$$\boxed{\text{σ-反对称泛函在 }DH\text{ 的离线零点处是否有\textbf{可证非零}的一阶（}O(\delta)\text{）值？}}$$ ✓✓
【为何不是已关闭路线的包装】 不用密度／不用固定核大值估计／不用 inertia ⟹ 纯**宇称＋反对称泛函** ✓
【为何不要求 `N_{\rm off}=O(1)`】 目标是在**已知有离线零点的模型**上**验证检测机制**，不是给 `\zeta` 的有限性 ✓
【第一轮可计算判据（必写）】 显式写出 `DH` 的 `T3`-型反对称泛函 `\mathcal A_{\rm DH}(\rho)`（由 `\chi` 与 `L`-组合的系数给出），并判：(i) 是否恒为 `0`（⟹ 机制在模型上即失败，立刻 CLOSED）；(ii) 若可证非零且符号固定 ⟹ **得到"宇称检测在已知失败模型上有效"的首个正面结论** ✓✓
【`SURVIVOR-5`】 ①未知名定理 ⚠️**需一次文献核查**（`DH` 离线零点本身是经典；**宇称检测框架**是否已有 ⟹ 第一轮任务）｜②非重包 ✓｜③不要求 `O(1)` ✓｜④核心量真变 ✓｜⑤有可计算判据 ✓ ⟹ **①核查通过即启动** ✓
```

## §3 ⭐⭐ **课题 II：Gabor 压缩的「盲测度定理」（把元障碍定理化）**

```
【原生资产】 `A3`（`v_\rho`、压缩阵、`(2.11)`）＋ `A5`（空关系/盲读）✓✓
【独立问题】 固定窗 `\psi` 与调制格 `\{\alpha_k\}`，问：$$\boxed{\text{哪些符号测度 }\nu\ \text{对一切压缩都\textbf{不可见}（blind）？}}$$ 即特征化"零构型在该分辨率下的**不可辨识类**" ✓✓
【意义】 这**正是** `T1/T2` 元障碍的**定理化形态**（此前只是判据）⟹ 目标：**Gabor 不可辨识性定理**（调和分析/测度论问题，独立成篇）✓✓
【第一轮可计算判据】 对显式窗（如 Gauss 窗、截断余弦窗）与 `d` 个调制：(i) 构造一个**非零不可见测度**（构造性，⟹ 盲测度存在）；或 (ii) 证明在带宽阈值以下**无**非零不可见测度（⟹ 不确定性型定理）——两向皆**可判定** ✓✓
【`SURVIVOR-5`】 ①未知名定理 ✓（该形式化未见；⚠️ 常规文献核查）｜②非重包 ✓｜③不要求 `O(1)` ✓｜④核心量真变（从"零点"变到"测度不可辨识类"）✓｜⑤有可构造判据 ✓ ⟹ **可启动** ✓
```

## §4 **课题 III（支撑型，小而干净）：Lemma R 的等号分类与进一步转移原理**

```
【原生资产】 `A1`（已证紧、已形式化）＋ `A2` ✓
【独立问题】 ①**等号/紧性分类**：刻画使 Lemma R 取等的 `(P,Q,b,r,c)` 结构；②**转移原理的完整谱**：哪些迹数据族强制 `\operatorname{rank}` 下界 ✓
【性质】 纯线性代数/算子理论，独立、可发表级、且**已有一半基础设施（Lean 形式化）** ✓
【第一轮判据】 对小尺寸（`n\le4`）**枚举等号情形**并列结构（可计算、可穷举）✓
【`SURVIVOR-5`】 ①⚠️ 需查是否已有"两矩阵 rank–trace 紧性"文献｜②③✓④✓⑤✓ ⟹ **候补启动** ✓
```

## §5 **提名排序与边界**

```
【排序】 **课题 I（首推：宇称检测／`DH` 模型）→ 课题 II（盲测度定理）→ 课题 III（Lemma R 等号分类）** ✓
【共同点】 三者**都不以 RH 为目标**，而以现有资产为**原生工具**；且都满足"第一轮可写出可计算判据" ✓✓
【⛔ 不做】 不再提名"某个 RH 弱化猜想"（密度车道已封 ✓）；不再重启 `P/Q`／inertia 优化 ✓
【边界】 ⚠️ 课题 I 的 `DH` 事实、课题 III 的文献状态按**档级**（第一轮须核）；⭐ 本档的三项提名、`SURVIVOR-5` 审计、三处第一轮判据为**本档自行推导** ✓；⛔ 未制造候选／未启动搜索／未改状态 ✓
```
技术词 Davenport        命中文件数=28   :: ./grh-goldbach-paper-draft-v2.md ./CROSS-0-additive-multiplicative-cross-invariant-MAP-CHECK.md ./C305-FSD-blind-spot-audit-program-four-classes-dual-ledger-five-rounds.md 
技术词 盲点           命中文件数=9    :: ./C308-direction1-prime-gap-anomaly-four-column-audit.md ./C305-FSD-blind-spot-audit-program-four-classes-dual-ledger-five-rounds.md ./C312-counterexample-envelope-three-constraints-for-round2.md 
技术词 宇称           命中文件数=2    :: ./CONNES2026-read-2-comparison.md ./DIMENSION-QUESTION-answer.md 
技术词 等号分类     命中文件数=0    :: 
