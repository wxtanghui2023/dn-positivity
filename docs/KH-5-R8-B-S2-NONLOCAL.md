已查地图：命中 5 处 —— 先逐条判 已DEAD/已封/已登记；命中即引既有条目，不得开新案
　判定：⭐⭐ **实质命中 ＝ `V294-A-finite-order-aggregation-gain-classification.md` 专档**（技术词 `无限阶聚合`／`非局部正性` **即出自它**）✓
　⟹ ⛔ **本刀三类候选与三战场划界均在档 ⟹ 引用，不开新案** ✓✓

# **KH-5 / R8-B / S2-NONLOCAL**：非局部主项生成机制（第一阶段；暂不碰 RH）

**唐先生令（2026-09-22 23:18）**：
① **开刀**，**接受重构**：**不再找第三对象、不再重审有限阶组合；直接攻 `S2` 非局部主项** ✓✓
② **当前问题固定为**：$$\boxed{\text{已定义的 }S2\text{ 非对角对象}\ \xrightarrow{\ ?\ }\ \text{所需 }H\text{-uniform 主项}}$$（**不再问"有没有新 observable"**）✓
③ **第一门：三类候选**：**A 无限阶聚合**（`\mathcal A_H=\sum_{k\ge2}a_k(H)\mathcal C_k`；⭐ **须证明不能截断到任意固定 `K` 而保持所需增益**；
　关键不是"有无限和"，而是「**无限阶性必须承担主项生成机制**」——否则只是把有限阶对象写成无限级数 ⟹ **`S5` FAIL**）✓✓｜
　**B 非平凡算术相关性** `\mathcal R(H_1,H_2,\ldots)`（硬门：`\mathcal R\ne F(\mathcal C_{H_1},\ldots,\mathcal C_{H_m})` 对**任意固定 `m`** ⟹ **genuine long-range dependence**，非"多算几个二体量"）✓｜
　**C 非局部正性** `\mathcal P_H[\mathfrak a]\ge0`（正性须依赖**全局/无限尺度**算术结构，**非**逐点局部条件 `\mathfrak a_p\in S(p)` 拼出 ⟹ **直接继承 `S6`**：「`\mathfrak S(h)` 只记 local admissibility `\not\Rightarrow` global positivity」⟹ "更漂亮的奇异级数"**不算 survivor**）✓✓
④ **第二门 `H`-uniform 硬门**：须产出真正的 `M(X,H)=\operatorname{Main}(X,H)+E(X,H)`，**主项在整个目标 `H`-regime 稳定存在**；
　⛔ 不接受 `H=H_0` 固定、或只在孤立尺度成立 ✓
⑤ **第三门增益**：`\mathfrak G_{\rm NL} := \dfrac{\text{非局部主项产生的信息/尺度增益}}{\text{R8 已有二体窗口基准}}`；**只接受 `\mathfrak G_{\rm NL}>0`**；
　`=0 ⟹ 记 **NONLOCAL-NO-GAIN**`；`<0 ⟹ 严格劣化`；升级测试＝`\limsup_{K\to\infty}G(K)>0\ ?`
　⛔ **不得把"取 `K\to\infty`"本身当非局部机制**：须证明**极限过程产生有限阶体系无法产生的主项** ✓✓
⑥ ⭐ **防伪三门（任一 YES ⟹ 立即淘汰）**：**`S5`** 无限表示是否**只是有限对象的展开**？｜**`S6`** 正性是否**只是 local admissibility 的积分/加权**？｜
　**`S2`** 主项是否**真的新增**，而非已有 `J` 的变换？✓✓
⑦ **第一阶段暂不碰 RH**：唯一目标＝找**真正非有限阶**的算术机制，且证明它给 `S2` 非对角对象带来 **`H`-uniform、闭包外、严格正**的主项增益 ✓；
　**成功后才**进 `nonlocal main term → β-sensitivity → RH relevance`；**失败则精确记录是 A/B/C 哪一类死掉、死在 `S5`/`S6` 还是增益为 `0`**（不得笼统宣布"非局部也不存在"）✓✓

D0: 本档对象 = **`S2` 非局部主项的候选审计（`S2-NONLOCAL` 第一阶段）**（引既有 `V294-A`；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算／零数值／未写程序**／未做数值实验／未算零点／未接 ζ／未找 `1/2`／**第一阶段未碰 RH** ✓

---

## §1 ⭐⭐ **决定性**：本刀三类候选**＝ `V294-A` §3 的既有候选集**（逐字 ✓✓）

```
【`V294-A` §3 逐字】「故**真正候选收窄为（唐先生）**：
　$$\boxed{\text{无限阶聚合}\ \big|\ \text{算术相关性}\ \big|\ \text{非局部正性}}$$」✓✓✓
【且 §3 一并给出定理级前提】「**RH 所需的聚合增益不可能来自有限阶局部零点几何；必须来自非局部**」✓
【§5 真问题重述（逐字）】「**现有无条件二体／有限体聚合的增益是否严格为 0，还是存在能产生 RH 级增益的非局部结构？**」✓✓
⟹ ⛔ **本刀不是新议程，而是 `V294-A` 未完成部分的第一阶段执行** ✓✓
```

## §2 三战场**文献校准**（`V294-A` §4 逐字；**这是本档的核心事实** ✓✓）

| 战场 | ① 零点侧 | ② 算术侧 | ③ 算子论（**非局部正性**） |
|:--|:--|:--|:--|
| 状态（逐字） | **已封** ✓✗ | **有增益但已饱和** ⚠️ | **已有条件 `2/3`**（**明确不经 RH**）⚠️✓ |
| 依据 | 有限阶 `G(K)\le0` 定理 ✓ | `arXiv:2306.04799`（无条件对相关）；前沿 §7.2(e)：`X\asymp T` **无所得**、`k=3` **差 `T^{1/3}`** ✓ | `arXiv:2608.13637`（Weil Hermitian form **有限压缩** ＋ `rank-trace` ＋ Sylvester 惯性）✓ |
| 备注 | — | **增益存在但有上限** ✓ | 完整 Weil 正性 **⟺ RH** ⚠️（故现阶段只能取**不含 RH 的 2/3**）✓ |

## §3 防伪三门 × 三类（**逐类判定** ✓）

| 类 | `S5`（是否只是有限对象的展开） | `S6`（是否只是 local admissibility 的积分/加权） | `S2`（是否只是 `J` 的变换） | 判定 |
|:--|:--|:--|:--|:--|
| **A 无限阶聚合** | ⚠️ **分裂**：若"无限"来自**截断极限**（筛权、`\Lambda^2` 型）⟹ **YES ⟹ 淘汰** ✓；若来自**全素数积** ⟹ 见 `S6` | ⚠️ **全 Euler 积型 ⟹ YES ⟹ 淘汰**（`\mathfrak S(h)` 型 ⟹ 正是 `S6` 锁死对象）✓✓ | 若基于同一批 `\mathfrak C_h` ⟹ **YES ⟹ 淘汰** ✓ | ⛔ **零点侧：`V294-A` 已封**；**算术侧：与 B 同类** ✓ |
| **B 非平凡算术相关性** | ✓ 非有限对象展开（若是真长程） ✓ | ✓ | ⚠️ **风险**：若"依赖全部 `\mathfrak C_h`" ⟹ 由 `J` 的二阶差分**可反演** ⟹ **是 `J` 的变换 ⟹ 淘汰** ✓✓ | ⛔ **＝战场 ②：有增益但已饱和**（`arXiv:2306.04799`；§7.2(e)：`X\asymp T` 无所得、`k=3` 差 `T^{1/3}`）⟹ **`\mathfrak G_{\rm NL}` 被前沿界住** ⚠️ |
| **C 非局部正性** | ✅ **非有限对象展开**（全局正性） ✓ | ✅ **非 local admissibility**（Weil 型正性是**全局二次型**） ✓ | ✅ 非 `J` 的变换 ✓ | ⭐ **＝战场 ③：`S5`/`S6` 双干净 ＋ 明确不经 RH，但只到条件 `2/3`** ⚠️✓ |

```
【⭐ 本档净判定】 **唯一通过全部防伪三门者 ＝ C 类（非局部正性／算子论）** ✓✓
　· 其**无条件部分（`2/3`）已达**，**且不经 RH** ✓；
　· ⛔ 但 **`2/3` ≠ `\mathfrak G_{\rm NL}>0` 的无条件成立** ⟹ **第三门未过** ⚠️
```

## §4 第二门（`H`-uniform）与第三门（增益）

```
【`H`-uniform】 ⛔ **未达**：三战场中**无一**已在**整个目标 `H`-regime** 产出 `S2` 所需的 `M(X,H)=\operatorname{Main}+E` ✓（② 饱和、③ 条件性）✓
【`\mathfrak G_{\rm NL}`】 ① 零点侧：**已封 ⟹ `\le0`**（定理 ✓）｜② 算术侧：**饱和 ⟹ 相对前沿基准增益 ≈ `0`**（`=0` ⟹ 记 **NONLOCAL-NO-GAIN** ✓）｜
　③ 非局部正性：**无条件部分 = `2/3`**，**缺 `1/3`** ⟹ **尚未达 `\mathfrak G_{\rm NL}>0`（无条件）** ⚠️
【升级测试】 `\limsup_{K\to\infty}G(K)>0\ ?` ⟹ **未达成**；⛔ 且本档**未**把"取 `K\to\infty`"当机制（依令 ⑤ 守 ✓）
```

## §5 判定（**逐类记录死因，不笼统否定** ✓✓）

```
【A 无限阶聚合】 ⛔ **死在两处之一**：**留在零点侧 ⟹ `V294-A` §4① 已封**；**转算术侧 ⟹ 落回 ② 饱和** ✓
　（另：截断极限型 ⟹ **`S5` FAIL**；全 Euler 积型 ⟹ **`S6` FAIL**）✓✓
【B 非平凡算术相关性】 ⛔ **＝战场 ②：有增益但已饱和**（`arXiv:2306.04799`；§7.2(e)）⟹ **`\mathfrak G_{\rm NL}=0` ⟹ NONLOCAL-NO-GAIN** ✓
　（另：若其"长程"仅由全部 `\mathfrak C_h` 生成 ⟹ **`S2` 门 FAIL**（＝`J` 的变换）✓）
【C 非局部正性】 ⭐ **唯一过 `S5`/`S6`/`S2` 三门者**；**无条件已得 `2/3` 且不经 RH**；⛔ **但 `2/3` 不足 ⟹ 三/四门未过** ✓
【本阶段结论】 ⛔ **无候选达成「`H`-uniform ＋ 闭包外 ＋ 严格正主项增益」** ⟹ 依令**精确记录**（上表）✓✓
【⭐ 残差（唯一活口，精确定位）】 $$\boxed{\text{C 类（算子论非局部正性）所缺的 }1/3\ \text{—— 且必须在不引入 RH 的前提下补齐}}$$ ✓✓✓
　（档案亦记：**完整 Weil 正性 ⟺ RH** ⟹ 故残差**只能**是"不经 RH 的剩余 `1/3`" ✓）
【状态】 `KH-5` **保持 OPEN**；唯一活动支线＝ **`KH-5/R8-B/S2-NONLOCAL`**；⛔ **不宣布"非局部也不存在"**（依令 ⑦ ✓）
```

## §6 边界与回查（✗✓）

```
✗ 零计算／零数值／未写程序／未做数值实验／未算零点／未接 ζ／未找 `1/2`／**第一阶段未碰 RH**／**未开新案** ✓
✗ ⛔ 未把"取 `K\to\infty`"当机制 ✓；⛔ 未提"更漂亮的奇异级数"（`S6` ✓）；⛔ 未笼统宣布"非局部不存在" ✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 非局部主项 命中文件数=0 ⟹ **本档新增** ✓｜长程依赖=0 ⟹ **新增** ✓
　技术词 无限阶聚合 命中文件数=2 ⟹ **档案已有（`V294-A` 专档）⟹ 引用** ✓✓
　技术词 非局部正性 命中文件数=4 ⟹ **档案已有（`V294-A`／`CLOSED-ROUTES-MAP`／`MASTER-STATUS`）⟹ 引用** ✓✓
　技术词 增益量 命中文件数=1 ⟹ **档案已有（异义）⟹ 不主张** ✓
【地图回查】 命中 5 ⟹ **实质命中 ＝ `V294-A` 专档** ⟹ **引用、不开新案** ✓
【档级引用】 **`V294-A`**（`§3` 三候选；`§4` 三战场校准；`§5` 真问题）✓｜`R-A8.2`（`S2` 非对角＝缺主项；`S5` 警示；`S6` 锁死）✓｜`C90-DEC-1`（曲率假设实测失败）✓
