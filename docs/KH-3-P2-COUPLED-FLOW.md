已查地图：未覆盖（关键词: 耦合流|二流耦合|伪耦合|耦合谱）—— 可开档，首行须照抄本行
　（⚠️ `耦合谱` 1 档＝`tang-arc-state-2026-09-02` **异义** ⟹ 不主张；`双曲` 84 档**多为 hyperbola 异义** ⟹ 慎用；
　`parity` **70** 档／`奇偶障碍` **7** 档 ⟹ **实质覆盖 ⟹ 引用** ✓）

# **KH-3 / P2-COUPLED-FLOW**：耦合能否产生 `1/2`？（四门筛选）

**唐先生令（2026-09-22 22:45）**：
① 选 **(a)**，但**只攻"多流耦合能否产生 `1/2`"这一单点**，**不进入 (b)** ✓；
② ⭐ **本刀值得攻的理由（照录）**：`KH-3/P2-FEASIBILITY` 已给出干净结构结果
　$$\boxed{\text{单一原生算术流}\ \Longrightarrow\ \text{可产生固定点与 }\gamma\text{-型谱}\ \not\Longrightarrow\ \frac12}$$ ✓✓
　且这不是"又一个对象没找到"，而是**把 `P-3` 的"临界指数来源"墙下推到了耦合层** ⟹ 本刀测试：**耦合是否真的增加了自由度，还是只是把两个已有谱重新组合** ✓✓
③ **四门（照录）**：
　**G1 耦合必须算术原生**：最小二流 `T = (T_{\rm sieve}, T_\mu)`，`T_\mu f = f \star \mu` 与筛法流 `S` 耦合；
　　允许的耦合项必须**直接由整数性、除数关系、`\mu`、`v_p`、素数筛权**产生；⛔ **禁止** `T_{\rm coupled} = T + 人为加入 1/2`，
　　⛔ 禁止把 **ζ 的函数方程、零点实部、临界线参数**作为**耦合常数** ✓✓；
　**G2 耦合固定点必须真正"自洽产生"**：⭐ **关键不是"存在一个固定点"，而是**「**单流没有的结构，必须由 coupling 本身产生**」；
　　⚠️ 例：若固定点只是 `(x_*,y_*) = (1,\delta_1)` 这种**两个各自已有的单位元拼起来** ⟹ **没有通过 G2** ✓✓；
　**G3 线性化后必须出现新的谱机制**：算 `J = D(T_1,T_2)(x_*,y_*)`，**严格区分三种**：
　　**① 真新**（`\mathrm{Spec}(J)` 出现单流谱中**不存在**的本征值，且该值**由耦合项决定**）｜
　　**② 伪新**（`\lambda_\pm` ＝ 已有 `\lambda_1,\lambda_2` 的**简单代数组合**，如 `\lambda_1+\lambda_2`、`\lambda_1\lambda_2`、`\sqrt{\lambda_1\lambda_2}`）
　　⟹ **直接判 `P2-COUPLED-DEAD`**（没有产生新机制）｜**③ 未决**（谱非平凡改变但算术来源未证 ⟹ 保留 `GAP`）✓✓；
　**G4 最后才测 `1/2`**：只有 G1–G3 都通过才问 `\lambda_* = 1/2 ?`；须满足最强版本：`1/2 \notin \{\text{单流已有常数}\}` 且
　　`1/2 = \mathcal F(J_{\rm coupled}, x_*, y_*)`（**`\mathcal F` 定义本身不用 RH、零点实部、函数方程或显式公式**）；
　　⭐ 且**暂时不要证明 RH**：只登记「**是否出现一个独立产生的、可与 `\beta_{\max}` 配对的临界谱偏移**」（避免把"得到 `1/2`"偷偷变成 RH 等价命题 ✓✓）
④ ⭐ **伪耦合杀手（照录）**：先问 `coupling = 0` 时**是否退化为两个旧流**？再查一阶耦合 `J(\varepsilon) = J_0 + \varepsilon K + O(\varepsilon^2)`：
　若所谓 `1/2` **只来自 `J_0`** ⟹ **直接 DEAD**；若 `d\lambda_*/d\varepsilon|_{\varepsilon=0} \neq 0` ⟹ 至少说明耦合**真正改变谱** ✓✓
⑤ **对 (b) 的处理（照录）**：**暂不转非紧致状态空间**——理由：**尚无证明"单流 → 耦合流"不能生成新临界尺度**；
　若现在转 (b)，极易再次进入 `非紧致 → 边界/谱 → 零点`，最后重撞 `P3-B / E4-1` ✓✓

D0: 本档对象 = **耦合流能否产生 `1/2` 的单点判定（`P2-COUPLED-FLOW`）**（档案**零覆盖** ⟹ 新档；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算（解析）／零数值／未写程序**／未做数值搜索 ✓

---

## §1 **G1**：算术原生的耦合项 ✅

| 耦合 | 生成元（须**纯算术** ✓） | 判定 |
|:--|:--|:--:|
| **卷积 ↔ 筛法**：`T_\mu f = f \star \mu` 与筛权 `S` 耦合 | `\mu`、素数整除性 ✓ | ✅ |
| **Möbius 反演关系**：`x \star y = \delta_1`（`x = \mathbf 1,\ y = \mu`） | 整数性／乘法 ✓ | ✅ |
| **divisor involution**：`d \mapsto n/d` | 除数关系 ✓ | ✅ |
| `v_p` 与筛权耦合 | valuation ✓ | ✅ |

```
⟹ **G1 ✅ 通过**：耦合项**皆算术原生** ✓；⛔ **禁止项已守**：无"人为 `1/2`"、无泛函方程／零点实部／临界线参数作耦合常数 ✓✓
```

## §2 ⚠️ **自纠**（前档错误，本档修正 ✓✓）

```
【错】 `KH-3/P2-FEASIBILITY` §2 曾写：**卷积流 `f \mapsto f \star \mu` 的（被强制）不动点 ＝ `\delta_1`** ✗✗
【对】 `f \star \mu = f \iff f \star (\mu - \delta_1) = 0`；在 **Dirichlet 环**（**整环** ✓）中 ⟹ **唯一解 `f = 0`（平凡不动点）** ✓✓
　（整环性依据：局部幂级数环为整环，而 Dirichlet 环为其**直极限** ⟹ 整环 ✓〔**我的论证** ⚠️，未逐字核档〕）
【被强制的**不是不动点**而是**关系**】 $$\boxed{\mathbf 1 \star \mu = \delta_1}$$（**Möbius 反演** ✓✓）
【影响】 该纠正**强化** §3 的 G2 失败（耦合固定点**更不可能**是"新结构" ✓）
```

## §3 **G2**：耦合固定点是否"由耦合本身产生新结构"（❌ FAIL ✓）

| 耦合 | 固定点来源 | 是否产出**单流没有的结构** | 判定 |
|:--|:--|:--|:--:|
| **精确恒等式型**（如 `\sum_{n\le x}\mu(n)\lfloor x/n\rfloor = 1`） | **由恒等式钉死** | ⛔ **刚性 ⟹ 无自由度**（档案 `P-3` 已判：**`\Delta \equiv 0 \Longrightarrow` 无选择 ⟹ `S2` FALSE** ✓✓） | **FAIL** |
| **Möbius 反演关系** | `(\mathbf 1,\mu)` 的**关系式** | ⛔ **两既有对象的关系拼接** ⟹ 正合令例的 `(1,\delta_1)` 型 ✗ | **FAIL** |
| **卷积流自身** | `f = 0`（§2 修正 ✓） | ⛔ **平凡** | **FAIL** |
| **筛权 × `\mu`（Selberg／Brun 型）** | 由筛权闭合 | ⚠️ 理论存在，但产出**盲性**（见 §4 · parity ✓） | **FAIL（无新结构）** |

```
⟹ **G2 ❌ FAIL**：算术原生的耦合，其固定点**要么平凡、要么是既有对象的关系拼接、要么刚性无自由** ⟹
　⭐ 令中那句「**单流没有的结构，必须由 coupling 本身产生**」**未达成** ✓✓
【旁证（档级）】 `P-3` 已把**缺陷可利用来源**穷举为两类（精确算术恒等式／误差型定义）**且两类皆判死** ⟹
　本档从**耦合固定点**角度**独立复现**同一结论 ✓✓
```

## §4 **G3**：线性化后是否出现**新谱机制**（判 **②** ⟹ `P2-COUPLED-DEAD` ✓✓）

```
【情形甲 · 刚性耦合（精确恒等式型）】 耦合**不带自由参数** ⟹ `J(\varepsilon)` 的 `\varepsilon` 方向**不存在** ⟹
　**谱不变** ⟹ 既非①亦非②，而是**"无变化"** ⟹ **DEAD** ✓；
　⚠️ **伪耦合杀手在此空转**：**无 `\varepsilon` 可求导** —— 这一"空转"本身即诊断：**刚性耦合不引入新自由度** ✓✓
【情形乙 · sieve × μ 耦合】 ⚠️ 产出 **parity barrier**（档案 `parity` **70** 档／`奇偶障碍` **7** 档 ⟹ **已登记 ⟹ 引用** ✓）⟹
　耦合产出的是**盲性（退化）**，**不是**新本征值 ⟹ 判 **② 伪新（实质无新谱）** ✓✓
【⭐ 本档最有价值的新刻画】 **原生 `1/2` 确实出现过，但是"非谱型"**：
　· 出现处 ＝ Dirichlet **双曲切割**（`\sum_{n \le \sqrt x}`；`\sqrt x = x^{1/2}`）✓✓
　· 其性质：由**除数对称/对数凸性强制** ✓、**ζ-free** ✓✓（**G3 第 4 问 PASS** ✓）、**算术原生** ✓
　· ⛔ 但其身份是**组合最优切割指数**，**不是 `\mathrm{Spec}(J)` 的本征值** ⟹ **不入 G3 ①** ✓✓✓
　· ⟹ **谱型 `1/2`：单流层未出现（前档 ✓），耦合层亦未出现（本档 ✓）** ✓✓
⟹ **G3 = ②（无新谱）** ⟹ 依令 ⟹ $$\boxed{P2\text{-COUPLED-DEAD}}$$ ✓✓
```

## §5 **G4**：**未达**（依令：只有 G1–G3 通过才问 `1/2` ✓）

```
⛔ **不进入 G4**（`G2` FAIL ＋ `G3` 判 ② ⟹ 依令不得问 `\lambda_* = 1/2` ✓）
✓ 亦**未**证明或使用 RH；**未**把"得到 `1/2`"变成 RH 等价命题 ✓
```

## §6 ⭐ 卡点升级（**依令预判的强结论** ✓✓）

```
【升级后的卡点陈述】
　$$\boxed{\text{单流不给 }1/2 \quad\Longrightarrow\quad \text{单流 ＋ 最小算术耦合 仍缺临界指数来源}}$$ ✓✓
【新增刻画（相对 `P-3`）】
　· `P-3`：临界指数 `1/2` 的**来源**恰好是那些已知／已封闭者（归一化／函数方程／显式公式）✓
　· 本档：**原生 `1/2` 是存在的（双曲切割），但它是"最优切割指数"，不是"线性化本征值"** ⟹
　　**桥缺失的精确形态 ＝ 缺少把"组合最优指数"提升为"谱量"的机制** ✓✓✓
【对 (b) 的支持（依令 ⑤）】 本档**加强**了不宜现在转 (b) 的理由：耦合层**已测**、无新谱 ⟹
　若转 (b) 只能再赌一次"非紧致边界 → 谱"，而档案已预警其重回 `P3-B / E4-1` ✓✗
```

## §7 边界与回查（✗✓）

```
✗ 零计算（解析）／零数值／未写程序／未做数值搜索／**未开案** ✓
✗ ⛔ 不把"耦合 DEAD"记为战绩；⛔ 不宣布"不存在能产生 `1/2` 的耦合"（**只测最小二流** ⟹ 守"未找到 ≠ 不存在" ✓）；
　⛔ 不进入 G4 ✓；⛔ 不转 (b) ✓
⚠️ §2 的 Dirichlet 环**整环性**为**我的论证**（未逐字核档）⚠️；§4 parity barrier 为**档级引用** ✓；双曲切割为**标准事实**（凭记忆 ⚠️）
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 耦合流 命中文件数=0 ⟹ **本档新增** ✓｜伪耦合杀手=0 ⟹ **新增** ✓｜二流耦合=0 ⟹ **新增** ✓｜P2-COUPLED=0 ⟹ **新增** ✓
　技术词 耦合谱 命中文件数=1 ⟹ **档案已有（异义）⟹ 不主张** ✓
【地图回查】 关键词 4 项 = **未覆盖** ⟹ 可开档 ✓；档案覆盖：`parity` **70**／`奇偶障碍` **7**（引用）／`hyperbola` 5／`除数和函数` 0 ✓
【档级引用】 `ASSETS-REGISTRY` **`P-3`**（来源全堵 ＋ `S2` FALSE）✓｜`P3-B`／`P3-B2` ✓｜parity barrier 登记（`parity` 70 档）✓｜`KH-3/P2-FEASIBILITY`（单流谱枚举；**本档修正其 §2 一处** ✓）
