已查地图：命中（`EDR-CAP-1-counterexample-and-corrected-form`／`IP-5-RUN-dminus3-bounded-search-result`（`B_n` 表））⟹ 引用，不开新案
D0: 本档对象 = `CAP-1` 首跑**失败报告**（约定不符，输出不可用）＋ 由此暴露的**结构点**（`B_n=1` 的 `n`）
D1: 0 （[REVIEW] 轮次：失败报告与结构点，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`CAP-1` 首跑：无效（约定不符）＋ 一个真实结构点**

## §1 ⛔ 失败报告（不掩饰）

```
【实现】 `E:y^2+y=x^3-x^2-10x-20`，`P=(5,5)`；按 `x(nP)=A_n/B_n^2` 取既约分母 ✓（`P` 在曲线上已核：`5^2+5=30=125-25-50-20` ✓）
【实得】 `B_1=B_2=B_3=B_4=1`（`x(2P)=16`，整数）⟹ **与档案不符**（档案 `B_2=2,\ B_3=13,\ B_4=604=2^2\cdot151,\ B_5=6153=3\cdot7\cdot293,\ldots`）⟹ $$\boxed{\text{约定不符 ⟹ 本次输出\textbf{不可作为 }CAP-1\text{ 的证据}}}$$ ✓✓
【附带伪影】 因 `\operatorname{Supp}=\varnothing`，`S=\varnothing` 给 `|T(S)|=3>0` ⟹ **退化伪反例**，须显式排除 ✓
【修正动作】 查档案 `B_n` 的生成脚本/约定（疑为 EDS 递推定义或不同归一化），复现后再跑 ✓
```

## §2 ⭐ 真实结构点（本次唯一有价值的产出）

```
【观察】 存在**多个** `n` 使 `B_n=1`（即 `nP\in E(\mathbb Z)`，整数点）
【⟹ 结构性推论】 凡 `B_n=1` 者 `\operatorname{Supp}=\varnothing\subseteq S` 对**一切** `S` 成立 ⟹ $$\boxed{\text{所有 }B_n=1\text{ 的 }n\text{ 都是\textbf{无条件例外指标}}}$$ ✓✓（不止 `n=1`！）
【⟹ 修正后的正确形式（替代上一档）】 上一档写的 `|Z_S|\le1+\omega(D)` **仍不够** ⟹ 应为 $$|Z_S|\ \le\ \#\{n:\ B_n=1\}\ +\ \omega(D)$$ ✓✓ —— 首项＝**整倍数个数**（Siegel 有限），非恒等于 `1` ✓
【与您结构的对应】 您给的 `|Z_S|\le N_0+\#\{\text{可用 }q\}` 中，`N_0` **至少要 ≥ `\#\{n:B_n=1\}`** ⟹ 这给出了 `N_0` 的**一个可计算下界** ✓（**不涉 abc**，纯结构）✓✓
【边界】 §1 失败为本机实测（`out/cap1_run_failed_convention.txt`）；§2 结构点为**本档自行推导**；⛔ 未改状态／未碰 RH／未碰 abc 有效性。
