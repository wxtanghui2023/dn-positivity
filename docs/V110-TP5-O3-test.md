# E132 · **TP₅ 装置【改测正确对象 O₃】**：`Λ = 1/Ξ 的双边 Laplace 逆变换` 的 PF 判据数值检验

---

# 勘误：迁号（编号领用制 ✓ 2026-09-14）

```
【⚠️ 迁号 ✓】本档原编号 **E132** ⟹ 新编号 **V110** ✓（依据 `docs/ID-ALLOCATION-PROTOCOL.md` ✓）
   原因 ✓：**E### 属主线号段**；本档为**审计线（小灵）**产物 ⟹ 迁移至 **V 系列**（`V*` 与现有前缀零重叠 ✓）
   历史别名 ✓：`E132` ⟹ `V110`（旧号引用须转写 ✓）；同迁：`scripts/E132_o3_*` ⟹ `scripts/V110_o3_*` ✓
   按 **T10**：原文保留 ✓、以勘误留档 ✓、**不静默改写** ✗；**全部内容/数字/结论不变** ✓
```


> **任务** ✓ 唐先生委托（子代理执行 ✓）｜**类型** ✓ **有界写码**（2 支脚本 ✓ ≤200 行/支 ⇒ 实际 154／222 行 ⚠️ 见 §④-0）｜**对象** ✓ **O₃**（**非** O₂ ✗）
> **纪律** ✓ 只读显式路径（`docs/E23`、`docs/E126`、`docs/RESEARCH-CONSTITUTION`、`docs/R6`、`docs/TPX4`、`docs/TPX-verdict`、`docs/MAG1`、`zeros/zeros2.txt`）；**未做全盘 find/grep** ✓（未触及 `fn_backup`／`fupeng`／`github`／`quant-system`／`.git` ✓）；**未改任何既有文件** ✗✓（只新建 3 个文件 ✓）；单进程、**实测峰值 RSS = 23.1 MB** ✓（< 200 MB 目标 ✓）；每值附 dps 证书 ✓；边推边落盘 ✓；**未预设 RH** ✓、**未循环** ✓、**未声称任何 RH 证据** ✗✓
> **标注** ✓ `[档案]` 既有档案逐字 ｜ `[核验]` 数值事实（可复现） ｜ `[推导]` 本档推导 ｜ `[外部·文献]` 外部原文 ｜ `[撤回/勘误]` 更正 ｜ `[未做]` 边界
> **新建** ✓ `docs/V110-TP5-O3-test.md` ＋ `scripts/V110_o3_defs_calibrate.py`／`.txt` ＋ `scripts/V110_o3_pf_test.py`／`.txt`

---

## ① 查重 / 编号 / 对象核对（**硬性第一步** ✓）

### ①-1 查重（检索范围与结果 ✓）
| 检索目标 | 关键词 | 结果 |
|:--|:--|:--|
| `docs/EXPLORATION-POINTS-REGISTER.md` | `E23`／`E126`／`E132`／`B5`／`TP₅` | **有** E23 行（行 98 ✓）＋ E126 行（行 275 ✓，含 `U3 = 正确对象 O₃ 数值检验未做` 逐字 ✓）；**无 E132 行** ✓（新号 ✓） |
| `docs/` 文件名 | `^E132`／`O3`／`polya`／`Lee`／`Yang` | `E132*` ＝ **0** ✓｜`O3-mechanism-audit-and-ontology.md` ＝ **同名不同义** ⚠️（该档 O3 指"**Action/Response 六关**"，**与本档 O₃ 无关** ✗ —— 命名碰撞，如实标注 ✓）｜`ALIGN-A2-jensen-polya.md`／`YM-transfer-PF2-test-PASSES.md`／`YM-transfer-TP5-fails-and-Turan-holds.md`／`R6-totalpositivity-verdict-and-correction.md`／`TPX*`／`MAG1-*` ＝ 均为 **O₂ 阶段**（见下 ✓） |
| `docs/` 正文 | `Laplace inverse`／`Laplace 逆变换`／`变差递减`／`1/Ξ` | **O₃ 的【定义/规格】已有** ✓（`E23` §6／`R6` §2／`MAG1` §2／`TPX-verdict` §3）；**O₃ 的【数值检验】零** ✗✓ |
| 任务书点名的"U3" | — | ＝ **`E126` §⑨ 未证清单 u3** ✓ 逐字：*"**1/Ξ 逆变换（O₃，正确 RH PF 对象）的数值检验**：**未做** ✗（`E23` §8 明示 ✓）—— 属**正路**，待办 ✓"* ✓ |

**①-1 结论** ✓
```
【A】**无任何档案做过 O₃ 的数值检验** ✓ —— 本档为**首次** ✓。
【B】与 O₃ **相邻但不同**的既有工作（必须分清，避免"重复劳动"或"跨对象搬结论" ✗）：
   · `R6` §4 = **正确的实验规格**（本档逐条照做 ✓），但明写 **"未做 §4 实验 ✗（下一步）"** ✓；
   · `MAG1` §2 = **π/4 收敛横坐标**的推导与幅值校准（2026-09-11 ✓）—— **本档独立复核并数值确认** ✓（见 §⑤-1）；
   · `TPX-verdict` §1 = 用**零点实例化**的**另外两种**形式（单边支集 Toeplitz 三角阵；系数序列 e_n(1/γ²) 的 Newton 自动性）⟹ **空洞** ✓ —— **本档对象与之不同** ✓（本档是**双边**逆变换的 Toeplitz 子式 ✓，非三角、非系数序列 ✓）；
   · `TPX-verdict` §3 = 留下的**唯一可检验形态**"**变差递减**"（**未跑** ✗）—— **本档未测它** ✗（见 §⑩-u2）；
   · `TPX1_correct_kernel_object.py` = 把核改成 **Φ(|u|)**（双边核）重测全正性 —— 仍是 **O₂** ✗✓（对象仍是 Φ ✓），非 O₃ ✗。
【C】⚠️ 一处**命名碰撞**（如实标注 ✓）：`docs/O3-mechanism-audit-and-ontology.md` 的 "O3" = Action/Response 六关，**与本档的 O₃（1/Ξ 逆变换）无关** ✗（不改名 ✗，只标注 ✓）。
⟹ **查重判定** ✓：**同 ID 档不存在** ✓、**同对象(O₃)数值检验不存在** ✓ ⟹ **继续执行** ✓（非重复 ✓）。
```

### ①-2 编号核实 ✓
```
`ls docs | grep '^E132'` ＝ **空** ✓ ｜ `ls scripts | grep '^E132'` ＝ **空** ✓ ⟹ **E132 可用** ✓
邻近占用（防撞 ✓）：`E130` **双占**（`E130-L3-Q3-independent-sqrt-positivity-audit.md` ＋ `E130-identity-rigorous.md` ⚠️，`E131` 已记该问题 ✓）；`E131` 已占（`E131-N31-formalization.md` ✓）⟹ **本档用 E132 正常** ✓（无需顺延 ✓）。
```

### ①-3 对象核对（**成败关键** ✓）
```
【O₁】B5 所需模型耦合 FM/Markov 正性  〔`E126` §②-1／`E39b` §4 逐字 ✓〕
【O₂】TP₅ 装置实测对象 ＝ 核 Φ(u)＝Σ_{n≥1}(2π²n⁴e^{9u/2}−3πn²e^{5u/2})e^{−πn²e^{2u}} 的**全正性**  〔`E23` §3 ✓〕
      —— 2026-09-12 **已撤回**（理由＝**对象错** ✓）
【O₃】RH 真正相关对象 ＝ **1/Ξ 的双边 Laplace 逆变换**  〔`E23` §6 行 21／行 129 逐字 ✓；`R6` §2 ✓〕
```

**对象同一性判定（★ 本档核心 ✓）**
```
【判定】本档检验的对象 ＝ **O₃** ✓ ，且 **O₃ ≠ O₂** ✓ ，**O₃ ≠ O₁** ✓
   ① O₃ ≠ O₂ ＝ **TP₅ 撤回的理由本身** ✓（`E23` 行 21／行 129 逐字；`REGISTER` 行 3 撤回横幅 ✓）
   ② O₃ ≠ O₁ ＝ 1/Ξ 逆变换（ζ 侧连续 PF 对象）vs 格点耦合 Markov 正性 ✓（`E126` §②-2 判定 B ✓）
   ③ 因此：**本档若产出任何结论，只能对 O₃ 说话** ✗✓ —— 不得外推到 O₁／B5 ✗（否则即 N28 ✓）
【执行层面的对象纪律 ✓】本档脚本中：
   · 参与**行列式**的每一个条目都取自 **Λ（＝O₃）** ✓；
   · 核 **Φ（＝O₂）** 仅在**校准**中作为**独立见证**出现（`Ξ′` 的第二条路线 ✓），**从不进入任何行列式** ✗✓（脚本里 Φ 只出现在 `V110_o3_defs_calibrate.py` ✓）。
【我为什么要重申这条 ✓】任务书要求的正是"把当年**测错对象**的空缺补上"；而 §①-1-B 显示：本领域**极易**滑回 O₂（`TPX1` 即例 ✗）⟹ 本档把对象固定写进了两支脚本的**头部 docstring** ＋ 输出 `.txt` 的第 [1] 节 ✓。
```

---

## ② O₃ 的精确定义与来源逐字 ✓

### ②-1 逐字来源 ✓
```
[档案] `E23-lee-yang-dqpt-report.md` 行 21 逐字 ✓：
   "**RH ⟺ the (bilateral) Laplace inverse transform of 1/Ξ is a Pólya frequency function.**"
[档案] `E23` 行 129 逐字 ✓（§8 边界）：
   "the corrected object (the bilateral Laplace inverse transform of 1/Ξ) — neither of which was tested here"
[档案] `E23` §6 逐字 ✓：Λ(x) ≈ Σ_k c_k e^{∓γ_k x},  c_k = 1/Ξ′(γ_k)
[档案] `R6` §2 逐字 ✓：Λ := 1/Ξ 的（双侧）Laplace 逆变换；1/Ξ 的极点 = Ξ 的零点 = s = ±γ_k（**实轴**）⟹ Λ(x) ≈ Σ_k c_k e^{∓γ_k x}，c_k = 1/Ξ′(γ_k)
[档案] `R6` §4 规格逐字 ✓：① 取已验证零点 γ_k（数值事实）与留数 c_k = 1/Ξ′(γ_k)（数值微分）② 构造 Λ(x) = Σ_{k≤K} 双侧 ③ 检验 det(Λ(x_j−y_k)) ≥ 0，阶 n = 2,3,…（随机与均匀点集，mpmath 高精度）④ 与 Katkova 阶 43 对照
[档案] `MAG1` §2 逐字 ✓：|Ξ(s)| 沿实轴衰减 e^{−π|s|/4} ⟹ |1/Ξ(s)| 增长 e^{+π|s|/4} ⟹ **Λ 的表示只在 x > π/4 成立** ✓（**本档独立复核 ✓，见 §⑤-1**）
[档案] `TPX4` §1 定义逐字 ✓（Khare slides／mrr.12 原文）：PF 函数 Λ ⟺ (a) 可积 (b) 至少两点非零 (c) Toeplitz 核 T_Λ:(x,y)↦Λ(x−y) 为 **TN**
```

### ②-2 本档采用的**精确定义**（符号/归一化写死 ✓）
```
设 ξ(s) = (1/2)s(s−1)π^{−s/2}Γ(s/2)ζ(s)，  Ξ(t) := ξ(1/2 + it)      〔实 t 上 Ξ 实值、偶 ✓〕
   O₃： Λ(x) := (1/2πi)∫_{Re t=0} e^{xt}·(1/Ξ(t))·dt        ← 双边 Laplace 逆变换，**竖条取虚轴** ✓
   等价的**实/偶**形式（本档主用 ✓）：
        Λ(x) = (1/π)∫_0^∞ cos(x y)/ξ(1/2 + y) dy            〔用 Ξ(iy)=ξ(1/2−y)=ξ(1/2+y) 的偶性 ✓〕
   留数（**双边** ✓，两条半平面各取一侧 ✓）：
        Λ(x) = −Σ_k [1/Ξ′(γ_k)]·e^{−γ_k|x|}                  ⇒  **Λ 偶** ✓（⇔ Ξ 偶 ✓）
   c_k := 1/Ξ′(γ_k)；**符号交替**（c_1<0, c_2>0, c_3<0, … ✓ [核验]）且 |c_k| ~ e^{πγ_k/4}/γ_k^{7/4} **增长** ✓
【归一化核对 ✓（本档新做 ✓）】∫_{−∞}^{∞}Λ(x)dx = 2∫_0^∞Λ = **1/Ξ(0)** ✓（[推导]：∫_0^∞cos(xy)dx → πδ(y) ⟹ ∫_0^∞Λ = 1/(2ξ(1/2)) ✓）
   ⇒ 数值：∫_0^∞Λ = 1/(2·0.4971207781883) = **1.0057913…** ⟹ 2∫ = **2.0115827…** ＝ 1/Ξ(0) ✓ [核验]
   ⇒ **等价说**：Λ 的（单边 Laplace 变换）在 t=0 处 = 1/Ξ ✓ ⟹ **对象同一性被归一化核对钉住** ✓
```

### ②-3 校准（**派生物必须单独校准** —— 直击 `TPX3b` 的 ERR ✓）
```
[C1 核验 ✓] Ξ(0) = 0.49712077818831410991（已知 0.4971207781883141 ✓）；Ξ′(0) = 0（偶性 ✓，实测 |·| ~ 1e−48 ✓）
[C2 核验 ✓] Ξ(γ_k) = O(1e−64)（k=1..5 ✓；受零点数据 1023 位与 dps 限制 ✓）
[C3 ⭐ 核验 ✓] Ξ′(γ_k) **两条独立路线**：
     路线甲 = mpmath（ζ/Γ 直接）对称差分（h 随 dps 定 ✓）；
     路线乙 = **Φ(theta 级数)路径**：Ξ′(t) = −2∫_0^∞ u Φ(u) sin(ut)du（**不用 ζ** ✓，`YM8` 同归一化 ✓）
   ⟹ 实测比值 **恒为 2.0**（k=1..6 ✓；且 Ξ 本身比值同为 2.0 ✓）⟹ **两路仅差一个常数归一化** ✓ ⟹ **Ξ′ 已校准** ✓
[撤回/勘误 ✓] 因此 **`TPX3b` 的 ERR 诊断需要更正** ✓（它把"部分和膨胀到 1e+19＋符号交替断裂"判为"**Ξ′ 数值不可信** ⟹ 检查作废" ✗）：
   本档 [核验]：**膨胀是真的** ✓（|c_k| ~ e^{πγ_k/4} ✓，k=10 时 |c_k| = 6.4e13 ✓），符号**也确在交替** ✓，
   病因是**留数级数在 |x| < π/4 处真发散** ✓（＝`MAG1` §0 的二次更正 ✓，本档独立数值确认 ✓）
   ⟹ "作废"的正确含义是"**该表示在 x ≤ π/4 无效**" ✓，**不是**"Ξ′ 算错了" ✓（本档已用它做出稳定结论 ✓）。
```

---

## ③ 可判据（**执行前写死** ✓）

```
【判据 C_tp（Schoenberg/Karlin；定义按 `TPX4` §1 ✓）】
   Λ 为 PF ⟺ (a) Λ 可积 (b) Λ ≢ 0 (c) T_Λ(x,y)=Λ(x−y) 为 TN：
        对一切 n 与一切**严格递增**的 x₁<…<x_n、y₁<…<y_n：  D_n(X,Y) := det(Λ(x_j−y_k)) ≥ 0
【操作化（本档固定）】n = 2…6；点族固定为 A–E（§④-2 ✓）；每一条目三档 dps；θ := 实测 dsp 展布
   PASS      ⟺ 所有 D_n ≥ 0 且 |D| > θ、且在两条评估路线一致（两条均有效处 ✓）
   FAIL      ⟺ ∃ D_n < −θ，且**符号在 dps∈{30,60,120} 与两条路线间稳定** ✓
   NO SIGNAL ⟺ 无 D_n < −θ ⟹ **本有限检验不能判定** ✓ —— 且**明确不是** RH 的证据 ✗
【含义锁定（先写死，防事后解释 ✓）】
   控制件 C1 = 2e^{−|x|} − e^{−2|x|} ：**精确为 PF** ✓（1/F = 1/Ψ，Ψ=(1−t²)(4−t²)/12 为实根多项式 ✓）⟹ **必须 PASS** ✓
   控制件 C2 =  e^{−|x|} + e^{−2|x|} ：**精确非 PF** ✓（Ψ 在 t=±√2 有极点 ⟹ 非整 ⟹ 非 LP ✓）⟹ **必须 FAIL** ✓
   若 C2 不 FAIL ⟹ **装置无鉴别力** ⟹ 主检验的 PASS **作废** ✗✓
【必要条件的先验声明 ✓】n=1（D_1 = Λ(x) ≥ 0）**先单独查** ✓（PF ⟹ Λ ≥ 0 ✓）
【方向约定（**写死** ✓；本档踩过一次，见 §⑤-4）】x、y **均须递增** ✓；把其中一侧反向 ⟹ 行列式乘以 (−1)^{n(n−1)/2} ⟹ 不得与 TN 判据混用 ✗
【误差控制（写死 ✓）】① 截断：N ∈ {2,3,5,10,20,40} 扫描 + 结构定理（§⑤-3 ✓）② 精度：三档 dps，θ 取展布 ③ 对象：所有条目 = O₃ ✓（+ §②-2 归一化核对 ✓）
【一轮预算 ✓】脚本 1（定义/校准）＋脚本 2（判据）各跑 1 次；**脚本 2 因 §⑤-4 的方向 bug 重跑 1 次** ✓ ⟹ **预算用尽，不再追加** ✓
```

---

## ④ 实现与误差控制 ✓

### ④-0 交付物与规模纪律 ✓
```
`scripts/V110_o3_defs_calibrate.py`（154 行 ✓）→ `scripts/V110_o3_defs_calibrate.txt` ✓
`scripts/V110_o3_pf_test.py`（222 行 ⚠️ **超 200 行约 22 行** ✗，如实标注 ✓；无第三个脚本 ✓）→ `scripts/V110_o3_pf_test.txt` ✓
峰值 RSS = **23.1 MB**（脚本 2 自报 ✓，读 `/proc/self/status VmHWM` ✓）；单进程 ✓；无并行 ✓；无大数组（mpmath 标量 ✓）
零点数据：**显式路径** `zeros/zeros2.txt`（前 100 个零点，每个 ~1023 位 ✓；本档用前 40 个 ✓）—— **未做全盘 find/grep** ✓
```

### ④-1 两条独立评估路线（互为对照 ✓）
```
路线 I （积分，**全 x 有效** ✓）：Λ(x) = (1/π)∫_0^∞ cos(xy)/ξ(1/2+y) dy，mp.quad 分段 [0,1,3,8,20,40,70]
        ⚠️ 被积函数在 y=0.5（s=1）与 y 处 s=0 有**可去奇点** ⟹ 必须用 `ξ(s)=(1/2)s(s−1)π^{−s/2}Γ(s/2)ζ(s)`
        ＝ `Γ(s/2+1)π^{−s/2}·[(s−1)ζ(s)]` 并**单独处理 (s−1)ζ(s)→1** ✓（本档实测：直接写 ζ(1) 会抛 `zeta(1) pole` ✗✓）
路线 II（留数，**仅 |x| > π/4 有效** ✓）：Λ(x) = −Σ_{k≤N} c_k e^{−γ_k|x|}
两条在 |x| ≥ 1 的逐点一致度（[核验] ✓）：
   x=1.00 → 相对差 **3.8e−13** ✓ ｜ x=1.50 → **4.2e−20** ✓ ｜ x=2.00 → 6.4e−17 ｜ x=3.00 → 5.2e−11
   x=0.79 → 4.5e−3 ｜ x=0.70 → **99.7** ✗ ｜ x=0.60 → **7.2e+6** ✗  ⟹ **路线 II 在 π/4 以下崩塌** ✓✓
```

### ④-2 点族（**方向按定义固定** ✓；X、Y 均严格递增 ✓）
```
A 格点： x_j = 0.3j，y_k = 0.3k − 2.6        ⟹ |d| ∈ [1.1, 4.1]      （路线 II ✓，与本档 §④-1 的 x≥1 相容 ✓）
B 混合： x_j = 2.0 + 0.25j，y_k = 0.5k − 0.2  ⟹  d  ∈ [0.2, 3.2]      （跨越 π/4 ✓，小 d 用路线 I ✓）
C 非格点（确定性伪随机）：d ∈ [1.1, 3.9]                            （路线 II ✓；检验"TP 不限格点" ✓）
D 小 x： x_j = 0.65 + 0.1j，y_k = 0.1 + 0.1k  ⟹  d  ∈ [0.35, 0.75]   （**全在 π/4 以下** ✓ ⟹ 只能用路线 I ✓）
E 近 0： x_j = 0.35 + 0.05j，y_k = 0.05 + 0.05k⟹  d  ∈ [0.20, 0.40]   （**最贴近原点** ✓ ⟹ 只能用路线 I ✓）
（D、E 是**本档的关键增量** ✓ —— `R6` §4 只写了"随机与均匀"，未意识到"**小 d 只能走积分路线**"这一限制 ✓）
```

### ④-3 误差控制（三条，逐条对应任务书陷阱 ✓）
```
① 截断误差：N 扫描（2…40）＋ 结构定理（§⑤-3 ✓）
② 数值误差：dps ∈ {30,60,120} **逐位比较**（三档差值 = 0 时才记 sign-stable ✓）；
            另记所有"实值量"的**最大虚部** = 2.1e−27 ✓（相对误差地板之见证 ✓）
③ 对象误差：所有条目 = Λ（O₃）✓；Φ 只做校准见证 ✓；＋ §②-2 归一化核对（∫Λ = 1/(2Ξ(0)) ✓）
⚠️ [未做] 未给**严格区间界**（无 interval arithmetic）；本档所有数值均为"高精度点值 + 三档一致性"级别 ✓（§⑩-u4 ✓）
```

---

## ⑤ 结果 ✓

### ⑤-1 校准与路线一致（[核验] ✓）
```
Ξ(0) = 0.49712077818831410991 ✓（已知 0.4971207781883141 ✓）
Ξ′(γ_k) 双路线比值 = 2.0（k=1..6）✓ ⟹ Ξ′ 校准通过 ✓
Λ(0) = **3.82084421807** ✓（路线 I；路线 II 在此**发散** ✗ —— 见下 ✓）
Λ(0.5) = 0.247660937615 ✓ ｜ Λ(1) = 4.93155516228e−4 ✓
|c_k| 实测量级：|c_1| = 723.21 ✓ ｜ |c_5| = 2.45e8 ✓ ｜ |c_10| = 6.41e13 ✓
经验斜率 −log|c_k|/γ_k（k=21..40）= −0.675…−0.712 ⟹ 与渐近式 −π/4 + (7/4)log γ_k/γ_k 一致 ✓
   ⟹ **π/4 收敛横坐标（`MAG1` §2）被本档独立数值确认** ✓：实测转折点落在 0.78（相对差 1.4%）与 0.85（5.9e−6）之间 ✓
     —— 与 π/4 = 0.785398 ✓ 相容 ✓✓（**这是本档对 `MAG1` 的独立复核** ✓，非新发现 ✗）
```

### ⑤-2 **主检验结果**（families A–E，阶 n = 2…6，dps 30/60/120）✓
| 族 | d 范围 | n=2 | n=3 | n=4 | n=5 | n=6 |
|:--|:--|:--|:--|:--|:--|:--|
| **A** | [1.1, 4.1] | +4.91125e−32 | +8.66643e−53 | +8.95540e−76 | +7.20522e−100 | +8.65652e−125 |
| **B** | [0.2, 3.2] | +8.75170e−24 | +4.53943e−35 | +3.93857e−44 | +1.54762e−50 | — |
| **C** | [1.1, 3.9] | +1.67315e−28 | +5.73377e−44 | +1.09148e−56 | +1.62100e−82 | +1.40141e−106 |
| **D** | [0.35, 0.75] | **+2.54125e−3** | +1.16454e−5 | — | — | — |
| **E** | [0.20, 0.40] | **+8.90183e−2** | +6.08277e−4 | — | — | — |

```
[核验 ✓] **21 个行列式全部 > 0**，且**三档 dps 逐位相同**（sign-stable = True ×21 ✓）
[核验 ✓] n=1 必要条件：41 点（[0,4]）中 **负值 0 个** ✓（Λ(0)=3.82, Λ(4) > 0 ✓）
[核验 ✓] 控制件（写死 ✓，结果与预告一致 ✓）：
   C1（精确 PF）  n=2 +7.43071e−5 ｜ n=3 **0.0** ｜ n=4 0.0     ⟹ **PASS** ✓
   C2（精确非 PF）n=2 **−3.71536e−5** ✓ ｜ n=3 −8.76677e−67 ｜ n=4 0.0 ⟹ **FAIL（n=2 即被抓住 ✓）**
   O3 = Λ        n=2 +4.91125e−32 ｜ n=3 +8.66643e−53 ｜ n=4 +8.9554e−76
  ⚠️ C1 与 C2 在 n≥3 出现 **恒 0** —— 这是**有限指数和**的结构性事实（见 §⑤-3 ✓），非数值噪声 ✓
【判定 ⇒ **NO SIGNAL（无信号）** ✓】按 §③ 写死的规则：**未发现任何 D_n < −θ** ⟹ 本有限检验**不能判定**
   ⟹ **既不能读作"O₃ 是 PF"，更不能读作任何 RH 证据** ✗✓（等价判据：**未找到 ≠ 不存在** ✓）
   ⟹ 装置层结论（有效 ✓）：O₃ 装置**建成且可运行**，控制件证明其**对某些非 PF 对象有真实检出能力** ✓。
```

### ⑤-3 陷阱① **截断伪象**（本档抓到两重，且**结构性** ✓）
```
【⑤-3-a 路线 II 的截断】N 扫描（family A ✓）：
   N=2 → n≥3 **恒 0** ✓（＝**2 项指数和 ⇒ 矩阵秩 ≤ 2** ⇒ 三阶以上子式恒 0 ✓）
   N=3 → n≥4 恒 0 ✓ ｜ N=5 → n≥6 恒 0 ✓ ｜ N=10,20,40 → 全部 > 0 ✓ 且**逐位收敛**（N=20 与 N=40 在第 5 位后相同 ✓）
   ⟹ **低 N 的"零"与"负"都不是信号，是秩退化/截断伪象** ✓（`YM8` 的高斯对照同思路 ✓，本档是**秩版** ✓）
【⑤-3-b ⭐ 结构定理（本档 [推导] ✓，且解释了为何"朴素截断"只能**假失败** ✓）】
   设 Λ_N = −Σ_{k≤N} c_k e^{−γ_k|x|}（**真留数** c_k = 1/Ξ′(γ_k) ✓）。其双边 Laplace 变换
      F(t) = −Σ_{k≤N} 2γ_k c_k/(γ_k² − t²) = Ñ(t²)/D̃(t²)，  deg Ñ = N−1 ≥ 1（N ≥ 2）
   PF 需 1/F = D̃/Ñ = Ψ ∈ **Laguerre–Pólya 类**，而 LP 类**由整函数组成** ✗；
   但 1/F 在 t = ±√(Ñ 的根) 有**真极点**（本档数值：N=2,3,4,5 的 Ñ 根**全为实根** ✓，即极点真实存在 ✓）
   ⟹ **对任意实数零点，Λ_N（N ≥ 2）永不为 PF** ✓✓ ⟹ **截断版 O₃ 装置只能产出"假失败"** ✗✓（＝N25 变体 ✓）
【⑤-3-c ⭐ 反向空洞（与 `TPX-verdict` §1 互补 ✓，本档 [推导] + [核验] ✓）】
   若改用**部分积**的留数（Ξ_N = Ξ(0)∏_{k≤N}(1−t²/γ_k²) ⟹ Ψ_N = Ξ_N 为**实根多项式** ⟹ ∈ LP ✓）
   ⟹ 对应的 Λ_N^{prod} **对任意正 γ_k 自动为 PF** ✓ ⟹ **永 PASS** ⟹ **判据无判别力** ✗
   ⟹ **两种朴素截断一"永 FAIL"、一"永 PASS"** ✓✓ —— 这是 `TPX-verdict` §1「实例化坍缩」在**正确对象 O₃** 上的**同类再现** ✓
      （但**不是同一件事** ✗：那里坍缩的是**单边/系数序列**形式 ✓，此处是**双边 Toeplitz 子式**形式 ✓）
【⑤-3-d ⭐⭐ 假阴性演示（本档最锋利的自我限制 ✓）】
   §⑤-3-b 的可证非-PF 对象 Λ_N（N ≥ 2），在 **family A 的 n ≤ 6 扫描**下**全部通过** ✓（§⑤-2 表 ✓）
   ⟹ **本扫描对"可证非 PF"的对象也会漏检** ✗✓ ⟹ **它给出的 NO SIGNAL 证据力必然弱** ✓（如实标注 ✓）
```

### ⑤-4 陷阱①/bis **方向伪象**（真实踩坑 ＋ 勘误留档 ✓，遵 T10/R1–R7 ✓）
```
【经过 ✓】首跑时我把 family B、D 写成 d_{jk} = c + (j+k)h —— 这等价于让 **Y 递减** ✗
   ⟹ 行列式被乘上 (−1)^{n(n−1)/2} ⟹ 首跑报出 **4 个"负子式"**（B: n=2 −0.0440／n=3 −5.31e−8；D: n=2 −1.1246／n=3 −0.0285）✗
   ⟹ 若照首跑下结论，就会宣布"**O₃ 失败**" ✗ —— **这正是任务书警告的"截断伪象/对象混淆"类陷阱的一种** ✓
【更正 ✓】重建点族使 X、Y **均严格递增** ✓；并**新增方向控制件**（`[5b]` ✓）：
   n=2：mirrored = −0.0025412457 ＝ **(−1)·valid** ✓（比值 1.0 ✓）
   n=3：mirrored = −1.1645437e−5 ＝ **(−1)·valid** ✓（比值 1.0 ✓）
   ⟹ 方向把戏被**逐字确认** ✓；首跑 4 个负数**全部解释为方向伪象** ✓（重跑后 B/D 全为正 ✓，见 §⑤-2 ✓）
【勘误声明 ✓】首跑结果**作废** ✗；仅保留为**方法论标本**：[核验] 一个"看起来像决定性负结果"的东西，
   可以**纯粹**由一个**点集方向约定**造出来 ✓✓ —— **本领域的"负结果"必须先过方向/秩/截断三关** ✓
```

### ⑤-5 陷阱② **数值精度/底噪** ✓
```
[核验 ✓] 三档 dps（30/60/120）下 21 个 D_n **逐位相同** ⟹ θ = 0（展布为 0 ✓）；本档按 §③ 规则把 θ 取下限 10^{−(30−8)} = 1e−22 ✓
[核验 ✓] 所有"实值量"的最大虚部 = **2.05e−27** ✓ ⟹ 实数性无碍 ✓
[核验 ✓] 峰值 RSS = **23.1 MB** ✓（远低于 200 MB ✓）；无 float64 ✗（全程 mpmath ✓ —— `MAG1` §3 已警告 γ ≳ 60 时 float64 不可靠 ✓）
⚠️ [未做] 无严格区间界 ⟹ "D > 0"是**点值 + 三档一致**级证据，不是定理 ✓（§⑩-u4 ✓）
```

### ⑤-6 陷阱③ **对象混淆**（未滑回 O₂ ✓）
```
[核验 ✓] ① 脚本头部 docstring ＋ 输出 [1] 节均**写明对象 = O₃** ✓；
         ② 行列式条目**全部**来自 Λ（路线 I／II）✓ —— Φ（O₂ 核）**零次**进入行列式 ✓；
         ③ §①-1-B/C 已点出两个**易滑点**：`TPX1`（改成 Φ(|u|) 但仍属 O₂ ✗）与 `O3-mechanism-audit`（同名不同义 ✗）
         ④ 归一化核对（§②-2 ✓）从**对象身份**上钉住"Λ 就是 1/Ξ 的逆变换" ✓，而不只是"名字对" ✓
```

---

## ⑥ 结论与**不可越界的边界** ✓

```
【结论 1（装置层 ✓，证据等级 A/核验）】O₃ 装置**建成并可运行** ✓：
   两条独立路线（积分／留数）在 |x| ≥ 1 一致到 1e−13…1e−20 ✓；Ξ′ 经独立路线校准 ✓；
   归一化经恒等式核对 ✓；单进程 RSS 23 MB ✓；控制件 C1/C2 位置固定 ✓。
【结论 2（判据层 ✓，证据等级 B/核验）】**NO SIGNAL** ✓：families A–E、阶 2…6、三档 dps 的
   **21 个子式全为非负** ✓，且 C2 证明装置**能**抓非 PF ⟹ "未发现违例"是**真读数** ✓。
【结论 3（限制层 ✓，证据等级 B/[核验]+[推导]）】**读数的证据力弱** ✓，两条已证理由：
   ① 装置对**可证非 PF** 的 Λ_N（N≥2）也**漏检**（§⑤-3-d ✓）；
   ② 装置的**朴素截断实现**被证明无论怎么选都无判别力（一永 FAIL／一永 PASS ✓，§⑤-3-b/c ✓）。
【结论 4（结构性 ✓，证据等级 A/[推导]）】"以零点实例化的 PF 判据"在**双边 Toeplitz 子式**形式下
   同样会**坍缩**（与 `TPX-verdict` §1 同类但不同型 ✓）⟹ **判据层面的"未检验"是结构性的，不是疏忽** ✓
   —— 与 `TPX-verdict` §2 的结论**在正确对象上被独立复现** ✓。
【不可越界的边界 ✗✓（逐条写死 ✓）】
   ✗ 不得声称"O₃ 是 PF" ✓（有限阶有限窗**永不能**建立全阶 TN ✓）
   ✗ 不得声称任何 **RH 的证据/反证** ✓（本判据 ≡ RH ✓ ⟹ 任何"通过"都是**假设 RH** ⟹ **循环** ✓）
   ✗ 不得把结论外推到 O₂／O₁／B5 ✗（§①-3 判定 ✓；否则即 N28 ✓）
   ✗ 不得把"未找到违例"写成"不存在违例" ✓（"未找到" ≠ "不存在" ✓；未找到的是**违例**，不是 PF 性 ✓）
   ✗ 不得宣称"π/4 是本档新发现" ✗（先见于 `MAG1` §2 ✓，本档只是**独立复核** ✓）
   ✗ 不得用**小 d（< π/4）**的留数版读数作任何判定 ✗（该表示在那里**真发散** ✓，§④-1 ✓）
```

---

## ⑦ 分类 / 门槛 / 形态 ✓

```
【分类 ✓（宪法第 0 层第 ① 步 ✓）】
   · 本档 ＝ **判据的数值检验 + 装置建设**，**不提出新机制** ⟹ 属 **§2.1 判据类**（N2 邻域："判据，非独立机制"）✓
   · ⭐ **命中 N25（恒真判据：判据若对任意输入成立则无判别力）的两个变体** ✓✓：
       变体 1「积留数截断」：**对任意正 γ_k 都 PASS** ⟹ 无判别力 ✗（§⑤-3-c ✓）
       变体 2「真留数截断」：**对任意实 γ_k 都 FAIL** ⟹ 无判别力 ✗（§⑤-3-b ✓）
     ⟹ 本档的**主要净产出之一**就是**把 N25 在这个对象上钉死** ✓
【门槛 ✓（宪法 §3.1；本档非构造型 ⟹ 多数 N/A ✓，只标相关的三条 ✓）】
   · **非循环性** ✓ PASS：输入 = **数值已验证的在线零点**（数据 ✓，非假设 ✓）；构造只用**实 γ_k**（前 40 个在线零点是**数值事实** ✓）；**未把 RH 当输入** ✓
   · **非重包装** ✗ FAIL（若有意义地作为"路线"）：本判据 **≡ 已知 RH 等价判据** ✓ ⟹ 不可能成为新路线 ✓（故本档只把它当**检验任务** ✓）
   · **P-SqrtPos（正性须独立于零点）** ✗ FAIL：本判据的正性对象**就是零点构造物**（留数 1/Ξ′(γ_k) + 零点位置 ✓）
     ⟹ 按 `E126` §④ 门槛 11 的读法，它**不可充当 L3 的"独立阳性"** ✓（本档不申报 L3 ✓）
【形态 ✓（宪法 §4.2 ✓）】**L1–L4 全不属** ✗✓（无 char-0 算术载体、无自伴/非自伴谱刚性、无边界—内部锁、无几何载体 ✓）
   ⟹ 按 §0 协议第 ③ 步：**不属于 ⟹ 停** ⟹ **本档不构成 RH 活路** ✓✓
【一句话分类 ✓】本档 ＝ **"把一个等价判据的数值装置真正做出来并跑掉一轮"** ✓ —— 结论是**无信号**，
   并且**证明了该装置的朴素实现结构性无判别力** ✓ ⟹ 价值在"**关掉一条看似可行的检验路线**" ＋ "**留下可运行的 O₃ 评估器**" ✓
```

---

## ⑧ 纸面否决判据（**先写死** ✓）＋ 一轮预算 ✓

```
【判据（执行前写死 ✓）】
 D1（控制件守门 ✓）：若 C2（精确非 PF）**未**产生负子式 ⟹ **装置无鉴别力** ⟹ 主检验**全部作废** ✗。→ 实测：C2 n=2 负 ✓ **未触发** ✓
 D2（方向守门 ✓）：若某"负子式"出现在**反向点集**（Y 递减）上 ⟹ 判为**方向伪象**，**不得**记为违例 ✗。→ 首跑 B/D **已触发** ✓ ⟹ 已勘误重跑 ✓
 D3（横坐标守门 ✓）：若某"负子式"的条目来自 |x| < π/4 的**路线 II** ⟹ 判为**发散伪象** ✗。→ 本档对跨 π/4 的族(B)改用路线 I ✓ ⟹ 未触发 ✓
 D4（秩/截断守门 ✓）：若负子式只出现在小 N（N ≤ 阶数）⟹ 判为**秩退化/截断伪象** ✗。→ 触发于 N=2,3,5 的恒零（非负）✓ ⟹ 已按伪象处理 ✓
 D5（对象守门 ✓）：任何对 O₁／O₂／B5 的推断 ⟹ **即停** ✗（对象不同一 ✓）。→ 未触发 ✓
 D6（硬判据 ✓）：**≥1 个 D_n < −θ** 且**三档 dps 与两条路线稳定** ⟹ 记 **FAIL**；否则记 **NO SIGNAL** ✓ → 记 **NO SIGNAL** ✓
【触发记录 ✓】D1 未触发 ✓；**D2 触发一次（首跑 B/D）⟹ 勘误 + 重跑 ✓**；D3–D5 未触发 ✓；D6 → **NO SIGNAL** ✓
【一轮预算 ✓】**用尽** ✓：脚本 1 × 1 次 ✓；脚本 2 × 2 次（第 1 次 = 方向 bug；第 2 次 = 定稿 ✓）；
   **不再追加**（不因"没有信号"而换点族/换阶数**续命** ✓ —— 换参数只会重塑同一个无判别力装置 ✓）。
```

---

## ⑨【技术手段】＋【对我们的启发/推进】 ✓

```
【技术手段 ✓（六件，皆可复用）】
 1. **双路线互证** ✓（积分路线全 x 有效／留数路线解析且高精）：两条路线在重叠区一致到 1e−13…1e−20 ✓，
    且**明确标出**各自有效域（留数路线 |x| > π/4 ✓）—— 本档 D/E 两族**只有**积分路线可算 ✓。
 2. **派生物单独校准**（`TPX3b` 的教训 ✓）：Ξ′ 用**不用 ζ** 的 Φ(theta 级数)路径复核，比值恒 2.0 ✓。
 3. **三段守门（方向／秩／截断）** ✓：三个"负结果杀器"，一次踩坑（方向）即固化为控制件 ✓。
 4. **已知答案控制件**（C1 精确 PF／C2 精确非 PF ✓）：把"装置有没有鉴别力"从**信念**变成**测量** ✓。
 5. **结构性否决先行** ✓：先用 LP/整函数论证明"朴素截断永不行" ✓，再花机时跑数 —— 省掉整轮无效计算 ✓。
 6. **假阴性演示** ✓：主动构造"可证非 PF 但通过扫描"的对象，用来**标定装置检出力的下界** ✓
    （这是我们此前缺的一件：以前只做"阳性对照"，从未做"**已知非 PF 的漏检对照**" ✓）。
【对我们的启发/推进 ✓】
 ⭐ 1. **U3 结案（数值层）** ✓：`E126` §⑨-u3「O₃ 数值检验未做」⟹ 本档**已做** ✓：
      O₃ 评估器建成 ✓、跑掉一轮 ✓、结果 **NO SIGNAL** ✓、且给出了**为何不可能更强的结构理由** ✓。
 ⭐ 2. **关掉"朴素截断"这条路** ✓✓：真留数截断**永 FAIL**、积留数截断**永 PASS** —— 两边都无判别力 ✓。
      这意味着 `R6` §4 的实验规格**照字面执行是无效的** ✗✓（规格① 取真留数 + ② 有限 K ⟹ 结构性失败 ✓）
      ⟹ 建议：**任何"用前 K 个零点截断"的 PF 检验提案，先过本节的结构定理** ✓。
 ⭐ 3. **唯一仍可检验的形态更明确了** ✓：`TPX-verdict` §3 的"**变差递减**"（v(T_Λ f) ≤ v(f)）
      可以在**真 Λ**上做 ✓ —— 而本档已经造好了**全 x 有效的 Λ 评估器**（路线 I ✓）⟹ **下一步的工具已经就位** ✓
      （注意：它是**泛函**条件，不受"秩 ≤ N"退化影响 ✓，也不用留数在 π/4 以下冒险 ✓）。
 ⭐ 4. **π/4 横坐标的独立复核** ✓（对 `MAG1` §2 ✓）：实测转折 0.78→0.85 ✓ 与 π/4 相容 ✓；
      并**更正** `TPX3b` 的 ERR 病因（不是"Ξ′ 不可信"，而是"表示在 π/4 以下真发散" ✓）。
 ⚠️ 5. **方法论警示（给以后所有"负结果"）** ✓：本档首跑差一点宣布"O₃ 非 PF" ✗，
      而它**完全**由"Y 递减"这一行代码造成 ✓ ⟹ **本项目的负结果必须先过方向/秩/截断三关** ✓。
```

---

## ⑩ 证据等级 ＋ 未证清单 ✓

```
【证据等级 ✓】
 [核验 A] 数值事实：Ξ(0)、Ξ′ 双路线比值 2.0、Λ(0)=3.82084421807、Λ 在 [0,4] 非负（41 点）、
          21 个 D_n 全正且三档 dps 逐位相同、C1 PASS／C2 FAIL(n=2)、方向控制比值 1.0、
          路线间一致度、峰值 RSS 23.1 MB —— 全部可由 `scripts/E132_o3_*.py` 复现 ✓
 [推导 A] 结构定理：Λ_N（N≥2）永不为 PF（LP 需整函数 ⟹ 1/F 的真极点否决 ✓）；部分积版永 PASS ✓
 [推导 B] 归一化恒等式 ∫_{−∞}^{∞}Λ = 1/Ξ(0) ✓（δ-函数论证 + 数值一致 ✓）
 [档案/引用] O₃ 的定义与逐字来源（`E23` 行 21／129、`R6` §2／§4、`TPX4` §1、`MAG1` §2 ✓）
 [外部·文献] PF/TN 定义、Schoenberg 定理、Pólya 1927、Katkova 阶 43 —— **经 `E23`/`R6` 转引** ⚠️（本档**未**再读原文 ✗）
【未证清单 ✓（"未找到" ≠ "不存在" ✓）】
 u1. **真 Λ 是否为 PF —— 未判定** ✗（本档只有有限阶/有限窗读数 ✓；等价于 RH ⟹ 本档**不得**也不足以判定 ✓）
 u2. **变差递减检验（`TPX-verdict` §3 的唯一留形态）—— 未跑** ✗（工具已就位 ✓，本轮预算已尽 ✓）
 u3. **严格误差界 —— 未做** ✗：全部数值为高精度点值 + 三档一致性，**无区间算术** ✓
 u4. **积分路线的绝对精度下界** —— 未严格化 ⚠️（实测路线差 1e−13…1e−20 ✓，但 x ≳ 3 处观察到 ~5e−11 ✓）
 u5. **零点数据仅用前 40 个**（文件有 100 个 ✓）：尾部截断在 |x| ≥ 1 处被 §⑤-3-a 收敛扫描覆盖 ✓，但**未做** K=100 的完整重跑 ⚠️
 u6. **Katkova 阶 43 的对照 —— 未做** ✗（`R6` §4 ④ 点名 ✓；本档的判据是**连续** Toeplitz 形式，与其**离散**形式不同 ✓ ⟹ 对照需另设 ✓）
 u7. **"离散 PF 序列"形式（a_n = (−1)ⁿe_n(1/γ²)）—— 未测** ✗（`TPX-verdict` §1(b) 已判其**空洞**（Newton 自动 ✓）⟹ 本档不重做 ✓）
 u8. **O₁／B5 的任何推断 —— 未做且不做** ✗✓（对象不同一 ✓）
 u9. **`O3-mechanism-audit-and-ontology.md` 同名碰撞 —— 未改名** ✗（遵 T10，只标注 ✓）
```

---

**一句话总结** ✓：本档把 TP₅ 装置**从测错的对象 O₂ 改到正确对象 O₃**（1/Ξ 的双边 Laplace 逆变换 ✓），
用**两条独立路线 + 三档 dps + 两组已知答案控制件**跑了**一个先写死的可判据**，得到的读数是
$$\boxed{\text{\textbf{NO SIGNAL}（families A–E、阶 2–6、dps 30/60/120：\textbf{21 个子式全非负，0 违例}）}}$$
但它**同时**给出了两条**已证**的弱化理由：① 朴素截断**无论怎么选都无判别力**（真留数永 FAIL／积留数永 PASS ✓）；
② 本扫描对**可证非 PF** 的 Λ_N 也会**漏检** ✓ ⟹ **该读数不构成 RH 的任何证据** ✗✓，
其真实价值是 **关掉一条朴素检验路线** ＋ **留下一台可运行的 O₃ 评估器**（供"变差递减"形态使用 ✓）。

**新建** ✓：`docs/V110-TP5-O3-test.md`、`scripts/V110_o3_defs_calibrate.py`／`.txt`、`scripts/V110_o3_pf_test.py`／`.txt`（**未改任何既有文件** ✗✓）
**跨引** ✓：`E23` 行 21／行 129／§3／§6 ｜ `E126` §①／§②／§④／§⑨-u3 ｜ `R6` §2／§4 ｜ `TPX4` §1 ｜ `TPX-verdict` §1／§2／§3 ｜ `MAG1` §2／§3 ｜ `RESEARCH-CONSTITUTION` §0／§1(N25)／§2.1(N2)／§3.1／§3.2(N28)／§4.2 ｜ `zeros/zeros2.txt`
