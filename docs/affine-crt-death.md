# Affine prime dynamics CRT/representation factorization test（2026-09-03 11:50）

## 对象
T_p(x) = px+1 mod q（primorial q）——U_p 置换表示——U_s = Σ_{p<q,gcd=1}Λ(p)p^{-s}U_p
Tr(U_s^k) = Σ_words (Πw(p_j))·N_q(P,B)——N_q = 固定点数

## 数值
**q=30（2·3·5）**：
- N_q CRT 因子化确认（word (7,11): N₃₀=2——N₂·N₃·N₅=2·1·1 ✓）
- U_s 谱非平凡（30 特征值——max|λ|=1.27）——Tr(U_s¹)=0, Tr(U_s²)=3.81, Tr(U_s³)=0

**q=210（2·3·5·7）——42 素数**：
- Tr(U_s^k)：奇数 k 全 0（Tr¹=0, Tr³=0, Tr⁵=0）——偶数非零（Tr²=18.6, Tr⁴=135, Tr⁶=962）
- **谱关于 0 完美对称**（±成对——实部 −0.1/+0.1 各 35——±0.2 各 31——±2.8 各 1——±1.6 各 1）
- 稠密小特征值谱（210 个——像正则表示谱）

## 判断：表示论塌缩（倾向唐先生死因——affine-prime 路线倾向判死）
1. **CRT 因子化确认**（N_q = ∏N_ℓ——固定点数完全局部化）
2. **U_p ∈ Aff(Z/qZ)——U_s ∈ C[Aff(Z/qZ)]（群代数）**——谱完全由 Aff 的不可约表示决定——**素数（p 的算术）只进入系数 w_p（调制）——不进入谱结构**
3. **谱 ± 对称（奇数迹消失）是表示论结构**（Aff 群——非素数算术——）
4. 特征值 = Σ_p w_p·χ_π(U_p)（角色值素数加权和——）——χ_π = Aff 表示角色——若 = Dirichlet 角色 → −L'/L(s,χ)（死——）——若高维 → 仍表示论/L 函数类
5. **结构性结论（唐先生预告）**：有限模/有限群制造闭环——但谱落入有限群表示论——素数降级为系数（调制）——CRT 局部化 + 表示论吸收 = 双重塌缩

## 待确认
χ_π(U_p) 的精确形式（Dirichlet 角色 vs 高维——）——但表示论框架内倾向死

## 文件
scripts/affine_crt_test.py, docs/affine-crt-death.md
