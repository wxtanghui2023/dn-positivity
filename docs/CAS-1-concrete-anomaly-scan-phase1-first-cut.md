已查地图：命中（`E-20`／`E-22`／`AMEND-4`／`J-2` 本线自档与既有档）⟹ **引用，不开新案** ✓

# **`CAS-1` Concrete Anomaly Scan（第一阶段第一刀）：结果＝干净负结果**

**唐先生令（2026-09-23 13:29）**：启动 **`Concrete Anomaly Scan`** ✓ —— **第一阶段只做计算，不做机制命名** ✓；
　对象限定天然 RH 对象（`\Lambda(s)`／`\Lambda'/\Lambda`／`\zeta(s+\alpha)`／自然 prime·divisor sums）｜**操作只允许 shift／derivative／finite difference**｜
　⛔ **不允许为目标（`\beta`／`rank`／`support`／`parity`／`majorant`）选系数** ✓；优先 **`D`（导数·差分层级）＋`A`（移位矩）交叉区** ✓

D0: 本档对象 = **`CAS-1` 第一刀（低阶有限差分异常扫描：`Z` 与对照 `C`）**（引既有档；**未开 bridge 案** ✓）
D1: 0 （`[RESEARCH]` 轮次；结论为**计算级负结果**，不主张新自由度 ✓）
FREEZE-ACK: D1=0 ✓
[RESEARCH]

---

## §1 设定（只计算，不命名 ✓）

```
【脚本】 `scripts/cas1_finite_order_anomaly_scan.py`｜【日志】 `out/cas1/cas1_scan.txt` ✓
【对象】 `Z(t)` = Riemann–Siegel 实值函数（零点＝临界线零点；RH-connected ✓）
　**对照** `C(t)=\sum_{n\le60}\cos(t\log n)/\sqrt n`（**非** `\zeta`；用于判别"是否 ζ 特异" ✓✓）
【操作】 只 shift ＋ finite difference：`\Delta_h^k`，`k\le2`，`h=2\delta`，`\delta=0.05` ✓（零目标导向系数 ✓）
【量】 有限差分 Gram 矩阵 `Q_{kl}=\sum_j \Delta_h^k Z(t_j)\,\Delta_h^l Z(t_j)`，`k,l\in\{0,1,2\}` ✓
【窗口】 `T_0\in\{500,1000,2000\}`，`W=100` ✓
```

## §2 **六项记录（逐项 ✓✓）**

| 记录项 | 结果 |
|:--|:--|
| **① 异常消失** | ⛔ **无**：三个尺度下 `\det` 均 **>0**（`1.03\times10^{-5}`／`1.92\times10^{-5}`／`3.88\times10^{-5}`）⟹ **无退化** ✓ |
| **② 整数／有理结构** | ⛔ **无**：`M_{11}/M_{22}`（`Z`）＝ **24.429／19.201／15.317**（**无稳定有理值**）✓ |
| **③ 稳定递推** | ⛔ **无**：`\det` 随尺度**单调增**（≈ ×2／尺度翻倍）⟹ 非递推 ✓ |
| **④ 跨尺度不变量** | ⛔ **无**：`Z` 比值**单调漂移**（24.4→19.2→15.3）；对照 `C` 比值**平坦**（10.49／9.67／10.06）✓ |
| **⑤ 非平凡因子化** | ⛔ **无**：特征值三个**互异**（非 rank-1）✓ |
| **⑥ 某阶后结构突变** | ⛔ **无**：秩恒为 `3`，最小相对特征值 `\sim10^{-3}`–`10^{-4}`，**无塌缩** ✓ |

```
【⭐ 对照的意义（本刀最能说明问题的一点 ✓✓）】 `Z` 与 `C` 的**可区分**（比值漂移 vs 比值平坦）⟹
　$$\boxed{\text{该管线\textbf{具备判别力}}（不是对任何函数都给同一答案）}$$ ✓✓
【⚠️ 唯一可见模式及其诚实归位】 `Z` 比值**单调漂移**（24.4→19.2→15.3）——本例**预期**由 `\zeta` 局部相关结构随高度的 `\log T` 生长（零点密度）决定 ⟹
　$$\boxed{\text{可用已知"零点密度／}\log\text{ 尺度"账本解释}}\ \Longrightarrow\ \text{属\textbf{旧账本}，非异常}$$ ✓✓（依 `E-20` 第 4 条 ✓）
```

## §3 判定

```
【判定】 $$\boxed{CAS\text{-}1\ \text{第一刀} = \text{无异常（informative negative）}}$$ ✓✓
【为什么这个负结果是有信息的 ✓】 ① **管线有判别力**（`Z` vs `C` 不同）｜② 六项记录**逐项为否**｜③ 唯一可见模式**已由旧账本解释** ⟹
　即：**不是"没算出来"，而是"算出来发现没有低阶异常"** ✓
【⚠️ 诚实边界（必须说 ✓）】 本轮**只**覆盖：`T_0\le2000`、`W=100`、`k\le2`、两种对象、单一 `\delta` ⟹
　**不能**推广为"不存在异常"（**"未找到 ≠ 不存在"** ✓）；且本轮**未做任何机制命名** ✓（依阶段一纪律 ✓）
【下一步（同在扫描框架内，非新机制 ✓，供裁）】
　`(甲)` **提高阶数** `k\le4\sim6`（"消失阶"现象本应在**更高阶**显现 ✓）｜`(乙)` **扩大窗口／提高 `T`**（`T_0` 至 `10^4\sim10^5`）｜
　`(丙)` **换入口**：`B`（离散–连续缺陷）或 `C`（零点函数与算术函数的双重变换）✓｜`(丁)` 收手记档 ✓
```

## §4 【技术词回查】（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）

```
技术词 异常扫描     命中文件数=0    :: 
技术词 有限差分 Gram 命中文件数=0    :: 
技术词 跨尺度不变量 命中文件数=0    :: 
```
【三分类标注】 `0` ⟹ **本档新增**；`>0` ⟹ **档案已有（引用，不列为提出）** ✓

## §5 边界

```
✗ 未证 RH／未接 ζ 的证明层｜⛔ 未做机制命名｜⛔ 未为目标选系数｜⛔ 未把负结果写成"不存在" ✓
【R7】 结论由数字驱动（`\det`／比值／特征值 逐项打印 ✓）｜【R1】 脚本头含目的／输入／输出／纪律 ✓
```
