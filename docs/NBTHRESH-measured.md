# ⭐⭐ **NB 阈值实测**：门⑤的转换形状被【定量测出】—— 弱型（log 型）确认 ✓✓

**依据**：唐先生 2026-09-11 20:46（"1，2"）｜**脚本**：`scripts/escape_range_scan.py`（我方 P50/P51 线 ✓）运行 ✓
**标注**：【核验·我方脚本】｜【推导】｜【分类确认】

---

## §1 **脚本的模型**（原文注释 ✓）
```
逃逸零点 (σ₀=½+δ, γ₀) 在 NB 距离中的签名：**d_N² 签名 ~ N^{2δ}·A(γ₀)/log²N，A ~ 1/γ₀²**
在线基线：**C_Burnol/log N**（C = 2+γ_E−log4π ≈ 0.0462 ✓）
检测条件：**N^{2δ}/γ₀² ≳ C/log N**  ⟹  解出 **N*(δ,γ₀)**
```

## §2 ⭐⭐ **实测（可检测边界；N_max = 可用 mollifier 长度）**
```
N_max = 1e6  (log N = 13.8):  γ₀=1e3 → 需 δ ≥ 0.2937 ｜ γ₀=1e6 → 需 δ ≥ 0.794（>½ ✗ 不可能）
                              γ₀=1e9 → ✗ ｜ γ₀=3e12 → ✗
N_max = 1e9  (log N = 20.7):  γ₀=1e3 → δ ≥ 0.1860 ｜ γ₀=1e6 → δ ≥ 0.519 ✗ ｜ γ₀=1e9/3e12 → ✗
N_max = 1e12 (log N = 27.6):  γ₀=1e3 → δ ≥ 0.1343 ｜ γ₀=1e6 → δ ≥ 0.3843
                              γ₀=1e9 → δ ≥ 0.634 ✗ ｜ γ₀=3e12 → δ ≥ 0.924 ✗
```
⭐ **斜率**：(1e3→1e6) Δδ/Δlog₁₀ = 0.25/3 = **1/12**；(1e6→1e9) = **1/12**；(1e9→3e12) = **1/12** ✓✓
   ⟹ **δ_req ≈ (1/12)·log₁₀ γ₀ + const**（= log₁₀γ₀ / log₁₀N ✓，由条件 2δ log N ≳ 2 log γ₀ 得 ✓）
$$\boxed{\delta_{\text{req}}\ \approx\ \frac{\log_{10}\gamma_0}{\log_{10}N}\cdot c\quad(c\approx1.6)\qquad\Longleftrightarrow\qquad \gamma_0\ \lesssim\ N^{\delta}}$$
## §3 ⭐⭐⭐ **解读（本轮结论）**
```
① **NB 的可检测高度 ~ N^δ** ✓ —— 即：**用长度 N 的 mollifier，只能"看见"高度 γ ≲ N^δ 的离轴零点** ✓✓
② 数值后果：N = 10¹²（已是天文长度）+ δ = 0.1 ⟹ **γ₀ ≲ 10^{1.2} ≈ 16** ✗
   δ = 0.5（最大可能）⟹ γ₀ ≲ 10^{6} ≈ 10⁶ ✗ —— **远低于已验证高度 T = 3.000175×10¹²** ✗✗
   ⟹ **NB 在被数值验证的高度区间内【完全无能】** ✓✓✓
③ 分类学确认：**NB = 弱型（log 型）** ✓✓ —— 且现在**定量**：其配对为
    **log γ₀ ≲ δ·log N**（系数随 δ 变化 ✓），而 **Li 为 n ≲ T²（二次 ✓，强）** ✓✓
   ⟹ **三型分类（T² 强 / log 弱 / 无）在第四例（NB）上被实测确认** ✓✓✓
④ 附带结论：**NB 路线在与"零点数值验证"的对比中彻底落后**（10⁶ vs 10¹² ✗）⟹ **不可作为主路线** ✓
```

## §4 边界
```
【核验】§2 的实测表与斜率 1/12（脚本可复现 ✓）
【推导】§3 的"γ ≲ N^δ"与"NB 在验证区间内无能"；③ 的分类确认
【⚠️依赖】§1 的签名模型（**我方 P50/P51 推导**，本轮**未重推** ✗）—— 结论以其为前提 ✓
【未做】未读 C–vS §5 正文与 Appendix A（truncation matrices）✗；未输入 1/2；未构造模型；未改 L2；未声称任何证明
```
## §5 提交链
```
CVS3（b35aa83 C–vS §5 + 发现 NB 阈值线）→ 本篇（NB 阈值实测 + 分类确认）
```
