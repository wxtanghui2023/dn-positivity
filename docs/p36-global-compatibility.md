# P36：Global Compatibility / Zero-Measure Rigidity——第一轮（最小模型搜索）

> 2026-09-02 09:25 · 唐先生 P36 指令 · 范式转变：Pointwise Forcing → Global Compatibility

## 范式转变（唐先生框架）
- **旧**：ρ ⟼ G(ρ) ⟼ 正性/代价 ⟼ G(ρ)=0 ⟼ ρ=Rρ（pointwise——S2 缺源）
- **新**：整个零点集是刚性全局对象——离轴轨道使全局兼容性失效
- **核心结构**：I = I_line + Σ_{ρ∉Fix(R)}Δ(ρ)（Δ≥0）——独立证明 I = I_line ⟹ ΣΔ(ρ)=0 ⟹ Δ(ρ)=0 对所有零点——**S2 由全局守恒律自动产生**
- **守恒律三条件**：①非 RH 定义 ②非显式公式 tautology ③对整个零点集求和产生刚性

## 第一实验场：有限维矩问题 M_n = Σw(γ)(β−½)^{2n}
**数值**（前 50 零点——假想离轴）：
| δ | M_1 | M_2 | M_3 | M_4 |
|---|---|---|---|---|
| 0.00 | 0 | 0 | 0 | 0（在线——全零） |
| 0.05 | 0.125 | 0.0003 | 0.000001 | 0（正——离轴） |
| 0.10 | 0.500 | 0.005 | 0.00005 | 0.0000005（正） |
| 0.20 | 2.000 | 0.080 | 0.0032 | 0.000128（正） |

**Hankel rank**：在线 rank 0（collapse）——离轴 rank 1（>0）

**⚠️ 关键问题——M_n 的算术侧独立公式？**
- M_n = Σ(β−½)^{2n}——"零点实部偶矩"——**无已知算术公式**
- 已知：Σ1/ρ^n（复幂——显式公式对数导数）——但 (β−½)^{2n}（实部幂）需要"实部滤波"——x^ρ 的 β 依赖是 x^β——对 β 求导给 log x 因子——但 Mellin 反演在 σ=½——回到零点——**循环**
- **⟹ M_n 无独立算术公式——"ΣM_n=0"无独立来源——检测≠排除**

## 全局量自适应检查（Weil/Jensen/迹/计数）
- **Weil**：Σh(ρ) = archimedean − prime——对任何配置成立（自适应）——无缺陷
- **Jensen**：∫log|ξ| = Σ距离项——恒等式（自适应）——离轴距离更大——但等式恒等
- **迹公式**：Σf(γ)——γ 通道——不含 β
- **计数**：N(T) = (1/π)arg ξ——不含 β
- **⚠️ 所有已知全局量的"算术侧"都自适应——"I = I_line"是恒等——不是"独立证明"——无刚性**

## ⭐ P36 第一轮结论——全局版 S2 缺口定位
- **矩问题**：离轴正（检测）——但——无算术侧公式——"ΣM_n=0"无独立来源
- **全局量（Weil/Jensen/迹/计数）**：全部自适应——"I=I_line"恒等——无约束
- **核心困难**：所有"零点侧表示"都来自 Hadamard/显式公式（编码零点——自适应）——"非自适应全局量"需要"不经过显式公式的零点侧表示"——不存在（已知）
- **精确缺口（唐先生语言）**："Equality must be global before decomposed into zero contributions"——但我们需要的"全局等式 I=I_arith"——其零点侧表示——要么自适应（Weil——恒等）——要么循环（V——依赖 ψ）——要么无公式（β 矩）——**"非自适应全局恒等式"不存在（已知结构）**

## ⚠️ 但——P36 未封——两个未真正探索的方向
1. **"test-function cone"方向**（唐先生第 5 节）——不是再证 Weil criterion——而是问"**是否存在 test-function cone 使 positivity 本身成为 arithmetic 必然性**"——第一轮只确认了"标准 Weil 自适应"——**没有真正尝试构造 cone**——未探索
2. **形式化"非自适应全局量"的不存在性论证**（候选 (b)）——如果所有零点侧表示都来自 Hadamard/显式公式——"I=I_arith"恒等——全局刚性无来源——可写成严格条件性定理

## 下一步候选
- (a) **test-function cone 探索**（唐先生第 5 节方向——positivity 的 arithmetic 必然性——未真正试）
- (b) **形式化不存在性论证**（"非自适应全局量"不存在——严格条件性定理——检测≠排除的全局版）
- (c) 矩问题的 w(γ) 加权扫描（检测深化——边际低）
- (d) 唐先生指示
