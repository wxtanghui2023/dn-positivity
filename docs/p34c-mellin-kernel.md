# P34-C：infinite-rank Mellin-feature 核——第一轮（n₋ 增长——μ_N(ε) 权重依赖）

> 2026-09-01 · 唐先生 P34-C 指令 · Gate 0-3 审计 · μ_N(ε) 是关键（Problem II 拆分）

## 候选核
**K_pq = w(p)w(q)Σ_{k≥1}a_k cos(k(log p−log q))——a_k = Σ_p cos(k log p)/n（素数 Fourier——自然来源）**

## Gate 0 初步（arithmetic provenance）
- a_k：正 28——负 32——**符号是数据的（非人为指定 ✓）**
- a_k = [0.58, 0.04, −0.24, −0.15, 0.07, 0.14, ...]（混合——自然涌现）
- ⚠️ 但——"独立来源"（可分析渐近）未确认——a_k 的衰减（PNT 主项+波动）——Gate 0 未完成

## 结果（K_terms 10→60——303 素数）
**n₋（Gate 2——随 K 增长）**：
- w=1：8→48——w=e^-x/2：8→47——w=1/x：8→48（全部增长——genuine sign 初步 ✓）

**μ_N(ε)（Gate 3——关键）**：
| w | μ(0.1) | μ(0.5) | μ(1.0) | 判定 |
|---|---|---|---|---|
| w=1 | 8→45 | 8→41 | 8→37 | **增长——uniform 候选？** |
| w=e^-x/2 | 4→5（停滞） | 0 | 0 | **moving-edge 型（P28-P33 复现）** |
| w=1/x | 8→29 | 4→6（停滞） | 2→2（停滞） | 中间——浅增长深停滞 |

## ⭐ 初步判定——权重决定 uniform sector
- **w=1（无衰减）**：μ_N(ε₀)→∞（ε₀=0.1/0.5/1.0 都增长——uniform sector 候选——Gate 3 可能通过）
- **w=e^-x/2（指数衰减）**：μ 停滞——moving-edge 型
- **w=1/x（幂衰减）**：浅 uniform 增长——深停滞

## ⚠️⚠️ 极度谨慎——w=1 的无界风险
- **w=1 时核 Σa_k cos(kΔx)——a_k 若不衰减——核无界——"伪 uniform"（无界算子的伪影）**
- **a_k 的衰减未确认（Gate 0 未完成）——若 a_k 不衰减——需要 w 衰减（e^-x/2——但——那个 moving-edge）**
- **核心张力：有界自然核倾向 moving-edge——"uniform"需无界/边界条件——像 P28-P33**
- λ_min ~ −45（w=1——大——无界信号？）vs −0.35（w=e^-x/2——有界）

## 下一步
- (a) **Gate 0 完成**：a_k 渐近分析（Σcos(k log p)/n 的衰减——PNT——独立来源确认）
- (b) **Gate 1**：rank 审计（cos(kx) 特征函数在素数集的线性独立）
- (c) **w=1 的有界性**（核收敛——伪 uniform 排除——a_k 衰减的必要性）
- (d) 唐先生指示
