# P50-A(a)-R2：Euler-L² Capacity Audit——确认 + OPEN⑤ 结构性定位

> 2026-09-02 19:10 · 唐先生理论框架验证 · 数值补充 · 收档建议

## 唐先生的理论结果（全部确认）
- **PASS ① Mellin 对应**：E=ψ−x ⟷ F(s) = (−ζ'/ζ − s/(s−1))/s——V_β 是 Euler-L² 的自然 Mellin 边界量（σ=1）
- **PASS ② 无条件全局有限容量**：∫₂^∞(ψ−x)²x^{−3}dx < ∞（纯 Euler/素数结构——无需 RH）
- **PASS ③ 尾容量趋零**：∫_T^∞(ψ−x)²x^{−3}dx → 0
- **PASS ④ 临界尺度正确**：单离轴贡献 T·C_ρ(T) ≍ T^{2δ}——δ=0 是临界指数
- **OPEN/WALL ⑤**：sup_T T·C(T) < ∞（uniform dyadic capacity——coercivity 需要）——未从 A_min 得

## 数值补充（本轮的）
1. **V_β ~ 0.63**（∫₂^{1e6}——低 x 主导——尾部(1000→∞)=5.1e-5 之前确认）
2. **σ 依赖正确**（σ=1.1: 0.44——σ=1.5: 0.14——Mellin 权重一致）
3. **⭐ 在线 dyadic T·A_k ~ 0.58-0.87（常数——无系统趋势）**——**数值上在线确实满足 uniform dyadic capacity（sup ~ 0.87）**——β=½ 行为检测
4. **离轴判别尺度**：δ=0.1: T·C ~ T^0.2（1e3→1e6: 3.98→15.9）——δ=0.2: T^0.4（15.9→251）——增长——判别窗口清晰

## OPEN⑤ 结构性审计（为什么 Euler-L² 全局不够）
- Euler-L² 给 Σ_k A_k < ∞（全局质量）——**允许稀疏 k 上 A_k 大**（可求和即可）
- 要 A_k ≤ C·2^{−k}（uniform dyadic）——需要 E 的**逐尺度局部 L²**（每 dyadic 区间）
- Mellin 侧：dyadic 局部 L² ↔ Littlewood-Paley 分解——需要 F(σ+it) 的 **t-局部 L² 控制**
- F 的 t-局部行为 ↔ **零点密度（Ingham——N(σ,T)）**——σ→1 边界——**需要零点信息**
- ⟹ **结构性墙**：全局 L²（Euler 积 σ>1）到逐尺度 dyadic（σ→1 边界——零点密度）——中间隔零点信息

## 收档建议（符合唐先生预期）
**P50-A(a) 收档**：
- 全局容量（V_β < ∞——Euler-L²）—— **PASS**（无条件——从 A_min）
- 尾容量趋零 —— **PASS**
- 临界局部容量（sup T·C(T) < ∞）—— **OPEN（结构性——需零点密度——Ingham——非 A_min 可得）**
- 数值：在线 T·C(T) ~ 0.87 max（β=½ 行为——检测非约束）
- **P50-A 容量机制结论**：M 的素数侧结构给全局 L² 容量——不给逐尺度临界容量——离轴判别（13.6× 常数级）在此容量下不可分辨——**容量墙 = 全局 vs 逐尺度的 gap（Littlewood-Paley 层）**

## 转 P50-B（b₂）理由
- P50-A 已到结构性边界（全局 PASS——dyadic 需零点密度）
- b₂（局部二阶刚性——ξ 的结构）是不同通道——可能避开 Littlewood-Paley gap

## 文件
- docs/p50a-variance-capacity-first.md（R1）
- 本文档（R2——收档）
