# P51-T2：Projective Compatibility——第一轮（extension lifetime 朴素版）

> 2026-09-02 19:40 · ψ-相对匹配不排除 · 需要不同 O_T 或绝对容忍

## 数值结果（extension lifetime——单零点移动）
### 额外相对误差包络（|ψ_W(δ) − ψ_W(0)|/x 的窗口 max）
**所有 (k, δ) 组合单调衰减**：
- zero#0 (γ=14) δ=0.2: 1.42% (x=500) → 0.41% (x=1e5)
- zero#4 (γ=33) δ=0.2: 0.66% → 0.16%
- zero#19 (γ=77) δ=0.2: 0.30% → 0.073%
- 衰减 ~ x^{−0.3} 到 x^{−0.5} 级（接近理论 x^{δ−½}/γ_k）

### 关键结构
1. **单零点离轴额外相对误差 → 0**——**相对匹配无限延拓可行（L=∞ 朴素版）——不排除**
2. **γ_k 依赖**：高零点移动影响小（γ=77: 0.073% vs γ=14: 0.41% at 1e5）——低零点"贵"
3. **δ 依赖**：δ=0.2 比 0.05 大 ~10×
4. **配置级预判**（P5.4 全体移动 δ=0.2——相对 0.23% at x=1e5）——相位抵消——也不排除

## 诚实结论
- **朴素 extension lifetime（相对容忍 ε ~ x 百分比）——离轴 L=∞**——额外误差衰减——无限延拓相对可行
- 排除需要：**绝对容忍（在线级 x^{½}——循环风险）**或**不同观察映射 O_T**（非 ψ 相对）或**加权范数**（P50-A 的 V_β——需独立 C——撞墙）
- **核心张力重现**：M_T 的容忍/范数选择——相对（松——离轴可入）vs 绝对/加权（紧——需独立 C——循环）——**观察映射选择 = P50-A 容量墙的另一种形式**

## 正面发现（保留）
- 额外误差衰减率 ~ x^{δ−½}/γ_k——**γ_k 依赖**（哪些自由度"贵"——低零点移动影响大但衰减）
- 这可能是 M_T 内部结构的线索——但需不同观察映射才能利用

## 状态
- P51-T2 第一轮：朴素版（ψ 相对）不排除——extension obstruction 未出现
- 开放：O_T 的选择（非 ψ——如——加权 ψ——或——零点计数 N(T) 类——或——S(t) 相位类）
- ⚠️ 若 O_T 只能是 ψ 类（显式公式——所有零点信息通道）——P51-T2 可能并入"相对宽容墙"

## 下一步候选
- (a) 不同 O_T 探测（加权 ψ——如 ∫(ψ_W−ψ_prime)²x^{−3} 的局部——或零点计数/相位——看哪个对离轴 extension 敏感）
- (b) 绝对容忍的算术来源再想（在线级 x^{½} 能否从素数侧推出——P50-A 的 dyadic gap）
- (c) 唐先生指示
