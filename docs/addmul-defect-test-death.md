# 加乘缺陷谱测试结果：T1/T3 死亡（γ 消失——酉共轭保谱）（2026-09-02 22:40）

## 测试执行
按唐先生构造：V_N={1..N}——加法行 r_+(a,b)=e_a+e_b−e_{a+b}——乘法行 r_×(a,b)=e_a+e_b−e_{ab}——J（加-乘共享整数 → 1）——K_N = B^T J B（= F_add·F_mul^T 对称化——高效闭式）——K_N(s,s̄) = D*KD（D=diag(n^{−s})）——λ_min(N,s)。

## 结果（N=50, 100）
1. **λ_min 与 γ 完全无关**（所有 γ 值逐位相同——1e-11 精度）
2. **λ_min 只依赖 σ——单调递减**（σ→1 时 →0——从负侧）——**无 σ=½ 谷线**
3. 谱 = |D|K|D| 的谱（验证：max|Δev| ~ 1e-11）

## 杀灭条件判定
- **T1 死**：无稳定 σ=½ 谷线（谷在 σ→1——平凡衰减）
- **T3 死**：与 γ 无关的普通谱效应（纯 σ 权重）
- **T2 死**（连带）：σ（δ）效应是纯整体 renormalization（n^{−2σ} 权重）

## 根本原因（数学定理——非实现问题）
D(s) = diag(n^{−s}) = diag(n^{−σ})·diag(n^{−iγ}) = |D|·U(γ)——**U(γ) 是酉对角矩阵（相位）**。
K_N(s,s̄) = D*KD = U†·|D|·K·|D|·U——**酉共轭保谱**。
⟹ **谱（λ_min——酉不变）与 γ 必然无关——只依赖 σ（通过 |D|=n^{−σ}——平凡单调权重）**。
任何"对角 D 乘两边"的构造——谱与 γ 无关（数学必然）。

## 诊断（如果要救——构造需改）
γ 要进入"可观测量"——不能用**酉不变**的量（特征值/迹/谱）——必须用**固定基**的量：
- K_N(s)[m,n] = d̄_m d_n K[m,n]——含相位 (n/m)^{−iγ}——**固定基元含 γ**——但特征值（酉不变）不含
- 或——非对角的 transport（如平移算子——n→n+1）
- 或——特征向量在固定基的投影（非酉不变）

## 结论
按唐先生杀灭条件——**构造（此版本）埋掉**。诚实记录：γ 消失是数学必然（酉共轭保谱）——不是数值/实现可修。
