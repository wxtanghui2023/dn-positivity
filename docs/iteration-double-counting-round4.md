# 迭代推导（双计数刚性——第 4 轮）：CM √d 主项 + "相反响应"假设检验（2026-09-02 22:00）

## CM 的深层检查
类数公式：h(d) = w√d·L(1,χ_d)/(2π)——**√d 显式出现（主项——无零点）——L(1,χ) 是修正（振荡——含 L 零点——经显式公式——检测——β 墙）**。
**CM 轨道 κ=½ = 主项（√d——类数平均）——δ 翼（如果有）在 L(1,χ) 修正（含零点——检测）——死**。

## ⚠️ 机制的核心假设需要理由
"同一对象的两种 realization 对 δ 响应相反"（κ₁ = a+δ, κ₂ = a−δ）——**这个假设本身需要理由**：
- 自然理由 = FE（s/1−s——ρ↔1−ρ̄——但禁止——第 11 点）
- 加性/乘性（膨胀/收缩自然相反）——但 Z 的加性/乘性计数（分拆 e^{√n} vs 素数 x/logx）不相等——不同增长——无强制——且"同一级数两种求和"（σ>1）是恒等式——自适应——死
- **"相反响应"无独立理由——这是机制的隐藏假设——需审计**

## 第 2-4 轮累积状态（双计数刚性）
1. **F7 修正**：a 消掉（κ₁=κ₂ ⟺ δ=0——不需 a=½ 推导）——机制干净
2. **CM ½ 发现**：a=½ 有算术来源（类数平均 √d——真实）——但是主项——δ 在修正（L(1,χ)——检测）——死
3. **第三类双计数需求清单**：含 δ + 非恒等式 + 非 Lefschetz + 增长率相等——**数学中无已知实例**
4. **"相反响应"假设无独立理由**（FE = 禁止——加性/乘性 = 恒等式/无强制）
5. **FE 幽灵**：所有"−δ 翼"自然候选纠缠 1−s（FE 对合）——非 FE 的 −δ 无来源

## 诚实评估
双计数方向 4 轮迭代——机制被推干净（F7 修正）——但核心障碍（第三类双计数）纹丝不动——所有候选撞：FE 幽灵 / 恒等式 / 检测。**"相反响应"假设（两种 realization 对 δ 反号）可能是机制的隐藏弱点——它没有独立理由（FE 之外）**。
