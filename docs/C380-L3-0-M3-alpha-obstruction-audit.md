已查地图：命中 194 处 —— 先逐条判 已DEAD/已封/已登记；命中即引既有条目，不得开新案
　判定（实质命中，**全部引用** ✓）：⭐ **`C-347`**（奇偶层分离 ＋ **低阶奇多项式表**）｜⭐ **`C-348`**（**反称配对障碍**；逐字：
　「**`C-347` 的障碍不在 `E` 内 ✓✓：反称配对（全部奇频 = 0 ✓）被偶频约束自动排除 ✓✓ ⟹ 奇频下界路线未被[杀死]**」）｜
　`C-349`（抵消理想）｜`C-350`（零矩流形审计）｜**`C-373`**（**`Z ∩ E = ∅`**）✓

# **C-380-L3-0**：`M3-α` **障碍审计**（纯代数，零计算）

**唐先生令（2026-09-22 22:08）**：先审 `M3-α(G)`，**再决定是否进入 L3 构造**（理由照录：「候选虽形式上绕开 `Φ`，
但**还没有证明它真的绕开了 `M3-α` 的零集**。若先构造，极容易得到"分子分母同时趋零、但整个候选在 paired fiber 上仍恒为 `0`/未定义"的**伪突破**」）✓✓
　① **L3-0 检验**：固定 `{|t_j|}`，符号变量 `ε_j ∈ {±1}`、`t_j = ε_j a_j`；考察 numerator `N(ε;a)` 的**符号 Fourier 展开**
　　`N = Σ_{S⊆{1..5}} c_S(a) ∏_{j∈S} ε_j`（只有 `|S|` 为奇者有意义）✓；
　② 检查**不是**"某个 `F_{2r+1}` 是否为零"，而是更强：`N(ε; a,−a,b,−b,0) ≡ 0` 是否对**整个候选类**恒成立
　　（**恒成立 ⟹ 该类直接 CLOSED**）✓；
　③ **审 denominator**：`D(a)` 须检 `D(a,−a,b,−b,0) ≠ 0` ＋ `D → 0` 的**阶数**；⛔ **禁止偷换**：`N=O(δ)`、`D=O(δ)`
　　只说明比值**可能有限**，**不**说明 `δ=0` 有**可定义的二阶延拓** ✓；
　④ **真正的 L3 要求**：存在局部坐标 `δ` 使 `Ψ(δ) = N(δ)/D(δ) = Ψ(0) + O(δ²)`（或等价**二阶稳定性证书**）✓；
　⑤ **判定树**：`候选类 →(M3-α)→ {恒零 ⟹ CLOSED；非恒零 ⟹ 继续}`，继续后再过
　　`有限 → 非恒定 → 近碰撞稳定 → 二阶稳定`；**四关全 PASS 才登记为 KERNEL-HUNT witness** ✓✓；
　⑥ ⭐ **结构点（照录）**：**不要预设"最近间隔归一化"一定是正确的 denominator**；应让
　　`counterexample geometry → required vanishing order` **反推** `Ψ` ✓✓

D0: 本档对象 = **`M3-α` 障碍对本候选族的致命性判定（`C380-L3-0`）**（引既有 `C-347／C-348／C-373`；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算（解析）／零数值／零 Lean／未写程序** ✓

---

## §1 符号 Fourier 框架（**代数分类** ✓✓）

```
【解析】 `T_n` 的奇偶性与 `n` 一致 ⟹ `T_{2r}` 偶、`T_{2r+1}` 奇 ✓
　· 偶频：`F_{2r} = Σ_j T_{2r}(t_j) = Σ_j T_{2r}(a_j)` ⟹ **符号不变**（与 `ε` 无关）✓（与 `C380-L2-1` 的 `Φ` 固定 `{a_j²}` 一致 ✓）
　· 奇频：`F_{2r+1} = Σ_j ε_j T_{2r+1}(a_j)` ⟹ **`ε` 的一次型** ⟹ 符号 Fourier 展开中 **仅 `|S| = 1`** ✓✓
【⭐ 候选类的奇部（对上一轮令 ⑥ 的答复）】
　| 候选 | 奇次 Fourier 部 | 可否作 sign-odd numerator |
　|:--|:--|:--|
　| `F_{2r+1}` | ✓（`|S| = 1`） | **可** ✓ |
　| `F_{2r+1}^2` | ✗ **无奇部**（偶次二次） | ⛔ **不可**（**代数上即出局**）✓ |
　| `F_{2r+1}F_{2s+1}` | ✗ **无奇部**（`|S| ≤ 2` ⟹ 奇部为零） | ⛔ **不可** ✓✓ |
　| `F_{2r+1}F_{2s+1}F_{2t+1}` | ✓（`|S| = 3`） | 可（但**更强**、更难控）✓ |
　⟹ ⭐ **平方与两两乘积被纯代数一次性排除**（它们**根本不携带奇部**）⟹ 上一轮令 ⑥ 的三类中**两类出局** ✓✓
【条件性结构注记 ⚠️】 在**配对型纤维**（`{a,a,b,b,0}`）上，一次型退化为两个符号和：
　`F_{2r+1} = T_{2r+1}(a)(ε_1+ε_2) + T_{2r+1}(b)(ε_3+ε_4)` ⟹ 奇频数据**非单射**（只依赖两个和）✓
　⚠️ 但**该注记的前提在 `E_even` 内不成立**（见 §2／§3）✓
```

## §2 `M3-α` 零集（**消去证明** ✓）

```
【配对点】 `t = (a, −a, b, −b, 0)` ⟹ 对**一切奇** `n`：
　`F_n = T_n(a) + T_n(−a) + T_n(b) + T_n(−b) + T_n(0) = T_n(a) − T_n(a) + T_n(b) − T_n(b) + 0 = 0` ✓✓
　⟹ **逐对消去 ⟹ 全部奇频为零**（＝ `M3-α` 的零集，**本档复核成立** ✓）
【推论】 任何**无常数项的奇频多项式**（`|S| = 1,3,5` 型）在该点**恒为零** ⟹ 若该点**可行**，则整个"符号奇量"类
　在 `δ=0` 处取零值，与 gap 型 `D = 0` 叠加 ⟹ **`0/0`** ⟹ **正是唐先生预警的伪突破形态** ✓✓
```

## §3 ⭐ **决策树执行：非恒零 ⟹ 继续**（**关键理由来自档案** ✓✓）

```
【决定性事实（档级，逐字）】 `C-348`：「**`C-347` 的障碍不在 `E` 内 ✓✓：反称配对（全部奇频 = 0 ✓）
　被偶频约束自动排除 ✓✓ ⟹ 奇频下界路线未被[杀死]**」＋ `C-373`：**`Z ∩ E = ∅`** ✓✓✓
　⟹ ⭐ **`M3-α` 的见证点（反称配对）根本不在 `E_even` 内** ⟹ 对本命题（要求 `X,Y ∈ E_even`）**不构成障碍** ✓✓
【判定】 候选族在 `E_even` 上**非恒零** ⟹ 依判定树 **非恒零 ⟹ 继续** ⟹ **可进入 L3-1 构造** ✓✓
【⚠️ 本档自纠（必记）】 `C380-L2-1` 把 `M3-α` 列为"障碍① 已闭"**过于悲观** ✗ —— 该障碍**只在离开 `E_even` 时才生效**；
　本档据 `C-348`／`C-373` 修正为：**`M3-α` 不阻断 `E_even` 内的奇频路线** ✓✓
```

## §4 ⭐ `counterexample geometry → required vanishing order`（依令 ⑥ **反推**，不预设 ✓✓）

```
【真正的几何（档级）】 障碍的**真实形态** ＝ **反称族 ＋ 偶频约束收取的"非对称代价"** ——
　`C-348`／`C-349` 引用逐字：**反称族 `\not\subset E`（成本 ≈ 1.14）** ✓✓
　⟹ 即：要离开反称族（使奇频非零），偶频约束**要求付出 ≈ 1.14 的代价** ⟹ 该代价即**近碰撞/离开反称的自然尺度** ✓
【⭐ 由几何反推的 `Ψ` 形态（本档结论）】
　`\Psi` 应取 **沿"离开反称族"方向的一阶奇量**（= 归一化后奇频在 `δ → 0` 的**极限斜率**），
　其极限值 **≠ 0**（因奇频对 `ε` 是**一次型**且系数 `T_{2r+1}(a_j)` 一般非零 ✓）✓✓
　⛔ **而非**"奇频多项式 ÷ 最近间隔"这一**先验选定的**组合（依令 ⑥ 禁止预设 ✓）
【denominator 的正当性条件（照录令 ③）】 须先证：`D(a,−a,b,−b,0) ≠ 0`，**或**若 `D = 0` 则须给出**与 `N` 同阶**的
　显式消没阶证书，**并且**比值有 `O(δ²)` 延拓（否则只是 `0/0`）✓✓
```

## §5 四关 ＋ 下一步（`L3-1`）

```
【四关（依令 ⑤）】 ① **有限**（`\Psi` 有限）② **非恒定**（在纤维上取不同值）③ **近碰撞稳定**（`δ→0` 不爆）④ **二阶稳定**（`O(δ²)` 或等价证书）✓
　**只有四关全 PASS 才登记为 KERNEL-HUNT witness** ✓
【L3-1 待做（下一刀）】 取 `N := F_{2r+1}`（一次型 ⇒ 天然符号奇 ✓）；由 §4 反推的 `D` 必须**显式**满足 §4 的正当性条件；
　逐关验证四关；若第 ④ 关失败 ⟹ 记录为**失败形态**（仍是有效负产出 ✓）
```

## §6 边界与回查（✗✓）

```
✗ 零计算（解析）／零数值／未写程序／未做 generator 文献搜索／**未开案** ✓
✗ ⛔ 不预设 denominator；⛔ 不把"形式上绕开 `Φ`"当成已绕开零集；⛔ 不把"存在核"当"存在新机制" ✓
⚠️ §1 的条件性坍缩注记**前提在 `E_even` 内不成立**（已明标）；§3 的修正为**自纠**（须保留 ✓）
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 符号Fourier展开 命中文件数=0 ⟹ **本档新增** ✓
　技术词 消没阶         命中文件数=0 ⟹ **本档新增** ✓
　技术词 一阶奇量       命中文件数=0 ⟹ **本档新增** ✓
　技术词 反例几何       命中文件数=0 ⟹ **本档新增** ✓
　技术词 坍缩           命中文件数=108 ⟹ **档案已有（异义）⟹ 不主张** ✓
【档级引用】 `C-347`（奇偶层分离＋低阶奇多项式表）／**`C-348`**（反称族 `\not\subset E`；成本 ≈ 1.14；奇频下界路线未被杀死）／
　`C-349`（抵消理想）／`C-350`／**`C-373`**（`Z ∩ E = ∅`）／`M3-α(G)` 封闭 ✓
