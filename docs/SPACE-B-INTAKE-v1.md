已查地图：命中（`RESEARCH-CONSTITUTION` AMEND-26 能力索引制 ＋ AMEND-27 双空间解耦 ＋ AMEND-28 倒置顺序／E0／E4-0..4）
D0: 本档对象 = **空间 B 候选入口表 v1**（**机制优先**产生候选，再过 Closure Gate v1）
D1: 0（流程＋候选登记型）
[REVIEW]

# **空间 B 候选入口表 v1**

## §1 强制流程（`AMEND-28 §4` 倒置顺序）

```
$$\text{机制}\to\text{该机制最自然攻击的对象类}\to\text{候选族}\to\boxed{E0＋E4\text{-}0..4}\to\text{真正未解决者}\to\text{才谈计算}$$
$$\text{登记字段}:\ \text{机制}\mid\text{对象类}\mid\text{候选问题（可判真假）}\mid\text{交付物类}(K1..K5)\mid\text{指纹}\mid E4\text{-}0..4\ \text{证据}\mid\text{闸判定}$$
$$\text{判定}:\ \boxed{ADMIT\ \text{才可开算}};\quad HOLD=\text{证据不足};\quad BLOCKED=\text{定理族／语义命中}$$
$$\textbf{禁止}:\ \text{以"数据库 }Open\ \text{表"为起点选课题}\ (\text{F-4 病根});\ \text{起点必须是机制}$$
```

## §2 首批种子（机制优先，8 项）

| # | 机制 | 对象类 | 候选问题（可判真假） | 交付物 | 闸判定（v1） |
|---|---|---|---|---|---|
| S1 | M9 精确有限搜索＋证书 | 二元非线性覆盖码 | $K(10,1)$ 能否 $\le 119$？或改进下界 $107$？ | K1 | HOLD |
| S2 | M12 整除性／p-adic 障碍 | **一般**差集（非 skew） | Turyn 型指数界能否批量判定 LJCR 的 abelian $p$-群 `Open` 格？ | K4 | HOLD |
| S3 | M5 单位根消去 | 循环称重矩阵 $CW(n,k)$ | 用单位根／整除障碍给出新的不存在证书？ | K1/K4 | HOLD |
| S4 | M3 商／修补／闭包 | 覆盖码／差集构造 | 由已知记录（如 120 码）做**结构化修补**达新记录？ | K1 | HOLD |
| S5 | M7 凸包／配分函数 | 有限极值量的 LP/SDP 界 | 用**精确对偶证书**改进某有限极值量的界？ | K1/K2 | HOLD |
| S6 | M2 全局证书 | 计数／饱和度型界 | 为仅有启发式证据的界构造**可独立复核**的全局证书？ | K2 | HOLD |
| S7 | M10 形式化 | 有限分类／枚举 | 用 Lean 形式化分类以获机器验证结论（TLDC 实实例）？ | K3 | HOLD |
| S8 | M11 机械同构审计 | 外部宣称成果 | 对 2026 外部成果做**同构／去包装审计**？ | K5 | HOLD |

```
$$\textbf{全项 }HOLD\ \text{是正确读数}:\ \text{它们尚未提交 }E4\text{-}0..4\ \text{证据};\ \text{按新制度\textbf{不得}开算} ✓$$
$$\textbf{下一步（择一或多项）}:\ \text{对选中项补齐 }E4\text{-}0..4\ \text{证据表}\to\text{闸判定}\to ADMIT\ \text{才推进}$$
```

## §3 与旧做法的区别（自查）

```
$$\text{旧}:\ \text{从 DB }Open\ \text{挑"漂亮小格"}\to\text{开算}\to E4\ \text{才发现已有定理}\ (\text{F-4 反复发生})$$
$$\text{新}:\ \text{从\textbf{我们的机制}出发}\to\text{对象类}\to\text{候选族}\to\boxed{\text{先闭环，再计算}};\ \text{且 }E4\text{-}0\ \text{先锁语义}$$
$$\text{本次 }S2\ \text{即\textbf{语义更正后的正确续做}}:\ \text{数据库问的是\textbf{一般差集}} \Longrightarrow \text{须用 Turyn 型界（非 CXS）} ✓$$
```
