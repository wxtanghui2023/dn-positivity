已查地图（所查：`C-126`（**"log-free 排除＝零例"——本档判定过强，须限定为"渐近"**）、`V188` §2（聚合（线性）通道饱和；反演到逐点需无界精度）、`V259`（**`\lim F_n` 有限局部聚合 ⟹ 判死**）、`V211`（**有限→无限缺陷审计**：八类机制全落已封类；`RH` 是 `\Pi_1`）、`V150` W2、`C-116`（**登记不否决**）、`V193` §⑤、`F5`）。**结论**：**(1) 合并**——`C-126` 的"尺度墙"与 `V188` §2 的聚合／逐点二分**是同一堵墙**，合并编号为 **"聚合-逐点墙"** ✓✓；**(2) ⚠️ 修正 `C-126`**："log-free 排除＝零例"**过强** —— log-free 排除**存在**，但**只在有限范围**（**验证法**：`N(T)` 为**整数** ＋ 定位符号变化 ⟹ 有限范围内"无离线零点"是 **log-free** 的，且 `\beta=\tfrac12` **精确**）✓✓；**渐近（对全部零点）的 log-free 排除才是零例** ✓✓；**(3) ⭐ 破法已被找到，且其极限障碍已精确定位**：**计数＋整数性 ⟹ 排除**这一机制**只在计数可被 `<1` 界住时有效**（即**有限**）；**渐近时** `N(\sigma,T)\to\infty` ⟹ **整数性失效** ⟹ **必须付精度尺度** ⟹ **这就是墙的机制** ✓✓✓；**(4)** ⟹ 该墙与 `V259`（`\lim F_n` 有限局部聚合判死）及 `V211`（**有限→无限缺陷**）**同族**，共名 **"有限→无限通道"**；**唯一破法＝在极限中存活的一致整数性／有限性界** ✓✓

# C-127 · **合并（聚合-逐点墙）＋ 破法定位（整数性只在有限有效）**

> **时间**：2026-09-18 18:34 唐先生：**「继续」** ⟹ 合并编号（`V188` 聚合／逐点 与 `C-126` 尺度墙）＋ 找唯一可能的破法（"密度→排除的非间距尺度转换"）✓

---

## §0 结论（先行）

$$\textbf{(1)}\ \textbf{合并}：\text{`C-126`}\ \text{的"尺度墙"}\ \equiv\ \text{`V188` §2}\ \text{的}\ \textbf{聚合／逐点二分} \Longrightarrow \text{合并编号}\ \boxed{\textbf{聚合-逐点墙}}✓✓$$
$$\textbf{(2)}\ ⚠️\ \textbf{修正 `C-126`}：\text{"log-free 排除＝零例"}\ \textbf{过强}✓✓$$
$$\qquad \text{log-free 排除}\ \textbf{存在}，\ \text{但}\ \textbf{只在有限范围}：\text{验证法}（N(T)\ \text{为}\ \textbf{整数} ＋ \text{定位符号变化}）✓$$
$$\qquad \qquad \Longrightarrow \text{有限范围内"无离线零点"是}\ \textbf{log-free}\ \text{的，且}\ \beta=\tfrac12\ \textbf{精确}✓✓$$
$$\qquad \textbf{正确表述}：\boxed{\text{仅}\ \textbf{渐近}（\text{对全部零点}）\text{的 log-free 排除为零例}}✓✓$$
$$\textbf{(3)}\ ⭐\ \textbf{破法已找到，其极限障碍已定位}：\text{机制}＝\textbf{计数＋整数性}\Longrightarrow\textbf{排除}✓$$
$$\qquad \text{该机制}\ \textbf{只在计数可被}\ <1\ \textbf{界住时有效}（\text{即}\ \textbf{有限}）✓✓$$
$$\qquad \qquad \textbf{渐近时}\ N(\sigma,T)\to\infty \Longrightarrow \textbf{整数性失效} \Longrightarrow \textbf{必须付精度尺度}✓✓✓$$
$$\qquad \qquad \qquad \Longrightarrow \textbf{这就是墙的机制}✓$$
$$\textbf{(4)}\ \Longrightarrow \text{与}\ \text{`V259`}（\textbf{有限局部聚合} \text{判死}）\ \text{与}\ \text{`V211`}（\textbf{有限}\to\textbf{无限缺陷}）\ \textbf{同族}：\text{共名}\ \boxed{\textbf{有限}\to\textbf{无限通道}}✓✓$$
$$\qquad \textbf{唯一破法}＝\boxed{\text{在极限中存活的}\ \textbf{一致整数性／有限性界}}✓✓$$

---

## §1 合并：聚合-逐点墙（两条等价表述）

$$\textbf{表述 I（信息层，}\text{`V188` §2}）：\text{线性（聚合）通道}\ \textbf{饱和};\ \textbf{反演到逐点位置需无界精度}✓$$
$$\textbf{表述 II（解析层，}\text{`C-125`／`C-126`}）：\text{任何}\ \textbf{逐点（排除性）} \text{结论，其阈值必含间距尺度}\ \frac{2\pi}{\log T}\ \text{型退化}✓$$
$$\qquad \text{（等价性）}：\text{计数}\leftrightarrow\text{线性统计量（饱和，}\textbf{尺度无关}）;\quad \text{排除}\leftrightarrow\text{位置反演（}\textbf{须付精度尺度}）✓✓$$
$$\Longrightarrow \boxed{\text{聚合-逐点墙}\ \text{有两条等价表述，今后}\ \textbf{合并引用，不再分别开案}}✓✓$$

## §2 ⭐ 修正 `C-126`：有限范围 vs 渐近

$$\textbf{反例（本档给出，}\textbf{推翻了 `C-126` 的"零例"表述}）：\text{验证法}✓$$
$$\qquad \text{①}\ N(T)\ \text{由}\ \text{RvM／argument principle}\ \text{算得，为}\ \textbf{整数}✓$$
$$\qquad \text{②}\ \text{由}\ Z(t)\ \text{的}\ \textbf{符号变化} \text{定位}\ N(T)\ \text{个零点}（\text{严格区间算术}）✓$$
$$\qquad \text{③}\ \text{若符号变化数}\ ＝\ N(T)\ \text{（整数相等）} \Longrightarrow \textbf{该范围内全部零点在临界线上}✓✓$$
$$\qquad \Longrightarrow \text{例}：\textbf{无离线零点于}\ |\gamma|\le3\times10^{12}（\text{Platt--Trudgian}）\ \text{是}\ \textbf{log-free 排除}✓✓$$
$$\qquad \qquad ⚠️\ \text{但它是}\ \textbf{有限范围}（\text{参数}\ T_0）;\ \text{对}\ \textbf{全部零点} \text{的表述仍是}\ \textbf{零例}✓✓$$
$$\Longrightarrow \boxed{\text{log-free 排除}\ \textbf{存在}\ \text{但}\ \textbf{只在有限范围};\ \textbf{渐近} \text{版本为零例}}✓✓$$

## §3 ⭐ 机制：整数性排除只在有限有效

$$\textbf{机制}：\text{若某区域内的对象数}\ \textbf{是整数} \text{且}\ \textbf{可被}\ <1\ \textbf{界住} \Longrightarrow \text{该数为}\ 0 \Longrightarrow \textbf{排除}✓✓$$
$$\qquad \text{（此为}\ \textbf{唯一已知} \text{的"计数}\Rightarrow\text{排除"通道，}\ \textbf{且}\ \textbf{不付精度尺度}）✓$$
$$\textbf{渐近时的失效}：\text{对全部零点，}\ N(\sigma,T)\to\infty\（\text{如}\ \ll T^{A(1-\sigma)}）✓$$
$$\qquad \Longrightarrow \textbf{整数性不再给出}\ <1\ \text{的界} \Longrightarrow \text{界只给}\ \textbf{计数}（\text{聚合}）\ \text{而非}\ \textbf{排除}✓✓$$
$$\qquad \Longrightarrow \text{要}\ \textbf{排除} \text{则须}\ \textbf{定位}（\text{符号变化／零点分辨}） \Longrightarrow \textbf{付间距尺度}\ \frac{2\pi}{\log T}✓✓$$
$$\Longrightarrow \boxed{\text{墙的机制}＝\textbf{"整数性排除在有限有效；渐近时计数}\to\infty\Longrightarrow\text{转付精度"}}✓✓✓$$

## §4 唯一破法：极限中存活的一致整数性／有限性界

$$\boxed{\text{破法形式}：\exists\ \text{一个}\ \textbf{一致有限性界}，使}\ \forall T:\ \#\{\text{区域内对象}\}\le C<1✓$$
$$\qquad \text{即：}\text{把"}\textbf{有限} \text{"从参数}\ T_0\ \text{升级为}\ \textbf{一致界}（\text{与}\ T\ \text{无关}）✓✓$$
$$\qquad ⚠️\ \text{而}\ \text{`C-126` §2}：\text{这}\ \textbf{恰好等价于} \text{固定}\ \delta\ \text{的排除};\ \text{故}\ \textbf{破法}\equiv\text{第一级靶}✓✓$$
$$\Longrightarrow \text{与}\ \text{`V259`／`V211`}\ \textbf{同族}：\text{共名}\ \boxed{\textbf{有限}\to\textbf{无限通道}}✓✓$$
$$\qquad \text{`V259`}：\text{可写成}\ \lim F_n\（\text{有限局部聚合}）\Longrightarrow\text{判死}✓$$
$$\qquad \text{`V211`}：\text{八类"有限层正常／无限层异常"机制}\ \textbf{全落已封类};\ \text{残余}\ \textbf{未实例化}✓$$
$$\qquad \Longrightarrow \text{本墙}\ \textbf{＝}\ \text{`V211` 的}\ \textbf{解析面};\ \text{破法}\ \textbf{＝}\ \text{`V211` 残余的}\ \textbf{一个具体候选形状}✓✓$$

## §5 边界与回查

- ⚠️ **本档修正 `C-126` 的"零例"表述**（限定为"渐近"）；`C-126` 的**其余结论**（计数 log-free、聚合逐点形态）不变 ✓
- ⚠️ **不否决**（`C-116`）：本档**不**声称一致有限性界不存在 ✓
- ⚠️ §2 的验证法细节（`N(T)` 整数 ＋ 符号变化 ＋ Platt–Trudgian `3\times10^{12}`）为**公开事实**，标 `[经典·本档未逐字核文献]` ✓
- **不声称**：破法可行 ✗；不证 RH ✗
- **纪律**：先查后判（R-1 ✓，**先跑后写** ✓）；**未用 RH 作推导** ✓

## §6 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-18 18:3x）`[纪律]`（先跑后写）

```
技术词 聚合-逐点墙          命中文件数=1  :: ./C127-…（本档）
技术词 整数性排除          命中文件数=1  :: ./C127-…（本档）
技术词 有限到无限通道         命中文件数=1  :: ./C127-…（本档）
```
**读数（按实测）**：三项均＝**1 档（仅本档）⟹ 本档新增措辞** ✓

```
⚠️ 唐先生 18:34「继续」⟹ 合并编号(V188 聚合/逐点 与 C-126 尺度墙) + 找唯一破法(密度→排除的非间距尺度转换)
⭐ (1) 合并: C-126 的"尺度墙" ≡ V188 §2 的聚合/逐点二分 ⟹ 合并编号 **"聚合-逐点墙"**
   表述 I(信息层): 线性(聚合)通道**饱和**; 反演到逐点位置需无界精度
   表述 II(解析层): 任何逐点(排除性)结论, 其阈值必含间距尺度 2π/logT 型退化
   等价: 计数 ↔ 线性统计量(饱和, 尺度无关); 排除 ↔ 位置反演(须付精度尺度) ⟹ 今后合并引用不再分别开案
⚠️ (2) **修正 C-126**: "log-free 排除=零例" **过强** —— log-free 排除**存在**, 但**只在有限范围**:
   验证法: ① N(T) 由 RvM/argument principle 算得, 为**整数**; ② 由 Z(t) 符号变化定位 N(T) 个零点(严格区间算术);
   ③ 若符号变化数 = N(T)(整数相等) ⟹ 该范围内全部零点在临界线上 ⟹ 例: 无离线零点于 |γ| ≤ 3×10^12(Platt–Trudgian) 是 **log-free 排除**
   ⚠️ 但它是**有限范围**(参数 T0); 对**全部零点**的表述仍是**零例**
   ⟹ 正确表述: **仅渐近(对全部零点)的 log-free 排除为零例**
⭐ (3) **破法已找到, 其极限障碍已定位**: 机制 = **计数+整数性 ⟹ 排除**(唯一已知的不付精度尺度的"计数⇒排除"通道);
   该机制**只在计数可被 <1 界住时有效**(即**有限**); **渐近时 N(σ,T) → ∞** ⟹ 整数性不再给 <1 的界 ⟹ 界只给**计数(聚合)** 而非**排除** ⟹ 要排除则须**定位**(符号变化/零点分辨) ⟹ **付间距尺度**
   ⟹ **墙的机制 = "整数性排除在有限有效; 渐近时计数→∞ ⟹ 转付精度"**
⭐ (4) 唯一破法 = **在极限中存活的一致整数性/有限性界**: ∃ 一致有限性界使 ∀T: #{区域内对象} ≤ C < 1
   即把"有限"从参数 T0 升级为**一致界**(与 T 无关); 而 C-126 §2: 这**恰好等价于**固定 δ 的排除 ⟹ **破法 ≡ 第一级靶**
   ⟹ 与 **V259**(可写成 lim F_n 有限局部聚合 ⟹ 判死) 与 **V211**(有限→无限缺陷审计: 八类机制全落已封类, 残余未实例化) **同族**
   ⟹ 共名 **"有限→无限通道"**; 本墙 = V211 的**解析面**; 破法 = V211 残余的**一个具体候选形状**
⚠️ 修正性质: 本档修正 C-126 的"零例"表述(限定为"渐近"); C-126 其余结论不变; 不否决(C-116); §2 验证法细节为公开事实[经典·待核]; 不证 RH
✅ 净产出: ①合并"聚合-逐点墙"(两条等价表述) ②修正 C-126(有限 vs 渐近; 验证法作为 log-free 排除实例) ③破法定位(计数+整数性, 只在有限有效; 渐近时 N→∞ 转付精度) ④与 V259/V211 同族(有限→无限通道); 破法 ≡ 第一级靶
```
