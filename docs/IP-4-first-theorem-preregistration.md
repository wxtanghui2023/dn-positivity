已查地图：本档属**独立问题线**（`IP-4`／Card 3），不涉 RH/ZF 地图条目 ⟹ 按唐先生 2026-09-23 15:11 令执行 ✓
D0: 本档对象 = Card 3 的 FIRST THEOREM 预注册（`n_0=-3`，证书存在命题 ＋ `A` 锁死）
D1: 0 （`[REVIEW]` 轮次：预注册，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`IP-4`：`FIRST THEOREM` 预注册（Card 3；`n_0=-3`）**（⛔ 零计算；`RUN` 待 `GO` ✓）

**唐先生裁定（15:11 ✓✓）**
```
【`R1` 锁定 ✓✓】 $$\boxed{n_0=\arg\min_{n\in\{-3,3,5,8,20\}}|n|=-3}$$ —— 理由**不是"−3 看起来更有希望"**，而是 **`R1` 是真正与数学结构无关的确定性选择规则** ✓
　⛔ **不选 `R2`**：它带入"已知结构最多"的人为偏好，会形成 $$\text{已有结构较多}\to\text{更易设计证明}\to\text{首轮成功率较高}$$ ⟹ 又开始**优化"看起来有希望"**（与刚禁掉的逻辑冲突）✓
　⛔ **不选 `R3`**：若无额外、客观、**事前可复现**的选择函数，本质即**人为指定** ✓
【⭐ `FIRST THEOREM` 收紧（照录 ✓✓）】 既然取**正方向**，**不能把"有界搜索"本身写成定理** ✓；正确逻辑：
　$$\boxed{\text{若在事前固定的有限搜索域内找到候选四元组}\Rightarrow\text{给出 exact certificate}\Rightarrow D(-3)\text{-quadruple exists}}$$；
　搜索域耗尽 ⟹ $$\boxed{\text{NOT FOUND}}$$ ⛔ **而不是** $$D(-3)\text{-quadruple does not exist}$$ ✓✓
　⟹ 故 `FIRST THEOREM` 须写成**证书存在命题**，而非"搜索成功/失败二选一" ✓
【⭐ `A` 的事前规则（照录 ✓✓）】 ⛔ 不得事后看**计算能力**决定 `A`（否则重新引入 post-hoc 优化）；最干净的规则＝$$\boxed{A=\text{当前文献已明确允许我们处理的固定有限界}}$$，并**在 `RUN` 前把具体数值锁死** ✓
【当前状态（照录 ✓✓）】 $$\boxed{R1\ \text{LOCKED}\to n_0=-3\to A\ \text{待事前确定}\to FIRST\ THEOREM\to RUN}$$；**现在仍然不计算** ✓✓
```

---

## §1 **`FIRST THEOREM`（预注册，精确陈述 ✓）**

```
$$\boxed{\exists\,1\le a<b<c<d\le A:\quad ab-3,\ ac-3,\ ad-3,\ bc-3,\ bd-3,\ cd-3\ \text{全为整数平方}}$$ ✓
【`A` 见 §3（事前锁死）】✓
【语义限制（照录 ✓✓）】 ⛔ 本命题**只**断言"存在证书"；**不**断言"不存在"；⛔ 不得写成搜索成功/失败二择一 ✓
```

## §2 **证书格式（命中时的精确验证 ✓✓）**

```
【证书】 四元组 `(a,b,c,d)` ＋ **六个精确平方恒等式**：
　$$ab-3=x_1^2,\ ac-3=x_2^2,\ ad-3=x_3^2,\ bc-3=x_4^2,\ bd-3=x_5^2,\ cd-3=x_6^2$$ ✓
【验证要求】 **全部用整数精确验证**（⛔ 不用浮点）✓；命中即得 $$\boxed{D(-3)\text{-quadruple exists}}$$ ✓（**直接解答 `Problem 5.1` 在该 `n_0` 处的存在性** ✓✓）
```

## §3 **`A` 的事前选择规则（本档新增，待您确认 ✓✓）**

```
【⭐ 措辞修正（唐先生 2026-09-23 15:12 ✓✓；**必须改**，否则 `RUN` 审计语义不够严）】 `D(1)` 的 `200` **不能**自然迁移为 `D(-3)` 的界（文献把 `D(-3)` 作为**独立未解决整数问题**处理，如 `Filipin–Jurasić` 2022 *On the existence of `D(-3)`-quadruples over `\mathbb Z`*；`Dujella` 亦记「no Diophantine quadruple with property `D(-3)` is known」）⟹ $$\boxed{\text{D(1) 的 }200\text{ 是先例，不是 }D(-3)\text{ 的数学界}}$$ ✓ ⟹ 须改写为：$$\boxed{A:=200\ \text{作为\textbf{预注册的、与结果无关的\textbf{计算截断}}}}$$（**预注册固定实验截断**，⛔ **不得称为「文献已明确允许我们处理的固定有限界」**，**不赋予其数学上的普适意义**）✓✓
【原（已废）表述】 取**类比情形 `D(1)`** 在文献中已有的**穷尽搜索界**作为先例 ⟹
　$$\boxed{A=200}$$ ✓（文献记载：`D(1)` 除 `\{1,3,8,120\}` 外**无最大项 `\le200` 的四元组** ✓；此界**与本问题的结构无关**，且**非由计算能力决定** ✓✓）
【⛔ 锁死要求】 `A=200` 须在 `RUN` 前**写死**；⛔ 不得因计算能力、耗时或中途发现而调整 ✗ ✓
【备选规则（若您不采纳先例规则）】 取"**事前固定的整数上界**使搜索可**完全穷尽**"，但仍须**先锁死** ✗ 不得事后优化 ✓
【⚠️ 诚实标注】 走正方向 **不保证命中**；若 `d\le A` 内无候选 ⟹ 记录 $$\boxed{\text{certificate not found for }d\le A}$$（**该失败不是结果**，只是搜索耗尽）✓
```

## §4 **`STOP`（预声明，继承 `IP-3` ＋ 新增 ✓）**

```
① 命中项落在**已知计算范围**内（重复验证）✗｜② 在中途**放宽**结构性条件（如 `a<b<c<d` 严格递增、六对全为平方）✗｜
③ ⛔ **不得**因计算能力**调整 `A`** ✗｜④ 需**假设 RH 或任何 RH 等价物** ✗｜⑤ 走 `(-)` 而机制与既有 `\bmod4/8` 结果重合 ✗ ✓
【结果语义（照录 ✓✓）】 命中 ⟹ **存在性定理（含证书）** ✓；耗尽 ⟹ **`NOT FOUND`（不得升级为不存在性）** ✓✓
```

## §5 **状态 ＋ 边界**

```
$$\boxed{R1\ \text{LOCKED};\ n_0=-3;\ A=200\ \text{（待确认）};\ FIRST\ THEOREM\ \text{已定稿};\ \text{COMPUTATION}=\text{LOCKED（待 }GO\text{）}}$$ ✓
【边界】 ✗ 零计算／⛔ 未碰 Card 2/4／⛔ 未连 ZF／⛔ 未连 RH／⛔ 未按计算能力定 `A`／⚠️ 文献项按不可信标注 ✓
```
