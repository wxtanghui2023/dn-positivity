已查地图：命中（`CONVENTION-AUDIT-result`／`EDR-CAP-1-run-invalid-and-Bn-equals-1-structural-point`）⟹ 引用，不开新案
D0: 本档对象 = EDS 线**输入数据基础错误**：`P=(5,5)` 为 **5-挠点** ⟹ EDS 论证无效；并给出修正动作
D1: 0 （[REVIEW] 轮次：根因判定，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **根因：`P=(5,5)` 是 5-挠点 ⟹ EDS 输入无效**

## §1 实算（本机，逐步）

```
`E:y^2+y=x^3-x^2-10x-20`，`P=(5,5)`（已在曲线上：`25+5=30=125-25-50-20` ✓）
`2P=(16,-61)` ✓（校验：`y^2+y=3721-61=3660=x^3-x^2-10x-20` ✓）
`3P=(16,60)` ✓（同 x；且 `60=-1-(-61)` ⟹ `3P=-2P`）✓✓
$$\Longrightarrow\ 5P=3P+2P=O\ \ (\text{因 }y_1+y_2+1=60-61+1=0)$$ ✓✓✓
```

## §2 ⟹ 判定（本档核心）

```
**【判定】** $$\boxed{P=(5,5)\ \text{是 }E\text{ 的 5-挠点（阶 5）}\ \Longrightarrow\ \text{不满足 EDS 前提（须无限阶）}}$$ ✓✓
【⟹ 这解释了全部异常】 **(i)** 我的 `B_1..B_4=1`：`nP` 对 `n\le4` 都是整点 ✓；**(ii)** 第 5 步出现"`Q=-P` 型归一 → `O`" ⟹ 循环终止 ✓；**(iii)** 所引档案表 `2,13,604,6153` **不可能来自该 `P`** ⟹ 该表来源进一步被否定 ✓✓
【⟹ 对 EDS 线的后果（严重）】 本线 `EDR`／`ZF-EDR` 各轮所用的"`E_{-11},P=(5,5)`"**不是合法 EDS 输入** ⟹ 那些轮的 `Z_S`／`r_q`／`|Z_S|\le N_0+|S|` 讨论**在前提上失效**（`11a1` 的挠群正是 `\mathbb Z/5`，`(5,5)` 是其生成元）✓✓
```

## §3 修正动作（不猜测）

```
**【修 1】** EDS 线须**改为非挠点**（`11a1` 上任取非挠生成元），并在脚本里**先断言阶数**（`nP=O` 的 `n` 不存在于计算范围内）✓
**【修 2】** `B_n` 约定**钉死**为 `x(nP)=A_n/B_n^2` 既约分母 ✓；**审计项**：`m\mid n\Rightarrow B_m\mid B_n`、`\gcd(B_m,B_n)=B_{\gcd(m,n)}` ✓
**【修 3】** 原表 `1,2,13,604,6153` **从 CAP 输入中永久撤除**（来源不可核）✓
**【修 4】** 只有完成 修 1–3 并通过对拍，才重跑 `CAP-1`（判据 `\exists S:|T(S)|>|S|`，`T` 只用 `B_n>1` 指标）✓
【边界】 §1 为**本机实算**（可复跑）；§2 判定与 §3 修正为**本档自行推导**；未制造候选／未启动搜索／未碰 RH／未碰 abc。
