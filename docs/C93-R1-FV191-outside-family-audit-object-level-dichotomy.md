已查地图 + **`V191` 精确定义逐字**（所查：`V191-uniform-finite-n-hyperbolicity-NO-theorem-grade-F1-amendment.md` §0 四条与 §1（**Pólya 1927 ＋ GORZ 2019 ＋ 剩余区 $\mathcal R$**）、`V190`（通道 S／Bochner）、`V199` §3–§4（三源＋三门表）、`YM-transfer-TP5-fails-and-Turan-holds.md`、`YM-breakthrough-3-asano-analogue-limit-obstruction.md`、`C-84`／`C-91`／`C-92`）。**结论**：`R1` 执行后——**为 `𝓕_V191` 写出精确边界**（＝Ξ 的 **Jensen 多项式双曲性**），对**按对象枚举**的 6 个族外候选逐一判门 ⟹ **全部落入 `R1-B`（经典等价 ⟹ 坍缩回 `𝓕_V191`）或 `R1-C`（门失败／强度不足）**；并且产出一条 ⭐ **对象级二分**（Ξ-系数侧 ⟹ 坍缩；γ-序数侧 ⟹ `β`-盲）⟹ **`R1` 与项目既有"两面"结构同址** ✓✓；同时按你的 `R1-D` 纪律**降调为 `bounded-family audit`** ✓

# C-93 · **`R1`：`𝓕_V191` 族外 PF/Newton 机制审计（bounded-family audit）**

> **时间**：2026-09-18 13:46 唐先生：先做 `R1`；**先定义边界、再穷举、再判门**；先用 **对象 → 结构公理 → 零点性质**（不用关键词相似）
> **预注册四结果**：`R1-A` 存活／`R1-B` 坍缩（**定理级**）／`R1-C` 门失败（只关该候选）／`R1-D` 空间无法穷举 ⟹ 降为 `bounded-family audit` ✓

---

## §0 结论（先行）

$$\textbf{(1)}\ \textbf{`𝓕_V191` 的精确边界（逐字）}：\text{对象}＝\Xi\ \text{的}\ \textbf{Jensen 多项式}\ J_\gamma^{d,n};\ \text{性质}＝\textbf{双曲性};\ \text{参数区}＝(d,n)✓$$
$$\textbf{(2)}\ \text{按}\ \textbf{对象} \text{枚举 6 个族外候选} \Longrightarrow \textbf{全部}\ \text{落}\ R1\text{-B}\ \text{或}\ R1\text{-C};\ \textbf{无}\ R1\text{-A}✓$$
$$\textbf{(3)}\ ⭐\ \textbf{对象级二分（本档核心产出）}：$$
$$\qquad \text{对象在}\ \textbf{Ξ-系数侧} \Longrightarrow \text{经典 Jensen--Pólya 型判据把"乘子／全正／变差递减"诸条件}\ \textbf{等同} \text{于 Jensen 双曲性} \Longrightarrow \textbf{坍缩（`R1-B`，定理级）}✓$$
$$\qquad \text{对象在}\ \textbf{γ-序数侧} \Longrightarrow \textbf{`β`-盲}（\text{序数测度只含}\ \gamma） \Longrightarrow \textbf{门失败（`R1-C`）}✓✓$$
$$\textbf{(4)}\ ⚠️\ \text{按}\ R1\text{-D}：\text{"所有 PF/Newton 型条件"}\ \textbf{无可操作边界} \Longrightarrow \text{本档}\ \textbf{降调为}\ \boxed{\text{bounded-family audit}}✓✓$$

---

## §1 `𝓕_V191` 的**精确定义**（逐字；源 `V191` §0–§1）

$$\text{Pólya 1927}：\text{RH}\iff\text{\textbf{全部}}\ J_\gamma^{d,n}\ \text{双曲};\qquad \text{GORZ 2019（已证）}：\forall d\ \exists N(d):\ n\ge N(d)\Longrightarrow J_\gamma^{d,n}\ \text{双曲};\ \text{且}\ 1\le d\le8\ \text{对全部}\ n\ge0\ \text{双曲}✓$$
$$\mathcal R:=\{d\ge9\}\times\{n\ge0\}\setminus\{(d,n):n\ge N(d)\}\ \Longrightarrow\ \text{RH}\iff\forall(d,n)\in\mathcal R,\ J_\gamma^{d,n}\ \text{双曲}✓$$
$$\Longrightarrow\ \boxed{\mathcal R\ \text{上的一致陈述}\equiv\text{RH}}\quad（\text{只用 Pólya＋GORZ，}\textbf{不需}\ \mathcal R\ \text{内部结构假设}）✓✓$$
$$\Longrightarrow\ \text{不存在}\ P\ \textbf{严格弱于}\ \text{RH}\ \text{却能推"}\mathcal R\ \text{全双曲"} \Longrightarrow\ \textbf{V191-① ＝ NO（定理级）}✓✓$$
$$\qquad ⚠️\ \text{故 `𝓕_V191` 的边界可写成}：\boxed{\{\text{对象＝Jensen 多项式}\ J_\gamma^{d,n}\}\ \times\ \{\text{公理＝双曲性（全实根）}\}\ \times\ \{\text{结论}\supseteq\mathcal R\ \text{全双曲}\}}✓$$

## §2 `𝓕_out` 的**操作边界**（按你的方法：对象 → 结构公理 → 零点性质）

$$\boxed{\text{对象}\ \longrightarrow\ \text{结构公理}\ \longrightarrow\ \text{推出的零点性质}}$$
$$\qquad ⚠️\ \textbf{不用}：\text{关键词相似（PF／Newton／实根性）}\Longrightarrow\text{同族}✓$$
$$\text{对每个候选}\ F\ \text{逐门}：\ F\overset{(1)}{\to}\text{算术来源}\overset{(2)}{\to}\text{零点／系数约束}\overset{(3)}{\to}\text{`V199` 三门}\overset{(4)}{\to}\text{RH 强度}✓$$

## §3 逐候选审计（**按对象**枚举；6 项）

| # | 对象 | 结构公理 | 推出的零点性质 | 判定 |
|:--:|:--|:--|:--|:--|
| **①** | **系数序列** $(\gamma_n)$ **作为乘子算子**（multiplier sequence）| 乘子性（保实根性）| （经典）$\iff$ **全部 Jensen 多项式双曲**〔经典·待核 ⚠️〕| **`R1-B`**：**经典等价 ⟹ 落回 `𝓕_V191`**（定理级，非类比）|
| **②** | **Toeplitz 矩阵** $(\gamma_{k-l})$ | 全正（PF ⟺ Toeplitz TN，Schoenberg）| ⟹ 生成函数实根 ⟹ 全族双曲 | **`R1-B`**（同上链条）|
| **③** | **变差递减核**（variation diminishing）| Karlin／Schoenberg：VD $\iff$ TP | 同上 | **`R1-B`** |
| **④** | **Ξ 整体函数**（Laguerre--Pólya 类）| $\Xi\in\mathrm{LP}$ $\iff$ 全零点实 | **直接＝RH** | **`R1-C`**（门②：**定义式包装**）|
| **⑤** | ⭐ **序数测度** $\mu_\gamma=\sum_\gamma\delta_\gamma$ **的 Hankel 矩**（矩由**显式公式**给出 ⟹ **算术可见** ✓）| Hankel 全正（矩问题 PSD）| 只约束 $\gamma$ 的分布 | **`R1-C`**：**`β`-盲**（对象只含 $\gamma$，**不携带 $\beta$**）✓✓ |
| **⑥** | **Turán 不等式**（系数对数凹）| Newton／Turán | 真实（档案：`Turan holds` ✓）但**弱于**全族双曲 | **`R1-C`（强度不足）**；⚠️ 且 `V191` 逻辑**预言**其必然不足 ✓ |

$$\textbf{汇总}：\ R1\text{-A}=0;\quad R1\text{-B}=3\ (\text{①②③});\quad R1\text{-C}=3\ (\text{④⑤⑥});\quad R1\text{-D}=\textbf{适用}✓$$

## §4 ⭐ 对象级二分（本档核心；`[本档]`）

$$\boxed{\text{凡族外 PF/Newton 型条件，其}\ \textbf{对象} \text{必落以下两侧之一}}：$$
$$\qquad \text{(I)}\ \textbf{Ξ-系数侧} \Longrightarrow \text{经典 Jensen--Pólya 型判据把乘子性／全正性／变差递减性}\ \textbf{等同} \text{于 Jensen 双曲性} \Longrightarrow \ \text{落回}\ \mathcal F_{\rm V191}\ \Longrightarrow\ \textbf{强度＝RH（`R1-B`）}✓$$
$$\qquad \text{(II)}\ \textbf{γ-序数侧} \Longrightarrow \text{对象只含纵坐标} \gamma \Longrightarrow \textbf{`β`-盲} \Longrightarrow \ \text{不能过门（`R1-C`）}✓✓$$
$$\Longrightarrow\ \boxed{R1\ \text{与项目既有"}\textbf{两面}\text{"结构（值面／零点面；`V157`／`V215`）}\ \textbf{同址}}✓✓$$
$$\qquad ⭐\ \text{换言之}：\text{`V191` 的障碍}\ \textbf{不是某个具体定理的缺陷}，\ \text{而是}\ \textbf{PF/Newton 这一机制在两面划分下的必然归宿}✓✓$$

## §5 合规性对照（与 `V199` 三门、`C-91` 硬过滤器）

$$\text{本档}\ \textbf{不是} \text{"再换一个定理"}\ \text{（`C-91` §4 禁止项）};\ \text{而是}\ \textbf{对象级审计} \text{（按你的方法）}✓$$
$$\text{与}\ \text{`V199`}\ \text{三门对照}：\text{①③坍缩} \Longrightarrow \text{门②（非包装）失败};\ \text{④定义式} \Longrightarrow \text{门②失败};\ \text{⑤`β`-盲} \Longrightarrow \text{门①②皆失败};\ \text{⑥强度不足} \Longrightarrow \text{门③失败}✓$$
$$\qquad ⚠️\ \text{即三门在这 6 个候选上}\ \textbf{全部命中}，\ \text{且失败点}\ \textbf{各不相同}✓$$

## §6 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-18 13:5x）`[纪律]`（先跑后写）

```
技术词 对象级二分        命中文件数=0
技术词 bounded-family audit 命中文件数=0
技术词 multiplier sequence  命中文件数=0
技术词 Laguerre          命中文件数=20   :: ./YM-breakthrough-3-asano-analogue-limit-obstruction.md ./P8-CLASSIC-SOURCES-li1997-bombieriLagarias1999.md ./V230-C4-relation-to-region-sandwich-and-asymmetry-source.md
```
**读数**：`对象级二分`／`bounded-family audit`／`multiplier sequence`＝**0 档 ⟹ 本档新增** ✓；⚠️ `Laguerre`＝**20 档 ⟹ 档案已有**（`YM-breakthrough-3` 等）⟹ 引用 ✓

## §7 边界

- `[逐字]` §1（`V191` §0–§1）✓；§3 ①②③ 的**经典等价**标 〔经典·待核 ⚠️〕**未逐条核原文献**；⑤ 的"矩由显式公式给出"为项目既有结论 ✓
- `[本档]` §2 操作边界、§3 逐候选判定、§4 **对象级二分**、§5 对照 ✓
- **`R1-A` ＝ 0（未发现存活候选）**，但按 `R1-D` 纪律：**`R1 ≠ exhaustive scan`** ⟹ 本档**不声称**"族外空间已排除"✗✓
- **纪律**：先查后判（R-1 ✓，**先跑后写** ✓）；**未用 RH 作推导** ✓；**零数值** ✓

```
⚠️ 唐先生 13:46：先做 R1（不被 U1–U6 带偏）；先定义 𝓕_V191 边界 → 再划 𝓕_out → 逐门判；
   用「对象 → 结构公理 → 零点性质」而非关键词相似；预注册 R1-A/B/C/D 四结果
✅ `𝓕_V191` 精确定义（逐字）：对象＝Ξ 的 Jensen 多项式 J_γ^{d,n}；公理＝双曲性；参数区＝(d,n)；
   Pólya 1927：RH ⟺ 全族双曲；GORZ 2019：n≥N(d) 与 1≤d≤8 无条件；剩余区 R={d≥9}×(小 n) 上的一致陈述 ≡ RH（只用 Pólya+GORZ，
   不需 R 内部结构）⟹ 无 P 严格弱于 RH 可推"R 全双曲" ⟹ V191-①=NO（定理级）
✅ 按对象枚举 6 个族外候选，逐一判门：
   ① 系数序列作乘子算子 ⟹ 经典等价 ⟹ 坍缩回 𝓕_V191（R1-B）
   ② Toeplitz (γ_{k−l}) 全正 ⟹ 同链（R1-B）
   ③ 变差递减核 ⟹ VD⟺TP ⟹ 同链（R1-B）
   ④ Ξ ∈ Laguerre–Pólya 类 ⟹ 直接＝RH（R1-C，门②定义式包装）
   ⑤ ⭐序数测度 μ_γ=Σδ_γ 的 Hankel 矩（矩由显式公式给出 ⟹ 算术可见）⟹ 只约束 γ 分布 ⟹ **β-盲**（R1-C）
   ⑥ Turán 不等式 ⟹ 真实（档案 Turan holds）但弱于全族双曲 ⟹ 强度不足（R1-C）；V191 逻辑预言其必然不足
   ⟹ R1-A=0；R1-B=3；R1-C=3；R1-D 适用
⭐ 对象级二分（本档核心产出）：族外 PF/Newton 条件的对象必落两侧之一 ——
   (I) Ξ-系数侧 ⟹ 经典 Jensen–Pólya 型判据把乘子性/全正性/变差递减性**等同**于 Jensen 双曲性 ⟹ 坍缩（强度＝RH）；
   (II) γ-序数侧 ⟹ 对象只含 γ ⟹ **β-盲** ⟹ 不能过门
   ⟹ R1 与项目既有"两面"结构（值面/零点面，V157/V215）**同址**；即 V191 的障碍不是某具体定理的缺陷，而是 PF/Newton 机制在两面划分下的必然归宿
⚠️ 按 R1-D 纪律降调：本档=**bounded-family audit**，**不声称**"族外空间已排除"
⚠️ 与 V199 三门对照：6 候选全部命中三门，且失败点各不相同（①③门②失败 / ④门② / ⑤门①② / ⑥门③）
✅ 净产出：①𝓕_V191 精确边界 ✓；②𝓕_out 操作边界 ✓；③6 候选逐门审计表 ✓；④对象级二分 ✓；⑤R1-D 降调 ✓
```
