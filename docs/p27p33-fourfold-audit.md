# P27-P33 四重审计（P-A/P-B/P-C/P-D——）——变分对象重估

> 2026-09-07 14:10 · 唐先生指令：回到 P27-P33——反循环审计
> 目标：r(n)=O(1)⟺RH 是否真正"中间层等价"——还是重包装？——变分结构是否避开 β 墙？

## 审计对象的核心结构

### r(n)（文献定位确认——）
r(n) = λ_n^Li − ½n log n − cn = **n·y_n**
- c = ½(γ−log2π−1) = −1.130331
- **Arias de Reyna (2011)：RH ⟺ (y_m) ∈ ℓ²**——已建立
- **我们的 r(n)=O(1) ⟹ y_n = O(1/n) ⟹ ℓ² ⟹ RH**——O(1) 是 ℓ² 的**强充分条件**（更强——数值新——n≤3000）
- ⚠️ 但"O(1) 无条件证明 = RH"（文档明确——）——等价难度

### 变分定理（8/31——无条件——）
- 在线零点虚部 {γ_k} 唯一最小化 S_proj(a) = Σ1/(a²+γ²)²——所有合法配置
- D = ΣP_γ(δ) = 2[S_proj(γ_ρ) − S_proj(γ_k)] ≥ 0

### P_γ（逐轨道——）
P_γ(δ) = δ²M₂/(2U²D₊D₋)——δ² 阶——零集 δ=0

## 四重审计判定

### P-A：独立可计算？
- r(n)：从 ξ 的无条件矩 m_j（ξ 的 Taylor——）——**免零点**——但需 ξ（解析——非纯整数/素数）
- S_proj：需零点虚部（谱侧 γ——）
- **判定：⚠️ 部分**（r(n) 免零点——变分对象在谱侧——）

### P-B：RH-free 正性？
- 变分定理：无条件（任意 ξ——）——RH-free ✓
- P_γ ≥ 0：逐轨道——无条件 ✓
- **判定：✅（正性确为 RH-free——）**

### P-C：离轴攻击（核心——）
- P27-G8：离轴 quartet ⟹ n₋(K_N) = N（有限负惯性——）
- **⚠️ 但 K_N 是"离轴缺陷核"——用 δ（离轴量——）作为输入构造**——"离轴 ⟹ 负"是定义的直接结果（非独立破坏——）
- **真正测试**：F_N 不用 δ 构造（独立——）——离轴配置使 F_N 变负——= Weil 正性（P28 缺口——）——未建立
- P28-P33：即使有独立核——负方向堆积谱边缘（λ→0⁻）——不形成均匀负扇区
- P26-A16 反向变分：循环（合法配置含离轴——）
- **判定：❌ P-C 循环确认**（G8 核含 δ——与 P26-A16 一致——）+ moving-edge 阻断（P28——）

### P-D：阈值精确？
- P_γ：逐轨道 δ² 阶——零集 δ=0 ✓
- 全局（F ≥ 0 ⟺ sup Re ρ ≤ ½——）：未建立
- **判定：⚠️ 逐轨道 ✓——全局 ✗**

## 综合判定（唐先生四情形——）
**情形 III（P-C 循环——）**：
1. r(n)=O(1) 不是"重包装"（是 ℓ² 的强充分条件——更强——）——但也**不是独立变分原理**（O(1) 证明 = RH 本身）
2. 变分结构（S_proj 最小——P_γ ≥ 0——）是**真的 RH-free 正性**——但形式是"谱侧/逐轨道"
3. **关键缺口（P-C——）双因**：
   a. **G8 的核含 δ 输入**——"离轴⟹负"是定义结果（循环——）——独立核（Weil——）未建立
   b. P28-P33：即使独立核——负方向不聚成全局矛盾（moving-edge——）
   c. P26-A16 反向变分：循环（合法配置含离轴——）

## 结论
**P27-P33 不能作为 RH 证明链**（P-C 失败——情形 III——）——但：
- 变分定理 + P_γ ≥ 0 是**真实的 RH-free 正性资产**（保留——）
- r(n)=O(1) 是**比 ℓ² 强的真命题**（数值新——但证明 = RH——）
- **与 β 墙关系**：变分结构没有避开 β 墙——它把 β 墙精确定位到"传输层"（P28-P33——）——即"逐点负方向存在但不聚成全局矛盾"

## 一句话
P27-P33 审计结论 = 唐先生情形 III：**有漂亮的正性（P_γ ≥ 0——变分——）但 P-C 循环确认（G8 核含 δ 输入——独立核未建立——）+ moving-edge 阻断传输（P28——）——不能作为 RH 证明链**——归档为"变分正性资产"而非"证明链"。
