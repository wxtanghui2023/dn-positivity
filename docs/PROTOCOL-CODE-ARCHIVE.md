# 📜 **制度：验证代码必须存档、可复现**（唐先生指令，2026-09-11 23:04）

> 原文：「**我不希望你一再使用临时代码，所有验证代码必须存档，可复现**」
> 本文件为**硬制度**，此后所有验证工作必须遵守。违反视为任务失败。

---

## 一、**六条硬规则**

```
【R1】⭐ **一切验证计算必须是【已提交的脚本】** —— 禁止用内联 heredoc（`python3 - << 'PY'`）做计算 ✗
      允许 heredoc 的**唯一情形**：非计算性的文件编辑（如向 memory/*.md 追加一段文本 ✓）
【R2】**脚本必须读 `data/`**（或经 `scripts/setup_data.sh` 恢复的 /tmp 兼容路径 ✓）
      **禁止**把结果**写**到 /tmp ✗（/tmp 容器重启即清 ⟹ 不可复现 ✗）
【R3】**输出必须落盘并提交**：`scripts/<脚本名>.txt` ✓（或 `results/` ✓）
【R4】**每个脚本须有头部 docstring**，含：目的 / 输入（路径+来源）/ 输出（路径）/ 结论 / provenance ✓
【R5】**提交前必须通过检查器**：`python3 scripts/check_archive.py` ✓（返回非 0 即禁止提交 ✗）
【R6】**命名规范**：`<线>-<用途>.py`（如 `BL7_S4_bothsigns_check.py` ✓）；
      与结论对应的文档写进 `docs/`，并互相引用 ✓
```

## 二、**为什么（本日教训）**
```
【教训 1】**P27–P33 等旧结论的脚本【从未以文件提交】** ✗（内联执行 + /tmp 输出 ⟹ 现已不可复跑 ✗）
   —— 这是**存档清点**时发现的最大技术债 ✓（本轮修复中 ✓）
【教训 2】**102 个脚本依赖 /tmp** ✗ —— 容器重启 ⟹ 静默失效 ✗✓（本轮已建 `setup_data.sh` + 数据入仓 ✓）
【教训 3】**零点表在 /tmp** ✗（16 MB；容器重启即失 ⟹ **全部数值工作失效** ✗✓）
   —— 现已复制到 **`data/zeros_odlyzko_2M.npy`** ✓ 并入仓 ✓✓
【教训 4】本轮的三次自查（丢 Abel 边界项 ✗ / 单侧双侧混淆 ✗ / near 计数错 ✗）
   —— **若代码未存档，这些错误将无法被发现** ✓✓（存档 = 可复核 = 自查的前提 ✓）
```

## 三、**已建立的基础设施**
```
【数据入仓 ✓】`data/`：zeros_odlyzko_2M.npy（16 MB ✓）｜zeros_odlyzko_100k.npy｜zeros_2000.npy
   ｜dn_results.json｜trackB_vals.npy｜ec_classdata_3000.json｜lmfdb_*.txt ✓
【引导脚本 ✓】`scripts/setup_data.sh` —— 容器重启后运行一次即可恢复旧脚本所需的 /tmp 路径 ✓
【检查器 ✓】`scripts/check_archive.py` —— 见下 ✓
```

## 三·B、**R7（本轮新增 ✓）：脚本的结论段必须由数字驱动**
```
【教训】BL10 初版的 READ-OFF **在运行前就写好了成功结论** ✗ —— 与实际数字矛盾 ✗✓
【规则】**脚本末尾的结论必须：(a) 由程序内计算出的判据驱动（如"违反计数 = 0" ✓）；
   (b) 明确写出"若判据为 X 则结论为 Y，否则必须复查，不得声称成功"** ✓✓
   —— **禁止**在脚本中预置成功/失败的断言 ✗
```

## 四、**检查器覆盖的违规类型**
```
【V1】scripts/ 中存在**未被 git 跟踪**的 .py ✗
【V2】脚本中出现**写 /tmp** 的模式（`open('/tmp...','w')` / `save('/tmp...')` / `to_csv('/tmp...')` 等）✗
【V3】脚本**缺少头部 docstring** ✗
【V4】scripts/ 中存在**未跟踪的 .txt 输出** ✗
```
