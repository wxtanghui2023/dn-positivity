已查地图：未覆盖（关键词: prime-native|Euler交互项|准入漏斗|β探针|算术导数）—— 可开档，首行须照抄本行
　⚠️ 但**候选级**实质命中（**必须引用，不得开新案** ✓）：**素数竞赛线** = `P0-RESEARCH-GENERATOR-CHANGE`（`P1-`α 调和加权素数竞赛）｜
　`P1-alpha-prime-race-numeric-prototype`（逐字「**finite blind ＋ limit visible（已跑）**」✓）｜`P3-B`（「⭐ **现存唯一已证**的'第三来源'实例：**素数竞赛机制**」）｜
　`P3-B2`（「把素数竞赛机制从 `L(s,\chi)` **搬到 ζ 自身**」＋ **exponent crux**）⟹ **该线已存在** ✓✓

# **KERNEL-HUNT-3**：Prime-Native Interface Search（**第一轮：候选普查 ＋ P1–P5 准入审计**）

**唐先生令（2026-09-22 22:37）**：
① ✅ 选 **(a)**，但**收紧成准入漏斗**（**不再做宽泛生成器**）✓；⭐ 转折已发生：
　$$\boxed{\text{生成器探索}\ \to\ \text{前沿测绘}\ \to\ \text{反向规定候选必须具备什么}}$$ ✓✓
　⛔ `KERNEL-HUNT-2` 的六个 DEAD **不应再扩展成第 7／8／9 个"熟悉对象"**（否则重回 `C-34` 循环）✓；
② **准入条件（照录）**：
　$$Q\in\mathcal Q_{\rm admit}\iff
　\begin{cases}\text{(P1) 有独立的 prime/Euler 侧定义}\ [\textbf{硬门}]\\ \text{(P2) 不是由 RH 等价式定义出来}\\
　\text{(P3) 同时具有至少一种可证明的组合律}\\ \text{(P4) 尚未被档案既有机器吸收}\\
　\text{(P5) 能产生新的 }β\text{-sensitive observable/interface}\end{cases}$$ ✓✓
　⛔ **没有 prime/Euler 侧定义的候选，不再进入深入审计** ✓
③ ⭐ **Rule T（照录）**：**先问**「这个 quantity 在 prime/Euler 世界里**本来是什么**？」，**再问**「它**怎样进入 ζ／zero side**？」
　—— **而不是**反过来从零点构造一个 quantity，再寻找它的"算术解释" ✓✓
④ **三类优先**：**A 素数上的局部—全局组合量**（`{p, ν_p(n), a(p), a(p^k)}` 原生；`Q(mn) = Q(m)·Q(n)` 或加性分解；**须查是否为 Dirichlet 卷积／Euler 积的已知重述**）｜
　**B Euler 因子的二阶／交互项**（`\mathcal E(p,q)`、`\mathcal E(p^a,q^b)`；**prime 侧原生**＋独立素数上组合律＋**不等同普通 Euler product**＋可证变换后产生 β-sensitive 量）｜
　**C β 的探针（非 β 的函数）**：`Q_{\rm prime} \to \mathcal T(Q) \to` 依赖 β 的 observable，**RH 只出现在最后的性质判定**，不在 `Q` 的定义里 ✓✓
⑤ **链（照录）**：$$\boxed{C380\text{-L3-2 CLOSED} \to \text{O1-1 CLOSED} \to \text{KERNEL-HUNT-2 CLOSED} \to \text{KERNEL-HUNT-3 / Prime-Native Interface Search}}$$ ✓
⑥ **本轮限定**：**只做候选普查 ＋ P1–P5 准入审计，不做长证明**；⛔ **P1 不过立即 DEAD**；**P1–P5 全过才允许进入机制构造** ✓✓

D0: 本档对象 = **prime-native 候选的 P1–P5 准入审计（KH-3 第一轮）**（档案**零覆盖** ⟹ 新档；但**候选级已引既有素数竞赛线** ✓；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算（解析）／零数值／未写程序**／未做数值搜索／未开案 ✓

---

## §1 漏斗（形式化 ✓）

```
【P1 硬门】 候选必须**在 prime/Euler 世界有原生定义**（不得由 `zero data`／`degeneracy` 反造 ✓）
【P2】 定义**不得**是 RH 等价式（`RH \iff …`）✓
【P3】 至少一种**可证明**的组合律（加性或乘性或派生律）✓
【P4】 **未被档案既有机器吸收**（须**逐项回查** ✓）
【P5】 能产生**新的** β-sensitive observable／interface ✓
【Rule T 顺序】 prime-native 定义 → （可证变换）→ zero/β 侧 observable；**禁止反向** ✓✗
```

## §2 候选普查 ＋ P1–P5 审计（**第一轮** ✓）

| # | 候选 | P1 prime-native | P2 非 RH 等价 | P3 组合律 | P4 未吸收 | P5 β-探针 | 判定 |
|:--|:--|:--:|:--:|:--:|:--:|:--:|:--|
| **A1** | **算术导数 `n'`**（Leibniz 律：`(ab)' = a'b + ab'`，`p' = 1`） | ✅ | ✅ | ✅ **Leibniz（可证 ✓✓）** | ✅ 档案 0 覆盖 ✓ | ⚠️ **未证** | **GAP（P5 未证）** |
| **A2** | **`\Omega(n)`／`\omega(n)`**（素因子计数） | ✅ | ✅ | ✅ **精确加性 ✓✓** | ✅ | ⛔ **FAIL：纯零盲** | **DEAD** |
| **A3** | **Selberg 类不变量**（次数、导子） | ✅ | ✅ | ✅ | ⚠️ | ⛔ **FAIL：导子零盲** | **DEAD** |
| **A4** | **Mertens／Chebyshev 型缺陷**（`\prod_{p\le X}(1-1/p)^{-1}` vs `\log X`） | ✅ | ⚠️ | ✅ | ⛔ **档案 `Mertens` 56 档 ⟹ 已吸收** | ⚠️（已知陈述多为 RH 等价 ⟹ 命中等价级） | **DEAD（P4＋等价级）** |
| **B1** | **跨素数交互项 `\mathcal E(p,q)`**（Euler 因子间的**交叉／缺陷／非线性余项**） | ✅✅ | ✅ | ✅ **独立素数集上的组合律 ✓** | ✅ 档案 0 覆盖 ✓ | ⚠️ **未证**（自然形式多为截断 Euler 积缺陷 ⟹ 恐落显式公式墙） | ⭐ **GAP-HOLD（唯一未死类，待 P5 审计）** |
| **B2** | **Hecke 关系"缺陷"** | ✅ | ✅ | ✅ | — | ⛔ **FAIL：定义式 ⟹ 无缺陷** | **DEAD** |
| **B3** | **二阶局部数据 `(a_p, a_{p^2})` 关系** | ✅ | ✅ | ✅ | — | ⛔ **FAIL：定义式 ＋ ζ 侧第一断裂（`α_p ≡ 1`）⟹ 空** | **DEAD** |
| **C1** | ⭐ **素数竞赛／Chebyshev bias**（调和加权 ＋ 低阶零点共振） | ✅✅ | ✅ | ✅ | ⛔⛔ **FAIL：档案已有整条线** ✓ | ✅✓（**finite blind ＋ limit visible** 逐字 ✓） | **DEAD-已存 ⟹ 引用，不开新案** ✓ |
| **C2** | 显式公式型探针 | ✅ | ⚠️ | — | ⛔ **FAIL：`T7`／Weil 路线已闭** | — | **DEAD** |

## §3 ⭐⭐ 第一轮的关键发现（**不是"没有候选"，而是"最强者已在档"** ✓✓）

```
【C1 的档案原文（逐字，**必须引用** ✓✓）】
　· `P3-B` §5：「`S1 = ② FALSE`（三分性由定义塞入；退化为已知判据）」「`S2 = ② FALSE`（精确恒等式 ⟹ 零缺陷 ⟹ 无选择）」＋
　　$$\boxed{③\text{-B 整体} = ③\ \mathrm{UNRESOLVED}\ (\text{第三来源未造出}，\text{规格已锐化为}①②③\text{三条})}$$ ✓
　· `P3-B` §6（**③-B-2 的目标形式，已登记**）：须同时满足 **(a) 状态空间非紧致；(b) 缺陷算术生成（非误差定义）；(c) 缺陷局部不可见**；
　　**参照模板 ＝ `P1-`α 的"调和权重 ＋ 低阶零点共振"**；**应用对象 ＝ ζ 自身（不是 `L(s,\chi)`）** ✓✓
　· `P3-B2` §4：「**`③-B-2 = ② FALSE`（搬运被"临界指数来源"精确阻断）**」＋
　　「**诚实**：这不是'我们没做出来'，而是**障碍被定位到一个单点：指数 `1/2` 的合法来源**」✓✓✓
　· `P3-B2` §5（**新规格**，照录）：要么 `1/2` 来自归一化（**禁止**）／对称（**已封闭**）／显式公式（**已知判据**），要么：
　　$$\boxed{\text{指数}\ \tfrac12\ \text{必须}\ \textbf{动态地}\ \text{产生——作为某个}\ \textbf{自洽方程／流的临界指数}}$$ ✓✓✓
　　（关键区别：临界指数作为**不动点线性化本征值**出现时，它是**导出的**，不是插入的 ✓）
　· `P3-B2` §5（**P2 的三条硬要求**，照录）：**(i) 流必须是算术的**（整数性／乘法结构）；**(ii) 不动点必须被强制**（不能假设）；
　　**(iii) 临界指数必须等同于 `β_{\max} − 1/2` 而不靠任何显式公式** ✓✓✓
【⭐ 对本漏斗的意义】 C1 虽 **P4 FAIL**（不得开新案 ✓），但**档案已经把"合格对象"的规格写出来了** ⟹
　$$\boxed{\text{本次漏斗的真正出口} = \text{审计"能否展示一个满足 }P2\text{ 三条硬要求的算术流"}}$$ ✓✓
　（＝**纵向换坐标**：不是再找熟悉对象，而是在既有模板上攻**单点卡点**（指数 `1/2` 的**合法来源**）✓✓）
```

## §4 判定与下一刀

```
【第一轮判定】 漏斗**有效**（`P1` 硬门 ＋ `P4` 回查一次性杀掉 A2／A3／A4／B2／B3／C2 ✓✓）；**幸存两处**：
　**(甲) B1 跨素数交互项**：P1–P4 ✓，**唯 `P5` 未证** ⟹ **GAP-HOLD**，须先审"其自然形式是否即截断 Euler 积缺陷 ⟹ 落显式公式墙"✓
　**(乙) 既有 `P2` 规格**（C1 线给出）：**已登记的目标形式** ＋ **三条硬要求** ⟹ **最高价值入口** ✓✓
【下一刀（建议，二选一，均不长证明）】
　**(甲)** B1 的 **P5 审计**：能否构造 `\mathcal E(p,q)`，其**可证变换**给出 β-探针而**不经显式公式** ✓
　**(乙)** **P2 可行性审计**：枚举"算术流"的候选（整数性／乘法结构）＋ 检查**不动点能否被强制**（不能假设）✓✓
【⛔ 禁项】 不再开第 7／8／9 个熟悉对象 ✓；不做长证明 ✓；P1 不过立即 DEAD ✓
```

## §5 边界与回查（✗✓）

```
✗ 零计算（解析）／零数值／未写程序／未做数值搜索／**未开案** ✓
✗ ⛔ 不把"六 DEAD"记为战绩 ✓；⛔ 不把 C1 当新案（**档案已有 ⟹ 引用** ✓）；⛔ 守 `V290` 禁语 ✓
⚠️ A1 的"算术导数"为**标准对象**（凭记忆 ⚠️，未逐字核档）；B1 的 P5 标**未证**（不得写成"不可能" ✓）
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 准入漏斗 命中文件数=0 ⟹ **本档新增** ✓｜prime-native=0 ⟹ **新增** ✓｜β探针=0 ⟹ **新增** ✓
　技术词 交互缺陷项=0 ⟹ **新增** ✓｜算术导数=0 ⟹ **新增** ✓
【地图回查】 关键词 5 项 = **未覆盖** ⟹ 可开档 ✓；**候选级**：`契比雪夫` 0／`算术导数` 0／`Selberg类` 0／`Omega函数` 0（⟹ P4 ✓）／
　`Mertens` **56**（⟹ A4 已吸收 ✓）／`素数竞赛` **5**（⟹ C1 已有专线 ✓✓）
【档级引用】 `P0-RESEARCH-GENERATOR-CHANGE`（`P1-`α）／`P1-alpha-prime-race-numeric-prototype`／**`P3-B`**／**`P3-B2`** ✓｜
　`V262`（紧致 ⟹ 无障碍；障碍须非紧致）✓｜`T7`（显式公式/Weil 路线已闭）✓｜`C-34`（攻缺口＝攻两墙）✓
