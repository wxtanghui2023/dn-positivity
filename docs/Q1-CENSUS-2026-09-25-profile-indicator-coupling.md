已查地图：已跑 scripts/prework_map_check.sh K(10,1) Q=1 census profile indicator 耦合 ⟹ 执行自 SECOND-ORDER-2026-09-25 与 GRAM-LIFT-2026-09-25 两档；本档为**纯推导＋两处勘误**（唐先生 23:36 指令）；不开新研究方向。
D0: 本档对象 = `Q=1` 分支的 (δ, 1_C) 六类 census 与消元（既有对象；非新对象）
D1: 0（无新独立自由度；产出为两处勘误、census 求解、结构性引理）

# Q1-CENSUS-2026-09-25 · Q=1 的 profile–indicator 耦合

## §1 ⚠️ 两处勘误（先更正，再推进）

### 勘误 ①：唐先生 §4 的"矛盾"是**记号混用**产生的假象 ✗

```
你的 2A₁ = "283 - r₀ + 2ε"，其中 r₀ = #(δ=1 且 ∉C)；
你的 2A₂ = "286 - r - 2ε"，其中 r  = #(δ=1 且 ∈C)。
⟹ 两式相加得 "569 - 2r" ✗ —— 因为 2A₁ 用的是 r₀、2A₂ 用的是 r，且 r + r₀ = 283。
正确（统一用 r:=#(δ=1 且 ∈C)=N_{11}, ε=N_{21}）：
$$\boxed{2A_1=r+2\varepsilon},\qquad \boxed{2A_2=286-r-2\varepsilon}\quad\Longrightarrow\quad 2(A_1+A_2)=286\ \checkmark$$
与 `2(A₁+A₂)=285+Q=286` **恒等一致** ⟹ **无矛盾** ✓（不是"归属/重复计数"问题 ✗，恒等式本身无冲突 ✓）
```

### 勘误 ②：我前一档的 κ-表是**错的** ✗（`SECOND-ORDER-2026-09-25` §4）

```
我写的 "星形 κ=4 / 中心+两邻居 κ=3 / 链形 κ=1" ✗ **全错**
正确事实：**两个不同闭球交 ≤ 2 点**（|N[c]∩N[c']|=2 当 d≤2，否则 0 ✓）
⟹ 若某三元组有两个不同公共中心 y₁≠y₂，则 |N[y₁]∩N[y₂]| ≥ 3 ✗ 不可能
$$\boxed{\kappa(\text{三元组})\in\{0,1\}\quad\text{且}\quad \kappa=1\iff\text{三元组被同一闭球覆盖}}$$
$$\Longrightarrow\ \boxed{T=\sum_x\binom{b(x)}{3}=\#\{\text{共覆盖三元组}\}}\quad(\text{每个共覆盖三元组只有唯一公共中心 ✓})$$
```

⟹ 结论：三阶**没有 κ-核结构** ✗（我前一档的"第三组对偶约束"实际不存在 ✓）——但 `T=Q+ΣC(δ,3)` 本身仍有效 ✓（纯计数 ✓）

## §2 Q=1 的六类 census（唐先生 §6）

```
$$N_{ab}:=\#\{x:\delta(x)=a,\ \mathbf1_C(x)=b\},\quad a\in\{0,1,2\},\ b\in\{0,1\}$$
profile 给出: N_{20}+N_{21}=1,  N_{10}+N_{11}=283,  N_{00}+N_{01}=740
码字总数:     N_{01}+N_{11}+N_{21}=119   （=M ✓）
```

**解**（本档）：自由参数取 `N_{10}` 与 `N_{21}∈{0,1}` ✓

```
N_{11} = 283 - N_{10}
N_{01} = N_{10} - 164 - N_{21}   ≥ 0  ⟹  **N_{10} ≥ 164 + N_{21}** ✓
N_{00} = 904 - N_{10} + N_{21}
N_{20} = 1 - N_{21}
```

## §3 三条恒等式翻译（消元后**不产生矛盾**，但出结构）

```
$$2A_1=N_{11}+2N_{21},\qquad 2A_2=3N_{20}+N_{21}+N_{10},\qquad T=1$$
一致性: 2(A₁+A₂) = 283 + 3(N_{20}+N_{21}) = **286** ✓ 恒成立（无约束 ✗）
奇偶性: 2A₁ 为偶数 ⟹ N_{11}+2N_{21} 偶 ⟹ **N_{11} 偶** ⟹ **N_{10} 奇** ✓（由 283 奇 ✓）
```

## §4 ⭐ 结构性引理（本档主产）

```
引理（Q=1 ⟹ 诱导子图 ≤ 2-正则）: 因 profile 最大 δ=2，而 x∈C 时 δ(x)=d_C(x) ✓
$$\Longrightarrow\ Q_{10}[C]\ \text{最大度}\le2\ \Longrightarrow\ \boxed{Q_{10}[C]=\text{路径} \sqcup \text{偶环}}$$
（偶环：$Q_{10}$ 二部 ⟹ 无奇环 ✓）
推论: N_{11}=2·(#路径数) ✓（路径两端点 ✓）⟹ N_{11} 偶 ✓（与 §3 一致 ✓）
      度-2 顶点数 N_{21} = (环上顶点) + (路径内点) ✓
结论: Q=1 分支被压缩为「119 个点生成 ≤2-正则诱导子图 + 唯一 b=3 中心 + census 约束」✓
```

## §5 诚实结论：**尚未获得矛盾** ✗

```
· census 全域可行（N_{10} ∈ [164+N_{21}, 283]、N_{10} 奇 ✓）⟹ **Q=1 未被排除** ✗
· 本题的"profile–indicator coupling"目前只给出**弱引理**（§4 ✓），没给出矛盾 ✓
· ⚠️ **逻辑提醒**：即使排除 Q=1，也**不能**直接排除 119 ✗ —— Q≥3 只给 `A₁+A₂ ≥ 144`，
  仍需**独立上界**（`A₁+A₂ ≤ 143`）才能闭合 ✓（"只需排除 Q=1"不成立 ✗）
```

## §6 下一步（按结构而非按计数）

```
① 用 §4 的"路径∪偶环"结构做**局部分类**（每条路径端点/内点的覆盖率约束 ✓）
② 唯一 b=3 中心的两种几何（x₀∈C 或 ∉C ✓）与 census 相容性细查 ✓
③ 追踪**唯一共覆盖三元组**（T=1 ✓）在各分支下的位置约束 ✓
④ 若仍不闭合 ⟹ 回到"上界"（难点 C ✓）：需 van Wee 原文核对是否已含二阶矩机制 ✗
```

## §7 边界（诚实标注）

- 勘误①为**记号核对**（我方独立重推 ✓）；勘误②为**我方前一档错误**，已在原档追加勘误节 ✓
- census 为**代数求解**（非程序 ✓）；引理 §4 为纯推导（未程序验证 ✗）
- **未**排除 Q=1 ✗、**未**排除 119 ✗；本轮**未跑程序** ✓
- 我方本档内**自己抓到一次错误**（曾误用"x∉C 时 |C∩N(x)|=δ(x)"✗，正确为 δ(x)+1 ✓）——未写入结论 ✓

## 【技术词回查】（定稿前逐字输出）

```
技术词 profile-indicator 耦合 命中文件数=0    :: 
技术词 六类 census    命中文件数=0    :: 
技术词 诱导子图     命中文件数=0    :: 
技术词 共覆盖三元组 命中文件数=0    ::
```

- **本档新增**（命中数=0）：`profile-indicator 耦合`、`六类 census`、`诱导子图`、`共覆盖三元组`
- **档案已有（引用，不列为提出）**：—
- **通用词（不计）**：—
