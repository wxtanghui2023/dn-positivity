已查地图：**命中且决定性**（所查档：`V254-canonical-signed-cancellation-threshold-equals-beta-star.md`（全文头部 §1–§3 ＋ §10 T1 收紧）、`V253`、`V255`（parity barrier）、`C-81`（`Q★`：量的翻转）、`C-82`（`DEC-1`：重排不能改值）、`C-79`（`K1--K4`）、`V215` §5（残余）、`振荡消解普查` §1(ii)）。**结论**：我提议的"带号量"**就是 `V254` 已登记并已闭合的那条**（典范带符号抵消阈值 ＝ `β_*`），且其闭合是 **按类**（`V254` §2 的结构性特征：任何典范带号权的 Dirichlet 级数都是 L-函数／其商 ⟹ **奇点结构＝零点结构**）⟹ **本会话第 8 次同址**；且这次是**按类关**，不是个案 ✓✓

# C-83 · **闸门判定：带号量＝`V254`（按类闭合）＋ 本会话策略读数 ＋ 综述骨架**

> **时间**：2026-09-18 12:59 唐先生「继续」（承接 `C-82` §4 残余：唯一方向是"换量"）
> **本档**：残余方向的闸门（命中 `V254`）＋ 策略读数 ＋ 综述骨架 ✓

---

## §0 结论（先行）

$$\textbf{"带号量"＝}\text{`V254` 已登记：}\boxed{\text{典范带符号抵消的阈值}＝\beta_*}✓✓\（\text{且}\ \textbf{按类闭合}）$$
$$\qquad \text{`V254` §2 四例（}\mu,\ \lambda,\ \chi,\ \text{Hecke}\ a_n）\ \textbf{共同特征}：\text{其 Dirichlet 级数都是}\ \textbf{L-函数／其商} \Longrightarrow \textbf{奇点结构＝零点结构}✓✓$$
$$\qquad \Longrightarrow\ \text{不是"这一类里没找到"，而是}\ \textbf{类本身被封}✓✓$$
$$\qquad \Longrightarrow\ \boxed{\text{本会话}\ \textbf{第 8 次同址}}\ ✓\（\text{C-61／C-64-65／C-69／C-70／C-71／C-72／C-82／本条}）$$

---

## §1 `V254` 逐字（核心）

$$\textbf{恒等式推向带号}：\frac{1}{a^c\log a}=\int_c^\infty a^{-s}ds\ \Longrightarrow\ \sum_a\frac{\mu(a)}{a^c\log a}=\int_c^\infty\Bigl(\sum_a\mu(a)a^{-s}\Bigr)ds=\boxed{\int_c^\infty\frac{ds}{\zeta(s)}}✓$$
$$\qquad ⚠️\ \text{关键}：\text{求和—积分交换是否合法，}\textbf{取决于内层级数在}\ (c,\infty)\ \text{上是否收敛}——\text{"不是技术细节，}\textbf{它就是全部信息所在}\text{"}✓✓$$
$$\text{收敛横坐标}：\sum_n\mu(n)n^{-s}=1/\zeta(s)\ \text{的奇点}\ \textbf{恰为}\ \zeta\ \text{的零点} \Longrightarrow\ \sigma_0=\sup\{\Re\rho:\zeta(\rho)=0\}=\beta_*✓$$
$$\qquad \Longrightarrow\ \textbf{带符号抵消的阈值}\ \ge\ \beta_*\ \（\text{`V254` §10 T1 收紧}：\text{奇点只给}\ \textbf{下界};\ \text{反向不等式}\ \textbf{需独立证明};\ \text{"把解析延拓当成级数收敛是错的"}\bigr)✓$$
$$\textbf{无条件情形}：\text{由 PNT（}\iff\beta_*\le1）\ \text{得}\ \textbf{无条件阈值}=1\（\text{与无符号通道}\textbf{相同}）$$
$$\qquad \Longrightarrow\ \boxed{\text{把带符号阈值从}\ 1\ \text{推到}\ \tfrac12\ \textbf{就是 RH 本身}} \Longrightarrow\ \textbf{逃逸 B 在最强意义上循环}✓✓✓$$

## §2 为什么这是"**按类**"闭合（`V254` §2）

| 典范带号权 | Dirichlet 级数 | 性质 |
|:--|:--|:--|
| **Möbius** `μ(n)` | `1/ζ(s)` | 商 · 奇点＝零点 |
| **Liouville** `λ(n)` | `ζ(2s)/ζ(s)` | 商 · 同 |
| **Dirichlet 特征** `χ` | `L(s,χ)` | L-函数 · 同 |
| **Hecke／Ramanujan** `a_n` | `L(s,f)` | L-函数 · 同 |
$$\Longrightarrow\ \boxed{\text{凡"典范"带号权，其级数的奇点结构必＝某零点结构}} \Longrightarrow\ \text{阈值必}\ \ge\ \text{相应}\ \beta\text{-量}✓✓$$
$$\qquad \Longrightarrow\ \text{这与}\ \text{`V255` parity barrier} \text{形成两道独立闸门（}\text{解析结构型／组合权重型}\text{）}✓$$

## §3 策略读数（诚实，`[本档]`）

$$\text{本会话内，}\textbf{框架内} \text{的每一条路都已同址：}\text{重排／分层（`DEC-1`）}\big|\text{majorant／对偶正性（`C-30`）}\big|\text{常数／sharpness（`SQ1`）}$$
$$\qquad \big|\ \textbf{带号权}（`V254`）\ \big|\ \text{parity／筛权重}（`V254`／`V255`）\ \big|\ \text{大值／密度}（`TYPE-MATCH`）\ \big|\ \text{识别侧接口}（`C-69`／`C-70`）✓$$
$$\text{且墙已被}\ \textbf{特征化}：\qquad \text{support}>1\iff\text{无条件三阶矩}\iff\text{prime-pair}\iff\text{Mertens 强度}\iff\beta_*\text{-阈值}✓✓$$
$$\Longrightarrow\ \boxed{\text{框架内继续"找路"＝边际收益递减}}✓\（\text{这不是放弃，而是}\textbf{把搜索空间报告完}）$$

## §4 剩下三类（诚实分类）

$$\textbf{(A)}\ \textbf{交付物}：\text{① 综述笔记（唐先生提议）};\ \text{② 论文 B 两处表述修正};\ \text{③ Palojärvi 常数注记}✓$$
$$\textbf{(B)}\ \textbf{需新对象}（\text{三个未实例化面}）：\text{第四类不变量}\ \big|\ \text{ARS 根定位型}\ \big|\ \text{canonical sign 接口}\ \Longrightarrow\ \text{均已预注册判据，}\textbf{无候选}⚠️$$
$$\textbf{(C)}\ \text{接受墙}✓$$

## §5 本会话**净收益**（非突破级，但是真东西）

$$\text{①}\ \textbf{support-1 障碍的技术解剖}：\text{六环文献链（Schur→MV→Preissmann→Yangjit→Rodgers ＋ Montgomery}\ \alpha\ge1\ \text{／RS Lemma 3.2／Lemma 3.5／BGSTB24）}\ \text{全部}\ \textbf{逐字}✓$$
$$\text{②}\ \textbf{定量门槛}：\eta\ge0.04\Longrightarrow0.70>0.682\ \text{（阶梯 1.04→0.70／1.26→0.80／1.70→0.90）}✓$$
$$\text{③}\ ⭐\ \textbf{"值 vs 界"的统一理由}（`C-82`）：\text{障碍是}\ \textbf{值的性质}，\text{故整族"重排／majorant／界改进"型攻击}\ \textbf{原理上无效}✓✓$$
$$\text{④}\ \textbf{两个新命名面＋判死理由}：Q^\star（\text{量的翻转}）／DEC-1（\text{不分解＋Mertens 强度}）✓$$
$$\text{⑤}\ \textbf{两处勘误}：8/23"无条件"措辞／`C-71` §4"接口类空集"／`C-78` `SQ1` 分层✓$$
$$\text{⑥}\ \textbf{判据工具箱扩容}：K1--K4／两型型判据／值-界判据／幂级 vs 常数判据✓$$

## §6 综述笔记**骨架**（供成文；性质＝过程性/综述）

$$\textbf{标题}：\text{"The support-1 barrier: a technical anatomy"}$$
$$\textbf{§1}\ \text{Montgomery}\ \alpha\ge1\ \text{"little information"（原始形式，逐字）};\quad \textbf{§2}\ \text{RS Lemma 3.2（消失引理）＋乘积界}\ X^k\le T^{2-\delta}$$
$$\textbf{§3}\ \text{RS Lemma 3.5 ＝ C--S ＋ Rankin--Selberg（一般度数）vs Montgomery 二阶矩（}\zeta,\ m=1\text{）};\quad \textbf{§4}\ \text{BGSTB24 无条件范围}\ 0\le x\le T$$
$$\textbf{§5}\ \text{MV／Hilbert 步的}\ \textbf{常数谱系}：\text{Schur 1911}\to\text{MV 1974}\to\text{Preissmann 1984}\to\text{Yangjit 2022/23}\to\text{Rodgers 2026}$$
$$\textbf{§6}\ \text{定量门槛}\ \eta\ge0.04\Longrightarrow0.70\quad ＋\quad \textbf{"值 vs 界"的判断（本会话新）}$$
$$\qquad ⚠️\ \text{写作纪律}：\text{① 不得写"Yangjit 已证最优常数}>\pi\text{"};\ \text{② 不得写"未被走过"（它是那堵墙）};\ \text{③ 不得记为}\ \textbf{突破级}✓$$

## §7 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-18 13:0x）`[纪律]`（先跑后写）

```
技术词 第八次同址  命中文件数=0
技术词 按类闭合   命中文件数=0
技术词 综述骨架   命中文件数=0
技术词 策略读数   命中文件数=0
```
**读数**：四项**均 0 档 ⟹ 本档新增** ✓

## §8 边界

- `[逐字]` §1／§2 取自 `V254`（含 §10 T1 收紧）✓；`[本档]` §0 按类判定、§3 策略读数、§4 分类、§5 净收益、§6 骨架 ✓
- **不声称**：三个未实例化面不可能 ✗；不判墙"永久不可破" ✗；不证 RH ✗；不修改原档 ✓
- **纪律**：先查后判（R-1 ✓，**先跑后写** ✓）；**未用 RH 作推导** ✓；**零数值** ✓

```
⚠️ 任务：唐先生 12:59「继续」= 跑闸门，看"带号量"是否已是 V254（我怀疑是同址）
✅ 闸门判定：**就是 V254**（"典范带符号抵消的阈值 ＝ β_*"），且**按类闭合** —— V254 §2 四例（μ/λ/χ/Hecke）共同特征：
   其 Dirichlet 级数都是 L-函数/其商 ⟹ 奇点结构＝零点结构 ⟹ 阈值必 ≥ 相应 β-量；无条件阈值＝1（由 PNT）；
   ⟹ 把带符号阈值从 1 推到 1/2 **就是 RH 本身**（逃逸 B 在最强意义上循环）；§10 T1 收紧：奇点只给下界，反向需独立证明
⚠️ ⟹ **本会话第 8 次同址**（C-61/C-64-65/C-69/C-70/C-71/C-72/C-82/本条）；且与 V255（parity barrier）构成两道独立闸门
⭐ 策略读数：框架内所有路（重排/分层、majorant/对偶正性、常数/sharpness、带号权、parity/筛权重、大值/密度、识别侧接口）全部同址；
   墙已被**特征化**：support>1 ⟺ 无条件三阶矩 ⟺ prime-pair ⟺ Mertens 强度 ⟺ β_*-阈值 ⟹ 框架内继续找路＝边际收益递减
⚠️ 剩下三类：(A) 交付物（综述笔记/论文B两处修正/Palojärvi 常数注记）；(B) 需新对象的三个未实例化面（第四类不变量/ARS/canonical sign 接口）；
   (C) 接受墙
✅ 本会话净收益（非突破级但真实）：①六环文献链逐字解剖 ②定量门槛 η≥0.04⟹0.70 ③"值 vs 界"的统一理由（关掉整家族）
   ④两个新命名面＋判死理由 ⑤三处勘误 ⑥判据工具箱扩容（K1–K4/两型判据/值-界判据/幂级 vs 常数判据）
✅ 综述骨架已列（六节＋三条写作纪律：不得写"Yangjit 已证最优常数>π"；不得写"未被走过"；不得记为突破级）
✅ 净产出：①带号量闸门=同址（按类）✓；②策略读数 ✓；③剩下三类分类 ✓；④本会话净收益清单 ✓；⑤综述骨架 ✓
```
