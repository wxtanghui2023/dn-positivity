# E26B — 截断地板与 N-趋势：d_N²(N,T)（计算结果 B）

**依据**：`docs/E26-A4-4-constructive-direction.md` §6(B)（设计）｜脚本 `scripts/E26B_dN_larger_T_N.py` ＋同名 `.txt` ✓
**未改既有文件；未提交 git** ✓｜zeta 网格缓存在**仓库外** `/tmp/dn_e26b_cache` ✓（T=400 读项目缓存 ✓）
**标签**：核验 = 本脚本对照 ｜ 引用 = 文献陈述 ｜ 推导 = 本文推导 ｜ 未做 = 未主张。

## §1 量、参数、判据
```
【量】d_N²(N,T)：长度 N、傅里叶截断 T 的 Báez-Duarte 距离；报 d_N²·log N ✓
【参数】N = 20,40,80,160,240,320,400 ✓；T = 400（h=0.05，项目缓存）｜2000、8000（h=0.10，本项自算并缓存仓库外）✓
【约定】A = 字面 BCF 定义（核含 (mn)^{−1/2}，E26A 已用直接积分核验）✓；B = NB3 实作（无该权重）✓
【判据 L2（引用/设计）】d_N²·log N 随 N **单调下降**并进入 [C,3C] = [0.046192, 0.138574] ✓
```
## §2 结果（核验）：**判据 L2 未达成** ✗（干净负结果）
```
约定 A，d_N²·log N（N=20 → 400）：
  T=400  ：0.1685 → 0.2827（/C：3.65 → 6.12）✗ 上升
  T=2000 ：0.1796 → 0.3091（/C：3.89 → 6.69）✗ 上升
  T=8000 ：0.1851 → 0.3235（/C：4.01 → 7.00）✗ 上升
⟹ **三个 T 下都单调上升，且与 C 的比值随 N 增大**；**从未**进入 [C,3C] ⟹ L2 明确不成立 ✗
（约定 B 同样上升：T=8000 时 2.064 → 4.093 ⟹ 与 `A4-1` §3 记录的方向一致 ✓）
```
## §3 关键发现①：d_N² 在 T 上**未收敛**，且随 T **上升**（核验）
```
N=160：d² = 0.04893 (T=400, h=.05) → 0.05317 (T=2000) → 0.05545 (T=8000) ｜ 增幅 +8.7%, +4.3%（递减）
N=400：d² = 0.04719 → 0.05158 → 0.05399 ｜ N=20：0.05624 → 0.05994 → 0.06179
⟹ T=400 的截断**不是**主导误差的说法需修正：T-依赖约 +13%（N=160），且实测 d² 随 T 单调**上升**
   （3 个 T、每个 N 皆然）⟹ 若该单调性延续，T=∞ 的值将 **大于** 已测最大值 0.05545；按增量比外推
   （推导，模型相关）得 ~0.058–0.065 —— 无论取哪个，仍约为 C/log160 = 0.00910 的 **6 倍以上** ✓
⟹ 因此 §2 的"上升"结论在 T→∞ 极限下不会翻转（T 越大越往上）✓
【网格步长自检（核验）】T=400 处 h=0.05 vs h=0.10：d² 0.048931 vs 0.049130（+0.4%）✓ 步长非主导 ✓
```
## §4 关键发现②：显式 BCF 多项式给出下降的 I(V_N)·log N（核验）
```
I(V_N)·log N（N=20 → 400）：
  T=400 ：1.3183 → 0.8534（/C：28.54 → 18.47）✓ 单调下降
  T=8000：1.3323 → 0.8926（/C：28.84 → 19.32）✓ 单调下降（T-依赖仅 ~1% ✓）
⟹ **显式 V_N 已显示 1/log N 的"速率"** ✓，但常数是 C 的 **19–29 倍**（未到 1）⟹ 归一化因子**不是**单一 k_N ✓
⟹ 这与最优 d_N² 的行为形成对照：**最小值**上升（§2），**显式多项式**下降（§4）⟹ 见 E26A §3 的核差异诊断 ✓
【引用】BCF Thm 1：RH + Σ_{|ρ|≤T}1/|ζ′(ρ)|² ≪ T^{3/2−δ} ⟹ I(V_N) ~ C/log N；本项 T=8000、N=400 处为 19.3·C ✓
```
## §5 已知答案校验（Möbius 符号，必须项）
```
约定 A：N≤160 全通过（T=400：97/98；T=2000/8000：**98/98**）✓；限制 n ≤ N/2 时 50/50（100%）✓
N=400：226/243（全）｜120/122（n ≤ N/2）⚠️ —— 退化源于 cond(K)（E26A §4 已定位：边界层零空间），非实现错误 ✓
```
## §6 裁定与边界
```
【裁定】L2 **未达成** ✗：三个 T 下 d_N²·log N 均随 N 单调**上升**，比值 /C 由 4.0 升到 7.0 ✓
【已排除的原因（核验）】① 网格步长（+0.4%）✓ ② 实现错误（Möbius 98/98）✓ ③ 单一 T 的截断（T 越大反而更高）✓
【尚存的可能（未定）】(a) 迭代/渐近项 o(1) 在 N ≤ 400 仍极大（BHLS/Burnol 的 o(1) 无显式界）⚠️；
   (b) 目标函数/测度的进一步归一化差异（E26A §3 已定位为核权重，但未必是唯一项）⚠️
   —— 两者本项**均未判定**，须更大学术条件或更多文献输入 ✓
【未做 ✗】未改既有文件 ✗；未提交 git ✗；未输入 1/2 ✗；**未声称任何证明，更未声称本项目证明 RH** ✗
```
