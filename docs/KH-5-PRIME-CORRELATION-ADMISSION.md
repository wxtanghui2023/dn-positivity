已查地图：命中 50 处 —— 先逐条判 已DEAD/已封/已登记；命中即引既有条目，不得开新案
　判定：⭐ **本候选 ＝ 档案既有 `R8` 素数对 carrier 专线全套**（`PROTOCOL-R8-prime-pair-carrier.md`｜`R-A8-prime-pair-carrier-audit.md`｜
　`R-A8.2-precision-gap-audit.md`｜`R-A8.4-hspace-carrier-audit.md`｜`PROTOCOL-R8v-transform-chain.md`｜`C97-prime-gap-memory-analysis`）✓
　＋ `CLOSED-ROUTES-MAP` L2851 已把 **prime-pair** 认作**墙**（「**原子墙**（无条件三阶矩 at `X≍T` ≡ **prime-pair** ≡ support>1；**不可再分**）」）✓
　⟹ ⛔ **不得开新案**；本档＝**承接既有 `R8` 线** ✓✓

# **KH-5 / PRIME-CORRELATION-ADMISSION**（第一轮 N1–N5）

**唐先生令（2026-09-22 23:08）**：
① **现在开刀**，且**不再做候选大清单**，直接按 `E-10` 新纪律做**第一轮准入筛**：先找少量真正符合 **N1–N5** 的对象，**N6/N7 再审** ✓；
② **候选对象**：固定平移素数相关量
　$$C_H(x) = \sum_{\substack{n\le x\\ n,\,n+H\ \mathrm{prime}}}1
　\qquad\text{或}\qquad
　\mathcal C_H(x) = \sum_{n\le x}\Lambda(n)\Lambda(n+H)$$，**`\theta = H`** ✓；
　⭐ 「`H` **不是人为塞进算子里的参数**，而是**直接改变算术对象本身**：`H` 改变的是素数对的关系结构」✓✓
③ **本轮只审 N1–N5**（**N6/N7 延后**）；**N5 为硬门**：**不接受**"它随 `H` 变化"这类**弱理由**；
　至少要找到类似 `\Delta_H C`、`C_{H_1+H_2}` 与 `C_{H_1},C_{H_2}` 的关系，或**算术分解／局部-整体组合关系／极值-单调结构**；**否则直接 DROP** ✓✓
④ **只有 N1–N5 全过**才进第二层：`\mathcal C_H \to \mathcal O(H) \to \beta\text{-sensitive observable} \to \mathrm{RH}`，
　且届时**明确登记** $$\boxed{\texttt{RH-interface} = \mathrm{UNKNOWN}}$$；
　⛔ **不允许**因"素数相关量通常和 RH 有关"就**提前**给它 RH relevance ✓✓
⑤ ⚠️ **预警（照录）**：**不能把 Hardy–Littlewood 猜想、孪生素数、显式公式等现成 RH 邻接物直接当作 N5 或 RH bridge** ✓✓
⑥ ⛔ **第一步不算零点、不接 ζ、不找 `1/2`、不做数值实验** ✓

D0: 本档对象 = **素数相关量（`\theta = H`）的 N1–N5 第一轮准入审计**（⚠️ **候选级已被档案 `R8` 线覆盖 ⟹ 承接不新开** ✓；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算／零数值／未写程序／未做数值实验**／未算零点／未接 ζ／未找 `1/2`／未证 RH ✓

---

## §1 **N1–N4**（✓ 通过；且**档案已有形式化** ✓✓）

| 门 | 令中要问的问题 | 本档判定 | 档案依据 |
|:--|:--|:--:|:--|
| **N1** | 是否完全可在不提 RH/零点下定义？ | ✅ **可以**（直接由素数／`\Lambda` 定义 ✓） | `R-A8` **`G0-1`** 逐字：`J(X,H)` 的定义「**unconditional；RH = 否；zero statistics = 否**」✓✓ |
| **N2** | `H` 固定时是否有独立数学问题？ | ✅ **是**（素数对／相关性本身即独立问题 ✓） | `R-A8` `G0-2`：**prime-pair → `J` 是"真[子对象]"**；`G0-4`：HL 型输入本身即**强未解决算术问题** ✓ |
| **N3** | `H` 是否**真变量**而非归一化参数？ | ✅ **是**（形式化确认 ✓） | ⭐ `R-A8` **Gate 1「`H\leftrightarrow\lambda` 字典」＝"本轮最有价值结果"**：`X = T^{\lambda}`、`H = X^{\eta}`、`T \asymp X/H \Rightarrow T \asymp X^{1-\eta}` ✓✓ |
| **N4** | 改变 `H` 是否**真改变 quantity** 而非坐标变换？ | ✅ **是**（字典给的是**对象层映射**，非坐标跃迁；档案且明令"**不做坐标跃迁**" ✓） | `R-A8.2` 逐字「**不做坐标跃迁；不宣布 `R8-C`**」✓✓ |

```
⟹ **N1–N4 全过 ✓**，且 **N3/N4 所问的"`H` 是否真变量"在档案中已有专名结果（Gate 1 字典）** ✓✓
```

## §2 **N5**：是否存在**可证明**的非平凡结构（⚠️ **结构已在档** ）

```
【令中禁止的弱理由】 ⛔ "它随 `H` 变化" ✗（本档不采用 ✓）
【本档找到的结构（**皆为档案既有、且为无条件/等价性级** ✓）】
　(i) **精确展开结构**：`\sum_{h\le H}(H-|h|)\sum_{n\sim X}\Lambda(n)\Lambda(n+h)` ⟹
　　`R-A8` 逐字：该展开「**结构上成立**」，但**公式层面必须规范化**（三角权、端点项、`\psi` 的连续平均）⟹ **不得写成未经处理的形式** ✓✓
　(ii) **代数到 `J` 的等价转换**（`G0-2`）：`prime-pair \overset{代数}{\longrightarrow} J` **无条件成立**，但明标「**尚不能称为新 β-sensitive info**」✓
　(iii) **`H\leftrightarrow\lambda` 字典**（Gate 1）✓ —— 结构上即"`H` 参数 ↔ 谱尺度参数"的**显式映射** ✓✓
【⛔ N5 判定】 结构**存在且可证**（(i)(ii)(iii) ✓），**但它们已全部是档案自有** ⟹
　⟹ **N5 按新纪律应记为**：$$\boxed{\text{结构存在 ✓ 但已被既有机器吸收 ⚠️（＝ N6 型的红旗提前亮起）}}$$ ✓✓
【⛔ 未采用者（依令 ⑤ 预警）】 Hardy–Littlewood 猜想 ✗（`G0-4`：**本身即强未解决算术输入**）；孪生素数猜想 ✗；显式公式型桥 ✗（`G0-3`）✓✓
```

## §3 ⭐⭐ **决定性地图结论**（本档最重要 ✓✓）

```
【本候选 ＝ 既有 `R8` 线】 且**档案早已用与 N1–N7 同构的判据审过**：
　| 本令之门 | 档案对应 |
　|:--|:--|
　| **N1＋N6＋N7** | ⭐ **`R8-A` 判据（逐字）**：「存在**不使用 RH／零点统计／Hardy–Littlewood** 的素数侧二阶对象，且给出 `\lambda > 1`」⟹ **罕见真新输入 ⟹ 进入 V1 主线** ✓✓ |
　| **N3/N4** | `R8` **Gate 1**（`H\leftrightarrow\lambda` 字典）✓ |
　| **N6/N7** | `R8` **Gate 0（强制）：RH Contamination Audit** ✓✓ |
　| **N5 的结构** | `R8` **Gate 2–4（三问形式化）** ＋ `R8v` **变换链逐箭审计（A1–A4）** ✓ |
【⭐⭐ 档案的实际裁决（逐字）】
　$$\boxed{\textbf{R8-B（当前）｜ R8-C 未证 ｜ R8-A 未证 ｜ R8-D 已排除}}$$（`R-A8.2` 强化版同裁决 ✓）
　· **`R8-B`** ＝「**载体存在，但无条件范围不足以支撑 `\lambda > 1`**」⟹ **部分结果 ＋ 须登记具体缺口** ✓；
　· **瓶颈已定位 ＝ `S2`**（`R-A8.2` 逐字："本轮真正定位：瓶颈 = `S2`；但尚不足以升级为 `R8-C`" ✓）；
　· **Gate 0 裁决（逐字）**：`prime pairs \overset{代数}{\to} J \overset{显式公式}{\leftrightarrow} zero pair correlation`，
　　「**右侧强等价目前带 RH 条件。未发现隐藏的"独立无条件 prime-pair → zeros"通道**」⟹ **`R8-C` 暂不能成立为定理** ✓✓
【⛔ 因此】 本令第 ⑤ 条的预警**在档案中已被独立验证**（HL 不是现成 carrier；显式公式方向带 RH）✓✓
```

## §4 **判定与下一步**

```
【KH-5 第一轮结论】
　**N1–N4 ✅ 全过**（且 N3/N4 已有档案专名结果 ✓）｜**N5 ✅ 结构存在但已被自有机器吸收 ⚠️**（= N6 型红旗）｜
　**N6/N7 延后**，但**档案 `Gate 0` 已先行审过**且结论为"未发现隐藏通道" ✓
　⟹ ⛔ **不开新案**：**本候选 ＝ 既有 `R8` 线**，其**当前裁决 ＝ `R8-B`**（载体存在、无条件范围不足、**瓶颈 ＝ `S2`**）✓✓
【登记（依令 ④）】 $$\boxed{\texttt{RH-interface} = \mathrm{UNKNOWN}}$$ ✓（与 `Gate 0` 的"未发现隐藏通道"**一致** ✓）
【⭐ 承接点（下一步的唯一生产力方向）】 **`R8-B` 的精确缺口 ＝ `S2`** ⟹ 后续若要推进，只能攻 `S2`，
　**不得**再重审"有没有 prime-pair 数据"（`R-A8` 逐字："下一轮不再审'有没有 prime-pair 数据'（答案：有）" ✓✓）
【⛔ 依新纪律的 DROP 条款】 本档**不**以"结构存在"自动升级；**结构已被吸收**即为 **N6 红红旗** ⟹ **不进入第二层** ✓
```

## §5 边界与回查（✗✓）

```
✗ 零计算／零数值／未写程序／未做数值实验／未算零点／未接 ζ／未找 `1/2`／未证 RH／**未开新案** ✓
✗ ⛔ 未把 HL／孪生素数／显式公式当 N5 或 RH bridge（依令 ⑤ ✓）；⛔ 未因"通常与 RH 有关"提前给 RH relevance ✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 平移相关量 命中文件数=0 ⟹ **本档新增** ✓｜局部密度因子=0 ⟹ **新增** ✓｜CRT分解=0 ⟹ **新增** ✓
　技术词 素数对相关 命中文件数=7 ⟹ **档案已有 ⟹ 引用** ✓｜算术自相关 命中文件数=4 ⟹ **档案已有 ⟹ 引用** ✓✓
【地图回查】 命中 **50**；**实质命中 ＝ `R8` 线全套 ＋ `CLOSED-ROUTES-MAP` L2851（prime-pair ≡ 原子墙）** ⟹ **承接、不开新案** ✓✓
【档级引用】 `PROTOCOL-R8-prime-pair-carrier.md`（`R8-A/B/C/D` 四输出；Gate 0 强制 RH 污染审计；Gate 1 `H\leftrightarrow\lambda` 字典；预先倾向 `R8-C` 须核实）✓｜
　`R-A8-prime-pair-carrier-audit.md`（**裁决 `R8-B`**；`G0-1`–`G0-4`；"未发现隐藏通道"**非穷尽性** ✓）✓｜
　`R-A8.2-precision-gap-audit.md`（强化版 `R8-B`；**瓶颈 ＝ `S2`**；不做坐标跃迁）✓｜`PROTOCOL-R8v-transform-chain.md`（变换链 `A1`–`A4`）✓｜
　`CLOSED-ROUTES-MAP` L2851（prime-pair ≡ 原子墙）✓｜`KH-FRONTIER-SHAPE`／`E-10`（N1–N7 准入筛）✓
