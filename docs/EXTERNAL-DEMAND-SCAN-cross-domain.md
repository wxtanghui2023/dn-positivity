已查地图：命中（`CROSS-DOMAIN-MAPPING-of-our-capabilities`（唐先生否定 `T-a/b/c`）／`S13-EXECUTION-TEST-FAIL`／`CORRECTION-idea-first-not-capacity`）⟹ **引用，不开新案** ✓
D0: 本档对象 = **跨领域外部需求表**（六域 × 五列：明确问题／当前数字或边界／为何未解／可验证成功条件／能否天级推进）
D1: 0 （`[REVIEW]` 轮次：需求扫描，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **外部需求表（problem-first 扫描，capability-second）**

## §1 **六个域的逐格结果**

| 域 | 明确问题 | 当前数字/边界 | 为何尚未解决 | 可验证成功条件 | 天级推进？ |
|:--|:--|:--|:--|:--|:--|
| **`D1` 组合/极值组合** | 改进某个已知界（van der Waerden／Schur／Golomb 最优标尺／超图 Ramsey 小情形） | 各有**明确数字**（如 `S(6)` 已知、更高阶未定） | **穷举规模**（分布式跑年）**或**需新剪枝/对称定理 | 新构型（自身即证书）或**穷举完整性证书** | ✗ 多数（规模）；少数极小情形 ⚠️ |
| **`D2` 整数规划/离散优化最优性证书** | 某些具体实例的最优值**只有黑箱求解器数值、缺严格证书** | 有数值最优值，证书缺/弱 | 求解器规模 ＋ 无证书导出 | 该实例的**严格下界证书**（LP 对偶／割／`B&B` 证明日志） | ⚠️ 可能但需求解器工程；**价值密度低** |
| **`D3` 形式化（Mathlib 缺口）** ⭐ | **官方待办清单**：逐字来源 `leanprover-community.github.io/undergrad_todo.html` "**Undergrad math not in mathlib**" | 清单公开且**逐项**（微分方程／子流形／测度与积分／傅立叶：**Dirichlet 定理、Fejér 定理**／**Gershgorin–Hadamard**／`LU` 分解／`SVD`／Jacobi–Gauss-Seidel 收敛 …） | **人力分散、单点贡献激励低** | **PR 被合并 ＋ CI 通过 ＋ `sorry`-free** ✓✓ | ✅ **能**（Lean 4.33 本地可 build；**已写过 Mathlib 缺的 von Neumann 迹不等式／Sylvester 惯性**）✓✓ |
| **`D4` verified computation／CAP 审计** ⭐ | **计算机辅助证明的证书常出错**（typo／边界／浮点／抽查不全）—— 逐字来源：`arXiv:2608.13067` "**Computer-assisted Proof Under Audit: Typos, Certificate** …" | 审计多为个案；证书格式与独立校验器**不统一** | 每个 CAP 都是定制物；**缺独立复核者** | 对一个**具体 CAP** 给出**独立复核报告 ＋ 可复现证书**（或**发现其缺陷**）✓✓ | ✅ **能**（证书链能力直接适配；需先锁定一个具体 CAP）✓ |
| **`D5` CS 小规模最优性** | sorting networks 等 | `S(13)\in\{44,45\}` | 我们已实测＝**`NO-IDEA`**（非算力） | 44 网络或 44 不可能证书 | ✗ **已 FAIL**（同类结构同因） |
| **`D6` 算法/软件验证** | 现实中实现的正确性/完备性靠经验测试 | 大量库无独立校验器 | 需领域知识 ＋ 长期维护 | 独立 checker 通过（能发现/排除缺陷） | ⚠️ 需**具体对象**；需求分散 |

## §2 ⭐⭐ **扫描结论（本档核心）**

```
【合格目标】**2/6 域**给出合格目标：**`D3`（Mathlib 官方待办清单）** 与 **`D4`（CAP 独立审计）** ✓✓ —— 二者合格的理由不是"我们的资产特别"，而是它们**本身就是公开的、需求侧的清单** ✓
【其余四域】 `D1`/`D5` 受**规模或想法**限制（`S(13)` 已 `NO-IDEA`）；`D2`/`D6` **价值密度低或需求分散** ✗
【⭐⭐ 更诚实的层次（您预告的那一层）】 合格目标**全部落在"贡献/工具/审计"**，**没有一个是"研究型发现"** ⟹ $$\boxed{\text{我们能力组合的外部定位＝验证／形式化／审计型选手，而非发现型选手}}$$ ✓✓ —— 这不是"找不到目标"，而是**目标的类型被我们的能力结构决定了** ✓
【四步链自检（`D3`）】 现有结果（该定理缺失）→ 具体缺口（清单列出）→ 我们的执行（本地形式化＋PR）→ 可验证新结果（合并＋CI）✓✓ **四步不断** ✓
【四步链自检（`D4`）】 现有结果（某 CAP 的证书）→ 缺口（无独立复核／疑有 typo）→ 执行（独立复算＋校验器）→ 可验证（复核报告＋可复现证书／反例）✓✓ **四步不断** ✓
```

## §3 **边界与下一步**

```
【边界】 ⚠️ `D3` 清单为**逐字来源**（官方页面）✓；`D4` 为**题录级**证据（`arXiv:2608.13067` 标题）⚠️ 未逐字核全文；`D1`/`D2`/`D5`/`D6` 的"当前数字/为何未解"按**档级**（领域常识，未逐字核）✓；§2 结论为**本档自行推导** ✓；⛔ 未制造候选／未启动搜索／未改状态／未碰 RH ✓
【两个可选起步（均"贡献型"，不等同研究）】 **(甲)** `D3`：从官方清单挑**一项**天级可完成的（如 **Gershgorin–Hadamard** 或 **Fejér 定理**），本地形式化并提交 PR ⟹ 成功条件＝**被合并** ✓；**(乙)** `D4`：锁定**一个具体 CAP**，做独立复核 ⟹ 成功条件＝**可复现证书或发现缺陷** ✓
```

## §4 【技术词回查】（逐字粘贴 ✓）

```
技术词 CAP              命中文件数=7    :: ./CROSS-DOMAIN-MAPPING-of-our-capabilities.md ./V2-29-single-ell-reverse-CS-capacity-test.md ./D0-D1-LEDGER.md 
技术词 Mathlib          命中文件数=33   :: ./CROSS-0-additive-multiplicative-cross-invariant-MAP-CHECK.md ./DECLARATION-2026-09-11-AI-MISALIGNMENT.md ./V186-inertia-mechanism-audit-endpoint-degenerates-to-positivity.md 
技术词 外部需求     命中文件数=2    :: ./EXTERNAL-DEMAND-SCAN-cross-domain.md ./MASTER-OUTPUT-INVENTORY-2026-09-20.md 
```
【三分类】 **本档新增**：六域五列需求表、`2/6` 合格判定、能力外部定位（验证/形式化/审计型）✓；**档案已有（引用）**：见上逐字；**通用词（不计）**：`外部需求` ✓
