已查地图：命中（`ZF-ZLG-2`（分岔≡anomaly）／`ZF-ZAI-2`／`ZF-PD-2`／`ZF-G5G6-DIM/CAN/PL/VI`／`§95–§118` 最小架构／`E-44`／`S6`／第一断裂 本线自档与既有封存）⟹ **引用，不开新案** ✓
D0: 本档对象 = 战略转向（母机制优先＋方法族筛除＋`PD-2` 终结）落档 ＋ 有限指数/缺陷机制的 `P/Q` 二分判定 ＋ 四硬门 `G1–G4` 审计
D1: 0 （`[REVIEW]` 轮次：机制审计与收敛判定，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`ZF-MECH-1`：有限指数/缺陷机制审计 ⟹ 收敛判定**（本档全为自行推导 ✓✓）

## §0 **战略转向落档（照录 ✓✓）**

```
【新搜索次序（照录）】 $$\boxed{\text{先找能产生「有限容量」的母机制，再问它能否接到离轴零}}\qquad⛔\ \text{不再反向}$$ ✓✓
【方法族一次性筛除表（照录）】 Euler 差分 ✗｜finite Dirichlet truncation ✗｜zero local jet ✗｜argument principle ✗｜majorant ✗｜resultant collision ✗｜**primitive divisor ✓（但 zero-link 缺失）**｜**有限指数/缺陷空间 ✓（值得真攻）** ⟹ 搜索空间缩为 $$\boxed{\text{有限指数/缺陷机制}}$$ ✓
【`PD-2` 终结封口（照录 ✓✓）】 ⛔ 不再做三/四阶差分、不再 `p,q,r` 增多、不再 nonlinear variant ⟹ **`PD-2` 直接封**；其**最大有效结果**＝$$\boxed{\text{算术周期差分能精确杀 Euler 项，但不能产生有限容量}}$$ ✓
【四硬门】 `G1` finite capacity｜`G2` genuinely arithmetic/geometric｜`G3` off-axis zero forces index consumption｜`G4` not a re-encoding of a known RH criterion ⟹ **`G3` 才是真正的墙** ✓✓
```

## §1 ⭐⭐⭐ **`P/Q` 二分（本档核心推导）**

```
【观察】 把"有限指数/缺陷"类机制按**容量是否内在**分两类：
**(P) 固定对象机制**（容量**内在有限**）：类群、`\mathrm{Sha}`、Mordell–Weil 秩、Betti 数、Euler 数、上同调维数（固定 `X`）⟹
　$$\boxed{\text{容量内在有限，但对固定对象\textbf{不变}\Longrightarrow\text{无事件可消耗}}}$$ ✗
**(Q) 族依赖机制**（容量随参数变化）：`s\mapsto\mathcal A(s)` 的离散不变量可跳变 ⟹ **有事件**，但
　$$\boxed{\text{跳变点计数一般\textbf{无限}（除非底空间紧或有外部有限性定理）}\Longrightarrow\text{无容量}}$$ ✗
【⟹ 二分结论】 $$\boxed{(P)\ \text{有容量无事件};\ (Q)\ \text{有事件无容量}}$$ —— 即您此前"三角墙"在**母机制层**重现 ✓✓✓
```

## §2 ⭐⭐ **推论：有限容量不可能来自"指数"本身**

```
【⟹】 "找一个小容量的指数"这条路**结构性不通**：指数只给**离散**，不给**有界计数** ✓
【⟹ 容量的真正来源只有两条】 **(a)** 底空间**紧** ⟹ 离散集有限（＝您 §109 的最小架构：`canonical discrete + compact + \infty`-分离 ⟹ finite ✓✓）；**(b)** 一个**独立的有限性定理**（Northcott／Siegel／有限性定理型）✓
【⟹ ⟹ `G1` 的正确实现形式】 $$\boxed{G1\ \text{须实现为「\textbf{紧底空间}＋离散不变量」，而非「小容量指数」}}$$ ✓✓
```

## §3 **`G2`：紧底空间的自然来源（清单）**

```
【自然出现紧底者】 模空间**紧化**｜Shimura 簇／射影簇｜Berkovich 紧化｜Arakelov 算术紧化｜有限型概型的射影闭包 ⟹ 皆**自然**，`G2` 可过 ✓
【与既有档的关系】 此即 `§99–§102` 已讨论的"compact 异常层"结构；本档补上的是：**紧性不是辅助条件，而是 `G1` 的唯一可自然实现方式** ✓✓
```

## §4 ⭐⭐ **`G3` 判定（真正的墙）**

```
【要求】 离轴零 `\rho` 须**强制**在紧底空间上的离散结构处触发事件 ⟹ 需要一个**自然映射** `\rho\mapsto` 该结构的点 ✓
【⟹ 但所有此类映射都要求】 $$\boxed{\text{零点的\textbf{算术描述}}}$$（否则映射无来源）⟹ 与 `S6`（局部可容许不能全局锁定）＋**第一断裂**（`\alpha_p\equiv1`、无内生算术动力学）同向 ⟹ **无已知零点算术描述** ✓✓
【⟹ 按您的指令判定】 ⛔ **不再换包装** ⟹ $$\boxed{G3\ \text{未过};\ \text{判定「有限指数母机制本身不接零」}}$$ ✓✓
```

## §5 **`G4`（非换壳）收紧**

```
【已证等价】 anomaly/special-point 类 **≡** `§95–§118` 最小架构（`ZLG-2` 等价定理）⟹ 属**同结构**，非新入口 ✗
【已封】 inertia/rank 类（`E-44`：只给 extensive）；monodromy/branch 类（`DIM`+`PL` FAIL）⟹ ✗
【⟹ `G4` 结论】 在已审计范式中**无通过 `G4` 者** ✓
```

## §6 **收敛判定 ＋ 账本 ＋ 收口建议**

```
【判定】 $$\boxed{\text{有限指数/缺陷母机制：}G1\ \text{可过（须走紧性）},\ G2\ \text{可过},\ G4\ \text{已收紧},\ \textbf{G3 未过}}$$ ⟹ 按您指令＝**该类母机制不接零**，⛔ 不再包装 ✓✓
【收敛陈述（本档）】 搜索次序已按您的要求翻转为"母机制优先"，并**实际执行完毕**：其结果为——在已审计的母机制族中，**唯一具内在有限容量者（`(P)` 类）不变，唯一能跳变者（`(Q)` 类）无容量**；而"紧性"这一唯一自然实现方式又把 `G3` 完全推给"零点的算术描述" ✓✓
【⟹ 本线收敛状态】 左半（`零 ⟹ 事件`）在**全部已审计范式**下结构性受阻；残留＝"**零点的算术描述**"这一**入口条件**（而非技术缺口）✓
【收口建议】 出**一份状态总结档 ＋ 登记**（把三角墙／`P/Q` 二分／方法族筛除表／最小架构／`G1–G4` 一并留档），并**停止本线**；等新的独立来源出现再启 ✓

| 层 | 状态 |
|:--|:--|
| `EDR`/`RPC-1`/`PD-2` 引擎与工具 | **保留（PASS/工具）** |
| 方法族表（8 类） | **全部筛除（除 `PD-2` 工具部分）** |
| 有限指数/缺陷母机制 | **`G3` 未过 ⟹ 不接零** |
| 右半（`event⟹`预算） | **干净** |
| 左半（`零⟹事件`） | **全部范式受阻** |
| 残留 | **零点的算术描述（入口条件）** |

【边界】 ⚠️ §3 紧化清单为常规事实（档级）；⛔ 未制造候选／未启动搜索／未改状态；⭐ §1 `P/Q` 二分、§2 推论、§4–§6 判定均为**本档自行推导** ✓
```
