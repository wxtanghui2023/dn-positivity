# 相关引理与猜想 · 四层台账（按与 RH 证明链的关系分层）

已查地图（**先查后写**）：`RPM-LEMMA-CONJECTURE-LEDGER.md`（(RP_M) 内部）、`MASTER-LEDGER-RELATED-LEMMAS-CONJECTURES.md`（按来源的三圈层视图）、`papers/`、`docs/C-143..C-177`。关键词回查：`四层台账`＝本档新建命名。
**本档任务（唐先生 2026-09-19 19:54）**：台账按**以下四层**重建 ——
```
第 1 层  与 RH 证明链相关（改善／突破／证伪）
第 2 层  由我们 RH 研究的内容带来的突破
第 3 层  与 RH 无关，但依我的分析可能被证明或突破
第 4 层  其它
```
**图例**：`[x]` 已确认 ｜ `[ ]` 未完成 ｜ ✅严格已证 ｜ 🟨证书 ｜ 🔶部分/条件 ｜ ⛔已证伪 ｜ 📖文献已核 ｜ 📖?待核

FREEZE-ACK: 本档即冻结期内的台账登记（依 `§8.1`；不产新数学结论）

D0: 本档对象 = **四层台账（RH 证明链／我方突破／RH 无关可攻／其它）＋ 逐项勾选** —— 关系 = 登记与分层，非新机制
D1: 0

---

## §1 第 1 层 · 与 RH 证明链相关（改善／突破／证伪）

### 1A 我们**改善**了链条上的结果

- [x] **1A1 Li 判据线性范围（论文 A）** ✅
      `λ_n ≥ 0` 对 `2 ≤ n ≤ 2T − O(1)`（线性范围、无假设、完全初等）· 出处 `papers/li-range/`
- [x] **1A2 Brown/Droll 修复（论文 B，τ=1）** ✅
      六引理 ＋ Abel 含边界项；精确余量 `(2/3)|b|H^{−3}`；覆盖一切 `k ≥ 2`、`H > e` · 出处 `papers/brown-thm2-classical/`
- [x] **1A3 Palojärvi Thm 4.1 的 `m=1` 常数改进** 🟨
      自有阈值 `4(K₁+K₄) n log n + 2` vs 源 `40 n log n(1/2+K₁+K₄)` ⟹ 系数 → `1/10` 且**去掉** `20 n log n` 项 · 出处 `papers/palojarvi-constant/note.md`
- [x] **1A4 Montgomery Lemma 2.2 的**单模**改进** 🟨✅
      `max_{n≤5M} Re Σ z_j^n ≥ 1/2`（`M ≤ 5`）vs 源 `1/20` ⟹ **常数 10×**
- [x] **1A5 `m_M ≤ M−1` 对一切 `M ≥ 2`（严格）** ✅ —— 使 1A4 相关的推论链无条件 · 出处 `C-176`＋`C-159`
- [~] **1A6 `m ≥ 2` 的自足性缺口** 🔶 —— 现仍引 Montgomery；由 §2 的 `(RP_M)` 进展决定
- [~] **1A7 Conjecture 3.2.7 的【第二条】不等式** 🔶 —— 论文 B 只处理第一条

### 1B 我们**证伪／修正**了链条上的说法

- [x] **1B1 属性归属误述**：`k ≤ 2T²logT` 属 **Conjecture 1.7.10**，非 3.2.7 ⛔ · 出处 `C-67`
- [x] **1B2 SQ1（Montgomery–Vaughan 加权 Hilbert）分层结论** ⛔
      常数谱系 `(3/2)π → (4/3)π → ∈(π, 4/3π]`（Rodgers 2026 首发严格 `>π`）；**常数改进不足以闭合幂次级缺口**；Layer 2（算术权重算子范数）**仍开放** · 出处 `C-78`/`C-79`/`C-80`
- [x] **1B3 "Yangjit 已证最优 > π"** = 误述（其为**方法族下界** `3.19497`）⛔ · 出处 `C-80`
- [x] **1B4 `(ISO)` 精确形式（"一个离线零点 ⟹ 无穷多"）** 🔶
      已精确化；但其与 RH 的**唯一连接现不可用**（算术侧输入不可达）⟹ 降优先级 · 出处 `C-59`/`C-62`/`C-63`
- [~] **1B5 B2-1（pairwise geometry / 单构型 vs 混合）** 🔶 —— **SUSPENDED / NEW-MATH-REQUIRED**（非"证伪"）· 出处 `C-126`
- [ ] **1B6 联合靶子（OP-3）** —— `∀φ,ψ：max_{k≤5(M+1)}[Σ_{j≤M}cos(kφ_j)+cos(kψ)] ≥ m_M`
      实证余量 0.22–1.10；**无证明路径** · 出处 `C-170`/`C-171`

### 1C 我们引用的**等价判据**（链条的入口/出口）

- [~] **1C1 经典 Li 判据** `λ_n ≥ 0 ∀n ⟺ RH` 📖
- [~] **1C2 Nyman–Beurling / Báez-Duarte** `lim d_N = 0 ⟺ RH` 📖
- [~] **1C3 Weil 正性** `Q_T(φ) ≥ 0 ⟺ RH` 📖
- [~] **1C4 Robin / Nicolas / Lagarias / Mertens** 📖
- [~] **1C5 de la Vallée Poussin 经典正性**（`3+4cos t+cos2t = 2(1+cos t)² ≥ 0`）📖 —— 完整三分法的依据 · 出处 POS1

### 1D 链条上的**已知结构障碍**（我们已定量登记）

- [x] **1D1 四路归约失效**：极值配置**分散且非通约** ⟹ 周期化／簇并／计数测度／二阶矩聚合**因同一结构原因失效** ✅ · 出处 `C-174`/`C-175`
- [x] **1D2 计数/鸽笼路线不足**（`n_k` 平均 ≈ `M/3` < `2M/3+1/3`）⛔ · 出处 `C-144`§4/`C-150`
- [x] **1D3 二阶矩路线失效**（交叉项 `M²log K` 吃掉余量）⛔ · 出处 `C-147`/`C-149`
- [x] **1D4 `support ≤ 1` 内相位可见性失败（数据类型层）** ⛔ · 出处 `C-137`

---

## §2 第 2 层 · 由我们 RH 研究内容带来的突破（自足成果）

> 判定：**产出源是我们 RH 线的研究过程**，内容本身可能 RH-free，但属"研究带出的真成果"。

- [x] **2A `(RP_M)` 程序：单点锐定理（鸽笼定理）** ✅
      `∀θ ∀N≥1：max_{m≤N} cos(mθ) ≥ cos(2π/(N+1))`，**最优**；三行鸽笼证明 · 出处 `C-159`
- [x] **2B `(RP_M)` 程序：`M ≤ 5` 的严格结果** 🟨✅
      `M=1` 引理 C（初等、`60°` 取等）；`M=2` 三段拼装（局部纯符号 `c=28√1677/559` ＋ 远场证书 ＋ 覆盖包含）；`M=3,4,5` 自适应证书 · 出处 `C-152..C-168`
- [x] **2C 一维极小极大新对象 `κ_N(λ) = inf_θ max_{m≤N}[cos(mθ) − λm²]`** ✅🟨
      闭式 `λ_max = 2 − √3`（两约束相切 ⟹ `λ²−4λ+1=0`）＋ `N=3` 的 1-D 证书（59 区间）· 出处 `C-173`
- [x] **2D 近似周期单调性引理及其推论（现无条件）** ✅ · 出处 `C-172`（引理三行）＋ `C-176`（关闭数值输入）
- [x] **2E 区间算术证书方法（假设闭合）** 🟨
      `m_M` 上界（`M ≤ 11`）与 `M=3` 证书：唯一非精确对象为 `π`（区间）· 出处 `C-176`/`C-177`
- [x] **2F 三件 T-I 级小结果（短文骨架已备）** ✅
      V227-A（`sup Re z` 非模长不变量）／V253（尾和界）／F3 反例（窗口隐形测度，秩 `47→256`）· 出处 `C-134`
- [x] **2G 判据工具箱**：核维数判据（`W<N/2` ⟺ 隐形能力）、可准入核 vs 实核维数、「局部隐形无意义」原理 ✅ · 出处 `C-133`/`C-139`/`C-134`
- [x] **2H 结构定位**：F3 机制 = **FRI／湮灭滤波器零空间**（经典，非新机制）📖 —— 诚实降级而非包装 · 出处 `C-135`
- [x] **2I 可复现资产**：证书附录 ＋ manifest ＋ 哈希；脚本与数据入仓 🟨 · 出处 `C-169`/`C-176`
- [~] **2J 论文交付线**：论文 A/B、Palojärvi 常数注记、support-1 综述（`papers/support1-barrier-anatomy/`）🔶

---

## §3 第 3 层 · 与 RH 无关，但依我的分析**可能被证明或突破**（分析性判断）

> ⚠️ 本层为**我的判断**（非承诺）；每条给出"为何可能"与"风险"。

- [ ] **3A `m_3` 的精确值收窄**（现 `≥ 1/2` 严格、`≤ 0.777171` 严格）
      · 为何可能：极小点邻域结构已量化（active 集 `{1,3,13,15}`、`0 ∈ int conv`、`c₃=0.7617`、半径收缩）⟹ 可仿 `M=2` 三段拼装
      · 风险：可能只收窄不闭合
- [ ] **3B `κ_N(λ)` 的一般 `N` 的精确形式**（`N=3` 已有闭式）
      · 为何可能：1-D 有限维、结构清晰（分段）；`N=5` 可先做
      · 风险：`N` 大时分段数增长
- [ ] **3C 加权版 `(RP_M)`**（簇权重 `w_l ≥ 1`）
      · 为何可能：少簇情形可归约
      · 风险：与主问题同源困难
- [ ] **3D 阻尼情形的最优常数**（数值 `≈ 0.36–0.40` vs `1/20`）
      · 为何可能：数值稳定、结构清晰；或先证 `≥ 1/4` 这类弱常数
      · 风险：无现成工具
- [ ] **3E Turán–Montgomery–Palojärvi 家族的最优常数**（单模情形 = `lim m_M`）
      · 为何可能：已有 `m_M` 序列与上界方法
      · 风险：需一般 `M`（见 1B6）
- [ ] **3F FRI／相位检索的"整数质量约束下的秩上界"问题**（RH 无关）
      · 陈述：固定总质量 ＋ 逐点整数性 ＋ 窗口幅谱 ⟹ 秩 `R ≤ B(W)`？
      · 为何可能：加性方向已封（`C-132`），只剩非加性变形
      · 风险：可能等价于 1B5（同一堵墙）
- [ ] **3G "模长型输入看不见相消"方法论短文**（三例并列 ＋ 四要素框架）
      · 为何可能：素材齐（三 T-I ＋ 框架）；定位诚实（主题是经典的，新在并列与锐度）
      · 风险：需先核 `V227-A`/`V253` 原文逐字（否则被审稿人一眼看破）
- [ ] **3H `κ_N` 极值族的文献定位**（Fejér／Chebyshev 型）
      · 为何可能：纯调查型，可完成；`C-159` 已独立证出
      · 风险：可能只是"已知结果的再发现"（需如实定位）

---

## §4 第 4 层 · 其它（制度／形式化／非数学产出）

- [x] **4A 负结果库与 NO-GO 地图**（`CLOSED-ROUTES-MAP`、`MASTER-STATUS-AND-CLOSURES`、`ASSETS-REGISTRY`）✅
- [x] **4B 档案制度**（`PROTOCOL-CODE-ARCHIVE` R1–R7、`D0/D1` 账本、`FREEZE-ACK`、技术词回查、先跑后写、「已查地图」）✅
- [~] **4C Lean／形式化线**（`V307–V309` 等）🟨 —— **独立于本工作**，仅登记关联
- [~] **4D 论文写作与投稿准备**（英文正文、附录、`κ_N` 文献核查、`M=5` 误差模型细化）🔶
- [x] **4E 历史事故与教训归档**（转义坑／`git add -A`／全盘 find／内存／误述教训）✅
- [~] **4F 外部文献核验清单（论文引用前必须逐字核）** 📖?
      Turán 1947、Turán 第二定理、Cassels 引理、`κ_N` 出处、Candès Thm 15.6 · 出处见三圈层台账 §2

---

## §5 勾选汇总

```
第 1 层  RH 证明链相关 : 1A[x]5/[~]2 ｜ 1B[x]3/[~]2/[ ]1 ｜ 1C[~]5 ｜ 1D[x]4
第 2 层  我方研究带出   : [x]8 ｜ [~]1（2J 论文交付）
第 3 层  RH 无关可攻    : [ ]8（全部为我的分析性候选，未开工）
第 4 层  其它           : [x]3 ｜ [~]3
```

## §6 边界

- ⚠️ 本台账为**登记与分层**，不新增数学结论
- ⚠️ 第 3 层为**我的分析性判断**（"可能可证"），不是承诺；每条已附"为何可能＋风险"
- ⚠️ 第 4 层的 `📖?` 类（外部文献）在论文引用前**必须逐字核** —— 档案已有此类误述教训
- **未用** RH；**未改**任何原档 ✓
