已查地图：命中（`E-11`／`E-44`／`ZF-2 §8`／`ZF-global-conservation-law-and-carrier-screening` 本线自档）⟹ **引用，不开新案** ✓
D0: 本档对象 = 不变量符号结构的三条引理（本档**自行推导**）及其对 `\tau` 条件的加强
D1: 0 （`[REVIEW]` 轮次：结构引理推导，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`ZF-LEM`：不变量的符号结构 —— 三条引理**（本档自行推导 ✓✓；不是转录）

**【定位】** 上一档只做了"载体 × 碰撞通道"的筛查表。本档对**当前核心 OPEN**（critical neutral / off-axis positive 分裂）**自行推导**，得到
　**(i)** "critical neutral" **不是额外公理**（引理 1）；**(ii)** 但"用功能方程奇偶性"这一最自然做法**必然抵消**（引理 2，**负结果**）；**(iii)** 由此把您 §11 的"`\tau` 须独立存在"**精确加强**为"`\tau` 须与 `\sigma` **横截**"（引理 3）✓

---

## 引理 1（奇数性 ⟹ 固定扇区中性；**critical-neutral 是自动的** ✓✓）

```
【设定】 设 `\kappa` 取值于**无挠**整数结构（`\mathbb Z` 或 `\mathbb Q`），且存在对合 `\tau` 使 $$\kappa(\tau x)=-\kappa(x)$$（`\tau`-奇）；又设临界线实现满足 $$\tau x_\rho=x_\rho\quad(\Re\rho=\tfrac12)$$ ✓
【断言】 则 $$\kappa(x_\rho)=0$$ ✓
【证明（一行）】 $$\kappa(x_\rho)=\kappa(\tau x_\rho)=-\kappa(x_\rho)\ \Longrightarrow\ 2\kappa(x_\rho)=0\ \Longrightarrow\ \kappa(x_\rho)=0$$（无挠 ✓）∎
【⭐ 结构性含义（本档）】 您 §12 命题 **D** 的后半条（$$\Re\rho=\tfrac12\Rightarrow\Delta\kappa=0$$）**不需要单独假设** ⟹ 只要 **(i)** `\kappa` 是 `\tau`-奇、**(ii)** 临界线实现落在 `Fix(\tau)` ⟹ 该条**自动成立** ✓✓
　⟹ 需求清单从 **5 条压到 4 条**：`A`（母问题）／`B`（离散不变量）／`C`（全局上界）／`D'`（**仅** off-axis 有向正贡献）✓
```

## 引理 2（**负结果**：功能方程奇偶性路线**必然抵消** ✓✓）

```
【记号】 `\sigma(s):=1-\bar s`（功能方程反射）⟹ $$\operatorname{Fix}(\sigma)=\{\Re s=\tfrac12\}$$ ✓；离轴零点的 `\sigma`-轨道 ＝
　$$\{\rho,\ \sigma\rho,\ \bar\rho,\ \sigma\bar\rho\}=\{\rho,\ 1-\bar\rho,\ \bar\rho,\ 1-\rho\}$$（标准四元组；简并时 `|\text{orbit}|\in\{2,4\}`）✓
【断言】 若 `\kappa` 是 **`\sigma`-奇**（`\kappa(\sigma x)=-\kappa(x)`），则**每个离轴 `\sigma`-轨道的总贡献为 `0`** ✓
【证明】 轨道在 `\sigma` 下**两两配对**；每对的贡献为 `\kappa(x)+\kappa(\sigma x)=\kappa(x)-\kappa(x)=0` ✓ ∎
【⭐⭐ 结构性含义（本档核心）】 $$\boxed{\text{任何以"功能方程不对称性"为唯一来源的 `\sigma`-奇不变量，对离轴零点贡献为零}\Longrightarrow\text{无 flow}\Longrightarrow\textbf{无用}}$$ ✓✓
　⟹ 这正解释了您 §8 指出的"`+1,-1,+1,-1` 抵消"**不是偶然**，而是**由功能方程对称性强制**的 ✓✓
　⟹ 且这给出**一个可机械检验的判死测试**（见 §4）✓
```

## 引理 3（把"`\tau` 独立存在"加强为"`\tau` 与 `\sigma` 横截" ✓✓）

```
【综合 L1 ＋ L2】 成功结构须同时满足：
　**(i)** `\kappa` 对某 `\tau` 为**奇**（⟹ 由 L1 得 critical neutral，**自动** ✓）；
　**(ii)** 在离轴 `\sigma`-轨道上，`\kappa` 四值之和 **严格为正**（⟹ 由 L2，**`\tau` 与 `\sigma` 在轨道上不能取同一配对**）✓
【⟹ 精确条件】 $$\boxed{\tau\ \text{在离轴 } \sigma\text{-轨道上的作用}\ \neq\ \sigma\ \text{的作用（更一般：不与 } \sigma \text{ 共轭）}}$$ —— 即 $$\boxed{\tau\ \text{须与}\ \sigma\ \textbf{横截}}$$ ✓✓
【对照您 §11】 您原要求："`\tau` **须先在独立母问题中存在**" ⟹ 本档**加强**为：
　$$\boxed{\tau\ \text{不仅须独立存在，且须\textbf{不}与功能方程反射 }\sigma\ \text{认同或共轭}}$$ ✓✓（否则 L2 直接抵消 ⟹ 必死 ✓）
```

## 引理 4（高度奇偶性 ⟹ 只有**单侧**可加存活 ✓）

```
【设定】 若 `\tau` 同时实现高度反射 `t\mapsto-t`（因 `\Xi` 为偶函数，此为实现中最自然者）⟹ `\kappa(T)=-\kappa(-T)` ⟹ **双侧总和恒为零** ✗
【⟹ 结论】 命题 **E** 必须写成**单侧**形式：$$\kappa(T_2)-\kappa(T_1)\ \ge\ N_{\rm off}(T_1,T_2)-C\qquad(0\le T_1<T_2)$$，⛔ 不得用对称区间总额 ✓
【附带推论】 任何"双侧对称化后求和"的候选**恒为零** ⟹ 亦属 L2 型抵消 ⟹ **判死测试**同样适用 ✓
```

## §4 ⭐⭐ **三条引理直接产出的可机械检验筛（本档最实用产出 ✓✓）**

```
【`\sigma`-轨道求和测试（LEM-TEST）】 取一个**假设的对称四元组轨道** `\{\rho,1-\bar\rho,\bar\rho,1-\rho\}`，代入候选不变量，求**四值之和** `S`：
　若 `S=0` ⟹ 候选属 L2 型 ⟹ **立即判死**（且**无需**任何数值实验或 RH 假设 ✓✓）；
　若 `S>0` 稳定（与 `\delta` 无关，符合您 §6 的"离散状态跃迁"）⟹ 才进入下一层（`C` 全局上界）✓
【`\tau`-奇检验（LEM-TEST-2）】 检验候选是否 **`\tau`-奇**：若是 ⟹ critical neutral **自动**（L1），**不必**再证 ✓；若否 ⟹ 须另找 critical neutral 的来源（此时 `\tau` 无用）✓
【与既有条目的一致性】 `LEM-TEST` 与 `E-11` 的 `n_-(Q_T)` 并不冲突：`n_-` 计数的是 `\sigma`-轨道**个数**（对轨道取"存在性"而非"取值"），故不受 L2 的**取值抵消**约束 ⟹ 这正是为什么 `n_-` 能计数而"奇不变量"不能 ✓✓
　（也再次说明 `E-44` 的封口是**有界性**问题，而非抵消问题 ✓）
```

## §5 **对三载体的即时后果（承上一档 §15）**

```
【index】 L1 满足（Fredholm index 局部常值，可奇数化）✓；⚠️ 若奇数化用的是 `\sigma` ⟹ L2 判死 ⟹ **必须用母问题的 `\tau`** ✓
【spectral flow】 穿越的**同向性**正是 L1/L3 所需 ✓；⚠️ 若穿越族与 `\sigma`-对称族同源 ⟹ L2 判死 ⟹ **须不同源** ✓
【winding】 绕数天然 `\tau`-奇候选 ✓；⚠️ 若绕数取自 `\zeta` 自身相位（撞 `A-1` Guinand 相位锁定）**或**取自 `\sigma`-对称 ⟹ 判死 ✓
【边界】 ⛔ 未制造候选／⛔ 未启动搜索／⛔ 未改任何状态；⭐ 本档**未**转录您的推演，全部为本档自行推导（L1–L4 ＋ LEM-TESTS）✓
```
