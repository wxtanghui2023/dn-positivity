已查地图：未覆盖（关键词: 双组合律|加性乘性律|独立问题枢轴|组合律扫描）—— 可开档，首行须照抄本行

# **KERNEL-HUNT-2**：独立问题枢轴 · **双组合律量**普查（第一刀）

**唐先生令（2026-09-22 22:32）**：
① ✅ **收口 O1-1**，记为「**两墙退化计数路线 = FAIL／精确封锁**」✓；⭐ 核心原理：**`β-sensitive ⇏ β-operational`** ✓✓；
② ⛔ **不建议 (b)**：第二类计数若仍走 `从零谱定义 → 重数/退化 → β` 而无新算术输入，只是**换一个谱计数符号**，仍撞同一堵墙 ✓；
③ ⛔ **暂不回到"没有明确新接口"的 KERNEL-HUNT 实验场** ✓；
④ ⭐ **KERNEL-HUNT 改入口**（照录）：
　$$\boxed{\text{独立问题}\ \to\ \text{产生新 quantity}\ \to\ \text{证明 arithmetic realization}\ \to\ \text{最后检查 RH interface}}$$ ✓✓
　（即 **"independent problem > adjacent asset > RH relevance"** 顺序，**不是**从 RH 出发反造 quantity ✓）
⑤ **下一刀（本档执行）**：**直接找"独立问题中天然出现、且同时具有 additive × multiplicative 两种组合律"的量**——
　判据（照录）：这是目前**最可能避开**〔**β-blind**／**谱反推**／**RH-equivalence**〕**三堵旧墙**的入口 ✓✓

D0: 本档对象 = **双组合律量的普查与逐项判定（KERNEL-HUNT-2 第一刀）**（档案**零覆盖** ⟹ 新档；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算（解析）／零数值／未写程序**／未做数值搜索／未开案 ✓

---

## §1 收口前置（依令 ①②③ ✓）

```
【O1-1】 = **FAIL／精确封锁**（另档 §5 存档 ✓）：压缩失败链 ＝
　`\mathcal N_{\rm deg} \overset{形式}{\to} β`，但**不**→{独立算术定义／稳定两墙间计数变化／Euler-乘性结构}，
　且**残留的 β–RH 关系是 equivalence-level** ⟹ 命中 `RH-EQUIVALENT-TOO-STRONG` ✓✓
【第二类计数】 ⛔ **不开**：须先证〔prime/Euler 侧定义 ∨ 非等价级 β-observable ∨ 新加/乘律〕之一 ✓（依令 ② ✓）
```

## §2 双组合律量普查（**候选 + 逐项过筛** ✓✓）

```
【过筛口径（沿用既有筛，不新造 ✓）】
　(a) 三墙：**β-blind？／仅从零谱反推？／核心是否 equivalence-level？** ✓
　(b) **是否已有成熟机器**（⟹ 吸收，`D-11` 继承规则 ✓）
　(c) 若其缺口＝**主流已知开放问题** ⟹ **不构成"未覆盖接口"**（既有准入判据 ✓）
```

| # | 候选量（独立问题） | 加性律 | 乘性律 | 算术实现 | 判定 |
|:--|:--|:--|:--|:--|:--|
| **①** | **Euler 特征 `χ`**（单纯复形／簇；独立问题＝**点计数**） | ✓ 不交并：`χ(A ⊔ B) = χ(A)+χ(B)` | ✓ 积：`χ(A × B) = χ(A)·χ(B)` | ✓✓ **Lefschetz 迹**：`\#X(\mathbb F_q) = Σ (−1)^i \mathrm{tr}(\mathrm{Frob}\,|\,H^i)` | ⛔ **DEAD**：(b) **机器完备**（Weil 猜想已证；档案 `Lefschetz` **22 档** ✓）；且档内**第一断裂**已说明 **ζ 侧 `α_p ≡ 1`** ⟹ 该机器**不能搬到 ζ** ✓✗ |
| **②** | **行列式／积和式 `\det, \mathrm{per}`**（独立问题＝**匹配计数／代数复杂度**） | ✓ 对行**多重线性** | ✓ `\det(AB)=\det A\det B`；`\mathrm{per}(A⊕B)=\mathrm{per}A·\mathrm{per}B` | ✓ 组合计数（完美匹配 ✓） | ⛔ **DEAD**：(b) 成熟代数；其**缺口＝Valiant `per`-vs-`det`** ⟹ **主流开放问题** ⟹ (c) **不构成未覆盖接口** ✓✗ |
| **③** | **Weil／Arakelov 高度 `h`**（独立问题＝**算术几何／计数**） | ✓ 次加性 `h(a+b) ≤ h(a)+h(b)+O(1)` | ✓ 乘性→加性 `h(ab) = h(a)+h(b)+O(1)` | ✓✓ **各 place 局部项** | ⛔ **DEAD**：(b) **Arakelov 理论成熟**（档案 `Arakelov` **47 档** ✓）；其核心（Vojta 型）**主流开放** ⟹ (c) ✗ |
| **④** | **熵 `H`**（独立问题＝信息论） | ✓ 独立可加 | ⚠️ 张量积下行为 | ✓（率区／编码） | ⛔ **DEAD**：(b) 成熟；**熵区域刻画＝主流开放**（且 `CROSS-0` 已判 **copy lemma／MAXE 机器吸收** ✓✗） |
| **⑤** | **混合判别式／`AF` 型量** | ✓ 多重线性 | ⚠️ | ✓ | ⛔ **DEAD**：`CROSS-0` 已判"**通用机器吸收**"（`van Handel`：源自**代数结构** ✓✗） |
| **⑥** | **Dirichlet 卷积环**（乘法函数 × 逐点积 vs 卷积和） | ✓ 卷积 | ✓ 逐点积 | ✓ 素数侧 | ⛔ **DEAD**：(b) **经典机器**（且这正是 ζ 的 Euler 积／Dirichlet 级数**双表述本身** ✓）；`第一断裂` 已说明其 ζ 侧退化 ✓✗ |

```
【第一刀结论】 ⛔ **已枚举候选全部 DEAD，且死因具体**（各自 = 成熟机器吸收 或 主流开放问题 ⟹ 不构成未覆盖接口）✓
【生成器状态（不升格）】 依"**未找到 ≠ 不存在**"（入口纪律 ✓）：**不宣布"不存在此类量"**；
　但**不升格**，其准入条件＝**必须先给出满足下列之一的具体候选**（＝令 ② 的三条件在"量"层面的版本 ✓）：
　$$\boxed{\text{prime/Euler 侧定义}\ \ \text{或}\ \ \text{非等价级 }β\text{-observable}\ \ \text{或}\ \ \text{新的加/乘律}}$$ ✓✓
```

## §3 ⚠️ 结构性读法（**读法，非定理** ✓；支持唐先生诊断）

```
【观察（连续四层元生成器）】 `换对象 → 换交互 → 换表示 → 双组合律` **全部**落到
　（甲）**成熟机器** 或 （乙）**主流开放问题** ⟹ 两次都在既有准入判据下**不构成未覆盖接口** ✓✓
【与档案既有诊断一致】 `C-34` 逐字：攻缺口与攻两侧墙**在此点重合**（结构性原因＝`C_0` 是紧的）✓
　⟹ 本档**为该"NO-GO 博物馆"风险提供了结构解释**：生成器搜索实际在**测绘当前前沿**，
　而准入判据（独立问题 ＋ 可证 ＋ 非重命名 ＋ 非主流开放 ＋ 新接口）**恰好是"前沿"的定义条件** ✓✓
【⛔ 严格边界】 这只是**读法**；**不**写成"不存在新接口"／"已全局封口"（守 `V290` 禁语 ✓）；
　亦**不**据此停止搜索（依令 ⑤ 仍以"具体候选"为准 ✓）
```

## §4 边界与回查（✗✓）

```
✗ 零计算（解析）／零数值／未写程序／未做数值搜索／**未开案** ✓
✗ ⛔ 不把"全部候选 DEAD"记为战绩；⛔ 不在无具体候选时宣布生成器"存在" ✓；⛔ 守 `V290` 禁语 ✓
⚠️ ③ 的 `h(ab) = h(a)+h(b)+O(1)`／`h(a+b) ≤ …` 为标准事实（**凭记忆** ⚠️，未逐字核档）；① 的 Lefschetz 迹为标准事实 ⚠️
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 双组合律     命中文件数=0 ⟹ **本档新增** ✓
　技术词 加性乘性律   命中文件数=0 ⟹ **本档新增** ✓
　技术词 独立问题枢轴 命中文件数=0 ⟹ **本档新增** ✓
　技术词 组合律扫描   命中文件数=0 ⟹ **本档新增** ✓
　技术词 操作性       命中文件数=1 ⟹ **自命中**（本日 O1-1 档）⟹ 不主张 ✓
【地图回查】 关键词 4 项 = **未覆盖** ⟹ 可开档 ✓；档案覆盖度：`Arakelov` **47**／`Lefschetz` **22**／`permanent` **1**／`高度函数` **0**／`积和式` **0** ✓
【档级引用】 `V192`（β 只能经退化/重数进入；`RH ⟺ 无本质退化`）✓｜`V290`（`O1-1` 两墙 ＋ 禁语）✓｜
　`V194` F1（`RH-EQUIVALENT-TOO-STRONG`）✓｜`C-34`（攻缺口与攻两墙重合）✓｜`CROSS-0`（AF／non-Shannon 机器吸收）✓｜
　`ASSETS-REGISTRY` `C-1`（异境退化计数）✓
