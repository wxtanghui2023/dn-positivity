已查地图：命中（`E-31`／`E-25`–`E-30`／`AMEND-6/7/8` 本线自档）⟹ **引用，不开新案** ✓

# **卡 4 `S4-PRE-EXACT` 预注册**（三钉机械化；**零计算** ✓；`COMPUTATION` 仍 **LOCKED** ✓）

**唐先生令（2026-09-23 14:07 ✓✓）**：目前**不应计算卡 4**；先做 `S4-PRE-EXACT`，审核重点从"形式上能否写出 `F`"**升级**为 $$\boxed{\text{能否在计算前把允许的对象空间完全钉死}}$$ ✓；
　⚠️ **`Q3`（RH 接口更间接）必须保留在账本**，⛔ 不得因卡 4 在 `S4` 上可能更干净就包装成"RH 接口更强" ⟹ **推动卡 4 的理由只能是其潜在的 `S4` 新对象生成能力** ✓✓

D0: 本档对象 = **卡 4 的 `S4-PRE-EXACT` 预注册（`(f,g)`／矩阵／`\mathcal R_{\rm old}` 三钉）**（引本线自档；**未开 bridge 案** ✓）
D1: 0 （`[REVIEW]` 轮次：预注册定义，不主张新自由度 ✓）
FREEZE-ACK: D1=0 ✓
[REVIEW]

---

## §1 **钉 1：`(f,g)` 事前固定（⛔ 不得运行后从大族里挑"表现异常"的形式 ✓）**

```
【候选族（事前写死 ✓）】 水平 1（`SL_2(\mathbb Z)`）新形式的权 `k\in\{12,16,18,20,22\}` ⟹ 该权下**唯一归一化 Hecke 特征形式**，记 `\Phi_k` ✓（系数为整数 ✓）
【候选对集合（事前写死 ✓）】 $$\mathcal P_{\rm pre}:=\{\{f,g\}: f=\Phi_k,\ g=\Phi_l,\ k<l,\ k,l\in\{12,16,18,20,22\}\}\quad|\mathcal P_{\rm pre}|=10$$ ✓
【规则（照录精神 ✓✓）】 ⛔ **不挑选**；**全部 `10` 对均须检验** ✓（可**同时报告**每对结果，但⛔ 不得只报"命中的那对" ✓）
```

## §2 **钉 2："同一组系数"＝精确矩阵问题（沿用卡 1 的可审计标准，⛔ 不因换 SOURCE 而降级 ✓）**

```
【离散坐标】 坐标＝**素数下标**（区间内素数按升序编号）✓；递推形式：$$R_J(x)_h:=\sum_{j=0}^{J}c_j\,x_{h+j}=0$$（`x_h:=a_{p_h}(f)`）✓
【尺度（事前写死 ✓）】 `X_1`: 素数 `\le10^4`（`\pi=1229`）｜`X_2`: 素数 `\le2\times10^4`（`\pi=2262`）✓
　**方程位置（两尺度一致）**：$$h=0,\dots,H_{\rm row},\qquad H_{\rm row}=1220$$（事前固定，独立于数据 ✓）
【四块堆叠（照录 ✓✓）】 $$M^{(f,X_1)},\ M^{(f,X_2)},\ M^{(g,X_1)},\ M^{(g,X_2)};\qquad M=\begin{pmatrix}M^{(f,X_1)}\\M^{(f,X_2)}\\M^{(g,X_1)}\\M^{(g,X_2)}\end{pmatrix}$$ ✓
【判定（**与卡 1 逐字同一标准** ✓✓）】 $$\boxed{\exists\,c\in\mathbb Z^{J+1}:\ Mc=0,\quad c_J=1}$$；`J=1,2,3,4`（`J_{\max}=4` 与卡 1 一致）✓
【维度】 `M` 为 `(4\times1221)\times(J+1)` ⟹ 高度超定 ⟹ 命中即强信号 ✓
```

## §3 **钉 3（最大风险）：`\mathcal R_{\rm old}` 必须先成为有限、可计算的对象 ✓✓**

```
【问题（照录 ✓）】 "不是 Hecke 关系"是**数学描述**，**还不是机器可判定的 `S4-EXACT`**；若不给生成规则，可能出现 命中 → 事后解释为某已知 Hecke/symmetry 关系 ⟹ `S4` 重新产生 post-hoc 自由度 ✗
【本档机械化（**在计算前写死** ✓✓）】
　① **吸收判别式（逐序列核，非并集生成）**：命中向量 `c` 判为**被吸收** ⟺ $$\boxed{c\in\displaystyle\bigcup_{x\in\mathcal L_{\rm old}}\ker_{\mathbb Q}\big(H_x\big)}$$，
　　其中 `H_x` ＝ 序列 `x` 在**同一坐标、同一 `J`、同一 `h` 范围、同一双尺度**下构造的**单块矩阵** ✓
　　即：只要 `c` 恰是"某个事前列出的经典结构所必然满足的关系"，就**不构成新对象** ✓
　② **事前写死的经典结构库 `\mathcal L_{\rm old}`（有限、可计算 ✓）**：
　　Ⅰ 常序列 `x_h\equiv1`（Hecke 归一化/平凡）｜Ⅱ `x_h=h`（下标线性）｜Ⅲ `x_h=p_h`（素数列本身）｜
　　Ⅳ `x_h=\log p_h`｜Ⅴ `x_h=\sqrt{p_h}`｜Ⅵ `x_h=\varepsilon_h:=(-1)^{h}`（下标奇偶）｜
　　Ⅶ `x_h=a_{p_h}(\Phi_m)` 对**库内其余**形式 `\Phi_m`（吸收"全体形式共有"的关系 ✓）
　③ **判定顺序（机械化 ✓）**：先算 `\exists c`；**若命中**，再做 ① 的吸收判别；**仅当 `c\notin\cup\ker` 时**，才登记为 **`FORCED OBJECT` 候选** ✓✓
【⚠️ 诚实边界（照录精神 ✓）】 吸收判别**只覆盖事前列出的结构**（这正是"事前"的意义 ✓）；⛔ 不得在事后**增补** `\mathcal L_{\rm old}` 条目 ✓
```

## §4 **反事后条件（照 `AMEND-7` 逐条，计算后填写，⛔ 不得改写 `S4-PRE` ✓）**

```
(1) `c` 的形式**非预选拟合数据**：`J_{\max}=4`、首一 `c_J=1`、四块同系数 —— 均在计算前固定 ✓
(2) **第二尺度迫使同一结构**：`X_1`／`X_2` 同系数同 `h` 范围 ⟹ 机械满足 ✓
(3) **非旧机制重命名**：由 §3① 的吸收判别式机械化 ✓
(4) **对象域脱离实验**：`c` 若命中且未被吸收，则其作用对象为**新形式空间**（既有对象空间 ✓；见 `E-31` `Q2`）✓
```

## §5 **状态 ＋ 待办（照录 ✓✓）**

```
$$\boxed{\begin{array}{ll}\text{SOURCE qualification}&\checkmark\\S4\text{-format}&\checkmark\\S4\text{-EXACT}&\textbf{PENDING}\\\text{computation}&\textbf{LOCKED}\end{array}}$$
【`S4-EXACT` 唯一待办（照录 ✓✓）】 $$\boxed{(f,g),\ J,\ X_1,X_2,\ \text{数据坐标},\ \mathcal R_{\rm Hecke},\ \mathcal R_{\rm sym}\quad\text{全部预先固定且可判定}}$$ ✓
　本档已完成：`(f,g)`＝`\mathcal P_{\rm pre}`（10 对全检）✓｜`J\le4` ✓｜`X_1,X_2` ✓｜数据坐标＝素数下标，`h=0..1220` ✓｜
　`\mathcal R_{\rm Hecke}\cup\mathcal R_{\rm sym}` **以 `\mathcal L_{\rm old}` 吸收判别式机械化**（Ⅰ–Ⅶ）✓ ⟹ **待第三方（唐先生）核可**，方可进入 `RUN` ✓
【⛔ 本档不做】 未计算／未挑形式／未改 `S4`／未增补 `\mathcal L_{\rm old}`／⛔ **未把"更间接的 RH 接口"包装成更强** ✓
【状态】 $$\boxed{\text{Card 1}=\text{CLOSED};\quad \text{Card 2}=\text{HOLD};\quad \text{Card 4}=\text{S4-PRE-EXACT PENDING};\quad D_{\rm new}=0;\quad \text{COMPUTATION}=\text{LOCKED}}$$ ✓
```
