# C380-LAYER5-GATE / Paper-0 — 三候选、零计算、逐项判死

已查地图（**先跑后写** ✓）：`C380-LAYER5-GATE-FREEZE.md`（**G0–G5 ＋ 三出口 ＋ STOP** ✓✓）、`C380-SUPPORT-CLOSURE-AND-LAYER5-GATE.md`（**k_min = 5 硬收官** ✓✓）、`K4-T-a-CERTIFICATE-RESULT.md`（**证书** ✓✓）、`C3891`（**T1 = CLOSED-NEGATIVE：矩/T-system 不适用** ✓✓）、`C3899h`（**H1/Fejér 引擎不迁移；步长 4 核夹不死** ✓✓）、`C380-0`（**多项式型跨层耦合 = 重包装（奇偶障碍）** ✓✓）、`C3899k`（**S1 独立性审计** ✓✓）

D0: 本档对象 = **C-380-137：Paper-0 三候选逐项判死**（档案已有对象 `k_min = 5` 与三候选形态的**关系**；非重命名、非新对象）
D1: 0
FREEZE-ACK: 本档即冻结审计（连续 D1=0 ＝ 35 ≥ 3；本档为**审计登记**，未引入新自由度）

---

## §0 令登记（照录唐先生 2026-09-22 18:47 ✓✓）

```
【总方针】 **不要马上算**；把 `C380-LAYER5-GATE` 从"概念口号"推进成**可杀死、可晋级的研究闸门** ✓✓
【本轮】 `Paper-0`：**三候选、零计算、逐项判死** ✓✓ —— 产物 = **知道下一刀该是数学证明／有限证书／立即换轨** ✓
【候选（只允许 3 个）】 ① 五点插值缺陷 ② 五层诱导的离散消元/差分量 ③ 五层 Chebyshev 频率结构的整数化量 ✓
【五过滤器（逐项过）】 **NEW ∧ NONTRIVIAL ∧ ARITHMETIC ∧ QUANTITATIVE ∧ RH-INTERFACE** ✓✓
   ⚠️ 不要求现在证 RH 接口；但**必须存在明确的接口对象**，而非一句"可能与 RH 有关" ✓
【硬 STOP（任一即 DEAD，不计算）】 只是 k=5 重命名 ｜ 只是 Vandermonde 非零 ｜ 只是矩阵正定 ｜
   只是已有偶层约束的线性组合 ｜ 只是数值分离 ｜ 只能得 Q > 0 而无算术内容 ｜
   RH 接口仍依赖旧的 local→global 跳跃 ✓✗
【目标重述（照录）】 **E_even ⟹ 5-layer rigidity ⟹ 𝒬₅**；先把 𝒬₅ 做成**独立、可证明、可复验的新数学对象** ✓✓
【框架（照录）】 C-380 **负责产生"新结构"，不负责强行把它变成 RH 证明** ✓✓
【期限约束】 今日 **2026-09-22**，Phase-IV 截止 **2026-10-20** ⟹ 候选须考虑 paper closure → certificate 能否在窗口内完成 ⚠️
```

## §0.1 输入事实与记号（LOCKED，不重证 ✓）

```
x_j = cos²φ_j ∈ (0,1)；u_j = 2x_j−1 = cos2φ_j；**z_j = u_j² = cos²2φ_j**；t_j := 2z_j−1 = cos4φ_j ✓
偶层约束： F_{2r} = Σ_j T_{2r}(u_j) = Σ_j cos(4rφ_j) = **Σ_j T_r(t_j) ≤ 1/2**，r = 1..12 ✓
已知（证书级）： **E_even ⊆ {k = 5}** ⟹ **t_1<…<t_5 严格互异**（不得重证/优化/重算 ✓✗）
```

---

## §1 候选 ①｜五点插值缺陷（Interpolation defect）

| 项目 ✓ | 内容 ✓ |
|---|---|
| 新量 𝒬 ✓ | 五点插值的**阶-4 缺陷**：`Φ_r := Σ_j λ_j T_r(t_j)`（λ_j := 1/Π_{k≠j}(t_j−t_k)）；r=5..12 为"超出四点的插值误差" ✓ |
| 五层来源 ✓ | 仅当 **5 个互异节点**时 λ_j 与"4 阶插值"才良定义（k=5 之前节点相撞 ⟹ λ 奇异 ✗）✓ |
| 非旧量？ ✗ | ✗✗ **失败**：Φ_r 是**恒等式**（对一切 5 节点构型成立），非约束强制量 ⟹ 属**重编码** ✓✗ |
| 可证明命题 ✓ | 恒等式：`Σ_j λ_j T_r(t_j) = D₄[T_r]`（4 阶差商）；且 **r ≤ 4 时恰等于 2^{r−1}**（r=1,2,3,4 → 1,2,4,8）✓✓ |
| 算术入口 ✗ | ✗ 无：右端 2^{r−1} 是 **Chebyshev 内部整数**，非素数/同余/乘法结构 ✓ |
| 谱入口 ✗ | ✗ 无 ✓ |
| RH 接口 ✗ | ✗ **不存在明确接口对象**（只能写"或许与矩结构有关" ⟹ 按令**不启动**）✗✗ |
| 计算需求 ✓ | 无（纯恒等式；且计算也无新内容 ✗） |
| **最大失败点** ✓ | 恒等式与约束**无耦合**：Φ_r 不引用 F_{2r} ≤ 1/2 ⟹ 不携带可行性信息 ✗✗ |
| **STOP 条件** ✓ | 已触发：**属"已有偶层/矩结构的重编码"**〔`C3891`：T1 矩/T-system = **CLOSED-NEGATIVE**（量级错配；Carathéodory 只给下界；只能事后认证 ✗✓）〕 |

**判决 ①：DEAD —— 由 `C3891` 直接引杀（重编码 ＋ 矩空间路线已闭）** ✗✗

---

## §2 候选 ②｜五层诱导的离散消元 / 差分量（Node-dependent elimination coefficients）

| 项目 ✓ | 内容 ✓ |
|---|---|
| 新量 𝒬 ✓ | **节点依赖（非固定）**系数 λ_j 的消元组合；核心候选 = `Σ_j λ_j T_r(t_j)` 与 `Σ_j λ_j`、`Σ_j λ_j t_j` 的约束耦合 ✓ |
| 五层来源 ✓ | k=5 ⟹ λ_j 有限且**符号交错**（λ_j 符号 = (−1)^{5−j}）⟹ 天然"交替符号"结构 ✓✓ |
| 非旧量？ ✓ | ✓✓ **通过形式门**：λ_j **依构型而变** ⟹ **不是**固定非负组合（`C3899b` 的零均值障碍**不适用**）✓✓；亦**非多项式** ⟹ 不受 `C380-0` 奇偶障碍限制 ✓✓ |
| 可证明命题 ✓ | ⓐ **恒等式组（已数值校验 ✓）**：`Σ_j λ_j = 0`；`Σ_j λ_j t_j = 0`；`Σ_j λ_j T_r(t_j) = D₄[T_r]`（**4 阶差商**），故 **r ≤ 3 时 = 0**、**r = 4 时 = 2³ = 8**、r ≥ 5 为 T_r 在 5 节点上的 4 阶差商 ✓✓<br>ⓑ ⚠️ **原拟"弱定理"已撤回**：曾由（错误）恒等式 `Σλ_j t_j = 1` 推出 `Σ_j|λ_j| ≥ 1/2` ✗✗；正确恒等式为 `Σλ_j t_j = 0` ⟹ 该推导**整体失效**（`Σλ_j(t_j−c) = 0` 对一切 c ⟹ **无任何定量下界**）✗✗ |
| 算术入口 ✗ | ✗ **失败**：λ_j 是构型的**有理函数**（非整数/非 Möbius）；正确恒等式均为**annihilation（消去）型** ⟹ 不产生整数/同余内容 ✗✗ |
| 谱入口 ✗ | ✗ 无 ✓ |
| RH 接口 ✗ | ✗ 无明确接口对象（仅"局部系数/离散量"）✗ |
| 计算需求 ✓ | **暂不**：按令"没有写出第一条非平凡定理者不准进入计算" ⟹ 现有 ⓑ 虽为真但**方向错误/无效** ✗ |
| **最大失败点** ✓ | **循环性** ＋ **annihilation 型**：λ_j 由节点决定，而节点正是被约束对象 ⟹ 组合退化为"节点自己的消去关系" ✗；且 Σλ_j T_r = 0（r ≤ 3）与 Σλ_j t_j = 0 均为**消去**（无定量内容）✗✗ |
| **STOP 条件** ✓ | 已触发：**无"第一条非平凡定理"**（原拟弱定理已撤回 ✗）；且恒等式与约束**不耦合** ⟹ 属消去/重编码 ✗✗ |

**判决 ②：DEAD —— 形式门通过（节点依赖 ⟹ 非固定组合；非多项式 ✓✓），但**恒等式全为 annihilation 型**、无约束耦合、无首条非平凡定理（原拟定理已因错误恒等式**撤回**）⟹ 按硬规则**不得进入计算**，且无晋级价值 ✗✗**

⚠️ **本档自检记录（必记）**：初稿曾写 `Σλ_j t_j = 1` 与 `Σλ_j T_r = 2^{r−1} (r ≤ 4)`，并由之导出"弱定理" ✗✗；
经**数值校验**（随机 5 节点）发现 **`Σλ_j t_j = 0`** ≠ 1 ⟹ 已按正确 Lagrange/差商恒等式改正、**并撤回该"定理"** ✓；
（纪律：正是 `TOOLS.md` 的"**结果异常先怀疑自己的分支**" —— 本档属**自纠**，非外部反馈 ✓✓）

---

## §3 候选 ③｜五层 Chebyshev 频率结构的整数化量（Integrality）

| 项目 ✓ | 内容 ✓ |
|---|---|
| 新量 𝒬 ✓ | **代数整数/整数夹逼**：对共谱构型（4φ_j = 2πp_j/q），T_r(t_j) = cos(4rφ_j) 为**代数整数**；Galois-闭（整轨道并）时 `Σ_j T_r(t_j) ∈ Z` ⟹ 配合 ≤ 1/2 ⟹ **≤ 0** ✓✓（= `C3849` 机制） |
| 五层来源 ✓ | 整数性不依赖 k=5；但"五层"是本轨当前对象 ⟹ 关联弱 ⚠️ |
| 非旧量？ ✗ | ✗✗ **失败**：即 `C3849` 的 Fejér/整数夹逼机制本身；且档案**已评**其在五节点偶频问题上的迁移：**步长 4 核存在但夹不死** ✓✗ |
| 可证明命题 ✓ | 已在 4 节点/边界情形给出**精确刚性**（`C3849`：⟹ z_j ∈ μ_9 ⟹ E_0 ∩ E_even = ∅）✓✓ |
| 算术入口 ✓ | ✓ 部分：**代数整数/整轨道**（真正的整数结构）✓ —— 但**仅在共谱子族**上 ✗ |
| 谱入口 ✗ | ✗ 无 ✓ |
| RH 接口 ✗ | ✗ 无（整数性属局部代数结构）✗ |
| 计算需求 ✓ | 无（且无需：机制已被评 ✗） |
| **最大失败点** ✓ | ① 共谱 = **零测子族** ⟹ 给不出通用定量结论 ✗✗；② 迁移失败有**明确原因**：质量 5（非 4）＋ 约束方向 ≤ +1/2（非 ≤ −1/2）⟹ `Σ_j K^{(4)}_m = 5 + 2Σ(1−j/m)F_{4j}` **夹不死**（要 ≤ 0 需 Σ(1−j/m)F_{4j} ≤ −5/2，而 F 可达 −5 ⟹ 该不等式**平凡成立** ⟹ 核不给信息）✗✗ |
| **STOP 条件** ✓ | 已触发：**重开已评机制**（`C3899h`：H1/Fejér **不迁移**；核支撑 ⊂ 4Z ⟹ 对奇层无信息）✗✗ |

**判决 ③：DEAD —— 由 `C3899h` 直接引杀（机制已评、迁移失败原因明确、且仅覆盖零测子族）** ✗✗

---

## §4 五过滤器审计矩阵（✓/✗）

| 候选 ✓ | NEW ✓ | NONTRIVIAL ✓ | ARITHMETIC ✓ | QUANTITATIVE ✓ | RH-INTERFACE ✓ | 判决 ✓ |
|---|:--:|:--:|:--:|:--:|:--:|---|
| ① 插值缺陷 ✓ | ✗ | ✗ | ✗ | ✗ | ✗ | **DEAD**（`C3891` 引杀）✗✗ |
| ② 节点依赖消元 ✓ | ✓ | ✗（恒等式全为消去型） | ✗ | ✗ | ✗ | **DEAD**（自检撤回后无首条定理）✗✗ |
| ③ 整数化量 ✓ | ✗ | ✓ | 部分（仅共谱） | ✗ | ✗ | **DEAD**（`C3899h` 引杀）✗✗ |

**综合：三候选**全部 DEAD**，无任何候选达 {NEW ∧ NONTRIVIAL ∧ ARITHMETIC ∧ QUANTITATIVE ∧ RH-INTERFACE} ⟹ 无 EXIT-A PROMOTE** ✗✗

---

## §5 ⭐ 全局阻碍（本档最重要的判断 ✓✓）

```
【绑定约束 = ARITHMETIC】 三个候选**全部**在 "算术入口" 一项失败 ⟹ 这是**绑定项** ✓✓
   原因（结构性）：C-380 的数据是 **cos(角度)** 的纯分析/代数对象 ⟹
   **不含素数/同余/乘法结构** ✗；唯一可得的整数性是
   ① Chebyshev 内部整数（2^{r−1}）✗ ② **共谱子族的代数整数**（零测 ✗）
   ⟹ 要过 ARITHMETIC 必须有**外部桥**，而档案里这类桥**已被反复封**（β-blind／local→global／Weil 正性 ✗）✓✓
【支撑结果的真实"产出方向"】 k_min = 5 的作用是**移除**一个曾被寄望的杠杆（**低支撑坍缩**，
   即已 ARCHIVED 的四节点/two-level 希望 ✗），**而不是**提供一个新杠杆 ✓✓
   ⟹ 且它**不排除近碰撞**：`C3899` 的 S1-A 反例本身就有 k = 5（z 互异）✓✗
   ⟹ 故"五层分离"对奇层/谱目标的**直接杠杆价值 ≈ 0**（诚实结论）✓
【期限】 Phase-IV 剩 **28 天**（至 2026-10-20）；本轮三候选全 DEAD ⟹ 无纸面闭合路径可言 ⚠️
【本轮范围的 EXIT-B】 在**本轮三候选范围内**："五层分离目前只产生已知结构、重编码或没有 RH 接口" **成立** ✓✓
   ⚠️ 但**三候选类 ≠ 一切可能** ⟹ **是否正式宣告 EXIT-B（= 五层分离无可晋级产出）由唐先生裁定** ✗✓
```

## §6 下一步选项（**决策权在唐先生**；本档只列不选 ✓）

```
(甲) **换轨**：承认 support 程序的产出 = ① `k_min = 5` 证书（结构事实）② **移除**低支撑杠杆；
     把剩余 Phase-IV 资源交给 Stage Report（15%）与其它主线 ✓ | 本档**弱推荐**（依 §5 ＋ 期限）⚠️
(乙) **纸面收束候选 ②**：❄ 已由本档**自检判定 DEAD**（恒等式全为消去型，无首条非平凡定理）⟹ **不建议**再投入 ✗✓
     （若唐先生仍要求，则唯一可能的残余 = 幂 4 阶差商 `Σλ_j T_r(t_j) = D₄[T_r] (r ≥ 5)` 与约束的**对照**，但无可见出口 ✗）
(丙) **立即 Stage Report / 最终审计**：把 C-380 全链（(2,2,1) ＋ [k ≤ 3] ＋ k=4 两证书、
     S1 判死、LAYER5 门卫 spec、Paper-0 三判死）整理为阶段报告 ✓
(丁) 其它建议（待唐先生提出）✓
```

## §7 【技术词回查】输出（`scripts/tech_word_check.sh`，**先跑后写** ✓✓）

```
技术词 五点插值     命中文件数=0    :: 
技术词 插值缺陷     命中文件数=0    :: 
技术词 跨层差分     命中文件数=0    :: 
技术词 整数化量     命中文件数=0    :: 
技术词 候选杀手榜  命中文件数=0    :: 
技术词 离散消元     命中文件数=0    :: 
⟹ 六项**均本档首次命名**（C-380 轨新增）✓✓
```

## §8 边界（✗✓）

- 本档**零计算**（三候选均为纸面审计）✓；**不**启动任何计算／证书／Scale Gate ✓
- **不**把本档读作 RH 进展；**不**声称"五层分离 ⟹ 新量"已被证实或已被否定（仅三候选被逐项判死）✓✓
- **不**重开 ①③ 所示旧墙（矩空间、Fejér 迁移）✗；**不**重开 S1／(2,2,1)／四节点 ✗
- 判决 ② **仅指"当前形态"**：若出现**逆向**机制（λ 大 ⟹ 分离小 ⟹ 偶层冲突），须**重新登记**并非自动复活 ✓
