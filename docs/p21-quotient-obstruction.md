# P21：Arithmetic Quotient / Range Obstruction——三门测试

> 2026-09-01 · 唐先生 P21 指令 · 唯一目标：arithmetic quotient/range obstruction（非第 21 个标量判据）

## P21 框架（唐先生）
**把 Gate F 硬化成"space-before-zero-set"检验**：Euler/Mellin 数据 → X_arith（定义不用零点）→ RH-independent 结构定理 → ζ 嵌入——三步连续闭合——做不到——判循环
- **商空间缺陷**：Q_arith = H_arith/M_arith——d(f_ρ, M_arith) = 0 ⟺ β = 0（不能来自 Weil）——离轴零点必须在 quotient 中留下不可消除的类
- **反循环测试**：出现 1/ζ、Ξ 零点、Li/Weil 求和、de Branges E#/E、Nyman-Beurling 闭包——Gate F failure
- **三门测试**：F1 空间先于零点 / F2 非显式公式耦合（β 进入 operator/range 几何）/ F3 真正 obstruction

## P21 三门测试结果（最小模型：H=L²(0,∞)——f_ρ = x^{−ρ}）
### 候选 (a)——M = span{x^{−n}: n=1..5}（整数指数——不含零点）
- β=0: d² = 0.851 > 0（**在线 f = x^{−(½+iγ)}——指数非整数——不在 span——F3 ✗**）
- **⚠️ d² 随 β 增大而减小（0.851→0.267——方向反——模型伪影——β→0.5 指数接近整数）**
- **(a) F1✓ F2✓ F3✗**

### 候选 (c)——M = span{f_{½+iγ}: γ∈在线谱}（含在线零点）
- d = 0 ⟺ β=0 ✓（形式）——**但——F1 ✗（M 用在线零点谱——零点信息）**

### 候选 (b)——Nyman-Beurling
- **已知等价——Gate F failure**

## ⭐ P21 三门测试判定（初步）
**三门无全过**：
- (a) F1✓ F2✓ F3✗——(c) F1✗——(b) Gate F failure

**核心两难（深刻——预期确认）**：
- **"d = 0 ⟺ β = 0"需要 M 含在线谱（F1 ✗——零点信息）**
- **"M 不含零点"（F1 ✓）——在线 f 不在 M（d>0 即使 β=0——F3 ✗）**
- **"算术生成元 = 在线谱"（HP——循环——或 Mellin 谱——自适应——F2 ✗）**
- **"space-before-zero-set"（F1）与"d=0⟺β=0"（F3）在自然构造中互斥（除非 HP/循环）**

## P5-P21 统一墙的最精确表述
**所有"排除机制"需要"含 β 的独立约束"——而"含 β"要么通过零点信息（F1 失败——空间用零点）——要么通过显式公式（F2 失败——自适应）——要么需要 HP（循环）——"算术生成元 = 在线谱"是唯一出路（HP 的另一种形式——未解决）**

## 下一步
- (a) 探索"算术生成元 = 在线谱"（HP 的 RH-independent 构造——P14 的核心——未解决——P21 再次确认）
- (b) 接受 P21 初步收口（三门无全过——两难确认——统一墙的最精确表述）
- (c) 唐先生指示
