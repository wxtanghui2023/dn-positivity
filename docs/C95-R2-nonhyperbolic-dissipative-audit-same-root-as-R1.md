已查地图（所查：`p11-zero-flow-lyapunov.md`（**第一轮判词：核心两难 ＋ Flow Budget 素数侧不可定义**）、`CLOSED-ROUTES-MAP.md:228`（**候选形状 D＝形变／连续性型：闭环为循环**，DBN 逐字）与 `:919`（**Mayer／Gauss 转移算子 Fredholm 行列式 ＝ ζ**）、`ATTACK-S2-pre-search-DBN-heatflow-ALREADY-CLOSED-and-my-proposal-illposed.md`、`V123`、`V219`、`V238`／`V239-D`、`L2 载体迁移`（`AUDIT-WALLS-AND-DIFFICULTIES-20260917.md`）、`V199` §3(c)、`C-91`／`C-92`）。**结论**：`R2`（非双曲耗散机制）执行后——**主要候选全部已在档案中被登记并关闭**（`R2-B`／`R2-C`），**无 `R2-A`**；⚠️ 且产出一条 ⭐ **机制级发现**：**`R2` 与 `R1` 同根**——同一"**两面二分**"（Φ／系数侧 ⟹ `β`-盲或坍缩；零点侧 ⟹ 循环）⟹ `C-92` 的两个"合法入口"实为**同一结构** ✓✓

# C-95 · **`R2`：非双曲耗散机制审计（bounded-family audit）**

> **时间**：2026-09-18 13:57 唐先生「继续」＝ 开 `R2`
> **方法（沿用 `R1`）**：先定义边界 → 按**对象**穷举 → 逐门判；预注册 `R2-A/B/C/D` ✓
> **纪律**：**先查地图**（本轮命中率极高：`R2` 残余**已被预注册关闭**）✓

---

## §0 结论（先行）

$$\textbf{(1)}\ \text{按对象枚举 5\ +\ 3（残余）候选} \Longrightarrow \textbf{全部}\ \text{落}\ R2\text{-B}\ \text{或}\ R2\text{-C};\ \ R2\text{-A}=\textbf{0}✓$$
$$\textbf{(2)}\ ⚠️\ \text{其中}\ \textbf{最主要的一项已被档案预注册关闭}：\text{`p11-zero-flow-lyapunov.md` 第一轮判词＝}\boxed{\text{核心两难}}✓✓$$
$$\qquad \text{逐字}：\text{"Sobolev 耗散}\ \textbf{不含}\ \beta\ \text{（无害）——}\textbf{"含}\ \beta\ \text{的耗散"}\ \textbf{两难}\（\Phi\ \text{侧不含——零点侧循环）}\text{"}✓✓$$
$$\textbf{(3)}\ ⭐\ \textbf{机制级发现（本档）}：\ R2\ \text{与}\ R1\ \textbf{同根}——\text{同一}\ \textbf{两面二分}：$$
$$\qquad \text{Φ／系数侧} \Longrightarrow \textbf{`β`-盲} \text{ 或 }\ \textbf{坍缩（强度＝RH）};\qquad \text{零点侧} \Longrightarrow \textbf{循环}✓✓$$
$$\textbf{(4)}\ \text{按}\ R2\text{-D}：\text{"所有非双曲耗散机制"}\ \textbf{无可操作边界} \Longrightarrow \text{本档}\ \textbf{降调为}\ \boxed{\text{bounded-family audit}}✓$$
$$\Longrightarrow\ \boxed{\text{三个叶子现状}：UQRL（四族已关）／D10(a)（0 候选）／W8(b)（`R1`、`R2` 均已实质关闭）}✓$$

---

## §1 `R2` 的边界定义（按你的方法：对象 → 结构公理 → 零点性质）

$$\boxed{R2\ \text{的操作定义}：\{\text{对象}＝\text{算术／动力学对象}\}\times\{\text{公理}＝\textbf{耗散／熵／单调性},\ \textbf{且不要求指数轨道增长}\}\times\{\text{结论}\supseteq\text{零点实部约束}\}}✓$$
$$\qquad ⚠️\ \text{即}\ \text{`V199` §3(c)} \text{的}\ \textbf{排除理由（"需指数级轨道增长；char-0 素数增长是多项式"）之反面}：\text{"}\textbf{不靠指数增长} \text{"的耗散机制}✓$$

## §2 按**对象**逐项审计（5 项 ＋ 3 项残余）

| # | 对象 | 结构公理 | **档案判词（逐字／出处）** | 判定 |
|:--:|:--|:--|:--|:--|
| **①** | **DBN（de Bruijn--Newman）热流**（形变／连续性型）| 热半群耗散（非双曲）| `CLOSED-ROUTES-MAP:228` 逐字：`(D) 形变／连续性型 ⚠️（`**闭环为循环**`）`：`RH ⟺ Λ≤0` ✓、`Λ≥0` 无条件（Rodgers--Tao）、上界 `0.22`（Polymath 15）⟹ **要证 `Λ≤0` 即证 RH ⟹ 循环** ✗ | **`R2-C`**（门②：**循环**）|
| **②** | **Sobolev／entropy／Fisher 耗散泛函**（对 Φ）| 耗散／熵 | `p11` 逐字：**"Sobolev 耗散不含 `β`（无害）——'含 `β` 的耗散' 两难（Φ 侧不含——零点侧循环）"**；且 `D=2∫H_{zz}^2` **不含 `β`**（`D=0 ⟺ H_{zz}≡0` 平凡）✗ | **`R2-C`**（**`β`-盲／循环两难**）|
| **③** | **流上单调性**（P11 框架 C：`d/dλ A(Λ_λ) ≷ 0`）| 路径／Lyapunov 单调 | `p11`：**第一轮收口**（"**两难 ＋ Flow Budget 素数侧不可定义**"）✗ | **`R2-B`／`C`** |
| **④** | **Mayer／Gauss 转移算子**（`det = ζ`）| Birkhoff--Hopf 射影收缩（RPF 正统）| `CLOSED-ROUTES-MAP:919` 逐字：**"Mayer／Gauss 转移算子的 Fredholm 行列式 ＝ ζ"**；⚠️ 但 `V239-D`：临界边界 `Re s=1/2` 是 **Selberg 世界**（modular／Selberg，**非** ζ）⟹ **硬门第一项即命中**；`V219`：**同一个 `1/2` ≠ 同一个零点机制** | **`R2-C`**（门①）|
| **⑤** | **抛物／非超曲内禀流**（horocycle 型载体）| 内禀流＋遍历 | `L2 载体迁移` 否决判据（逐字）：**"承载物皆'几何／群对象＋内禀流'，`Spec ℤ` 无内禀流；否决判据：只给 height／log 尺度或 ζ 只能经显式公式进入 ⟹ 关"** ✗ | **`R2-B`**（**载体否决**）|

$$\textbf{残余候选（本档新列，⚠️ 标 [本档] 推理）}：$$
| # | 对象 | 判定 |
|:--:|:--|:--|
| **⑥** | **非一致双曲＋多项式衰减的转移算子谱隙**（Liverani／Dolgopyat／Young 塔型）| **门③失败**：该族理论只给 **统计型**结论（相关衰减／CLT／大偏差），**不给谱正性** ⟹ `prime-side ⟹ P` 不成立 |
| **⑦** | **曲率／熵凸（Bakry--Émery、log-Sobolev）** | **门①失败**：正性来自**凸性**，需要**算术凸性输入**（缺）⟹ `prime-side ⟹ P` 不成立 |
| **⑧** | **多项式轨道增长的热力学形式**（素数＝周期 `log p`、计数多项式增长）| **门①失败**：多项式增长系统的**动力学 ζ 一般非亚纯** ⟹ **无正性理论**可用 |

$$\textbf{汇总}：\ R2\text{-A}=0;\quad R2\text{-B}=2\ (\text{③⑤});\quad R2\text{-C}=6\ (\text{①②③④⑥⑦⑧});\quad R2\text{-D}=\textbf{适用}✓$$

## §3 ⭐ 机制级发现（本档核心）：**`R1` 与 `R2` 同根**

$$\textbf{R1 的二分（`C-93`）}：\text{Ξ-系数侧 ⟹ 经典等价 ⟹ 坍缩（强度＝RH）};\qquad \gamma\text{-侧 ⟹ }\textbf{`β`-盲}✓$$
$$\textbf{R2 的两难（`p11`，逐字）}：\ \Phi\ \text{侧耗散 ⟹ }\textbf{不含}\ \beta;\qquad \text{零点侧 ⟹ }\textbf{循环}✓✓$$
$$\Longrightarrow\ \boxed{\text{两者是}\ \textbf{同一二分} \text{的两个实例}：\text{（函数／系数）侧 ⟹ 不含}\ \beta\ \text{或坍缩};\ \text{（零点）侧 ⟹ 需零点位置 ⟹ 循环}}✓✓$$
$$\qquad \Longrightarrow\ \text{`C-92` 标为"唯二合法入口"的}\ R1\ \text{与}\ R2\ \textbf{实为同一结构};\ \text{均}\ \textbf{bounded-family 实质关闭}✓✓$$
$$\qquad ⚠️\ \text{这}\ \textbf{回填了}\ \text{`C-92` §4}\ \text{的}\ R2\ \text{项}：\text{"非双曲耗散机制未逐项排除"}\ \text{——}\textbf{现已逐项排除（已枚举类内）}✓$$

## §4 预注册四结果判定

$$R2\text{-A（存活）}：\textbf{0 项}✗;\qquad R2\text{-B（坍缩回已登记关闭）}：\text{③⑤}\ ✓$$
$$R2\text{-C（族外但门失败）}：\text{①②④⑥⑦⑧}\ ✓;\qquad R2\text{-D（空间不可穷举）}：\textbf{适用}✓$$
$$\qquad \Longrightarrow\ \text{按}\ R2\text{-D}\ \text{纪律}：\textbf{不声称}\ \text{"所有非双曲耗散机制已排除"}✗✓$$

## §5 与 `V199` 三门 ＋ `C-91` 硬过滤器对照

| 候选 | 门①(prime-side⟹P) | 门②(非包装) | 门③(可无条件证明) |
|:--:|:--:|:--:|:--:|
| ① DBN | ✗（不含 P）| ✗（循环）| — |
| ② Sobolev 耗散 | ✗（`β`-盲）| ✗（循环）| — |
| ④ Mayer | ✗（Selberg 世界）| — | ✗ |
| ⑥⑦⑧ 残余 | ✗ | — | ✗ |

$$\text{合规（`C-91` §4 硬过滤器）}：\text{本档}\ \textbf{不是} \text{"再换一个定理"},\ \text{而是}\ \textbf{对象级审计};\ \text{且}\ \textbf{先查地图} \text{避免了重复劳动}（\text{②③ 的判词由 `p11` 预注册给出}）✓$$

## §6 【技术词回查】输出（`scripts/tech_word_check.sh`，2026-09-18 13:5x）`[纪律]`（先跑后写）

```
技术词 两面同源   命中文件数=0
技术词 非超曲    命中文件数=0
技术词 R2-A     命中文件数=0
技术词 耗散泛函   命中文件数=0
```
**读数**：四项**均 0 档 ⟹ 本档新增** ✓

## §7 边界

- `[逐字]` §2 ①②③ 的档案判词（`CLOSED-ROUTES-MAP:228`／`p11`）✓；④（`:919` 逐字＋`V239-D`／`V219`）✓；⑤（`L2` 否决判据逐字）✓
- `[本档]` §1 操作定义、§2 残余 ⑥⑦⑧ 的判定、§3 **同根发现**、§4 判定 ✓
- **`R2-A`＝0**，但按 `R2-D`：**不声称族外空间已排除** ✗；**不声称**任何关闭为错 ✗；不证 RH ✗
- **纪律**：先查后判（R-1 ✓，**先跑后写** ✓）；**未用 RH 作推导** ✓；**零数值** ✓

```
⚠️ 唐先生 13:57「继续」⟹ 执行 R2（非双曲耗散机制审计），方法：先定义边界 → 按对象穷举 → 判门；预注册 R2-A/B/C/D
✅ 先查地图（命中率高）：R2 的残余核心**已被预注册关闭**：
   p11-zero-flow-lyapunov.md 第一轮判词逐字："Sobolev 耗散**不含 β**（无害）——'含 β 的耗散'**两难**（Φ 侧不含——零点侧循环）"；
   D = 2∫H_zz² 不含 β（D=0 ⟺ H_zz≡0 平凡）；收口 = "两难 + Flow Budget 素数侧不可定义"
✅ 按对象枚举 5 + 3 候选，逐一判门：
   ① DBN 热流 ⟹ CLOSED-ROUTES-MAP:228 逐字（候选形状 D＝形变/连续性型，**闭环为循环**：RH ⟺ Λ≤0；Λ≥0 无条件(Rodgers–Tao)；
      上界 0.22(Polymath 15) ⟹ 证 Λ≤0 即证 RH ⟹ 循环）⟹ R2-C（门② 循环）
   ② Sobolev/entropy/Fisher 耗散泛函 ⟹ p11 两难（Φ 侧不含 β）⟹ R2-C（β-盲/循环）
   ③ 流上单调性（P11 框架 C：d/dλ A(Λ_λ) ≷ 0）⟹ 第一轮收口 ⟹ R2-B/C
   ④ Mayer/Gauss 转移算子（det = ζ，CLOSED-ROUTES-MAP:919 逐字）⟹ 但 V239-D：临界边界 Re s=1/2 是 **Selberg 世界**（非 ζ）；
      硬门第一项即命中；V219：同一个 1/2 ≠ 同一个零点机制 ⟹ R2-C（门①）
   ⑤ 抛物/非超曲内禀流 ⟹ L2 否决判据逐字（承载物皆几何/群对象＋内禀流；Spec ℤ 无内禀流；ζ 只能经显式公式进入 ⟹ 关）⟹ R2-B
   ⑥⑦⑧ 残余（本档新列）：非一致双曲＋多项式衰减谱隙（只给统计型，无谱正性）/ 曲率·熵凸（需算术凸性输入）/ 多项式轨道增长热力学
      （动力学 ζ 一般非亚纯，无正性理论）⟹ 三者均门①或③失败 ⟹ R2-C
   ⟹ R2-A=0；R2-B=2；R2-C=6；R2-D 适用
⭐ 机制级发现（本档核心）：**R1 与 R2 同根** —— R1 二分（Ξ-系数侧 ⟹ 坍缩；γ-侧 ⟹ β-盲）与 R2 两难（Φ 侧 ⟹ 不含 β；零点侧 ⟹ 循环）
   是**同一二分**的两个实例 ⟹ C-92 标为"唯二合法入口"的 R1、R2 **实为同一结构**，均已 bounded-family 实质关闭
   ⟹ 并回填 C-92 §4 的 R2 项（"非双曲耗散机制未逐项排除" ⟹ 现已逐项排除（已枚举类内））
⚠️ 按 R2-D 降调：本档=bounded-family audit，**不声称**"所有非双曲耗散机制已排除"
✅ 三个叶子现状（更新）：UQRL（四族已关）／D10(a)（0 候选）／W8(b)（**R1、R2 均已实质关闭**）⟹ 剩下 W6 原子墙 + UQRL + D10(a)
✅ 净产出：①R2 操作定义 ✓；②8 候选逐项审计（含档案判词逐字）✓；③⭐同根发现 ✓；④判词与降调 ✓
```
