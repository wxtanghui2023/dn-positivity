# Maślanka 方法——λ_n 路径复活（2026-08-22 22:00）

## 一、重大文献发现（任务 1 的成果）

### Maślanka (2004) 两篇论文
1. **math/0402168**："Effective method of computing Li's coefficients"——**3300 个 λ_n**——"严格增长趋势 + 极小振荡"
2. **math/0406312**："Explicit formula relating Stieltjes constants and Li's numbers"——λ~_n 的显式公式

### 关键分解（Bombieri-Lagarias 隐含——Maślanka 2.4）
```
λ_n = λ⁻_n（趋势）+ λ~_n（振荡）
趋势 λ⁻_n = 1 − (log(4π)+γ)n/2 + Σ_{j=2}^n (−1)^j C(n,j)(1−2^{−j})ζ(j)
振荡 λ~_n = −Σ_{j=1}^n C(n,j)·η_{j−1}
趋势渐近：λ⁻_n ~ ½(1+n·ln n) + c·n, c = ½(γ−1−ln2π) = −1.130330701（Voros）
RH ⟺ λ~_n 被趋势 λ⁻_n 界住（Maślanka 明确——等价问题）
```

### effective method（避开灾难性抵消）
```
公式 (3.8)：c^{(k)}_0 = γ_0^k；c^{(k)}_m = (1/(mγ_0))Σ_{i<m}[km−(k+1)i]γ_{m−i}c^{(k)}_i
公式 (3.10)：η_n = (n+1)Σ_{k=0}^n (−1)^{k+1}/(k+1)·c^{(k+1)}_{n−k}
λ~_n = −Σ_{j=1}^n C(n,j)·η_{j−1}
```

## 二、实现结果（γ 约定修复后）

### 关键：γ 约定
**Maślanka γ_n = (−1)^n/n!·γ_n^标准（mpmath stieltjes）**——γ_1^Maš = +0.0728（标准 −0.0728——符号翻转）

### λ~_n（振荡部分 = 我们的 λ_n^S）
```
n ≤ 80：O(1) 波动（+0.67 到 +1.50——n=80: −1.69——在 ±2 内）✓
n ≥ 90：爆炸（n=100: 2.5e5——n=120: 3.7e9）——effective method 递推累积误差
mpmath stieltjes 本身精确（不同 dps 一致——k ≤ 150）
```

## 三、核心结论

1. **λ~_n（振荡 = 我们的 λ_n^S）O(1) 到 n=80**——正确方法确认（γ 约定修复——λ~_2 = 0.9669 匹配 Maślanka）
2. **Maślanka：O(1) 到 3300**（他的数值——更高精度实现）
3. **λ_n = 趋势（½nlogn）+ λ~_n（O(1)）≥ 0——RH 的强数值基础**
4. **证明 λ~_n = O(1)（或被趋势界住）仍是 RH 等价**（研究级——但数值证据强：80 + 3300）

## 四、战略意义（重大）

**λ_n 路径从"灾难性抵消死路"复活**：
- 我们以为 b_m·m! 组合的爆炸是"历史难题"——**Maślanka 证明有正确工具**（c^{(k)}_m 递推——避开灾难性抵消）
- **λ~_n = O(1)（数值——到 3300）**——**λ_n = 趋势 + O(1) ≥ 0 的强证据**
- **RH ⟺ λ~_n 被趋势界住**（等价问题——清晰——Maślanka 框架）

## 五、待办

1. **复现 Maślanka 的 3300**（需他的实现细节——更高中间精度/符号方法——或 Keiper/Kreminski Stieltjes 常数算法）
2. **深入 λ~_n = O(1) 的结构**（为什么振荡极小——c^{(k)}_m 递推的稳定性暗示组合结构——可能给证明线索）
3. **λ~_n = O(1) 的严格证明**（= RH 等价——研究级——但 Maślanka 的递推结构可能是线索）
