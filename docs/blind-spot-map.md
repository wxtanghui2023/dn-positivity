# 盲区地图评估：P5 后的四条正交路线（①-⑥）——诚实收口

> 2026-09-01 · 唐先生盲区地图 · 最后一次非数值检查

## 盲区地图（唐先生）
```
 ┌─ difference-spectrum rigidity ── X（P5 已封口）
 │
zero configuration ──────┼─ ① arithmetic realizability ───── ?
 │
 ├─ ③ higher-order moments ───── ?
 │
 └─ ⑥ test-function duality ──── ?
Euler product ──────────── ② prime-power discreteness ───── ?
```

## 各路线检查结果

### ① 算术可实现性（第一优先级——唐先生最看重）
- 离轴配置的 ψ' 差分——**泄漏随 δ 单调增大**（δ=0: 0.026 → δ=0.2: 0.303——结构性效应——Λ' 偏离素数幂 support）
- **但——"实际离轴"（RH 假——ζ 的 ψ）算术**——"离轴 ⟹ 不算术"不成立（RH 假反例）
- **撞唯一性/循环墙**（与 P5.8 的 W_δ 测试相同）——除非"独立于 ψ_actual 的算术性判据"——目前没有

### ② 素数幂离散性
- 同上——泄漏随 δ 增大——但——实际离轴算术——循环

### ③ 高阶矩 cumulant
- **需"发明四阶显式公式"**（Weil 是二阶——四阶矩没有对应的零点-素数恒等式——不存在现成）
- 二阶矩看 pair correlation——四阶看 additive energy——**但——"四阶的算术恒等式"不存在**（Euler 积给系数——不给零点高阶矩的约束）

### ④ canonical weights（已执行数值）
- 不同权重（1/ρ, 1/ρ², 1/|ρ|, 1/(ρ log γ), 1/√|ρ|）——F 随 δ 变化 < 0.1%——**都不提供"离轴不可消除"的符号/凸性**
- 规范权重已固定（1/ρ——显式公式）——其他无独立算术来源——回到"换 observable"

### ⑤ 乘法结构
- difference spectrum ≠ full arithmetic spectrum——**但——零点和（ρ₁+...+ρ_k）的算术约束——无现成恒等式**（同③）

### ⑥ test-function family / convex separation
- **需要"F_U 之外的独立算术约束"**——③ 是唯一候选——但——需发明新恒等式——可能不存在
- 若"算术锥" = F_U——W_δ ∈ C（P5.8）——不被分离——循环

## ⭐ 总判断
**盲区地图的路线——大部分撞同样的"唯一性/循环"墙**：
- "实际离轴"（RH 假）算术——是"离轴不可实现"的普遍反例——循环
- 除非——**找到"F_U 之外的独立算术约束"**（③⑥——需发明——可能不存在）
- "F_U 完备性"（P5.8 的 W_δ 通过 15 条）——**支持"无独立约束"**——但——**"完备性"未证明**（P5.8 只测 15 条——不是穷举）

## 真正的新方向（唯一可能绕过循环的）
**寻找"F_U 之外的算术约束"**：
- 如果存在——W_δ 不满足它——**convex separation（⑥）成立**——重大遗漏被找到
- 如果不存在——**F_U 完备——循环确认——Rigidity Gap 定位完成**——项目边界确定

**候选（未穷尽）**：
- 高阶矩的"新恒等式"（③——需发明——Weil 是二阶——四阶可能不存在）
- test-function 族跨 h 的"兼容性条件"（⑥——超越单个 Weil）
- 零点和的"乘法结构恒等式"（⑤——零点和 vs log p 组合）

## 结论
- **P5 系列的 Rigidity Gap 定位**（density vs 差谱最坏相位）**成立且完整**
- **盲区地图的路线**——大部分确认同样的墙——**但——"F_U 完备性"未证明**——是唯一可能的遗漏点
- **如果唐先生认为值得**：检查"F_U 之外是否可能存在独立算术约束"（③⑥——高阶/兼容性）——这是最后一次"非数值"检查的自然终点
