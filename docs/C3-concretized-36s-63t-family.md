已查地图：命中（`C3-check-premise-conflict`（本档更正其判定）／`HM-candidate-scan-round1`）⟹ 引用，不开新案
D0: 本档对象 = `C3` 定界：**rank 4 locally toroidal "almost complete"**，**唯一 "Somewhat open" 族＝`\{\{3,6\}_s,\{6,3\}_t\}`** ⟹ 具体化得手 ＋ `G2.5-R` 初评
D1: 0 （[REVIEW] 轮次：定界，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`C3` 具体化：`\{\{3,6\}_s,\{6,3\}_t\}`**

## §0 更正（照录）

```
**`Hartley` completion ＝ locally projective `\ne` locally toroidal** ✓✓（您的一手核查）
⟹ 我前档"证据 B 否定 C3"的判定**作废** ✓；`C3` **不因证据 B 退出** ✓
【且据您裁定，`Chapter 14` 不作入口】 入口改 **`McMullen–Schulte` §`11E`（`\{3,6,3\}`）／`11F`–`11H`** ＋ `Monson–Schulte` modular linear groups ✓
```

## §1 ⭐ 一手定界（`Schulte` Fields 讲义，逐字）

```
**"Locally Toroidal Regular Polytopes … Rank `n=4`: `\{\{4,4\}_s,\{4,3\}\}`、`\{\{4,4\}_s,\{4,4\}_t\}`、`\{\{6,3\}_s,\{3,r\}\}\ (r=3,4,5)`、`\{\{6,3\}_s,\{3,6\}_t\}`、`\{\{3,6\}_s,\{6,3\}_t\}`，其中 `s=(s,0)` 或 `(s,s)`、`t=(t,0)` 或 `(t,t)`"** ✓✓
**"Somewhat open: `\{\{3,6\}_s,\{6,3\}_t\}`"** ✓✓✓ —— **唯一未闭合族** ✓
**"Locally toroidally polytopes only in ranks `n=4,5,6` … Enumeration complete for `n=5`; almost complete for `n=4`; conjectures for `n=6`"** ✓✓
```

## §2 ⭐⭐ 具体化目标（`G1` 钉死）

```
$$\boxed{(\{3,6\}_s\times\{6,3\}_t,\ (s,t)\ \text{具体取值},\ \text{universal polytope 有限还是无限未知})}$$ ✓✓
【参数形态】 `s\in\{(s,0),(s,s)\}`、`t\in\{(t,0),(t,t)\}` ⟹ **离散参数对，天然有限** ✓
【已知控制结构（照录）】 `1992` `McMullen–Schulte` 原文：`\{6,3,p\}` 与已知 `\{3,6,3\}` 类**由 complex Hermitian form 控制**，并有**正定性与有限性的联系** ✓✓
【⟹ 关键机制】 有限性 `\leftrightarrow` **某 Hermitian 型的正定性**（`\leftrightarrow` 相应 **modular linear group 有限**）⟹ **可化为有限代数判定** ✓✓
```

## §3 `G2.5-R` 初评（四源）

```
**`\mathcal C_{\rm rank}` ✓✓** —— **Hermitian 型正定性／modular linear group 有限性** ＝ 真高阶代数兼容（非普通 SAT）✓✓
**`\mathcal C_{\rm sym}` ✓** —— 型群 `[3,6,3]`／Coxeter 群作用天然 ✓
**`\mathcal C_{\rm cert}` ✓** —— 证书＝**有限群阶／型签名／显式有限商**（短）✓
**`\mathcal C_{\rm prop}` ⚠️ 中** —— C-group relations 的传播强度未评估
【计数】**`3/4`** ✓✓ ⟹ **过 `G2.5-R` 硬门** ✓
【竞争】⚠️ **中**（`Monson–Schulte` modular linear groups 已处理该族的一部分；`McMullen–Schulte` §11E 有专门一节）⟹ **须钉"哪些 `(s,t)` 尚未决定"** ✓
【`N_{\rm eff}`】⚠️ **待估**（形态为"有限群阶计算／型的签名" ⟹ **可能是小量级**，但须具体 `(s,t)` 才能估）✓
```

## §4 状态与下一步

```
$$\boxed{C3:\ \text{具体族已钉住}\ (\{\{3,6\}_s,\{6,3\}_t\})\ \Longrightarrow\ \text{进入 `G1` 通过态；`G2.5-R`=3/4\ \text{过}}$$ ✓✓
【下一动作（不计算）】 逐 `(s,t)` 核：**(i)** 哪些已被 `McMullen–Schulte` §`11E`／`Monson–Schulte` 决定；**(ii)** **剩余未决 `(s,t)` 取最小者**；**(iii)** 写出该 `(s,t)` 的 Hermitian 型／群表示；**(iv)** 估 `N_{\rm eff}` ✓
【您给的退出条件（照录并保留）】 若后续证明 `\{3,6,3\}`／`\{4,4,4\}` **全部有限 universal 情形已分类**，只剩 quotient enumeration 或几何 realization ⟹ **`C3` 立即退出** ✓✓
【`C2` 本轮未做】 预算所限 ⟹ 下一轮与 `C3` 同轮进行 ✓
【⛔ 纪律】 零计算、零实现；`n=38`／`TARGET-L9`／RH 未作筛选依据 ✓
【边界】 §1 三句为**逐字讲义 snippet**（`Schulte`，Fields）；§2 的 `1992` 原文要点为**档级**（未读原文）；§3–§4 为**本档自行评估**；未制造候选／未启动搜索／未碰 RH。
