# 迭代推导（双计数刚性——第 3 轮）：CM 轨道 κ=½ + 第三类确认（2026-09-02 21:55）

## 攻"计数率 = Re s"的候选扫描
- ζ 零点计数 N(T) ~ T logT——指数 1（≠ Re ρ = ½）——不是
- zero-density N(σ,T) ~ T^{c(σ)}——指数依赖 σ——但 σ 是"扫描线"（非零点实部）
- 动力系统熵 = 谱参数——无（Gauss 熵 π²/6log2——非 Re s）
- Hecke 特征值增长——无直接

## ⭐ 发现：CM 轨道增长指数 = ½（真实算术来源——非假设）
CM 点（二次域）的 Hecke 轨道计数：高度 ≤ B 的个数 ~ 类数平均 h(d) ~ √d ~ √(高度) ⟹ **N(B) ~ B^{½}——κ = ½**。
**"a = ½"有真实算术来源（类数平均 √d——二次域类数）——不是 L² 临界/黎曼直觉——是 CM/类数的算术**。

## 但——CM 轨道无"δ 翼"
CM 轨道计数与 Hecke L（CM 的 L）零点——GRH——误差项 = L 零点（β_max——检测——死）。
CM 轨道（κ=½）是"a=½ 的来源"但无 δ 翼——不能直接用。

## β_max/β_min 翼测试
κ₁ = β_max（最大零点实部——"上翼"）——κ₂ = β_min = 1−β_max（FE 配对——"下翼"）——相等 ⟺ RH——但——"相等"不是先验定理（β_max ≠ β_min 通常——如果离轴）——无机制强制——循环——死。

## 第 3 轮产出
1. **CM 轨道 κ=½**（a=½ 的算术来源——类数平均——新）
2. 第三类双计数（含 δ + 非恒等式 + 非 Lefschetz）仍无实例——确认核心障碍
3. "δ 翼"（κ₂ = a−δ）的所有候选仍纠缠 FE 幽灵（1−s）或检测（L 零点）
