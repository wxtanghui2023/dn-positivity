# R3 定向考古——变分定理资产映射 + 反向缺口定位

> 2026-09-07 15:30 · 阶段 B 考古第一轮：把 8/31 变分定理映射到 R3 五类刚性

## 核心资产：变分定理（8/31——无条件——）
**在线 ξ 是 S_proj 的极小元**：
- S_proj(γ_ρ) ≥ S_proj(γ_k)——所有合法配置（任何满足 FE 的 ξ——）
- 等号 ⟺ γ_ρ = γ_k（全在线——）
- 证明链：G = W(a)/2（Weil 素数侧——无条件——）→ Re[f] ≤ R ≤ S_proj（逐项代数——）→ S_proj ≥ 在线值
- **S_proj(γ_k) = W(a)/2——从 Weil 素数侧独立可算！**

## R3 I 类（等号刚性）映射——精确缺口
```
模板：Q ≥ 0 + 独立恒等式 ⟹ Q = 0 ⟹ 等号条件 ⟹ δ = 0
我们的实例：
  Q = S_proj(γ_ρ) − S_proj(γ_k) ≥ 0（变分定理——✓ 无条件——）
  等号 ⟺ 全在线（✓ 极点谱唯一性——）
  缺：Q = 0 的独立论证 = 实际配置 S_proj(γ_ρ) 的独立上界（≤ 在线值——）
```
**缺口精确定位**：变分定理给**下界**（任何合法 ≥ 在线——）——需要**反向上界**（S_proj(γ_ρ) ≤ W(a)/2——）——方向反——变分本身不能给。

8/31 尝试（双变分——最大化——）失败——反向需要"非 Weil 约束"（N(T) 不够——RH 核心困难）。

## 五类刚性的候选来源（补反向——）

### I 等号刚性（主框架——）
反向上界来源候选：
- (a) S_proj 的另一个极值原理（最大化——8/31 失败——）
- (b) 完全不同的算术上界（非 Weil——未找到——）

### III 不确定性原理型（⭐ 最有希望的衔接——）
- S_proj(a) = Σ1/(a²+γ²)²——a 参数——研究其完全单调性/凸性（8/31 D1.4 提过——未深入——）
- 1/(a²+γ²)² 是 a 的完全单调函数（逐项——）——但和（零点密度——）的完全单调性？
- 位置（γ——）-频率（a——）对偶——若 S_proj(a) 有不确定性下界 + Weil 恒等式饱和——可能给反向
- **待考古**：S_proj(a) 的完全单调性/ Bernstein 表示 + a→0, a→∞ 边界

### IV 矩/测度确定性（第二衔接——）
- 1/(a²+γ²)² = Σ(−1)^k (k+1)γ^{2k}/a^{2k+4}（大 a 展开——）
- S_proj(a) 大 a 渐近 = 零点偶矩（Σγ^{2k}——发散——需正规化——）
- Weil 侧（素数——）给 S_proj(a) 全 a——若给"矩的确定性"——反向
- **待考古**：S_proj(a) 的 Weil 全 a 表示 → 完全单调性 → 矩唯一性

### II 自伴/反演（弱形式——）
- ξ 的 FE 已用（Weil——）——额外自伴需 HP（死——）——低优先

### V 反演/唯一延拓
- S_proj(a) 的解析性质（a 复化——）——极点/分支——可能给唯一性
- **待考古**：S_proj(a) 复化后的解析结构（从 Weil 侧——）

## 考古结论（初步——）
**I 类主框架已就位（变分定理——Q ≥ 0 + 等号条件——）——缺反向上界**。
反向上界最可能的来源：**III（S_proj(a) 的完全单调性/不确定性结构——）+ IV（矩确定性——）**——都是"S_proj(a) 作为 a 的函数"的全局性质（8/31 只研究了单点 a——没研究全 a 结构——）。

**关键考古问题**：S_proj(a)（从 Weil 素数侧全 a 可算——无条件——）作为 a 的函数——是否有足够强的结构（完全单调/凸/矩确定——）——能约束实际配置（含离轴——）的 S_proj 值 ≤ 在线值？

## 文件
- docs/r3-saturation-rigidity.md——R3 框架
