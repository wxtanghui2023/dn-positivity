已查地图：未覆盖（关键词: 纤维刚性|精确纤维|符号纤维|零节点|近碰撞稳健）—— 可开档，首行须照抄本行

# **C-380-L2-1**：exact fiber 的**刚性层级判定**（纯代数，零数值）

**唐先生令（2026-09-22 21:45）**：
① 选定 **(a) C-380 → L2** ✓，但加**关键限制（照录）**：
　⛔ 「**不要把"任意符号翻转保持 even constraints"当成 L2 的成功**。那个精确纤维本身**已经被 `M3-α(G)`／paired configuration 捕获**，
　属于**已知的结构墙**。真正要升格的是：**把 S1-A 的"近碰撞"本身变成一个非平凡的 exact fiber**，
　而不是退回到已有的 `t_j ↦ −t_j` 对称」✓✓
② **下一刀只攻这个命题**：`Phi(t_1..t_5) = (Σ_j T_{2r}(t_j))_{r=1..12}`；求
　`X ≠ Y`、`Phi(X)=Phi(Y)`、`X,Y ∈ E_even`、**X,Y 不由逐点 `t_j ↦ ±t_{π(j)}` 得到**，且 `∃` 奇频 `n`：`F_n(X) ≠ F_n(Y)` ✓；
③ **两层分叉（照录）**：`L2_trivial`（`{|t_j|}` 相同 ⟹ **纯符号/排列纤维，已有墙**）**vs** `L2_nontrivial`（`{|t_j|}` **不同**）
　⟹ **只有后者成功，KERNEL-HUNT 才产生新方向** ✓✓；
④ **第一子任务不是找 witness，而是先判定刚性层级**：
　**12 个 even 约束是否已经把五个 `|t_j|` 的多重集唯一固定？** ✓✓
　（**YES** ⟹ 非平凡 L2 **被精确封口**，剩余 sign fiber 成唯一可用 kernel，**然后做 L3**；
　　**NO** ⟹ **这才是真正想要的突破口**——构造非符号型 exact fiber）✓
⑤ **若刚性成立**：**不要再做数值搜索，也不要重新证明 S1**；直接转
　`even-moment fiber → sign fiber → 最小 permutation/sign-invariant odd-sensitive Ψ` ✓；
⑥ 重点检查 `F_{2r+1}`／`F_{2r+1}F_{2s+1}`／`F_{2r+1}^2` 在 sign fiber 上：**① 非常数 ② 对近碰撞 `|t_i|−|t_j| → 0` 不爆炸 ③ 最好二阶稳定** ✓

D0: 本档对象 = **`C-380` even-约束精确纤维的刚性判定（`C-380-L2-1`）**（档案**零覆盖** ⟹ 新档；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算／零数值／零 Lean／未写程序**／未调用 C-380 代码／未做 generator 文献搜索 ✓

---

## §1 `C-380-L2-1`：**刚性证明**（纯代数；只用 `r ≤ 5`）✓✓

```
【档级事实（引）】 `C3898-T2b` 逐字：「结构性事实 ✓：**`T_{2q}(u)` 在 `u` 的首项系数为 `2^(2q-1)`**」✓
　⟹ `T_2q` 为**偶多项式**、次数 `2q`、首项系数 `2^(2q−1)`（＝Chebyshev 首项，一致 ✓）
【换元】 记 `s_j := t_j^2`（`j = 1..5`）⟹ 存在多项式 `P_r` 使
　`F_{2r} = Σ_j T_{2r}(t_j) = Σ_j P_r(s_j)`，且 **`deg P_r = r`、首项系数 `2^(2r−1)`** ✓✓
【三角性】 于是 `F_{2r}` 是 `p_r(s) := Σ_j s_j^r` 的**三角**组合：
　`F_{2r} = 2^(2r−1) · p_r(s) + (仅含 p_1..p_{r−1} 的项)` ✓✓
　⟹ 由 `F_2, F_4, F_6, F_8, F_10`（**只需前 5 条**）可依次解出 `p_1(s), …, p_5(s)` ✓✓
【Newton 恒等式】 5 个幂和 `p_1..p_5` **唯一决定** 初等对称函数 `e_1..e_5` ⟹ 唯一决定多项式 `∏_j (X − s_j)`
　⟹ ⭐ **多重集 `{s_j}` 唯一** ✓✓✓
【刚性命题（本档结论）】
　$$\boxed{\ \Phi(X)=\Phi(Y)\ \Longrightarrow\ \{t_j(X)^2\}=\{t_j(Y)^2\}\ }$$ ✓✓
【推论 1】 因 `{s_j}` 已定，**`r ≥ 6` 的 7 条约束在 `k=5` 层自动满足** ⟹
　**`\Phi` 的水平集 ＝ 符号/排列纤维**（`k=5` 时）✓✓
【推论 2（新，须登记）】 若允许 **`k` 变化**：核中另有 **零节点方向** —— 取 `t_j = 0`（因 `T_n(0)=0` 对一切 `n`）
　⟹ **奇偶频率全不变** ⟹ 它**不满足**"`∃` 奇频 `n`：`F_n(X)≠F_n(Y)`" ⟹ **不作 witness**，
　但它是**真实的核元**（改变支撑计数 `k` 而不改变任何 `F`）⟹ ⭐ 登记为**核的次结构** ✓
```

## §2 判定

```
【`C-380-L2-1` 结论】 ⭐ **刚性成立（YES）** ⟹
　$$\boxed{\text{非平凡 L2 精确纤维（}\{|t_j|\}\text{ 不同）＝ CLOSED}}$$ ✓✓
【因此（依令 ⑤）】 ⛔ **不做数值搜索**；⛔ **不重新证明 S1** ✓
【剩余 kernel（`k=5` 层）】 ＝ **符号/排列纤维**（＋ 允许 `k` 变化时的**零节点方向**）✓
　⟹ 而**符号/排列纤维正是许可墙**（`M3-α(G)`／paired configuration，档案已闭）⟹ 依令 **不得当作 L2 成功** ✓✓
　⟹ **进入 L3** ✓
```

## §3 **L3 设置**（本档给出框架；含两条既有障碍与候选类 ✓）

```
【⭐ 关键推论（本档）】 既然 `\Phi` 已**完全固定** `{s_j} = {t_j^2}` ⟹
　**任何 sign-invariant 的 `\Psi` 都只是 `\Phi` 的函数 ⟹ 不新** ✗✗
　⟹ 唯一可能有新内容的 `\Psi` 必须**符号奇**（即**含奇频**）✓✓ —— 这把 L3 的搜索空间**精确砍到一半**
【障碍 ①（档案已闭）】 **`M3-α(G)`**：存在 paired config（`(0,π/2)^5` 内域）使**全部奇频为零** ⟹
　**任何"仅由奇频构成"的 `\Psi` 在该点为零** ⟹ **不可能一致非零** ✗✓
【障碍 ②（档案）】 **S1-A 近碰撞**（`|δ| ≲ 8.8×10^-20`）＋ **pairwise-cancellation wall** ⟹
　奇频量在近碰撞下**可成对消去** ⟹ 须"**近碰撞稳健**" ✓
【⭐ 候选 `\Psi` 类（本档提出，避开二障碍）】
　$$\boxed{\ \Psi := \frac{\text{符号奇量}}{\text{由 } \Phi \text{ 固定的间隙/导数型数据}}\ }$$
　例：`F_{2r+1}` 以"**最近 `|t_i|` 间隔**"或"`\Phi` 可定的导数型可测量"**归一** ✓✓
　⟹ 该构造**天然近碰撞稳健**（分子与分母**同时消失**），且**不是 `\Phi` 的函数**（分子符号奇 ✓）
【待检三项（照录令 ⑥）】 ① **非常数** ② **近碰撞 `|t_i|−|t_j| → 0` 时不爆炸** ③ **最好二阶稳定** ✓
【⚠️ 诚实标注】 上列候选类为**本档提出之框架**，**未证**任何具体 `\Psi` 满足三项；亦**未**声称它给出新定理 ✓
```

## §4 边界与回查（✗✓）

```
✗ 零计算／零数值／未写程序／未调用 C-380 代码／未做 generator 文献搜索／**未开案** ✓
✗ ⛔ **不**把符号/排列纤维当 L2 成功（依令 ①）✓；⛔ **不**重证 S1、**不**做数值搜索（依令 ⑤）✓
⚠️ §3 候选类为**框架提案**（未证）；§1 刚性证明**只用代数＋Newton**，其正确性依赖 `T_{2q}` 首项系数事实（**档级引用** ✓）
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 纤维刚性预检     命中文件数=0 ⟹ **本档新增** ✓
　技术词 非平凡精确纤维   命中文件数=0 ⟹ **本档新增** ✓
　技术词 符号奇量         命中文件数=0 ⟹ **本档新增** ✓
　技术词 间隙归一         命中文件数=0 ⟹ **本档新增** ✓
　技术词 零节点方向       命中文件数=0 ⟹ **本档新增** ✓
【地图回查】 `纤维刚性|精确纤维|符号纤维|零节点|近碰撞稳健` = **未覆盖** ⟹ 可开档 ✓
【档级引用】 `C3898-T2b`（`T_{2q}(u)` 首项系数 `2^(2q−1)`）✓｜`M3-α(G)` 封闭（paired config 全奇频为零）✓｜
　S1-A 近碰撞 `|δ| ≲ 8.8×10^-20` ＋ pairwise-cancellation wall ✓
