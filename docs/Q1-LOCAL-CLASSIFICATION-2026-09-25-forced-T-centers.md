已查地图：已跑 scripts/prework_map_check.sh K(10,1) Q=1 局部分类 forced T-center ⟹ 执行自 Q1-CENSUS-2026-09-25 档；本档为**纯推导**（唐先生 23:45 "继续压，三结构同时推进，不回到全局矩计数"）；不开新研究方向。
D0: 本档对象 = `Q=1` 分支的 z 局部半径-2 结构分类（既有对象；非新对象）
D1: 0（无新独立自由度；产出为结构锐化、两分支 forced 表、T=1 钉子判定）

# Q1-LOCAL-2026-09-25 · z 的局部完全分类与 forced T-center 判定

## §1 ⭐ 结构锐化：**根本没有环**（对昨日"路径⊔偶环"的收紧）

```
Q=1 ⟹ 唯一 δ=2 点为 z；又 x∈C ⟹ δ(x)=d_C(x) ✓
$$\Longrightarrow\ \forall c\in C:\ \deg_{Q_{10}[C]}(c)=\delta(c)\ \le 2,\ \text{且}\ =2\ \textbf{仅当}\ c=z$$
⟹ 任何偶环要求环上全部顶点 deg=2 ✗（但只有一个 deg-2 顶点）⟹ **无环** ✓✓
⟹ 组分只能是 路径 与 孤立点；且非平凡路径至多一个（需 deg-2 内点 ⟹ 必须含 z ✓）
$$\boxed{\text{分支 A}(z\notin C):\ Q_{10}[C]=K_2\text{(匹配)}\ \sqcup\ K_1\ \text{(孤立)};\quad \text{分支 B}(z\in C):\ Q_{10}[C]=P_3\ \sqcup\ K_2\ \sqcup\ K_1}$$
```

**⟹ 代码层直接推论**（比 census 强）

```
· 除 z 外**每个码字内部度 ≤1** ✓ ⟹ 码字分解为「配对」（相距 1 的两点）与「孤立点」✓
· 分支 B：z 恰有两个码邻居 u,v，且 deg(u)=deg(v)=1 且其唯一邻居就是 z ✓ ⟹ z 是一条 P₃ 的中点 ✓
· 分支 A：三码邻居 c₁,c₂,c₃ 各自 deg≤1，配对伙伴只能在 L₂(z) 中（见 §2）✓
```

## §2 分支 A（z∉C）：局部 forced 表

```
b(z)=3 且 z∉C ⟹ N(z) 中**恰有 3 个码字** c₁=z⊕e_i, c₂=z⊕e_j, c₃=z⊕e_k（i,j,k 互异 ✓）
⟹ 其余 **7 个邻居必非码字** ✓
每个 c_i: deg ≤1 ✓；若配对，伙伴 = z⊕e_i⊕e_l，**l ∉ {i,j,k}** ✓（因若 l∈{j,k} 则伙伴 = z⊕e_i⊕e_j = y_ij ∉C ✓见下）
$$\text{forced T-centers}:\ \textbf{无};\quad \text{forced }b=2\ \text{非码字中心}:\ y_{ij}:=z\oplus e_i\oplus e_j\ (3\ \text{个，互异})$$
    理由: {c_i,c_j} 相距 2 ⟹ 公共中心恰 2 个 = z 与 y_ij ✓；而 y_ij∈C 会给出 b(y_ij)≥1+2=3 ✗ ⟹ **y_ij∉C 且 b(y_ij)=2** ✓
⟹ 每个 y_ij 的**其余 8 个邻居必非码字** ✓ ⟹ L₃(z) 中 forced 非码字 = **22** 个（|A∪B∪C| = 24-3+1 ✓）
⟹ 对每个 l∉{i,j,k}: #{i: l(i)=l} + #{m∉{i,j,k,l}: z⊕e_l⊕e_m∈C} ≤ 2 ✓（因 b(z⊕e_l)≤2 ✓）
```

## §3 分支 B（z∈C）：局部 forced 表

```
δ(z)=d_C(z)=2 ⟹ z 恰有 2 个码邻居 u=z⊕e_i, v=z⊕e_j ⟹ 其余 **8 个邻居必非码字** ✓
deg(u)=deg(v)=1 且其唯一邻居 = z ✓ ⟹ 组分 = P₃: u—z—v ✓（不能再延伸 ✓）
$$\text{forced T-centers}:\ \textbf{无};\quad \text{forced 非码字中心}:\ w:=z\oplus e_i\oplus e_j\ \text{满足}\ b(w)=2\ \checkmark$$
    理由: {u,v} 相距 2 ⟹ 公共中心 = z 与 w ✓；若 w∈C 则 deg(w)≥2 ⟹ δ(w)≥2 ✗（唯一 δ=2 是 z）⟹ **w∉C 且 b(w)=2** ✓
⟹ w 的其余 8 个邻居必非码字 ✓ ⟹ forced 非码字 = 8(L₁) + 1(w) + 8(L₃) = **17** ✓
```

## §4 ⭐ T=1 钉子的判定（本档关键结论）

```
$$T=\sum_x\binom{b(x)}{3}=1\iff \textbf{恰有一个中心 }b\ge3\ \text{（即}\ z\ \text{本身）}\iff \text{全空间只有一个共覆盖三元组}$$
（因 Q=1 时 b≤3 ✓；且 κ∈{0,1} ⟹ 每个共覆盖三元组只有唯一中心 ✓）
⟹ 要出矛盾，需在分支中**强迫出第二个 b≥3 的中心** ✓ —— 但 forced 表给出的是**反面**：
$$\boxed{\text{局部强迫的全部非 z 中心都被**钉在 }b=2\ \text{恰等**上}\ \Longrightarrow\ \textbf{T=1 不产生局部矛盾}\ ✗}$$
（机制：b≤2 的约束方向是「禁止更多码字」，它对被迫中心是**压制型**，不是**产生型** ✓）
```

## §5 结论：三结构同时使用后**仍未获矛盾** ✗（诚实）

```
已同时使用: ① Q=1 profile ✓ ② Q₁₀[C]=匹配∪孤立 / P₃∪匹配∪孤立 ✓ ③ T=1 唯一共覆盖三元组 ✓
仍未闭合 ✓；剩余自由度（本档识别）:
  · 分支 A: 配对伙伴的 l-指标分配（受 per-l ≤2 ✓ 约束）＋ 全局 119 点布局 ✓
  · 分支 B: 其余码字的匹配/孤立分布 ✓
  · 两者共同: **全局计数未用** —— forced 非码字数 22/17 ≪ 905（非码字总数）⟹ 局部 forcing 量级不够 ✗
```

## §6 下一步建议（按信息量排序）

```
① **全局计数联立**：把 forced 非码字集合逐层累积（L₁..L₄），与 census 的 N_{00}+N_{10}=905 对表 ✓
   —— 若某分支迫使非码字 > 905 或码字 > 119，即闭合 ✓
② **小 n 同型探测**：在 n=4..7 的真实最优码上检查是否存在"Q=1 型"profile（用已知精确 K(n,1) ✓）
   —— 提供结构性证据（非证明 ✓）
③ **van Wee 原文核对**（L2/L3 ✓）：确认二阶矩机制是否已被其 excess counting 覆盖 ✗ ✓
```

## §7 边界（诚实标注）

- 全部为**纯推导**（未程序验证 ✗）；本档**未跑程序** ✓
- §1 的"无环"是对昨日"路径⊔偶环"的**收紧**（昨日陈述为真但不够锐 ✓，非错误 ✗）
- **未**排除 Q=1 ✗、**未**排除 119 ✗；forced 计数为局部下界，非全局矛盾 ✓

## 【技术词回查】（定稿前逐字输出）

```
技术词 forced T-center  命中文件数=0    :: 
技术词 局部型表     命中文件数=0    :: 
技术词 缺陷守恒     命中文件数=0    :: 
技术词 P₃-奇点      命中文件数=0    ::
```

- **本档新增**（命中数=0）：`forced T-center`、`局部型表`、`缺陷守恒`、`P₃-奇点`
- **档案已有（引用，不列为提出）**：—
- **通用词（不计）**：—
