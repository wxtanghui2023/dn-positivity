已查地图：命中（`E-35`／`E-29`／`E-25`–`E-34`／`AMEND-6/7/8` 本线自档）⟹ **引用，不开新案** ✓

# **负筛选对（`RUN-1`／`RUN-2`）＋ 递推通道（域内）耗尽 ＋ 下一张卡硬要求**（治理级）

**唐先生裁决（2026-09-23 14:22 ✓✓）**：E-35 认可；**正式把 Card 4 从 `SOURCE-SEARCH` 主流程移出** ✓；关键**不是"0 命中"本身**，而是整个负结果**已满足既定的干净关闭条件** ✓

D0: 本档对象 = **两轮 `RUN` 的负筛选对 ＋ 递推通道域内耗尽 ＋ 下一张卡硬要求（治理级登记）**（引本线自档；**未开 bridge 案** ✓）
D1: 0 （`[REVIEW]` 轮次：治理结项，不主张新自由度 ✓）
FREEZE-ACK: D1=0 ✓
[REVIEW]

---

## §1 **`RUN-2` 证据链闭合（照录 ✓✓）**

```
【最强形式（照录）】 $$\operatorname{rank}_{\mathbb Q}M=J+1,\qquad J=1,2,3,4\qquad\text{对全部 }10\times4=40\ \text{个实例成立}\Longrightarrow\ker_{\mathbb Q}M=\{0\}$$ ⟹
　$$\boxed{\not\exists\,c\in\mathbb Z^{J+1}\setminus\{0\},\quad Mc=0}$$ —— **比预注册判据要求的 `c_J=1` 还强** ✓✓
　（即：不是"没找到整系数解"，而是**连非零有理解都不存在** ✓）
```

## §2 **三次无效执行被严格隔离（质量控制要点，照录 ✓✓）**

```
【实际链条（照录）】 $$\text{错误实现}\to\text{自检失败}\to\text{结果作废}\to\text{修正}\to\text{独立 sanity checks}\to\text{正式 RUN}$$ ✓✓
【独立复核通过项 ✓】 $$\tau(2)=-24;\qquad a_{p^2}=a_p^2-p^{k-1}\ (\Phi_k,\ p=2,3)$$ ⟹ 输入的 Hecke 系数**至少通过关键局部一致性检查** ✓
【⟹ 结论】 E-35 的"0 命中"可与三次错误执行**严格隔离** ✓✓（否则整个 E-35 都会存在**污染风险** ✓）
```

## §3 **应归档的 `SOURCE-SEARCH` 结论（保持域限制 ✓✓）**

```
$$\boxed{\text{跨形式同系数}+\text{真 Hecke 特征值}+\text{两互不相交窗口}\not\Rightarrow\text{低阶线性动力学对象}}$$ ✓
【域限制（照录）】 ⛔ **不是**"模形式系数不存在任何新结构"；✅ 而是
　$$\boxed{\text{在预先固定的 }5\text{ 个形式、}10\text{ 个形式对、两个具体素数窗口、}h\le600,\ J\le4\ \text{与指定共同递推模型中，没有出现该对象}}$$ ✓
```

## §4 ⭐ **两轮 `RUN` 形成"负筛选对"（照录 ✓✓）**

| SOURCE | 独立结构 | 预注册检测 | 结果 | `S4` |
|:--|:--|:--|:--|:--|
| **Card 1** | shifted divisor correlation | 双尺度共同低阶递推 | 无 | **未触发** |
| **Card 4** | cross-form Hecke coefficients | 双窗口共同低阶递推 | 无 | **未触发** |

```
【可安全提炼 ✓】 $$\boxed{\text{"共同低阶线性递推"作为 SOURCE}\to\text{OBJECT 通道目前没有产出}}$$ ✓
【⛔ 不得继续问（尾巴搜索，已禁）】 `J=5` 呢？｜更大 `X` 呢？｜更多形式呢？｜更多窗口呢？ ✓
```

## §5 ⭐⭐ **最重要的治理结论（照录 ✓✓）**

```
【背景】 `Card 1` 与 `Card 4` **不是同一个数学 SOURCE**（`d(n)d(n+h)` 与 `a_p(f)` 来源明显不同）✓；
　但二者**经过同一个筛选器** $$\text{SOURCE}\to\text{精确共同递推}\to\text{新对象}$$ 并**均在 `S4` 前置的确定性负分支退出** ✓
【⟹ 治理规则（照录 ✓✓）】 若下一轮仍选 $$\text{某种序列}\longrightarrow\text{寻找低阶共同递推}$$ ⟹ 将开始发生
　$$\boxed{\text{discovery-channel repetition}}$$ —— **这比单个 Card 关闭更重要** ✓✓
【⟹ 下一张合法卡的硬要求（照录 ✓✓）】 $$\boxed{\text{改变 SOURCE 的数学来源}\ +\ \text{改变 OBJECT 类型}}$$ ⛔ **而不仅是** "换一批序列，再测试共同递推" ✓✓
```

## §6 **账本（照录，含限定 ✓✓）**

```
$$\boxed{\begin{array}{ll}\text{Card 1}&=\mathrm{CLOSED}\ (\mathrm{RUN\!-\!1\ negative})\\\text{Card 2}&=\mathrm{HOLD}\\\text{Card 3}&=\mathrm{CLOSED}\\\text{Card 4}&=\mathrm{CLOSED}\ (\mathrm{RUN\!-\!2\ negative})\\D_{\rm new}&=0\\\text{linear-recurrence SOURCE channel}&=\mathrm{CLOSED/EXHAUSTED\ within\ registered\ cards}\\\text{COMPUTATION}&=\mathrm{LOCKED}\\\text{RH target}&=\mathrm{OPEN}\end{array}}$$
【⚠️ "exhausted"必须限定（照录 ✓✓）】 $$\boxed{\text{已注册的 Card 1 + Card 4 通道}}$$ ⛔ **不是**"所有线性递推 SOURCE 都已数学上排除" ✓✓
【⛔ 下一步不应做（照录 ✓✓）】 **不做 `Card 2` 的 `RUN`** —— 其 `\mathcal R_{\rm dens}` **尚未机械化到足以授权计算** ✓；
　$$\boxed{\text{不要因为两个负结果而降低 Card 2 的 admission 标准}}$$ ✓✓
【当前最干净状态（照录）】 $$\boxed{\text{Card 1 CLOSED}\quad\text{Card 4 CLOSED}\quad\text{Card 2 HOLD}\quad D_{\rm new}=0}$$ ✓
【边界】 ✗ 未计算／⛔ 未降 `Card 2` 标准／⛔ 未开新卡／⛔ 未把"域内耗尽"写成"数学上排除" ✓
```
