已查地图（**先查后写**）：`C-318`（`C-271`→GAP CONFIRMED ✓）、`C-272`（正本：待审箱失败模式审计，零反例 ＋ 粗箱积压 ✓）、`C-273`（v5 设计审计：损失分离 ＋ 机制审计 ✓）、`C-314`（协议 ✓）。回查见 §6 ✓

D0: 本档对象 = **C-319：`C-272` 定向回查（待审箱集合的数学语义）**，**零计算**
D1: 0
FREEZE-ACK: 本档即冻结审计

---

## §0 结论

$$\boxed{\textbf{C-272}\ \to\ \textbf{GAP CONFIRMED}}✓（\text{工程型缺口}✓；\text{原状态保留}✓）$$
$$\textbf{新增关键发现}✓✓：\text{待审箱集合是}\ \textbf{运行相关集（run-dependent）}✓，\textbf{不是} \text{内禀数学对象}✗ \Longrightarrow \textbf{不能}充当机制载体}✗✓（\text{见 §3}✓）$$

## §1 协议（沿用 `C-314` ＋ 本组附加纪律 ✓）

六问（唐先生本档口径 ✓）＋ 三出口 ✓；附加纪律 ✓：**箱集合大而漂亮 ≠ 存在独立机制** ✗✓。

## §2 `C-272` 原结论（✓，正本口径）

- 数据源 ✓：`/tmp/v4_pending.npz`，**完整待审集 670,091 箱** ✓（8M 预算回放时落盘 ✓）
- 方法 ✓：抽样 20,000 箱（3.0% ✓）；`L(B) = max_k sum_j min_{I_j} cos` ✓（可证下界 ✓）；`U_sample(B)` ＝ 中心 ＋ 8 个内点的 `min_theta max_k` ✓
- 结果 ✓✓：`L < 1/2` 占 **70.3%** ✓；`U_sample > 1/2` 占 **100.0%** ✓；**真反例 0** ✓；中位 `L = 0.3827`／`U = 2.1766`／间隙 `1.8319` ✓；**箱宽中位数 ≈ π/12** ✓（粗箱 ✓）
- 边界 ✓：`U_sample` 只是**上估计** ✓ ⟹ **零反例不是证明** ✓✓

## §3 ⭐ 箱集合的数学语义（本档关键 ✓✓）

**① 它是什么对象？** ✓ 每箱 ＝ **乘积区间** `B = prod_{j=1}^{5} I_j ⊂ [0,pi]^5` ✓（M=5 余弦和问题的构型空间子箱 ✓）
**② 生成链是否完整？** ✓ —— B&B 每步**只二分最宽坐标** ✓（无非均匀分裂 ✓），深度上限 80 ✓；落盘为**全部未认证叶** ✓ ⟹ **就该次运行而言完整** ✓
**③ ⭐ 但它依赖运行** ✗✓ —— `pending` 是"**预算终止时的未认证叶集**"✓ ⟹ **随预算变化** ✓✓ ⟹ 它**不是**内禀数学对象 ✗，而是**算法状态** ✓✓
**④ 数学 vs 算法** ✓✓：\text{数学问题} ＝ "\exists B: min_theta max_k F_k(theta) <= 1/2" ✓；**待审箱集合 ≠ 该问题** ✗✓

## §4 六问回答（✓）

**① 代表什么数学对象** ✓：构型空间的未认证乘积子箱 ✓（运行相关 ✓）
**② 生成完整性** ✓：口径完整（最宽维二分 ＋ 深度上限 ✓），无重叠/遗漏（体积自检一致 ✓）；**但**完整性属**该次运行** ✓
**③ 是否有真正的数学 failure 解释** ✗✓：**纯粹算法状态** ✓；可证下界 `L` 与抽样上界 `U` 均**是该箱的估计** ✓，不是禁形 ✓
**④ 是否诱导两个独立传播** ✗：仅**箱二分**一个 ✓
**⑤ 是否有非平凡兼容律** ✗：未见 ✓
**⑥ 出口** ✓✓：②③ 皆无 ⟹ **不进 FSD 入口** ✗ ⟹ **GAP CONFIRMED** ✓（\textbf{不} REOPEN ✗；\textbf{不} CLOSED ✗ —— 证书确实未闭合 ✓）

## §5 补记（协议第④条 ✓）

1. **非 failure-型** ✓✓：与 `C-271`／`C-260`／`C-270` **同型诊断** ✓ ⟹ 七类反例探针**不适用** ✓
2. **零反例的诚实含义** ✓✓：`U_sample` 是**上估计** ✓ ⟹ **745** 箱零反例**只说明本次搜索未找到** ✓；\textbf{不}可升级为数学不可能性 ✗✓
3. **运行相关性的后果** ✓✓：任何"以箱集合为对象"的机制提案 ✗ **先天不合格** ✓（对象不内禀 ✓）
4. **不扩大** ✓：\textbf{不}跑 60M ✗、\textbf{不}动 v4 ✗、\textbf{不}改动 `C-181` 的 GAP-A ✗

## §6 校准表更新 ＋【技术词回查】输出（**先跑后写**✓）

| 回查 | 出口 | 性质 |
|---|---|---|
| `C-270` ✓ | GAP CONFIRMED ✓ | 结构缺口＋封存分支 ✓ |
| `C-246` ✓ | CLOSED ✓ | 后续材料已闭合 ✓ |
| `C-260` ✓ | GAP CONFIRMED ✓ | 非-failure 型 ✓ |
| `C-261` ✓ | CLOSED(reopen)＋资产 ✓ | 结构性 no-bridge ✓ |
| `C-271` ✓ | GAP CONFIRMED ✓ | 工程型 ✓ |
| `C-272` ✓ | **GAP CONFIRMED** ✓ | 工程型 ＋ **运行相关集** ✓✓ |

```
技术词 定向回查     命中文件数=5    :: ./C318-directed-recheck-C271-M5-certificate-backlog-engineering-type.md ./C316-directed-recheck-C260-mainline-candidate-zero-GAP-CONFIRMED.md ./C314-directed-recheck-of-old-GAPs-protocol-and-first-verdicts.md 
技术词 运行相关集  命中文件数=0    :: 
技术词 箱集合语义  命中文件数=0    :: 
```
- 本档新增 ✓：`运行相关集`／`箱集合语义`（依上表判 ✓）；既有引用 ✓：`定向回查`／`待审箱`（`C-314`／`C-272`✓）
- **零计算** ✗；未读 pending ✗；未改他档正本 ✓（仅追加 ✓）；未动 v4 ✗；`C-181` 的 `u<=5` 仍为 **GAP-A** ✓
- **不得**写成：箱集合＝数学对象 ✗；零反例＝不可能 ✗；箱集合诱导机制 ✗
