# （RP_M）线 · 引理与猜想总台账

已查地图（**先查后写**）：`C-143`–`C-176` 全线（`(RP_M)` 窗口余弦和问题）；论文骨架 `papers/rpM-window-cosines/OUTLINE.md`。关键词回查：`总台账`（本档新建命名）。
**本档任务（唐先生 2026-09-19 19:12）**：把所有关联引理／猜想列成一份表，**逐项勾选确认**；无法完成的写明进度与难点，并拆成"可完成／暂时无法完成"；被证伪的也勾选确认。
**图例**：`[x]` 已完成／已确认 ｜ `[ ]` 未完成 ｜ ✅ 严格手证 ｜ 🟨 计算机辅助证书 ｜ 🔶 部分／条件性 ｜ ⛔ 已证伪

FREEZE-ACK: 本档即冻结期内的台账登记（依 `§8.1`；不产新数学结论）

D0: 本档对象 = **（RP_M）线引理／猜想总台账（状态、出处、缺口）** —— 关系 = 登记与勾选，非新机制
D1: 0

---

## A. 已证（严格，手证）—— `[x]` 全部确认

- [x] **A1 引理 C（M=1）**：`|z|=1 ⟹ max_{1≤k≤5} Re z^k ≥ 1/2`；`z=e^{iπ/3}`（60°）取等 ✅
      —— 出处 `E4-ENGINE-2`、`C-152`；初等（区间覆盖）
- [x] **A2 鸽笼定理**：`∀θ ∀N≥1：max_{1≤m≤N} cos(mθ) ≥ cos(2π/(N+1))`，且**最优**（等号在 `θ=2π/(N+1)`）✅
      —— 出处 `C-159`；三行（鸽笼 + cos 单调）
- [x] **A3 覆盖引理（N=3 特例）**：`∀θ ∃m≤3：cos(mθ) ≥ 0` ✅ —— 出处 `C-158`／`C-159`；A2 的直接推论
- [x] **A4 Case A（任意 M）**：若 `#{j：dist(φ_j,πℤ) ≤ δ} ≥ 2M/3 + 1/3`（δ ≤ π/6）则 `f(2) := Σ cos(2φ_j) ≥ 1/2` ✅
      —— 出处 `C-144`；三行
- [x] **A5 周期单调性引理**：若 `φ_1..φ_M` 有公共周期 `P` 且 `⌊5(M+1)/P⌋ ≥ 3`，则单调性步成立（分级）✅
      —— 出处 `C-159`；用 A2
- [x] **A6 近似周期单调性引理**：若 `dist(Pφ_j, 2πℤ) ≤ ε ∀j` 且 `3P ≤ 5(M+1)`，则
      `∀ψ：max_{1≤k≤5(M+1)} [Σ_j cos(kφ_j) + cos(kψ)] ≥ M − (9/2)Mε²` ✅
      —— 出处 `C-172`；三行（覆盖引理 + `cos x ≥ 1−x²/2`）
- [x] **A7 A6 的推论**：若 `ε ≤ √(2/(9M))` 且 `m_M ≤ M−1`，则**单调性步成立** ✅
      —— 出处 `C-172` §3；**数值输入已由 `C-176` 关闭 ⟹ 推论现无条件** ✅✅
- [x] **A8 `m_M ≤ √(2M ln 10M)`（M ≥ 12）** ✅ —— 出处 `C-159`；概率方法（Hoeffding + 并集界；常数已复核）
- [x] **A9 `m_M ≤ M−1`（2 ≤ M ≤ 11）** ✅ —— 出处 `C-176`；显式配置 + **区间算术证书**（余量 0.5–7.8）
- [x] **A10 闭式 `λ_max = 2 − √3`** ✅（解析）—— 出处 `C-173`；两约束（m=1 与 m=2）相切 ⟹ `λ² − 4λ + 1 = 0`；数值吻合 7 位
- [x] **A11 `κ_3(λ) ≥ −1` 对 `λ ≤ 0.2679`** 🟨 —— 出处 `C-173`；1-D 自适应证书（59 区间、0 未决、余量 5.2e-5）

---

## B. 计算机辅助证书 —— `[x]` 完成（**残余缺口标注**）

- [x] **B1 定理 1（M=2）：`max_{k≤10}[cos kφ₁ + cos kφ₂] ≥ 1/2`** 🟨
      —— 三段拼装：局部**纯符号**（`c = 28√1677/559` 精确、半径 `c/32 = 3.6727°`）＋ 远场**格点＋Lipschitz 证书**（N=1500，认证下界 0.56662）＋ 覆盖包含（2.9133° < 3.6727°）✅
      —— 出处 `C-152`／`C-153`／`C-154`；脚本 `scripts/rp2_far_field_certificate.py`
- [x] **B2 (RP_3)**：`m_3 ≥ 1/2` 🟨 —— 自适应分支定界，**17,440 箱、0 未决、余量 0.002005**；双实现逐位一致；误差模型已验（1,569,600 参数，裕度 9007×）
      —— 出处 `C-163`／`C-165`／`C-167`
- [x] **B3 (RP_4)**：`m_4 ≥ 1/2` 🟨 —— 1,036,096 箱、余量 0.000210 —— 出处 `C-164`／`C-167`
- [x] **B4 (RP_5)**：`m_5 ≥ 1/2` 🟨 —— 72,440,000 箱、余量 3.96e-6 —— 出处 `C-168`
- [x] **B5 证书附录（可独立复核）** 🟨 —— 参数、脚本、manifest、哈希 —— 出处 `C-169`
- [x] **B6 `m_M` 上界证书（M ≤ 11）** 🟨 —— **误差全部由区间算术吸收**（唯一非精确对象是 π）—— 出处 `C-176`
- ⚠️ **B 类共同残余缺口**：B2–B4 使用 `SLACK = 1e-12` 作为"浮点误差不超过此值"的**假设**；B1 的远场为格点+Lipschitz（浮点）
      —— 可闭合部分：**M=3**（17,440 箱可行）→ 见 D1a；**M=4**（1.04e6 箱，较慢）→ D1b；**M=5** 超算力 ✗

---

## C. 部分完成／条件性 —— `[~]`

- [~] **C1 (RP_M) for M ≤ 5**：M=1,2 手证（A1/B1）；M=3,4,5 证书（B2–B4）🔶 —— M ≥ 6 未证 → D2a
- [~] **C2 `m_M` 的精确值**：`m_1 = m_2 = 1/2` 精确；`m_3 ≤ 0.777171`、`m_4 ≤ 0.846053`、`m_5 ≤ 0.944329`（认证上界，C-176）+ 下界 `1/2`（B2–B4）⟹ **两侧都有，但未闭合**
- [~] **C3 一维极小极大 `κ_N(λ)`**：`N=3` 完成（闭式 A10 + 证书 A11）🔶 —— 一般 `N` 未做 → D1d
- [~] **C4 阻尼版常数（引理 2.2 原形）**：`M=1` 由 A1 得 `1/2`；`M=2,3,4` 数值 `0.3955 / 0.3731 / 0.3634`；**无严格证明** → D2d
- [~] **C5 证书路线的算力上限**：`M≤5` 可行；`M=6` 估算 ~4.7e9 箱 ⟹ **超出算力**（非数学障碍）；出处 `C-166`／`C-168`
- [~] **C6 论文待办**（骨架 §7）：`M=5` 误差模型细化；区间算术版；`κ_N` 文献核查；英文正文

---

## D. 未完成 —— 拆为"可完成"与"暂时无法完成"

### D-1 可完成（有明确路径、成本已知）

- [ ] **D1a `M=3` 证书的区间算术版**（闭合 B2 的 `SLACK` 假设）
      · 路径：把每箱下界 `LB(B) = max_k Σ_j min_{[a_j,b_j]} cos(kφ_j)` 用 `mpmath.iv` 重算（π 取区间；端点比较用区间；`kφ ≡ π` 判定用区间严格化）
      · 成本：17,440 箱 ⟹ 约 10 s 级，**可行** · 状态：**本轮开始（下一条）**
- [ ] **D1b `M=4` 证书的区间算术版** · 路径同 D1a · 成本 1.04e6 箱（较慢，估计数分钟–数十分钟）· **可行**
- [ ] **D1c `m_3` 的收紧下界**（现仅 `≥ 1/2`；上界 `≤ 0.777171`）· 需加强 B2（例如对极小点邻域做局部符号分析，仿 B1 的三段拼装）
- [ ] **D1d `κ_N(λ)` 的 `N=5` 版本** · 路径同 A11（1-D 证书）· 成本极小 · **可行**
- [ ] **D1e `m_M` 上界证书扩展到 `M = 12..20`** · 路径同 B6 · **可行但冗余**（A8 已覆盖 `M ≥ 12`）

### D-2 暂时无法完成（难点已定位，缺工具或思路）

- [ ] **D2a `(RP_6)` 及一般 M** —— 难点：**需要真正的"多点、分散型"不等式**；
      四条归约式路线已**定量失效**：周期化（`C-174`）、簇并（`C-175`）、计数/测度（`C-144`§4、`C-150`）、二阶矩/聚合（`C-147`、`C-149`）
- [ ] **D2b 单调性 `m_{M+1} ≥ m_M`（一般 M）** —— 与 D2a 等价（配 `m_1 = m_2 = 1/2`）；已证的仅是 A5/A6/A7 的**限制类**
- [ ] **D2c 联合靶子（OP-3）** —— `∀φ,ψ：max_{k≤5(M+1)}[Σ_{j≤M}cos(kφ_j) + cos(kψ)] ≥ m_M`
      实证稳健（余量 0.22–1.10，`C-170`／`C-171`）；**无证明路径**；两条子路线已死（计数；"新增窗口+1"）
- [ ] **D2d 阻尼版最优常数** —— 数值 `0.36–0.40` vs 引理 2.2 的 `1/20`；缺严格论证
- [ ] **D2e `m_M` 精确值（M ≥ 3）** —— 两侧都有界但未闭合；需要新的下界工具

---

## E. 已证伪 —— `[x]` 逐项确认

- [x] **E1「`max_{k≤5(M+1)}A(k) ≥ m_M+1` 对一切配置成立」= 假** ⛔
      —— 反证数据：`m'_M = 0.841 / 0.962 / 0.841 / 0.919`（M=1..4）< `m_M+1` —— 出处 `C-171`
- [x] **E2「无约束的支撑大小刚性（F3）」= 假** ⛔
      —— 反例：窗口隐形测度（均匀测度 `u`，`û(j)=0` 于窗内）使秩 `47 → 256` 而幅谱不变 —— 出处 `C-132`
- [x] **E3「秩刚性 ⟺ 窗口完备（W = N−1）」= 过强** ⛔
      —— 正确判据 = **共轭饱和** `W ≥ N/2`（周期梳构造）—— 出处 `C-139`
- [x] **E4「`τ` 极小 ⟹ 相位整数化」= 量化否证** ⛔
      —— 缺口定理真空（`10^-650 ≫ τ`）—— 出处 `C-124`
- [x] **E5「二阶矩路线可闭合」= 结构失效** ⛔ —— 交叉项 `M²log K` 吃掉全部余量 —— 出处 `C-147`／`C-149`
- [x] **E6「计数/鸽笼路线（需 `n_k ≥ 2M/3+1/3`）」= 结构性不足** ⛔
      —— 平均 `n_k ≈ M/3`；且在 `(60°,90°)` 上交集为空 —— 出处 `C-144` §4、`C-150`
- [x] **E7「簇并归约」= 失效** ⛔ —— 极小点最小间距 `≥ 10°`，而需 `δ ≪ 0.46°` —— 出处 `C-175`
- [x] **E8「`support ≤ 1` 内相位可见性可定秩」= 失效** ⛔ —— 失败在**数据类型层**（模长 ≠ 相位）—— 出处 `C-137`
- [x] **E9「引擎 5：二阶矩族自 `M≈12` 失去分辨率」= 其证明无效** ⛔ —— 出处 `C-143`
- [x] **E10「引擎 4：`M ≤ 11` 二阶矩路线可证」= "可证"部分撤回** ⛔ —— 出处 `C-147`
- [x] **E11「`C-173` §5『只差 0.8%』」= 误诊，撤回** ⛔ —— `q*=19` 不可准入（需 `P ≤ 6`）—— 出处 `C-174`
- [x] **E12「δ 敏感性的 10× 缺口 = 结构性 DEAD」= 降级** ⛔ —— 改为"约 2× short，取决于未证的联合不等式"—— 出处 `C-148`

---

## F. 统计（勾选确认）

```
A 已证（手证）      ：11 项  [x] 全部
B 证书              ：6 项   [x] 全部（残余缺口见 B 末条）
C 部分／条件性      ：6 项   [~]
D-1 可完成（未做）  ：5 项   [ ]   ← D1a 本轮开始
D-2 暂时无法完成    ：5 项   [ ]   （难点均已定位）
E 已证伪            ：12 项  [x] 全部确认
```

## G. 边界

- ⚠️ 本台账为**登记**，不新增数学结论；每项的状态与出处均已给到档（可回溯）
- ⚠️ `🟨` 类（B）为计算机辅助；其严格性来源与残余缺口已逐条标注
- ⚠️ D-2 类的"暂时无法完成"是**当前工具/思路下的判定**，不是"原则上不可能"（可随时重开）
- **未用** RH；**未改**任何原档 ✓
