# 低处特征量分析：A₁ 干扰模型严格化（2026-09-04）

## 状态
唐先生"低处可测可分析可验证——高处不可测但机制一样——低处搞定高处递推"战略——低处特征量（located-expansion A₁/A₂——）分析——干扰模型完美验证。

## 特征量（located-expansion——8/25 工具）
在 z₀ = iγ₀+ε 展开 log ξ(½+z₀+w)——A_k 系数——A₁ = d/ds log ξ(s₀)——s₀ = ½+iγ₀+ε
- 目标零点（γ₀ 处——）主导：A₁ ≈ −1/(β₀−½−ε)——在线（β₀=½）：+1/ε——离轴（δ=β₀−½）：−1/(δ−ε)（符号翻转点 = δ——）
- A₁ 符号翻转扫描（ε——）= β 读数

## 低处数值（前 30 零点——ε=0.01——）
Re(A₁)·ε = 1.0000（4 位——所有——）——Re(A₂)·ε² = −0.5000——在线理论精确匹配
ε 扫描（γ₁——到 0.002——无翻转——）⟹ γ₁ 的 δ < 0.002

## 扩展（114 采样——γ 14→928——）
Re(A₁)·ε 保持 1.0000-1.0005（偏差最大 4.6e-4——γ≈748——）——偏差随 γ 增长（斜率 0.65——）

## ⭐ 干扰模型（在线假设的精确预测——）
A₁ = −Σ_ρ 1/(ρ−½−z₀)——目标（在线）+1/ε——其他：Σ_{j≠0} 1/(ε−i(γ_j−γ₀))
**恒等式：Re(A₁)·ε = 1 + ε²·Σ_{j≠0} 1/(ε² + (γ_j−γ₀)²)**
数值匹配到 ~1e-7（mpmath 精度级——）：
- k=1: 数值 1.000007 vs 预测 1.000005（差 1.3e-6）
- k=500: 1.000211 vs 1.000211（差 9.8e-8）

## 结论
1. 特征量偏差 = 纯干扰（在线——）——无离轴签名
2. 匹配是联合证据（目标 + 干扰零点全在线——γ<1000——δ 约束 ~1e-6 级）
3. 低处可测可验证的完整示范——特征量行为完全被在线理论解释

## 检测极限（诚实——）
- δ 可检测下限：偏离（~δ/ε 当 δ<ε——）需超过干扰 S（~1e-4——）⟹ δ > ~1e-6 可检测——数值匹配到 1e-7 ⟹ δ < ~1e-6（采样零点——）
- 高处（γ 大——）干扰更大（间距小——）——检测极限更差——逐点检测——不自动覆盖

## 文件
- scripts/low_char_quantity.py——low_char_extend.py——interference_model.py
- 本文档——dn-project/docs/low-region-characteristic-quantity.md
