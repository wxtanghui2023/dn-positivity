已查地图：命中（`P1-2-stricter-search-no-math-gap-no-go`（本档废其问法）／`C2-round1-no-open-conflict-located`（本档停其路线）／`RESEARCH-PIVOT-asset-centered-independent-problem-pool`）⟹ 引用，不开新案
D0: 本档对象 = `P1-3` **未解决有限问题池**首建（`8` 题，每题五栏 ＋ 写死成功命题）；**本轮不计算**
D1: 0 （[REVIEW] 轮次：建池，不主张新自由度）
FREEZE-ACK: D1=0
[REVIEW]

# **`P1-3`：未解决有限问题池（首建）**

## §0 路线更正（照录）

```
【废止】 `P1-2` 的"寻可审计的计算论文缺口" ✗（必落入 `已发表→已有人做→` 只能审计 ⟹ `G4` 不成立）✓
【新向】 $$\boxed{\text{资产}\to\text{数学问题空间}\to\textbf{未解决问题}\to\text{构造可攻击的具体问题}}$$ ✓✓
【文献之职责（收窄）】 只需确认三件事：**(1)** 问题存在；**(2)** 该参数**确实未知/未解决**；**(3)** **无后续结果已解决** ✓
【偏好（照录）】 找**小参数未解决**问题，**不找著名未解决猜想** ⟹ 甚至不该先搜"未解决猜想"，而搜**"未解决的具体有限实例"** ✓✓
【`C2`/`L5`/`L1`】 **全部停止** ✓
```

## §1 ⭐ 池（每题五栏：问题／现状／为何有限／资产核心变量／成功命题）

| `#` | 问题（具体有限实例） | 现状（⚠️档级，本轮未核） | 为何有限 | 我方核心资产 | **成功后写死的新定理** |
|:--|:--|:--|:--|:--|:--|
| **`F1`** | **`\mathrm{ES}(7)`**（`7` 点集中必有 `7` 点凸位的临界点数） | `33\le\mathrm{ES}(7)\le113` | 有限点的凸位判定，可 SAT/signotope 编码 | `D`（穷尽＋`DRAT`＋signotope 编码） | **`\mathrm{ES}(7)\le112`**（新上界）或**新下界构造** |
| **`F2`** | **`z(n;2,2)`**（`C_4`-自由二部图最大边数）最小**未知** `n` | 部分 `n` 精确值缺失 | 纯有限极值枚举 | `D`（SAT/精确优化＋对偶证书） | **`z(n)=N`**（上界给构造、下界给穷尽/对偶证书） |
| **`F3`** | **`5`-色单位距离图最小阶**（现记录 `509`） | 记录 `509`，更小未知 | 有限点集图，可穷尽＋对称压缩 | `A`＋`D`（对称性＋穷尽＋证书） | **存在 `K<509` 的实例** 或 **`\le K_0` 阶者不存在** |
| **`F4`** | **覆盖码 `K_q(n,R)` 表缺口**（小参数） | 若干 `(q,n,R)` 上下界未闭 | 有限综合征覆盖 | `A`＋`D` | **`K_q(n,R)=N`** |
| **`F5`** | **差族／循环设计 `(v,k,\lambda)` 表缺口** | 部分参数未决 | 有限群上差集构造 | `A`＋`D` | **存在**或**不存在**（含穷尽证书） |
| **`F6`** | **最小未知阶 Hadamard 矩阵** | ⚠️ 需先确认**当前最小未知阶**（大族已被 `2026` 年 AI 团队关掉） | 有限正交矩阵存在性 | `D`＋`A` | **存在阶 `N` 的 Hadamard 矩阵** 或 **`N` 阶不存在** |
| **`F7`** | **最优 Golomb 尺（`n` 标记）最小未知 `n`** | ⚠️ 已知最优至某 `n`，下一 `n` 未决 | 有限标记集极值 | `D`（穷尽＋并行＋证书） | **最优长度为 `L`**（构造＋穷尽下界证书） |
| **`F8`** | **`R(5,5)`**（`43\le R(5,5)\le46`） | 竞争激烈（算力型团队在推） | 有限 Ramsey 图 | `D`（SAT＋对称＋证书） | **`R(5,5)\le45`** 或 **`R(5,5)\ge44`** |
| **`F9`** | **`16` 线索数独不存在**（归约**完整性**） | 结果已有，**但归约链条的完备性**可查 ⚠️ | 有限 hitting-set | `D`＋`H` | **归约完备性引理** 或 **缺陷** |
| **`F10`** | **某无限猜想的最小参数实例**（如"该猜想在 `n=17` 是否成立"） | 逐题不同 | 参数固定后为有限问题 | `D` | **该参数下成立/不成立** |

## §2 筛选与下一步

```
【排序（本档初判）】 `F9`／`F2`／`F4`／`F5`／`F1`／`F3`／`F7`／`F6`／`F8`／`F10` ✓（**未做可得性核查**）
【三点确认（下一轮唯一动作，**不计算**）】 对**前 3 名**逐题核：**(1)** 问题确实存在且被公认为开放；**(2)** 目标参数**确实未知**；**(3)** **无后续已解决** ⟹ 任一不通过即换 ✓
【`G4` 已写死】**成功命题必须是** `Q(n)=k`／`\exists X`／`\nexists X`／`f(n)\le k`／`f(n)\ge k` 之一；**不得**是"我们验证了某论文" ✓✓
【⛔ 纪律】 本轮**不做计算**；下轮**先做三点确认**再谈攻击；`n=38`／`TARGET-L9`／RH **全面禁作筛选依据** ✓
【边界】 §1 各栏现状为**我方知识（⚠️ 档级，本轮未逐条核查）**；`F6`/`F7` 的"当前未知参数"**必须下轮确认**；池为**本档提出**；未制造候选／未启动搜索／未碰 RH。
