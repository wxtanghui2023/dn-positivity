# E89 · ⭐⭐ **文献找到了：无条件、显式、区间长正比的局部二阶矩定理** ✓✓

> 委托 ✓ 唐先生（20:31–20:40 "继续搜索，看看能不能找到文献内容" ✓）｜执行 ✓ 小灵
> 纪律 ✓ 未用 RH ✓（所引均为**无条件**结果 ✓）；未跑 Lean ✓

---

## 0. 结论（✓ 三条 ✓）

```
⭐⭐ **① 找到了 ✓**：文献中【确有】**无条件、显式常数、主项正比于区间长**的
   **计数误差【增量】局部二阶矩**定理 ✓✓ —— 即 **Tsang《The Distribution of S(t)》Theorem 13.2** ✓
✅ **② 其形态【正是】E88 的结构性结论预测的** ✓✓：主项 $=H\cdot A_k\cdot(\log(2+h\log T))^k$ ✓
   ⟹ **"两个全局二阶矩相减"的人为损失【确实可避免】** ✓✓（文献定理本身即 band-direct ✓）
✅ **③ 常数【显式】** ✓：$A_k=\frac{(2k)!}{2^k\pi^{2k}k!}$ ✓ ⟹ $k=1$：$A_1=\frac{1}{\pi^2}=0.10132$ ✓
⚠️ **剩余一步 ✗**：从"增量二阶矩" $S(t+h)-S(t)$ 到"带内 $\int_I S^2$"的换算 ✓ —— **我尚未完成** ✓（诚实 ✓）
```

## 1. 三条文献（✓ 全部无条件 ✓）

| 来源 | 结果 | 条件 |
|:--|:--|:--|
| **Tsang Thm 13.2**（《The Distribution of S(t)》✓；经 Odlyzko 综述转述 ✓） | $\int_T^{T+H}\{S(t+h)-S(t)\}^{2k}dt=H A_k(\log(2+h\log T))^k+O\bigl(Hc^kk![k^k+(\log(2+h\log T))^{k-1/2}]\bigr)$ ✓，$A_k=\frac{(2k)!}{2^k\pi^{2k}k!}$ ✓ | **无条件** ✓，$T^\eta\le H\le T$，$0<h<1$ ✓ |
| **Fujii**（arXiv:2211.14918v1 §1.3 ✓） | $\int_0^T[S(t+\Delta)-S(t)]^{2k}dt=\frac{(2k)!}{(2\pi)^{2k}k!}T\bigl(2\log(2+\Delta\log T)\bigr)^k+O\bigl(T(\log(2+\Delta\log T))^{k-1/2}\bigr)$ ✓ | **无条件** ✓，$0<\Delta\ll1$ ✓ |
| **Fujii（RH 下）** | 同型，主项 $\frac{(2k)!2^k}{(2\pi)^{2k}k!}T\bigl(\log\log T-\log\lvert\zeta(1+i\Delta)\rvert\bigr)^k$ ✓ | RH ✓，$1\le\Delta\le T$ ✓ |
| **Simonič (arXiv:2010.10675)** ✓ | *"the first **unconditional** results on gaps (large and small) **second and fourth power moments of $S(t+h)-S(t)$**"* ✓ | **无条件** ✓ |

## 2. ⭐ 为什么这与我们的接口**同源**（✓ 对照 E84b／E88 ✓）

```
✅ **对象一致 ✓**：$S(t+h)-S(t)$ ＝ **计数误差在区间 $(t,t+h]$ 上的增量** ✓
   ⟹ 其 $k=1$ 的二阶矩 ＝ 我们要的"局部二阶矩"的**增量版本** ✓（E84b 接口 ✓）
✅ **无条件 ✓**（三条文献均 ✓ —— 不依赖 RH ✓，也**不混用**短区间素数体系 ✓）
✅ **主项 ∝ $H$** ✓✓ —— **E88 从 Dirichlet 多项式对角项推出的结构，文献里已有定理** ✓✓
   ⟹ **人为损失可避免** ✓✓（不必 $M(bT)-M(aT)$ ✓）
✅ **常数显式 ✓**：$A_1=1/\pi^2=0.10132$ ✓ —— **不是"未知的 $O$-常数"** ✓✓
```

## 3. ⚠️ 剩余的一步（✓ 诚实标注 ✓）

$$\text{文献给：}\int_T^{T+H}\bigl[S(t+h)-S(t)\bigr]^2dt\quad\ne\quad\text{我们需的：}\int_I S(t)^2dt\ \ ✗$$

```
⟹ 需一步换算 ✓（两种可能路径 ✓）：
   ① 用增量矩 ＋ 谱关系反推 $\int_I S^2$ ✓（需论证 ✓）
   ② 或改用**增量形式**重做 Abel 分解 ✓（把 $\sum_\gamma e^{i\varphi(\gamma)}$ 写成对 **dS 的增量** 展开 ✓）
⟹ **我尚未完成该步** ✗ —— 但不影响本轮结论 ✓：**文献侧的关键原料【确实存在且显式】✓✓**
```

## 4. 对项目的影响（✓）

```
⭐ **E84b 接口从"需要一条未显式的 $O(T\sqrt{\log\log T})$"**
   **升级为"文献中已有【无条件、显式常数、∝区间长】的增量局部二阶矩"** ✓✓
⟹ **E85 的"常数窗口"焦虑**（$C\le0.05$）**可能根本不需要** ✓：
   若改用**增量形式**（路径 ② ✓），则常数是**文献已给的 $A_k$** ✓，而非需要从 Selberg 证明里抽的 $\cdot$ ✗✓
⟹ ⭐ **故今日的路线可能比 E85 判定时【更宽】** ✓（待换算完成后方可确认 ✓）
```

## 5. 纪律（✓）

```
✓ 所引三条均为**无条件** ✓（RH 版仅列出作对比 ✗）；未混用 Selberg 两套体系 ✓
✓ **未宣称接口已解决** ✗（换算未完成 ✓）；**未宣称 $C\le0.05$** ✗
✓ **未跑 Lean** ✓
```
