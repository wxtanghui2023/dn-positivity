已查地图：命中 48 处 —— 先逐条判 已DEAD/已封/已登记；命中即引既有条目，不得开新案
　判定（**实质命中，全部引用** ✓）：⭐⭐ **`V294-A`**（`CLOSED-ROUTES-MAP` F.5ey）：**有限阶聚合增益分类定理**
　（委托内容**正是本刀**：一次性分类**二体／三体／有限阶**组合 ＋ **增益不变量 `G(K)`**）✓｜**`W6` 原子墙**（L2851：**无条件三阶矩 at `X≍T` ≡ prime-pair ≡ support>1；不可再分**）✓｜
　⭐ **`R-A8.2` 六槽审计总表**（`S2` 非对角 ★ 核心瓶颈；**`S6` 锁死**；`S5` 警示）✓

# **KH-5 / R8-B / S2**：prime-side 二阶算术 → **新的 β-sensitive observable**？

**唐先生令（2026-09-22 23:12）**：
① 选 **(a) 攻 `S2`**；**不换候选、不收 `KH-5`** ✓；理由（照录）：**新候选筛不是找不到对象，而是成功发现候选后，发现它已被 `R8` 完整吸收**；**继续换候选的边际收益低于直接攻已精确定位的 `S2`** ✓✓
② **当前结构（照录）**：$$\boxed{\text{prime-pair second-order object}\ \xrightarrow{\ ?\ }\ \beta\text{-sensitive zero observable}}$$
　（⛔ **而非** `prime pairs → J → explicit formula → zero correlation`；后者已被 `Gate 0` 标记为**带 RH 条件，不能充当新桥** ✓）
③ **第一刀只问**：「能否从**无条件的 prime-side 二阶对象**，直接构造一个 **β-sensitive quantity**，使其信息**不是由 RH／零统计预先输入**，**也不是 explicit-formula 的重新包装**？」✓
　要找的是 $$\boxed{\text{prime arithmetic} \to \mathcal B(H,X) \to \beta\text{-sensitive}}$$，`\mathcal B` 须满足六条：
　**1** 无条件定义｜**2** 不以零点作定义输入｜**3** 不是 `J` 的单纯代数改写｜**4** 不是显式公式换皮｜**5** `H` 或相应参数**真正改变 quantity**｜
　**6** ⭐ 最关键：**改变 `\beta` 时 `\mathcal B` 的数学内容确实改变** ✓✓
④ **三 Gate**：**`S2`-A 找"第三对象"** `\mathcal B(X,H) \not\equiv F(J,X,H)`（若只能得 `J` 的积分／差分／归一化／Mellin-Laplace ⟹ **SURVIVOR-5 FAIL**）｜
　**`S2`-B 检查 `\beta` 是否真进入**（须**结构性** `\mathcal B=\mathcal B(\beta,\theta)`；⛔ 防 `\mathcal B \overset{EF}{\to} \{\gamma\} \overset{数值拟合}{\to} \beta` ＝ **β-extraction**，非 β-native interface）｜
　**`S2`-C ⭐ 方向性测试**：`\beta_1\ne\beta_2 \Rightarrow \mathcal B_{\beta_1}\ne\mathcal B_{\beta_2}`，且区别**来自算术对象本身结构**，非把 `\beta` 当参数塞入定义 ✓✓
⑤ ⛔ **边界**：**不攻两端** —— 不证"prime-pair 猜想 `\Rightarrow` RH"；不证"某 `\lambda>1 \Rightarrow \beta\le 1/2`"（会立刻落回 `R8` 旧结构）✓
⑥ **工作顺序**：**不写程序**，先做**符号准入审计**：`R8 已有对象 → 列出其信息生成闭包 → 寻找闭包外的 prime-native observable` ✓；
　**四坐标**（**仅搜索坐标，非预设 survivor**）：① `H` 的**离散二阶差分/交叉差**｜② 不同 shift `H_1,H_2` 的**交叉相关**｜③ **residue-class/congruence 条件下**的二阶相关｜④ 对 `H` 做算术分解后的**非线性组合**；
　**任一能化回 `J`／显式公式／已有 `R8` carrier ⟹ 直接淘汰** ✓✓

D0: 本档对象 = **`S2`（`R8-B` 瓶颈）的符号准入审计**（引既有 `V294-A`／`W6`／`R-A8.2`；**未开案** ✓）
D1: 0
FREEZE-ACK: **零计算／零数值／未写程序／未做数值实验**／未算零点／未接 ζ／未找 `1/2`／未证 RH ✓

---

## §1 ⭐⭐ **决定性发现**：本刀所要求的"分类"**已在档案完成**（`V294-A`）✓✓

```
【`V294-A`（`CLOSED-ROUTES-MAP` F.5ey；2026-09-16 13:20）逐字】
　委托（唐先生 13:07）：「**不把目标写成"证明无条件三阶矩 X≍T"**；改写为"寻找无条件的聚合恒等式／不等式"；
　**要求一次性分类二体／三体／有限阶组合；给出增益不变量 `G(K)` 与 U1–U5**」✓✓ —— **与本刀委托同构** ✓
　**§2 定理**：**乘积型** ⟹ 因子分解 ⟹ `N_* \in \{\gamma/|c|,\ \gamma^2/(|c|\delta)\}` ⟹ `G \le 0`；
　**耦合型** ⟹ `\Delta\theta = -\Delta\gamma/\gamma^2` ⟹ `N_* \asymp \gamma^2/|\Delta\gamma| \ge \gamma` ⟹ $$\boxed{G(K) \le 0}$$（**等号 ＝ 1-体相位机制**）
　⟹ 「**已有线性窗口 `n \asymp \gamma` 是该类最优**」✓✓
　**两刀形式化**：**二体差相位** `N_* \asymp \gamma^2\log\gamma`（**更差**）；**三体**消 `1/\gamma` ⟹ **窗口推远** ⟹ 「**消掉 `1/\gamma` 不是突破**」✓
　**§3 ⭐**：「**RH 所需增益必须非局部**（**无限阶／算术相关性／非局部正性**）」✓✓
　**§4 三战场**：① **零点侧有限阶已封**｜② **算术侧有增益但已饱和**｜③ 算子论**已有条件 2/3**（不经 RH）⚠️（完整 Weil 正性 ⟺ RH）✓
　**§5 ⭐ 真问题**：「**现有无条件二体／有限体聚合的增益是否严格为 0？**」✓✓
【本刀意义】 用户四坐标**全部是"有限阶"构造**（二阶差分＝二体变换；交叉相关＝三体；congruence 细化＝局部化；`H` 算术分解＝有限组合）
　⟹ **全部落在 `V294-A` 的 `G(K) \le 0` 覆盖域内** ⟹ **"有限阶第三对象"不是"没找到"，而是"已由定理排除增益"** ✓✓✓
```

## §2 闭包与四坐标对照（**逐条 ✓**）

```
【闭包 `\mathcal C(R8)` 定义】 由**两体相关 `\mathfrak C_h(X)`（对一切 `h`；由 `J` 的 `H`-二阶差分反演可得）**生成的：
　有限线性组合／差分／积分／Mellin-Laplace 变换／标准规范化 ✓
```

| # | 搜索坐标 | 是否在闭包内 | 依据 | 判定 |
|:--|:--|:--:|:--|:--:|
| **①** | `H` 的**离散二阶差分** | ✅ **在闭包内** | 三角权 `(H-|h|)` 的二阶差分是 **`h=H` 处的 delta** ⟹ `J` 的二阶差分**就是** `\mathfrak C_H` ⟹ **未产生新对象** ✓ | ⛔ **淘汰** |
| **②** | 不同 shift 的**交叉相关**（三体） | ⚠️ 闭包外，**但等价** | ⭐ `W6` 原子墙逐字：「**无条件三阶矩 at `X\asymp T` ≡ prime-pair ≡ support>1；不可再分**」＋ `V294-A`「**三体消 `1/\gamma` ⟹ 窗口推远 ⟹ 不是突破**」✓ | ⛔ **淘汰（等价于同一墙）** |
| **③** | **congruence／residue-class 细化**二阶相关 | ⚠️ 技术性闭包外（未扭曲 `J` 不含） | ⛔ 其 β-敏感性**只经显式公式**（`G0-3` 已判**带 RH**）⟹ **`S2`-B FAIL**；且 χ-扭曲为**既有机器**（含我方"素数竞赛机制从 `L(s,\chi)` 搬到 ζ"档）✓ | ⛔ **淘汰（吸收＋S2-B 失败）** |
| **④** | `H` 算术分解后的**非线性组合** | ✅ 仍是**闭包数据的函数** | 由同一批 `\mathfrak C_h` 生成 ⟹ 非第三对象 ✓ | ⛔ **淘汰** |

```
【`S2`-A 结论（有限阶层）】 ⛔ **未找到闭包外的 prime-native 第三对象**；且**不是"未找到"而是"已证无增益"**（`V294-A` `G(K)\le0`）✓✓
```

## §3 ⭐⭐ **本刀最重要的重构**：`S2` 的真身**不是"缺对象"，而是"缺主项"** ✓✓

```
【`R-A8.2` 六槽审计总表 逐字】 **`S2` 非对角 ★：有定义、有上界，但缺所需主项** ⟹ **核心瓶颈** ⚠️
　（旁：`S1` 对角 ✓｜`S3` H-均匀性 ⚠️｜`S4` 主项精度 ❌｜`S5` 规范化（**技术层，非新机制**）✓｜`S6` 局部性 🔒）✓
【⭐ 因此令中 `S2`-A 的提法应改写（本档建议 ✓）】
　从「寻找闭包外的第三对象」改写为
　$$\boxed{\text{为【已定义、已有上界】的 }S2\text{ 非对角对象补齐所需主项（且需 }H\text{-uniform）}}$$ ✓✓
　理由：档案的定位是**定量缺口（缺主项）**，不是**对象缺口**；沿"找第三对象"走只会**无限重演 `G(K)\le0` 的有限阶定理** ✓
【⛔ 两条必须继承的档案硬约束】 ① **`S5` 警示**：它决定"你是否正确计算了 `S2`"，**不得让 `S5` 偷偷变成新机制** ✓；
　② **`S6` 锁死**：`\mathfrak S(h)=\prod_p(\cdot)` 只记录**局部可容许性** ⟹ **`R8` 的活口不能是"发现一个更漂亮的奇异级数"**（否则**重落 local→global 旧 NO-GO**）✓✓
```

## §4 ⭐ 唯一正方向（**由档案定理给出** ✓）

```
【`V294-A` §3 逐字】「**RH 所需增益必须非局部**（**无限阶／算术相关性／非局部正性**）」✓✓
【`V294-A` §5 真问题 逐字】「**现有无条件二体／有限体聚合的增益是否严格为 0？**」✓✓
【本档定位】 ⟹ 若 `S2` 有活口，它**必须落在"非局部"类**（无限阶／算术相关性／非局部正性）；
　⛔ 任何**有限阶**构造（含用户四坐标、含所有二次型/二次矩重排）**已被 `G(K) \le 0` 覆盖** ⟹ 不必再审 ✓✓
```

## §5 判定与状态

```
【本刀结果】 **`S2`-A（有限阶层）＝ 已证无增益**（`V294-A`）⟹ **无第三对象**；**`S2` 真身 ＝ 缺主项**（`R-A8.2`）✓
　**`S2`-B／`S2`-C 未进入**（依序门控：A 未产新对象 ⟹ B/C 无对象可测 ✓）
【`KH-5` 状态】 ⟹ $$\boxed{\texttt{KH-5} = \mathrm{OPEN}}$$，**唯一活动支线＝ `KH-5/R8-B/S2`** ✓；**本刀＝窄口径收口（有限阶层）** ✓
【⛔ 不宣布"不存在"】 依"未找到 ≠ 不存在"＋`V294-A` 明确把活口指向**非局部**类 ⟹ **保持 OPEN** ✓✓
【下一刀的**唯一**合法方向（承上）】 攻**非局部**增益：**无限阶聚合／算术相关性／非局部正性**（且守 `S5`／`S6` 两条约束）✓
```

## §6 边界与回查（✗✓）

```
✗ 零计算／零数值／未写程序／未做数值实验／未算零点／未接 ζ／未找 `1/2`／未证 RH／**未开新案** ✓
✗ ⛔ 未攻两端（未证"prime-pair ⟹ RH"／未证"`\lambda>1 \Rightarrow \beta\le1/2`"）✓；⛔ 未让 `S5` 变新机制 ✓；⛔ 未提"更漂亮的奇异级数" ✓
【技术词回查（`scripts/tech_word_check.sh`，**先跑后写** ✓；逐字粘贴 ✓）】
　技术词 信息生成闭包 命中文件数=0 ⟹ **本档新增** ✓｜离散二阶差分=0 ⟹ **新增** ✓｜乘性二阶对象=0 ⟹ **新增** ✓｜闭包外=0 ⟹ **新增** ✓
　技术词 第三对象 命中文件数=2 ⟹ **档案已有（异义）⟹ 不主张** ✓
【地图回查】 命中 **48**；**实质命中 ＝ `V294-A`（有限阶增益分类定理）／`W6` 原子墙／`R-A8.2` 六槽表** ⟹ **引用、不开新案** ✓✓
【档级引用】 `CLOSED-ROUTES-MAP` **F.5ey（`V294-A`）** ／ L2851（`W6` 原子墙）✓｜`R-A8.2-precision-gap-audit.md`（**`S2` 非对角＝核心瓶颈；`S5` 警示；`S6` 锁死**）✓｜`R-A8`（`Gate 0`／`R8-B`）✓｜`KH-5-PRIME-CORRELATION-ADMISSION`（承接）✓
